/**
 * Auto-generated confusion rules (G)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.509Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesG: GrammarRule[] = [
  {
    id: 'confusion_give_five_307',
    name: 'give vs five',
    description: 'Commonly confused words: give and five',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgive\b/gi,
    message: 'Did you mean "five"? "give" ("give") is often confused with "five" ("five").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'five';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gait_gate_327',
    name: 'gait vs gate',
    description: 'Commonly confused words: gait and gate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgait\b/gi,
    message: 'Check: "gait" ("gait") vs "gate" ("gate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_geese_goose_328',
    name: 'geese vs goose',
    description: 'Commonly confused words: geese and goose',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgeese\b/gi,
    message: 'Check: "geese" ("geese") vs "goose" ("goose"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'goose';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gild_gilled_329',
    name: 'gild vs gilled',
    description: 'Commonly confused words: gild and gilled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgild\b/gi,
    message: 'Check: "gild" ("gild") vs "gilled" ("gilled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gilled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gnawed_nod_330',
    name: 'gnawed vs nod',
    description: 'Commonly confused words: gnawed and nod',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgnawed\b/gi,
    message: 'Check: "gnawed" ("gnawed") vs "nod" ("nod"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nod';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gnu_knew_331',
    name: 'gnu vs knew',
    description: 'Commonly confused words: gnu and knew',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgnu\b/gi,
    message: 'Check: "gnu" ("gnu") vs "knew" (past tense of know). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'knew';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_god_good_332',
    name: 'god vs good',
    description: 'Commonly confused words: god and good',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgod\b/gi,
    message: 'Check: "god" ("god") vs "good" ("good"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'good';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gods_goods_333',
    name: 'gods vs goods',
    description: 'Commonly confused words: gods and goods',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgods\b/gi,
    message: 'Check: "gods" ("gods") vs "goods" ("goods"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'goods';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gored_gourd_334',
    name: 'gored vs gourd',
    description: 'Commonly confused words: gored and gourd',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgored\b/gi,
    message: 'Check: "gored" ("gored") vs "gourd" ("gourd"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gourd';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grade_grayed_335',
    name: 'grade vs grayed',
    description: 'Commonly confused words: grade and grayed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrade\b/gi,
    message: 'Check: "grade" ("grade") vs "grayed" ("grayed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grayed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_graft_graphed_336',
    name: 'graft vs graphed',
    description: 'Commonly confused words: graft and graphed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgraft\b/gi,
    message: 'Check: "graft" ("graft") vs "graphed" ("graphed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'graphed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grays_graze_337',
    name: 'grays vs graze',
    description: 'Commonly confused words: grays and graze',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrays\b/gi,
    message: 'Check: "grays" ("grays") vs "graze" ("graze"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'graze';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_guessed_guest_338',
    name: 'guessed vs guest',
    description: 'Commonly confused words: guessed and guest',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bguessed\b/gi,
    message: 'Check: "guessed" ("guessed") vs "guest" ("guest"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'guest';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_guise_guys_339',
    name: 'guise vs guys',
    description: 'Commonly confused words: guise and guys',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bguise\b/gi,
    message: 'Check: "guise" ("guise") vs "guys" ("guys"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'guys';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gabby_tabby_2128',
    name: 'gabby vs tabby',
    description: 'Commonly confused words: gabby and tabby',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgabby\b/gi,
    message: 'Check: "gabby" ("gabby") vs "tabby" ("tabby"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tabby';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gabled_gables_2129',
    name: 'gabled vs gables',
    description: 'Commonly confused words: gabled and gables',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgabled\b/gi,
    message: 'Check: "gabled" ("gabled") vs "gables" ("gables"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gables';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gabled_tabled_2130',
    name: 'gabled vs tabled',
    description: 'Commonly confused words: gabled and tabled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgabled\b/gi,
    message: 'Check: "gabled" ("gabled") vs "tabled" ("tabled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tabled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gabs_gags_2131',
    name: 'gabs vs gags',
    description: 'Commonly confused words: gabs and gags',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgabs\b/gi,
    message: 'Check: "gabs" ("gabs") vs "gags" ("gags"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gags';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gaffed_gaffes_2132',
    name: 'gaffed vs gaffes',
    description: 'Commonly confused words: gaffed and gaffes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgaffed\b/gi,
    message: 'Check: "gaffed" ("gaffed") vs "gaffes" ("gaffes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gaffes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gag_fag_2133',
    name: 'gag vs fag',
    description: 'Commonly confused words: gag and fag',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgag\b/gi,
    message: 'Check: "gag" ("gag") vs "fag" ("fag"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fag';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gags_fags_2134',
    name: 'gags vs fags',
    description: 'Commonly confused words: gags and fags',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgags\b/gi,
    message: 'Check: "gags" ("gags") vs "fags" ("fags"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fags';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gags_gaga_2135',
    name: 'gags vs gaga',
    description: 'Commonly confused words: gags and gaga',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgags\b/gi,
    message: 'Check: "gags" ("gags") vs "gaga" ("gaga"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gaga';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gales_bales_2136',
    name: 'gales vs bales',
    description: 'Commonly confused words: gales and bales',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgales\b/gi,
    message: 'Check: "gales" ("gales") vs "bales" ("bales"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bales';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_galled_balled_2137',
    name: 'galled vs balled',
    description: 'Commonly confused words: galled and balled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgalled\b/gi,
    message: 'Check: "galled" ("galled") vs "balled" ("balled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'balled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_galling_balling_2138',
    name: 'galling vs balling',
    description: 'Commonly confused words: galling and balling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgalling\b/gi,
    message: 'Check: "galling" ("galling") vs "balling" ("balling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'balling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gallows_hallows_2139',
    name: 'gallows vs hallows',
    description: 'Commonly confused words: gallows and hallows',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgallows\b/gi,
    message: 'Check: "gallows" ("gallows") vs "hallows" ("hallows"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hallows';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_galvanized_galvanizes_2140',
    name: 'galvanized vs galvanizes',
    description: 'Commonly confused words: galvanized and galvanizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgalvanized\b/gi,
    message: 'Check: "galvanized" ("galvanized") vs "galvanizes" ("galvanizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'galvanizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gambled_gambles_2141',
    name: 'gambled vs gambles',
    description: 'Commonly confused words: gambled and gambles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgambled\b/gi,
    message: 'Check: "gambled" ("gambled") vs "gambles" ("gambles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gambles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gaming_taming_2142',
    name: 'gaming vs taming',
    description: 'Commonly confused words: gaming and taming',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgaming\b/gi,
    message: 'Check: "gaming" ("gaming") vs "taming" ("taming"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'taming';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ganging_banging_2143',
    name: 'ganging vs banging',
    description: 'Commonly confused words: ganging and banging',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bganging\b/gi,
    message: 'Check: "ganging" ("ganging") vs "banging" ("banging"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'banging';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gangling_tangling_2144',
    name: 'gangling vs tangling',
    description: 'Commonly confused words: gangling and tangling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgangling\b/gi,
    message: 'Check: "gangling" ("gangling") vs "tangling" ("tangling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tangling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gangs_bangs_2145',
    name: 'gangs vs bangs',
    description: 'Commonly confused words: gangs and bangs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgangs\b/gi,
    message: 'Check: "gangs" ("gangs") vs "bangs" ("bangs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bangs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gangs_hangs_2146',
    name: 'gangs vs hangs',
    description: 'Commonly confused words: gangs and hangs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgangs\b/gi,
    message: 'Check: "gangs" ("gangs") vs "hangs" ("hangs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hangs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gaping_taping_2147',
    name: 'gaping vs taping',
    description: 'Commonly confused words: gaping and taping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgaping\b/gi,
    message: 'Check: "gaping" ("gaping") vs "taping" ("taping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'taping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_garaged_garages_2148',
    name: 'garaged vs garages',
    description: 'Commonly confused words: garaged and garages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgaraged\b/gi,
    message: 'Check: "garaged" ("garaged") vs "garages" ("garages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'garages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_garb_barb_2149',
    name: 'garb vs barb',
    description: 'Commonly confused words: garb and barb',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgarb\b/gi,
    message: 'Check: "garb" ("garb") vs "barb" ("barb"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'barb';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_garbed_barbed_2150',
    name: 'garbed vs barbed',
    description: 'Commonly confused words: garbed and barbed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgarbed\b/gi,
    message: 'Check: "garbed" ("garbed") vs "barbed" ("barbed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'barbed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_garbled_garbles_2151',
    name: 'garbled vs garbles',
    description: 'Commonly confused words: garbled and garbles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgarbled\b/gi,
    message: 'Check: "garbled" ("garbled") vs "garbles" ("garbles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'garbles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_garbled_gargled_2152',
    name: 'garbled vs gargled',
    description: 'Commonly confused words: garbled and gargled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgarbled\b/gi,
    message: 'Check: "garbled" ("garbled") vs "gargled" ("gargled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gargled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_garbling_gargling_2153',
    name: 'garbling vs gargling',
    description: 'Commonly confused words: garbling and gargling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgarbling\b/gi,
    message: 'Check: "garbling" ("garbling") vs "gargling" ("gargling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gargling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gardeners_hardeners_2154',
    name: 'gardeners vs hardeners',
    description: 'Commonly confused words: gardeners and hardeners',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgardeners\b/gi,
    message: 'Check: "gardeners" ("gardeners") vs "hardeners" ("hardeners"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hardeners';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gardening_hardening_2155',
    name: 'gardening vs hardening',
    description: 'Commonly confused words: gardening and hardening',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgardening\b/gi,
    message: 'Check: "gardening" ("gardening") vs "hardening" ("hardening"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hardening';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_garners_garnets_2156',
    name: 'garners vs garnets',
    description: 'Commonly confused words: garners and garnets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgarners\b/gi,
    message: 'Check: "garners" ("garners") vs "garnets" ("garnets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'garnets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_garnished_garnishee_2157',
    name: 'garnished vs garnishee',
    description: 'Commonly confused words: garnished and garnishee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgarnished\b/gi,
    message: 'Check: "garnished" ("garnished") vs "garnishee" ("garnishee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'garnishee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_garnished_garnishes_2158',
    name: 'garnished vs garnishes',
    description: 'Commonly confused words: garnished and garnishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgarnished\b/gi,
    message: 'Check: "garnished" ("garnished") vs "garnishes" ("garnishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'garnishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_garnished_tarnished_2159',
    name: 'garnished vs tarnished',
    description: 'Commonly confused words: garnished and tarnished',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgarnished\b/gi,
    message: 'Check: "garnished" ("garnished") vs "tarnished" ("tarnished"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tarnished';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_garnishes_tarnishes_2160',
    name: 'garnishes vs tarnishes',
    description: 'Commonly confused words: garnishes and tarnishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgarnishes\b/gi,
    message: 'Check: "garnishes" ("garnishes") vs "tarnishes" ("tarnishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tarnishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_garnishing_tarnishing_2161',
    name: 'garnishing vs tarnishing',
    description: 'Commonly confused words: garnishing and tarnishing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgarnishing\b/gi,
    message: 'Check: "garnishing" ("garnishing") vs "tarnishing" ("tarnishing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tarnishing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_garnish_tarnish_2162',
    name: 'garnish vs tarnish',
    description: 'Commonly confused words: garnish and tarnish',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgarnish\b/gi,
    message: 'Check: "garnish" ("garnish") vs "tarnish" ("tarnish"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tarnish';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_garter_barter_2163',
    name: 'garter vs barter',
    description: 'Commonly confused words: garter and barter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgarter\b/gi,
    message: 'Check: "garter" ("garter") vs "barter" ("barter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'barter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gases_bases_2164',
    name: 'gases vs bases',
    description: 'Commonly confused words: gases and bases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgases\b/gi,
    message: 'Check: "gases" ("gases") vs "bases" ("bases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gashed_gashes_2165',
    name: 'gashed vs gashes',
    description: 'Commonly confused words: gashed and gashes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgashed\b/gi,
    message: 'Check: "gashed" ("gashed") vs "gashes" ("gashes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gashes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gashing_bashing_2166',
    name: 'gashing vs bashing',
    description: 'Commonly confused words: gashing and bashing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgashing\b/gi,
    message: 'Check: "gashing" ("gashing") vs "bashing" ("bashing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bashing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gasp_hasp_2167',
    name: 'gasp vs hasp',
    description: 'Commonly confused words: gasp and hasp',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgasp\b/gi,
    message: 'Check: "gasp" ("gasp") vs "hasp" ("hasp"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hasp';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gated_bated_2168',
    name: 'gated vs bated',
    description: 'Commonly confused words: gated and bated',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgated\b/gi,
    message: 'Check: "gated" ("gated") vs "bated" ("bated"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bated';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gated_fated_2169',
    name: 'gated vs fated',
    description: 'Commonly confused words: gated and fated',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgated\b/gi,
    message: 'Check: "gated" ("gated") vs "fated" ("fated"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fated';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gated_gates_2170',
    name: 'gated vs gates',
    description: 'Commonly confused words: gated and gates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgated\b/gi,
    message: 'Check: "gated" ("gated") vs "gates" ("gates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gates_bates_2171',
    name: 'gates vs bates',
    description: 'Commonly confused words: gates and bates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgates\b/gi,
    message: 'Check: "gates" ("gates") vs "bates" ("bates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gather_bather_2172',
    name: 'gather vs bather',
    description: 'Commonly confused words: gather and bather',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgather\b/gi,
    message: 'Check: "gather" ("gather") vs "bather" ("bather"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bather';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gather_father_2173',
    name: 'gather vs father',
    description: 'Commonly confused words: gather and father',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgather\b/gi,
    message: 'Check: "gather" ("gather") vs "father" ("father"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'father';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gating_bating_2174',
    name: 'gating vs bating',
    description: 'Commonly confused words: gating and bating',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgating\b/gi,
    message: 'Check: "gating" ("gating") vs "bating" ("bating"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bating';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gauged_gauges_2175',
    name: 'gauged vs gauges',
    description: 'Commonly confused words: gauged and gauges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgauged\b/gi,
    message: 'Check: "gauged" ("gauged") vs "gauges" ("gauges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gauges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gaunt_haunt_2176',
    name: 'gaunt vs haunt',
    description: 'Commonly confused words: gaunt and haunt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgaunt\b/gi,
    message: 'Check: "gaunt" ("gaunt") vs "haunt" ("haunt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'haunt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gaunt_taunt_2177',
    name: 'gaunt vs taunt',
    description: 'Commonly confused words: gaunt and taunt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgaunt\b/gi,
    message: 'Check: "gaunt" ("gaunt") vs "taunt" ("taunt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'taunt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gays_bays_2178',
    name: 'gays vs bays',
    description: 'Commonly confused words: gays and bays',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgays\b/gi,
    message: 'Check: "gays" ("gays") vs "bays" ("bays"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bays';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gazed_fazed_2179',
    name: 'gazed vs fazed',
    description: 'Commonly confused words: gazed and fazed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgazed\b/gi,
    message: 'Check: "gazed" ("gazed") vs "fazed" ("fazed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fazed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gazetted_gazettes_2180',
    name: 'gazetted vs gazettes',
    description: 'Commonly confused words: gazetted and gazettes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgazetted\b/gi,
    message: 'Check: "gazetted" ("gazetted") vs "gazettes" ("gazettes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gazettes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_geared_teared_2181',
    name: 'geared vs teared',
    description: 'Commonly confused words: geared and teared',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgeared\b/gi,
    message: 'Check: "geared" ("geared") vs "teared" ("teared"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'teared';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gear_hear_2182',
    name: 'gear vs hear',
    description: 'Commonly confused words: gear and hear',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgear\b/gi,
    message: 'Check: "gear" ("gear") vs "hear" (to perceive sound). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hear';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gears_hears_2183',
    name: 'gears vs hears',
    description: 'Commonly confused words: gears and hears',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgears\b/gi,
    message: 'Check: "gears" ("gears") vs "hears" ("hears"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hears';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gees_fees_2184',
    name: 'gees vs fees',
    description: 'Commonly confused words: gees and fees',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgees\b/gi,
    message: 'Check: "gees" ("gees") vs "fees" ("fees"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fees';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gelled_belled_2185',
    name: 'gelled vs belled',
    description: 'Commonly confused words: gelled and belled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgelled\b/gi,
    message: 'Check: "gelled" ("gelled") vs "belled" ("belled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'belled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gems_gens_2186',
    name: 'gems vs gens',
    description: 'Commonly confused words: gems and gens',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgems\b/gi,
    message: 'Check: "gems" ("gems") vs "gens" ("gens"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gens';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gender_bender_2187',
    name: 'gender vs bender',
    description: 'Commonly confused words: gender and bender',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgender\b/gi,
    message: 'Check: "gender" ("gender") vs "bender" ("bender"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bender';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gendered_tendered_2188',
    name: 'gendered vs tendered',
    description: 'Commonly confused words: gendered and tendered',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgendered\b/gi,
    message: 'Check: "gendered" ("gendered") vs "tendered" ("tendered"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tendered';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_genders_tenders_2189',
    name: 'genders vs tenders',
    description: 'Commonly confused words: genders and tenders',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgenders\b/gi,
    message: 'Check: "genders" ("genders") vs "tenders" ("tenders"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tenders';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_generated_generates_2190',
    name: 'generated vs generates',
    description: 'Commonly confused words: generated and generates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgenerated\b/gi,
    message: 'Check: "generated" ("generated") vs "generates" ("generates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'generates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_generically_genetically_2191',
    name: 'generically vs genetically',
    description: 'Commonly confused words: generically and genetically',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgenerically\b/gi,
    message: 'Check: "generically" ("generically") vs "genetically" ("genetically"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'genetically';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gens_fens_2192',
    name: 'gens vs fens',
    description: 'Commonly confused words: gens and fens',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgens\b/gi,
    message: 'Check: "gens" ("gens") vs "fens" ("fens"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fens';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_germinated_germinates_2193',
    name: 'germinated vs germinates',
    description: 'Commonly confused words: germinated and germinates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgerminated\b/gi,
    message: 'Check: "germinated" ("germinated") vs "germinates" ("germinates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'germinates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_germinates_terminates_2194',
    name: 'germinates vs terminates',
    description: 'Commonly confused words: germinates and terminates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgerminates\b/gi,
    message: 'Check: "germinates" ("germinates") vs "terminates" ("terminates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'terminates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_germinate_terminate_2195',
    name: 'germinate vs terminate',
    description: 'Commonly confused words: germinate and terminate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgerminate\b/gi,
    message: 'Check: "germinate" ("germinate") vs "terminate" ("terminate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'terminate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_germination_termination_2196',
    name: 'germination vs termination',
    description: 'Commonly confused words: germination and termination',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgermination\b/gi,
    message: 'Check: "germination" ("germination") vs "termination" ("termination"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'termination';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gestate_testate_2197',
    name: 'gestate vs testate',
    description: 'Commonly confused words: gestate and testate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgestate\b/gi,
    message: 'Check: "gestate" ("gestate") vs "testate" ("testate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'testate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gestured_gestures_2198',
    name: 'gestured vs gestures',
    description: 'Commonly confused words: gestured and gestures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgestured\b/gi,
    message: 'Check: "gestured" ("gestured") vs "gestures" ("gestures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gestures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ghats_gnats_2199',
    name: 'ghats vs gnats',
    description: 'Commonly confused words: ghats and gnats',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bghats\b/gi,
    message: 'Check: "ghats" ("ghats") vs "gnats" ("gnats"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gnats';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gibber_gibbet_2200',
    name: 'gibber vs gibbet',
    description: 'Commonly confused words: gibber and gibbet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgibber\b/gi,
    message: 'Check: "gibber" ("gibber") vs "gibbet" ("gibbet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gibbet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gibbering_gibbeting_2201',
    name: 'gibbering vs gibbeting',
    description: 'Commonly confused words: gibbering and gibbeting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgibbering\b/gi,
    message: 'Check: "gibbering" ("gibbering") vs "gibbeting" ("gibbeting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gibbeting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gibes_gives_2202',
    name: 'gibes vs gives',
    description: 'Commonly confused words: gibes and gives',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgibes\b/gi,
    message: 'Check: "gibes" ("gibes") vs "gives" ("gives"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gives';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gifts_girts_2203',
    name: 'gifts vs girts',
    description: 'Commonly confused words: gifts and girts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgifts\b/gi,
    message: 'Check: "gifts" ("gifts") vs "girts" ("girts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'girts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gillies_gullies_2204',
    name: 'gillies vs gullies',
    description: 'Commonly confused words: gillies and gullies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgillies\b/gi,
    message: 'Check: "gillies" ("gillies") vs "gullies" ("gullies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gullies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gills_fills_2205',
    name: 'gills vs fills',
    description: 'Commonly confused words: gills and fills',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgills\b/gi,
    message: 'Check: "gills" ("gills") vs "fills" ("fills"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fills';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ginned_gunned_2206',
    name: 'ginned vs gunned',
    description: 'Commonly confused words: ginned and gunned',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bginned\b/gi,
    message: 'Check: "ginned" ("ginned") vs "gunned" ("gunned"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gunned';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ginned_tinned_2207',
    name: 'ginned vs tinned',
    description: 'Commonly confused words: ginned and tinned',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bginned\b/gi,
    message: 'Check: "ginned" ("ginned") vs "tinned" ("tinned"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tinned';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ginning_gunning_2208',
    name: 'ginning vs gunning',
    description: 'Commonly confused words: ginning and gunning',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bginning\b/gi,
    message: 'Check: "ginning" ("ginning") vs "gunning" ("gunning"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gunning';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ginning_tinning_2209',
    name: 'ginning vs tinning',
    description: 'Commonly confused words: ginning and tinning',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bginning\b/gi,
    message: 'Check: "ginning" ("ginning") vs "tinning" ("tinning"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tinning';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gins_bins_2210',
    name: 'gins vs bins',
    description: 'Commonly confused words: gins and bins',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgins\b/gi,
    message: 'Check: "gins" ("gins") vs "bins" ("bins"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bins';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gins_fins_2211',
    name: 'gins vs fins',
    description: 'Commonly confused words: gins and fins',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgins\b/gi,
    message: 'Check: "gins" ("gins") vs "fins" ("fins"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fins';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_girdled_girdles_2212',
    name: 'girdled vs girdles',
    description: 'Commonly confused words: girdled and girdles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgirdled\b/gi,
    message: 'Check: "girdled" ("girdled") vs "girdles" ("girdles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'girdles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gist_gust_2213',
    name: 'gist vs gust',
    description: 'Commonly confused words: gist and gust',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgist\b/gi,
    message: 'Check: "gist" ("gist") vs "gust" ("gust"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gust';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gites_bites_2214',
    name: 'gites vs bites',
    description: 'Commonly confused words: gites and bites',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgites\b/gi,
    message: 'Check: "gites" ("gites") vs "bites" ("bites"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bites';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_givers_fivers_2215',
    name: 'givers vs fivers',
    description: 'Commonly confused words: givers and fivers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgivers\b/gi,
    message: 'Check: "givers" ("givers") vs "fivers" ("fivers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fivers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glace_glade_2216',
    name: 'glace vs glade',
    description: 'Commonly confused words: glace and glade',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglace\b/gi,
    message: 'Check: "glace" ("glace") vs "glade" ("glade"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glade';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glaces_glades_2217',
    name: 'glaces vs glades',
    description: 'Commonly confused words: glaces and glades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglaces\b/gi,
    message: 'Check: "glaces" ("glaces") vs "glades" ("glades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glad_goad_2218',
    name: 'glad vs goad',
    description: 'Commonly confused words: glad and goad',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglad\b/gi,
    message: 'Check: "glad" ("glad") vs "goad" ("goad"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'goad';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glads_glass_2219',
    name: 'glads vs glass',
    description: 'Commonly confused words: glads and glass',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglads\b/gi,
    message: 'Check: "glads" ("glads") vs "glass" ("glass"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glass';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glads_goads_2220',
    name: 'glads vs goads',
    description: 'Commonly confused words: glads and goads',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglads\b/gi,
    message: 'Check: "glads" ("glads") vs "goads" ("goads"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'goads';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glamorized_glamorizes_2221',
    name: 'glamorized vs glamorizes',
    description: 'Commonly confused words: glamorized and glamorizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglamorized\b/gi,
    message: 'Check: "glamorized" ("glamorized") vs "glamorizes" ("glamorizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glamorizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glans_flans_2222',
    name: 'glans vs flans',
    description: 'Commonly confused words: glans and flans',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglans\b/gi,
    message: 'Check: "glans" ("glans") vs "flans" ("flans"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flans';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glare_blare_2223',
    name: 'glare vs blare',
    description: 'Commonly confused words: glare and blare',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglare\b/gi,
    message: 'Check: "glare" ("glare") vs "blare" ("blare"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'blare';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glassed_glasses_2224',
    name: 'glassed vs glasses',
    description: 'Commonly confused words: glassed and glasses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglassed\b/gi,
    message: 'Check: "glassed" ("glassed") vs "glasses" ("glasses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glasses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glazed_glazes_2225',
    name: 'glazed vs glazes',
    description: 'Commonly confused words: glazed and glazes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglazed\b/gi,
    message: 'Check: "glazed" ("glazed") vs "glazes" ("glazes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glazes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gleams_gleans_2226',
    name: 'gleams vs gleans',
    description: 'Commonly confused words: gleams and gleans',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgleams\b/gi,
    message: 'Check: "gleams" ("gleams") vs "gleans" ("gleans"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gleans';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glee_flee_2227',
    name: 'glee vs flee',
    description: 'Commonly confused words: glee and flee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglee\b/gi,
    message: 'Check: "glee" ("glee") vs "flee" ("flee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glib_glob_2228',
    name: 'glib vs glob',
    description: 'Commonly confused words: glib and glob',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglib\b/gi,
    message: 'Check: "glib" ("glib") vs "glob" ("glob"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glob';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glimpsed_glimpses_2229',
    name: 'glimpsed vs glimpses',
    description: 'Commonly confused words: glimpsed and glimpses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglimpsed\b/gi,
    message: 'Check: "glimpsed" ("glimpsed") vs "glimpses" ("glimpses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glimpses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_globes_gloves_2230',
    name: 'globes vs gloves',
    description: 'Commonly confused words: globes and gloves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglobes\b/gi,
    message: 'Check: "globes" ("globes") vs "gloves" ("gloves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gloves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gloried_glories_2231',
    name: 'gloried vs glories',
    description: 'Commonly confused words: gloried and glories',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgloried\b/gi,
    message: 'Check: "gloried" ("gloried") vs "glories" ("glories"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glories';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glorified_glorifies_2232',
    name: 'glorified vs glorifies',
    description: 'Commonly confused words: glorified and glorifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglorified\b/gi,
    message: 'Check: "glorified" ("glorified") vs "glorifies" ("glorifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glorifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glossed_glosses_2233',
    name: 'glossed vs glosses',
    description: 'Commonly confused words: glossed and glosses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglossed\b/gi,
    message: 'Check: "glossed" ("glossed") vs "glosses" ("glosses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glosses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gloved_gloves_2234',
    name: 'gloved vs gloves',
    description: 'Commonly confused words: gloved and gloves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgloved\b/gi,
    message: 'Check: "gloved" ("gloved") vs "gloves" ("gloves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gloves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glued_glues_2235',
    name: 'glued vs glues',
    description: 'Commonly confused words: glued and glues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglued\b/gi,
    message: 'Check: "glued" ("glued") vs "glues" ("glues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glue_flue_2236',
    name: 'glue vs flue',
    description: 'Commonly confused words: glue and flue',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglue\b/gi,
    message: 'Check: "glue" ("glue") vs "flue" ("flue"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flue';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glues_flues_2237',
    name: 'glues vs flues',
    description: 'Commonly confused words: glues and flues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglues\b/gi,
    message: 'Check: "glues" ("glues") vs "flues" ("flues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_glut_gout_2238',
    name: 'glut vs gout',
    description: 'Commonly confused words: glut and gout',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bglut\b/gi,
    message: 'Check: "glut" ("glut") vs "gout" ("gout"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gout';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gobbles_hobbles_2239',
    name: 'gobbles vs hobbles',
    description: 'Commonly confused words: gobbles and hobbles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgobbles\b/gi,
    message: 'Check: "gobbles" ("gobbles") vs "hobbles" ("hobbles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hobbles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gobs_fobs_2240',
    name: 'gobs vs fobs',
    description: 'Commonly confused words: gobs and fobs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgobs\b/gi,
    message: 'Check: "gobs" ("gobs") vs "fobs" ("fobs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fobs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gods_goes_2241',
    name: 'gods vs goes',
    description: 'Commonly confused words: gods and goes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgods\b/gi,
    message: 'Check: "gods" ("gods") vs "goes" ("goes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'goes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_goes_foes_2242',
    name: 'goes vs foes',
    description: 'Commonly confused words: goes and foes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgoes\b/gi,
    message: 'Check: "goes" ("goes") vs "foes" ("foes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'foes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_goggled_goggles_2243',
    name: 'goggled vs goggles',
    description: 'Commonly confused words: goggled and goggles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgoggled\b/gi,
    message: 'Check: "goggled" ("goggled") vs "goggles" ("goggles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'goggles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_goggled_toggled_2244',
    name: 'goggled vs toggled',
    description: 'Commonly confused words: goggled and toggled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgoggled\b/gi,
    message: 'Check: "goggled" ("goggled") vs "toggled" ("toggled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toggled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_goggles_toggles_2245',
    name: 'goggles vs toggles',
    description: 'Commonly confused words: goggles and toggles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgoggles\b/gi,
    message: 'Check: "goggles" ("goggles") vs "toggles" ("toggles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toggles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gold_bold_2246',
    name: 'gold vs bold',
    description: 'Commonly confused words: gold and bold',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgold\b/gi,
    message: 'Check: "gold" ("gold") vs "bold" ("bold"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bold';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_golds_golfs_2247',
    name: 'golds vs golfs',
    description: 'Commonly confused words: golds and golfs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgolds\b/gi,
    message: 'Check: "golds" ("golds") vs "golfs" ("golfs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'golfs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_golfed_goofed_2248',
    name: 'golfed vs goofed',
    description: 'Commonly confused words: golfed and goofed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgolfed\b/gi,
    message: 'Check: "golfed" ("golfed") vs "goofed" ("goofed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'goofed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_golfing_goofing_2249',
    name: 'golfing vs goofing',
    description: 'Commonly confused words: golfing and goofing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgolfing\b/gi,
    message: 'Check: "golfing" ("golfing") vs "goofing" ("goofing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'goofing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_golfs_goofs_2250',
    name: 'golfs vs goofs',
    description: 'Commonly confused words: golfs and goofs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgolfs\b/gi,
    message: 'Check: "golfs" ("golfs") vs "goofs" ("goofs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'goofs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gone_bone_2251',
    name: 'gone vs bone',
    description: 'Commonly confused words: gone and bone',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgone\b/gi,
    message: 'Check: "gone" ("gone") vs "bone" ("bone"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bone';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_goners_toners_2252',
    name: 'goners vs toners',
    description: 'Commonly confused words: goners and toners',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgoners\b/gi,
    message: 'Check: "goners" ("goners") vs "toners" ("toners"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toners';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gonks_honks_2253',
    name: 'gonks vs honks',
    description: 'Commonly confused words: gonks and honks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgonks\b/gi,
    message: 'Check: "gonks" ("gonks") vs "honks" ("honks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'honks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_good_gold_2254',
    name: 'good vs gold',
    description: 'Commonly confused words: good and gold',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgood\b/gi,
    message: 'Check: "good" ("good") vs "gold" ("gold"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gold';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gooey_hooey_2255',
    name: 'gooey vs hooey',
    description: 'Commonly confused words: gooey and hooey',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgooey\b/gi,
    message: 'Check: "gooey" ("gooey") vs "hooey" ("hooey"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hooey';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_goofed_hoofed_2256',
    name: 'goofed vs hoofed',
    description: 'Commonly confused words: goofed and hoofed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgoofed\b/gi,
    message: 'Check: "goofed" ("goofed") vs "hoofed" ("hoofed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hoofed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_goofing_hoofing_2257',
    name: 'goofing vs hoofing',
    description: 'Commonly confused words: goofing and hoofing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgoofing\b/gi,
    message: 'Check: "goofing" ("goofing") vs "hoofing" ("hoofing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hoofing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gore_bore_2258',
    name: 'gore vs bore',
    description: 'Commonly confused words: gore and bore',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgore\b/gi,
    message: 'Check: "gore" ("gore") vs "bore" ("bore"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bore';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gored_gores_2259',
    name: 'gored vs gores',
    description: 'Commonly confused words: gored and gores',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgored\b/gi,
    message: 'Check: "gored" ("gored") vs "gores" ("gores"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gores';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gorged_forged_2260',
    name: 'gorged vs forged',
    description: 'Commonly confused words: gorged and forged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgorged\b/gi,
    message: 'Check: "gorged" ("gorged") vs "forged" ("forged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'forged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gorged_gorges_2261',
    name: 'gorged vs gorges',
    description: 'Commonly confused words: gorged and gorges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgorged\b/gi,
    message: 'Check: "gorged" ("gorged") vs "gorges" ("gorges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gorges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gorging_forging_2262',
    name: 'gorging vs forging',
    description: 'Commonly confused words: gorging and forging',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgorging\b/gi,
    message: 'Check: "gorging" ("gorging") vs "forging" ("forging"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'forging';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_goring_boring_2263',
    name: 'goring vs boring',
    description: 'Commonly confused words: goring and boring',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgoring\b/gi,
    message: 'Check: "goring" ("goring") vs "boring" ("boring"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'boring';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gouged_gouges_2264',
    name: 'gouged vs gouges',
    description: 'Commonly confused words: gouged and gouges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgouged\b/gi,
    message: 'Check: "gouged" ("gouged") vs "gouges" ("gouges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gouges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grab_gran_2265',
    name: 'grab vs gran',
    description: 'Commonly confused words: grab and gran',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrab\b/gi,
    message: 'Check: "grab" ("grab") vs "gran" ("gran"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gran';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_graced_graces_2266',
    name: 'graced vs graces',
    description: 'Commonly confused words: graced and graces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgraced\b/gi,
    message: 'Check: "graced" ("graced") vs "graces" ("graces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'graces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_graced_graded_2267',
    name: 'graced vs graded',
    description: 'Commonly confused words: graced and graded',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgraced\b/gi,
    message: 'Check: "graced" ("graced") vs "graded" ("graded"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'graded';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_graced_graved_2268',
    name: 'graced vs graved',
    description: 'Commonly confused words: graced and graved',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgraced\b/gi,
    message: 'Check: "graced" ("graced") vs "graved" ("graved"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'graved';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_graced_traced_2269',
    name: 'graced vs traced',
    description: 'Commonly confused words: graced and traced',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgraced\b/gi,
    message: 'Check: "graced" ("graced") vs "traced" ("traced"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'traced';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grace_grade_2270',
    name: 'grace vs grade',
    description: 'Commonly confused words: grace and grade',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrace\b/gi,
    message: 'Check: "grace" ("grace") vs "grade" ("grade"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grade';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_graces_braces_2271',
    name: 'graces vs braces',
    description: 'Commonly confused words: graces and braces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgraces\b/gi,
    message: 'Check: "graces" ("graces") vs "braces" ("braces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'braces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_graces_grades_2272',
    name: 'graces vs grades',
    description: 'Commonly confused words: graces and grades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgraces\b/gi,
    message: 'Check: "graces" ("graces") vs "grades" ("grades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_graded_grades_2273',
    name: 'graded vs grades',
    description: 'Commonly confused words: graded and grades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgraded\b/gi,
    message: 'Check: "graded" ("graded") vs "grades" ("grades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_graders_traders_2274',
    name: 'graders vs traders',
    description: 'Commonly confused words: graders and traders',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgraders\b/gi,
    message: 'Check: "graders" ("graders") vs "traders" ("traders"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'traders';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grads_brads_2275',
    name: 'grads vs brads',
    description: 'Commonly confused words: grads and brads',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrads\b/gi,
    message: 'Check: "grads" ("grads") vs "brads" ("brads"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brads';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_graduated_graduates_2276',
    name: 'graduated vs graduates',
    description: 'Commonly confused words: graduated and graduates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgraduated\b/gi,
    message: 'Check: "graduated" ("graduated") vs "graduates" ("graduates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'graduates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grained_trained_2277',
    name: 'grained vs trained',
    description: 'Commonly confused words: grained and trained',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrained\b/gi,
    message: 'Check: "grained" ("grained") vs "trained" ("trained"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trained';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grains_trains_2278',
    name: 'grains vs trains',
    description: 'Commonly confused words: grains and trains',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrains\b/gi,
    message: 'Check: "grains" ("grains") vs "trains" ("trains"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trains';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gram_gran_2279',
    name: 'gram vs gran',
    description: 'Commonly confused words: gram and gran',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgram\b/gi,
    message: 'Check: "gram" ("gram") vs "gran" ("gran"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gran';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gran_bran_2280',
    name: 'gran vs bran',
    description: 'Commonly confused words: gran and bran',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgran\b/gi,
    message: 'Check: "gran" ("gran") vs "bran" ("bran"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bran';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grander_grandee_2281',
    name: 'grander vs grandee',
    description: 'Commonly confused words: grander and grandee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrander\b/gi,
    message: 'Check: "grander" ("grander") vs "grandee" ("grandee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grandee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grassed_grasses_2282',
    name: 'grassed vs grasses',
    description: 'Commonly confused words: grassed and grasses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrassed\b/gi,
    message: 'Check: "grassed" ("grassed") vs "grasses" ("grasses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grasses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grated_grates_2283',
    name: 'grated vs grates',
    description: 'Commonly confused words: grated and grates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrated\b/gi,
    message: 'Check: "grated" ("grated") vs "grates" ("grates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gratified_gratifies_2284',
    name: 'gratified vs gratifies',
    description: 'Commonly confused words: gratified and gratifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgratified\b/gi,
    message: 'Check: "gratified" ("gratified") vs "gratifies" ("gratifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gratifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gravel_travel_2285',
    name: 'gravel vs travel',
    description: 'Commonly confused words: gravel and travel',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgravel\b/gi,
    message: 'Check: "gravel" ("gravel") vs "travel" ("travel"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'travel';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gravitated_gravitates_2286',
    name: 'gravitated vs gravitates',
    description: 'Commonly confused words: gravitated and gravitates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgravitated\b/gi,
    message: 'Check: "gravitated" ("gravitated") vs "gravitates" ("gravitates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gravitates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grayed_grated_2287',
    name: 'grayed vs grated',
    description: 'Commonly confused words: grayed and grated',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrayed\b/gi,
    message: 'Check: "grayed" ("grayed") vs "grated" ("grated"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grated';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grayer_grater_2288',
    name: 'grayer vs grater',
    description: 'Commonly confused words: grayer and grater',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrayer\b/gi,
    message: 'Check: "grayer" ("grayer") vs "grater" ("grater"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grater';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_graying_grating_2289',
    name: 'graying vs grating',
    description: 'Commonly confused words: graying and grating',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgraying\b/gi,
    message: 'Check: "graying" ("graying") vs "grating" ("grating"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grating';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grays_brays_2290',
    name: 'grays vs brays',
    description: 'Commonly confused words: grays and brays',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrays\b/gi,
    message: 'Check: "grays" ("grays") vs "brays" ("brays"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brays';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_graze_braze_2291',
    name: 'graze vs braze',
    description: 'Commonly confused words: graze and braze',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgraze\b/gi,
    message: 'Check: "graze" ("graze") vs "braze" ("braze"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'braze';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grazed_brazed_2292',
    name: 'grazed vs brazed',
    description: 'Commonly confused words: grazed and brazed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrazed\b/gi,
    message: 'Check: "grazed" ("grazed") vs "brazed" ("brazed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brazed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_greased_greases_2293',
    name: 'greased vs greases',
    description: 'Commonly confused words: greased and greases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgreased\b/gi,
    message: 'Check: "greased" ("greased") vs "greases" ("greases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'greases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grilled_grilles_2294',
    name: 'grilled vs grilles',
    description: 'Commonly confused words: grilled and grilles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrilled\b/gi,
    message: 'Check: "grilled" ("grilled") vs "grilles" ("grilles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grilles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grilled_trilled_2295',
    name: 'grilled vs trilled',
    description: 'Commonly confused words: grilled and trilled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrilled\b/gi,
    message: 'Check: "grilled" ("grilled") vs "trilled" ("trilled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trilled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grills_trills_2296',
    name: 'grills vs trills',
    description: 'Commonly confused words: grills and trills',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrills\b/gi,
    message: 'Check: "grills" ("grills") vs "trills" ("trills"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trills';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grins_grind_2297',
    name: 'grins vs grind',
    description: 'Commonly confused words: grins and grind',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrins\b/gi,
    message: 'Check: "grins" ("grins") vs "grind" ("grind"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grind';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_griped_groped_2298',
    name: 'griped vs groped',
    description: 'Commonly confused words: griped and groped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgriped\b/gi,
    message: 'Check: "griped" ("griped") vs "groped" ("groped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'groped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gripe_grope_2299',
    name: 'gripe vs grope',
    description: 'Commonly confused words: gripe and grope',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgripe\b/gi,
    message: 'Check: "gripe" ("gripe") vs "grope" ("grope"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grope';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_griper_groper_2300',
    name: 'griper vs groper',
    description: 'Commonly confused words: griper and groper',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgriper\b/gi,
    message: 'Check: "griper" ("griper") vs "groper" ("groper"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'groper';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gripped_tripped_2301',
    name: 'gripped vs tripped',
    description: 'Commonly confused words: gripped and tripped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgripped\b/gi,
    message: 'Check: "gripped" ("gripped") vs "tripped" ("tripped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tripped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grippers_trippers_2302',
    name: 'grippers vs trippers',
    description: 'Commonly confused words: grippers and trippers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrippers\b/gi,
    message: 'Check: "grippers" ("grippers") vs "trippers" ("trippers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trippers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gripping_tripping_2303',
    name: 'gripping vs tripping',
    description: 'Commonly confused words: gripping and tripping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgripping\b/gi,
    message: 'Check: "gripping" ("gripping") vs "tripping" ("tripping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tripping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gritty_grotty_2304',
    name: 'gritty vs grotty',
    description: 'Commonly confused words: gritty and grotty',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgritty\b/gi,
    message: 'Check: "gritty" ("gritty") vs "grotty" ("grotty"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grotty';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grooved_grooves_2305',
    name: 'grooved vs grooves',
    description: 'Commonly confused words: grooved and grooves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrooved\b/gi,
    message: 'Check: "grooved" ("grooved") vs "grooves" ("grooves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grooves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gropes_tropes_2306',
    name: 'gropes vs tropes',
    description: 'Commonly confused words: gropes and tropes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgropes\b/gi,
    message: 'Check: "gropes" ("gropes") vs "tropes" ("tropes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tropes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grope_trope_2307',
    name: 'grope vs trope',
    description: 'Commonly confused words: grope and trope',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrope\b/gi,
    message: 'Check: "grope" ("grope") vs "trope" ("trope"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trope';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_groupers_troupers_2308',
    name: 'groupers vs troupers',
    description: 'Commonly confused words: groupers and troupers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgroupers\b/gi,
    message: 'Check: "groupers" ("groupers") vs "troupers" ("troupers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'troupers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grouping_trouping_2309',
    name: 'grouping vs trouping',
    description: 'Commonly confused words: grouping and trouping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrouping\b/gi,
    message: 'Check: "grouping" ("grouping") vs "trouping" ("trouping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trouping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grow_brow_2310',
    name: 'grow vs brow',
    description: 'Commonly confused words: grow and brow',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrow\b/gi,
    message: 'Check: "grow" ("grow") vs "brow" ("brow"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brow';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grows_brows_2311',
    name: 'grows vs brows',
    description: 'Commonly confused words: grows and brows',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrows\b/gi,
    message: 'Check: "grows" ("grows") vs "brows" ("brows"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brows';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grows_gross_2312',
    name: 'grows vs gross',
    description: 'Commonly confused words: grows and gross',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrows\b/gi,
    message: 'Check: "grows" ("grows") vs "gross" ("gross"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gross';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grudges_trudges_2313',
    name: 'grudges vs trudges',
    description: 'Commonly confused words: grudges and trudges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrudges\b/gi,
    message: 'Check: "grudges" ("grudges") vs "trudges" ("trudges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trudges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grudge_trudge_2314',
    name: 'grudge vs trudge',
    description: 'Commonly confused words: grudge and trudge',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrudge\b/gi,
    message: 'Check: "grudge" ("grudge") vs "trudge" ("trudge"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trudge';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_grudging_trudging_2315',
    name: 'grudging vs trudging',
    description: 'Commonly confused words: grudging and trudging',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgrudging\b/gi,
    message: 'Check: "grudging" ("grudging") vs "trudging" ("trudging"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trudging';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_guaranteed_guarantees_2316',
    name: 'guaranteed vs guarantees',
    description: 'Commonly confused words: guaranteed and guarantees',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bguaranteed\b/gi,
    message: 'Check: "guaranteed" ("guaranteed") vs "guarantees" ("guarantees"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'guarantees';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_guillotined_guillotines_2317',
    name: 'guillotined vs guillotines',
    description: 'Commonly confused words: guillotined and guillotines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bguillotined\b/gi,
    message: 'Check: "guillotined" ("guillotined") vs "guillotines" ("guillotines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'guillotines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gull_bull_2318',
    name: 'gull vs bull',
    description: 'Commonly confused words: gull and bull',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgull\b/gi,
    message: 'Check: "gull" ("gull") vs "bull" ("bull"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bull';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gulled_hulled_2319',
    name: 'gulled vs hulled',
    description: 'Commonly confused words: gulled and hulled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgulled\b/gi,
    message: 'Check: "gulled" ("gulled") vs "hulled" ("hulled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hulled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gull_full_2320',
    name: 'gull vs full',
    description: 'Commonly confused words: gull and full',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgull\b/gi,
    message: 'Check: "gull" ("gull") vs "full" ("full"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'full';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gulls_fulls_2321',
    name: 'gulls vs fulls',
    description: 'Commonly confused words: gulls and fulls',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgulls\b/gi,
    message: 'Check: "gulls" ("gulls") vs "fulls" ("fulls"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fulls';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gulls_gills_2322',
    name: 'gulls vs gills',
    description: 'Commonly confused words: gulls and gills',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgulls\b/gi,
    message: 'Check: "gulls" ("gulls") vs "gills" ("gills"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gills';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gum_bum_2323',
    name: 'gum vs bum',
    description: 'Commonly confused words: gum and bum',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgum\b/gi,
    message: 'Check: "gum" ("gum") vs "bum" ("bum"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bum';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gum_fum_2324',
    name: 'gum vs fum',
    description: 'Commonly confused words: gum and fum',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgum\b/gi,
    message: 'Check: "gum" ("gum") vs "fum" ("fum"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fum';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gummed_hummed_2325',
    name: 'gummed vs hummed',
    description: 'Commonly confused words: gummed and hummed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgummed\b/gi,
    message: 'Check: "gummed" ("gummed") vs "hummed" ("hummed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hummed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gumming_humming_2326',
    name: 'gumming vs humming',
    description: 'Commonly confused words: gumming and humming',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgumming\b/gi,
    message: 'Check: "gumming" ("gumming") vs "humming" ("humming"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'humming';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gushed_hushed_2327',
    name: 'gushed vs hushed',
    description: 'Commonly confused words: gushed and hushed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgushed\b/gi,
    message: 'Check: "gushed" ("gushed") vs "hushed" ("hushed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hushed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gushes_bushes_2328',
    name: 'gushes vs bushes',
    description: 'Commonly confused words: gushes and bushes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgushes\b/gi,
    message: 'Check: "gushes" ("gushes") vs "bushes" ("bushes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bushes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gushes_hushes_2329',
    name: 'gushes vs hushes',
    description: 'Commonly confused words: gushes and hushes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgushes\b/gi,
    message: 'Check: "gushes" ("gushes") vs "hushes" ("hushes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hushes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gushing_bushing_2330',
    name: 'gushing vs bushing',
    description: 'Commonly confused words: gushing and bushing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgushing\b/gi,
    message: 'Check: "gushing" ("gushing") vs "bushing" ("bushing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bushing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gushing_hushing_2331',
    name: 'gushing vs hushing',
    description: 'Commonly confused words: gushing and hushing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgushing\b/gi,
    message: 'Check: "gushing" ("gushing") vs "hushing" ("hushing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hushing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gussy_hussy_2332',
    name: 'gussy vs hussy',
    description: 'Commonly confused words: gussy and hussy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgussy\b/gi,
    message: 'Check: "gussy" ("gussy") vs "hussy" ("hussy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hussy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gust_bust_2333',
    name: 'gust vs bust',
    description: 'Commonly confused words: gust and bust',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgust\b/gi,
    message: 'Check: "gust" ("gust") vs "bust" ("bust"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bust';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gusted_busted_2334',
    name: 'gusted vs busted',
    description: 'Commonly confused words: gusted and busted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgusted\b/gi,
    message: 'Check: "gusted" ("gusted") vs "busted" ("busted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'busted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gusting_busting_2335',
    name: 'gusting vs busting',
    description: 'Commonly confused words: gusting and busting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgusting\b/gi,
    message: 'Check: "gusting" ("gusting") vs "busting" ("busting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'busting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gusts_busts_2336',
    name: 'gusts vs busts',
    description: 'Commonly confused words: gusts and busts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgusts\b/gi,
    message: 'Check: "gusts" ("gusts") vs "busts" ("busts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'busts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gutters_butters_2337',
    name: 'gutters vs butters',
    description: 'Commonly confused words: gutters and butters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgutters\b/gi,
    message: 'Check: "gutters" ("gutters") vs "butters" ("butters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'butters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_guy_buy_2338',
    name: 'guy vs buy',
    description: 'Commonly confused words: guy and buy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bguy\b/gi,
    message: 'Check: "guy" ("guy") vs "buy" ("buy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'buy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gyrated_gyrates_2339',
    name: 'gyrated vs gyrates',
    description: 'Commonly confused words: gyrated and gyrates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgyrated\b/gi,
    message: 'Check: "gyrated" ("gyrated") vs "gyrates" ("gyrates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gyrates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_gyro_tyro_2340',
    name: 'gyro vs tyro',
    description: 'Commonly confused words: gyro and tyro',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bgyro\b/gi,
    message: 'Check: "gyro" ("gyro") vs "tyro" ("tyro"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tyro';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
