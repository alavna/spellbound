/**
 * Auto-generated confusion rules (B)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.496Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesB: GrammarRule[] = [
  {
    id: 'confusion_bacteria_bacterium_54',
    name: 'bacteria vs bacterium',
    description: 'Commonly confused words: bacteria and bacterium',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbacteria\b/gi,
    message: 'Check: "bacteria" ("bacteria") vs "bacterium" ("bacterium"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bacterium';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bade_bayed_55',
    name: 'bade vs bayed',
    description: 'Commonly confused words: bade and bayed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbade\b/gi,
    message: 'Check: "bade" ("bade") vs "bayed" ("bayed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bayed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bed_bad_56',
    name: 'bed vs bad',
    description: 'Commonly confused words: bed and bad',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbed\b/gi,
    message: 'Did you mean "bad"? "bed" ("bed") is often confused with "bad" ("bad").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bad';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bad_bed_57',
    name: 'bad vs bed',
    description: 'Commonly confused words: bad and bed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbad\b/gi,
    message: 'Did you mean "bed"? "bad" ("bad") is often confused with "bed" ("bed").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bail_bale_58',
    name: 'bail vs bale',
    description: 'Commonly confused words: bail and bale',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbail\b/gi,
    message: 'Check: "bail" ("bail") vs "bale" ("bale"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bale';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_baize_bays_59',
    name: 'baize vs bays',
    description: 'Commonly confused words: baize and bays',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbaize\b/gi,
    message: 'Check: "baize" ("baize") vs "bays" ("bays"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bays';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_balled_bawled_60',
    name: 'balled vs bawled',
    description: 'Commonly confused words: balled and bawled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bballed\b/gi,
    message: 'Check: "balled" ("balled") vs "bawled" ("bawled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bawled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ballet_ballot_61',
    name: 'ballet vs ballot',
    description: 'Commonly confused words: ballet and ballot',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bballet\b/gi,
    message: 'Check: "ballet" ("ballet") vs "ballot" ("ballot"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ballot';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ballets_ballots_62',
    name: 'ballets vs ballots',
    description: 'Commonly confused words: ballets and ballots',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bballets\b/gi,
    message: 'Check: "ballets" ("ballets") vs "ballots" ("ballots"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ballots';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ballot_bullet_63',
    name: 'ballot vs bullet',
    description: 'Commonly confused words: ballot and bullet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bballot\b/gi,
    message: 'Check: "ballot" ("ballot") vs "bullet" ("bullet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bullet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bard_barred_64',
    name: 'bard vs barred',
    description: 'Commonly confused words: bard and barred',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbard\b/gi,
    message: 'Check: "bard" ("bard") vs "barred" ("barred"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'barred';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_barely_barley_65',
    name: 'barely vs barley',
    description: 'Commonly confused words: barely and barley',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbarely\b/gi,
    message: 'Check: "barely" ("barely") vs "barley" ("barley"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'barley';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bare_bear_66',
    name: 'bare vs bear',
    description: 'Commonly confused words: bare and bear',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbare\b/gi,
    message: 'Check: "bare" (uncovered) vs "bear" (animal or to carry). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bear';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_baron_barren_67',
    name: 'baron vs barren',
    description: 'Commonly confused words: baron and barren',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbaron\b/gi,
    message: 'Check: "baron" ("baron") vs "barren" ("barren"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'barren';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_basal_basil_68',
    name: 'basal vs basil',
    description: 'Commonly confused words: basal and basil',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbasal\b/gi,
    message: 'Check: "basal" ("basal") vs "basil" ("basil"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'basil';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_based_baste_69',
    name: 'based vs baste',
    description: 'Commonly confused words: based and baste',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbased\b/gi,
    message: 'Check: "based" ("based") vs "baste" ("baste"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'baste';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_base_bass_70',
    name: 'base vs bass',
    description: 'Commonly confused words: base and bass',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbase\b/gi,
    message: 'Check: "base" ("base") vs "bass" ("bass"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bass';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_baud_bawd_71',
    name: 'baud vs bawd',
    description: 'Commonly confused words: baud and bawd',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbaud\b/gi,
    message: 'Check: "baud" ("baud") vs "bawd" ("bawd"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bawd';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bawdily_bodily_72',
    name: 'bawdily vs bodily',
    description: 'Commonly confused words: bawdily and bodily',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbawdily\b/gi,
    message: 'Check: "bawdily" ("bawdily") vs "bodily" ("bodily"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bodily';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bazaar_bizarre_73',
    name: 'bazaar vs bizarre',
    description: 'Commonly confused words: bazaar and bizarre',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbazaar\b/gi,
    message: 'Check: "bazaar" ("bazaar") vs "bizarre" ("bizarre"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bizarre';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bean_been_74',
    name: 'bean vs been',
    description: 'Commonly confused words: bean and been',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbean\b/gi,
    message: 'Check: "bean" ("bean") vs "been" ("been"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'been';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_beau_bow_75',
    name: 'beau vs bow',
    description: 'Commonly confused words: beau and bow',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbeau\b/gi,
    message: 'Check: "beau" ("beau") vs "bow" ("bow"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bow';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_beaus_bows_76',
    name: 'beaus vs bows',
    description: 'Commonly confused words: beaus and bows',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbeaus\b/gi,
    message: 'Check: "beaus" ("beaus") vs "bows" ("bows"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bows';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bee_be_77',
    name: 'bee vs be',
    description: 'Commonly confused words: bee and be',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbee\b/gi,
    message: 'Did you mean "be"? "bee" ("bee") is often confused with "be" ("be").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'be';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_be_bee_78',
    name: 'be vs bee',
    description: 'Commonly confused words: be and bee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbe\b/gi,
    message: 'Did you mean "bee"? "be" ("be") is often confused with "bee" ("bee").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bedding_betting_79',
    name: 'bedding vs betting',
    description: 'Commonly confused words: bedding and betting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbedding\b/gi,
    message: 'Check: "bedding" ("bedding") vs "betting" ("betting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'betting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_been_bin_80',
    name: 'been vs bin',
    description: 'Commonly confused words: been and bin',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbeen\b/gi,
    message: 'Check: "been" ("been") vs "bin" ("bin"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bin';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_begin_being_81',
    name: 'begin vs being',
    description: 'Commonly confused words: begin and being',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbegin\b/gi,
    message: 'Check: "begin" ("begin") vs "being" ("being"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'being';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_belief_believe_82',
    name: 'belief vs believe',
    description: 'Commonly confused words: belief and believe',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbelief\b/gi,
    message: 'Check: "belief" ("belief") vs "believe" ("believe"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'believe';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_beliefs_believes_83',
    name: 'beliefs vs believes',
    description: 'Commonly confused words: beliefs and believes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbeliefs\b/gi,
    message: 'Check: "beliefs" ("beliefs") vs "believes" ("believes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'believes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bellow_below_84',
    name: 'bellow vs below',
    description: 'Commonly confused words: bellow and below',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbellow\b/gi,
    message: 'Check: "bellow" ("bellow") vs "below" ("below"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'below';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_benefactor_beneficiary_85',
    name: 'benefactor vs beneficiary',
    description: 'Commonly confused words: benefactor and beneficiary',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbenefactor\b/gi,
    message: 'Check: "benefactor" ("benefactor") vs "beneficiary" ("beneficiary"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'beneficiary';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_berth_birth_86',
    name: 'berth vs birth',
    description: 'Commonly confused words: berth and birth',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bberth\b/gi,
    message: 'Check: "berth" ("berth") vs "birth" ("birth"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'birth';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_berths_births_87',
    name: 'berths vs births',
    description: 'Commonly confused words: berths and births',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bberths\b/gi,
    message: 'Check: "berths" ("berths") vs "births" ("births"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'births';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_besot_besought_88',
    name: 'besot vs besought',
    description: 'Commonly confused words: besot and besought',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbesot\b/gi,
    message: 'Check: "besot" ("besot") vs "besought" ("besought"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'besought';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_best_bets_89',
    name: 'best vs bets',
    description: 'Commonly confused words: best and bets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbest\b/gi,
    message: 'Check: "best" ("best") vs "bets" ("bets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_be_we_90',
    name: 'be vs we',
    description: 'Commonly confused words: be and we',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbe\b/gi,
    message: 'Check: "be" ("be") vs "we" ("we"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'we';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bidder_bitter_91',
    name: 'bidder vs bitter',
    description: 'Commonly confused words: bidder and bitter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbidder\b/gi,
    message: 'Check: "bidder" ("bidder") vs "bitter" ("bitter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bitter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bidders_bitters_92',
    name: 'bidders vs bitters',
    description: 'Commonly confused words: bidders and bitters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbidders\b/gi,
    message: 'Check: "bidders" ("bidders") vs "bitters" ("bitters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bitters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bights_bites_93',
    name: 'bights vs bites',
    description: 'Commonly confused words: bights and bites',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbights\b/gi,
    message: 'Check: "bights" ("bights") vs "bites" ("bites"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bites';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_billed_build_94',
    name: 'billed vs build',
    description: 'Commonly confused words: billed and build',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbilled\b/gi,
    message: 'Check: "billed" ("billed") vs "build" ("build"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'build';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bite_byte_95',
    name: 'bite vs byte',
    description: 'Commonly confused words: bite and byte',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbite\b/gi,
    message: 'Check: "bite" ("bite") vs "byte" ("byte"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'byte';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bites_bytes_96',
    name: 'bites vs bytes',
    description: 'Commonly confused words: bites and bytes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbites\b/gi,
    message: 'Check: "bites" ("bites") vs "bytes" ("bytes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bytes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blew_blue_97',
    name: 'blew vs blue',
    description: 'Commonly confused words: blew and blue',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblew\b/gi,
    message: 'Check: "blew" ("blew") vs "blue" ("blue"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'blue';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bloc_block_98',
    name: 'bloc vs block',
    description: 'Commonly confused words: bloc and block',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbloc\b/gi,
    message: 'Check: "bloc" ("bloc") vs "block" ("block"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'block';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blocks_blocs_99',
    name: 'blocks vs blocs',
    description: 'Commonly confused words: blocks and blocs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblocks\b/gi,
    message: 'Check: "blocks" ("blocks") vs "blocs" ("blocs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'blocs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blunt_brunt_100',
    name: 'blunt vs brunt',
    description: 'Commonly confused words: blunt and brunt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblunt\b/gi,
    message: 'Check: "blunt" ("blunt") vs "brunt" ("brunt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brunt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_board_bored_101',
    name: 'board vs bored',
    description: 'Commonly confused words: board and bored',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bboard\b/gi,
    message: 'Check: "board" (flat piece of wood) vs "bored" (uninterested). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bored';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_boated_boded_102',
    name: 'boated vs boded',
    description: 'Commonly confused words: boated and boded',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bboated\b/gi,
    message: 'Check: "boated" ("boated") vs "boded" ("boded"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'boded';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_boating_boding_103',
    name: 'boating vs boding',
    description: 'Commonly confused words: boating and boding',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bboating\b/gi,
    message: 'Check: "boating" ("boating") vs "boding" ("boding"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'boding';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bode_bowed_104',
    name: 'bode vs bowed',
    description: 'Commonly confused words: bode and bowed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbode\b/gi,
    message: 'Check: "bode" ("bode") vs "bowed" ("bowed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bowed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bond_bound_105',
    name: 'bond vs bound',
    description: 'Commonly confused words: bond and bound',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbond\b/gi,
    message: 'Check: "bond" ("bond") vs "bound" ("bound"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bound';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bonds_bounds_106',
    name: 'bonds vs bounds',
    description: 'Commonly confused words: bonds and bounds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbonds\b/gi,
    message: 'Check: "bonds" ("bonds") vs "bounds" ("bounds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bounds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bootees_booties_107',
    name: 'bootees vs booties',
    description: 'Commonly confused words: bootees and booties',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbootees\b/gi,
    message: 'Check: "bootees" ("bootees") vs "booties" ("booties"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'booties';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_borrow_lend_108',
    name: 'borrow vs lend',
    description: 'Commonly confused words: borrow and lend',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bborrow\b/gi,
    message: 'Check: "borrow" ("borrow") vs "lend" ("lend"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'lend';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_borrows_lends_109',
    name: 'borrows vs lends',
    description: 'Commonly confused words: borrows and lends',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bborrows\b/gi,
    message: 'Check: "borrows" ("borrows") vs "lends" ("lends"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'lends';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bough_bow_110',
    name: 'bough vs bow',
    description: 'Commonly confused words: bough and bow',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbough\b/gi,
    message: 'Check: "bough" ("bough") vs "bow" ("bow"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bow';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bounce_bounds_111',
    name: 'bounce vs bounds',
    description: 'Commonly confused words: bounce and bounds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbounce\b/gi,
    message: 'Check: "bounce" ("bounce") vs "bounds" ("bounds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bounds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_braid_brayed_112',
    name: 'braid vs brayed',
    description: 'Commonly confused words: braid and brayed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbraid\b/gi,
    message: 'Check: "braid" ("braid") vs "brayed" ("brayed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brayed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_braise_braze_113',
    name: 'braise vs braze',
    description: 'Commonly confused words: braise and braze',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbraise\b/gi,
    message: 'Check: "braise" ("braise") vs "braze" ("braze"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'braze';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_braise_brays_114',
    name: 'braise vs brays',
    description: 'Commonly confused words: braise and brays',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbraise\b/gi,
    message: 'Check: "braise" ("braise") vs "brays" ("brays"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brays';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brake_break_115',
    name: 'brake vs break',
    description: 'Commonly confused words: brake and break',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrake\b/gi,
    message: 'Check: "brake" (device to stop movement) vs "break" (to shatter or pause). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'break';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brakes_breaks_116',
    name: 'brakes vs breaks',
    description: 'Commonly confused words: brakes and breaks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrakes\b/gi,
    message: 'Check: "brakes" ("brakes") vs "breaks" ("breaks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'breaks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_breach_breech_117',
    name: 'breach vs breech',
    description: 'Commonly confused words: breach and breech',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbreach\b/gi,
    message: 'Check: "breach" ("breach") vs "breech" ("breech"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'breech';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_breaches_breeches_118',
    name: 'breaches vs breeches',
    description: 'Commonly confused words: breaches and breeches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbreaches\b/gi,
    message: 'Check: "breaches" ("breaches") vs "breeches" ("breeches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'breeches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bread_bred_119',
    name: 'bread vs bred',
    description: 'Commonly confused words: bread and bred',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbread\b/gi,
    message: 'Check: "bread" ("bread") vs "bred" ("bred"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bred';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_breath_breathe_120',
    name: 'breath vs breathe',
    description: 'Commonly confused words: breath and breathe',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbreath\b/gi,
    message: 'Check: "breath" (air inhaled (noun)) vs "breathe" (to inhale air (verb)). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'breathe';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_breathes_breaths_121',
    name: 'breathes vs breaths',
    description: 'Commonly confused words: breathes and breaths',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbreathes\b/gi,
    message: 'Check: "breathes" ("breathes") vs "breaths" ("breaths"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'breaths';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brewed_brood_122',
    name: 'brewed vs brood',
    description: 'Commonly confused words: brewed and brood',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrewed\b/gi,
    message: 'Check: "brewed" ("brewed") vs "brood" ("brood"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brood';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brews_bruise_123',
    name: 'brews vs bruise',
    description: 'Commonly confused words: brews and bruise',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrews\b/gi,
    message: 'Check: "brews" ("brews") vs "bruise" ("bruise"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bruise';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bridal_bridle_124',
    name: 'bridal vs bridle',
    description: 'Commonly confused words: bridal and bridle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbridal\b/gi,
    message: 'Check: "bridal" ("bridal") vs "bridle" ("bridle"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bridle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brows_browse_125',
    name: 'brows vs browse',
    description: 'Commonly confused words: brows and browse',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrows\b/gi,
    message: 'Check: "brows" ("brows") vs "browse" ("browse"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'browse';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bruit_brute_126',
    name: 'bruit vs brute',
    description: 'Commonly confused words: bruit and brute',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbruit\b/gi,
    message: 'Check: "bruit" ("bruit") vs "brute" ("brute"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brute';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bused_bust_127',
    name: 'bused vs bust',
    description: 'Commonly confused words: bused and bust',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbused\b/gi,
    message: 'Check: "bused" ("bused") vs "bust" ("bust"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bust';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bussed_bust_128',
    name: 'bussed vs bust',
    description: 'Commonly confused words: bussed and bust',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbussed\b/gi,
    message: 'Check: "bussed" ("bussed") vs "bust" ("bust"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bust';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_but_nut_129',
    name: 'but vs nut',
    description: 'Commonly confused words: but and nut',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbut\b/gi,
    message: 'Check: "but" ("but") vs "nut" ("nut"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nut';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_buy_bye_130',
    name: 'buy vs bye',
    description: 'Commonly confused words: buy and bye',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbuy\b/gi,
    message: 'Check: "buy" ("buy") vs "bye" ("bye"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bye';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_by_my_131',
    name: 'by vs my',
    description: 'Commonly confused words: by and my',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bby\b/gi,
    message: 'Did you mean "my"? "by" ("by") is often confused with "my" ("my").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'my';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blue_clue_775',
    name: 'blue vs clue',
    description: 'Commonly confused words: blue and clue',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblue\b/gi,
    message: 'Did you mean "clue"? "blue" ("blue") is often confused with "clue" ("clue").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clue';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_be_me_782',
    name: 'be vs me',
    description: 'Commonly confused words: be and me',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbe\b/gi,
    message: 'Check: "be" ("be") vs "me" ("me"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'me';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_babels_bagels_924',
    name: 'babels vs bagels',
    description: 'Commonly confused words: babels and bagels',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbabels\b/gi,
    message: 'Check: "babels" ("babels") vs "bagels" ("bagels"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bagels';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_baffled_baffles_925',
    name: 'baffled vs baffles',
    description: 'Commonly confused words: baffled and baffles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbaffled\b/gi,
    message: 'Check: "baffled" ("baffled") vs "baffles" ("baffles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'baffles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bagged_gagged_926',
    name: 'bagged vs gagged',
    description: 'Commonly confused words: bagged and gagged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbagged\b/gi,
    message: 'Check: "bagged" ("bagged") vs "gagged" ("gagged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gagged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bagging_gagging_927',
    name: 'bagging vs gagging',
    description: 'Commonly confused words: bagging and gagging',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbagging\b/gi,
    message: 'Check: "bagging" ("bagging") vs "gagging" ("gagging"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gagging';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bait_gait_928',
    name: 'bait vs gait',
    description: 'Commonly confused words: bait and gait',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbait\b/gi,
    message: 'Check: "bait" ("bait") vs "gait" ("gait"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gait';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_balanced_balances_929',
    name: 'balanced vs balances',
    description: 'Commonly confused words: balanced and balances',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbalanced\b/gi,
    message: 'Check: "balanced" ("balanced") vs "balances" ("balances"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'balances';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bald_bale_930',
    name: 'bald vs bale',
    description: 'Commonly confused words: bald and bale',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbald\b/gi,
    message: 'Check: "bald" ("bald") vs "bale" ("bale"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bale';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_baled_bales_931',
    name: 'baled vs bales',
    description: 'Commonly confused words: baled and bales',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbaled\b/gi,
    message: 'Check: "baled" ("baled") vs "bales" ("bales"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bales';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_balked_balled_932',
    name: 'balked vs balled',
    description: 'Commonly confused words: balked and balled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbalked\b/gi,
    message: 'Check: "balked" ("balked") vs "balled" ("balled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'balled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_banished_vanished_933',
    name: 'banished vs vanished',
    description: 'Commonly confused words: banished and vanished',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbanished\b/gi,
    message: 'Check: "banished" ("banished") vs "vanished" ("vanished"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vanished';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_banishes_vanishes_934',
    name: 'banishes vs vanishes',
    description: 'Commonly confused words: banishes and vanishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbanishes\b/gi,
    message: 'Check: "banishes" ("banishes") vs "vanishes" ("vanishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vanishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_banish_vanish_935',
    name: 'banish vs vanish',
    description: 'Commonly confused words: banish and vanish',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbanish\b/gi,
    message: 'Check: "banish" ("banish") vs "vanish" ("vanish"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vanish';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_baptized_baptizes_936',
    name: 'baptized vs baptizes',
    description: 'Commonly confused words: baptized and baptizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbaptized\b/gi,
    message: 'Check: "baptized" ("baptized") vs "baptizes" ("baptizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'baptizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_barb_barn_937',
    name: 'barb vs barn',
    description: 'Commonly confused words: barb and barn',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbarb\b/gi,
    message: 'Check: "barb" ("barb") vs "barn" ("barn"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'barn';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_barbecued_barbecues_938',
    name: 'barbecued vs barbecues',
    description: 'Commonly confused words: barbecued and barbecues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbarbecued\b/gi,
    message: 'Check: "barbecued" ("barbecued") vs "barbecues" ("barbecues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'barbecues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_barbed_barged_939',
    name: 'barbed vs barged',
    description: 'Commonly confused words: barbed and barged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbarbed\b/gi,
    message: 'Check: "barbed" ("barbed") vs "barged" ("barged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'barged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_barfed_barged_940',
    name: 'barfed vs barged',
    description: 'Commonly confused words: barfed and barged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbarfed\b/gi,
    message: 'Check: "barfed" ("barfed") vs "barged" ("barged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'barged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_barged_barges_941',
    name: 'barged vs barges',
    description: 'Commonly confused words: barged and barges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbarged\b/gi,
    message: 'Check: "barged" ("barged") vs "barges" ("barges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'barges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_barraged_barrages_942',
    name: 'barraged vs barrages',
    description: 'Commonly confused words: barraged and barrages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbarraged\b/gi,
    message: 'Check: "barraged" ("barraged") vs "barrages" ("barrages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'barrages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_barred_barres_943',
    name: 'barred vs barres',
    description: 'Commonly confused words: barred and barres',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbarred\b/gi,
    message: 'Check: "barred" ("barred") vs "barres" ("barres"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'barres';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_barricaded_barricades_944',
    name: 'barricaded vs barricades',
    description: 'Commonly confused words: barricaded and barricades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbarricaded\b/gi,
    message: 'Check: "barricaded" ("barricaded") vs "barricades" ("barricades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'barricades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_barrows_narrows_945',
    name: 'barrows vs narrows',
    description: 'Commonly confused words: barrows and narrows',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbarrows\b/gi,
    message: 'Check: "barrows" ("barrows") vs "narrows" ("narrows"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'narrows';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bartered_battered_946',
    name: 'bartered vs battered',
    description: 'Commonly confused words: bartered and battered',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbartered\b/gi,
    message: 'Check: "bartered" ("bartered") vs "battered" ("battered"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'battered';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bartering_battering_947',
    name: 'bartering vs battering',
    description: 'Commonly confused words: bartering and battering',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbartering\b/gi,
    message: 'Check: "bartering" ("bartering") vs "battering" ("battering"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'battering';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_barters_batters_948',
    name: 'barters vs batters',
    description: 'Commonly confused words: barters and batters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbarters\b/gi,
    message: 'Check: "barters" ("barters") vs "batters" ("batters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'batters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_barters_garters_949',
    name: 'barters vs garters',
    description: 'Commonly confused words: barters and garters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbarters\b/gi,
    message: 'Check: "barters" ("barters") vs "garters" ("garters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'garters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_basally_nasally_950',
    name: 'basally vs nasally',
    description: 'Commonly confused words: basally and nasally',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbasally\b/gi,
    message: 'Check: "basally" ("basally") vs "nasally" ("nasally"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nasally';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_basal_nasal_951',
    name: 'basal vs nasal',
    description: 'Commonly confused words: basal and nasal',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbasal\b/gi,
    message: 'Check: "basal" ("basal") vs "nasal" ("nasal"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nasal';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_based_bases_952',
    name: 'based vs bases',
    description: 'Commonly confused words: based and bases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbased\b/gi,
    message: 'Check: "based" ("based") vs "bases" ("bases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_basks_basis_953',
    name: 'basks vs basis',
    description: 'Commonly confused words: basks and basis',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbasks\b/gi,
    message: 'Check: "basks" ("basks") vs "basis" ("basis"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'basis';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_batched_batches_954',
    name: 'batched vs batches',
    description: 'Commonly confused words: batched and batches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbatched\b/gi,
    message: 'Check: "batched" ("batched") vs "batches" ("batches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'batches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bated_bared_955',
    name: 'bated vs bared',
    description: 'Commonly confused words: bated and bared',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbated\b/gi,
    message: 'Check: "bated" ("bated") vs "bared" ("bared"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bared';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bathed_bathes_956',
    name: 'bathed vs bathes',
    description: 'Commonly confused words: bathed and bathes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbathed\b/gi,
    message: 'Check: "bathed" ("bathed") vs "bathes" ("bathes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bathes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bathers_gathers_957',
    name: 'bathers vs gathers',
    description: 'Commonly confused words: bathers and gathers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbathers\b/gi,
    message: 'Check: "bathers" ("bathers") vs "gathers" ("gathers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gathers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bats_bags_958',
    name: 'bats vs bags',
    description: 'Commonly confused words: bats and bags',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbats\b/gi,
    message: 'Check: "bats" ("bats") vs "bags" ("bags"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bags';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_batter_barter_959',
    name: 'batter vs barter',
    description: 'Commonly confused words: batter and barter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbatter\b/gi,
    message: 'Check: "batter" ("batter") vs "barter" ("barter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'barter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_battering_nattering_960',
    name: 'battering vs nattering',
    description: 'Commonly confused words: battering and nattering',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbattering\b/gi,
    message: 'Check: "battering" ("battering") vs "nattering" ("nattering"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nattering';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bawd_gawd_961',
    name: 'bawd vs gawd',
    description: 'Commonly confused words: bawd and gawd',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbawd\b/gi,
    message: 'Check: "bawd" ("bawd") vs "gawd" ("gawd"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gawd';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bayed_bated_962',
    name: 'bayed vs bated',
    description: 'Commonly confused words: bayed and bated',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbayed\b/gi,
    message: 'Check: "bayed" ("bayed") vs "bated" ("bated"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bated';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_baying_bating_963',
    name: 'baying vs bating',
    description: 'Commonly confused words: baying and bating',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbaying\b/gi,
    message: 'Check: "baying" ("baying") vs "bating" ("bating"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bating';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_beached_beaches_964',
    name: 'beached vs beaches',
    description: 'Commonly confused words: beached and beaches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbeached\b/gi,
    message: 'Check: "beached" ("beached") vs "beaches" ("beaches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'beaches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_beaked_braked_965',
    name: 'beaked vs braked',
    description: 'Commonly confused words: beaked and braked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbeaked\b/gi,
    message: 'Check: "beaked" ("beaked") vs "braked" ("braked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'braked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_beam_bean_966',
    name: 'beam vs bean',
    description: 'Commonly confused words: beam and bean',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbeam\b/gi,
    message: 'Check: "beam" ("beam") vs "bean" ("bean"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bean';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_beamed_beaned_967',
    name: 'beamed vs beaned',
    description: 'Commonly confused words: beamed and beaned',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbeamed\b/gi,
    message: 'Check: "beamed" ("beamed") vs "beaned" ("beaned"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'beaned';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_beams_beans_968',
    name: 'beams vs beans',
    description: 'Commonly confused words: beams and beans',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbeams\b/gi,
    message: 'Check: "beams" ("beams") vs "beans" ("beans"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'beans';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bearing_beating_969',
    name: 'bearing vs beating',
    description: 'Commonly confused words: bearing and beating',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbearing\b/gi,
    message: 'Check: "bearing" ("bearing") vs "beating" ("beating"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'beating';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bearings_beatings_970',
    name: 'bearings vs beatings',
    description: 'Commonly confused words: bearings and beatings',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbearings\b/gi,
    message: 'Check: "bearings" ("bearings") vs "beatings" ("beatings"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'beatings';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bears_beard_971',
    name: 'bears vs beard',
    description: 'Commonly confused words: bears and beard',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbears\b/gi,
    message: 'Check: "bears" ("bears") vs "beard" ("beard"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'beard';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_beaten_neaten_972',
    name: 'beaten vs neaten',
    description: 'Commonly confused words: beaten and neaten',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbeaten\b/gi,
    message: 'Check: "beaten" ("beaten") vs "neaten" ("neaten"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'neaten';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_becks_necks_973',
    name: 'becks vs necks',
    description: 'Commonly confused words: becks and necks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbecks\b/gi,
    message: 'Check: "becks" ("becks") vs "necks" ("necks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'necks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_beds_bees_974',
    name: 'beds vs bees',
    description: 'Commonly confused words: beds and bees',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbeds\b/gi,
    message: 'Check: "beds" ("beds") vs "bees" ("bees"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bees';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bee_bed_975',
    name: 'bee vs bed',
    description: 'Commonly confused words: bee and bed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbee\b/gi,
    message: 'Check: "bee" ("bee") vs "bed" ("bed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_beers_beets_976',
    name: 'beers vs beets',
    description: 'Commonly confused words: beers and beets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbeers\b/gi,
    message: 'Check: "beers" ("beers") vs "beets" ("beets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'beets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_befuddled_befuddles_977',
    name: 'befuddled vs befuddles',
    description: 'Commonly confused words: befuddled and befuddles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbefuddled\b/gi,
    message: 'Check: "befuddled" ("befuddled") vs "befuddles" ("befuddles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'befuddles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_begin_begun_978',
    name: 'begin vs begun',
    description: 'Commonly confused words: begin and begun',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbegin\b/gi,
    message: 'Check: "begin" ("begin") vs "begun" ("begun"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'begun';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_begs_bets_979',
    name: 'begs vs bets',
    description: 'Commonly confused words: begs and bets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbegs\b/gi,
    message: 'Check: "begs" ("begs") vs "bets" ("bets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_beguiled_beguiles_980',
    name: 'beguiled vs beguiles',
    description: 'Commonly confused words: beguiled and beguiles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbeguiled\b/gi,
    message: 'Check: "beguiled" ("beguiled") vs "beguiles" ("beguiles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'beguiles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_begum_begun_981',
    name: 'begum vs begun',
    description: 'Commonly confused words: begum and begun',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbegum\b/gi,
    message: 'Check: "begum" ("begum") vs "begun" ("begun"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'begun';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_behaved_behaves_982',
    name: 'behaved vs behaves',
    description: 'Commonly confused words: behaved and behaves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbehaved\b/gi,
    message: 'Check: "behaved" ("behaved") vs "behaves" ("behaves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'behaves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_being_bring_983',
    name: 'being vs bring',
    description: 'Commonly confused words: being and bring',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbeing\b/gi,
    message: 'Check: "being" ("being") vs "bring" ("bring"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bring';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_beings_brings_984',
    name: 'beings vs brings',
    description: 'Commonly confused words: beings and brings',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbeings\b/gi,
    message: 'Check: "beings" ("beings") vs "brings" ("brings"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brings';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_belated_belayed_985',
    name: 'belated vs belayed',
    description: 'Commonly confused words: belated and belayed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbelated\b/gi,
    message: 'Check: "belated" ("belated") vs "belayed" ("belayed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'belayed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_belied_belief_986',
    name: 'belied vs belief',
    description: 'Commonly confused words: belied and belief',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbelied\b/gi,
    message: 'Check: "belied" ("belied") vs "belief" ("belief"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'belief';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_belied_belies_987',
    name: 'belied vs belies',
    description: 'Commonly confused words: belied and belies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbelied\b/gi,
    message: 'Check: "belied" ("belied") vs "belies" ("belies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'belies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_believed_believes_988',
    name: 'believed vs believes',
    description: 'Commonly confused words: believed and believes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbelieved\b/gi,
    message: 'Check: "believed" ("believed") vs "believes" ("believes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'believes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_belled_belles_989',
    name: 'belled vs belles',
    description: 'Commonly confused words: belled and belles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbelled\b/gi,
    message: 'Check: "belled" ("belled") vs "belles" ("belles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'belles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bellied_bellies_990',
    name: 'bellied vs bellies',
    description: 'Commonly confused words: bellied and bellies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbellied\b/gi,
    message: 'Check: "bellied" ("bellied") vs "bellies" ("bellies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bellies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_belting_belying_991',
    name: 'belting vs belying',
    description: 'Commonly confused words: belting and belying',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbelting\b/gi,
    message: 'Check: "belting" ("belting") vs "belying" ("belying"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'belying';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_benders_genders_992',
    name: 'benders vs genders',
    description: 'Commonly confused words: benders and genders',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbenders\b/gi,
    message: 'Check: "benders" ("benders") vs "genders" ("genders"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'genders';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_berried_berries_993',
    name: 'berried vs berries',
    description: 'Commonly confused words: berried and berries',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bberried\b/gi,
    message: 'Check: "berried" ("berried") vs "berries" ("berries"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'berries';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bested_nested_994',
    name: 'bested vs nested',
    description: 'Commonly confused words: bested and nested',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbested\b/gi,
    message: 'Check: "bested" ("bested") vs "nested" ("nested"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nested';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_besting_nesting_995',
    name: 'besting vs nesting',
    description: 'Commonly confused words: besting and nesting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbesting\b/gi,
    message: 'Check: "besting" ("besting") vs "nesting" ("nesting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nesting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bewitched_bewitches_996',
    name: 'bewitched vs bewitches',
    description: 'Commonly confused words: bewitched and bewitches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbewitched\b/gi,
    message: 'Check: "bewitched" ("bewitched") vs "bewitches" ("bewitches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bewitches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_biased_biases_997',
    name: 'biased vs biases',
    description: 'Commonly confused words: biased and biases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbiased\b/gi,
    message: 'Check: "biased" ("biased") vs "biases" ("biases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'biases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bib_big_998',
    name: 'bib vs big',
    description: 'Commonly confused words: bib and big',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbib\b/gi,
    message: 'Check: "bib" ("bib") vs "big" ("big"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'big';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bicycled_bicycles_999',
    name: 'bicycled vs bicycles',
    description: 'Commonly confused words: bicycled and bicycles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbicycled\b/gi,
    message: 'Check: "bicycled" ("bicycled") vs "bicycles" ("bicycles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bicycles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_biddies_buddies_1000',
    name: 'biddies vs buddies',
    description: 'Commonly confused words: biddies and buddies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbiddies\b/gi,
    message: 'Check: "biddies" ("biddies") vs "buddies" ("buddies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'buddies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bidding_budding_1001',
    name: 'bidding vs budding',
    description: 'Commonly confused words: bidding and budding',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbidding\b/gi,
    message: 'Check: "bidding" ("bidding") vs "budding" ("budding"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'budding';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_biddy_giddy_1002',
    name: 'biddy vs giddy',
    description: 'Commonly confused words: biddy and giddy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbiddy\b/gi,
    message: 'Check: "biddy" ("biddy") vs "giddy" ("giddy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'giddy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bides_bodes_1003',
    name: 'bides vs bodes',
    description: 'Commonly confused words: bides and bodes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbides\b/gi,
    message: 'Check: "bides" ("bides") vs "bodes" ("bodes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bodes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_biff_buff_1004',
    name: 'biff vs buff',
    description: 'Commonly confused words: biff and buff',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbiff\b/gi,
    message: 'Check: "biff" ("biff") vs "buff" ("buff"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'buff';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bifurcated_bifurcates_1005',
    name: 'bifurcated vs bifurcates',
    description: 'Commonly confused words: bifurcated and bifurcates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbifurcated\b/gi,
    message: 'Check: "bifurcated" ("bifurcated") vs "bifurcates" ("bifurcates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bifurcates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_biggies_buggies_1006',
    name: 'biggies vs buggies',
    description: 'Commonly confused words: biggies and buggies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbiggies\b/gi,
    message: 'Check: "biggies" ("biggies") vs "buggies" ("buggies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'buggies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_biked_bikes_1007',
    name: 'biked vs bikes',
    description: 'Commonly confused words: biked and bikes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbiked\b/gi,
    message: 'Check: "biked" ("biked") vs "bikes" ("bikes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bikes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bile_bike_1008',
    name: 'bile vs bike',
    description: 'Commonly confused words: bile and bike',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbile\b/gi,
    message: 'Check: "bile" ("bile") vs "bike" ("bike"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bike';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bilges_bulges_1009',
    name: 'bilges vs bulges',
    description: 'Commonly confused words: bilges and bulges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbilges\b/gi,
    message: 'Check: "bilges" ("bilges") vs "bulges" ("bulges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bulges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bilk_bulk_1010',
    name: 'bilk vs bulk',
    description: 'Commonly confused words: bilk and bulk',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbilk\b/gi,
    message: 'Check: "bilk" ("bilk") vs "bulk" ("bulk"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bulk';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bilked_billed_1011',
    name: 'bilked vs billed',
    description: 'Commonly confused words: bilked and billed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbilked\b/gi,
    message: 'Check: "bilked" ("bilked") vs "billed" ("billed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'billed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bilked_bulked_1012',
    name: 'bilked vs bulked',
    description: 'Commonly confused words: bilked and bulked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbilked\b/gi,
    message: 'Check: "bilked" ("bilked") vs "bulked" ("bulked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bulked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bilking_billing_1013',
    name: 'bilking vs billing',
    description: 'Commonly confused words: bilking and billing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbilking\b/gi,
    message: 'Check: "bilking" ("bilking") vs "billing" ("billing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'billing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bilking_bulking_1014',
    name: 'bilking vs bulking',
    description: 'Commonly confused words: bilking and bulking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbilking\b/gi,
    message: 'Check: "bilking" ("bilking") vs "bulking" ("bulking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bulking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_billed_bulled_1015',
    name: 'billed vs bulled',
    description: 'Commonly confused words: billed and bulled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbilled\b/gi,
    message: 'Check: "billed" ("billed") vs "bulled" ("bulled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bulled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_billion_bullion_1016',
    name: 'billion vs bullion',
    description: 'Commonly confused words: billion and bullion',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbillion\b/gi,
    message: 'Check: "billion" ("billion") vs "bullion" ("bullion"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bullion';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_billion_gillion_1017',
    name: 'billion vs gillion',
    description: 'Commonly confused words: billion and gillion',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbillion\b/gi,
    message: 'Check: "billion" ("billion") vs "gillion" ("gillion"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gillion';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_binds_bonds_1018',
    name: 'binds vs bonds',
    description: 'Commonly confused words: binds and bonds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbinds\b/gi,
    message: 'Check: "binds" ("binds") vs "bonds" ("bonds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bonds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bins_bind_1019',
    name: 'bins vs bind',
    description: 'Commonly confused words: bins and bind',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbins\b/gi,
    message: 'Check: "bins" ("bins") vs "bind" ("bind"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bind';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_biodegraded_biodegrades_1020',
    name: 'biodegraded vs biodegrades',
    description: 'Commonly confused words: biodegraded and biodegrades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbiodegraded\b/gi,
    message: 'Check: "biodegraded" ("biodegraded") vs "biodegrades" ("biodegrades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'biodegrades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_biol_bill_1021',
    name: 'biol vs bill',
    description: 'Commonly confused words: biol and bill',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbiol\b/gi,
    message: 'Check: "biol" ("biol") vs "bill" ("bill"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bill';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_biol_viol_1022',
    name: 'biol vs viol',
    description: 'Commonly confused words: biol and viol',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbiol\b/gi,
    message: 'Check: "biol" ("biol") vs "viol" ("viol"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'viol';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_biopsied_biopsies_1023',
    name: 'biopsied vs biopsies',
    description: 'Commonly confused words: biopsied and biopsies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbiopsied\b/gi,
    message: 'Check: "biopsied" ("biopsied") vs "biopsies" ("biopsies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'biopsies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_birching_bitching_1024',
    name: 'birching vs bitching',
    description: 'Commonly confused words: birching and bitching',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbirching\b/gi,
    message: 'Check: "birching" ("birching") vs "bitching" ("bitching"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bitching';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_birder_girder_1025',
    name: 'birder vs girder',
    description: 'Commonly confused words: birder and girder',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbirder\b/gi,
    message: 'Check: "birder" ("birder") vs "girder" ("girder"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'girder';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_birding_girding_1026',
    name: 'birding vs girding',
    description: 'Commonly confused words: birding and girding',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbirding\b/gi,
    message: 'Check: "birding" ("birding") vs "girding" ("girding"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'girding';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_birds_girds_1027',
    name: 'birds vs girds',
    description: 'Commonly confused words: birds and girds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbirds\b/gi,
    message: 'Check: "birds" ("birds") vs "girds" ("girds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'girds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_births_girths_1028',
    name: 'births vs girths',
    description: 'Commonly confused words: births and girths',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbirths\b/gi,
    message: 'Check: "births" ("births") vs "girths" ("girths"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'girths';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bitch_butch_1029',
    name: 'bitch vs butch',
    description: 'Commonly confused words: bitch and butch',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbitch\b/gi,
    message: 'Check: "bitch" ("bitch") vs "butch" ("butch"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'butch';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bitched_bitches_1030',
    name: 'bitched vs bitches',
    description: 'Commonly confused words: bitched and bitches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbitched\b/gi,
    message: 'Check: "bitched" ("bitched") vs "bitches" ("bitches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bitches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bitched_botched_1031',
    name: 'bitched vs botched',
    description: 'Commonly confused words: bitched and botched',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbitched\b/gi,
    message: 'Check: "bitched" ("bitched") vs "botched" ("botched"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'botched';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bitching_botching_1032',
    name: 'bitching vs botching',
    description: 'Commonly confused words: bitching and botching',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbitching\b/gi,
    message: 'Check: "bitching" ("bitching") vs "botching" ("botching"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'botching';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bitter_butter_1033',
    name: 'bitter vs butter',
    description: 'Commonly confused words: bitter and butter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbitter\b/gi,
    message: 'Check: "bitter" ("bitter") vs "butter" ("butter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'butter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blanched_blanches_1034',
    name: 'blanched vs blanches',
    description: 'Commonly confused words: blanched and blanches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblanched\b/gi,
    message: 'Check: "blanched" ("blanched") vs "blanches" ("blanches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'blanches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bland_gland_1035',
    name: 'bland vs gland',
    description: 'Commonly confused words: bland and gland',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbland\b/gi,
    message: 'Check: "bland" ("bland") vs "gland" ("gland"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gland';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blared_glared_1036',
    name: 'blared vs glared',
    description: 'Commonly confused words: blared and glared',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblared\b/gi,
    message: 'Check: "blared" ("blared") vs "glared" ("glared"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glared';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blast_boast_1037',
    name: 'blast vs boast',
    description: 'Commonly confused words: blast and boast',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblast\b/gi,
    message: 'Check: "blast" ("blast") vs "boast" ("boast"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'boast';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blasted_boasted_1038',
    name: 'blasted vs boasted',
    description: 'Commonly confused words: blasted and boasted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblasted\b/gi,
    message: 'Check: "blasted" ("blasted") vs "boasted" ("boasted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'boasted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blasting_boasting_1039',
    name: 'blasting vs boasting',
    description: 'Commonly confused words: blasting and boasting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblasting\b/gi,
    message: 'Check: "blasting" ("blasting") vs "boasting" ("boasting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'boasting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blasts_boasts_1040',
    name: 'blasts vs boasts',
    description: 'Commonly confused words: blasts and boasts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblasts\b/gi,
    message: 'Check: "blasts" ("blasts") vs "boasts" ("boasts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'boasts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blazed_glazed_1041',
    name: 'blazed vs glazed',
    description: 'Commonly confused words: blazed and glazed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblazed\b/gi,
    message: 'Check: "blazed" ("blazed") vs "glazed" ("glazed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glazed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blazing_glazing_1042',
    name: 'blazing vs glazing',
    description: 'Commonly confused words: blazing and glazing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblazing\b/gi,
    message: 'Check: "blazing" ("blazing") vs "glazing" ("glazing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glazing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bleached_bleaches_1043',
    name: 'bleached vs bleaches',
    description: 'Commonly confused words: bleached and bleaches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbleached\b/gi,
    message: 'Check: "bleached" ("bleached") vs "bleaches" ("bleaches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bleaches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blemished_blemishes_1044',
    name: 'blemished vs blemishes',
    description: 'Commonly confused words: blemished and blemishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblemished\b/gi,
    message: 'Check: "blemished" ("blemished") vs "blemishes" ("blemishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'blemishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blinders_blunders_1045',
    name: 'blinders vs blunders',
    description: 'Commonly confused words: blinders and blunders',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblinders\b/gi,
    message: 'Check: "blinders" ("blinders") vs "blunders" ("blunders"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'blunders';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blinds_blonds_1046',
    name: 'blinds vs blonds',
    description: 'Commonly confused words: blinds and blonds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblinds\b/gi,
    message: 'Check: "blinds" ("blinds") vs "blonds" ("blonds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'blonds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blister_bluster_1047',
    name: 'blister vs bluster',
    description: 'Commonly confused words: blister and bluster',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblister\b/gi,
    message: 'Check: "blister" ("blister") vs "bluster" ("bluster"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bluster';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blistering_blustering_1048',
    name: 'blistering vs blustering',
    description: 'Commonly confused words: blistering and blustering',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblistering\b/gi,
    message: 'Check: "blistering" ("blistering") vs "blustering" ("blustering"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'blustering';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blisters_glisters_1049',
    name: 'blisters vs glisters',
    description: 'Commonly confused words: blisters and glisters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblisters\b/gi,
    message: 'Check: "blisters" ("blisters") vs "glisters" ("glisters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glisters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blitzed_blitzes_1050',
    name: 'blitzed vs blitzes',
    description: 'Commonly confused words: blitzed and blitzes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblitzed\b/gi,
    message: 'Check: "blitzed" ("blitzed") vs "blitzes" ("blitzes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'blitzes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bloated_gloated_1051',
    name: 'bloated vs gloated',
    description: 'Commonly confused words: bloated and gloated',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbloated\b/gi,
    message: 'Check: "bloated" ("bloated") vs "gloated" ("gloated"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gloated';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bloating_gloating_1052',
    name: 'bloating vs gloating',
    description: 'Commonly confused words: bloating and gloating',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbloating\b/gi,
    message: 'Check: "bloating" ("bloating") vs "gloating" ("gloating"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gloating';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bloats_gloats_1053',
    name: 'bloats vs gloats',
    description: 'Commonly confused words: bloats and gloats',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbloats\b/gi,
    message: 'Check: "bloats" ("bloats") vs "gloats" ("gloats"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gloats';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blob_blog_1054',
    name: 'blob vs blog',
    description: 'Commonly confused words: blob and blog',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblob\b/gi,
    message: 'Check: "blob" ("blob") vs "blog" ("blog"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'blog';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blog_blot_1055',
    name: 'blog vs blot',
    description: 'Commonly confused words: blog and blot',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblog\b/gi,
    message: 'Check: "blog" ("blog") vs "blot" ("blot"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'blot';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blot_boot_1056',
    name: 'blot vs boot',
    description: 'Commonly confused words: blot and boot',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblot\b/gi,
    message: 'Check: "blot" ("blot") vs "boot" ("boot"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'boot';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blotched_blotches_1057',
    name: 'blotched vs blotches',
    description: 'Commonly confused words: blotched and blotches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblotched\b/gi,
    message: 'Check: "blotched" ("blotched") vs "blotches" ("blotches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'blotches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blower_glower_1058',
    name: 'blower vs glower',
    description: 'Commonly confused words: blower and glower',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblower\b/gi,
    message: 'Check: "blower" ("blower") vs "glower" ("glower"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glower';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blowing_glowing_1059',
    name: 'blowing vs glowing',
    description: 'Commonly confused words: blowing and glowing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblowing\b/gi,
    message: 'Check: "blowing" ("blowing") vs "glowing" ("glowing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glowing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bluets_blurts_1060',
    name: 'bluets vs blurts',
    description: 'Commonly confused words: bluets and blurts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbluets\b/gi,
    message: 'Check: "bluets" ("bluets") vs "blurts" ("blurts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'blurts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bluing_gluing_1061',
    name: 'bluing vs gluing',
    description: 'Commonly confused words: bluing and gluing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbluing\b/gi,
    message: 'Check: "bluing" ("bluing") vs "gluing" ("gluing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gluing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blunder_blinder_1062',
    name: 'blunder vs blinder',
    description: 'Commonly confused words: blunder and blinder',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblunder\b/gi,
    message: 'Check: "blunder" ("blunder") vs "blinder" ("blinder"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'blinder';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blunder_bounder_1063',
    name: 'blunder vs bounder',
    description: 'Commonly confused words: blunder and bounder',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblunder\b/gi,
    message: 'Check: "blunder" ("blunder") vs "bounder" ("bounder"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bounder';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blunders_bounders_1064',
    name: 'blunders vs bounders',
    description: 'Commonly confused words: blunders and bounders',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblunders\b/gi,
    message: 'Check: "blunders" ("blunders") vs "bounders" ("bounders"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bounders';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blurred_blurted_1065',
    name: 'blurred vs blurted',
    description: 'Commonly confused words: blurred and blurted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblurred\b/gi,
    message: 'Check: "blurred" ("blurred") vs "blurted" ("blurted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'blurted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_blurring_blurting_1066',
    name: 'blurring vs blurting',
    description: 'Commonly confused words: blurring and blurting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bblurring\b/gi,
    message: 'Check: "blurring" ("blurring") vs "blurting" ("blurting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'blurting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bobbled_gobbled_1067',
    name: 'bobbled vs gobbled',
    description: 'Commonly confused words: bobbled and gobbled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbobbled\b/gi,
    message: 'Check: "bobbled" ("bobbled") vs "gobbled" ("gobbled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gobbled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_boding_biding_1068',
    name: 'boding vs biding',
    description: 'Commonly confused words: boding and biding',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bboding\b/gi,
    message: 'Check: "boding" ("boding") vs "biding" ("biding"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'biding';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_boga_bogs_1069',
    name: 'boga vs bogs',
    description: 'Commonly confused words: boga and bogs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bboga\b/gi,
    message: 'Check: "boga" ("boga") vs "bogs" ("bogs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bogs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_boggled_boggles_1070',
    name: 'boggled vs boggles',
    description: 'Commonly confused words: boggled and boggles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bboggled\b/gi,
    message: 'Check: "boggled" ("boggled") vs "boggles" ("boggles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'boggles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_boggled_goggled_1071',
    name: 'boggled vs goggled',
    description: 'Commonly confused words: boggled and goggled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bboggled\b/gi,
    message: 'Check: "boggled" ("boggled") vs "goggled" ("goggled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'goggled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_boggles_goggles_1072',
    name: 'boggles vs goggles',
    description: 'Commonly confused words: boggles and goggles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bboggles\b/gi,
    message: 'Check: "boggles" ("boggles") vs "goggles" ("goggles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'goggles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bolster_booster_1073',
    name: 'bolster vs booster',
    description: 'Commonly confused words: bolster and booster',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbolster\b/gi,
    message: 'Check: "bolster" ("bolster") vs "booster" ("booster"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'booster';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bolsters_boosters_1074',
    name: 'bolsters vs boosters',
    description: 'Commonly confused words: bolsters and boosters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbolsters\b/gi,
    message: 'Check: "bolsters" ("bolsters") vs "boosters" ("boosters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'boosters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bolted_booted_1075',
    name: 'bolted vs booted',
    description: 'Commonly confused words: bolted and booted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbolted\b/gi,
    message: 'Check: "bolted" ("bolted") vs "booted" ("booted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'booted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bonds_bones_1076',
    name: 'bonds vs bones',
    description: 'Commonly confused words: bonds and bones',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbonds\b/gi,
    message: 'Check: "bonds" ("bonds") vs "bones" ("bones"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bones';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_boobies_boonies_1077',
    name: 'boobies vs boonies',
    description: 'Commonly confused words: boobies and boonies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bboobies\b/gi,
    message: 'Check: "boobies" ("boobies") vs "boonies" ("boonies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'boonies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_boodle_noodle_1078',
    name: 'boodle vs noodle',
    description: 'Commonly confused words: boodle and noodle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bboodle\b/gi,
    message: 'Check: "boodle" ("boodle") vs "noodle" ("noodle"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'noodle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_booms_boons_1079',
    name: 'booms vs boons',
    description: 'Commonly confused words: booms and boons',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbooms\b/gi,
    message: 'Check: "booms" ("booms") vs "boons" ("boons"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'boons';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_booted_bootee_1080',
    name: 'booted vs bootee',
    description: 'Commonly confused words: booted and bootee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbooted\b/gi,
    message: 'Check: "booted" ("booted") vs "bootee" ("bootee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bootee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bored_bores_1081',
    name: 'bored vs bores',
    description: 'Commonly confused words: bored and bores',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbored\b/gi,
    message: 'Check: "bored" (uninterested) vs "bores" ("bores"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bores';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bossed_bosses_1082',
    name: 'bossed vs bosses',
    description: 'Commonly confused words: bossed and bosses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbossed\b/gi,
    message: 'Check: "bossed" ("bossed") vs "bosses" ("bosses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bosses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_botched_botches_1083',
    name: 'botched vs botches',
    description: 'Commonly confused words: botched and botches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbotched\b/gi,
    message: 'Check: "botched" ("botched") vs "botches" ("botches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'botches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bottled_bottles_1084',
    name: 'bottled vs bottles',
    description: 'Commonly confused words: bottled and bottles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbottled\b/gi,
    message: 'Check: "bottled" ("bottled") vs "bottles" ("bottles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bottles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bounced_bounded_1085',
    name: 'bounced vs bounded',
    description: 'Commonly confused words: bounced and bounded',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbounced\b/gi,
    message: 'Check: "bounced" ("bounced") vs "bounded" ("bounded"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bounded';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bouncer_bounder_1086',
    name: 'bouncer vs bounder',
    description: 'Commonly confused words: bouncer and bounder',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbouncer\b/gi,
    message: 'Check: "bouncer" ("bouncer") vs "bounder" ("bounder"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bounder';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bouncers_bounders_1087',
    name: 'bouncers vs bounders',
    description: 'Commonly confused words: bouncers and bounders',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbouncers\b/gi,
    message: 'Check: "bouncers" ("bouncers") vs "bounders" ("bounders"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bounders';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bouncing_bounding_1088',
    name: 'bouncing vs bounding',
    description: 'Commonly confused words: bouncing and bounding',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbouncing\b/gi,
    message: 'Check: "bouncing" ("bouncing") vs "bounding" ("bounding"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bounding';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bout_gout_1089',
    name: 'bout vs gout',
    description: 'Commonly confused words: bout and gout',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbout\b/gi,
    message: 'Check: "bout" ("bout") vs "gout" ("gout"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gout';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bowels_vowels_1090',
    name: 'bowels vs vowels',
    description: 'Commonly confused words: bowels and vowels',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbowels\b/gi,
    message: 'Check: "bowels" ("bowels") vs "vowels" ("vowels"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vowels';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_braced_braces_1091',
    name: 'braced vs braces',
    description: 'Commonly confused words: braced and braces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbraced\b/gi,
    message: 'Check: "braced" ("braced") vs "braces" ("braces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'braces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_braced_braved_1092',
    name: 'braced vs braved',
    description: 'Commonly confused words: braced and braved',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbraced\b/gi,
    message: 'Check: "braced" ("braced") vs "braved" ("braved"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'braved';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_braced_graced_1093',
    name: 'braced vs graced',
    description: 'Commonly confused words: braced and graced',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbraced\b/gi,
    message: 'Check: "braced" ("braced") vs "graced" ("graced"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'graced';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bracer_braver_1094',
    name: 'bracer vs braver',
    description: 'Commonly confused words: bracer and braver',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbracer\b/gi,
    message: 'Check: "bracer" ("bracer") vs "braver" ("braver"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'braver';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_braces_braves_1095',
    name: 'braces vs braves',
    description: 'Commonly confused words: braces and braves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbraces\b/gi,
    message: 'Check: "braces" ("braces") vs "braves" ("braves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'braves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bracing_braving_1096',
    name: 'bracing vs braving',
    description: 'Commonly confused words: bracing and braving',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbracing\b/gi,
    message: 'Check: "bracing" ("bracing") vs "braving" ("braving"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'braving';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bracing_gracing_1097',
    name: 'bracing vs gracing',
    description: 'Commonly confused words: bracing and gracing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbracing\b/gi,
    message: 'Check: "bracing" ("bracing") vs "gracing" ("gracing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gracing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brad_bead_1098',
    name: 'brad vs bead',
    description: 'Commonly confused words: brad and bead',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrad\b/gi,
    message: 'Check: "brad" ("brad") vs "bead" ("bead"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bead';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brads_beads_1099',
    name: 'brads vs beads',
    description: 'Commonly confused words: brads and beads',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrads\b/gi,
    message: 'Check: "brads" ("brads") vs "beads" ("beads"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'beads';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brags_brats_1100',
    name: 'brags vs brats',
    description: 'Commonly confused words: brags and brats',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrags\b/gi,
    message: 'Check: "brags" ("brags") vs "brats" ("brats"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brats';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_braided_braised_1101',
    name: 'braided vs braised',
    description: 'Commonly confused words: braided and braised',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbraided\b/gi,
    message: 'Check: "braided" ("braided") vs "braised" ("braised"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'braised';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brained_grained_1102',
    name: 'brained vs grained',
    description: 'Commonly confused words: brained and grained',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrained\b/gi,
    message: 'Check: "brained" ("brained") vs "grained" ("grained"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grained';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brains_grains_1103',
    name: 'brains vs grains',
    description: 'Commonly confused words: brains and grains',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrains\b/gi,
    message: 'Check: "brains" ("brains") vs "grains" ("grains"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grains';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brainy_grainy_1104',
    name: 'brainy vs grainy',
    description: 'Commonly confused words: brainy and grainy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrainy\b/gi,
    message: 'Check: "brainy" ("brainy") vs "grainy" ("grainy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grainy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_branched_branches_1105',
    name: 'branched vs branches',
    description: 'Commonly confused words: branched and branches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbranched\b/gi,
    message: 'Check: "branched" ("branched") vs "branches" ("branches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'branches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brander_grander_1106',
    name: 'brander vs grander',
    description: 'Commonly confused words: brander and grander',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrander\b/gi,
    message: 'Check: "brander" ("brander") vs "grander" ("grander"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grander';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brasses_grasses_1107',
    name: 'brasses vs grasses',
    description: 'Commonly confused words: brasses and grasses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrasses\b/gi,
    message: 'Check: "brasses" ("brasses") vs "grasses" ("grasses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grasses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brassy_grassy_1108',
    name: 'brassy vs grassy',
    description: 'Commonly confused words: brassy and grassy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrassy\b/gi,
    message: 'Check: "brassy" ("brassy") vs "grassy" ("grassy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grassy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brave_brace_1109',
    name: 'brave vs brace',
    description: 'Commonly confused words: brave and brace',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrave\b/gi,
    message: 'Check: "brave" ("brave") vs "brace" ("brace"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brace';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_braved_graved_1110',
    name: 'braved vs graved',
    description: 'Commonly confused words: braved and graved',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbraved\b/gi,
    message: 'Check: "braved" ("braved") vs "graved" ("graved"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'graved';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bravely_gravely_1111',
    name: 'bravely vs gravely',
    description: 'Commonly confused words: bravely and gravely',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbravely\b/gi,
    message: 'Check: "bravely" ("bravely") vs "gravely" ("gravely"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gravely';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bravest_gravest_1112',
    name: 'bravest vs gravest',
    description: 'Commonly confused words: bravest and gravest',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbravest\b/gi,
    message: 'Check: "bravest" ("bravest") vs "gravest" ("gravest"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gravest';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_braving_graving_1113',
    name: 'braving vs graving',
    description: 'Commonly confused words: braving and graving',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbraving\b/gi,
    message: 'Check: "braving" ("braving") vs "graving" ("graving"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'graving';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_braying_graying_1114',
    name: 'braying vs graying',
    description: 'Commonly confused words: braying and graying',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbraying\b/gi,
    message: 'Check: "braying" ("braying") vs "graying" ("graying"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'graying';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brazing_grazing_1115',
    name: 'brazing vs grazing',
    description: 'Commonly confused words: brazing and grazing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrazing\b/gi,
    message: 'Check: "brazing" ("brazing") vs "grazing" ("grazing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grazing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_breezed_breezes_1116',
    name: 'breezed vs breezes',
    description: 'Commonly confused words: breezed and breezes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbreezed\b/gi,
    message: 'Check: "breezed" ("breezed") vs "breezes" ("breezes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'breezes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brew_grew_1117',
    name: 'brew vs grew',
    description: 'Commonly confused words: brew and grew',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrew\b/gi,
    message: 'Check: "brew" ("brew") vs "grew" ("grew"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grew';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bribe_brine_1118',
    name: 'bribe vs brine',
    description: 'Commonly confused words: bribe and brine',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbribe\b/gi,
    message: 'Check: "bribe" ("bribe") vs "brine" ("brine"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brine';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bribed_bribes_1119',
    name: 'bribed vs bribes',
    description: 'Commonly confused words: bribed and bribes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbribed\b/gi,
    message: 'Check: "bribed" ("bribed") vs "bribes" ("bribes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bribes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bridled_bridles_1120',
    name: 'bridled vs bridles',
    description: 'Commonly confused words: bridled and bridles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbridled\b/gi,
    message: 'Check: "bridled" ("bridled") vs "bridles" ("bridles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bridles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brief_grief_1121',
    name: 'brief vs grief',
    description: 'Commonly confused words: brief and grief',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrief\b/gi,
    message: 'Check: "brief" ("brief") vs "grief" ("grief"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grief';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_briefs_griefs_1122',
    name: 'briefs vs griefs',
    description: 'Commonly confused words: briefs and griefs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbriefs\b/gi,
    message: 'Check: "briefs" ("briefs") vs "griefs" ("griefs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'griefs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brill_grill_1123',
    name: 'brill vs grill',
    description: 'Commonly confused words: brill and grill',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrill\b/gi,
    message: 'Check: "brill" ("brill") vs "grill" ("grill"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grill';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brim_grim_1124',
    name: 'brim vs grim',
    description: 'Commonly confused words: brim and grim',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrim\b/gi,
    message: 'Check: "brim" ("brim") vs "grim" ("grim"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grim';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brioches_brooches_1125',
    name: 'brioches vs brooches',
    description: 'Commonly confused words: brioches and brooches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrioches\b/gi,
    message: 'Check: "brioches" ("brioches") vs "brooches" ("brooches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brooches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bristled_bristles_1126',
    name: 'bristled vs bristles',
    description: 'Commonly confused words: bristled and bristles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbristled\b/gi,
    message: 'Check: "bristled" ("bristled") vs "bristles" ("bristles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bristles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_broached_broaches_1127',
    name: 'broached vs broaches',
    description: 'Commonly confused words: broached and broaches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbroached\b/gi,
    message: 'Check: "broached" ("broached") vs "broaches" ("broaches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'broaches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_broadsided_broadsides_1128',
    name: 'broadsided vs broadsides',
    description: 'Commonly confused words: broadsided and broadsides',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbroadsided\b/gi,
    message: 'Check: "broadsided" ("broadsided") vs "broadsides" ("broadsides"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'broadsides';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brocaded_brocades_1129',
    name: 'brocaded vs brocades',
    description: 'Commonly confused words: brocaded and brocades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrocaded\b/gi,
    message: 'Check: "brocaded" ("brocaded") vs "brocades" ("brocades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brocades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bronzed_bronzes_1130',
    name: 'bronzed vs bronzes',
    description: 'Commonly confused words: bronzed and bronzes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbronzed\b/gi,
    message: 'Check: "bronzed" ("bronzed") vs "bronzes" ("bronzes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bronzes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brooms_grooms_1131',
    name: 'brooms vs grooms',
    description: 'Commonly confused words: brooms and grooms',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrooms\b/gi,
    message: 'Check: "brooms" ("brooms") vs "grooms" ("grooms"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grooms';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bros_brow_1132',
    name: 'bros vs brow',
    description: 'Commonly confused words: bros and brow',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbros\b/gi,
    message: 'Check: "bros" ("bros") vs "brow" ("brow"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brow';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_browsed_browses_1133',
    name: 'browsed vs browses',
    description: 'Commonly confused words: browsed and browses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrowsed\b/gi,
    message: 'Check: "browsed" ("browsed") vs "browses" ("browses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'browses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bruised_bruises_1134',
    name: 'bruised vs bruises',
    description: 'Commonly confused words: bruised and bruises',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbruised\b/gi,
    message: 'Check: "bruised" ("bruised") vs "bruises" ("bruises"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bruises';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brunt_grunt_1135',
    name: 'brunt vs grunt',
    description: 'Commonly confused words: brunt and grunt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrunt\b/gi,
    message: 'Check: "brunt" ("brunt") vs "grunt" ("grunt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grunt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_brutalized_brutalizes_1136',
    name: 'brutalized vs brutalizes',
    description: 'Commonly confused words: brutalized and brutalizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbrutalized\b/gi,
    message: 'Check: "brutalized" ("brutalized") vs "brutalizes" ("brutalizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'brutalizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bub_bib_1137',
    name: 'bub vs bib',
    description: 'Commonly confused words: bub and bib',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbub\b/gi,
    message: 'Check: "bub" ("bub") vs "bib" ("bib"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bib';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bubbled_bubbles_1138',
    name: 'bubbled vs bubbles',
    description: 'Commonly confused words: bubbled and bubbles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbubbled\b/gi,
    message: 'Check: "bubbled" ("bubbled") vs "bubbles" ("bubbles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bubbles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_buckled_buckles_1139',
    name: 'buckled vs buckles',
    description: 'Commonly confused words: buckled and buckles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbuckled\b/gi,
    message: 'Check: "buckled" ("buckled") vs "buckles" ("buckles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'buckles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_budging_nudging_1140',
    name: 'budging vs nudging',
    description: 'Commonly confused words: budging and nudging',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbudging\b/gi,
    message: 'Check: "budging" ("budging") vs "nudging" ("nudging"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nudging';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_buds_bids_1141',
    name: 'buds vs bids',
    description: 'Commonly confused words: buds and bids',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbuds\b/gi,
    message: 'Check: "buds" ("buds") vs "bids" ("bids"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bids';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_buffaloed_buffaloes_1142',
    name: 'buffaloed vs buffaloes',
    description: 'Commonly confused words: buffaloed and buffaloes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbuffaloed\b/gi,
    message: 'Check: "buffaloed" ("buffaloed") vs "buffaloes" ("buffaloes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'buffaloes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_buffered_buffeted_1143',
    name: 'buffered vs buffeted',
    description: 'Commonly confused words: buffered and buffeted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbuffered\b/gi,
    message: 'Check: "buffered" ("buffered") vs "buffeted" ("buffeted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'buffeted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_buffering_buffeting_1144',
    name: 'buffering vs buffeting',
    description: 'Commonly confused words: buffering and buffeting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbuffering\b/gi,
    message: 'Check: "buffering" ("buffering") vs "buffeting" ("buffeting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'buffeting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_buffers_buffets_1145',
    name: 'buffers vs buffets',
    description: 'Commonly confused words: buffers and buffets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbuffers\b/gi,
    message: 'Check: "buffers" ("buffers") vs "buffets" ("buffets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'buffets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_builders_guilders_1146',
    name: 'builders vs guilders',
    description: 'Commonly confused words: builders and guilders',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbuilders\b/gi,
    message: 'Check: "builders" ("builders") vs "guilders" ("guilders"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'guilders';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_build_guild_1147',
    name: 'build vs guild',
    description: 'Commonly confused words: build and guild',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbuild\b/gi,
    message: 'Check: "build" ("build") vs "guild" ("guild"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'guild';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_builds_guilds_1148',
    name: 'builds vs guilds',
    description: 'Commonly confused words: builds and guilds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbuilds\b/gi,
    message: 'Check: "builds" ("builds") vs "guilds" ("guilds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'guilds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_built_guilt_1149',
    name: 'built vs guilt',
    description: 'Commonly confused words: built and guilt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbuilt\b/gi,
    message: 'Check: "built" ("built") vs "guilt" ("guilt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'guilt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bulged_bulges_1150',
    name: 'bulged vs bulges',
    description: 'Commonly confused words: bulged and bulges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbulged\b/gi,
    message: 'Check: "bulged" ("bulged") vs "bulges" ("bulges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bulges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bulk_bull_1151',
    name: 'bulk vs bull',
    description: 'Commonly confused words: bulk and bull',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbulk\b/gi,
    message: 'Check: "bulk" ("bulk") vs "bull" ("bull"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bull';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bulkhead_bullhead_1152',
    name: 'bulkhead vs bullhead',
    description: 'Commonly confused words: bulkhead and bullhead',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbulkhead\b/gi,
    message: 'Check: "bulkhead" ("bulkhead") vs "bullhead" ("bullhead"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bullhead';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bulkheads_bullheads_1153',
    name: 'bulkheads vs bullheads',
    description: 'Commonly confused words: bulkheads and bullheads',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbulkheads\b/gi,
    message: 'Check: "bulkheads" ("bulkheads") vs "bullheads" ("bullheads"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bullheads';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bulky_bully_1154',
    name: 'bulky vs bully',
    description: 'Commonly confused words: bulky and bully',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbulky\b/gi,
    message: 'Check: "bulky" ("bulky") vs "bully" ("bully"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bully';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bullied_bullies_1155',
    name: 'bullied vs bullies',
    description: 'Commonly confused words: bullied and bullies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbullied\b/gi,
    message: 'Check: "bullied" ("bullied") vs "bullies" ("bullies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bullies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bullies_gullies_1156',
    name: 'bullies vs gullies',
    description: 'Commonly confused words: bullies and gullies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbullies\b/gi,
    message: 'Check: "bullies" ("bullies") vs "gullies" ("gullies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gullies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bummed_gummed_1157',
    name: 'bummed vs gummed',
    description: 'Commonly confused words: bummed and gummed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbummed\b/gi,
    message: 'Check: "bummed" ("bummed") vs "gummed" ("gummed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gummed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bunched_bunches_1158',
    name: 'bunched vs bunches',
    description: 'Commonly confused words: bunched and bunches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbunched\b/gi,
    message: 'Check: "bunched" ("bunched") vs "bunches" ("bunches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bunches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bundled_bundles_1159',
    name: 'bundled vs bundles',
    description: 'Commonly confused words: bundled and bundles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbundled\b/gi,
    message: 'Check: "bundled" ("bundled") vs "bundles" ("bundles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bundles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bungled_bungles_1160',
    name: 'bungled vs bungles',
    description: 'Commonly confused words: bungled and bungles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbungled\b/gi,
    message: 'Check: "bungled" ("bungled") vs "bungles" ("bungles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bungles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_burble_burgle_1161',
    name: 'burble vs burgle',
    description: 'Commonly confused words: burble and burgle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bburble\b/gi,
    message: 'Check: "burble" ("burble") vs "burgle" ("burgle"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'burgle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_burbs_burgs_1162',
    name: 'burbs vs burgs',
    description: 'Commonly confused words: burbs and burgs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bburbs\b/gi,
    message: 'Check: "burbs" ("burbs") vs "burgs" ("burgs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'burgs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_burgle_gurgle_1163',
    name: 'burgle vs gurgle',
    description: 'Commonly confused words: burgle and gurgle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bburgle\b/gi,
    message: 'Check: "burgle" ("burgle") vs "gurgle" ("gurgle"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gurgle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_burgling_gurgling_1164',
    name: 'burgling vs gurgling',
    description: 'Commonly confused words: burgling and gurgling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bburgling\b/gi,
    message: 'Check: "burgling" ("burgling") vs "gurgling" ("gurgling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gurgling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_burlesqued_burlesques_1165',
    name: 'burlesqued vs burlesques',
    description: 'Commonly confused words: burlesqued and burlesques',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bburlesqued\b/gi,
    message: 'Check: "burlesqued" ("burlesqued") vs "burlesques" ("burlesques"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'burlesques';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_burros_burrow_1166',
    name: 'burros vs burrow',
    description: 'Commonly confused words: burros and burrow',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bburros\b/gi,
    message: 'Check: "burros" ("burros") vs "burrow" ("burrow"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'burrow';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bursae_bursar_1167',
    name: 'bursae vs bursar',
    description: 'Commonly confused words: bursae and bursar',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbursae\b/gi,
    message: 'Check: "bursae" ("bursae") vs "bursar" ("bursar"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bursar';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bus_bis_1168',
    name: 'bus vs bis',
    description: 'Commonly confused words: bus and bis',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbus\b/gi,
    message: 'Check: "bus" ("bus") vs "bis" ("bis"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bis';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_buses_bused_1169',
    name: 'buses vs bused',
    description: 'Commonly confused words: buses and bused',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbuses\b/gi,
    message: 'Check: "buses" ("buses") vs "bused" ("bused"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bused';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bushed_bushes_1170',
    name: 'bushed vs bushes',
    description: 'Commonly confused words: bushed and bushes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbushed\b/gi,
    message: 'Check: "bushed" ("bushed") vs "bushes" ("bushes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bushes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bushed_gushed_1171',
    name: 'bushed vs gushed',
    description: 'Commonly confused words: bushed and gushed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbushed\b/gi,
    message: 'Check: "bushed" ("bushed") vs "gushed" ("gushed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gushed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bushing_busying_1172',
    name: 'bushing vs busying',
    description: 'Commonly confused words: bushing and busying',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbushing\b/gi,
    message: 'Check: "bushing" ("bushing") vs "busying" ("busying"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'busying';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_bushy_gushy_1173',
    name: 'bushy vs gushy',
    description: 'Commonly confused words: bushy and gushy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbushy\b/gi,
    message: 'Check: "bushy" ("bushy") vs "gushy" ("gushy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gushy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_busied_busies_1174',
    name: 'busied vs busies',
    description: 'Commonly confused words: busied and busies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbusied\b/gi,
    message: 'Check: "busied" ("busied") vs "busies" ("busies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'busies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_buss_buds_1175',
    name: 'buss vs buds',
    description: 'Commonly confused words: buss and buds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbuss\b/gi,
    message: 'Check: "buss" ("buss") vs "buds" ("buds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'buds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_busty_gusty_1176',
    name: 'busty vs gusty',
    description: 'Commonly confused words: busty and gusty',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbusty\b/gi,
    message: 'Check: "busty" ("busty") vs "gusty" ("gusty"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gusty';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_busy_bush_1177',
    name: 'busy vs bush',
    description: 'Commonly confused words: busy and bush',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbusy\b/gi,
    message: 'Check: "busy" ("busy") vs "bush" ("bush"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bush';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_busy_bust_1178',
    name: 'busy vs bust',
    description: 'Commonly confused words: busy and bust',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbusy\b/gi,
    message: 'Check: "busy" ("busy") vs "bust" ("bust"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bust';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_busying_busting_1179',
    name: 'busying vs busting',
    description: 'Commonly confused words: busying and busting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbusying\b/gi,
    message: 'Check: "busying" ("busying") vs "busting" ("busting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'busting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_butted_buttes_1180',
    name: 'butted vs buttes',
    description: 'Commonly confused words: butted and buttes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbutted\b/gi,
    message: 'Check: "butted" ("butted") vs "buttes" ("buttes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'buttes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_butted_gutted_1181',
    name: 'butted vs gutted',
    description: 'Commonly confused words: butted and gutted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbutted\b/gi,
    message: 'Check: "butted" ("butted") vs "gutted" ("gutted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gutted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_buttering_guttering_1182',
    name: 'buttering vs guttering',
    description: 'Commonly confused words: buttering and guttering',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbuttering\b/gi,
    message: 'Check: "buttering" ("buttering") vs "guttering" ("guttering"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'guttering';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_buttressed_buttresses_1183',
    name: 'buttressed vs buttresses',
    description: 'Commonly confused words: buttressed and buttresses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbuttressed\b/gi,
    message: 'Check: "buttressed" ("buttressed") vs "buttresses" ("buttresses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'buttresses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_buying_guying_1184',
    name: 'buying vs guying',
    description: 'Commonly confused words: buying and guying',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbuying\b/gi,
    message: 'Check: "buying" ("buying") vs "guying" ("guying"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'guying';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_byre_byte_1185',
    name: 'byre vs byte',
    description: 'Commonly confused words: byre and byte',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbyre\b/gi,
    message: 'Check: "byre" ("byre") vs "byte" ("byte"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'byte';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_byres_bytes_1186',
    name: 'byres vs bytes',
    description: 'Commonly confused words: byres and bytes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bbyres\b/gi,
    message: 'Check: "byres" ("byres") vs "bytes" ("bytes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bytes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
