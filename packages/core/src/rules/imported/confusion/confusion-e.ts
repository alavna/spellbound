/**
 * Auto-generated confusion rules (E)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.491Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesE: GrammarRule[] = [
  {
    id: 'confusion_except_accept_4',
    name: 'except vs accept',
    description: 'Commonly confused words: except and accept',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexcept\b/gi,
    message: 'Did you mean "accept"? "except" (excluding or other than) is often confused with "accept" (to receive or agree to).',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'accept';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_editions_additions_10',
    name: 'editions vs additions',
    description: 'Commonly confused words: editions and additions',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beditions\b/gi,
    message: 'Did you mean "additions"? "editions" ("editions") is often confused with "additions" ("additions").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'additions';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_effect_affect_17',
    name: 'effect vs affect',
    description: 'Commonly confused words: effect and affect',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beffect\b/gi,
    message: 'Did you mean "affect"? "effect" (a result (noun) or to cause (verb)) is often confused with "affect" (to influence (verb)).',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'affect';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eight_ate_47',
    name: 'eight vs ate',
    description: 'Commonly confused words: eight and ate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beight\b/gi,
    message: 'Did you mean "ate"? "eight" ("eight") is often confused with "ate" ("ate").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ear_eat_255',
    name: 'ear vs eat',
    description: 'Commonly confused words: ear and eat',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bear\b/gi,
    message: 'Did you mean "eat"? "ear" ("ear") is often confused with "eat" ("eat").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eat';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eat_ear_256',
    name: 'eat vs ear',
    description: 'Commonly confused words: eat and ear',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beat\b/gi,
    message: 'Did you mean "ear"? "eat" ("eat") is often confused with "ear" ("ear").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ear';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_earn_urn_257',
    name: 'earn vs urn',
    description: 'Commonly confused words: earn and urn',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bearn\b/gi,
    message: 'Check: "earn" ("earn") vs "urn" ("urn"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'urn';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_earns_urns_258',
    name: 'earns vs urns',
    description: 'Commonly confused words: earns and urns',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bearns\b/gi,
    message: 'Check: "earns" ("earns") vs "urns" ("urns"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'urns';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ears_eats_259',
    name: 'ears vs eats',
    description: 'Commonly confused words: ears and eats',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bears\b/gi,
    message: 'Check: "ears" ("ears") vs "eats" ("eats"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eats';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_either_ether_260',
    name: 'either vs ether',
    description: 'Commonly confused words: either and ether',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beither\b/gi,
    message: 'Check: "either" ("either") vs "ether" ("ether"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ether';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_elicit_illicit_261',
    name: 'elicit vs illicit',
    description: 'Commonly confused words: elicit and illicit',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\belicit\b/gi,
    message: 'Check: "elicit" ("elicit") vs "illicit" ("illicit"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'illicit';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ellipses_ellipsis_262',
    name: 'ellipses vs ellipsis',
    description: 'Commonly confused words: ellipses and ellipsis',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bellipses\b/gi,
    message: 'Check: "ellipses" ("ellipses") vs "ellipsis" ("ellipsis"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ellipsis';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_elude_illude_263',
    name: 'elude vs illude',
    description: 'Commonly confused words: elude and illude',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\belude\b/gi,
    message: 'Check: "elude" ("elude") vs "illude" ("illude"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'illude';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_em_mi_264',
    name: 'em vs mi',
    description: 'Commonly confused words: em and mi',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bem\b/gi,
    message: 'Check: "em" ("em") vs "mi" ("mi"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mi';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eminent_imminent_265',
    name: 'eminent vs imminent',
    description: 'Commonly confused words: eminent and imminent',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beminent\b/gi,
    message: 'Check: "eminent" ("eminent") vs "imminent" ("imminent"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'imminent';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_emit_omit_266',
    name: 'emit vs omit',
    description: 'Commonly confused words: emit and omit',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bemit\b/gi,
    message: 'Check: "emit" ("emit") vs "omit" ("omit"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'omit';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_emits_omits_267',
    name: 'emits vs omits',
    description: 'Commonly confused words: emits and omits',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bemits\b/gi,
    message: 'Check: "emits" ("emits") vs "omits" ("omits"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'omits';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_emitting_omitting_268',
    name: 'emitting vs omitting',
    description: 'Commonly confused words: emitting and omitting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bemitting\b/gi,
    message: 'Check: "emitting" ("emitting") vs "omitting" ("omitting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'omitting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_emphases_emphasis_269',
    name: 'emphases vs emphasis',
    description: 'Commonly confused words: emphases and emphasis',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bemphases\b/gi,
    message: 'Check: "emphases" ("emphases") vs "emphasis" ("emphasis"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'emphasis';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_endemic_epidemic_270',
    name: 'endemic vs epidemic',
    description: 'Commonly confused words: endemic and epidemic',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bendemic\b/gi,
    message: 'Check: "endemic" ("endemic") vs "epidemic" ("epidemic"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'epidemic';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ensured_insured_271',
    name: 'ensured vs insured',
    description: 'Commonly confused words: ensured and insured',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bensured\b/gi,
    message: 'Check: "ensured" ("ensured") vs "insured" ("insured"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'insured';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_entomology_etymology_272',
    name: 'entomology vs etymology',
    description: 'Commonly confused words: entomology and etymology',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bentomology\b/gi,
    message: 'Check: "entomology" ("entomology") vs "etymology" ("etymology"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'etymology';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_envelop_envelope_273',
    name: 'envelop vs envelope',
    description: 'Commonly confused words: envelop and envelope',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\benvelop\b/gi,
    message: 'Check: "envelop" ("envelop") vs "envelope" ("envelope"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'envelope';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_envelopes_envelops_274',
    name: 'envelopes vs envelops',
    description: 'Commonly confused words: envelopes and envelops',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\benvelopes\b/gi,
    message: 'Check: "envelopes" ("envelopes") vs "envelops" ("envelops"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'envelops';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_epic_epoch_275',
    name: 'epic vs epoch',
    description: 'Commonly confused words: epic and epoch',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bepic\b/gi,
    message: 'Check: "epic" ("epic") vs "epoch" ("epoch"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'epoch';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_epics_epochs_276',
    name: 'epics vs epochs',
    description: 'Commonly confused words: epics and epochs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bepics\b/gi,
    message: 'Check: "epics" ("epics") vs "epochs" ("epochs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'epochs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_epitaph_epithet_277',
    name: 'epitaph vs epithet',
    description: 'Commonly confused words: epitaph and epithet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bepitaph\b/gi,
    message: 'Check: "epitaph" ("epitaph") vs "epithet" ("epithet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'epithet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ere_heir_278',
    name: 'ere vs heir',
    description: 'Commonly confused words: ere and heir',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bere\b/gi,
    message: 'Check: "ere" ("ere") vs "heir" ("heir"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'heir';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_exact_extract_279',
    name: 'exact vs extract',
    description: 'Commonly confused words: exact and extract',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexact\b/gi,
    message: 'Check: "exact" ("exact") vs "extract" ("extract"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'extract';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_except_expect_280',
    name: 'except vs expect',
    description: 'Commonly confused words: except and expect',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexcept\b/gi,
    message: 'Check: "except" (excluding or other than) vs "expect" ("expect"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'expect';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_excepts_expects_281',
    name: 'excepts vs expects',
    description: 'Commonly confused words: excepts and expects',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexcepts\b/gi,
    message: 'Check: "excepts" ("excepts") vs "expects" ("expects"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'expects';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_existing_exiting_282',
    name: 'existing vs exiting',
    description: 'Commonly confused words: existing and exiting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexisting\b/gi,
    message: 'Check: "existing" ("existing") vs "exiting" ("exiting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'exiting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_extend_extent_283',
    name: 'extend vs extent',
    description: 'Commonly confused words: extend and extent',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bextend\b/gi,
    message: 'Check: "extend" ("extend") vs "extent" ("extent"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'extent';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eager_eater_1774',
    name: 'eager vs eater',
    description: 'Commonly confused words: eager and eater',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beager\b/gi,
    message: 'Check: "eager" ("eager") vs "eater" ("eater"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eater';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eared_dared_1775',
    name: 'eared vs dared',
    description: 'Commonly confused words: eared and dared',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beared\b/gi,
    message: 'Check: "eared" ("eared") vs "dared" ("dared"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dared';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_earn_darn_1776',
    name: 'earn vs darn',
    description: 'Commonly confused words: earn and darn',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bearn\b/gi,
    message: 'Check: "earn" ("earn") vs "darn" ("darn"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'darn';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_earner_darner_1777',
    name: 'earner vs darner',
    description: 'Commonly confused words: earner and darner',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bearner\b/gi,
    message: 'Check: "earner" ("earner") vs "darner" ("darner"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'darner';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_earnings_warnings_1778',
    name: 'earnings vs warnings',
    description: 'Commonly confused words: earnings and warnings',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bearnings\b/gi,
    message: 'Check: "earnings" ("earnings") vs "warnings" ("warnings"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'warnings';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_earning_warning_1779',
    name: 'earning vs warning',
    description: 'Commonly confused words: earning and warning',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bearning\b/gi,
    message: 'Check: "earning" ("earning") vs "warning" ("warning"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'warning';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_earring_warring_1780',
    name: 'earring vs warring',
    description: 'Commonly confused words: earring and warring',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bearring\b/gi,
    message: 'Check: "earring" ("earring") vs "warring" ("warring"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'warring';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_east_easy_1781',
    name: 'east vs easy',
    description: 'Commonly confused words: east and easy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beast\b/gi,
    message: 'Check: "east" ("east") vs "easy" ("easy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'easy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eaters_esters_1782',
    name: 'eaters vs esters',
    description: 'Commonly confused words: eaters and esters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beaters\b/gi,
    message: 'Check: "eaters" ("eaters") vs "esters" ("esters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'esters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eccl_excl_1783',
    name: 'eccl vs excl',
    description: 'Commonly confused words: eccl and excl',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beccl\b/gi,
    message: 'Check: "eccl" ("eccl") vs "excl" ("excl"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'excl';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_echoed_echoes_1784',
    name: 'echoed vs echoes',
    description: 'Commonly confused words: echoed and echoes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bechoed\b/gi,
    message: 'Check: "echoed" ("echoed") vs "echoes" ("echoes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'echoes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eclipsed_eclipses_1785',
    name: 'eclipsed vs eclipses',
    description: 'Commonly confused words: eclipsed and eclipses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beclipsed\b/gi,
    message: 'Check: "eclipsed" ("eclipsed") vs "eclipses" ("eclipses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eclipses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_econ_exon_1786',
    name: 'econ vs exon',
    description: 'Commonly confused words: econ and exon',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\becon\b/gi,
    message: 'Check: "econ" ("econ") vs "exon" ("exon"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'exon';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_edged_edges_1787',
    name: 'edged vs edges',
    description: 'Commonly confused words: edged and edges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bedged\b/gi,
    message: 'Check: "edged" ("edged") vs "edges" ("edges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'edges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eider_wider_1788',
    name: 'eider vs wider',
    description: 'Commonly confused words: eider and wider',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beider\b/gi,
    message: 'Check: "eider" ("eider") vs "wider" ("wider"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wider';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eighth_eighty_1789',
    name: 'eighth vs eighty',
    description: 'Commonly confused words: eighth and eighty',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beighth\b/gi,
    message: 'Check: "eighth" ("eighth") vs "eighty" ("eighty"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eighty';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ejaculated_ejaculates_1790',
    name: 'ejaculated vs ejaculates',
    description: 'Commonly confused words: ejaculated and ejaculates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bejaculated\b/gi,
    message: 'Check: "ejaculated" ("ejaculated") vs "ejaculates" ("ejaculates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ejaculates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_elaborated_elaborates_1791',
    name: 'elaborated vs elaborates',
    description: 'Commonly confused words: elaborated and elaborates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\belaborated\b/gi,
    message: 'Check: "elaborated" ("elaborated") vs "elaborates" ("elaborates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'elaborates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_elapsed_elapses_1792',
    name: 'elapsed vs elapses',
    description: 'Commonly confused words: elapsed and elapses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\belapsed\b/gi,
    message: 'Check: "elapsed" ("elapsed") vs "elapses" ("elapses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'elapses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_electrified_electrifies_1793',
    name: 'electrified vs electrifies',
    description: 'Commonly confused words: electrified and electrifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\belectrified\b/gi,
    message: 'Check: "electrified" ("electrified") vs "electrifies" ("electrifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'electrifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_elided_eluded_1794',
    name: 'elided vs eluded',
    description: 'Commonly confused words: elided and eluded',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\belided\b/gi,
    message: 'Check: "elided" ("elided") vs "eluded" ("eluded"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eluded';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_elide_elude_1795',
    name: 'elide vs elude',
    description: 'Commonly confused words: elide and elude',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\belide\b/gi,
    message: 'Check: "elide" ("elide") vs "elude" ("elude"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'elude';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eliding_eluding_1796',
    name: 'eliding vs eluding',
    description: 'Commonly confused words: eliding and eluding',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beliding\b/gi,
    message: 'Check: "eliding" ("eliding") vs "eluding" ("eluding"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eluding';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eliminated_eliminates_1797',
    name: 'eliminated vs eliminates',
    description: 'Commonly confused words: eliminated and eliminates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beliminated\b/gi,
    message: 'Check: "eliminated" ("eliminated") vs "eliminates" ("eliminates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eliminates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_elongated_elongates_1798',
    name: 'elongated vs elongates',
    description: 'Commonly confused words: elongated and elongates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\belongated\b/gi,
    message: 'Check: "elongated" ("elongated") vs "elongates" ("elongates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'elongates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eloped_elopes_1799',
    name: 'eloped vs elopes',
    description: 'Commonly confused words: eloped and elopes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beloped\b/gi,
    message: 'Check: "eloped" ("eloped") vs "elopes" ("elopes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'elopes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_elucidated_elucidates_1800',
    name: 'elucidated vs elucidates',
    description: 'Commonly confused words: elucidated and elucidates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\belucidated\b/gi,
    message: 'Check: "elucidated" ("elucidated") vs "elucidates" ("elucidates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'elucidates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eluded_eludes_1801',
    name: 'eluded vs eludes',
    description: 'Commonly confused words: eluded and eludes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beluded\b/gi,
    message: 'Check: "eluded" ("eluded") vs "eludes" ("eludes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eludes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_embargoed_embargoes_1802',
    name: 'embargoed vs embargoes',
    description: 'Commonly confused words: embargoed and embargoes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bembargoed\b/gi,
    message: 'Check: "embargoed" ("embargoed") vs "embargoes" ("embargoes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'embargoes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_embarrassed_embarrasses_1803',
    name: 'embarrassed vs embarrasses',
    description: 'Commonly confused words: embarrassed and embarrasses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bembarrassed\b/gi,
    message: 'Check: "embarrassed" ("embarrassed") vs "embarrasses" ("embarrasses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'embarrasses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_embellished_embellishes_1804',
    name: 'embellished vs embellishes',
    description: 'Commonly confused words: embellished and embellishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bembellished\b/gi,
    message: 'Check: "embellished" ("embellished") vs "embellishes" ("embellishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'embellishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_embezzled_embezzles_1805',
    name: 'embezzled vs embezzles',
    description: 'Commonly confused words: embezzled and embezzles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bembezzled\b/gi,
    message: 'Check: "embezzled" ("embezzled") vs "embezzles" ("embezzles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'embezzles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_embodied_embodies_1806',
    name: 'embodied vs embodies',
    description: 'Commonly confused words: embodied and embodies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bembodied\b/gi,
    message: 'Check: "embodied" ("embodied") vs "embodies" ("embodies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'embodies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_emceed_emcees_1807',
    name: 'emceed vs emcees',
    description: 'Commonly confused words: emceed and emcees',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bemceed\b/gi,
    message: 'Check: "emceed" ("emceed") vs "emcees" ("emcees"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'emcees';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_emir_emit_1808',
    name: 'emir vs emit',
    description: 'Commonly confused words: emir and emit',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bemir\b/gi,
    message: 'Check: "emir" ("emir") vs "emit" ("emit"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'emit';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_emirs_emits_1809',
    name: 'emirs vs emits',
    description: 'Commonly confused words: emirs and emits',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bemirs\b/gi,
    message: 'Check: "emirs" ("emirs") vs "emits" ("emits"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'emits';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_emphasized_emphasizes_1810',
    name: 'emphasized vs emphasizes',
    description: 'Commonly confused words: emphasized and emphasizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bemphasized\b/gi,
    message: 'Check: "emphasized" ("emphasized") vs "emphasizes" ("emphasizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'emphasizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_employed_employee_1811',
    name: 'employed vs employee',
    description: 'Commonly confused words: employed and employee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bemployed\b/gi,
    message: 'Check: "employed" ("employed") vs "employee" ("employee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'employee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_employee_employer_1812',
    name: 'employee vs employer',
    description: 'Commonly confused words: employee and employer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bemployee\b/gi,
    message: 'Check: "employee" ("employee") vs "employer" ("employer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'employer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_emptied_empties_1813',
    name: 'emptied vs empties',
    description: 'Commonly confused words: emptied and empties',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bemptied\b/gi,
    message: 'Check: "emptied" ("emptied") vs "empties" ("empties"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'empties';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_enabled_enables_1814',
    name: 'enabled vs enables',
    description: 'Commonly confused words: enabled and enables',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\benabled\b/gi,
    message: 'Check: "enabled" ("enabled") vs "enables" ("enables"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'enables';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_encapsulated_encapsulates_1815',
    name: 'encapsulated vs encapsulates',
    description: 'Commonly confused words: encapsulated and encapsulates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bencapsulated\b/gi,
    message: 'Check: "encapsulated" ("encapsulated") vs "encapsulates" ("encapsulates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'encapsulates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_encased_encases_1816',
    name: 'encased vs encases',
    description: 'Commonly confused words: encased and encases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bencased\b/gi,
    message: 'Check: "encased" ("encased") vs "encases" ("encases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'encases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_encircled_encircles_1817',
    name: 'encircled vs encircles',
    description: 'Commonly confused words: encircled and encircles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bencircled\b/gi,
    message: 'Check: "encircled" ("encircled") vs "encircles" ("encircles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'encircles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_enclosed_encloses_1818',
    name: 'enclosed vs encloses',
    description: 'Commonly confused words: enclosed and encloses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\benclosed\b/gi,
    message: 'Check: "enclosed" ("enclosed") vs "encloses" ("encloses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'encloses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_encoded_encodes_1819',
    name: 'encoded vs encodes',
    description: 'Commonly confused words: encoded and encodes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bencoded\b/gi,
    message: 'Check: "encoded" ("encoded") vs "encodes" ("encodes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'encodes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_encored_encores_1820',
    name: 'encored vs encores',
    description: 'Commonly confused words: encored and encores',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bencored\b/gi,
    message: 'Check: "encored" ("encored") vs "encores" ("encores"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'encores';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_encroached_encroaches_1821',
    name: 'encroached vs encroaches',
    description: 'Commonly confused words: encroached and encroaches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bencroached\b/gi,
    message: 'Check: "encroached" ("encroached") vs "encroaches" ("encroaches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'encroaches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ency_envy_1822',
    name: 'ency vs envy',
    description: 'Commonly confused words: ency and envy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bency\b/gi,
    message: 'Check: "ency" ("ency") vs "envy" ("envy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'envy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_endued_ensued_1823',
    name: 'endued vs ensued',
    description: 'Commonly confused words: endued and ensued',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bendued\b/gi,
    message: 'Check: "endued" ("endued") vs "ensued" ("ensued"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ensued';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_endured_ensured_1824',
    name: 'endured vs ensured',
    description: 'Commonly confused words: endured and ensured',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bendured\b/gi,
    message: 'Check: "endured" ("endured") vs "ensured" ("ensured"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ensured';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_endure_ensure_1825',
    name: 'endure vs ensure',
    description: 'Commonly confused words: endure and ensure',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bendure\b/gi,
    message: 'Check: "endure" ("endure") vs "ensure" ("ensure"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ensure';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_endures_ensures_1826',
    name: 'endures vs ensures',
    description: 'Commonly confused words: endures and ensures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bendures\b/gi,
    message: 'Check: "endures" ("endures") vs "ensures" ("ensures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ensures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_enduring_ensuring_1827',
    name: 'enduring vs ensuring',
    description: 'Commonly confused words: enduring and ensuring',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\benduring\b/gi,
    message: 'Check: "enduring" ("enduring") vs "ensuring" ("ensuring"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ensuring';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_energized_energizes_1828',
    name: 'energized vs energizes',
    description: 'Commonly confused words: energized and energizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\benergized\b/gi,
    message: 'Check: "energized" ("energized") vs "energizes" ("energizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'energizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_engraved_engraves_1829',
    name: 'engraved vs engraves',
    description: 'Commonly confused words: engraved and engraves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bengraved\b/gi,
    message: 'Check: "engraved" ("engraved") vs "engraves" ("engraves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'engraves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_enlarged_enlarges_1830',
    name: 'enlarged vs enlarges',
    description: 'Commonly confused words: enlarged and enlarges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\benlarged\b/gi,
    message: 'Check: "enlarged" ("enlarged") vs "enlarges" ("enlarges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'enlarges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ennobled_ennobles_1831',
    name: 'ennobled vs ennobles',
    description: 'Commonly confused words: ennobled and ennobles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bennobled\b/gi,
    message: 'Check: "ennobled" ("ennobled") vs "ennobles" ("ennobles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ennobles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_enriched_enriches_1832',
    name: 'enriched vs enriches',
    description: 'Commonly confused words: enriched and enriches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\benriched\b/gi,
    message: 'Check: "enriched" ("enriched") vs "enriches" ("enriches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'enriches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_enshrined_enshrines_1833',
    name: 'enshrined vs enshrines',
    description: 'Commonly confused words: enshrined and enshrines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\benshrined\b/gi,
    message: 'Check: "enshrined" ("enshrined") vs "enshrines" ("enshrines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'enshrines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_enslaved_enslaves_1834',
    name: 'enslaved vs enslaves',
    description: 'Commonly confused words: enslaved and enslaves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\benslaved\b/gi,
    message: 'Check: "enslaved" ("enslaved") vs "enslaves" ("enslaves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'enslaves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ensnared_ensnares_1835',
    name: 'ensnared vs ensnares',
    description: 'Commonly confused words: ensnared and ensnares',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bensnared\b/gi,
    message: 'Check: "ensnared" ("ensnared") vs "ensnares" ("ensnares"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ensnares';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ensured_ensures_1836',
    name: 'ensured vs ensures',
    description: 'Commonly confused words: ensured and ensures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bensured\b/gi,
    message: 'Check: "ensured" ("ensured") vs "ensures" ("ensures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ensures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_entangled_entangles_1837',
    name: 'entangled vs entangles',
    description: 'Commonly confused words: entangled and entangles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bentangled\b/gi,
    message: 'Check: "entangled" ("entangled") vs "entangles" ("entangles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'entangles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_enthused_enthuses_1838',
    name: 'enthused vs enthuses',
    description: 'Commonly confused words: enthused and enthuses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\benthused\b/gi,
    message: 'Check: "enthused" ("enthused") vs "enthuses" ("enthuses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'enthuses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_enticed_entices_1839',
    name: 'enticed vs entices',
    description: 'Commonly confused words: enticed and entices',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\benticed\b/gi,
    message: 'Check: "enticed" ("enticed") vs "entices" ("entices"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'entices';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_entranced_entrances_1840',
    name: 'entranced vs entrances',
    description: 'Commonly confused words: entranced and entrances',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bentranced\b/gi,
    message: 'Check: "entranced" ("entranced") vs "entrances" ("entrances"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'entrances';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_entrenched_entrenches_1841',
    name: 'entrenched vs entrenches',
    description: 'Commonly confused words: entrenched and entrenches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bentrenched\b/gi,
    message: 'Check: "entrenched" ("entrenched") vs "entrenches" ("entrenches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'entrenches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_entwined_entwines_1842',
    name: 'entwined vs entwines',
    description: 'Commonly confused words: entwined and entwines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bentwined\b/gi,
    message: 'Check: "entwined" ("entwined") vs "entwines" ("entwines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'entwines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_enumerated_enumerates_1843',
    name: 'enumerated vs enumerates',
    description: 'Commonly confused words: enumerated and enumerates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\benumerated\b/gi,
    message: 'Check: "enumerated" ("enumerated") vs "enumerates" ("enumerates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'enumerates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_enunciated_enunciates_1844',
    name: 'enunciated vs enunciates',
    description: 'Commonly confused words: enunciated and enunciates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\benunciated\b/gi,
    message: 'Check: "enunciated" ("enunciated") vs "enunciates" ("enunciates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'enunciates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_envisaged_envisages_1845',
    name: 'envisaged vs envisages',
    description: 'Commonly confused words: envisaged and envisages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\benvisaged\b/gi,
    message: 'Check: "envisaged" ("envisaged") vs "envisages" ("envisages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'envisages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eons_dons_1846',
    name: 'eons vs dons',
    description: 'Commonly confused words: eons and dons',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beons\b/gi,
    message: 'Check: "eons" ("eons") vs "dons" ("dons"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dons';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_epitomized_epitomizes_1847',
    name: 'epitomized vs epitomizes',
    description: 'Commonly confused words: epitomized and epitomizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bepitomized\b/gi,
    message: 'Check: "epitomized" ("epitomized") vs "epitomizes" ("epitomizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'epitomizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_equated_equates_1848',
    name: 'equated vs equates',
    description: 'Commonly confused words: equated and equates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bequated\b/gi,
    message: 'Check: "equated" ("equated") vs "equates" ("equates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'equates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eradicated_eradicates_1849',
    name: 'eradicated vs eradicates',
    description: 'Commonly confused words: eradicated and eradicates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beradicated\b/gi,
    message: 'Check: "eradicated" ("eradicated") vs "eradicates" ("eradicates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eradicates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_erased_erases_1850',
    name: 'erased vs erases',
    description: 'Commonly confused words: erased and erases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\berased\b/gi,
    message: 'Check: "erased" ("erased") vs "erases" ("erases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'erases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eroded_erodes_1851',
    name: 'eroded vs erodes',
    description: 'Commonly confused words: eroded and erodes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beroded\b/gi,
    message: 'Check: "eroded" ("eroded") vs "erodes" ("erodes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'erodes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ester_eater_1852',
    name: 'ester vs eater',
    description: 'Commonly confused words: ester and eater',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bester\b/gi,
    message: 'Check: "ester" ("ester") vs "eater" ("eater"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eater';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_estimated_estimates_1853',
    name: 'estimated vs estimates',
    description: 'Commonly confused words: estimated and estimates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bestimated\b/gi,
    message: 'Check: "estimated" ("estimated") vs "estimates" ("estimates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'estimates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_estranged_estranges_1854',
    name: 'estranged vs estranges',
    description: 'Commonly confused words: estranged and estranges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bestranged\b/gi,
    message: 'Check: "estranged" ("estranged") vs "estranges" ("estranges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'estranges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_etas_eras_1855',
    name: 'etas vs eras',
    description: 'Commonly confused words: etas and eras',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\betas\b/gi,
    message: 'Check: "etas" ("etas") vs "eras" ("eras"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eras';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_etched_etches_1856',
    name: 'etched vs etches',
    description: 'Commonly confused words: etched and etches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\betched\b/gi,
    message: 'Check: "etched" ("etched") vs "etches" ("etches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'etches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eulogized_eulogizes_1857',
    name: 'eulogized vs eulogizes',
    description: 'Commonly confused words: eulogized and eulogizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beulogized\b/gi,
    message: 'Check: "eulogized" ("eulogized") vs "eulogizes" ("eulogizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eulogizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_evaluated_evaluates_1858',
    name: 'evaluated vs evaluates',
    description: 'Commonly confused words: evaluated and evaluates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bevaluated\b/gi,
    message: 'Check: "evaluated" ("evaluated") vs "evaluates" ("evaluates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'evaluates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_evaporated_evaporates_1859',
    name: 'evaporated vs evaporates',
    description: 'Commonly confused words: evaporated and evaporates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bevaporated\b/gi,
    message: 'Check: "evaporated" ("evaporated") vs "evaporates" ("evaporates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'evaporates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eventuated_eventuates_1860',
    name: 'eventuated vs eventuates',
    description: 'Commonly confused words: eventuated and eventuates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beventuated\b/gi,
    message: 'Check: "eventuated" ("eventuated") vs "eventuates" ("eventuates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eventuates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_evidenced_evidences_1861',
    name: 'evidenced vs evidences',
    description: 'Commonly confused words: evidenced and evidences',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bevidenced\b/gi,
    message: 'Check: "evidenced" ("evidenced") vs "evidences" ("evidences"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'evidences';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_evinced_evinces_1862',
    name: 'evinced vs evinces',
    description: 'Commonly confused words: evinced and evinces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bevinced\b/gi,
    message: 'Check: "evinced" ("evinced") vs "evinces" ("evinces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'evinces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_evoked_evokes_1863',
    name: 'evoked vs evokes',
    description: 'Commonly confused words: evoked and evokes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bevoked\b/gi,
    message: 'Check: "evoked" ("evoked") vs "evokes" ("evokes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'evokes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_exacerbated_exacerbates_1864',
    name: 'exacerbated vs exacerbates',
    description: 'Commonly confused words: exacerbated and exacerbates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexacerbated\b/gi,
    message: 'Check: "exacerbated" ("exacerbated") vs "exacerbates" ("exacerbates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'exacerbates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_examined_examines_1865',
    name: 'examined vs examines',
    description: 'Commonly confused words: examined and examines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexamined\b/gi,
    message: 'Check: "examined" ("examined") vs "examines" ("examines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'examines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_exasperated_exasperates_1866',
    name: 'exasperated vs exasperates',
    description: 'Commonly confused words: exasperated and exasperates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexasperated\b/gi,
    message: 'Check: "exasperated" ("exasperated") vs "exasperates" ("exasperates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'exasperates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_exchanged_exchanges_1867',
    name: 'exchanged vs exchanges',
    description: 'Commonly confused words: exchanged and exchanges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexchanged\b/gi,
    message: 'Check: "exchanged" ("exchanged") vs "exchanges" ("exchanges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'exchanges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_excised_excises_1868',
    name: 'excised vs excises',
    description: 'Commonly confused words: excised and excises',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexcised\b/gi,
    message: 'Check: "excised" ("excised") vs "excises" ("excises"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'excises';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_excised_excused_1869',
    name: 'excised vs excused',
    description: 'Commonly confused words: excised and excused',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexcised\b/gi,
    message: 'Check: "excised" ("excised") vs "excused" ("excused"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'excused';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_excise_excuse_1870',
    name: 'excise vs excuse',
    description: 'Commonly confused words: excise and excuse',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexcise\b/gi,
    message: 'Check: "excise" ("excise") vs "excuse" ("excuse"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'excuse';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_excises_excuses_1871',
    name: 'excises vs excuses',
    description: 'Commonly confused words: excises and excuses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexcises\b/gi,
    message: 'Check: "excises" ("excises") vs "excuses" ("excuses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'excuses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_excited_excites_1872',
    name: 'excited vs excites',
    description: 'Commonly confused words: excited and excites',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexcited\b/gi,
    message: 'Check: "excited" ("excited") vs "excites" ("excites"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'excites';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_excluded_excludes_1873',
    name: 'excluded vs excludes',
    description: 'Commonly confused words: excluded and excludes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexcluded\b/gi,
    message: 'Check: "excluded" ("excluded") vs "excludes" ("excludes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'excludes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_excommunicated_excommunicates_1874',
    name: 'excommunicated vs excommunicates',
    description: 'Commonly confused words: excommunicated and excommunicates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexcommunicated\b/gi,
    message: 'Check: "excommunicated" ("excommunicated") vs "excommunicates" ("excommunicates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'excommunicates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_excoriated_excoriates_1875',
    name: 'excoriated vs excoriates',
    description: 'Commonly confused words: excoriated and excoriates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexcoriated\b/gi,
    message: 'Check: "excoriated" ("excoriated") vs "excoriates" ("excoriates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'excoriates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_excreted_excretes_1876',
    name: 'excreted vs excretes',
    description: 'Commonly confused words: excreted and excretes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexcreted\b/gi,
    message: 'Check: "excreted" ("excreted") vs "excretes" ("excretes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'excretes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_exemplified_exemplifies_1877',
    name: 'exemplified vs exemplifies',
    description: 'Commonly confused words: exemplified and exemplifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexemplified\b/gi,
    message: 'Check: "exemplified" ("exemplified") vs "exemplifies" ("exemplifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'exemplifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_exercised_exercises_1878',
    name: 'exercised vs exercises',
    description: 'Commonly confused words: exercised and exercises',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexercised\b/gi,
    message: 'Check: "exercised" ("exercised") vs "exercises" ("exercises"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'exercises';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_exfoliated_exfoliates_1879',
    name: 'exfoliated vs exfoliates',
    description: 'Commonly confused words: exfoliated and exfoliates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexfoliated\b/gi,
    message: 'Check: "exfoliated" ("exfoliated") vs "exfoliates" ("exfoliates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'exfoliates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_exhaled_exhales_1880',
    name: 'exhaled vs exhales',
    description: 'Commonly confused words: exhaled and exhales',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexhaled\b/gi,
    message: 'Check: "exhaled" ("exhaled") vs "exhales" ("exhales"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'exhales';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_exhumed_exhumes_1881',
    name: 'exhumed vs exhumes',
    description: 'Commonly confused words: exhumed and exhumes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexhumed\b/gi,
    message: 'Check: "exhumed" ("exhumed") vs "exhumes" ("exhumes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'exhumes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_exiled_exiles_1882',
    name: 'exiled vs exiles',
    description: 'Commonly confused words: exiled and exiles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexiled\b/gi,
    message: 'Check: "exiled" ("exiled") vs "exiles" ("exiles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'exiles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_exonerated_exonerates_1883',
    name: 'exonerated vs exonerates',
    description: 'Commonly confused words: exonerated and exonerates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexonerated\b/gi,
    message: 'Check: "exonerated" ("exonerated") vs "exonerates" ("exonerates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'exonerates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_expatriated_expatriates_1884',
    name: 'expatriated vs expatriates',
    description: 'Commonly confused words: expatriated and expatriates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexpatriated\b/gi,
    message: 'Check: "expatriated" ("expatriated") vs "expatriates" ("expatriates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'expatriates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_expedited_expedites_1885',
    name: 'expedited vs expedites',
    description: 'Commonly confused words: expedited and expedites',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexpedited\b/gi,
    message: 'Check: "expedited" ("expedited") vs "expedites" ("expedites"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'expedites';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_experienced_experiences_1886',
    name: 'experienced vs experiences',
    description: 'Commonly confused words: experienced and experiences',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexperienced\b/gi,
    message: 'Check: "experienced" ("experienced") vs "experiences" ("experiences"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'experiences';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_exp_esp_1887',
    name: 'exp vs esp',
    description: 'Commonly confused words: exp and esp',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexp\b/gi,
    message: 'Check: "exp" ("exp") vs "esp" ("esp"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'esp';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_expired_expires_1888',
    name: 'expired vs expires',
    description: 'Commonly confused words: expired and expires',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexpired\b/gi,
    message: 'Check: "expired" ("expired") vs "expires" ("expires"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'expires';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_explored_explores_1889',
    name: 'explored vs explores',
    description: 'Commonly confused words: explored and explores',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexplored\b/gi,
    message: 'Check: "explored" ("explored") vs "explores" ("explores"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'explores';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_expressed_expresses_1890',
    name: 'expressed vs expresses',
    description: 'Commonly confused words: expressed and expresses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexpressed\b/gi,
    message: 'Check: "expressed" ("expressed") vs "expresses" ("expresses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'expresses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_exterminated_exterminates_1891',
    name: 'exterminated vs exterminates',
    description: 'Commonly confused words: exterminated and exterminates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bexterminated\b/gi,
    message: 'Check: "exterminated" ("exterminated") vs "exterminates" ("exterminates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'exterminates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_extinguished_extinguishes_1892',
    name: 'extinguished vs extinguishes',
    description: 'Commonly confused words: extinguished and extinguishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bextinguished\b/gi,
    message: 'Check: "extinguished" ("extinguished") vs "extinguishes" ("extinguishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'extinguishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_extradited_extradites_1893',
    name: 'extradited vs extradites',
    description: 'Commonly confused words: extradited and extradites',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bextradited\b/gi,
    message: 'Check: "extradited" ("extradited") vs "extradites" ("extradites"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'extradites';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_extrapolated_extrapolates_1894',
    name: 'extrapolated vs extrapolates',
    description: 'Commonly confused words: extrapolated and extrapolates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bextrapolated\b/gi,
    message: 'Check: "extrapolated" ("extrapolated") vs "extrapolates" ("extrapolates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'extrapolates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_extricated_extricates_1895',
    name: 'extricated vs extricates',
    description: 'Commonly confused words: extricated and extricates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bextricated\b/gi,
    message: 'Check: "extricated" ("extricated") vs "extricates" ("extricates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'extricates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_extruded_extrudes_1896',
    name: 'extruded vs extrudes',
    description: 'Commonly confused words: extruded and extrudes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bextruded\b/gi,
    message: 'Check: "extruded" ("extruded") vs "extrudes" ("extrudes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'extrudes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_eyed_dyed_1897',
    name: 'eyed vs dyed',
    description: 'Commonly confused words: eyed and dyed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\beyed\b/gi,
    message: 'Check: "eyed" ("eyed") vs "dyed" ("dyed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dyed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
