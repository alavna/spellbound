/**
 * Auto-generated confusion rules (J)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.511Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesJ: GrammarRule[] = [
  {
    id: 'confusion_juggler_jugular_372',
    name: 'juggler vs jugular',
    description: 'Commonly confused words: juggler and jugular',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjuggler\b/gi,
    message: 'Check: "juggler" ("juggler") vs "jugular" ("jugular"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jugular';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_just_juts_373',
    name: 'just vs juts',
    description: 'Commonly confused words: just and juts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjust\b/gi,
    message: 'Check: "just" ("just") vs "juts" ("juts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'juts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jaded_jades_2599',
    name: 'jaded vs jades',
    description: 'Commonly confused words: jaded and jades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjaded\b/gi,
    message: 'Check: "jaded" ("jaded") vs "jades" ("jades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jag_hag_2600',
    name: 'jag vs hag',
    description: 'Commonly confused words: jag and hag',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjag\b/gi,
    message: 'Check: "jag" ("jag") vs "hag" ("hag"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hag';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jailed_mailed_2601',
    name: 'jailed vs mailed',
    description: 'Commonly confused words: jailed and mailed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjailed\b/gi,
    message: 'Check: "jailed" ("jailed") vs "mailed" ("mailed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mailed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jailing_mailing_2602',
    name: 'jailing vs mailing',
    description: 'Commonly confused words: jailing and mailing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjailing\b/gi,
    message: 'Check: "jailing" ("jailing") vs "mailing" ("mailing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mailing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jail_mail_2603',
    name: 'jail vs mail',
    description: 'Commonly confused words: jail and mail',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjail\b/gi,
    message: 'Check: "jail" ("jail") vs "mail" (postal delivery). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mail';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jails_mails_2604',
    name: 'jails vs mails',
    description: 'Commonly confused words: jails and mails',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjails\b/gi,
    message: 'Check: "jails" ("jails") vs "mails" ("mails"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mails';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jangled_mangled_2605',
    name: 'jangled vs mangled',
    description: 'Commonly confused words: jangled and mangled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjangled\b/gi,
    message: 'Check: "jangled" ("jangled") vs "mangled" ("mangled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mangled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jarred_marred_2606',
    name: 'jarred vs marred',
    description: 'Commonly confused words: jarred and marred',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjarred\b/gi,
    message: 'Check: "jarred" ("jarred") vs "marred" ("marred"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'marred';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jarring_marring_2607',
    name: 'jarring vs marring',
    description: 'Commonly confused words: jarring and marring',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjarring\b/gi,
    message: 'Check: "jarring" ("jarring") vs "marring" ("marring"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'marring';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jeeps_keeps_2608',
    name: 'jeeps vs keeps',
    description: 'Commonly confused words: jeeps and keeps',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjeeps\b/gi,
    message: 'Check: "jeeps" ("jeeps") vs "keeps" ("keeps"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'keeps';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jellied_jellies_2609',
    name: 'jellied vs jellies',
    description: 'Commonly confused words: jellied and jellies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjellied\b/gi,
    message: 'Check: "jellied" ("jellied") vs "jellies" ("jellies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jellies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jeopardized_jeopardizes_2610',
    name: 'jeopardized vs jeopardizes',
    description: 'Commonly confused words: jeopardized and jeopardizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjeopardized\b/gi,
    message: 'Check: "jeopardized" ("jeopardized") vs "jeopardizes" ("jeopardizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jeopardizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jibed_jibes_2611',
    name: 'jibed vs jibes',
    description: 'Commonly confused words: jibed and jibes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjibed\b/gi,
    message: 'Check: "jibed" ("jibed") vs "jibes" ("jibes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jibes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jibes_jives_2612',
    name: 'jibes vs jives',
    description: 'Commonly confused words: jibes and jives',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjibes\b/gi,
    message: 'Check: "jibes" ("jibes") vs "jives" ("jives"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jives';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jigged_jogged_2613',
    name: 'jigged vs jogged',
    description: 'Commonly confused words: jigged and jogged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjigged\b/gi,
    message: 'Check: "jigged" ("jigged") vs "jogged" ("jogged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jogged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jigged_jugged_2614',
    name: 'jigged vs jugged',
    description: 'Commonly confused words: jigged and jugged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjigged\b/gi,
    message: 'Check: "jigged" ("jigged") vs "jugged" ("jugged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jugged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jigger_jogger_2615',
    name: 'jigger vs jogger',
    description: 'Commonly confused words: jigger and jogger',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjigger\b/gi,
    message: 'Check: "jigger" ("jigger") vs "jogger" ("jogger"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jogger';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jigging_jogging_2616',
    name: 'jigging vs jogging',
    description: 'Commonly confused words: jigging and jogging',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjigging\b/gi,
    message: 'Check: "jigging" ("jigging") vs "jogging" ("jogging"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jogging';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jiggled_jiggles_2617',
    name: 'jiggled vs jiggles',
    description: 'Commonly confused words: jiggled and jiggles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjiggled\b/gi,
    message: 'Check: "jiggled" ("jiggled") vs "jiggles" ("jiggles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jiggles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jiggle_juggle_2618',
    name: 'jiggle vs juggle',
    description: 'Commonly confused words: jiggle and juggle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjiggle\b/gi,
    message: 'Check: "jiggle" ("jiggle") vs "juggle" ("juggle"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'juggle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jiggles_juggles_2619',
    name: 'jiggles vs juggles',
    description: 'Commonly confused words: jiggles and juggles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjiggles\b/gi,
    message: 'Check: "jiggles" ("jiggles") vs "juggles" ("juggles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'juggles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jigs_jogs_2620',
    name: 'jigs vs jogs',
    description: 'Commonly confused words: jigs and jogs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjigs\b/gi,
    message: 'Check: "jigs" ("jigs") vs "jogs" ("jogs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jogs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jigs_jugs_2621',
    name: 'jigs vs jugs',
    description: 'Commonly confused words: jigs and jugs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjigs\b/gi,
    message: 'Check: "jigs" ("jigs") vs "jugs" ("jugs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jugs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jilt_jolt_2622',
    name: 'jilt vs jolt',
    description: 'Commonly confused words: jilt and jolt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjilt\b/gi,
    message: 'Check: "jilt" ("jilt") vs "jolt" ("jolt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jolt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jilt_kilt_2623',
    name: 'jilt vs kilt',
    description: 'Commonly confused words: jilt and kilt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjilt\b/gi,
    message: 'Check: "jilt" ("jilt") vs "kilt" ("kilt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'kilt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jilts_kilts_2624',
    name: 'jilts vs kilts',
    description: 'Commonly confused words: jilts and kilts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjilts\b/gi,
    message: 'Check: "jilts" ("jilts") vs "kilts" ("kilts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'kilts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jingle_mingle_2625',
    name: 'jingle vs mingle',
    description: 'Commonly confused words: jingle and mingle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjingle\b/gi,
    message: 'Check: "jingle" ("jingle") vs "mingle" ("mingle"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mingle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jingles_jungles_2626',
    name: 'jingles vs jungles',
    description: 'Commonly confused words: jingles and jungles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjingles\b/gi,
    message: 'Check: "jingles" ("jingles") vs "jungles" ("jungles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jungles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jingles_mingles_2627',
    name: 'jingles vs mingles',
    description: 'Commonly confused words: jingles and mingles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjingles\b/gi,
    message: 'Check: "jingles" ("jingles") vs "mingles" ("mingles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mingles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jingling_mingling_2628',
    name: 'jingling vs mingling',
    description: 'Commonly confused words: jingling and mingling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjingling\b/gi,
    message: 'Check: "jingling" ("jingling") vs "mingling" ("mingling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mingling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jinked_junked_2629',
    name: 'jinked vs junked',
    description: 'Commonly confused words: jinked and junked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjinked\b/gi,
    message: 'Check: "jinked" ("jinked") vs "junked" ("junked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'junked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jinked_kinked_2630',
    name: 'jinked vs kinked',
    description: 'Commonly confused words: jinked and kinked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjinked\b/gi,
    message: 'Check: "jinked" ("jinked") vs "kinked" ("kinked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'kinked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jinking_junking_2631',
    name: 'jinking vs junking',
    description: 'Commonly confused words: jinking and junking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjinking\b/gi,
    message: 'Check: "jinking" ("jinking") vs "junking" ("junking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'junking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jinking_kinking_2632',
    name: 'jinking vs kinking',
    description: 'Commonly confused words: jinking and kinking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjinking\b/gi,
    message: 'Check: "jinking" ("jinking") vs "kinking" ("kinking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'kinking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jink_junk_2633',
    name: 'jink vs junk',
    description: 'Commonly confused words: jink and junk',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjink\b/gi,
    message: 'Check: "jink" ("jink") vs "junk" ("junk"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'junk';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jinks_junks_2634',
    name: 'jinks vs junks',
    description: 'Commonly confused words: jinks and junks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjinks\b/gi,
    message: 'Check: "jinks" ("jinks") vs "junks" ("junks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'junks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jinks_kinks_2635',
    name: 'jinks vs kinks',
    description: 'Commonly confused words: jinks and kinks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjinks\b/gi,
    message: 'Check: "jinks" ("jinks") vs "kinks" ("kinks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'kinks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jives_hives_2636',
    name: 'jives vs hives',
    description: 'Commonly confused words: jives and hives',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjives\b/gi,
    message: 'Check: "jives" ("jives") vs "hives" ("hives"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hives';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jobbed_mobbed_2637',
    name: 'jobbed vs mobbed',
    description: 'Commonly confused words: jobbed and mobbed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjobbed\b/gi,
    message: 'Check: "jobbed" ("jobbed") vs "mobbed" ("mobbed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mobbed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jocks_mocks_2638',
    name: 'jocks vs mocks',
    description: 'Commonly confused words: jocks and mocks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjocks\b/gi,
    message: 'Check: "jocks" ("jocks") vs "mocks" ("mocks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mocks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_joist_moist_2639',
    name: 'joist vs moist',
    description: 'Commonly confused words: joist and moist',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjoist\b/gi,
    message: 'Check: "joist" ("joist") vs "moist" ("moist"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'moist';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_joists_jousts_2640',
    name: 'joists vs jousts',
    description: 'Commonly confused words: joists and jousts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjoists\b/gi,
    message: 'Check: "joists" ("joists") vs "jousts" ("jousts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jousts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_joked_jokes_2641',
    name: 'joked vs jokes',
    description: 'Commonly confused words: joked and jokes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjoked\b/gi,
    message: 'Check: "joked" ("joked") vs "jokes" ("jokes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jokes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_joke_hoke_2642',
    name: 'joke vs hoke',
    description: 'Commonly confused words: joke and hoke',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjoke\b/gi,
    message: 'Check: "joke" ("joke") vs "hoke" ("hoke"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hoke';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jokes_hokes_2643',
    name: 'jokes vs hokes',
    description: 'Commonly confused words: jokes and hokes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjokes\b/gi,
    message: 'Check: "jokes" ("jokes") vs "hokes" ("hokes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hokes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jolted_molted_2644',
    name: 'jolted vs molted',
    description: 'Commonly confused words: jolted and molted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjolted\b/gi,
    message: 'Check: "jolted" ("jolted") vs "molted" ("molted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'molted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jolts_molts_2645',
    name: 'jolts vs molts',
    description: 'Commonly confused words: jolts and molts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjolts\b/gi,
    message: 'Check: "jolts" ("jolts") vs "molts" ("molts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'molts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jostled_jostles_2646',
    name: 'jostled vs jostles',
    description: 'Commonly confused words: jostled and jostles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjostled\b/gi,
    message: 'Check: "jostled" ("jostled") vs "jostles" ("jostles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jostles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jots_hots_2647',
    name: 'jots vs hots',
    description: 'Commonly confused words: jots and hots',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjots\b/gi,
    message: 'Check: "jots" ("jots") vs "hots" ("hots"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hots';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_judged_judges_2648',
    name: 'judged vs judges',
    description: 'Commonly confused words: judged and judges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjudged\b/gi,
    message: 'Check: "judged" ("judged") vs "judges" ("judges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'judges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jugged_mugged_2649',
    name: 'jugged vs mugged',
    description: 'Commonly confused words: jugged and mugged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjugged\b/gi,
    message: 'Check: "jugged" ("jugged") vs "mugged" ("mugged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mugged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jug_hug_2650',
    name: 'jug vs hug',
    description: 'Commonly confused words: jug and hug',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjug\b/gi,
    message: 'Check: "jug" ("jug") vs "hug" ("hug"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hug';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jugs_hugs_2651',
    name: 'jugs vs hugs',
    description: 'Commonly confused words: jugs and hugs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjugs\b/gi,
    message: 'Check: "jugs" ("jugs") vs "hugs" ("hugs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hugs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jugs_juts_2652',
    name: 'jugs vs juts',
    description: 'Commonly confused words: jugs and juts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjugs\b/gi,
    message: 'Check: "jugs" ("jugs") vs "juts" ("juts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'juts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jugs_mugs_2653',
    name: 'jugs vs mugs',
    description: 'Commonly confused words: jugs and mugs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjugs\b/gi,
    message: 'Check: "jugs" ("jugs") vs "mugs" ("mugs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mugs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_juiced_juices_2654',
    name: 'juiced vs juices',
    description: 'Commonly confused words: juiced and juices',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjuiced\b/gi,
    message: 'Check: "juiced" ("juiced") vs "juices" ("juices"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'juices';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jumbled_jumbles_2655',
    name: 'jumbled vs jumbles',
    description: 'Commonly confused words: jumbled and jumbles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjumbled\b/gi,
    message: 'Check: "jumbled" ("jumbled") vs "jumbles" ("jumbles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jumbles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jumbled_mumbled_2656',
    name: 'jumbled vs mumbled',
    description: 'Commonly confused words: jumbled and mumbled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjumbled\b/gi,
    message: 'Check: "jumbled" ("jumbled") vs "mumbled" ("mumbled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mumbled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jumble_mumble_2657',
    name: 'jumble vs mumble',
    description: 'Commonly confused words: jumble and mumble',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjumble\b/gi,
    message: 'Check: "jumble" ("jumble") vs "mumble" ("mumble"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mumble';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jumbling_mumbling_2658',
    name: 'jumbling vs mumbling',
    description: 'Commonly confused words: jumbling and mumbling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjumbling\b/gi,
    message: 'Check: "jumbling" ("jumbling") vs "mumbling" ("mumbling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mumbling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_jumps_mumps_2659',
    name: 'jumps vs mumps',
    description: 'Commonly confused words: jumps and mumps',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjumps\b/gi,
    message: 'Check: "jumps" ("jumps") vs "mumps" ("mumps"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mumps';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_justified_justifies_2660',
    name: 'justified vs justifies',
    description: 'Commonly confused words: justified and justifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjustified\b/gi,
    message: 'Check: "justified" ("justified") vs "justifies" ("justifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'justifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_just_must_2661',
    name: 'just vs must',
    description: 'Commonly confused words: just and must',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjust\b/gi,
    message: 'Check: "just" ("just") vs "must" ("must"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'must';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_juts_huts_2662',
    name: 'juts vs huts',
    description: 'Commonly confused words: juts and huts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjuts\b/gi,
    message: 'Check: "juts" ("juts") vs "huts" ("huts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'huts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_juxtaposed_juxtaposes_2663',
    name: 'juxtaposed vs juxtaposes',
    description: 'Commonly confused words: juxtaposed and juxtaposes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bjuxtaposed\b/gi,
    message: 'Check: "juxtaposed" ("juxtaposed") vs "juxtaposes" ("juxtaposes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'juxtaposes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
