/**
 * Auto-generated confusion rules (Q)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.519Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesQ: GrammarRule[] = [
  {
    id: 'confusion_quiet_quite_550',
    name: 'quiet vs quite',
    description: 'Commonly confused words: quiet and quite',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bquiet\b/gi,
    message: 'Check: "quiet" ("quiet") vs "quite" ("quite"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'quite';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_quit_quite_551',
    name: 'quit vs quite',
    description: 'Commonly confused words: quit and quite',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bquit\b/gi,
    message: 'Check: "quit" ("quit") vs "quite" ("quite"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'quite';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_quadrupled_quadruples_3257',
    name: 'quadrupled vs quadruples',
    description: 'Commonly confused words: quadrupled and quadruples',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bquadrupled\b/gi,
    message: 'Check: "quadrupled" ("quadrupled") vs "quadruples" ("quadruples"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'quadruples';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_quaked_quakes_3258',
    name: 'quaked vs quakes',
    description: 'Commonly confused words: quaked and quakes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bquaked\b/gi,
    message: 'Check: "quaked" ("quaked") vs "quakes" ("quakes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'quakes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_quantified_quantifies_3259',
    name: 'quantified vs quantifies',
    description: 'Commonly confused words: quantified and quantifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bquantified\b/gi,
    message: 'Check: "quantified" ("quantified") vs "quantifies" ("quantifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'quantifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_quarantined_quarantines_3260',
    name: 'quarantined vs quarantines',
    description: 'Commonly confused words: quarantined and quarantines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bquarantined\b/gi,
    message: 'Check: "quarantined" ("quarantined") vs "quarantines" ("quarantines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'quarantines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_quarried_quarries_3261',
    name: 'quarried vs quarries',
    description: 'Commonly confused words: quarried and quarries',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bquarried\b/gi,
    message: 'Check: "quarried" ("quarried") vs "quarries" ("quarries"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'quarries';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_quarter_quartet_3262',
    name: 'quarter vs quartet',
    description: 'Commonly confused words: quarter and quartet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bquarter\b/gi,
    message: 'Check: "quarter" ("quarter") vs "quartet" ("quartet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'quartet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_quarters_quartets_3263',
    name: 'quarters vs quartets',
    description: 'Commonly confused words: quarters and quartets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bquarters\b/gi,
    message: 'Check: "quarters" ("quarters") vs "quartets" ("quartets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'quartets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_quashed_quashes_3264',
    name: 'quashed vs quashes',
    description: 'Commonly confused words: quashed and quashes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bquashed\b/gi,
    message: 'Check: "quashed" ("quashed") vs "quashes" ("quashes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'quashes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_quenched_quenches_3265',
    name: 'quenched vs quenches',
    description: 'Commonly confused words: quenched and quenches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bquenched\b/gi,
    message: 'Check: "quenched" ("quenched") vs "quenches" ("quenches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'quenches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_quibbled_quibbles_3266',
    name: 'quibbled vs quibbles',
    description: 'Commonly confused words: quibbled and quibbles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bquibbled\b/gi,
    message: 'Check: "quibbled" ("quibbled") vs "quibbles" ("quibbles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'quibbles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_quite_quote_3267',
    name: 'quite vs quote',
    description: 'Commonly confused words: quite and quote',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bquite\b/gi,
    message: 'Check: "quite" ("quite") vs "quote" ("quote"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'quote';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_quizzed_quizzes_3268',
    name: 'quizzed vs quizzes',
    description: 'Commonly confused words: quizzed and quizzes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bquizzed\b/gi,
    message: 'Check: "quizzed" ("quizzed") vs "quizzes" ("quizzes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'quizzes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_quot_quit_3269',
    name: 'quot vs quit',
    description: 'Commonly confused words: quot and quit',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bquot\b/gi,
    message: 'Check: "quot" ("quot") vs "quit" ("quit"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'quit';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
