/**
 * Auto-generated confusion rules (S)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.501Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesS: GrammarRule[] = [
  {
    id: 'confusion_sell_cell_156',
    name: 'sell vs cell',
    description: 'Commonly confused words: sell and cell',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsell\b/gi,
    message: 'Did you mean "cell"? "sell" ("sell") is often confused with "cell" ("cell").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cell';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sacks_sax_586',
    name: 'sacks vs sax',
    description: 'Commonly confused words: sacks and sax',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsacks\b/gi,
    message: 'Check: "sacks" ("sacks") vs "sax" ("sax"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sax';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sacred_scared_587',
    name: 'sacred vs scared',
    description: 'Commonly confused words: sacred and scared',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsacred\b/gi,
    message: 'Check: "sacred" ("sacred") vs "scared" ("scared"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scared';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sails_sales_588',
    name: 'sails vs sales',
    description: 'Commonly confused words: sails and sales',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsails\b/gi,
    message: 'Check: "sails" ("sails") vs "sales" ("sales"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sales';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sail_sale_589',
    name: 'sail vs sale',
    description: 'Commonly confused words: sail and sale',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsail\b/gi,
    message: 'Check: "sail" (to travel by water) vs "sale" (exchange for money). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sale';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_same_sane_590',
    name: 'same vs sane',
    description: 'Commonly confused words: same and sane',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsame\b/gi,
    message: 'Did you mean "sane"? "same" ("same") is often confused with "sane" ("sane").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sane';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sane_same_591',
    name: 'sane vs same',
    description: 'Commonly confused words: sane and same',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsane\b/gi,
    message: 'Did you mean "same"? "sane" ("sane") is often confused with "same" ("same").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'same';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_same_some_592',
    name: 'same vs some',
    description: 'Commonly confused words: same and some',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsame\b/gi,
    message: 'Did you mean "some"? "same" ("same") is often confused with "some" ("some").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'some';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_some_same_593',
    name: 'some vs same',
    description: 'Commonly confused words: some and same',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsome\b/gi,
    message: 'Did you mean "same"? "some" ("some") is often confused with "same" ("same").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'same';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sand_stand_594',
    name: 'sand vs stand',
    description: 'Commonly confused words: sand and stand',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsand\b/gi,
    message: 'Did you mean "stand"? "sand" ("sand") is often confused with "stand" ("stand").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stand';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sands_stands_595',
    name: 'sands vs stands',
    description: 'Commonly confused words: sands and stands',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsands\b/gi,
    message: 'Check: "sands" ("sands") vs "stands" ("stands"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stands';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_saner_seiner_596',
    name: 'saner vs seiner',
    description: 'Commonly confused words: saner and seiner',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsaner\b/gi,
    message: 'Check: "saner" ("saner") vs "seiner" ("seiner"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seiner';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sank_sink_597',
    name: 'sank vs sink',
    description: 'Commonly confused words: sank and sink',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsank\b/gi,
    message: 'Check: "sank" ("sank") vs "sink" ("sink"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sink';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sate_state_598',
    name: 'sate vs state',
    description: 'Commonly confused words: sate and state',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsate\b/gi,
    message: 'Check: "sate" ("sate") vs "state" ("state"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'state';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sat_seat_599',
    name: 'sat vs seat',
    description: 'Commonly confused words: sat and seat',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsat\b/gi,
    message: 'Check: "sat" ("sat") vs "seat" ("seat"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seat';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sat_set_600',
    name: 'sat vs set',
    description: 'Commonly confused words: sat and set',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsat\b/gi,
    message: 'Did you mean "set"? "sat" ("sat") is often confused with "set" ("set").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'set';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_set_sat_601',
    name: 'set vs sat',
    description: 'Commonly confused words: set and sat',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bset\b/gi,
    message: 'Did you mean "sat"? "set" ("set") is often confused with "sat" ("sat").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sat';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_saver_savor_602',
    name: 'saver vs savor',
    description: 'Commonly confused words: saver and savor',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsaver\b/gi,
    message: 'Check: "saver" ("saver") vs "savor" ("savor"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'savor';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_savers_savors_603',
    name: 'savers vs savors',
    description: 'Commonly confused words: savers and savors',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsavers\b/gi,
    message: 'Check: "savers" ("savers") vs "savors" ("savors"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'savors';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sawed_sod_604',
    name: 'sawed vs sod',
    description: 'Commonly confused words: sawed and sod',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsawed\b/gi,
    message: 'Check: "sawed" ("sawed") vs "sod" ("sod"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sod';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scene_seen_605',
    name: 'scene vs seen',
    description: 'Commonly confused words: scene and seen',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscene\b/gi,
    message: 'Did you mean "seen"? "scene" ("scene") is often confused with "seen" ("seen").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seen';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_seen_scene_606',
    name: 'seen vs scene',
    description: 'Commonly confused words: seen and scene',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bseen\b/gi,
    message: 'Did you mean "scene"? "seen" ("seen") is often confused with "scene" ("scene").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scene';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scent_sent_607',
    name: 'scent vs sent',
    description: 'Commonly confused words: scent and sent',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscent\b/gi,
    message: 'Check: "scent" ("scent") vs "sent" ("sent"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sent';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_seamen_semen_608',
    name: 'seamen vs semen',
    description: 'Commonly confused words: seamen and semen',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bseamen\b/gi,
    message: 'Check: "seamen" ("seamen") vs "semen" ("semen"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'semen';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sea_see_609',
    name: 'sea vs see',
    description: 'Commonly confused words: sea and see',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsea\b/gi,
    message: 'Check: "sea" (body of water) vs "see" (to perceive with eyes). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'see';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_seas_sees_610',
    name: 'seas vs sees',
    description: 'Commonly confused words: seas and sees',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bseas\b/gi,
    message: 'Check: "seas" ("seas") vs "sees" ("sees"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sees';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_seat_set_611',
    name: 'seat vs set',
    description: 'Commonly confused words: seat and set',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bseat\b/gi,
    message: 'Check: "seat" ("seat") vs "set" ("set"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'set';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_seats_sets_612',
    name: 'seats vs sets',
    description: 'Commonly confused words: seats and sets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bseats\b/gi,
    message: 'Check: "seats" ("seats") vs "sets" ("sets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sects_sex_613',
    name: 'sects vs sex',
    description: 'Commonly confused words: sects and sex',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsects\b/gi,
    message: 'Check: "sects" ("sects") vs "sex" ("sex"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sex';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sees_seize_614',
    name: 'sees vs seize',
    description: 'Commonly confused words: sees and seize',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsees\b/gi,
    message: 'Check: "sees" ("sees") vs "seize" ("seize"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seize';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sense_since_615',
    name: 'sense vs since',
    description: 'Commonly confused words: sense and since',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsense\b/gi,
    message: 'Check: "sense" ("sense") vs "since" ("since"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'since';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_servers_serves_616',
    name: 'servers vs serves',
    description: 'Commonly confused words: servers and serves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bservers\b/gi,
    message: 'Check: "servers" ("servers") vs "serves" ("serves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'serves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_serge_surge_617',
    name: 'serge vs surge',
    description: 'Commonly confused words: serge and surge',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bserge\b/gi,
    message: 'Check: "serge" ("serge") vs "surge" ("surge"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'surge';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sever_severe_618',
    name: 'sever vs severe',
    description: 'Commonly confused words: sever and severe',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsever\b/gi,
    message: 'Check: "sever" ("sever") vs "severe" ("severe"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'severe';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shell_shall_619',
    name: 'shell vs shall',
    description: 'Commonly confused words: shell and shall',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshell\b/gi,
    message: 'Did you mean "shall"? "shell" ("shell") is often confused with "shall" ("shall").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shall';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shall_shell_620',
    name: 'shall vs shell',
    description: 'Commonly confused words: shall and shell',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshall\b/gi,
    message: 'Did you mean "shell"? "shall" ("shall") is often confused with "shell" ("shell").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shell';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shear_sheer_621',
    name: 'shear vs sheer',
    description: 'Commonly confused words: shear and sheer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshear\b/gi,
    message: 'Check: "shear" ("shear") vs "sheer" ("sheer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sheer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_showed_sowed_622',
    name: 'showed vs sowed',
    description: 'Commonly confused words: showed and sowed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshowed\b/gi,
    message: 'Check: "showed" ("showed") vs "sowed" ("sowed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sowed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shown_sown_623',
    name: 'shown vs sown',
    description: 'Commonly confused words: shown and sown',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshown\b/gi,
    message: 'Check: "shown" ("shown") vs "sown" ("sown"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sown';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_show_sow_624',
    name: 'show vs sow',
    description: 'Commonly confused words: show and sow',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshow\b/gi,
    message: 'Check: "show" ("show") vs "sow" ("sow"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sow';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sic_sick_625',
    name: 'sic vs sick',
    description: 'Commonly confused words: sic and sick',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsic\b/gi,
    message: 'Check: "sic" ("sic") vs "sick" ("sick"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sick';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sight_site_626',
    name: 'sight vs site',
    description: 'Commonly confused words: sight and site',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsight\b/gi,
    message: 'Check: "sight" (vision or view) vs "site" (location). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'site';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sing_using_627',
    name: 'sing vs using',
    description: 'Commonly confused words: sing and using',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsing\b/gi,
    message: 'Check: "sing" ("sing") vs "using" ("using"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'using';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sign_sine_628',
    name: 'sign vs sine',
    description: 'Commonly confused words: sign and sine',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsign\b/gi,
    message: 'Check: "sign" ("sign") vs "sine" ("sine"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sine';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sire_sure_629',
    name: 'sire vs sure',
    description: 'Commonly confused words: sire and sure',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsire\b/gi,
    message: 'Check: "sire" ("sire") vs "sure" ("sure"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sure';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slay_sleigh_630',
    name: 'slay vs sleigh',
    description: 'Commonly confused words: slay and sleigh',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslay\b/gi,
    message: 'Check: "slay" ("slay") vs "sleigh" ("sleigh"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sleigh';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slays_sleighs_631',
    name: 'slays vs sleighs',
    description: 'Commonly confused words: slays and sleighs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslays\b/gi,
    message: 'Check: "slays" ("slays") vs "sleighs" ("sleighs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sleighs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slew_slough_632',
    name: 'slew vs slough',
    description: 'Commonly confused words: slew and slough',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslew\b/gi,
    message: 'Check: "slew" ("slew") vs "slough" ("slough"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slough';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_soared_sword_633',
    name: 'soared vs sword',
    description: 'Commonly confused words: soared and sword',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsoared\b/gi,
    message: 'Check: "soared" ("soared") vs "sword" ("sword"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sword';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_soar_sore_634',
    name: 'soar vs sore',
    description: 'Commonly confused words: soar and sore',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsoar\b/gi,
    message: 'Check: "soar" ("soar") vs "sore" ("sore"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sore';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_soars_sores_635',
    name: 'soars vs sores',
    description: 'Commonly confused words: soars and sores',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsoars\b/gi,
    message: 'Check: "soars" ("soars") vs "sores" ("sores"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sores';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sole_soul_636',
    name: 'sole vs soul',
    description: 'Commonly confused words: sole and soul',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsole\b/gi,
    message: 'Check: "sole" ("sole") vs "soul" ("soul"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'soul';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_some_sum_637',
    name: 'some vs sum',
    description: 'Commonly confused words: some and sum',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsome\b/gi,
    message: 'Did you mean "sum"? "some" ("some") is often confused with "sum" ("sum").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sum';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sum_some_638',
    name: 'sum vs some',
    description: 'Commonly confused words: sum and some',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsum\b/gi,
    message: 'Did you mean "some"? "sum" ("sum") is often confused with "some" ("some").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'some';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_son_sun_639',
    name: 'son vs sun',
    description: 'Commonly confused words: son and sun',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bson\b/gi,
    message: 'Check: "son" (male child) vs "sun" (star). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sun';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sonny_sunny_640',
    name: 'sonny vs sunny',
    description: 'Commonly confused words: sonny and sunny',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsonny\b/gi,
    message: 'Check: "sonny" ("sonny") vs "sunny" ("sunny"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sunny';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sons_suns_641',
    name: 'sons vs suns',
    description: 'Commonly confused words: sons and suns',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsons\b/gi,
    message: 'Check: "sons" ("sons") vs "suns" ("suns"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suns';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_soot_suit_642',
    name: 'soot vs suit',
    description: 'Commonly confused words: soot and suit',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsoot\b/gi,
    message: 'Check: "soot" ("soot") vs "suit" ("suit"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suit';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spoor_spore_643',
    name: 'spoor vs spore',
    description: 'Commonly confused words: spoor and spore',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspoor\b/gi,
    message: 'Check: "spoor" ("spoor") vs "spore" ("spore"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spore';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spoors_spores_644',
    name: 'spoors vs spores',
    description: 'Commonly confused words: spoors and spores',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspoors\b/gi,
    message: 'Check: "spoors" ("spoors") vs "spores" ("spores"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spores';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stair_stare_645',
    name: 'stair vs stare',
    description: 'Commonly confused words: stair and stare',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstair\b/gi,
    message: 'Check: "stair" (step) vs "stare" (to look fixedly). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stare';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stairs_stares_646',
    name: 'stairs vs stares',
    description: 'Commonly confused words: stairs and stares',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstairs\b/gi,
    message: 'Check: "stairs" ("stairs") vs "stares" ("stares"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stares';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stake_steak_647',
    name: 'stake vs steak',
    description: 'Commonly confused words: stake and steak',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstake\b/gi,
    message: 'Check: "stake" ("stake") vs "steak" ("steak"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'steak';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stand_sand_648',
    name: 'stand vs sand',
    description: 'Commonly confused words: stand and sand',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstand\b/gi,
    message: 'Did you mean "sand"? "stand" ("stand") is often confused with "sand" ("sand").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sand';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stared_starred_649',
    name: 'stared vs starred',
    description: 'Commonly confused words: stared and starred',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstared\b/gi,
    message: 'Check: "stared" ("stared") vs "starred" ("starred"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'starred';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_staring_starring_650',
    name: 'staring vs starring',
    description: 'Commonly confused words: staring and starring',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstaring\b/gi,
    message: 'Check: "staring" ("staring") vs "starring" ("starring"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'starring';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_start_stat_651',
    name: 'start vs stat',
    description: 'Commonly confused words: start and stat',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstart\b/gi,
    message: 'Check: "start" ("start") vs "stat" ("stat"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stat';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_starts_stats_652',
    name: 'starts vs stats',
    description: 'Commonly confused words: starts and stats',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstarts\b/gi,
    message: 'Check: "starts" ("starts") vs "stats" ("stats"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stats';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stationary_stationery_653',
    name: 'stationary vs stationery',
    description: 'Commonly confused words: stationary and stationery',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstationary\b/gi,
    message: 'Check: "stationary" (not moving) vs "stationery" (writing materials). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stationery';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_steal_steel_654',
    name: 'steal vs steel',
    description: 'Commonly confused words: steal and steel',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsteal\b/gi,
    message: 'Check: "steal" (to take without permission) vs "steel" (metal alloy). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'steel';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_step_steppe_655',
    name: 'step vs steppe',
    description: 'Commonly confused words: step and steppe',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstep\b/gi,
    message: 'Check: "step" ("step") vs "steppe" ("steppe"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'steppe';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_straight_strait_656',
    name: 'straight vs strait',
    description: 'Commonly confused words: straight and strait',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstraight\b/gi,
    message: 'Check: "straight" ("straight") vs "strait" ("strait"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'strait';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_subsided_subsidized_657',
    name: 'subsided vs subsidized',
    description: 'Commonly confused words: subsided and subsidized',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsubsided\b/gi,
    message: 'Check: "subsided" ("subsided") vs "subsidized" ("subsidized"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'subsidized';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_subside_subsidize_658',
    name: 'subside vs subsidize',
    description: 'Commonly confused words: subside and subsidize',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsubside\b/gi,
    message: 'Check: "subside" ("subside") vs "subsidize" ("subsidize"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'subsidize';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_subsides_subsidizes_659',
    name: 'subsides vs subsidizes',
    description: 'Commonly confused words: subsides and subsidizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsubsides\b/gi,
    message: 'Check: "subsides" ("subsides") vs "subsidizes" ("subsidizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'subsidizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_suede_swayed_660',
    name: 'suede vs swayed',
    description: 'Commonly confused words: suede and swayed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsuede\b/gi,
    message: 'Check: "suede" ("suede") vs "swayed" ("swayed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'swayed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sue_sure_661',
    name: 'sue vs sure',
    description: 'Commonly confused words: sue and sure',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsue\b/gi,
    message: 'Check: "sue" ("sue") vs "sure" ("sure"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sure';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_suite_sweet_662',
    name: 'suite vs sweet',
    description: 'Commonly confused words: suite and sweet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsuite\b/gi,
    message: 'Check: "suite" ("suite") vs "sweet" ("sweet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sweet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sour_your_767',
    name: 'sour vs your',
    description: 'Commonly confused words: sour and your',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsour\b/gi,
    message: 'Did you mean "your"? "sour" ("sour") is often confused with "your" (belonging to you).',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'your';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_split_spilt_771',
    name: 'split vs spilt',
    description: 'Commonly confused words: split and spilt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsplit\b/gi,
    message: 'Did you mean "spilt"? "split" ("split") is often confused with "spilt" ("spilt").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spilt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slitting_splitting_772',
    name: 'slitting vs splitting',
    description: 'Commonly confused words: slitting and splitting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslitting\b/gi,
    message: 'Did you mean "splitting"? "slitting" ("slitting") is often confused with "splitting" ("splitting").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'splitting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_split_slit_773',
    name: 'split vs slit',
    description: 'Commonly confused words: split and slit',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsplit\b/gi,
    message: 'Did you mean "slit"? "split" ("split") is often confused with "slit" ("slit").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slit';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_site_size_788',
    name: 'site vs size',
    description: 'Commonly confused words: site and size',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsite\b/gi,
    message: 'Check: "site" (location) vs "size" ("size"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'size';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_saber_sager_3518',
    name: 'saber vs sager',
    description: 'Commonly confused words: saber and sager',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsaber\b/gi,
    message: 'Check: "saber" ("saber") vs "sager" ("sager"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sager';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_saber_saver_3519',
    name: 'saber vs saver',
    description: 'Commonly confused words: saber and saver',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsaber\b/gi,
    message: 'Check: "saber" ("saber") vs "saver" ("saver"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'saver';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sacrificed_sacrifices_3520',
    name: 'sacrificed vs sacrifices',
    description: 'Commonly confused words: sacrificed and sacrifices',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsacrificed\b/gi,
    message: 'Check: "sacrificed" ("sacrificed") vs "sacrifices" ("sacrifices"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sacrifices';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_saddled_saddles_3521',
    name: 'saddled vs saddles',
    description: 'Commonly confused words: saddled and saddles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsaddled\b/gi,
    message: 'Check: "saddled" ("saddled") vs "saddles" ("saddles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'saddles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_saddles_waddles_3522',
    name: 'saddles vs waddles',
    description: 'Commonly confused words: saddles and waddles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsaddles\b/gi,
    message: 'Check: "saddles" ("saddles") vs "waddles" ("waddles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'waddles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_saddling_waddling_3523',
    name: 'saddling vs waddling',
    description: 'Commonly confused words: saddling and waddling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsaddling\b/gi,
    message: 'Check: "saddling" ("saddling") vs "waddling" ("waddling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'waddling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_safely_sagely_3524',
    name: 'safely vs sagely',
    description: 'Commonly confused words: safely and sagely',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsafely\b/gi,
    message: 'Check: "safely" ("safely") vs "sagely" ("sagely"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sagely';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_safer_sager_3525',
    name: 'safer vs sager',
    description: 'Commonly confused words: safer and sager',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsafer\b/gi,
    message: 'Check: "safer" ("safer") vs "sager" ("sager"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sager';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_safer_saver_3526',
    name: 'safer vs saver',
    description: 'Commonly confused words: safer and saver',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsafer\b/gi,
    message: 'Check: "safer" ("safer") vs "saver" ("saver"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'saver';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_safer_wafer_3527',
    name: 'safer vs wafer',
    description: 'Commonly confused words: safer and wafer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsafer\b/gi,
    message: 'Check: "safer" ("safer") vs "wafer" ("wafer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wafer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_safe_sage_3528',
    name: 'safe vs sage',
    description: 'Commonly confused words: safe and sage',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsafe\b/gi,
    message: 'Check: "safe" ("safe") vs "sage" ("sage"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sage';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_salaried_salaries_3529',
    name: 'salaried vs salaries',
    description: 'Commonly confused words: salaried and salaries',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsalaried\b/gi,
    message: 'Check: "salaried" ("salaried") vs "salaries" ("salaries"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'salaries';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sallied_sallies_3530',
    name: 'sallied vs sallies',
    description: 'Commonly confused words: sallied and sallies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsallied\b/gi,
    message: 'Check: "sallied" ("sallied") vs "sallies" ("sallies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sallies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_salvaged_salvages_3531',
    name: 'salvaged vs salvages',
    description: 'Commonly confused words: salvaged and salvages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsalvaged\b/gi,
    message: 'Check: "salvaged" ("salvaged") vs "salvages" ("salvages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'salvages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_salved_salves_3532',
    name: 'salved vs salves',
    description: 'Commonly confused words: salved and salves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsalved\b/gi,
    message: 'Check: "salved" ("salved") vs "salves" ("salves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'salves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_same_dame_3533',
    name: 'same vs dame',
    description: 'Commonly confused words: same and dame',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsame\b/gi,
    message: 'Check: "same" ("same") vs "dame" ("dame"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dame';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sanctified_sanctifies_3534',
    name: 'sanctified vs sanctifies',
    description: 'Commonly confused words: sanctified and sanctifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsanctified\b/gi,
    message: 'Check: "sanctified" ("sanctified") vs "sanctifies" ("sanctifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sanctifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sandwiched_sandwiches_3535',
    name: 'sandwiched vs sandwiches',
    description: 'Commonly confused words: sandwiched and sandwiches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsandwiched\b/gi,
    message: 'Check: "sandwiched" ("sandwiched") vs "sandwiches" ("sandwiches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sandwiches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sashes_dashes_3536',
    name: 'sashes vs dashes',
    description: 'Commonly confused words: sashes and dashes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsashes\b/gi,
    message: 'Check: "sashes" ("sashes") vs "dashes" ("dashes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dashes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_saturated_saturates_3537',
    name: 'saturated vs saturates',
    description: 'Commonly confused words: saturated and saturates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsaturated\b/gi,
    message: 'Check: "saturated" ("saturated") vs "saturates" ("saturates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'saturates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sauced_sauces_3538',
    name: 'sauced vs sauces',
    description: 'Commonly confused words: sauced and sauces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsauced\b/gi,
    message: 'Check: "sauced" ("sauced") vs "sauces" ("sauces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sauces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_says_days_3539',
    name: 'says vs days',
    description: 'Commonly confused words: says and days',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsays\b/gi,
    message: 'Check: "says" ("says") vs "days" ("days"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'days';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scab_scag_3540',
    name: 'scab vs scag',
    description: 'Commonly confused words: scab and scag',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscab\b/gi,
    message: 'Check: "scab" ("scab") vs "scag" ("scag"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scag';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scag_scat_3541',
    name: 'scag vs scat',
    description: 'Commonly confused words: scag and scat',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscag\b/gi,
    message: 'Check: "scag" ("scag") vs "scat" ("scat"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scat';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scaled_scales_3542',
    name: 'scaled vs scales',
    description: 'Commonly confused words: scaled and scales',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscaled\b/gi,
    message: 'Check: "scaled" ("scaled") vs "scales" ("scales"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scales';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scandalized_scandalizes_3543',
    name: 'scandalized vs scandalizes',
    description: 'Commonly confused words: scandalized and scandalizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscandalized\b/gi,
    message: 'Check: "scandalized" ("scandalized") vs "scandalizes" ("scandalizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scandalizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scapulae_scapular_3544',
    name: 'scapulae vs scapular',
    description: 'Commonly confused words: scapulae and scapular',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscapulae\b/gi,
    message: 'Check: "scapulae" ("scapulae") vs "scapular" ("scapular"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scapular';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scarfed_scarred_3545',
    name: 'scarfed vs scarred',
    description: 'Commonly confused words: scarfed and scarred',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscarfed\b/gi,
    message: 'Check: "scarfed" ("scarfed") vs "scarred" ("scarred"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scarred';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scavenged_scavenges_3546',
    name: 'scavenged vs scavenges',
    description: 'Commonly confused words: scavenged and scavenges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscavenged\b/gi,
    message: 'Check: "scavenged" ("scavenged") vs "scavenges" ("scavenges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scavenges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scheduled_schedules_3547',
    name: 'scheduled vs schedules',
    description: 'Commonly confused words: scheduled and schedules',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscheduled\b/gi,
    message: 'Check: "scheduled" ("scheduled") vs "schedules" ("schedules"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'schedules';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_schemed_schemes_3548',
    name: 'schemed vs schemes',
    description: 'Commonly confused words: schemed and schemes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bschemed\b/gi,
    message: 'Check: "schemed" ("schemed") vs "schemes" ("schemes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'schemes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sci_xci_3549',
    name: 'sci vs xci',
    description: 'Commonly confused words: sci and xci',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsci\b/gi,
    message: 'Check: "sci" ("sci") vs "xci" ("xci"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'xci';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scoped_scopes_3550',
    name: 'scoped vs scopes',
    description: 'Commonly confused words: scoped and scopes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscoped\b/gi,
    message: 'Check: "scoped" ("scoped") vs "scopes" ("scopes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scopes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scorched_scorches_3551',
    name: 'scorched vs scorches',
    description: 'Commonly confused words: scorched and scorches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscorched\b/gi,
    message: 'Check: "scorched" ("scorched") vs "scorches" ("scorches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scorches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scourged_scourges_3552',
    name: 'scourged vs scourges',
    description: 'Commonly confused words: scourged and scourges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscourged\b/gi,
    message: 'Check: "scourged" ("scourged") vs "scourges" ("scourges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scourges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scour_scout_3553',
    name: 'scour vs scout',
    description: 'Commonly confused words: scour and scout',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscour\b/gi,
    message: 'Check: "scour" ("scour") vs "scout" ("scout"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scout';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scours_scouts_3554',
    name: 'scours vs scouts',
    description: 'Commonly confused words: scours and scouts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscours\b/gi,
    message: 'Check: "scours" ("scours") vs "scouts" ("scouts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scouts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_screed_screes_3555',
    name: 'screed vs screes',
    description: 'Commonly confused words: screed and screes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscreed\b/gi,
    message: 'Check: "screed" ("screed") vs "screes" ("screes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'screes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_screes_screws_3556',
    name: 'screes vs screws',
    description: 'Commonly confused words: screes and screws',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscrees\b/gi,
    message: 'Check: "screes" ("screes") vs "screws" ("screws"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'screws';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scrimmaged_scrimmages_3557',
    name: 'scrimmaged vs scrimmages',
    description: 'Commonly confused words: scrimmaged and scrimmages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscrimmaged\b/gi,
    message: 'Check: "scrimmaged" ("scrimmaged") vs "scrimmages" ("scrimmages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scrimmages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scrimmage_scrummage_3558',
    name: 'scrimmage vs scrummage',
    description: 'Commonly confused words: scrimmage and scrummage',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscrimmage\b/gi,
    message: 'Check: "scrimmage" ("scrimmage") vs "scrummage" ("scrummage"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scrummage';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scrim_scrum_3559',
    name: 'scrim vs scrum',
    description: 'Commonly confused words: scrim and scrum',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscrim\b/gi,
    message: 'Check: "scrim" ("scrim") vs "scrum" ("scrum"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scrum';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scrutinized_scrutinizes_3560',
    name: 'scrutinized vs scrutinizes',
    description: 'Commonly confused words: scrutinized and scrutinizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscrutinized\b/gi,
    message: 'Check: "scrutinized" ("scrutinized") vs "scrutinizes" ("scrutinizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scrutinizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scuffled_scuffles_3561',
    name: 'scuffled vs scuffles',
    description: 'Commonly confused words: scuffled and scuffles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscuffled\b/gi,
    message: 'Check: "scuffled" ("scuffled") vs "scuffles" ("scuffles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scuffles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scuff_scurf_3562',
    name: 'scuff vs scurf',
    description: 'Commonly confused words: scuff and scurf',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscuff\b/gi,
    message: 'Check: "scuff" ("scuff") vs "scurf" ("scurf"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scurf';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sculptured_sculptures_3563',
    name: 'sculptured vs sculptures',
    description: 'Commonly confused words: sculptured and sculptures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsculptured\b/gi,
    message: 'Check: "sculptured" ("sculptured") vs "sculptures" ("sculptures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sculptures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scurfy_scurry_3564',
    name: 'scurfy vs scurry',
    description: 'Commonly confused words: scurfy and scurry',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscurfy\b/gi,
    message: 'Check: "scurfy" ("scurfy") vs "scurry" ("scurry"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scurry';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scurfy_scurvy_3565',
    name: 'scurfy vs scurvy',
    description: 'Commonly confused words: scurfy and scurvy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscurfy\b/gi,
    message: 'Check: "scurfy" ("scurfy") vs "scurvy" ("scurvy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scurvy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_scythed_scythes_3566',
    name: 'scythed vs scythes',
    description: 'Commonly confused words: scythed and scythes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bscythed\b/gi,
    message: 'Check: "scythed" ("scythed") vs "scythes" ("scythes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scythes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_seals_deals_3567',
    name: 'seals vs deals',
    description: 'Commonly confused words: seals and deals',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bseals\b/gi,
    message: 'Check: "seals" ("seals") vs "deals" ("deals"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deals';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_searched_searches_3568',
    name: 'searched vs searches',
    description: 'Commonly confused words: searched and searches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsearched\b/gi,
    message: 'Check: "searched" ("searched") vs "searches" ("searches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'searches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_seating_searing_3569',
    name: 'seating vs searing',
    description: 'Commonly confused words: seating and searing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bseating\b/gi,
    message: 'Check: "seating" ("seating") vs "searing" ("searing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'searing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_secluded_secludes_3570',
    name: 'secluded vs secludes',
    description: 'Commonly confused words: secluded and secludes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsecluded\b/gi,
    message: 'Check: "secluded" ("secluded") vs "secludes" ("secludes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'secludes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_secreted_secretes_3571',
    name: 'secreted vs secretes',
    description: 'Commonly confused words: secreted and secretes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsecreted\b/gi,
    message: 'Check: "secreted" ("secreted") vs "secretes" ("secretes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'secretes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sect_secy_3572',
    name: 'sect vs secy',
    description: 'Commonly confused words: sect and secy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsect\b/gi,
    message: 'Check: "sect" ("sect") vs "secy" ("secy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'secy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_secy_sexy_3573',
    name: 'secy vs sexy',
    description: 'Commonly confused words: secy and sexy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsecy\b/gi,
    message: 'Check: "secy" ("secy") vs "sexy" ("sexy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sexy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sedated_sedates_3574',
    name: 'sedated vs sedates',
    description: 'Commonly confused words: sedated and sedates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsedated\b/gi,
    message: 'Check: "sedated" ("sedated") vs "sedates" ("sedates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sedates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_seeds_deeds_3575',
    name: 'seeds vs deeds',
    description: 'Commonly confused words: seeds and deeds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bseeds\b/gi,
    message: 'Check: "seeds" ("seeds") vs "deeds" ("deeds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deeds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_seedy_weedy_3576',
    name: 'seedy vs weedy',
    description: 'Commonly confused words: seedy and weedy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bseedy\b/gi,
    message: 'Check: "seedy" ("seedy") vs "weedy" ("weedy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'weedy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_seem_seen_3577',
    name: 'seem vs seen',
    description: 'Commonly confused words: seem and seen',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bseem\b/gi,
    message: 'Check: "seem" ("seem") vs "seen" ("seen"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seen';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_seer_deer_3578',
    name: 'seer vs deer',
    description: 'Commonly confused words: seer and deer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bseer\b/gi,
    message: 'Check: "seer" ("seer") vs "deer" ("deer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_segregated_segregates_3579',
    name: 'segregated vs segregates',
    description: 'Commonly confused words: segregated and segregates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsegregated\b/gi,
    message: 'Check: "segregated" ("segregated") vs "segregates" ("segregates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'segregates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sells_dells_3580',
    name: 'sells vs dells',
    description: 'Commonly confused words: sells and dells',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsells\b/gi,
    message: 'Check: "sells" ("sells") vs "dells" ("dells"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dells';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sens_dens_3581',
    name: 'sens vs dens',
    description: 'Commonly confused words: sens and dens',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsens\b/gi,
    message: 'Check: "sens" ("sens") vs "dens" ("dens"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dens';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sensitized_sensitizes_3582',
    name: 'sensitized vs sensitizes',
    description: 'Commonly confused words: sensitized and sensitizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsensitized\b/gi,
    message: 'Check: "sensitized" ("sensitized") vs "sensitizes" ("sensitizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sensitizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sent_dent_3583',
    name: 'sent vs dent',
    description: 'Commonly confused words: sent and dent',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsent\b/gi,
    message: 'Check: "sent" ("sent") vs "dent" ("dent"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dent';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sentenced_sentences_3584',
    name: 'sentenced vs sentences',
    description: 'Commonly confused words: sentenced and sentences',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsentenced\b/gi,
    message: 'Check: "sentenced" ("sentenced") vs "sentences" ("sentences"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sentences';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_separated_separates_3585',
    name: 'separated vs separates',
    description: 'Commonly confused words: separated and separates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bseparated\b/gi,
    message: 'Check: "separated" ("separated") vs "separates" ("separates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'separates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sequenced_sequences_3586',
    name: 'sequenced vs sequences',
    description: 'Commonly confused words: sequenced and sequences',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsequenced\b/gi,
    message: 'Check: "sequenced" ("sequenced") vs "sequences" ("sequences"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sequences';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_serialized_serializes_3587',
    name: 'serialized vs serializes',
    description: 'Commonly confused words: serialized and serializes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bserialized\b/gi,
    message: 'Check: "serialized" ("serialized") vs "serializes" ("serializes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'serializes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_served_serves_3588',
    name: 'served vs serves',
    description: 'Commonly confused words: served and serves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bserved\b/gi,
    message: 'Check: "served" ("served") vs "serves" ("serves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'serves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_setbacks_wetbacks_3589',
    name: 'setbacks vs wetbacks',
    description: 'Commonly confused words: setbacks and wetbacks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsetbacks\b/gi,
    message: 'Check: "setbacks" ("setbacks") vs "wetbacks" ("wetbacks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wetbacks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_setback_wetback_3590',
    name: 'setback vs wetback',
    description: 'Commonly confused words: setback and wetback',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsetback\b/gi,
    message: 'Check: "setback" ("setback") vs "wetback" ("wetback"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wetback';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_setter_settee_3591',
    name: 'setter vs settee',
    description: 'Commonly confused words: setter and settee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsetter\b/gi,
    message: 'Check: "setter" ("setter") vs "settee" ("settee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'settee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_setters_settees_3592',
    name: 'setters vs settees',
    description: 'Commonly confused words: setters and settees',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsetters\b/gi,
    message: 'Check: "setters" ("setters") vs "settees" ("settees"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'settees';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_seventh_seventy_3593',
    name: 'seventh vs seventy',
    description: 'Commonly confused words: seventh and seventy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bseventh\b/gi,
    message: 'Check: "seventh" ("seventh") vs "seventy" ("seventy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seventy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sexed_sexes_3594',
    name: 'sexed vs sexes',
    description: 'Commonly confused words: sexed and sexes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsexed\b/gi,
    message: 'Check: "sexed" ("sexed") vs "sexes" ("sexes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sexes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shacked_whacked_3595',
    name: 'shacked vs whacked',
    description: 'Commonly confused words: shacked and whacked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshacked\b/gi,
    message: 'Check: "shacked" ("shacked") vs "whacked" ("whacked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'whacked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shacking_snacking_3596',
    name: 'shacking vs snacking',
    description: 'Commonly confused words: shacking and snacking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshacking\b/gi,
    message: 'Check: "shacking" ("shacking") vs "snacking" ("snacking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snacking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shackled_shackles_3597',
    name: 'shackled vs shackles',
    description: 'Commonly confused words: shackled and shackles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshackled\b/gi,
    message: 'Check: "shackled" ("shackled") vs "shackles" ("shackles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shackles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shack_snack_3598',
    name: 'shack vs snack',
    description: 'Commonly confused words: shack and snack',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshack\b/gi,
    message: 'Check: "shack" ("shack") vs "snack" ("snack"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snack';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shaded_shades_3599',
    name: 'shaded vs shades',
    description: 'Commonly confused words: shaded and shades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshaded\b/gi,
    message: 'Check: "shaded" ("shaded") vs "shades" ("shades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shahs_shays_3600',
    name: 'shahs vs shays',
    description: 'Commonly confused words: shahs and shays',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshahs\b/gi,
    message: 'Check: "shahs" ("shahs") vs "shays" ("shays"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shays';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shale_shake_3601',
    name: 'shale vs shake',
    description: 'Commonly confused words: shale and shake',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshale\b/gi,
    message: 'Check: "shale" ("shale") vs "shake" ("shake"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shake';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shaped_shapes_3602',
    name: 'shaped vs shapes',
    description: 'Commonly confused words: shaped and shapes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshaped\b/gi,
    message: 'Check: "shaped" ("shaped") vs "shapes" ("shapes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shapes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shared_shares_3603',
    name: 'shared vs shares',
    description: 'Commonly confused words: shared and shares',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshared\b/gi,
    message: 'Check: "shared" ("shared") vs "shares" ("shares"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shares';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sheathed_sheathes_3604',
    name: 'sheathed vs sheathes',
    description: 'Commonly confused words: sheathed and sheathes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsheathed\b/gi,
    message: 'Check: "sheathed" ("sheathed") vs "sheathes" ("sheathes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sheathes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sheen_shewn_3605',
    name: 'sheen vs shewn',
    description: 'Commonly confused words: sheen and shewn',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsheen\b/gi,
    message: 'Check: "sheen" ("sheen") vs "shewn" ("shewn"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shewn';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sheered_sneered_3606',
    name: 'sheered vs sneered',
    description: 'Commonly confused words: sheered and sneered',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsheered\b/gi,
    message: 'Check: "sheered" ("sheered") vs "sneered" ("sneered"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sneered';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sheer_sheet_3607',
    name: 'sheer vs sheet',
    description: 'Commonly confused words: sheer and sheet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsheer\b/gi,
    message: 'Check: "sheer" ("sheer") vs "sheet" ("sheet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sheet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sheers_sneers_3608',
    name: 'sheers vs sneers',
    description: 'Commonly confused words: sheers and sneers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsheers\b/gi,
    message: 'Check: "sheers" ("sheers") vs "sneers" ("sneers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sneers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shelved_shelves_3609',
    name: 'shelved vs shelves',
    description: 'Commonly confused words: shelved and shelves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshelved\b/gi,
    message: 'Check: "shelved" ("shelved") vs "shelves" ("shelves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shelves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shh_shy_3610',
    name: 'shh vs shy',
    description: 'Commonly confused words: shh and shy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshh\b/gi,
    message: 'Check: "shh" ("shh") vs "shy" ("shy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shied_shies_3611',
    name: 'shied vs shies',
    description: 'Commonly confused words: shied and shies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshied\b/gi,
    message: 'Check: "shied" ("shied") vs "shies" ("shies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shifts_shirts_3612',
    name: 'shifts vs shirts',
    description: 'Commonly confused words: shifts and shirts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshifts\b/gi,
    message: 'Check: "shifts" ("shifts") vs "shirts" ("shirts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shirts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shifty_shirty_3613',
    name: 'shifty vs shirty',
    description: 'Commonly confused words: shifty and shirty',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshifty\b/gi,
    message: 'Check: "shifty" ("shifty") vs "shirty" ("shirty"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shirty';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shims_shins_3614',
    name: 'shims vs shins',
    description: 'Commonly confused words: shims and shins',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshims\b/gi,
    message: 'Check: "shims" ("shims") vs "shins" ("shins"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shins';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shiners_whiners_3615',
    name: 'shiners vs whiners',
    description: 'Commonly confused words: shiners and whiners',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshiners\b/gi,
    message: 'Check: "shiners" ("shiners") vs "whiners" ("whiners"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'whiners';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shinned_shunned_3616',
    name: 'shinned vs shunned',
    description: 'Commonly confused words: shinned and shunned',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshinned\b/gi,
    message: 'Check: "shinned" ("shinned") vs "shunned" ("shunned"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shunned';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shipped_whipped_3617',
    name: 'shipped vs whipped',
    description: 'Commonly confused words: shipped and whipped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshipped\b/gi,
    message: 'Check: "shipped" ("shipped") vs "whipped" ("whipped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'whipped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shippers_whippers_3618',
    name: 'shippers vs whippers',
    description: 'Commonly confused words: shippers and whippers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshippers\b/gi,
    message: 'Check: "shippers" ("shippers") vs "whippers" ("whippers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'whippers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shipping_shopping_3619',
    name: 'shipping vs shopping',
    description: 'Commonly confused words: shipping and shopping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshipping\b/gi,
    message: 'Check: "shipping" ("shipping") vs "shopping" ("shopping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shopping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ship_shop_3620',
    name: 'ship vs shop',
    description: 'Commonly confused words: ship and shop',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bship\b/gi,
    message: 'Check: "ship" ("ship") vs "shop" ("shop"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shop';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shire_shore_3621',
    name: 'shire vs shore',
    description: 'Commonly confused words: shire and shore',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshire\b/gi,
    message: 'Check: "shire" ("shire") vs "shore" ("shore"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shore';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shirted_shorted_3622',
    name: 'shirted vs shorted',
    description: 'Commonly confused words: shirted and shorted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshirted\b/gi,
    message: 'Check: "shirted" ("shirted") vs "shorted" ("shorted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shorted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shirting_shitting_3623',
    name: 'shirting vs shitting',
    description: 'Commonly confused words: shirting and shitting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshirting\b/gi,
    message: 'Check: "shirting" ("shirting") vs "shitting" ("shitting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shitting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shirty_shitty_3624',
    name: 'shirty vs shitty',
    description: 'Commonly confused words: shirty and shitty',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshirty\b/gi,
    message: 'Check: "shirty" ("shirty") vs "shitty" ("shitty"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shitty';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shitting_shutting_3625',
    name: 'shitting vs shutting',
    description: 'Commonly confused words: shitting and shutting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshitting\b/gi,
    message: 'Check: "shitting" ("shitting") vs "shutting" ("shutting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shutting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shoeing_shoring_3626',
    name: 'shoeing vs shoring',
    description: 'Commonly confused words: shoeing and shoring',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshoeing\b/gi,
    message: 'Check: "shoeing" ("shoeing") vs "shoring" ("shoring"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shoring';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shoes_shies_3627',
    name: 'shoes vs shies',
    description: 'Commonly confused words: shoes and shies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshoes\b/gi,
    message: 'Check: "shoes" ("shoes") vs "shies" ("shies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shoes_shows_3628',
    name: 'shoes vs shows',
    description: 'Commonly confused words: shoes and shows',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshoes\b/gi,
    message: 'Check: "shoes" ("shoes") vs "shows" ("shows"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shows';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shoots_snoots_3629',
    name: 'shoots vs snoots',
    description: 'Commonly confused words: shoots and snoots',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshoots\b/gi,
    message: 'Check: "shoots" ("shoots") vs "snoots" ("snoots"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snoots';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shored_shores_3630',
    name: 'shored vs shores',
    description: 'Commonly confused words: shored and shores',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshored\b/gi,
    message: 'Check: "shored" ("shored") vs "shores" ("shores"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shores';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shored_snored_3631',
    name: 'shored vs snored',
    description: 'Commonly confused words: shored and snored',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshored\b/gi,
    message: 'Check: "shored" ("shored") vs "snored" ("snored"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snored';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shores_snores_3632',
    name: 'shores vs snores',
    description: 'Commonly confused words: shores and snores',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshores\b/gi,
    message: 'Check: "shores" ("shores") vs "snores" ("snores"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snores';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shorted_snorted_3633',
    name: 'shorted vs snorted',
    description: 'Commonly confused words: shorted and snorted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshorted\b/gi,
    message: 'Check: "shorted" ("shorted") vs "snorted" ("snorted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snorted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shorter_snorter_3634',
    name: 'shorter vs snorter',
    description: 'Commonly confused words: shorter and snorter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshorter\b/gi,
    message: 'Check: "shorter" ("shorter") vs "snorter" ("snorter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snorter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shout_snout_3635',
    name: 'shout vs snout',
    description: 'Commonly confused words: shout and snout',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshout\b/gi,
    message: 'Check: "shout" ("shout") vs "snout" ("snout"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snout';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shrews_shrewd_3636',
    name: 'shrews vs shrewd',
    description: 'Commonly confused words: shrews and shrewd',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshrews\b/gi,
    message: 'Check: "shrews" ("shrews") vs "shrewd" ("shrewd"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shrewd';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shrink_shrunk_3637',
    name: 'shrink vs shrunk',
    description: 'Commonly confused words: shrink and shrunk',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshrink\b/gi,
    message: 'Check: "shrink" ("shrink") vs "shrunk" ("shrunk"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shrunk';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shrub_shrug_3638',
    name: 'shrub vs shrug',
    description: 'Commonly confused words: shrub and shrug',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshrub\b/gi,
    message: 'Check: "shrub" ("shrub") vs "shrug" ("shrug"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shrug';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shuffled_shuffles_3639',
    name: 'shuffled vs shuffles',
    description: 'Commonly confused words: shuffled and shuffles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshuffled\b/gi,
    message: 'Check: "shuffled" ("shuffled") vs "shuffles" ("shuffles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shuffles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shuffles_snuffles_3640',
    name: 'shuffles vs snuffles',
    description: 'Commonly confused words: shuffles and snuffles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshuffles\b/gi,
    message: 'Check: "shuffles" ("shuffles") vs "snuffles" ("snuffles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snuffles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shuns_shins_3641',
    name: 'shuns vs shins',
    description: 'Commonly confused words: shuns and shins',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshuns\b/gi,
    message: 'Check: "shuns" ("shuns") vs "shins" ("shins"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shins';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shut_shit_3642',
    name: 'shut vs shit',
    description: 'Commonly confused words: shut and shit',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshut\b/gi,
    message: 'Check: "shut" ("shut") vs "shit" ("shit"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shit';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shuts_shits_3643',
    name: 'shuts vs shits',
    description: 'Commonly confused words: shuts and shits',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshuts\b/gi,
    message: 'Check: "shuts" ("shuts") vs "shits" ("shits"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shits';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_shuttled_shuttles_3644',
    name: 'shuttled vs shuttles',
    description: 'Commonly confused words: shuttled and shuttles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bshuttled\b/gi,
    message: 'Check: "shuttled" ("shuttled") vs "shuttles" ("shuttles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shuttles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sick_dick_3645',
    name: 'sick vs dick',
    description: 'Commonly confused words: sick and dick',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsick\b/gi,
    message: 'Check: "sick" ("sick") vs "dick" ("dick"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dick';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sicker_dicker_3646',
    name: 'sicker vs dicker',
    description: 'Commonly confused words: sicker and dicker',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsicker\b/gi,
    message: 'Check: "sicker" ("sicker") vs "dicker" ("dicker"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dicker';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sickles_suckles_3647',
    name: 'sickles vs suckles',
    description: 'Commonly confused words: sickles and suckles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsickles\b/gi,
    message: 'Check: "sickles" ("sickles") vs "suckles" ("suckles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suckles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sickle_suckle_3648',
    name: 'sickle vs suckle',
    description: 'Commonly confused words: sickle and suckle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsickle\b/gi,
    message: 'Check: "sickle" ("sickle") vs "suckle" ("suckle"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suckle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sided_sides_3649',
    name: 'sided vs sides',
    description: 'Commonly confused words: sided and sides',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsided\b/gi,
    message: 'Check: "sided" ("sided") vs "sides" ("sides"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sides';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sidelined_sidelines_3650',
    name: 'sidelined vs sidelines',
    description: 'Commonly confused words: sidelined and sidelines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsidelined\b/gi,
    message: 'Check: "sidelined" ("sidelined") vs "sidelines" ("sidelines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sidelines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_siding_aiding_3651',
    name: 'siding vs aiding',
    description: 'Commonly confused words: siding and aiding',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsiding\b/gi,
    message: 'Check: "siding" ("siding") vs "aiding" ("aiding"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'aiding';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sieved_sieves_3652',
    name: 'sieved vs sieves',
    description: 'Commonly confused words: sieved and sieves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsieved\b/gi,
    message: 'Check: "sieved" ("sieved") vs "sieves" ("sieves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sieves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sighing_signing_3653',
    name: 'sighing vs signing',
    description: 'Commonly confused words: sighing and signing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsighing\b/gi,
    message: 'Check: "sighing" ("sighing") vs "signing" ("signing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'signing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_signer_signet_3654',
    name: 'signer vs signet',
    description: 'Commonly confused words: signer and signet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsigner\b/gi,
    message: 'Check: "signer" ("signer") vs "signet" ("signet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'signet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_signers_signets_3655',
    name: 'signers vs signets',
    description: 'Commonly confused words: signers and signets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsigners\b/gi,
    message: 'Check: "signers" ("signers") vs "signets" ("signets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'signets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_signified_signifies_3656',
    name: 'signified vs signifies',
    description: 'Commonly confused words: signified and signifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsignified\b/gi,
    message: 'Check: "signified" ("signified") vs "signifies" ("signifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'signifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_silhouetted_silhouettes_3657',
    name: 'silhouetted vs silhouettes',
    description: 'Commonly confused words: silhouetted and silhouettes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsilhouetted\b/gi,
    message: 'Check: "silhouetted" ("silhouetted") vs "silhouettes" ("silhouettes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'silhouettes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_silkier_sillier_3658',
    name: 'silkier vs sillier',
    description: 'Commonly confused words: silkier and sillier',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsilkier\b/gi,
    message: 'Check: "silkier" ("silkier") vs "sillier" ("sillier"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sillier';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_silks_sills_3659',
    name: 'silks vs sills',
    description: 'Commonly confused words: silks and sills',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsilks\b/gi,
    message: 'Check: "silks" ("silks") vs "sills" ("sills"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sills';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sillies_sullies_3660',
    name: 'sillies vs sullies',
    description: 'Commonly confused words: sillies and sullies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsillies\b/gi,
    message: 'Check: "sillies" ("sillies") vs "sullies" ("sullies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sullies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_silly_dilly_3661',
    name: 'silly vs dilly',
    description: 'Commonly confused words: silly and dilly',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsilly\b/gi,
    message: 'Check: "silly" ("silly") vs "dilly" ("dilly"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dilly';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_silos_sills_3662',
    name: 'silos vs sills',
    description: 'Commonly confused words: silos and sills',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsilos\b/gi,
    message: 'Check: "silos" ("silos") vs "sills" ("sills"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sills';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_silos_solos_3663',
    name: 'silos vs solos',
    description: 'Commonly confused words: silos and solos',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsilos\b/gi,
    message: 'Check: "silos" ("silos") vs "solos" ("solos"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'solos';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_silting_wilting_3664',
    name: 'silting vs wilting',
    description: 'Commonly confused words: silting and wilting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsilting\b/gi,
    message: 'Check: "silting" ("silting") vs "wilting" ("wilting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wilting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_silvers_solvers_3665',
    name: 'silvers vs solvers',
    description: 'Commonly confused words: silvers and solvers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsilvers\b/gi,
    message: 'Check: "silvers" ("silvers") vs "solvers" ("solvers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'solvers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_simmered_summered_3666',
    name: 'simmered vs summered',
    description: 'Commonly confused words: simmered and summered',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsimmered\b/gi,
    message: 'Check: "simmered" ("simmered") vs "summered" ("summered"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'summered';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_simmering_summering_3667',
    name: 'simmering vs summering',
    description: 'Commonly confused words: simmering and summering',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsimmering\b/gi,
    message: 'Check: "simmering" ("simmering") vs "summering" ("summering"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'summering';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sims_aims_3668',
    name: 'sims vs aims',
    description: 'Commonly confused words: sims and aims',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsims\b/gi,
    message: 'Check: "sims" ("sims") vs "aims" ("aims"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'aims';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sims_dims_3669',
    name: 'sims vs dims',
    description: 'Commonly confused words: sims and dims',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsims\b/gi,
    message: 'Check: "sims" ("sims") vs "dims" ("dims"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dims';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sine_dine_3670',
    name: 'sine vs dine',
    description: 'Commonly confused words: sine and dine',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsine\b/gi,
    message: 'Check: "sine" ("sine") vs "dine" ("dine"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dine';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sines_dines_3671',
    name: 'sines vs dines',
    description: 'Commonly confused words: sines and dines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsines\b/gi,
    message: 'Check: "sines" ("sines") vs "dines" ("dines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sinew_sines_3672',
    name: 'sinew vs sines',
    description: 'Commonly confused words: sinew and sines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsinew\b/gi,
    message: 'Check: "sinew" ("sinew") vs "sines" ("sines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_singes_singed_3673',
    name: 'singes vs singed',
    description: 'Commonly confused words: singes and singed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsinges\b/gi,
    message: 'Check: "singes" ("singes") vs "singed" ("singed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'singed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_singled_singles_3674',
    name: 'singled vs singles',
    description: 'Commonly confused words: singled and singles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsingled\b/gi,
    message: 'Check: "singled" ("singled") vs "singles" ("singles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'singles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sinless_sunless_3675',
    name: 'sinless vs sunless',
    description: 'Commonly confused words: sinless and sunless',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsinless\b/gi,
    message: 'Check: "sinless" ("sinless") vs "sunless" ("sunless"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sunless';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sinners_winners_3676',
    name: 'sinners vs winners',
    description: 'Commonly confused words: sinners and winners',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsinners\b/gi,
    message: 'Check: "sinners" ("sinners") vs "winners" ("winners"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'winners';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sipper_dipper_3677',
    name: 'sipper vs dipper',
    description: 'Commonly confused words: sipper and dipper',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsipper\b/gi,
    message: 'Check: "sipper" ("sipper") vs "dipper" ("dipper"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dipper';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sir_air_3678',
    name: 'sir vs air',
    description: 'Commonly confused words: sir and air',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsir\b/gi,
    message: 'Check: "sir" ("sir") vs "air" ("air"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'air';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sired_aired_3679',
    name: 'sired vs aired',
    description: 'Commonly confused words: sired and aired',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsired\b/gi,
    message: 'Check: "sired" ("sired") vs "aired" ("aired"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'aired';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sire_dire_3680',
    name: 'sire vs dire',
    description: 'Commonly confused words: sire and dire',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsire\b/gi,
    message: 'Check: "sire" ("sire") vs "dire" ("dire"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dire';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sired_sires_3681',
    name: 'sired vs sires',
    description: 'Commonly confused words: sired and sires',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsired\b/gi,
    message: 'Check: "sired" ("sired") vs "sires" ("sires"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sires';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_siring_airing_3682',
    name: 'siring vs airing',
    description: 'Commonly confused words: siring and airing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsiring\b/gi,
    message: 'Check: "siring" ("siring") vs "airing" ("airing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'airing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sited_sired_3683',
    name: 'sited vs sired',
    description: 'Commonly confused words: sited and sired',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsited\b/gi,
    message: 'Check: "sited" ("sited") vs "sired" ("sired"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sired';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sixth_sixty_3684',
    name: 'sixth vs sixty',
    description: 'Commonly confused words: sixth and sixty',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsixth\b/gi,
    message: 'Check: "sixth" ("sixth") vs "sixty" ("sixty"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sixty';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sized_sizes_3685',
    name: 'sized vs sizes',
    description: 'Commonly confused words: sized and sizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsized\b/gi,
    message: 'Check: "sized" ("sized") vs "sizes" ("sizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skated_slated_3686',
    name: 'skated vs slated',
    description: 'Commonly confused words: skated and slated',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskated\b/gi,
    message: 'Check: "skated" ("skated") vs "slated" ("slated"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slated';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skates_slates_3687',
    name: 'skates vs slates',
    description: 'Commonly confused words: skates and slates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskates\b/gi,
    message: 'Check: "skates" ("skates") vs "slates" ("slates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skating_slating_3688',
    name: 'skating vs slating',
    description: 'Commonly confused words: skating and slating',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskating\b/gi,
    message: 'Check: "skating" ("skating") vs "slating" ("slating"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slating';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sketched_sketches_3689',
    name: 'sketched vs sketches',
    description: 'Commonly confused words: sketched and sketches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsketched\b/gi,
    message: 'Check: "sketched" ("sketched") vs "sketches" ("sketches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sketches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skew_slew_3690',
    name: 'skew vs slew',
    description: 'Commonly confused words: skew and slew',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskew\b/gi,
    message: 'Check: "skew" ("skew") vs "slew" ("slew"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slew';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skid_slid_3691',
    name: 'skid vs slid',
    description: 'Commonly confused words: skid and slid',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskid\b/gi,
    message: 'Check: "skid" ("skid") vs "slid" ("slid"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slid';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skier_slier_3692',
    name: 'skier vs slier',
    description: 'Commonly confused words: skier and slier',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskier\b/gi,
    message: 'Check: "skier" ("skier") vs "slier" ("slier"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slier';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skill_skull_3693',
    name: 'skill vs skull',
    description: 'Commonly confused words: skill and skull',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskill\b/gi,
    message: 'Check: "skill" ("skill") vs "skull" ("skull"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'skull';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skimmed_slimmed_3694',
    name: 'skimmed vs slimmed',
    description: 'Commonly confused words: skimmed and slimmed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskimmed\b/gi,
    message: 'Check: "skimmed" ("skimmed") vs "slimmed" ("slimmed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slimmed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skimmer_slimmer_3695',
    name: 'skimmer vs slimmer',
    description: 'Commonly confused words: skimmer and slimmer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskimmer\b/gi,
    message: 'Check: "skimmer" ("skimmer") vs "slimmer" ("slimmer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slimmer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skimmers_slimmers_3696',
    name: 'skimmers vs slimmers',
    description: 'Commonly confused words: skimmers and slimmers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskimmers\b/gi,
    message: 'Check: "skimmers" ("skimmers") vs "slimmers" ("slimmers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slimmers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skimming_slimming_3697',
    name: 'skimming vs slimming',
    description: 'Commonly confused words: skimming and slimming',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskimming\b/gi,
    message: 'Check: "skimming" ("skimming") vs "slimming" ("slimming"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slimming';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skims_skins_3698',
    name: 'skims vs skins',
    description: 'Commonly confused words: skims and skins',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskims\b/gi,
    message: 'Check: "skims" ("skims") vs "skins" ("skins"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'skins';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skipped_slipped_3699',
    name: 'skipped vs slipped',
    description: 'Commonly confused words: skipped and slipped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskipped\b/gi,
    message: 'Check: "skipped" ("skipped") vs "slipped" ("slipped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slipped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skippers_slippers_3700',
    name: 'skippers vs slippers',
    description: 'Commonly confused words: skippers and slippers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskippers\b/gi,
    message: 'Check: "skippers" ("skippers") vs "slippers" ("slippers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slippers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skip_slip_3701',
    name: 'skip vs slip',
    description: 'Commonly confused words: skip and slip',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskip\b/gi,
    message: 'Check: "skip" ("skip") vs "slip" ("slip"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slip';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skirmished_skirmishes_3702',
    name: 'skirmished vs skirmishes',
    description: 'Commonly confused words: skirmished and skirmishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskirmished\b/gi,
    message: 'Check: "skirmished" ("skirmished") vs "skirmishes" ("skirmishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'skirmishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skis_skid_3703',
    name: 'skis vs skid',
    description: 'Commonly confused words: skis and skid',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskis\b/gi,
    message: 'Check: "skis" ("skis") vs "skid" ("skid"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'skid';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_skulks_skulls_3704',
    name: 'skulks vs skulls',
    description: 'Commonly confused words: skulks and skulls',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bskulks\b/gi,
    message: 'Check: "skulks" ("skulks") vs "skulls" ("skulls"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'skulls';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slab_slag_3705',
    name: 'slab vs slag',
    description: 'Commonly confused words: slab and slag',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslab\b/gi,
    message: 'Check: "slab" ("slab") vs "slag" ("slag"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slag';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slabs_slags_3706',
    name: 'slabs vs slags',
    description: 'Commonly confused words: slabs and slags',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslabs\b/gi,
    message: 'Check: "slabs" ("slabs") vs "slags" ("slags"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slags';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slaked_soaked_3707',
    name: 'slaked vs soaked',
    description: 'Commonly confused words: slaked and soaked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslaked\b/gi,
    message: 'Check: "slaked" ("slaked") vs "soaked" ("soaked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'soaked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slaking_soaking_3708',
    name: 'slaking vs soaking',
    description: 'Commonly confused words: slaking and soaking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslaking\b/gi,
    message: 'Check: "slaking" ("slaking") vs "soaking" ("soaking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'soaking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slang_slant_3709',
    name: 'slang vs slant',
    description: 'Commonly confused words: slang and slant',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslang\b/gi,
    message: 'Check: "slang" ("slang") vs "slant" ("slant"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slant';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slates_slated_3710',
    name: 'slates vs slated',
    description: 'Commonly confused words: slates and slated',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslates\b/gi,
    message: 'Check: "slates" ("slates") vs "slated" ("slated"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slated';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slays_slats_3711',
    name: 'slays vs slats',
    description: 'Commonly confused words: slays and slats',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslays\b/gi,
    message: 'Check: "slays" ("slays") vs "slats" ("slats"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slats';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sledged_sledges_3712',
    name: 'sledged vs sledges',
    description: 'Commonly confused words: sledged and sledges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsledged\b/gi,
    message: 'Check: "sledged" ("sledged") vs "sledges" ("sledges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sledges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sliced_slices_3713',
    name: 'sliced vs slices',
    description: 'Commonly confused words: sliced and slices',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsliced\b/gi,
    message: 'Check: "sliced" ("sliced") vs "slices" ("slices"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slices';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slicer_slider_3714',
    name: 'slicer vs slider',
    description: 'Commonly confused words: slicer and slider',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslicer\b/gi,
    message: 'Check: "slicer" ("slicer") vs "slider" ("slider"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slider';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slicer_sliver_3715',
    name: 'slicer vs sliver',
    description: 'Commonly confused words: slicer and sliver',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslicer\b/gi,
    message: 'Check: "slicer" ("slicer") vs "sliver" ("sliver"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sliver';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slices_slides_3716',
    name: 'slices vs slides',
    description: 'Commonly confused words: slices and slides',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslices\b/gi,
    message: 'Check: "slices" ("slices") vs "slides" ("slides"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slides';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slims_skims_3717',
    name: 'slims vs skims',
    description: 'Commonly confused words: slims and skims',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslims\b/gi,
    message: 'Check: "slims" ("slims") vs "skims" ("skims"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'skims';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slims_slums_3718',
    name: 'slims vs slums',
    description: 'Commonly confused words: slims and slums',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslims\b/gi,
    message: 'Check: "slims" ("slims") vs "slums" ("slums"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slums';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sling_slung_3719',
    name: 'sling vs slung',
    description: 'Commonly confused words: sling and slung',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsling\b/gi,
    message: 'Check: "sling" ("sling") vs "slung" ("slung"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slung';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slipping_slopping_3720',
    name: 'slipping vs slopping',
    description: 'Commonly confused words: slipping and slopping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslipping\b/gi,
    message: 'Check: "slipping" ("slipping") vs "slopping" ("slopping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slopping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slippy_sloppy_3721',
    name: 'slippy vs sloppy',
    description: 'Commonly confused words: slippy and sloppy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslippy\b/gi,
    message: 'Check: "slippy" ("slippy") vs "sloppy" ("sloppy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sloppy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slitting_slotting_3722',
    name: 'slitting vs slotting',
    description: 'Commonly confused words: slitting and slotting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslitting\b/gi,
    message: 'Check: "slitting" ("slitting") vs "slotting" ("slotting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slotting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sloped_slopes_3723',
    name: 'sloped vs slopes',
    description: 'Commonly confused words: sloped and slopes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsloped\b/gi,
    message: 'Check: "sloped" ("sloped") vs "slopes" ("slopes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slopes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slops_slips_3724',
    name: 'slops vs slips',
    description: 'Commonly confused words: slops and slips',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslops\b/gi,
    message: 'Check: "slops" ("slops") vs "slips" ("slips"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slips';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sloshed_sloshes_3725',
    name: 'sloshed vs sloshes',
    description: 'Commonly confused words: sloshed and sloshes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsloshed\b/gi,
    message: 'Check: "sloshed" ("sloshed") vs "sloshes" ("sloshes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sloshes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sloth_sooth_3726',
    name: 'sloth vs sooth',
    description: 'Commonly confused words: sloth and sooth',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsloth\b/gi,
    message: 'Check: "sloth" ("sloth") vs "sooth" ("sooth"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sooth';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slot_slit_3727',
    name: 'slot vs slit',
    description: 'Commonly confused words: slot and slit',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslot\b/gi,
    message: 'Check: "slot" ("slot") vs "slit" ("slit"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slit';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slots_slits_3728',
    name: 'slots vs slits',
    description: 'Commonly confused words: slots and slits',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslots\b/gi,
    message: 'Check: "slots" ("slots") vs "slits" ("slits"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slits';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slouched_slouches_3729',
    name: 'slouched vs slouches',
    description: 'Commonly confused words: slouched and slouches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslouched\b/gi,
    message: 'Check: "slouched" ("slouched") vs "slouches" ("slouches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slouches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slows_sloes_3730',
    name: 'slows vs sloes',
    description: 'Commonly confused words: slows and sloes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslows\b/gi,
    message: 'Check: "slows" ("slows") vs "sloes" ("sloes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sloes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sluiced_sluices_3731',
    name: 'sluiced vs sluices',
    description: 'Commonly confused words: sluiced and sluices',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsluiced\b/gi,
    message: 'Check: "sluiced" ("sluiced") vs "sluices" ("sluices"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sluices';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slum_slim_3732',
    name: 'slum vs slim',
    description: 'Commonly confused words: slum and slim',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslum\b/gi,
    message: 'Check: "slum" ("slum") vs "slim" ("slim"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slim';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slur_slue_3733',
    name: 'slur vs slue',
    description: 'Commonly confused words: slur and slue',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslur\b/gi,
    message: 'Check: "slur" ("slur") vs "slue" ("slue"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slue';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slur_slut_3734',
    name: 'slur vs slut',
    description: 'Commonly confused words: slur and slut',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslur\b/gi,
    message: 'Check: "slur" ("slur") vs "slut" ("slut"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slut';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slur_sour_3735',
    name: 'slur vs sour',
    description: 'Commonly confused words: slur and sour',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslur\b/gi,
    message: 'Check: "slur" ("slur") vs "sour" ("sour"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sour';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slurs_sluts_3736',
    name: 'slurs vs sluts',
    description: 'Commonly confused words: slurs and sluts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslurs\b/gi,
    message: 'Check: "slurs" ("slurs") vs "sluts" ("sluts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sluts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_slurs_sours_3737',
    name: 'slurs vs sours',
    description: 'Commonly confused words: slurs and sours',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bslurs\b/gi,
    message: 'Check: "slurs" ("slurs") vs "sours" ("sours"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sours';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sly_soy_3738',
    name: 'sly vs soy',
    description: 'Commonly confused words: sly and soy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsly\b/gi,
    message: 'Check: "sly" ("sly") vs "soy" ("soy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'soy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_smacking_snacking_3739',
    name: 'smacking vs snacking',
    description: 'Commonly confused words: smacking and snacking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsmacking\b/gi,
    message: 'Check: "smacking" ("smacking") vs "snacking" ("snacking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snacking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_smack_snack_3740',
    name: 'smack vs snack',
    description: 'Commonly confused words: smack and snack',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsmack\b/gi,
    message: 'Check: "smack" ("smack") vs "snack" ("snack"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snack';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_smacks_snacks_3741',
    name: 'smacks vs snacks',
    description: 'Commonly confused words: smacks and snacks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsmacks\b/gi,
    message: 'Check: "smacks" ("smacks") vs "snacks" ("snacks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snacks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_smudged_smudges_3742',
    name: 'smudged vs smudges',
    description: 'Commonly confused words: smudged and smudges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsmudged\b/gi,
    message: 'Check: "smudged" ("smudged") vs "smudges" ("smudges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'smudges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_smuggled_snuggled_3743',
    name: 'smuggled vs snuggled',
    description: 'Commonly confused words: smuggled and snuggled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsmuggled\b/gi,
    message: 'Check: "smuggled" ("smuggled") vs "snuggled" ("snuggled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snuggled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_smuggle_snuggle_3744',
    name: 'smuggle vs snuggle',
    description: 'Commonly confused words: smuggle and snuggle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsmuggle\b/gi,
    message: 'Check: "smuggle" ("smuggle") vs "snuggle" ("snuggle"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snuggle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_smugly_snugly_3745',
    name: 'smugly vs snugly',
    description: 'Commonly confused words: smugly and snugly',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsmugly\b/gi,
    message: 'Check: "smugly" ("smugly") vs "snugly" ("snugly"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snugly';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_smug_smut_3746',
    name: 'smug vs smut',
    description: 'Commonly confused words: smug and smut',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsmug\b/gi,
    message: 'Check: "smug" ("smug") vs "smut" ("smut"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'smut';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_snacks_shacks_3747',
    name: 'snacks vs shacks',
    description: 'Commonly confused words: snacks and shacks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsnacks\b/gi,
    message: 'Check: "snacks" ("snacks") vs "shacks" ("shacks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shacks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_snared_snares_3748',
    name: 'snared vs snares',
    description: 'Commonly confused words: snared and snares',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsnared\b/gi,
    message: 'Check: "snared" ("snared") vs "snares" ("snares"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snares';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sneer_sheer_3749',
    name: 'sneer vs sheer',
    description: 'Commonly confused words: sneer and sheer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsneer\b/gi,
    message: 'Check: "sneer" ("sneer") vs "sheer" ("sheer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sheer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sniffed_snuffed_3750',
    name: 'sniffed vs snuffed',
    description: 'Commonly confused words: sniffed and snuffed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsniffed\b/gi,
    message: 'Check: "sniffed" ("sniffed") vs "snuffed" ("snuffed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snuffed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sniffing_snuffing_3751',
    name: 'sniffing vs snuffing',
    description: 'Commonly confused words: sniffing and snuffing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsniffing\b/gi,
    message: 'Check: "sniffing" ("sniffing") vs "snuffing" ("snuffing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snuffing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sniff_snuff_3752',
    name: 'sniff vs snuff',
    description: 'Commonly confused words: sniff and snuff',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsniff\b/gi,
    message: 'Check: "sniff" ("sniff") vs "snuff" ("snuff"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'snuff';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_snoring_shoring_3753',
    name: 'snoring vs shoring',
    description: 'Commonly confused words: snoring and shoring',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsnoring\b/gi,
    message: 'Check: "snoring" ("snoring") vs "shoring" ("shoring"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shoring';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_soared_spared_3754',
    name: 'soared vs spared',
    description: 'Commonly confused words: soared and spared',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsoared\b/gi,
    message: 'Check: "soared" ("soared") vs "spared" ("spared"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spared';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sobs_dobs_3755',
    name: 'sobs vs dobs',
    description: 'Commonly confused words: sobs and dobs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsobs\b/gi,
    message: 'Check: "sobs" ("sobs") vs "dobs" ("dobs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dobs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_socialized_socializes_3756',
    name: 'socialized vs socializes',
    description: 'Commonly confused words: socialized and socializes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsocialized\b/gi,
    message: 'Check: "socialized" ("socialized") vs "socializes" ("socializes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'socializes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sodas_sofas_3757',
    name: 'sodas vs sofas',
    description: 'Commonly confused words: sodas and sofas',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsodas\b/gi,
    message: 'Check: "sodas" ("sodas") vs "sofas" ("sofas"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sofas';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_softer_sifter_3758',
    name: 'softer vs sifter',
    description: 'Commonly confused words: softer and sifter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsofter\b/gi,
    message: 'Check: "softer" ("softer") vs "sifter" ("sifter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sifter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_softer_sorter_3759',
    name: 'softer vs sorter',
    description: 'Commonly confused words: softer and sorter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsofter\b/gi,
    message: 'Check: "softer" ("softer") vs "sorter" ("sorter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sorter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_softies_sorties_3760',
    name: 'softies vs sorties',
    description: 'Commonly confused words: softies and sorties',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsofties\b/gi,
    message: 'Check: "softies" ("softies") vs "sorties" ("sorties"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sorties';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_soft_sort_3761',
    name: 'soft vs sort',
    description: 'Commonly confused words: soft and sort',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsoft\b/gi,
    message: 'Check: "soft" ("soft") vs "sort" ("sort"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sort';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_soil_soul_3762',
    name: 'soil vs soul',
    description: 'Commonly confused words: soil and soul',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsoil\b/gi,
    message: 'Check: "soil" ("soil") vs "soul" ("soul"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'soul';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_soils_souls_3763',
    name: 'soils vs souls',
    description: 'Commonly confused words: soils and souls',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsoils\b/gi,
    message: 'Check: "soils" ("soils") vs "souls" ("souls"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'souls';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sold_sole_3764',
    name: 'sold vs sole',
    description: 'Commonly confused words: sold and sole',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsold\b/gi,
    message: 'Check: "sold" ("sold") vs "sole" ("sole"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sole';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_soled_doled_3765',
    name: 'soled vs doled',
    description: 'Commonly confused words: soled and doled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsoled\b/gi,
    message: 'Check: "soled" ("soled") vs "doled" ("doled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'doled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_soled_soles_3766',
    name: 'soled vs soles',
    description: 'Commonly confused words: soled and soles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsoled\b/gi,
    message: 'Check: "soled" ("soled") vs "soles" ("soles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'soles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_soles_doles_3767',
    name: 'soles vs doles',
    description: 'Commonly confused words: soles and doles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsoles\b/gi,
    message: 'Check: "soles" ("soles") vs "doles" ("doles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'doles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_soling_doling_3768',
    name: 'soling vs doling',
    description: 'Commonly confused words: soling and doling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsoling\b/gi,
    message: 'Check: "soling" ("soling") vs "doling" ("doling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'doling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_solves_wolves_3769',
    name: 'solves vs wolves',
    description: 'Commonly confused words: solves and wolves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsolves\b/gi,
    message: 'Check: "solves" ("solves") vs "wolves" ("wolves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wolves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sophisticated_sophisticates_3770',
    name: 'sophisticated vs sophisticates',
    description: 'Commonly confused words: sophisticated and sophisticates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsophisticated\b/gi,
    message: 'Check: "sophisticated" ("sophisticated") vs "sophisticates" ("sophisticates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sophisticates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sorry_worry_3771',
    name: 'sorry vs worry',
    description: 'Commonly confused words: sorry and worry',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsorry\b/gi,
    message: 'Check: "sorry" ("sorry") vs "worry" ("worry"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'worry';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sorts_sorta_3772',
    name: 'sorts vs sorta',
    description: 'Commonly confused words: sorts and sorta',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsorts\b/gi,
    message: 'Check: "sorts" ("sorts") vs "sorta" ("sorta"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sorta';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sots_dots_3773',
    name: 'sots vs dots',
    description: 'Commonly confused words: sots and dots',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsots\b/gi,
    message: 'Check: "sots" ("sots") vs "dots" ("dots"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dots';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sounder_wounder_3774',
    name: 'sounder vs wounder',
    description: 'Commonly confused words: sounder and wounder',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsounder\b/gi,
    message: 'Check: "sounder" ("sounder") vs "wounder" ("wounder"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wounder';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sounds_wounds_3775',
    name: 'sounds vs wounds',
    description: 'Commonly confused words: sounds and wounds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsounds\b/gi,
    message: 'Check: "sounds" ("sounds") vs "wounds" ("wounds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wounds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sound_wound_3776',
    name: 'sound vs wound',
    description: 'Commonly confused words: sound and wound',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsound\b/gi,
    message: 'Check: "sound" ("sound") vs "wound" ("wound"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wound';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sower_dower_3777',
    name: 'sower vs dower',
    description: 'Commonly confused words: sower and dower',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsower\b/gi,
    message: 'Check: "sower" ("sower") vs "dower" ("dower"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dower';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spaced_spaces_3778',
    name: 'spaced vs spaces',
    description: 'Commonly confused words: spaced and spaces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspaced\b/gi,
    message: 'Check: "spaced" ("spaced") vs "spaces" ("spaces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spaces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spangled_spangles_3779',
    name: 'spangled vs spangles',
    description: 'Commonly confused words: spangled and spangles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspangled\b/gi,
    message: 'Check: "spangled" ("spangled") vs "spangles" ("spangles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spangles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spared_spares_3780',
    name: 'spared vs spares',
    description: 'Commonly confused words: spared and spares',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspared\b/gi,
    message: 'Check: "spared" ("spared") vs "spares" ("spares"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spares';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sparing_soaring_3781',
    name: 'sparing vs soaring',
    description: 'Commonly confused words: sparing and soaring',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsparing\b/gi,
    message: 'Check: "sparing" ("sparing") vs "soaring" ("soaring"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'soaring';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spar_soar_3782',
    name: 'spar vs soar',
    description: 'Commonly confused words: spar and soar',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspar\b/gi,
    message: 'Check: "spar" ("spar") vs "soar" ("soar"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'soar';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spars_soars_3783',
    name: 'spars vs soars',
    description: 'Commonly confused words: spars and soars',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspars\b/gi,
    message: 'Check: "spars" ("spars") vs "soars" ("soars"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'soars';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spate_spare_3784',
    name: 'spate vs spare',
    description: 'Commonly confused words: spate and spare',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspate\b/gi,
    message: 'Check: "spate" ("spate") vs "spare" ("spare"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spare';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spates_spares_3785',
    name: 'spates vs spares',
    description: 'Commonly confused words: spates and spares',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspates\b/gi,
    message: 'Check: "spates" ("spates") vs "spares" ("spares"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spares';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spay_spat_3786',
    name: 'spay vs spat',
    description: 'Commonly confused words: spay and spat',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspay\b/gi,
    message: 'Check: "spay" ("spay") vs "spat" ("spat"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spat';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_specialized_specializes_3787',
    name: 'specialized vs specializes',
    description: 'Commonly confused words: specialized and specializes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspecialized\b/gi,
    message: 'Check: "specialized" ("specialized") vs "specializes" ("specializes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'specializes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spec_sped_3788',
    name: 'spec vs sped',
    description: 'Commonly confused words: spec and sped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspec\b/gi,
    message: 'Check: "spec" ("spec") vs "sped" ("sped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sped_aped_3789',
    name: 'sped vs aped',
    description: 'Commonly confused words: sped and aped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsped\b/gi,
    message: 'Check: "sped" ("sped") vs "aped" ("aped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'aped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spiced_spices_3790',
    name: 'spiced vs spices',
    description: 'Commonly confused words: spiced and spices',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspiced\b/gi,
    message: 'Check: "spiced" ("spiced") vs "spices" ("spices"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spices';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spied_spies_3791',
    name: 'spied vs spies',
    description: 'Commonly confused words: spied and spies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspied\b/gi,
    message: 'Check: "spied" ("spied") vs "spies" ("spies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spiked_spikes_3792',
    name: 'spiked vs spikes',
    description: 'Commonly confused words: spiked and spikes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspiked\b/gi,
    message: 'Check: "spiked" ("spiked") vs "spikes" ("spikes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spikes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spike_spoke_3793',
    name: 'spike vs spoke',
    description: 'Commonly confused words: spike and spoke',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspike\b/gi,
    message: 'Check: "spike" ("spike") vs "spoke" ("spoke"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spoke';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spindled_spindles_3794',
    name: 'spindled vs spindles',
    description: 'Commonly confused words: spindled and spindles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspindled\b/gi,
    message: 'Check: "spindled" ("spindled") vs "spindles" ("spindles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spindles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spite_spire_3795',
    name: 'spite vs spire',
    description: 'Commonly confused words: spite and spire',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspite\b/gi,
    message: 'Check: "spite" ("spite") vs "spire" ("spire"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spire';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spitting_spotting_3796',
    name: 'spitting vs spotting',
    description: 'Commonly confused words: spitting and spotting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspitting\b/gi,
    message: 'Check: "spitting" ("spitting") vs "spotting" ("spotting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spotting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_splat_splay_3797',
    name: 'splat vs splay',
    description: 'Commonly confused words: splat and splay',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsplat\b/gi,
    message: 'Check: "splat" ("splat") vs "splay" ("splay"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'splay';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_splats_splays_3798',
    name: 'splats vs splays',
    description: 'Commonly confused words: splats and splays',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsplats\b/gi,
    message: 'Check: "splats" ("splats") vs "splays" ("splays"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'splays';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_splotched_splotches_3799',
    name: 'splotched vs splotches',
    description: 'Commonly confused words: splotched and splotches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsplotched\b/gi,
    message: 'Check: "splotched" ("splotched") vs "splotches" ("splotches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'splotches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spoiling_spooling_3800',
    name: 'spoiling vs spooling',
    description: 'Commonly confused words: spoiling and spooling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspoiling\b/gi,
    message: 'Check: "spoiling" ("spoiling") vs "spooling" ("spooling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spooling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spoil_spool_3801',
    name: 'spoil vs spool',
    description: 'Commonly confused words: spoil and spool',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspoil\b/gi,
    message: 'Check: "spoil" ("spoil") vs "spool" ("spool"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spool';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sponged_sponges_3802',
    name: 'sponged vs sponges',
    description: 'Commonly confused words: sponged and sponges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsponged\b/gi,
    message: 'Check: "sponged" ("sponged") vs "sponges" ("sponges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sponges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spooked_spooled_3803',
    name: 'spooked vs spooled',
    description: 'Commonly confused words: spooked and spooled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspooked\b/gi,
    message: 'Check: "spooked" ("spooked") vs "spooled" ("spooled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spooled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spored_spores_3804',
    name: 'spored vs spores',
    description: 'Commonly confused words: spored and spores',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspored\b/gi,
    message: 'Check: "spored" ("spored") vs "spores" ("spores"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spores';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spore_spire_3805',
    name: 'spore vs spire',
    description: 'Commonly confused words: spore and spire',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspore\b/gi,
    message: 'Check: "spore" ("spore") vs "spire" ("spire"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spire';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sprats_sprays_3806',
    name: 'sprats vs sprays',
    description: 'Commonly confused words: sprats and sprays',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsprats\b/gi,
    message: 'Check: "sprats" ("sprats") vs "sprays" ("sprays"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sprays';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spreed_sprees_3807',
    name: 'spreed vs sprees',
    description: 'Commonly confused words: spreed and sprees',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspreed\b/gi,
    message: 'Check: "spreed" ("spreed") vs "sprees" ("sprees"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sprees';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_springing_sprinting_3808',
    name: 'springing vs sprinting',
    description: 'Commonly confused words: springing and sprinting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspringing\b/gi,
    message: 'Check: "springing" ("springing") vs "sprinting" ("sprinting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sprinting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spruced_spruces_3809',
    name: 'spruced vs spruces',
    description: 'Commonly confused words: spruced and spruces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspruced\b/gi,
    message: 'Check: "spruced" ("spruced") vs "spruces" ("spruces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spruces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spurred_spurted_3810',
    name: 'spurred vs spurted',
    description: 'Commonly confused words: spurred and spurted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspurred\b/gi,
    message: 'Check: "spurred" ("spurred") vs "spurted" ("spurted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spurted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spurring_spurting_3811',
    name: 'spurring vs spurting',
    description: 'Commonly confused words: spurring and spurting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspurring\b/gi,
    message: 'Check: "spurring" ("spurring") vs "spurting" ("spurting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'spurting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spur_sour_3812',
    name: 'spur vs sour',
    description: 'Commonly confused words: spur and sour',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspur\b/gi,
    message: 'Check: "spur" ("spur") vs "sour" ("sour"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sour';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_spy_soy_3813',
    name: 'spy vs soy',
    description: 'Commonly confused words: spy and soy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bspy\b/gi,
    message: 'Check: "spy" ("spy") vs "soy" ("soy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'soy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_squabbled_squabbles_3814',
    name: 'squabbled vs squabbles',
    description: 'Commonly confused words: squabbled and squabbles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsquabbled\b/gi,
    message: 'Check: "squabbled" ("squabbled") vs "squabbles" ("squabbles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'squabbles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_squashed_squashes_3815',
    name: 'squashed vs squashes',
    description: 'Commonly confused words: squashed and squashes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsquashed\b/gi,
    message: 'Check: "squashed" ("squashed") vs "squashes" ("squashes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'squashes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_squeaked_squealed_3816',
    name: 'squeaked vs squealed',
    description: 'Commonly confused words: squeaked and squealed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsqueaked\b/gi,
    message: 'Check: "squeaked" ("squeaked") vs "squealed" ("squealed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'squealed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_squired_squires_3817',
    name: 'squired vs squires',
    description: 'Commonly confused words: squired and squires',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsquired\b/gi,
    message: 'Check: "squired" ("squired") vs "squires" ("squires"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'squires';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_squished_squishes_3818',
    name: 'squished vs squishes',
    description: 'Commonly confused words: squished and squishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsquished\b/gi,
    message: 'Check: "squished" ("squished") vs "squishes" ("squishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'squishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stabilized_stabilizes_3819',
    name: 'stabilized vs stabilizes',
    description: 'Commonly confused words: stabilized and stabilizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstabilized\b/gi,
    message: 'Check: "stabilized" ("stabilized") vs "stabilizes" ("stabilizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stabilizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stabled_stables_3820',
    name: 'stabled vs stables',
    description: 'Commonly confused words: stabled and stables',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstabled\b/gi,
    message: 'Check: "stabled" ("stabled") vs "stables" ("stables"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stables';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stabs_stags_3821',
    name: 'stabs vs stags',
    description: 'Commonly confused words: stabs and stags',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstabs\b/gi,
    message: 'Check: "stabs" ("stabs") vs "stags" ("stags"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stags';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stab_stag_3822',
    name: 'stab vs stag',
    description: 'Commonly confused words: stab and stag',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstab\b/gi,
    message: 'Check: "stab" ("stab") vs "stag" ("stag"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stag';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_staged_stages_3823',
    name: 'staged vs stages',
    description: 'Commonly confused words: staged and stages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstaged\b/gi,
    message: 'Check: "staged" ("staged") vs "stages" ("stages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_staging_stating_3824',
    name: 'staging vs stating',
    description: 'Commonly confused words: staging and stating',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstaging\b/gi,
    message: 'Check: "staging" ("staging") vs "stating" ("stating"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stating';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stagnated_stagnates_3825',
    name: 'stagnated vs stagnates',
    description: 'Commonly confused words: stagnated and stagnates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstagnated\b/gi,
    message: 'Check: "stagnated" ("stagnated") vs "stagnates" ("stagnates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stagnates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_staked_stakes_3826',
    name: 'staked vs stakes',
    description: 'Commonly confused words: staked and stakes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstaked\b/gi,
    message: 'Check: "staked" ("staked") vs "stakes" ("stakes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stakes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stake_stale_3827',
    name: 'stake vs stale',
    description: 'Commonly confused words: stake and stale',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstake\b/gi,
    message: 'Check: "stake" ("stake") vs "stale" ("stale"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stale';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_staking_staling_3828',
    name: 'staking vs staling',
    description: 'Commonly confused words: staking and staling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstaking\b/gi,
    message: 'Check: "staking" ("staking") vs "staling" ("staling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'staling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stalemated_stalemates_3829',
    name: 'stalemated vs stalemates',
    description: 'Commonly confused words: stalemated and stalemates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstalemated\b/gi,
    message: 'Check: "stalemated" ("stalemated") vs "stalemates" ("stalemates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stalemates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stalks_stalls_3830',
    name: 'stalks vs stalls',
    description: 'Commonly confused words: stalks and stalls',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstalks\b/gi,
    message: 'Check: "stalks" ("stalks") vs "stalls" ("stalls"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stalls';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stalk_stall_3831',
    name: 'stalk vs stall',
    description: 'Commonly confused words: stalk and stall',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstalk\b/gi,
    message: 'Check: "stalk" ("stalk") vs "stall" ("stall"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stall';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stapled_staples_3832',
    name: 'stapled vs staples',
    description: 'Commonly confused words: stapled and staples',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstapled\b/gi,
    message: 'Check: "stapled" ("stapled") vs "staples" ("staples"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'staples';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_starched_starches_3833',
    name: 'starched vs starches',
    description: 'Commonly confused words: starched and starches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstarched\b/gi,
    message: 'Check: "starched" ("starched") vs "starches" ("starches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'starches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_starved_starves_3834',
    name: 'starved vs starves',
    description: 'Commonly confused words: starved and starves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstarved\b/gi,
    message: 'Check: "starved" ("starved") vs "starves" ("starves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'starves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stature_statute_3835',
    name: 'stature vs statute',
    description: 'Commonly confused words: stature and statute',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstature\b/gi,
    message: 'Check: "stature" ("stature") vs "statute" ("statute"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'statute';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stayed_stated_3836',
    name: 'stayed vs stated',
    description: 'Commonly confused words: stayed and stated',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstayed\b/gi,
    message: 'Check: "stayed" ("stayed") vs "stated" ("stated"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stated';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stayer_stater_3837',
    name: 'stayer vs stater',
    description: 'Commonly confused words: stayer and stater',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstayer\b/gi,
    message: 'Check: "stayer" ("stayer") vs "stater" ("stater"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stater';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_staying_stating_3838',
    name: 'staying vs stating',
    description: 'Commonly confused words: staying and stating',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstaying\b/gi,
    message: 'Check: "staying" ("staying") vs "stating" ("stating"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stating';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stays_stats_3839',
    name: 'stays vs stats',
    description: 'Commonly confused words: stays and stats',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstays\b/gi,
    message: 'Check: "stays" ("stays") vs "stats" ("stats"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stats';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_steak_steal_3840',
    name: 'steak vs steal',
    description: 'Commonly confused words: steak and steal',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsteak\b/gi,
    message: 'Check: "steak" ("steak") vs "steal" (to take without permission). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'steal';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_steep_strep_3841',
    name: 'steep vs strep',
    description: 'Commonly confused words: steep and strep',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsteep\b/gi,
    message: 'Check: "steep" ("steep") vs "strep" ("strep"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'strep';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stepped_steppes_3842',
    name: 'stepped vs steppes',
    description: 'Commonly confused words: stepped and steppes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstepped\b/gi,
    message: 'Check: "stepped" ("stepped") vs "steppes" ("steppes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'steppes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stereotyped_stereotypes_3843',
    name: 'stereotyped vs stereotypes',
    description: 'Commonly confused words: stereotyped and stereotypes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstereotyped\b/gi,
    message: 'Check: "stereotyped" ("stereotyped") vs "stereotypes" ("stereotypes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stereotypes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sterilized_sterilizes_3844',
    name: 'sterilized vs sterilizes',
    description: 'Commonly confused words: sterilized and sterilizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsterilized\b/gi,
    message: 'Check: "sterilized" ("sterilized") vs "sterilizes" ("sterilizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sterilizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sticking_stocking_3845',
    name: 'sticking vs stocking',
    description: 'Commonly confused words: sticking and stocking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsticking\b/gi,
    message: 'Check: "sticking" ("sticking") vs "stocking" ("stocking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stocking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stick_stuck_3846',
    name: 'stick vs stuck',
    description: 'Commonly confused words: stick and stuck',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstick\b/gi,
    message: 'Check: "stick" ("stick") vs "stuck" ("stuck"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stuck';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stiffed_stuffed_3847',
    name: 'stiffed vs stuffed',
    description: 'Commonly confused words: stiffed and stuffed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstiffed\b/gi,
    message: 'Check: "stiffed" ("stiffed") vs "stuffed" ("stuffed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stuffed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stiffs_stuffs_3848',
    name: 'stiffs vs stuffs',
    description: 'Commonly confused words: stiffs and stuffs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstiffs\b/gi,
    message: 'Check: "stiffs" ("stiffs") vs "stuffs" ("stuffs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stuffs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stiff_stuff_3849',
    name: 'stiff vs stuff',
    description: 'Commonly confused words: stiff and stuff',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstiff\b/gi,
    message: 'Check: "stiff" ("stiff") vs "stuff" ("stuff"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stuff';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stifled_stifles_3850',
    name: 'stifled vs stifles',
    description: 'Commonly confused words: stifled and stifles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstifled\b/gi,
    message: 'Check: "stifled" ("stifled") vs "stifles" ("stifles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stifles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stigmatized_stigmatizes_3851',
    name: 'stigmatized vs stigmatizes',
    description: 'Commonly confused words: stigmatized and stigmatizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstigmatized\b/gi,
    message: 'Check: "stigmatized" ("stigmatized") vs "stigmatizes" ("stigmatizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stigmatizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stile_stole_3852',
    name: 'stile vs stole',
    description: 'Commonly confused words: stile and stole',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstile\b/gi,
    message: 'Check: "stile" ("stile") vs "stole" ("stole"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stole';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stimulated_stimulates_3853',
    name: 'stimulated vs stimulates',
    description: 'Commonly confused words: stimulated and stimulates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstimulated\b/gi,
    message: 'Check: "stimulated" ("stimulated") vs "stimulates" ("stimulates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stimulates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stings_stints_3854',
    name: 'stings vs stints',
    description: 'Commonly confused words: stings and stints',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstings\b/gi,
    message: 'Check: "stings" ("stings") vs "stints" ("stints"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stints';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stink_stunk_3855',
    name: 'stink vs stunk',
    description: 'Commonly confused words: stink and stunk',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstink\b/gi,
    message: 'Check: "stink" ("stink") vs "stunk" ("stunk"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stunk';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stinted_stunted_3856',
    name: 'stinted vs stunted',
    description: 'Commonly confused words: stinted and stunted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstinted\b/gi,
    message: 'Check: "stinted" ("stinted") vs "stunted" ("stunted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stunted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stints_stunts_3857',
    name: 'stints vs stunts',
    description: 'Commonly confused words: stints and stunts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstints\b/gi,
    message: 'Check: "stints" ("stints") vs "stunts" ("stunts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stunts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stint_stunt_3858',
    name: 'stint vs stunt',
    description: 'Commonly confused words: stint and stunt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstint\b/gi,
    message: 'Check: "stint" ("stint") vs "stunt" ("stunt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stunt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stipulated_stipulates_3859',
    name: 'stipulated vs stipulates',
    description: 'Commonly confused words: stipulated and stipulates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstipulated\b/gi,
    message: 'Check: "stipulated" ("stipulated") vs "stipulates" ("stipulates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stipulates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stirs_sties_3860',
    name: 'stirs vs sties',
    description: 'Commonly confused words: stirs and sties',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstirs\b/gi,
    message: 'Check: "stirs" ("stirs") vs "sties" ("sties"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sties';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stitched_stitches_3861',
    name: 'stitched vs stitches',
    description: 'Commonly confused words: stitched and stitches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstitched\b/gi,
    message: 'Check: "stitched" ("stitched") vs "stitches" ("stitches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stitches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stockaded_stockades_3862',
    name: 'stockaded vs stockades',
    description: 'Commonly confused words: stockaded and stockades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstockaded\b/gi,
    message: 'Check: "stockaded" ("stockaded") vs "stockades" ("stockades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stockades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stockpiled_stockpiles_3863',
    name: 'stockpiled vs stockpiles',
    description: 'Commonly confused words: stockpiled and stockpiles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstockpiled\b/gi,
    message: 'Check: "stockpiled" ("stockpiled") vs "stockpiles" ("stockpiles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stockpiles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stole_stoke_3864',
    name: 'stole vs stoke',
    description: 'Commonly confused words: stole and stoke',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstole\b/gi,
    message: 'Check: "stole" ("stole") vs "stoke" ("stoke"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stoke';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stooping_stopping_3865',
    name: 'stooping vs stopping',
    description: 'Commonly confused words: stooping and stopping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstooping\b/gi,
    message: 'Check: "stooping" ("stooping") vs "stopping" ("stopping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stopping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stored_stores_3866',
    name: 'stored vs stores',
    description: 'Commonly confused words: stored and stores',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstored\b/gi,
    message: 'Check: "stored" ("stored") vs "stores" ("stores"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stores';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_strafed_strafes_3867',
    name: 'strafed vs strafes',
    description: 'Commonly confused words: strafed and strafes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstrafed\b/gi,
    message: 'Check: "strafed" ("strafed") vs "strafes" ("strafes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'strafes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_strangled_strangles_3868',
    name: 'strangled vs strangles',
    description: 'Commonly confused words: strangled and strangles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstrangled\b/gi,
    message: 'Check: "strangled" ("strangled") vs "strangles" ("strangles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'strangles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stratified_stratifies_3869',
    name: 'stratified vs stratifies',
    description: 'Commonly confused words: stratified and stratifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstratified\b/gi,
    message: 'Check: "stratified" ("stratified") vs "stratifies" ("stratifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stratifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stressed_stresses_3870',
    name: 'stressed vs stresses',
    description: 'Commonly confused words: stressed and stresses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstressed\b/gi,
    message: 'Check: "stressed" ("stressed") vs "stresses" ("stresses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stresses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stretched_stretches_3871',
    name: 'stretched vs stretches',
    description: 'Commonly confused words: stretched and stretches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstretched\b/gi,
    message: 'Check: "stretched" ("stretched") vs "stretches" ("stretches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stretches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stride_strife_3872',
    name: 'stride vs strife',
    description: 'Commonly confused words: stride and strife',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstride\b/gi,
    message: 'Check: "stride" ("stride") vs "strife" ("strife"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'strife';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stride_strode_3873',
    name: 'stride vs strode',
    description: 'Commonly confused words: stride and strode',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstride\b/gi,
    message: 'Check: "stride" ("stride") vs "strode" ("strode"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'strode';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_strife_strive_3874',
    name: 'strife vs strive',
    description: 'Commonly confused words: strife and strive',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstrife\b/gi,
    message: 'Check: "strife" ("strife") vs "strive" ("strive"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'strive';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stringer_stronger_3875',
    name: 'stringer vs stronger',
    description: 'Commonly confused words: stringer and stronger',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstringer\b/gi,
    message: 'Check: "stringer" ("stringer") vs "stronger" ("stronger"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stronger';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_striped_stripes_3876',
    name: 'striped vs stripes',
    description: 'Commonly confused words: striped and stripes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstriped\b/gi,
    message: 'Check: "striped" ("striped") vs "stripes" ("stripes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stripes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_strips_strops_3877',
    name: 'strips vs strops',
    description: 'Commonly confused words: strips and strops',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstrips\b/gi,
    message: 'Check: "strips" ("strips") vs "strops" ("strops"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'strops';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_strive_strove_3878',
    name: 'strive vs strove',
    description: 'Commonly confused words: strive and strove',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstrive\b/gi,
    message: 'Check: "strive" ("strive") vs "strove" ("strove"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'strove';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_strobe_strove_3879',
    name: 'strobe vs strove',
    description: 'Commonly confused words: strobe and strove',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstrobe\b/gi,
    message: 'Check: "strobe" ("strobe") vs "strove" ("strove"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'strove';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_structured_structures_3880',
    name: 'structured vs structures',
    description: 'Commonly confused words: structured and structures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstructured\b/gi,
    message: 'Check: "structured" ("structured") vs "structures" ("structures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'structures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stub_stun_3881',
    name: 'stub vs stun',
    description: 'Commonly confused words: stub and stun',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstub\b/gi,
    message: 'Check: "stub" ("stub") vs "stun" ("stun"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stun';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stuccoed_stuccoes_3882',
    name: 'stuccoed vs stuccoes',
    description: 'Commonly confused words: stuccoed and stuccoes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstuccoed\b/gi,
    message: 'Check: "stuccoed" ("stuccoed") vs "stuccoes" ("stuccoes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stuccoes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_studied_studies_3883',
    name: 'studied vs studies',
    description: 'Commonly confused words: studied and studies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstudied\b/gi,
    message: 'Check: "studied" ("studied") vs "studies" ("studies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'studies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stung_sting_3884',
    name: 'stung vs sting',
    description: 'Commonly confused words: stung and sting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstung\b/gi,
    message: 'Check: "stung" ("stung") vs "sting" ("sting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stung_stunt_3885',
    name: 'stung vs stunt',
    description: 'Commonly confused words: stung and stunt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstung\b/gi,
    message: 'Check: "stung" ("stung") vs "stunt" ("stunt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stunt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_stymied_stymies_3886',
    name: 'stymied vs stymies',
    description: 'Commonly confused words: stymied and stymies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bstymied\b/gi,
    message: 'Check: "stymied" ("stymied") vs "stymies" ("stymies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stymies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_subdivided_subdivides_3887',
    name: 'subdivided vs subdivides',
    description: 'Commonly confused words: subdivided and subdivides',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsubdivided\b/gi,
    message: 'Check: "subdivided" ("subdivided") vs "subdivides" ("subdivides"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'subdivides';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_subjugated_subjugates_3888',
    name: 'subjugated vs subjugates',
    description: 'Commonly confused words: subjugated and subjugates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsubjugated\b/gi,
    message: 'Check: "subjugated" ("subjugated") vs "subjugates" ("subjugates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'subjugates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sublimated_sublimates_3889',
    name: 'sublimated vs sublimates',
    description: 'Commonly confused words: sublimated and sublimates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsublimated\b/gi,
    message: 'Check: "sublimated" ("sublimated") vs "sublimates" ("sublimates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sublimates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sublimed_sublimes_3890',
    name: 'sublimed vs sublimes',
    description: 'Commonly confused words: sublimed and sublimes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsublimed\b/gi,
    message: 'Check: "sublimed" ("sublimed") vs "sublimes" ("sublimes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sublimes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_subordinated_subordinates_3891',
    name: 'subordinated vs subordinates',
    description: 'Commonly confused words: subordinated and subordinates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsubordinated\b/gi,
    message: 'Check: "subordinated" ("subordinated") vs "subordinates" ("subordinates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'subordinates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_subscribed_subscribes_3892',
    name: 'subscribed vs subscribes',
    description: 'Commonly confused words: subscribed and subscribes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsubscribed\b/gi,
    message: 'Check: "subscribed" ("subscribed") vs "subscribes" ("subscribes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'subscribes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_subsets_sunsets_3893',
    name: 'subsets vs sunsets',
    description: 'Commonly confused words: subsets and sunsets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsubsets\b/gi,
    message: 'Check: "subsets" ("subsets") vs "sunsets" ("sunsets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sunsets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_subset_sunset_3894',
    name: 'subset vs sunset',
    description: 'Commonly confused words: subset and sunset',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsubset\b/gi,
    message: 'Check: "subset" ("subset") vs "sunset" ("sunset"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sunset';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_subsided_subsides_3895',
    name: 'subsided vs subsides',
    description: 'Commonly confused words: subsided and subsides',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsubsided\b/gi,
    message: 'Check: "subsided" ("subsided") vs "subsides" ("subsides"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'subsides';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_substantiated_substantiates_3896',
    name: 'substantiated vs substantiates',
    description: 'Commonly confused words: substantiated and substantiates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsubstantiated\b/gi,
    message: 'Check: "substantiated" ("substantiated") vs "substantiates" ("substantiates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'substantiates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_subsumed_subsumes_3897',
    name: 'subsumed vs subsumes',
    description: 'Commonly confused words: subsumed and subsumes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsubsumed\b/gi,
    message: 'Check: "subsumed" ("subsumed") vs "subsumes" ("subsumes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'subsumes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_subtitled_subtitles_3898',
    name: 'subtitled vs subtitles',
    description: 'Commonly confused words: subtitled and subtitles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsubtitled\b/gi,
    message: 'Check: "subtitled" ("subtitled") vs "subtitles" ("subtitles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'subtitles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sucker_sicker_3899',
    name: 'sucker vs sicker',
    description: 'Commonly confused words: sucker and sicker',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsucker\b/gi,
    message: 'Check: "sucker" ("sucker") vs "sicker" ("sicker"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sicker';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_suckled_suckles_3900',
    name: 'suckled vs suckles',
    description: 'Commonly confused words: suckled and suckles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsuckled\b/gi,
    message: 'Check: "suckled" ("suckled") vs "suckles" ("suckles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suckles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_suffer_duffer_3901',
    name: 'suffer vs duffer',
    description: 'Commonly confused words: suffer and duffer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsuffer\b/gi,
    message: 'Check: "suffer" ("suffer") vs "duffer" ("duffer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'duffer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_suffers_surfers_3902',
    name: 'suffers vs surfers',
    description: 'Commonly confused words: suffers and surfers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsuffers\b/gi,
    message: 'Check: "suffers" ("suffers") vs "surfers" ("surfers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'surfers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sufficed_suffices_3903',
    name: 'sufficed vs suffices',
    description: 'Commonly confused words: sufficed and suffices',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsufficed\b/gi,
    message: 'Check: "sufficed" ("sufficed") vs "suffices" ("suffices"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suffices';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sufficed_suffixed_3904',
    name: 'sufficed vs suffixed',
    description: 'Commonly confused words: sufficed and suffixed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsufficed\b/gi,
    message: 'Check: "sufficed" ("sufficed") vs "suffixed" ("suffixed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suffixed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_suffices_suffixes_3905',
    name: 'suffices vs suffixes',
    description: 'Commonly confused words: suffices and suffixes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsuffices\b/gi,
    message: 'Check: "suffices" ("suffices") vs "suffixes" ("suffixes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suffixes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sufficing_suffixing_3906',
    name: 'sufficing vs suffixing',
    description: 'Commonly confused words: sufficing and suffixing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsufficing\b/gi,
    message: 'Check: "sufficing" ("sufficing") vs "suffixing" ("suffixing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suffixing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_suffused_suffuses_3907',
    name: 'suffused vs suffuses',
    description: 'Commonly confused words: suffused and suffuses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsuffused\b/gi,
    message: 'Check: "suffused" ("suffused") vs "suffuses" ("suffuses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suffuses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_suited_suites_3908',
    name: 'suited vs suites',
    description: 'Commonly confused words: suited and suites',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsuited\b/gi,
    message: 'Check: "suited" ("suited") vs "suites" ("suites"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suites';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sulkies_sullies_3909',
    name: 'sulkies vs sullies',
    description: 'Commonly confused words: sulkies and sullies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsulkies\b/gi,
    message: 'Check: "sulkies" ("sulkies") vs "sullies" ("sullies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sullies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sulks_silks_3910',
    name: 'sulks vs silks',
    description: 'Commonly confused words: sulks and silks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsulks\b/gi,
    message: 'Check: "sulks" ("sulks") vs "silks" ("silks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'silks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sullied_sullies_3911',
    name: 'sullied vs sullies',
    description: 'Commonly confused words: sullied and sullies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsullied\b/gi,
    message: 'Check: "sullied" ("sullied") vs "sullies" ("sullies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sullies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_summarized_summarizes_3912',
    name: 'summarized vs summarizes',
    description: 'Commonly confused words: summarized and summarizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsummarized\b/gi,
    message: 'Check: "summarized" ("summarized") vs "summarizes" ("summarizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'summarizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_summonsed_summonses_3913',
    name: 'summonsed vs summonses',
    description: 'Commonly confused words: summonsed and summonses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsummonsed\b/gi,
    message: 'Check: "summonsed" ("summonsed") vs "summonses" ("summonses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'summonses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sumo_sump_3914',
    name: 'sumo vs sump',
    description: 'Commonly confused words: sumo and sump',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsumo\b/gi,
    message: 'Check: "sumo" ("sumo") vs "sump" ("sump"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sump';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sumps_dumps_3915',
    name: 'sumps vs dumps',
    description: 'Commonly confused words: sumps and dumps',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsumps\b/gi,
    message: 'Check: "sumps" ("sumps") vs "dumps" ("dumps"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dumps';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sunning_sinning_3916',
    name: 'sunning vs sinning',
    description: 'Commonly confused words: sunning and sinning',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsunning\b/gi,
    message: 'Check: "sunning" ("sunning") vs "sinning" ("sinning"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sinning';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_superseded_supersedes_3917',
    name: 'superseded vs supersedes',
    description: 'Commonly confused words: superseded and supersedes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsuperseded\b/gi,
    message: 'Check: "superseded" ("superseded") vs "supersedes" ("supersedes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'supersedes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_supervened_supervenes_3918',
    name: 'supervened vs supervenes',
    description: 'Commonly confused words: supervened and supervenes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsupervened\b/gi,
    message: 'Check: "supervened" ("supervened") vs "supervenes" ("supervenes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'supervenes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_supped_sipped_3919',
    name: 'supped vs sipped',
    description: 'Commonly confused words: supped and sipped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsupped\b/gi,
    message: 'Check: "supped" ("supped") vs "sipped" ("sipped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sipped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_supper_sipper_3920',
    name: 'supper vs sipper',
    description: 'Commonly confused words: supper and sipper',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsupper\b/gi,
    message: 'Check: "supper" ("supper") vs "sipper" ("sipper"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sipper';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_supping_sipping_3921',
    name: 'supping vs sipping',
    description: 'Commonly confused words: supping and sipping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsupping\b/gi,
    message: 'Check: "supping" ("supping") vs "sipping" ("sipping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sipping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_supplied_supplies_3922',
    name: 'supplied vs supplies',
    description: 'Commonly confused words: supplied and supplies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsupplied\b/gi,
    message: 'Check: "supplied" ("supplied") vs "supplies" ("supplies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'supplies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_suppressed_suppresses_3923',
    name: 'suppressed vs suppresses',
    description: 'Commonly confused words: suppressed and suppresses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsuppressed\b/gi,
    message: 'Check: "suppressed" ("suppressed") vs "suppresses" ("suppresses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suppresses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sups_sips_3924',
    name: 'sups vs sips',
    description: 'Commonly confused words: sups and sips',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsups\b/gi,
    message: 'Check: "sups" ("sups") vs "sips" ("sips"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sips';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_surcharged_surcharges_3925',
    name: 'surcharged vs surcharges',
    description: 'Commonly confused words: surcharged and surcharges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsurcharged\b/gi,
    message: 'Check: "surcharged" ("surcharged") vs "surcharges" ("surcharges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'surcharges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_surfing_surging_3926',
    name: 'surfing vs surging',
    description: 'Commonly confused words: surfing and surging',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsurfing\b/gi,
    message: 'Check: "surfing" ("surfing") vs "surging" ("surging"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'surging';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_surged_surges_3927',
    name: 'surged vs surges',
    description: 'Commonly confused words: surged and surges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsurged\b/gi,
    message: 'Check: "surged" ("surged") vs "surges" ("surges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'surges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_surmised_surmises_3928',
    name: 'surmised vs surmises',
    description: 'Commonly confused words: surmised and surmises',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsurmised\b/gi,
    message: 'Check: "surmised" ("surmised") vs "surmises" ("surmises"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'surmises';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sutured_sutures_3929',
    name: 'sutured vs sutures',
    description: 'Commonly confused words: sutured and sutures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsutured\b/gi,
    message: 'Check: "sutured" ("sutured") vs "sutures" ("sutures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sutures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_swabs_swags_3930',
    name: 'swabs vs swags',
    description: 'Commonly confused words: swabs and swags',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bswabs\b/gi,
    message: 'Check: "swabs" ("swabs") vs "swags" ("swags"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'swags';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_swam_seam_3931',
    name: 'swam vs seam',
    description: 'Commonly confused words: swam and seam',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bswam\b/gi,
    message: 'Check: "swam" ("swam") vs "seam" ("seam"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seam';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_swathed_swathes_3932',
    name: 'swathed vs swathes',
    description: 'Commonly confused words: swathed and swathes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bswathed\b/gi,
    message: 'Check: "swathed" ("swathed") vs "swathes" ("swathes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'swathes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_swats_swags_3933',
    name: 'swats vs swags',
    description: 'Commonly confused words: swats and swags',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bswats\b/gi,
    message: 'Check: "swats" ("swats") vs "swags" ("swags"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'swags';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_swearer_sweater_3934',
    name: 'swearer vs sweater',
    description: 'Commonly confused words: swearer and sweater',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bswearer\b/gi,
    message: 'Check: "swearer" ("swearer") vs "sweater" ("sweater"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sweater';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_swearing_sweating_3935',
    name: 'swearing vs sweating',
    description: 'Commonly confused words: swearing and sweating',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bswearing\b/gi,
    message: 'Check: "swearing" ("swearing") vs "sweating" ("sweating"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sweating';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_swears_sweats_3936',
    name: 'swears vs sweats',
    description: 'Commonly confused words: swears and sweats',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bswears\b/gi,
    message: 'Check: "swears" ("swears") vs "sweats" ("sweats"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sweats';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_swine_seine_3937',
    name: 'swine vs seine',
    description: 'Commonly confused words: swine and seine',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bswine\b/gi,
    message: 'Check: "swine" ("swine") vs "seine" ("seine"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seine';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_swines_seines_3938',
    name: 'swines vs seines',
    description: 'Commonly confused words: swines and seines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bswines\b/gi,
    message: 'Check: "swines" ("swines") vs "seines" ("seines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_swing_awing_3939',
    name: 'swing vs awing',
    description: 'Commonly confused words: swing and awing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bswing\b/gi,
    message: 'Check: "swing" ("swing") vs "awing" ("awing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'awing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_sword_swore_3940',
    name: 'sword vs swore',
    description: 'Commonly confused words: sword and swore',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsword\b/gi,
    message: 'Check: "sword" ("sword") vs "swore" ("swore"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'swore';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_symbolized_symbolizes_3941',
    name: 'symbolized vs symbolizes',
    description: 'Commonly confused words: symbolized and symbolizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsymbolized\b/gi,
    message: 'Check: "symbolized" ("symbolized") vs "symbolizes" ("symbolizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'symbolizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_synchronized_synchronizes_3942',
    name: 'synchronized vs synchronizes',
    description: 'Commonly confused words: synchronized and synchronizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsynchronized\b/gi,
    message: 'Check: "synchronized" ("synchronized") vs "synchronizes" ("synchronizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'synchronizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_systematized_systematizes_3943',
    name: 'systematized vs systematizes',
    description: 'Commonly confused words: systematized and systematizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bsystematized\b/gi,
    message: 'Check: "systematized" ("systematized") vs "systematizes" ("systematizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'systematizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
