/**
 * Auto-generated confusion rules (P)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.518Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesP: GrammarRule[] = [
  {
    id: 'confusion_packed_pact_487',
    name: 'packed vs pact',
    description: 'Commonly confused words: packed and pact',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpacked\b/gi,
    message: 'Check: "packed" ("packed") vs "pact" ("pact"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pact';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pail_pale_488',
    name: 'pail vs pale',
    description: 'Commonly confused words: pail and pale',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpail\b/gi,
    message: 'Check: "pail" (bucket) vs "pale" (light colored). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pale';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pails_pales_489',
    name: 'pails vs pales',
    description: 'Commonly confused words: pails and pales',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpails\b/gi,
    message: 'Check: "pails" ("pails") vs "pales" ("pales"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pales';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pairs_pares_490',
    name: 'pairs vs pares',
    description: 'Commonly confused words: pairs and pares',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpairs\b/gi,
    message: 'Check: "pairs" ("pairs") vs "pares" ("pares"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pares';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_palate_palette_491',
    name: 'palate vs palette',
    description: 'Commonly confused words: palate and palette',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpalate\b/gi,
    message: 'Check: "palate" ("palate") vs "palette" ("palette"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'palette';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pare_pear_492',
    name: 'pare vs pear',
    description: 'Commonly confused words: pare and pear',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpare\b/gi,
    message: 'Check: "pare" ("pare") vs "pear" (fruit). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pear';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pares_pears_493',
    name: 'pares vs pears',
    description: 'Commonly confused words: pares and pears',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpares\b/gi,
    message: 'Check: "pares" ("pares") vs "pears" ("pears"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pears';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_passed_past_494',
    name: 'passed vs past',
    description: 'Commonly confused words: passed and past',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpassed\b/gi,
    message: 'Check: "passed" (went by (verb)) vs "past" (previous time (noun/adj/prep)). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'past';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_patients_patience_495',
    name: 'patients vs patience',
    description: 'Commonly confused words: patients and patience',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpatients\b/gi,
    message: 'Did you mean "patience"? "patients" ("patients") is often confused with "patience" ("patience").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'patience';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_patience_patients_496',
    name: 'patience vs patients',
    description: 'Commonly confused words: patience and patients',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpatience\b/gi,
    message: 'Did you mean "patients"? "patience" ("patience") is often confused with "patients" ("patients").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'patients';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peace_piece_497',
    name: 'peace vs piece',
    description: 'Commonly confused words: peace and piece',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeace\b/gi,
    message: 'Check: "peace" (absence of war) vs "piece" (a part of something). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'piece';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peaked_peeked_498',
    name: 'peaked vs peeked',
    description: 'Commonly confused words: peaked and peeked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeaked\b/gi,
    message: 'Check: "peaked" ("peaked") vs "peeked" ("peeked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'peeked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peak_peek_499',
    name: 'peak vs peek',
    description: 'Commonly confused words: peak and peek',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeak\b/gi,
    message: 'Check: "peak" ("peak") vs "peek" ("peek"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'peek';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pealed_peeled_500',
    name: 'pealed vs peeled',
    description: 'Commonly confused words: pealed and peeled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpealed\b/gi,
    message: 'Check: "pealed" ("pealed") vs "peeled" ("peeled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'peeled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peal_peel_501',
    name: 'peal vs peel',
    description: 'Commonly confused words: peal and peel',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeal\b/gi,
    message: 'Check: "peal" ("peal") vs "peel" ("peel"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'peel';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peals_peels_502',
    name: 'peals vs peels',
    description: 'Commonly confused words: peals and peels',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeals\b/gi,
    message: 'Check: "peals" ("peals") vs "peels" ("peels"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'peels';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pea_pee_503',
    name: 'pea vs pee',
    description: 'Commonly confused words: pea and pee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpea\b/gi,
    message: 'Check: "pea" ("pea") vs "pee" ("pee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peeked_piqued_504',
    name: 'peeked vs piqued',
    description: 'Commonly confused words: peeked and piqued',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeeked\b/gi,
    message: 'Check: "peeked" ("peeked") vs "piqued" ("piqued"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'piqued';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peek_pique_505',
    name: 'peek vs pique',
    description: 'Commonly confused words: peek and pique',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeek\b/gi,
    message: 'Check: "peek" ("peek") vs "pique" ("pique"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pique';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peeks_piques_506',
    name: 'peeks vs piques',
    description: 'Commonly confused words: peeks and piques',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeeks\b/gi,
    message: 'Check: "peeks" ("peeks") vs "piques" ("piques"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'piques';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peers_piers_507',
    name: 'peers vs piers',
    description: 'Commonly confused words: peers and piers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeers\b/gi,
    message: 'Check: "peers" ("peers") vs "piers" ("piers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'piers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_picnic_pyknic_508',
    name: 'picnic vs pyknic',
    description: 'Commonly confused words: picnic and pyknic',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpicnic\b/gi,
    message: 'Check: "picnic" ("picnic") vs "pyknic" ("pyknic"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pyknic';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pieced_piste_509',
    name: 'pieced vs piste',
    description: 'Commonly confused words: pieced and piste',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpieced\b/gi,
    message: 'Check: "pieced" ("pieced") vs "piste" ("piste"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'piste';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_placed_places_510',
    name: 'placed vs places',
    description: 'Commonly confused words: placed and places',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bplaced\b/gi,
    message: 'Check: "placed" ("placed") vs "places" ("places"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'places';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_plains_planes_511',
    name: 'plains vs planes',
    description: 'Commonly confused words: plains and planes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bplains\b/gi,
    message: 'Check: "plains" ("plains") vs "planes" ("planes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'planes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_plain_plane_512',
    name: 'plain vs plane',
    description: 'Commonly confused words: plain and plane',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bplain\b/gi,
    message: 'Check: "plain" (simple or flat land) vs "plane" (aircraft or flat surface). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'plane';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_plan_plant_513',
    name: 'plan vs plant',
    description: 'Commonly confused words: plan and plant',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bplan\b/gi,
    message: 'Did you mean "plant"? "plan" ("plan") is often confused with "plant" ("plant").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'plant';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_plant_plan_514',
    name: 'plant vs plan',
    description: 'Commonly confused words: plant and plan',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bplant\b/gi,
    message: 'Did you mean "plan"? "plant" ("plant") is often confused with "plan" ("plan").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'plan';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_plans_plants_515',
    name: 'plans vs plants',
    description: 'Commonly confused words: plans and plants',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bplans\b/gi,
    message: 'Check: "plans" ("plans") vs "plants" ("plants"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'plants';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pleaded_pleated_516',
    name: 'pleaded vs pleated',
    description: 'Commonly confused words: pleaded and pleated',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpleaded\b/gi,
    message: 'Check: "pleaded" ("pleaded") vs "pleated" ("pleated"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pleated';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pleas_please_517',
    name: 'pleas vs please',
    description: 'Commonly confused words: pleas and please',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpleas\b/gi,
    message: 'Check: "pleas" ("pleas") vs "please" ("please"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'please';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pointed_pointer_518',
    name: 'pointed vs pointer',
    description: 'Commonly confused words: pointed and pointer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpointed\b/gi,
    message: 'Check: "pointed" ("pointed") vs "pointer" ("pointer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pointer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pole_poll_519',
    name: 'pole vs poll',
    description: 'Commonly confused words: pole and poll',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpole\b/gi,
    message: 'Check: "pole" ("pole") vs "poll" ("poll"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'poll';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_poled_polled_520',
    name: 'poled vs polled',
    description: 'Commonly confused words: poled and polled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpoled\b/gi,
    message: 'Check: "poled" ("poled") vs "polled" ("polled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'polled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pooling_pulling_521',
    name: 'pooling vs pulling',
    description: 'Commonly confused words: pooling and pulling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpooling\b/gi,
    message: 'Check: "pooling" ("pooling") vs "pulling" ("pulling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pulling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pools_pulls_522',
    name: 'pools vs pulls',
    description: 'Commonly confused words: pools and pulls',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpools\b/gi,
    message: 'Check: "pools" ("pools") vs "pulls" ("pulls"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pulls';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pool_pull_523',
    name: 'pool vs pull',
    description: 'Commonly confused words: pool and pull',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpool\b/gi,
    message: 'Check: "pool" ("pool") vs "pull" ("pull"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pull';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_populace_populous_524',
    name: 'populace vs populous',
    description: 'Commonly confused words: populace and populous',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpopulace\b/gi,
    message: 'Check: "populace" ("populace") vs "populous" ("populous"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'populous';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pores_pours_525',
    name: 'pores vs pours',
    description: 'Commonly confused words: pores and pours',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpores\b/gi,
    message: 'Check: "pores" ("pores") vs "pours" ("pours"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pours';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pore_pour_526',
    name: 'pore vs pour',
    description: 'Commonly confused words: pore and pour',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpore\b/gi,
    message: 'Check: "pore" ("pore") vs "pour" ("pour"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pour';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_portable_potable_527',
    name: 'portable vs potable',
    description: 'Commonly confused words: portable and potable',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bportable\b/gi,
    message: 'Check: "portable" ("portable") vs "potable" ("potable"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'potable';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_portables_potables_528',
    name: 'portables vs potables',
    description: 'Commonly confused words: portables and potables',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bportables\b/gi,
    message: 'Check: "portables" ("portables") vs "potables" ("potables"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'potables';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_port_post_529',
    name: 'port vs post',
    description: 'Commonly confused words: port and post',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bport\b/gi,
    message: 'Check: "port" ("port") vs "post" ("post"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'post';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_portrait_portray_530',
    name: 'portrait vs portray',
    description: 'Commonly confused words: portrait and portray',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bportrait\b/gi,
    message: 'Check: "portrait" ("portrait") vs "portray" ("portray"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'portray';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_portraits_portrays_531',
    name: 'portraits vs portrays',
    description: 'Commonly confused words: portraits and portrays',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bportraits\b/gi,
    message: 'Check: "portraits" ("portraits") vs "portrays" ("portrays"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'portrays';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_praised_prayed_532',
    name: 'praised vs prayed',
    description: 'Commonly confused words: praised and prayed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpraised\b/gi,
    message: 'Check: "praised" ("praised") vs "prayed" ("prayed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prayed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prayed_preyed_533',
    name: 'prayed vs preyed',
    description: 'Commonly confused words: prayed and preyed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprayed\b/gi,
    message: 'Check: "prayed" ("prayed") vs "preyed" ("preyed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'preyed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prays_preys_534',
    name: 'prays vs preys',
    description: 'Commonly confused words: prays and preys',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprays\b/gi,
    message: 'Check: "prays" ("prays") vs "preys" ("preys"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'preys';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pray_prey_535',
    name: 'pray vs prey',
    description: 'Commonly confused words: pray and prey',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpray\b/gi,
    message: 'Check: "pray" ("pray") vs "prey" ("prey"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prey';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_precede_proceed_536',
    name: 'precede vs proceed',
    description: 'Commonly confused words: precede and proceed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprecede\b/gi,
    message: 'Check: "precede" ("precede") vs "proceed" ("proceed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'proceed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_preceded_proceeded_537',
    name: 'preceded vs proceeded',
    description: 'Commonly confused words: preceded and proceeded',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpreceded\b/gi,
    message: 'Check: "preceded" ("preceded") vs "proceeded" ("proceeded"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'proceeded';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_precedence_precedents_538',
    name: 'precedence vs precedents',
    description: 'Commonly confused words: precedence and precedents',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprecedence\b/gi,
    message: 'Check: "precedence" ("precedence") vs "precedents" ("precedents"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'precedents';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_precedes_proceeds_539',
    name: 'precedes vs proceeds',
    description: 'Commonly confused words: precedes and proceeds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprecedes\b/gi,
    message: 'Check: "precedes" ("precedes") vs "proceeds" ("proceeds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'proceeds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_preceding_proceeding_540',
    name: 'preceding vs proceeding',
    description: 'Commonly confused words: preceding and proceeding',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpreceding\b/gi,
    message: 'Check: "preceding" ("preceding") vs "proceeding" ("proceeding"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'proceeding';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_presence_presents_541',
    name: 'presence vs presents',
    description: 'Commonly confused words: presence and presents',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpresence\b/gi,
    message: 'Check: "presence" ("presence") vs "presents" ("presents"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'presents';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prince_prints_542',
    name: 'prince vs prints',
    description: 'Commonly confused words: prince and prints',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprince\b/gi,
    message: 'Check: "prince" ("prince") vs "prints" ("prints"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prints';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_principal_principle_543',
    name: 'principal vs principle',
    description: 'Commonly confused words: principal and principle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprincipal\b/gi,
    message: 'Check: "principal" (main or head of school) vs "principle" (a fundamental truth or rule). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'principle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_principals_principles_544',
    name: 'principals vs principles',
    description: 'Commonly confused words: principals and principles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprincipals\b/gi,
    message: 'Check: "principals" ("principals") vs "principles" ("principles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'principles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_profit_prophet_545',
    name: 'profit vs prophet',
    description: 'Commonly confused words: profit and prophet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprofit\b/gi,
    message: 'Check: "profit" ("profit") vs "prophet" ("prophet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prophet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_profits_prophets_546',
    name: 'profits vs prophets',
    description: 'Commonly confused words: profits and prophets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprofits\b/gi,
    message: 'Check: "profits" ("profits") vs "prophets" ("prophets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prophets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_proof_prove_547',
    name: 'proof vs prove',
    description: 'Commonly confused words: proof and prove',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bproof\b/gi,
    message: 'Check: "proof" ("proof") vs "prove" ("prove"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prove';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prostate_prostrate_548',
    name: 'prostate vs prostrate',
    description: 'Commonly confused words: prostate and prostrate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprostate\b/gi,
    message: 'Check: "prostate" ("prostate") vs "prostrate" ("prostrate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prostrate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_psi_sigh_549',
    name: 'psi vs sigh',
    description: 'Commonly confused words: psi and sigh',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpsi\b/gi,
    message: 'Check: "psi" ("psi") vs "sigh" ("sigh"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sigh';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_perquisites_prerequisites_762',
    name: 'perquisites vs prerequisites',
    description: 'Commonly confused words: perquisites and prerequisites',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bperquisites\b/gi,
    message: 'Check: "perquisites" ("perquisites") vs "prerequisites" ("prerequisites"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prerequisites';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_planned_planed_769',
    name: 'planned vs planed',
    description: 'Commonly confused words: planned and planed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bplanned\b/gi,
    message: 'Did you mean "planed"? "planned" ("planned") is often confused with "planed" ("planed").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'planed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_planed_planned_770',
    name: 'planed vs planned',
    description: 'Commonly confused words: planed and planned',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bplaned\b/gi,
    message: 'Did you mean "planned"? "planed" ("planed") is often confused with "planned" ("planned").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'planned';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_purse_pursue_778',
    name: 'purse vs pursue',
    description: 'Commonly confused words: purse and pursue',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpurse\b/gi,
    message: 'Did you mean "pursue"? "purse" ("purse") is often confused with "pursue" ("pursue").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pursue';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pursue_purse_779',
    name: 'pursue vs purse',
    description: 'Commonly confused words: pursue and purse',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpursue\b/gi,
    message: 'Did you mean "purse"? "pursue" ("pursue") is often confused with "purse" ("purse").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'purse';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_paced_paces_3015',
    name: 'paced vs paces',
    description: 'Commonly confused words: paced and paces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpaced\b/gi,
    message: 'Check: "paced" ("paced") vs "paces" ("paces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'paces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pacified_pacifies_3016',
    name: 'pacified vs pacifies',
    description: 'Commonly confused words: pacified and pacifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpacified\b/gi,
    message: 'Check: "pacified" ("pacified") vs "pacifies" ("pacifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pacifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_packaged_packages_3017',
    name: 'packaged vs packages',
    description: 'Commonly confused words: packaged and packages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpackaged\b/gi,
    message: 'Check: "packaged" ("packaged") vs "packages" ("packages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'packages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_packer_packet_3018',
    name: 'packer vs packet',
    description: 'Commonly confused words: packer and packet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpacker\b/gi,
    message: 'Check: "packer" ("packer") vs "packet" ("packet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'packet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_packers_packets_3019',
    name: 'packers vs packets',
    description: 'Commonly confused words: packers and packets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpackers\b/gi,
    message: 'Check: "packers" ("packers") vs "packets" ("packets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'packets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_paddled_paddles_3020',
    name: 'paddled vs paddles',
    description: 'Commonly confused words: paddled and paddles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpaddled\b/gi,
    message: 'Check: "paddled" ("paddled") vs "paddles" ("paddles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'paddles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_palatalized_palatalizes_3021',
    name: 'palatalized vs palatalizes',
    description: 'Commonly confused words: palatalized and palatalizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpalatalized\b/gi,
    message: 'Check: "palatalized" ("palatalized") vs "palatalizes" ("palatalizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'palatalizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_palpated_palpates_3022',
    name: 'palpated vs palpates',
    description: 'Commonly confused words: palpated and palpates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpalpated\b/gi,
    message: 'Check: "palpated" ("palpated") vs "palpates" ("palpates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'palpates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_palsied_palsies_3023',
    name: 'palsied vs palsies',
    description: 'Commonly confused words: palsied and palsies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpalsied\b/gi,
    message: 'Check: "palsied" ("palsied") vs "palsies" ("palsies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'palsies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pancaked_pancakes_3024',
    name: 'pancaked vs pancakes',
    description: 'Commonly confused words: pancaked and pancakes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpancaked\b/gi,
    message: 'Check: "pancaked" ("pancaked") vs "pancakes" ("pancakes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pancakes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pantomimed_pantomimes_3025',
    name: 'pantomimed vs pantomimes',
    description: 'Commonly confused words: pantomimed and pantomimes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpantomimed\b/gi,
    message: 'Check: "pantomimed" ("pantomimed") vs "pantomimes" ("pantomimes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pantomimes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pants_pangs_3026',
    name: 'pants vs pangs',
    description: 'Commonly confused words: pants and pangs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpants\b/gi,
    message: 'Check: "pants" ("pants") vs "pangs" ("pangs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pangs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_paraded_parades_3027',
    name: 'paraded vs parades',
    description: 'Commonly confused words: paraded and parades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bparaded\b/gi,
    message: 'Check: "paraded" ("paraded") vs "parades" ("parades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'parades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_paralyzed_paralyzes_3028',
    name: 'paralyzed vs paralyzes',
    description: 'Commonly confused words: paralyzed and paralyzes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bparalyzed\b/gi,
    message: 'Check: "paralyzed" ("paralyzed") vs "paralyzes" ("paralyzes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'paralyzes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_parched_patched_3029',
    name: 'parched vs patched',
    description: 'Commonly confused words: parched and patched',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bparched\b/gi,
    message: 'Check: "parched" ("parched") vs "patched" ("patched"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'patched';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pardon_parson_3030',
    name: 'pardon vs parson',
    description: 'Commonly confused words: pardon and parson',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpardon\b/gi,
    message: 'Check: "pardon" ("pardon") vs "parson" ("parson"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'parson';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_parodied_parodies_3031',
    name: 'parodied vs parodies',
    description: 'Commonly confused words: parodied and parodies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bparodied\b/gi,
    message: 'Check: "parodied" ("parodied") vs "parodies" ("parodies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'parodies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_paroled_parolee_3032',
    name: 'paroled vs parolee',
    description: 'Commonly confused words: paroled and parolee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bparoled\b/gi,
    message: 'Check: "paroled" ("paroled") vs "parolee" ("parolee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'parolee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_paroled_paroles_3033',
    name: 'paroled vs paroles',
    description: 'Commonly confused words: paroled and paroles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bparoled\b/gi,
    message: 'Check: "paroled" ("paroled") vs "paroles" ("paroles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'paroles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_parried_parries_3034',
    name: 'parried vs parries',
    description: 'Commonly confused words: parried and parries',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bparried\b/gi,
    message: 'Check: "parried" ("parried") vs "parries" ("parries"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'parries';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_parried_partied_3035',
    name: 'parried vs partied',
    description: 'Commonly confused words: parried and partied',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bparried\b/gi,
    message: 'Check: "parried" ("parried") vs "partied" ("partied"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'partied';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_parsed_parsec_3036',
    name: 'parsed vs parsec',
    description: 'Commonly confused words: parsed and parsec',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bparsed\b/gi,
    message: 'Check: "parsed" ("parsed") vs "parsec" ("parsec"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'parsec';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_parsed_parses_3037',
    name: 'parsed vs parses',
    description: 'Commonly confused words: parsed and parses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bparsed\b/gi,
    message: 'Check: "parsed" ("parsed") vs "parses" ("parses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'parses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_participated_participates_3038',
    name: 'participated vs participates',
    description: 'Commonly confused words: participated and participates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bparticipated\b/gi,
    message: 'Check: "participated" ("participated") vs "participates" ("participates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'participates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_passed_passes_3039',
    name: 'passed vs passes',
    description: 'Commonly confused words: passed and passes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpassed\b/gi,
    message: 'Check: "passed" (went by (verb)) vs "passes" ("passes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'passes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pasta_pasts_3040',
    name: 'pasta vs pasts',
    description: 'Commonly confused words: pasta and pasts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpasta\b/gi,
    message: 'Check: "pasta" ("pasta") vs "pasts" ("pasts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pasts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pastured_pastures_3041',
    name: 'pastured vs pastures',
    description: 'Commonly confused words: pastured and pastures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpastured\b/gi,
    message: 'Check: "pastured" ("pastured") vs "pastures" ("pastures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pastures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_patched_patches_3042',
    name: 'patched vs patches',
    description: 'Commonly confused words: patched and patches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpatched\b/gi,
    message: 'Check: "patched" ("patched") vs "patches" ("patches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'patches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_patronized_patronizes_3043',
    name: 'patronized vs patronizes',
    description: 'Commonly confused words: patronized and patronizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpatronized\b/gi,
    message: 'Check: "patronized" ("patronized") vs "patronizes" ("patronizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'patronizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_patted_parted_3044',
    name: 'patted vs parted',
    description: 'Commonly confused words: patted and parted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpatted\b/gi,
    message: 'Check: "patted" ("patted") vs "parted" ("parted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'parted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_patting_parting_3045',
    name: 'patting vs parting',
    description: 'Commonly confused words: patting and parting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpatting\b/gi,
    message: 'Check: "patting" ("patting") vs "parting" ("parting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'parting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_paved_paced_3046',
    name: 'paved vs paced',
    description: 'Commonly confused words: paved and paced',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpaved\b/gi,
    message: 'Check: "paved" ("paved") vs "paced" ("paced"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'paced';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_paved_paves_3047',
    name: 'paved vs paves',
    description: 'Commonly confused words: paved and paves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpaved\b/gi,
    message: 'Check: "paved" ("paved") vs "paves" ("paves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'paves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_paves_paces_3048',
    name: 'paves vs paces',
    description: 'Commonly confused words: paves and paces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpaves\b/gi,
    message: 'Check: "paves" ("paves") vs "paces" ("paces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'paces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_paving_pacing_3049',
    name: 'paving vs pacing',
    description: 'Commonly confused words: paving and pacing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpaving\b/gi,
    message: 'Check: "paving" ("paving") vs "pacing" ("pacing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pacing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_payee_payed_3050',
    name: 'payee vs payed',
    description: 'Commonly confused words: payee and payed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpayee\b/gi,
    message: 'Check: "payee" ("payee") vs "payed" ("payed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'payed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_payee_payer_3051',
    name: 'payee vs payer',
    description: 'Commonly confused words: payee and payer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpayee\b/gi,
    message: 'Check: "payee" ("payee") vs "payer" ("payer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'payer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_payees_payers_3052',
    name: 'payees vs payers',
    description: 'Commonly confused words: payees and payers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpayees\b/gi,
    message: 'Check: "payees" ("payees") vs "payers" ("payers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'payers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peaked_pealed_3053',
    name: 'peaked vs pealed',
    description: 'Commonly confused words: peaked and pealed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeaked\b/gi,
    message: 'Check: "peaked" ("peaked") vs "pealed" ("pealed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pealed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peaking_pealing_3054',
    name: 'peaking vs pealing',
    description: 'Commonly confused words: peaking and pealing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeaking\b/gi,
    message: 'Check: "peaking" ("peaking") vs "pealing" ("pealing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pealing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peaky_pesky_3055',
    name: 'peaky vs pesky',
    description: 'Commonly confused words: peaky and pesky',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeaky\b/gi,
    message: 'Check: "peaky" ("peaky") vs "pesky" ("pesky"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pesky';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peat_pest_3056',
    name: 'peat vs pest',
    description: 'Commonly confused words: peat and pest',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeat\b/gi,
    message: 'Check: "peat" ("peat") vs "pest" ("pest"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pest';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pebbled_pebbles_3057',
    name: 'pebbled vs pebbles',
    description: 'Commonly confused words: pebbled and pebbles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpebbled\b/gi,
    message: 'Check: "pebbled" ("pebbled") vs "pebbles" ("pebbles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pebbles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pedigreed_pedigrees_3058',
    name: 'pedigreed vs pedigrees',
    description: 'Commonly confused words: pedigreed and pedigrees',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpedigreed\b/gi,
    message: 'Check: "pedigreed" ("pedigreed") vs "pedigrees" ("pedigrees"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pedigrees';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peeked_peeled_3059',
    name: 'peeked vs peeled',
    description: 'Commonly confused words: peeked and peeled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeeked\b/gi,
    message: 'Check: "peeked" ("peeked") vs "peeled" ("peeled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'peeled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peeked_perked_3060',
    name: 'peeked vs perked',
    description: 'Commonly confused words: peeked and perked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeeked\b/gi,
    message: 'Check: "peeked" ("peeked") vs "perked" ("perked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'perked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peeking_peeling_3061',
    name: 'peeking vs peeling',
    description: 'Commonly confused words: peeking and peeling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeeking\b/gi,
    message: 'Check: "peeking" ("peeking") vs "peeling" ("peeling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'peeling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peeking_perking_3062',
    name: 'peeking vs perking',
    description: 'Commonly confused words: peeking and perking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeeking\b/gi,
    message: 'Check: "peeking" ("peeking") vs "perking" ("perking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'perking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peeks_peels_3063',
    name: 'peeks vs peels',
    description: 'Commonly confused words: peeks and peels',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeeks\b/gi,
    message: 'Check: "peeks" ("peeks") vs "peels" ("peels"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'peels';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_penalized_penalizes_3064',
    name: 'penalized vs penalizes',
    description: 'Commonly confused words: penalized and penalizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpenalized\b/gi,
    message: 'Check: "penalized" ("penalized") vs "penalizes" ("penalizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'penalizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_penetrated_penetrates_3065',
    name: 'penetrated vs penetrates',
    description: 'Commonly confused words: penetrated and penetrates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpenetrated\b/gi,
    message: 'Check: "penetrated" ("penetrated") vs "penetrates" ("penetrates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'penetrates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_peopled_peoples_3066',
    name: 'peopled vs peoples',
    description: 'Commonly confused words: peopled and peoples',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpeopled\b/gi,
    message: 'Check: "peopled" ("peopled") vs "peoples" ("peoples"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'peoples';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_perceived_perceives_3067',
    name: 'perceived vs perceives',
    description: 'Commonly confused words: perceived and perceives',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bperceived\b/gi,
    message: 'Check: "perceived" ("perceived") vs "perceives" ("perceives"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'perceives';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_perched_perches_3068',
    name: 'perched vs perches',
    description: 'Commonly confused words: perched and perches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bperched\b/gi,
    message: 'Check: "perched" ("perched") vs "perches" ("perches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'perches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_percolated_percolates_3069',
    name: 'percolated vs percolates',
    description: 'Commonly confused words: percolated and percolates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpercolated\b/gi,
    message: 'Check: "percolated" ("percolated") vs "percolates" ("percolates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'percolates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_perforated_perforates_3070',
    name: 'perforated vs perforates',
    description: 'Commonly confused words: perforated and perforates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bperforated\b/gi,
    message: 'Check: "perforated" ("perforated") vs "perforates" ("perforates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'perforates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_perfumed_perfumes_3071',
    name: 'perfumed vs perfumes',
    description: 'Commonly confused words: perfumed and perfumes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bperfumed\b/gi,
    message: 'Check: "perfumed" ("perfumed") vs "perfumes" ("perfumes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'perfumes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_perjured_perjures_3072',
    name: 'perjured vs perjures',
    description: 'Commonly confused words: perjured and perjures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bperjured\b/gi,
    message: 'Check: "perjured" ("perjured") vs "perjures" ("perjures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'perjures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_perks_peeks_3073',
    name: 'perks vs peeks',
    description: 'Commonly confused words: perks and peeks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bperks\b/gi,
    message: 'Check: "perks" ("perks") vs "peeks" ("peeks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'peeks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_permeated_permeates_3074',
    name: 'permeated vs permeates',
    description: 'Commonly confused words: permeated and permeates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpermeated\b/gi,
    message: 'Check: "permeated" ("permeated") vs "permeates" ("permeates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'permeates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_perpetrated_perpetrates_3075',
    name: 'perpetrated vs perpetrates',
    description: 'Commonly confused words: perpetrated and perpetrates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bperpetrated\b/gi,
    message: 'Check: "perpetrated" ("perpetrated") vs "perpetrates" ("perpetrates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'perpetrates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_perpetuated_perpetuates_3076',
    name: 'perpetuated vs perpetuates',
    description: 'Commonly confused words: perpetuated and perpetuates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bperpetuated\b/gi,
    message: 'Check: "perpetuated" ("perpetuated") vs "perpetuates" ("perpetuates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'perpetuates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_perplexed_perplexes_3077',
    name: 'perplexed vs perplexes',
    description: 'Commonly confused words: perplexed and perplexes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bperplexed\b/gi,
    message: 'Check: "perplexed" ("perplexed") vs "perplexes" ("perplexes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'perplexes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_persecuted_persecutes_3078',
    name: 'persecuted vs persecutes',
    description: 'Commonly confused words: persecuted and persecutes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpersecuted\b/gi,
    message: 'Check: "persecuted" ("persecuted") vs "persecutes" ("persecutes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'persecutes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_personified_personifies_3079',
    name: 'personified vs personifies',
    description: 'Commonly confused words: personified and personifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpersonified\b/gi,
    message: 'Check: "personified" ("personified") vs "personifies" ("personifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'personifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_perused_peruses_3080',
    name: 'perused vs peruses',
    description: 'Commonly confused words: perused and peruses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bperused\b/gi,
    message: 'Check: "perused" ("perused") vs "peruses" ("peruses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'peruses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_petrified_petrifies_3081',
    name: 'petrified vs petrifies',
    description: 'Commonly confused words: petrified and petrifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpetrified\b/gi,
    message: 'Check: "petrified" ("petrified") vs "petrifies" ("petrifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'petrifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_phased_phases_3082',
    name: 'phased vs phases',
    description: 'Commonly confused words: phased and phases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bphased\b/gi,
    message: 'Check: "phased" ("phased") vs "phases" ("phases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'phases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_phobic_phonic_3083',
    name: 'phobic vs phonic',
    description: 'Commonly confused words: phobic and phonic',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bphobic\b/gi,
    message: 'Check: "phobic" ("phobic") vs "phonic" ("phonic"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'phonic';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_phobics_phonics_3084',
    name: 'phobics vs phonics',
    description: 'Commonly confused words: phobics and phonics',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bphobics\b/gi,
    message: 'Check: "phobics" ("phobics") vs "phonics" ("phonics"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'phonics';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_phoned_phones_3085',
    name: 'phoned vs phones',
    description: 'Commonly confused words: phoned and phones',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bphoned\b/gi,
    message: 'Check: "phoned" ("phoned") vs "phones" ("phones"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'phones';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_photocopied_photocopies_3086',
    name: 'photocopied vs photocopies',
    description: 'Commonly confused words: photocopied and photocopies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bphotocopied\b/gi,
    message: 'Check: "photocopied" ("photocopied") vs "photocopies" ("photocopies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'photocopies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pickers_pickets_3087',
    name: 'pickers vs pickets',
    description: 'Commonly confused words: pickers and pickets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpickers\b/gi,
    message: 'Check: "pickers" ("pickers") vs "pickets" ("pickets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pickets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_picketed_pocketed_3088',
    name: 'picketed vs pocketed',
    description: 'Commonly confused words: picketed and pocketed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpicketed\b/gi,
    message: 'Check: "picketed" ("picketed") vs "pocketed" ("pocketed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pocketed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_picketing_pocketing_3089',
    name: 'picketing vs pocketing',
    description: 'Commonly confused words: picketing and pocketing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpicketing\b/gi,
    message: 'Check: "picketing" ("picketing") vs "pocketing" ("pocketing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pocketing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_picket_picker_3090',
    name: 'picket vs picker',
    description: 'Commonly confused words: picket and picker',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpicket\b/gi,
    message: 'Check: "picket" ("picket") vs "picker" ("picker"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'picker';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pickled_pickles_3091',
    name: 'pickled vs pickles',
    description: 'Commonly confused words: pickled and pickles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpickled\b/gi,
    message: 'Check: "pickled" ("pickled") vs "pickles" ("pickles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pickles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_piddling_puddling_3092',
    name: 'piddling vs puddling',
    description: 'Commonly confused words: piddling and puddling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpiddling\b/gi,
    message: 'Check: "piddling" ("piddling") vs "puddling" ("puddling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'puddling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pied_pies_3093',
    name: 'pied vs pies',
    description: 'Commonly confused words: pied and pies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpied\b/gi,
    message: 'Check: "pied" ("pied") vs "pies" ("pies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pierced_pierces_3094',
    name: 'pierced vs pierces',
    description: 'Commonly confused words: pierced and pierces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpierced\b/gi,
    message: 'Check: "pierced" ("pierced") vs "pierces" ("pierces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pierces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pigeonholed_pigeonholes_3095',
    name: 'pigeonholed vs pigeonholes',
    description: 'Commonly confused words: pigeonholed and pigeonholes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpigeonholed\b/gi,
    message: 'Check: "pigeonholed" ("pigeonholed") vs "pigeonholes" ("pigeonholes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pigeonholes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_piked_pikes_3096',
    name: 'piked vs pikes',
    description: 'Commonly confused words: piked and pikes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpiked\b/gi,
    message: 'Check: "piked" ("piked") vs "pikes" ("pikes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pikes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pikers_pokers_3097',
    name: 'pikers vs pokers',
    description: 'Commonly confused words: pikers and pokers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpikers\b/gi,
    message: 'Check: "pikers" ("pikers") vs "pokers" ("pokers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pokers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_piled_piked_3098',
    name: 'piled vs piked',
    description: 'Commonly confused words: piled and piked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpiled\b/gi,
    message: 'Check: "piled" ("piled") vs "piked" ("piked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'piked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_piled_piles_3099',
    name: 'piled vs piles',
    description: 'Commonly confused words: piled and piles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpiled\b/gi,
    message: 'Check: "piled" ("piled") vs "piles" ("piles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'piles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_piles_pikes_3100',
    name: 'piles vs pikes',
    description: 'Commonly confused words: piles and pikes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpiles\b/gi,
    message: 'Check: "piles" ("piles") vs "pikes" ("pikes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pikes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pillaged_pillages_3101',
    name: 'pillaged vs pillages',
    description: 'Commonly confused words: pillaged and pillages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpillaged\b/gi,
    message: 'Check: "pillaged" ("pillaged") vs "pillages" ("pillages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pillages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pilloried_pillories_3102',
    name: 'pilloried vs pillories',
    description: 'Commonly confused words: pilloried and pillories',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpilloried\b/gi,
    message: 'Check: "pilloried" ("pilloried") vs "pillories" ("pillories"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pillories';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pimpled_pimples_3103',
    name: 'pimpled vs pimples',
    description: 'Commonly confused words: pimpled and pimples',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpimpled\b/gi,
    message: 'Check: "pimpled" ("pimpled") vs "pimples" ("pimples"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pimples';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pimp_pomp_3104',
    name: 'pimp vs pomp',
    description: 'Commonly confused words: pimp and pomp',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpimp\b/gi,
    message: 'Check: "pimp" ("pimp") vs "pomp" ("pomp"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pomp';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pinched_punched_3105',
    name: 'pinched vs punched',
    description: 'Commonly confused words: pinched and punched',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpinched\b/gi,
    message: 'Check: "pinched" ("pinched") vs "punched" ("punched"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'punched';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pinging_ponging_3106',
    name: 'pinging vs ponging',
    description: 'Commonly confused words: pinging and ponging',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpinging\b/gi,
    message: 'Check: "pinging" ("pinging") vs "ponging" ("ponging"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ponging';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pinning_punning_3107',
    name: 'pinning vs punning',
    description: 'Commonly confused words: pinning and punning',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpinning\b/gi,
    message: 'Check: "pinning" ("pinning") vs "punning" ("punning"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'punning';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pint_ping_3108',
    name: 'pint vs ping',
    description: 'Commonly confused words: pint and ping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpint\b/gi,
    message: 'Check: "pint" ("pint") vs "ping" ("ping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_piqued_piques_3109',
    name: 'piqued vs piques',
    description: 'Commonly confused words: piqued and piques',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpiqued\b/gi,
    message: 'Check: "piqued" ("piqued") vs "piques" ("piques"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'piques';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pirated_pirates_3110',
    name: 'pirated vs pirates',
    description: 'Commonly confused words: pirated and pirates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpirated\b/gi,
    message: 'Check: "pirated" ("pirated") vs "pirates" ("pirates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pirates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pissed_pisses_3111',
    name: 'pissed vs pisses',
    description: 'Commonly confused words: pissed and pisses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpissed\b/gi,
    message: 'Check: "pissed" ("pissed") vs "pisses" ("pisses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pisses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pitched_pitches_3112',
    name: 'pitched vs pitches',
    description: 'Commonly confused words: pitched and pitches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpitched\b/gi,
    message: 'Check: "pitched" ("pitched") vs "pitches" ("pitches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pitches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pithead_pothead_3113',
    name: 'pithead vs pothead',
    description: 'Commonly confused words: pithead and pothead',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpithead\b/gi,
    message: 'Check: "pithead" ("pithead") vs "pothead" ("pothead"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pothead';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pitting_pitying_3114',
    name: 'pitting vs pitying',
    description: 'Commonly confused words: pitting and pitying',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpitting\b/gi,
    message: 'Check: "pitting" ("pitting") vs "pitying" ("pitying"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pitying';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pitting_potting_3115',
    name: 'pitting vs potting',
    description: 'Commonly confused words: pitting and potting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpitting\b/gi,
    message: 'Check: "pitting" ("pitting") vs "potting" ("potting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'potting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pkt_pit_3116',
    name: 'pkt vs pit',
    description: 'Commonly confused words: pkt and pit',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpkt\b/gi,
    message: 'Check: "pkt" ("pkt") vs "pit" ("pit"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pit';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_placated_placates_3117',
    name: 'placated vs placates',
    description: 'Commonly confused words: placated and placates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bplacated\b/gi,
    message: 'Check: "placated" ("placated") vs "placates" ("placates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'placates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_plagued_plagues_3118',
    name: 'plagued vs plagues',
    description: 'Commonly confused words: plagued and plagues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bplagued\b/gi,
    message: 'Check: "plagued" ("plagued") vs "plagues" ("plagues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'plagues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_planed_planes_3119',
    name: 'planed vs planes',
    description: 'Commonly confused words: planed and planes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bplaned\b/gi,
    message: 'Check: "planed" ("planed") vs "planes" ("planes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'planes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_plated_plates_3120',
    name: 'plated vs plates',
    description: 'Commonly confused words: plated and plates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bplated\b/gi,
    message: 'Check: "plated" ("plated") vs "plates" ("plates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'plates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pleaded_pleased_3121',
    name: 'pleaded vs pleased',
    description: 'Commonly confused words: pleaded and pleased',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpleaded\b/gi,
    message: 'Check: "pleaded" ("pleaded") vs "pleased" ("pleased"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pleased';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pleading_pleasing_3122',
    name: 'pleading vs pleasing',
    description: 'Commonly confused words: pleading and pleasing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpleading\b/gi,
    message: 'Check: "pleading" ("pleading") vs "pleasing" ("pleasing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pleasing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pleased_pleases_3123',
    name: 'pleased vs pleases',
    description: 'Commonly confused words: pleased and pleases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpleased\b/gi,
    message: 'Check: "pleased" ("pleased") vs "pleases" ("pleases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pleases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pleas_plead_3124',
    name: 'pleas vs plead',
    description: 'Commonly confused words: pleas and plead',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpleas\b/gi,
    message: 'Check: "pleas" ("pleas") vs "plead" ("plead"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'plead';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pleasured_pleasures_3125',
    name: 'pleasured vs pleasures',
    description: 'Commonly confused words: pleasured and pleasures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpleasured\b/gi,
    message: 'Check: "pleasured" ("pleasured") vs "pleasures" ("pleasures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pleasures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_plumed_plumes_3126',
    name: 'plumed vs plumes',
    description: 'Commonly confused words: plumed and plumes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bplumed\b/gi,
    message: 'Check: "plumed" ("plumed") vs "plumes" ("plumes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'plumes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_poached_poaches_3127',
    name: 'poached vs poaches',
    description: 'Commonly confused words: poached and poaches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpoached\b/gi,
    message: 'Check: "poached" ("poached") vs "poaches" ("poaches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'poaches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pocked_picked_3128',
    name: 'pocked vs picked',
    description: 'Commonly confused words: pocked and picked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpocked\b/gi,
    message: 'Check: "pocked" ("pocked") vs "picked" ("picked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'picked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_poets_ports_3129',
    name: 'poets vs ports',
    description: 'Commonly confused words: poets and ports',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpoets\b/gi,
    message: 'Check: "poets" ("poets") vs "ports" ("ports"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ports';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_poked_piked_3130',
    name: 'poked vs piked',
    description: 'Commonly confused words: poked and piked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpoked\b/gi,
    message: 'Check: "poked" ("poked") vs "piked" ("piked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'piked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_poker_piker_3131',
    name: 'poker vs piker',
    description: 'Commonly confused words: poker and piker',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpoker\b/gi,
    message: 'Check: "poker" ("poker") vs "piker" ("piker"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'piker';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_poking_poling_3132',
    name: 'poking vs poling',
    description: 'Commonly confused words: poking and poling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpoking\b/gi,
    message: 'Check: "poking" ("poking") vs "poling" ("poling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'poling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_polarized_polarizes_3133',
    name: 'polarized vs polarizes',
    description: 'Commonly confused words: polarized and polarizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpolarized\b/gi,
    message: 'Check: "polarized" ("polarized") vs "polarizes" ("polarizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'polarizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_poled_piled_3134',
    name: 'poled vs piled',
    description: 'Commonly confused words: poled and piled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpoled\b/gi,
    message: 'Check: "poled" ("poled") vs "piled" ("piled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'piled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_poled_poked_3135',
    name: 'poled vs poked',
    description: 'Commonly confused words: poled and poked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpoled\b/gi,
    message: 'Check: "poled" ("poled") vs "poked" ("poked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'poked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_poled_poles_3136',
    name: 'poled vs poles',
    description: 'Commonly confused words: poled and poles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpoled\b/gi,
    message: 'Check: "poled" ("poled") vs "poles" ("poles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'poles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_policed_polices_3137',
    name: 'policed vs polices',
    description: 'Commonly confused words: policed and polices',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpoliced\b/gi,
    message: 'Check: "policed" ("policed") vs "polices" ("polices"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'polices';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_politicized_politicizes_3138',
    name: 'politicized vs politicizes',
    description: 'Commonly confused words: politicized and politicizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpoliticized\b/gi,
    message: 'Check: "politicized" ("politicized") vs "politicizes" ("politicizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'politicizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_polled_pooled_3139',
    name: 'polled vs pooled',
    description: 'Commonly confused words: polled and pooled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpolled\b/gi,
    message: 'Check: "polled" ("polled") vs "pooled" ("pooled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pooled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_polling_pilling_3140',
    name: 'polling vs pilling',
    description: 'Commonly confused words: polling and pilling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpolling\b/gi,
    message: 'Check: "polling" ("polling") vs "pilling" ("pilling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pilling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_polling_pooling_3141',
    name: 'polling vs pooling',
    description: 'Commonly confused words: polling and pooling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpolling\b/gi,
    message: 'Check: "polling" ("polling") vs "pooling" ("pooling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pooling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_polluted_pollutes_3142',
    name: 'polluted vs pollutes',
    description: 'Commonly confused words: polluted and pollutes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpolluted\b/gi,
    message: 'Check: "polluted" ("polluted") vs "pollutes" ("pollutes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pollutes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_polo_poll_3143',
    name: 'polo vs poll',
    description: 'Commonly confused words: polo and poll',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpolo\b/gi,
    message: 'Check: "polo" ("polo") vs "poll" ("poll"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'poll';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ponds_pones_3144',
    name: 'ponds vs pones',
    description: 'Commonly confused words: ponds and pones',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bponds\b/gi,
    message: 'Check: "ponds" ("ponds") vs "pones" ("pones"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pones';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pones_pines_3145',
    name: 'pones vs pines',
    description: 'Commonly confused words: pones and pines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpones\b/gi,
    message: 'Check: "pones" ("pones") vs "pines" ("pines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pooping_popping_3146',
    name: 'pooping vs popping',
    description: 'Commonly confused words: pooping and popping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpooping\b/gi,
    message: 'Check: "pooping" ("pooping") vs "popping" ("popping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'popping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pops_oops_3147',
    name: 'pops vs oops',
    description: 'Commonly confused words: pops and oops',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpops\b/gi,
    message: 'Check: "pops" ("pops") vs "oops" ("oops"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'oops';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pored_pores_3148',
    name: 'pored vs pores',
    description: 'Commonly confused words: pored and pores',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpored\b/gi,
    message: 'Check: "pored" ("pored") vs "pores" ("pores"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pores';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_portaged_portages_3149',
    name: 'portaged vs portages',
    description: 'Commonly confused words: portaged and portages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bportaged\b/gi,
    message: 'Check: "portaged" ("portaged") vs "portages" ("portages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'portages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ported_potted_3150',
    name: 'ported vs potted',
    description: 'Commonly confused words: ported and potted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bported\b/gi,
    message: 'Check: "ported" ("ported") vs "potted" ("potted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'potted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_posed_poses_3151',
    name: 'posed vs poses',
    description: 'Commonly confused words: posed and poses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bposed\b/gi,
    message: 'Check: "posed" ("posed") vs "poses" ("poses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'poses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_posses_pisses_3152',
    name: 'posses vs pisses',
    description: 'Commonly confused words: posses and pisses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bposses\b/gi,
    message: 'Check: "posses" ("posses") vs "pisses" ("pisses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pisses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_possessed_possesses_3153',
    name: 'possessed vs possesses',
    description: 'Commonly confused words: possessed and possesses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpossessed\b/gi,
    message: 'Check: "possessed" ("possessed") vs "possesses" ("possesses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'possesses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_postponed_postpones_3154',
    name: 'postponed vs postpones',
    description: 'Commonly confused words: postponed and postpones',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpostponed\b/gi,
    message: 'Check: "postponed" ("postponed") vs "postpones" ("postpones"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'postpones';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_postulated_postulates_3155',
    name: 'postulated vs postulates',
    description: 'Commonly confused words: postulated and postulates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpostulated\b/gi,
    message: 'Check: "postulated" ("postulated") vs "postulates" ("postulates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'postulates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_postured_postures_3156',
    name: 'postured vs postures',
    description: 'Commonly confused words: postured and postures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpostured\b/gi,
    message: 'Check: "postured" ("postured") vs "postures" ("postures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'postures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_posy_posh_3157',
    name: 'posy vs posh',
    description: 'Commonly confused words: posy and posh',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bposy\b/gi,
    message: 'Check: "posy" ("posy") vs "posh" ("posh"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'posh';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_potentiated_potentiates_3158',
    name: 'potentiated vs potentiates',
    description: 'Commonly confused words: potentiated and potentiates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpotentiated\b/gi,
    message: 'Check: "potentiated" ("potentiated") vs "potentiates" ("potentiates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'potentiates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_potholed_potholes_3159',
    name: 'potholed vs potholes',
    description: 'Commonly confused words: potholed and potholes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpotholed\b/gi,
    message: 'Check: "potholed" ("potholed") vs "potholes" ("potholes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'potholes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pouched_pouches_3160',
    name: 'pouched vs pouches',
    description: 'Commonly confused words: pouched and pouches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpouched\b/gi,
    message: 'Check: "pouched" ("pouched") vs "pouches" ("pouches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pouches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pouf_pour_3161',
    name: 'pouf vs pour',
    description: 'Commonly confused words: pouf and pour',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpouf\b/gi,
    message: 'Check: "pouf" ("pouf") vs "pour" ("pour"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pour';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pounced_pounded_3162',
    name: 'pounced vs pounded',
    description: 'Commonly confused words: pounced and pounded',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpounced\b/gi,
    message: 'Check: "pounced" ("pounced") vs "pounded" ("pounded"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pounded';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pouring_pouting_3163',
    name: 'pouring vs pouting',
    description: 'Commonly confused words: pouring and pouting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpouring\b/gi,
    message: 'Check: "pouring" ("pouring") vs "pouting" ("pouting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pouting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pour_pout_3164',
    name: 'pour vs pout',
    description: 'Commonly confused words: pour and pout',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpour\b/gi,
    message: 'Check: "pour" ("pour") vs "pout" ("pout"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pout';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ppm_pom_3165',
    name: 'ppm vs pom',
    description: 'Commonly confused words: ppm and pom',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bppm\b/gi,
    message: 'Check: "ppm" ("ppm") vs "pom" ("pom"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pom';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_practiced_practices_3166',
    name: 'practiced vs practices',
    description: 'Commonly confused words: practiced and practices',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpracticed\b/gi,
    message: 'Check: "practiced" ("practiced") vs "practices" ("practices"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'practices';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_praised_praises_3167',
    name: 'praised vs praises',
    description: 'Commonly confused words: praised and praises',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpraised\b/gi,
    message: 'Check: "praised" ("praised") vs "praises" ("praises"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'praises';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prats_prays_3168',
    name: 'prats vs prays',
    description: 'Commonly confused words: prats and prays',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprats\b/gi,
    message: 'Check: "prats" ("prats") vs "prays" ("prays"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prays';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_preceded_precedes_3169',
    name: 'preceded vs precedes',
    description: 'Commonly confused words: preceded and precedes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpreceded\b/gi,
    message: 'Check: "preceded" ("preceded") vs "precedes" ("precedes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'precedes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_precious_previous_3170',
    name: 'precious vs previous',
    description: 'Commonly confused words: precious and previous',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprecious\b/gi,
    message: 'Check: "precious" ("precious") vs "previous" ("previous"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'previous';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_precipitated_precipitates_3171',
    name: 'precipitated vs precipitates',
    description: 'Commonly confused words: precipitated and precipitates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprecipitated\b/gi,
    message: 'Check: "precipitated" ("precipitated") vs "precipitates" ("precipitates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'precipitates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_precluded_precludes_3172',
    name: 'precluded vs precludes',
    description: 'Commonly confused words: precluded and precludes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprecluded\b/gi,
    message: 'Check: "precluded" ("precluded") vs "precludes" ("precludes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'precludes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_predatory_prefatory_3173',
    name: 'predatory vs prefatory',
    description: 'Commonly confused words: predatory and prefatory',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpredatory\b/gi,
    message: 'Check: "predatory" ("predatory") vs "prefatory" ("prefatory"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prefatory';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_predeceased_predeceases_3174',
    name: 'predeceased vs predeceases',
    description: 'Commonly confused words: predeceased and predeceases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpredeceased\b/gi,
    message: 'Check: "predeceased" ("predeceased") vs "predeceases" ("predeceases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'predeceases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_predicated_predicates_3175',
    name: 'predicated vs predicates',
    description: 'Commonly confused words: predicated and predicates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpredicated\b/gi,
    message: 'Check: "predicated" ("predicated") vs "predicates" ("predicates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'predicates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_predisposed_predisposes_3176',
    name: 'predisposed vs predisposes',
    description: 'Commonly confused words: predisposed and predisposes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpredisposed\b/gi,
    message: 'Check: "predisposed" ("predisposed") vs "predisposes" ("predisposes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'predisposes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_predominated_predominates_3177',
    name: 'predominated vs predominates',
    description: 'Commonly confused words: predominated and predominates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpredominated\b/gi,
    message: 'Check: "predominated" ("predominated") vs "predominates" ("predominates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'predominates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prefaced_prefaces_3178',
    name: 'prefaced vs prefaces',
    description: 'Commonly confused words: prefaced and prefaces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprefaced\b/gi,
    message: 'Check: "prefaced" ("prefaced") vs "prefaces" ("prefaces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prefaces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prefigured_prefigures_3179',
    name: 'prefigured vs prefigures',
    description: 'Commonly confused words: prefigured and prefigures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprefigured\b/gi,
    message: 'Check: "prefigured" ("prefigured") vs "prefigures" ("prefigures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prefigures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prefixed_prefixes_3180',
    name: 'prefixed vs prefixes',
    description: 'Commonly confused words: prefixed and prefixes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprefixed\b/gi,
    message: 'Check: "prefixed" ("prefixed") vs "prefixes" ("prefixes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prefixes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prejudiced_prejudices_3181',
    name: 'prejudiced vs prejudices',
    description: 'Commonly confused words: prejudiced and prejudices',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprejudiced\b/gi,
    message: 'Check: "prejudiced" ("prejudiced") vs "prejudices" ("prejudices"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prejudices';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_premised_premixed_3182',
    name: 'premised vs premixed',
    description: 'Commonly confused words: premised and premixed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpremised\b/gi,
    message: 'Check: "premised" ("premised") vs "premixed" ("premixed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'premixed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_premises_premixes_3183',
    name: 'premises vs premixes',
    description: 'Commonly confused words: premises and premixes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpremises\b/gi,
    message: 'Check: "premises" ("premises") vs "premixes" ("premixes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'premixes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_premixed_premixes_3184',
    name: 'premixed vs premixes',
    description: 'Commonly confused words: premixed and premixes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpremixed\b/gi,
    message: 'Check: "premixed" ("premixed") vs "premixes" ("premixes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'premixes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_preoccupied_preoccupies_3185',
    name: 'preoccupied vs preoccupies',
    description: 'Commonly confused words: preoccupied and preoccupies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpreoccupied\b/gi,
    message: 'Check: "preoccupied" ("preoccupied") vs "preoccupies" ("preoccupies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'preoccupies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prep_peep_3186',
    name: 'prep vs peep',
    description: 'Commonly confused words: prep and peep',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprep\b/gi,
    message: 'Check: "prep" ("prep") vs "peep" ("peep"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'peep';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_presaged_presages_3187',
    name: 'presaged vs presages',
    description: 'Commonly confused words: presaged and presages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpresaged\b/gi,
    message: 'Check: "presaged" ("presaged") vs "presages" ("presages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'presages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_preserved_preserves_3188',
    name: 'preserved vs preserves',
    description: 'Commonly confused words: preserved and preserves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpreserved\b/gi,
    message: 'Check: "preserved" ("preserved") vs "preserves" ("preserves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'preserves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_presided_presides_3189',
    name: 'presided vs presides',
    description: 'Commonly confused words: presided and presides',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpresided\b/gi,
    message: 'Check: "presided" ("presided") vs "presides" ("presides"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'presides';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pres_ores_3190',
    name: 'pres vs ores',
    description: 'Commonly confused words: pres and ores',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpres\b/gi,
    message: 'Check: "pres" ("pres") vs "ores" ("ores"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ores';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pressured_pressures_3191',
    name: 'pressured vs pressures',
    description: 'Commonly confused words: pressured and pressures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpressured\b/gi,
    message: 'Check: "pressured" ("pressured") vs "pressures" ("pressures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pressures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pressurized_pressurizes_3192',
    name: 'pressurized vs pressurizes',
    description: 'Commonly confused words: pressurized and pressurizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpressurized\b/gi,
    message: 'Check: "pressurized" ("pressurized") vs "pressurizes" ("pressurizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pressurizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_presumed_presumes_3193',
    name: 'presumed vs presumes',
    description: 'Commonly confused words: presumed and presumes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpresumed\b/gi,
    message: 'Check: "presumed" ("presumed") vs "presumes" ("presumes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'presumes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pretest_pretext_3194',
    name: 'pretest vs pretext',
    description: 'Commonly confused words: pretest and pretext',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpretest\b/gi,
    message: 'Check: "pretest" ("pretest") vs "pretext" ("pretext"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pretext';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prettied_pretties_3195',
    name: 'prettied vs pretties',
    description: 'Commonly confused words: prettied and pretties',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprettied\b/gi,
    message: 'Check: "prettied" ("prettied") vs "pretties" ("pretties"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pretties';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_priced_prices_3196',
    name: 'priced vs prices',
    description: 'Commonly confused words: priced and prices',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpriced\b/gi,
    message: 'Check: "priced" ("priced") vs "prices" ("prices"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prices';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_priced_prided_3197',
    name: 'priced vs prided',
    description: 'Commonly confused words: priced and prided',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpriced\b/gi,
    message: 'Check: "priced" ("priced") vs "prided" ("prided"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prided';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pricing_priding_3198',
    name: 'pricing vs priding',
    description: 'Commonly confused words: pricing and priding',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpricing\b/gi,
    message: 'Check: "pricing" ("pricing") vs "priding" ("priding"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'priding';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prides_prudes_3199',
    name: 'prides vs prudes',
    description: 'Commonly confused words: prides and prudes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprides\b/gi,
    message: 'Check: "prides" ("prides") vs "prudes" ("prudes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prudes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_privileged_privileges_3200',
    name: 'privileged vs privileges',
    description: 'Commonly confused words: privileged and privileges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprivileged\b/gi,
    message: 'Check: "privileged" ("privileged") vs "privileges" ("privileges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'privileges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prized_prizes_3201',
    name: 'prized vs prizes',
    description: 'Commonly confused words: prized and prizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprized\b/gi,
    message: 'Check: "prized" ("prized") vs "prizes" ("prizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_probated_probates_3202',
    name: 'probated vs probates',
    description: 'Commonly confused words: probated and probates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprobated\b/gi,
    message: 'Check: "probated" ("probated") vs "probates" ("probates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'probates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_probed_probes_3203',
    name: 'probed vs probes',
    description: 'Commonly confused words: probed and probes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprobed\b/gi,
    message: 'Check: "probed" ("probed") vs "probes" ("probes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'probes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_probe_prone_3204',
    name: 'probe vs prone',
    description: 'Commonly confused words: probe and prone',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprobe\b/gi,
    message: 'Check: "probe" ("probe") vs "prone" ("prone"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prone';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_probe_prove_3205',
    name: 'probe vs prove',
    description: 'Commonly confused words: probe and prove',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprobe\b/gi,
    message: 'Check: "probe" ("probe") vs "prove" ("prove"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prove';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_probes_proves_3206',
    name: 'probes vs proves',
    description: 'Commonly confused words: probes and proves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprobes\b/gi,
    message: 'Check: "probes" ("probes") vs "proves" ("proves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'proves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_probing_proving_3207',
    name: 'probing vs proving',
    description: 'Commonly confused words: probing and proving',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprobing\b/gi,
    message: 'Check: "probing" ("probing") vs "proving" ("proving"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'proving';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_processed_processes_3208',
    name: 'processed vs processes',
    description: 'Commonly confused words: processed and processes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprocessed\b/gi,
    message: 'Check: "processed" ("processed") vs "processes" ("processes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'processes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_produced_produces_3209',
    name: 'produced vs produces',
    description: 'Commonly confused words: produced and produces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bproduced\b/gi,
    message: 'Check: "produced" ("produced") vs "produces" ("produces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'produces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prolapsed_prolapses_3210',
    name: 'prolapsed vs prolapses',
    description: 'Commonly confused words: prolapsed and prolapses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprolapsed\b/gi,
    message: 'Check: "prolapsed" ("prolapsed") vs "prolapses" ("prolapses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prolapses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_proliferated_proliferates_3211',
    name: 'proliferated vs proliferates',
    description: 'Commonly confused words: proliferated and proliferates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bproliferated\b/gi,
    message: 'Check: "proliferated" ("proliferated") vs "proliferates" ("proliferates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'proliferates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_promenaded_promenades_3212',
    name: 'promenaded vs promenades',
    description: 'Commonly confused words: promenaded and promenades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpromenaded\b/gi,
    message: 'Check: "promenaded" ("promenaded") vs "promenades" ("promenades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'promenades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_promised_promises_3213',
    name: 'promised vs promises',
    description: 'Commonly confused words: promised and promises',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpromised\b/gi,
    message: 'Check: "promised" ("promised") vs "promises" ("promises"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'promises';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_promoted_prompted_3214',
    name: 'promoted vs prompted',
    description: 'Commonly confused words: promoted and prompted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpromoted\b/gi,
    message: 'Check: "promoted" ("promoted") vs "prompted" ("prompted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prompted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_promoter_prompter_3215',
    name: 'promoter vs prompter',
    description: 'Commonly confused words: promoter and prompter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpromoter\b/gi,
    message: 'Check: "promoter" ("promoter") vs "prompter" ("prompter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prompter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prom_prim_3216',
    name: 'prom vs prim',
    description: 'Commonly confused words: prom and prim',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprom\b/gi,
    message: 'Check: "prom" ("prom") vs "prim" ("prim"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prim';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prom_pron_3217',
    name: 'prom vs pron',
    description: 'Commonly confused words: prom and pron',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprom\b/gi,
    message: 'Check: "prom" ("prom") vs "pron" ("pron"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pron';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_promulgated_promulgates_3218',
    name: 'promulgated vs promulgates',
    description: 'Commonly confused words: promulgated and promulgates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpromulgated\b/gi,
    message: 'Check: "promulgated" ("promulgated") vs "promulgates" ("promulgates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'promulgates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_propagated_propagates_3219',
    name: 'propagated vs propagates',
    description: 'Commonly confused words: propagated and propagates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpropagated\b/gi,
    message: 'Check: "propagated" ("propagated") vs "propagates" ("propagates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'propagates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_propertied_properties_3220',
    name: 'propertied vs properties',
    description: 'Commonly confused words: propertied and properties',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpropertied\b/gi,
    message: 'Check: "propertied" ("propertied") vs "properties" ("properties"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'properties';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prophesied_prophesies_3221',
    name: 'prophesied vs prophesies',
    description: 'Commonly confused words: prophesied and prophesies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprophesied\b/gi,
    message: 'Check: "prophesied" ("prophesied") vs "prophesies" ("prophesies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prophesies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_proscribed_proscribes_3222',
    name: 'proscribed vs proscribes',
    description: 'Commonly confused words: proscribed and proscribes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bproscribed\b/gi,
    message: 'Check: "proscribed" ("proscribed") vs "proscribes" ("proscribes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'proscribes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prosecuted_prosecutes_3223',
    name: 'prosecuted vs prosecutes',
    description: 'Commonly confused words: prosecuted and prosecutes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprosecuted\b/gi,
    message: 'Check: "prosecuted" ("prosecuted") vs "prosecutes" ("prosecutes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prosecutes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_prostituted_prostitutes_3224',
    name: 'prostituted vs prostitutes',
    description: 'Commonly confused words: prostituted and prostitutes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprostituted\b/gi,
    message: 'Check: "prostituted" ("prostituted") vs "prostitutes" ("prostitutes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prostitutes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_provided_provides_3225',
    name: 'provided vs provides',
    description: 'Commonly confused words: provided and provides',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bprovided\b/gi,
    message: 'Check: "provided" ("provided") vs "provides" ("provides"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'provides';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pruned_prunes_3226',
    name: 'pruned vs prunes',
    description: 'Commonly confused words: pruned and prunes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpruned\b/gi,
    message: 'Check: "pruned" ("pruned") vs "prunes" ("prunes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'prunes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_psyched_psyches_3227',
    name: 'psyched vs psyches',
    description: 'Commonly confused words: psyched and psyches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpsyched\b/gi,
    message: 'Check: "psyched" ("psyched") vs "psyches" ("psyches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'psyches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_publicized_publicizes_3228',
    name: 'publicized vs publicizes',
    description: 'Commonly confused words: publicized and publicizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpublicized\b/gi,
    message: 'Check: "publicized" ("publicized") vs "publicizes" ("publicizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'publicizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_puckers_pickers_3229',
    name: 'puckers vs pickers',
    description: 'Commonly confused words: puckers and pickers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpuckers\b/gi,
    message: 'Check: "puckers" ("puckers") vs "pickers" ("pickers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pickers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_puddled_puddles_3230',
    name: 'puddled vs puddles',
    description: 'Commonly confused words: puddled and puddles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpuddled\b/gi,
    message: 'Check: "puddled" ("puddled") vs "puddles" ("puddles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'puddles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_puddle_piddle_3231',
    name: 'puddle vs piddle',
    description: 'Commonly confused words: puddle and piddle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpuddle\b/gi,
    message: 'Check: "puddle" ("puddle") vs "piddle" ("piddle"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'piddle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_puked_piked_3232',
    name: 'puked vs piked',
    description: 'Commonly confused words: puked and piked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpuked\b/gi,
    message: 'Check: "puked" ("puked") vs "piked" ("piked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'piked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pukes_pikes_3233',
    name: 'pukes vs pikes',
    description: 'Commonly confused words: pukes and pikes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpukes\b/gi,
    message: 'Check: "pukes" ("pukes") vs "pikes" ("pikes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pikes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pullers_pullets_3234',
    name: 'pullers vs pullets',
    description: 'Commonly confused words: pullers and pullets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpullers\b/gi,
    message: 'Check: "pullers" ("pullers") vs "pullets" ("pullets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pullets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pullet_puller_3235',
    name: 'pullet vs puller',
    description: 'Commonly confused words: pullet and puller',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpullet\b/gi,
    message: 'Check: "pullet" ("pullet") vs "puller" ("puller"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'puller';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pullets_pulleys_3236',
    name: 'pullets vs pulleys',
    description: 'Commonly confused words: pullets and pulleys',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpullets\b/gi,
    message: 'Check: "pullets" ("pullets") vs "pulleys" ("pulleys"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pulleys';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pulley_pullet_3237',
    name: 'pulley vs pullet',
    description: 'Commonly confused words: pulley and pullet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpulley\b/gi,
    message: 'Check: "pulley" ("pulley") vs "pullet" ("pullet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pullet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pulsed_pulses_3238',
    name: 'pulsed vs pulses',
    description: 'Commonly confused words: pulsed and pulses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpulsed\b/gi,
    message: 'Check: "pulsed" ("pulsed") vs "pulses" ("pulses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pulses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_punctuated_punctuates_3239',
    name: 'punctuated vs punctuates',
    description: 'Commonly confused words: punctuated and punctuates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpunctuated\b/gi,
    message: 'Check: "punctuated" ("punctuated") vs "punctuates" ("punctuates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'punctuates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_punctured_punctures_3240',
    name: 'punctured vs punctures',
    description: 'Commonly confused words: punctured and punctures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpunctured\b/gi,
    message: 'Check: "punctured" ("punctured") vs "punctures" ("punctures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'punctures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_punt_pint_3241',
    name: 'punt vs pint',
    description: 'Commonly confused words: punt and pint',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpunt\b/gi,
    message: 'Check: "punt" ("punt") vs "pint" ("pint"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pint';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_puny_punt_3242',
    name: 'puny vs punt',
    description: 'Commonly confused words: puny and punt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpuny\b/gi,
    message: 'Check: "puny" ("puny") vs "punt" ("punt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'punt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pupa_pups_3243',
    name: 'pupa vs pups',
    description: 'Commonly confused words: pupa and pups',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpupa\b/gi,
    message: 'Check: "pupa" ("pupa") vs "pups" ("pups"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pups';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pureed_purees_3244',
    name: 'pureed vs purees',
    description: 'Commonly confused words: pureed and purees',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpureed\b/gi,
    message: 'Check: "pureed" ("pureed") vs "purees" ("purees"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'purees';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pureed_purred_3245',
    name: 'pureed vs purred',
    description: 'Commonly confused words: pureed and purred',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpureed\b/gi,
    message: 'Check: "pureed" ("pureed") vs "purred" ("purred"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'purred';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_purer_puree_3246',
    name: 'purer vs puree',
    description: 'Commonly confused words: purer and puree',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpurer\b/gi,
    message: 'Check: "purer" ("purer") vs "puree" ("puree"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'puree';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_purged_purges_3247',
    name: 'purged vs purges',
    description: 'Commonly confused words: purged and purges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpurged\b/gi,
    message: 'Check: "purged" ("purged") vs "purges" ("purges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'purges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_purified_purifies_3248',
    name: 'purified vs purifies',
    description: 'Commonly confused words: purified and purifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpurified\b/gi,
    message: 'Check: "purified" ("purified") vs "purifies" ("purifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'purifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_purses_pursed_3249',
    name: 'purses vs pursed',
    description: 'Commonly confused words: purses and pursed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpurses\b/gi,
    message: 'Check: "purses" ("purses") vs "pursed" ("pursed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pursed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_puss_piss_3250',
    name: 'puss vs piss',
    description: 'Commonly confused words: puss and piss',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpuss\b/gi,
    message: 'Check: "puss" ("puss") vs "piss" ("piss"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'piss';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_puts_outs_3251',
    name: 'puts vs outs',
    description: 'Commonly confused words: puts and outs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bputs\b/gi,
    message: 'Check: "puts" ("puts") vs "outs" ("outs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'outs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_puttee_putted_3252',
    name: 'puttee vs putted',
    description: 'Commonly confused words: puttee and putted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bputtee\b/gi,
    message: 'Check: "puttee" ("puttee") vs "putted" ("putted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'putted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_puttee_putter_3253',
    name: 'puttee vs putter',
    description: 'Commonly confused words: puttee and putter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bputtee\b/gi,
    message: 'Check: "puttee" ("puttee") vs "putter" ("putter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'putter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_putters_puttees_3254',
    name: 'putters vs puttees',
    description: 'Commonly confused words: putters and puttees',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bputters\b/gi,
    message: 'Check: "putters" ("putters") vs "puttees" ("puttees"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'puttees';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_puzzled_puzzles_3255',
    name: 'puzzled vs puzzles',
    description: 'Commonly confused words: puzzled and puzzles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpuzzled\b/gi,
    message: 'Check: "puzzled" ("puzzled") vs "puzzles" ("puzzles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'puzzles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_pwns_pens_3256',
    name: 'pwns vs pens',
    description: 'Commonly confused words: pwns and pens',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bpwns\b/gi,
    message: 'Check: "pwns" ("pwns") vs "pens" ("pens"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'pens';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
