/**
 * Auto-generated confusion rules (A)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.488Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesA: GrammarRule[] = [
  {
    id: 'confusion_able_bale_0',
    name: 'able vs bale',
    description: 'Commonly confused words: able and bale',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bable\b/gi,
    message: 'Check: "able" ("able") vs "bale" ("bale"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bale';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_abstruse_obtuse_1',
    name: 'abstruse vs obtuse',
    description: 'Commonly confused words: abstruse and obtuse',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\babstruse\b/gi,
    message: 'Check: "abstruse" ("abstruse") vs "obtuse" ("obtuse"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'obtuse';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_accede_exceed_2',
    name: 'accede vs exceed',
    description: 'Commonly confused words: accede and exceed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baccede\b/gi,
    message: 'Check: "accede" ("accede") vs "exceed" ("exceed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'exceed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_accept_except_3',
    name: 'accept vs except',
    description: 'Commonly confused words: accept and except',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baccept\b/gi,
    message: 'Did you mean "except"? "accept" (to receive or agree to) is often confused with "except" (excluding or other than).',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'except';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_acclamation_acclimation_5',
    name: 'acclamation vs acclimation',
    description: 'Commonly confused words: acclamation and acclimation',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bacclamation\b/gi,
    message: 'Check: "acclamation" ("acclamation") vs "acclimation" ("acclimation"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'acclimation';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_acts_ax_6',
    name: 'acts vs ax',
    description: 'Commonly confused words: acts and ax',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bacts\b/gi,
    message: 'Check: "acts" ("acts") vs "ax" ("ax"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ax';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_adapting_adopting_7',
    name: 'adapting vs adopting',
    description: 'Commonly confused words: adapting and adopting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\badapting\b/gi,
    message: 'Check: "adapting" ("adapting") vs "adopting" ("adopting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'adopting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_addition_edition_8',
    name: 'addition vs edition',
    description: 'Commonly confused words: addition and edition',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baddition\b/gi,
    message: 'Check: "addition" ("addition") vs "edition" ("edition"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'edition';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_additions_editions_9',
    name: 'additions vs editions',
    description: 'Commonly confused words: additions and editions',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\badditions\b/gi,
    message: 'Did you mean "editions"? "additions" ("additions") is often confused with "editions" ("editions").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'editions';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_adds_ads_11',
    name: 'adds vs ads',
    description: 'Commonly confused words: adds and ads',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\badds\b/gi,
    message: 'Check: "adds" ("adds") vs "ads" ("ads"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ads';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_adolescence_adolescents_12',
    name: 'adolescence vs adolescents',
    description: 'Commonly confused words: adolescence and adolescents',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\badolescence\b/gi,
    message: 'Check: "adolescence" ("adolescence") vs "adolescents" ("adolescents"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'adolescents';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_adverse_averse_13',
    name: 'adverse vs averse',
    description: 'Commonly confused words: adverse and averse',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\badverse\b/gi,
    message: 'Check: "adverse" ("adverse") vs "averse" ("averse"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'averse';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_advice_advise_14',
    name: 'advice vs advise',
    description: 'Commonly confused words: advice and advise',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\badvice\b/gi,
    message: 'Did you mean "advise"? "advice" (guidance (noun)) is often confused with "advise" (to give guidance (verb)).',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'advise';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_advise_advice_15',
    name: 'advise vs advice',
    description: 'Commonly confused words: advise and advice',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\badvise\b/gi,
    message: 'Did you mean "advice"? "advise" (to give guidance (verb)) is often confused with "advice" (guidance (noun)).',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'advice';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_affect_effect_16',
    name: 'affect vs effect',
    description: 'Commonly confused words: affect and effect',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baffect\b/gi,
    message: 'Did you mean "effect"? "affect" (to influence (verb)) is often confused with "effect" (a result (noun) or to cause (verb)).',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'effect';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_affects_effects_18',
    name: 'affects vs effects',
    description: 'Commonly confused words: affects and effects',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baffects\b/gi,
    message: 'Check: "affects" ("affects") vs "effects" ("effects"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'effects';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_affluence_effluence_19',
    name: 'affluence vs effluence',
    description: 'Commonly confused words: affluence and effluence',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baffluence\b/gi,
    message: 'Check: "affluence" ("affluence") vs "effluence" ("effluence"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'effluence';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_aisle_isle_20',
    name: 'aisle vs isle',
    description: 'Commonly confused words: aisle and isle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baisle\b/gi,
    message: 'Check: "aisle" ("aisle") vs "isle" ("isle"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'isle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_aisles_isles_21',
    name: 'aisles vs isles',
    description: 'Commonly confused words: aisles and isles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baisles\b/gi,
    message: 'Check: "aisles" ("aisles") vs "isles" ("isles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'isles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_alliterate_illiterate_22',
    name: 'alliterate vs illiterate',
    description: 'Commonly confused words: alliterate and illiterate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\balliterate\b/gi,
    message: 'Check: "alliterate" ("alliterate") vs "illiterate" ("illiterate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'illiterate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_allowed_aloud_23',
    name: 'allowed vs aloud',
    description: 'Commonly confused words: allowed and aloud',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\ballowed\b/gi,
    message: 'Check: "allowed" (permitted) vs "aloud" (audibly, with sound). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'aloud';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_allude_elude_24',
    name: 'allude vs elude',
    description: 'Commonly confused words: allude and elude',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\ballude\b/gi,
    message: 'Check: "allude" ("allude") vs "elude" ("elude"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'elude';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_alludes_eludes_25',
    name: 'alludes vs eludes',
    description: 'Commonly confused words: alludes and eludes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\balludes\b/gi,
    message: 'Check: "alludes" ("alludes") vs "eludes" ("eludes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eludes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_allusion_illusion_26',
    name: 'allusion vs illusion',
    description: 'Commonly confused words: allusion and illusion',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\ballusion\b/gi,
    message: 'Did you mean "illusion"? "allusion" ("allusion") is often confused with "illusion" ("illusion").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'illusion';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_allusions_illusions_28',
    name: 'allusions vs illusions',
    description: 'Commonly confused words: allusions and illusions',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\ballusions\b/gi,
    message: 'Did you mean "illusions"? "allusions" ("allusions") is often confused with "illusions" ("illusions").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'illusions';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_altar_alter_30',
    name: 'altar vs alter',
    description: 'Commonly confused words: altar and alter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baltar\b/gi,
    message: 'Check: "altar" ("altar") vs "alter" ("alter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'alter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_altars_alters_31',
    name: 'altars vs alters',
    description: 'Commonly confused words: altars and alters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baltars\b/gi,
    message: 'Check: "altars" ("altars") vs "alters" ("alters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'alters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_alumni_alumnus_32',
    name: 'alumni vs alumnus',
    description: 'Commonly confused words: alumni and alumnus',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\balumni\b/gi,
    message: 'Check: "alumni" ("alumni") vs "alumnus" ("alumnus"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'alumnus';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_and_end_33',
    name: 'and vs end',
    description: 'Commonly confused words: and and end',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\band\b/gi,
    message: 'Check: "and" ("and") vs "end" ("end"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'end';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_anecdote_antidote_34',
    name: 'anecdote vs antidote',
    description: 'Commonly confused words: anecdote and antidote',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\banecdote\b/gi,
    message: 'Check: "anecdote" ("anecdote") vs "antidote" ("antidote"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'antidote';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_anti_auntie_35',
    name: 'anti vs auntie',
    description: 'Commonly confused words: anti and auntie',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\banti\b/gi,
    message: 'Check: "anti" ("anti") vs "auntie" ("auntie"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'auntie';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_apatite_appetite_36',
    name: 'apatite vs appetite',
    description: 'Commonly confused words: apatite and appetite',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bapatite\b/gi,
    message: 'Check: "apatite" ("apatite") vs "appetite" ("appetite"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'appetite';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_appendix_appendices_37',
    name: 'appendix vs appendices',
    description: 'Commonly confused words: appendix and appendices',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bappendix\b/gi,
    message: 'Did you mean "appendices"? "appendix" ("appendix") is often confused with "appendices" ("appendices").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'appendices';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_appendices_appendix_38',
    name: 'appendices vs appendix',
    description: 'Commonly confused words: appendices and appendix',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bappendices\b/gi,
    message: 'Did you mean "appendix"? "appendices" ("appendices") is often confused with "appendix" ("appendix").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'appendix';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_appraise_apprise_39',
    name: 'appraise vs apprise',
    description: 'Commonly confused words: appraise and apprise',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bappraise\b/gi,
    message: 'Check: "appraise" ("appraise") vs "apprise" ("apprise"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'apprise';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_arrant_errant_40',
    name: 'arrant vs errant',
    description: 'Commonly confused words: arrant and errant',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\barrant\b/gi,
    message: 'Check: "arrant" ("arrant") vs "errant" ("errant"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'errant';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_artesian_artisanal_41',
    name: 'artesian vs artisanal',
    description: 'Commonly confused words: artesian and artisanal',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bartesian\b/gi,
    message: 'Check: "artesian" ("artesian") vs "artisanal" ("artisanal"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'artisanal';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_as_at_42',
    name: 'as vs at',
    description: 'Commonly confused words: as and at',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bas\b/gi,
    message: 'Check: "as" ("as") vs "at" ("at"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'at';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ascent_assent_43',
    name: 'ascent vs assent',
    description: 'Commonly confused words: ascent and assent',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bascent\b/gi,
    message: 'Check: "ascent" ("ascent") vs "assent" ("assent"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'assent';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_aspect_respect_44',
    name: 'aspect vs respect',
    description: 'Commonly confused words: aspect and respect',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baspect\b/gi,
    message: 'Check: "aspect" ("aspect") vs "respect" ("respect"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'respect';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_aspects_respects_45',
    name: 'aspects vs respects',
    description: 'Commonly confused words: aspects and respects',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baspects\b/gi,
    message: 'Did you mean "respects"? "aspects" ("aspects") is often confused with "respects" ("respects").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'respects';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ate_eight_48',
    name: 'ate vs eight',
    description: 'Commonly confused words: ate and eight',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bate\b/gi,
    message: 'Did you mean "eight"? "ate" ("ate") is often confused with "eight" ("eight").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eight';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_aural_oral_49',
    name: 'aural vs oral',
    description: 'Commonly confused words: aural and oral',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baural\b/gi,
    message: 'Check: "aural" ("aural") vs "oral" ("oral"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'oral';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_avid_avoid_50',
    name: 'avid vs avoid',
    description: 'Commonly confused words: avid and avoid',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bavid\b/gi,
    message: 'Check: "avid" ("avid") vs "avoid" ("avoid"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'avoid';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_aw_awe_51',
    name: 'aw vs awe',
    description: 'Commonly confused words: aw and awe',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baw\b/gi,
    message: 'Check: "aw" ("aw") vs "awe" ("awe"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'awe';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_awe_oar_52',
    name: 'awe vs oar',
    description: 'Commonly confused words: awe and oar',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bawe\b/gi,
    message: 'Check: "awe" ("awe") vs "oar" ("oar"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'oar';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_axes_axis_53',
    name: 'axes vs axis',
    description: 'Commonly confused words: axes and axis',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baxes\b/gi,
    message: 'Check: "axes" ("axes") vs "axis" ("axis"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'axis';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_abut_about_792',
    name: 'abut vs about',
    description: 'Commonly confused words: abut and about',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\babut\b/gi,
    message: 'Did you mean "about"? "abut" ("abut") is often confused with "about" ("about").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'about';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_abated_abates_793',
    name: 'abated vs abates',
    description: 'Commonly confused words: abated and abates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\babated\b/gi,
    message: 'Check: "abated" ("abated") vs "abates" ("abates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'abates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_abates_agates_794',
    name: 'abates vs agates',
    description: 'Commonly confused words: abates and agates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\babates\b/gi,
    message: 'Check: "abates" ("abates") vs "agates" ("agates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'agates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_abbreviated_abbreviates_795',
    name: 'abbreviated vs abbreviates',
    description: 'Commonly confused words: abbreviated and abbreviates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\babbreviated\b/gi,
    message: 'Check: "abbreviated" ("abbreviated") vs "abbreviates" ("abbreviates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'abbreviates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_abed_aged_796',
    name: 'abed vs aged',
    description: 'Commonly confused words: abed and aged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\babed\b/gi,
    message: 'Check: "abed" ("abed") vs "aged" ("aged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'aged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_abide_abode_797',
    name: 'abide vs abode',
    description: 'Commonly confused words: abide and abode',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\babide\b/gi,
    message: 'Check: "abide" ("abide") vs "abode" ("abode"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'abode';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_abides_abodes_798',
    name: 'abides vs abodes',
    description: 'Commonly confused words: abides and abodes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\babides\b/gi,
    message: 'Check: "abides" ("abides") vs "abodes" ("abodes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'abodes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_abode_anode_799',
    name: 'abode vs anode',
    description: 'Commonly confused words: abode and anode',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\babode\b/gi,
    message: 'Check: "abode" ("abode") vs "anode" ("anode"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'anode';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_abodes_anodes_800',
    name: 'abodes vs anodes',
    description: 'Commonly confused words: abodes and anodes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\babodes\b/gi,
    message: 'Check: "abodes" ("abodes") vs "anodes" ("anodes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'anodes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_abraded_abrades_801',
    name: 'abraded vs abrades',
    description: 'Commonly confused words: abraded and abrades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\babraded\b/gi,
    message: 'Check: "abraded" ("abraded") vs "abrades" ("abrades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'abrades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_abridged_abridges_802',
    name: 'abridged vs abridges',
    description: 'Commonly confused words: abridged and abridges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\babridged\b/gi,
    message: 'Check: "abridged" ("abridged") vs "abridges" ("abridges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'abridges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_abrogated_abrogates_803',
    name: 'abrogated vs abrogates',
    description: 'Commonly confused words: abrogated and abrogates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\babrogated\b/gi,
    message: 'Check: "abrogated" ("abrogated") vs "abrogates" ("abrogates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'abrogates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_abscessed_abscesses_804',
    name: 'abscessed vs abscesses',
    description: 'Commonly confused words: abscessed and abscesses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\babscessed\b/gi,
    message: 'Check: "abscessed" ("abscessed") vs "abscesses" ("abscesses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'abscesses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_absented_absentee_805',
    name: 'absented vs absentee',
    description: 'Commonly confused words: absented and absentee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\babsented\b/gi,
    message: 'Check: "absented" ("absented") vs "absentee" ("absentee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'absentee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_absolved_absolves_806',
    name: 'absolved vs absolves',
    description: 'Commonly confused words: absolved and absolves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\babsolved\b/gi,
    message: 'Check: "absolved" ("absolved") vs "absolves" ("absolves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'absolves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_accentuated_accentuates_807',
    name: 'accentuated vs accentuates',
    description: 'Commonly confused words: accentuated and accentuates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baccentuated\b/gi,
    message: 'Check: "accentuated" ("accentuated") vs "accentuates" ("accentuates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'accentuates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_accommodated_accommodates_808',
    name: 'accommodated vs accommodates',
    description: 'Commonly confused words: accommodated and accommodates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baccommodated\b/gi,
    message: 'Check: "accommodated" ("accommodated") vs "accommodates" ("accommodates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'accommodates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_accrued_accrues_809',
    name: 'accrued vs accrues',
    description: 'Commonly confused words: accrued and accrues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baccrued\b/gi,
    message: 'Check: "accrued" ("accrued") vs "accrues" ("accrues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'accrues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ache_acne_810',
    name: 'ache vs acne',
    description: 'Commonly confused words: ache and acne',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bache\b/gi,
    message: 'Check: "ache" ("ache") vs "acne" ("acne"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'acne';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ached_aches_811',
    name: 'ached vs aches',
    description: 'Commonly confused words: ached and aches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bached\b/gi,
    message: 'Check: "ached" ("ached") vs "aches" ("aches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'aches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_acidified_acidifies_812',
    name: 'acidified vs acidifies',
    description: 'Commonly confused words: acidified and acidifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bacidified\b/gi,
    message: 'Check: "acidified" ("acidified") vs "acidifies" ("acidifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'acidifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_acidity_avidity_813',
    name: 'acidity vs avidity',
    description: 'Commonly confused words: acidity and avidity',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bacidity\b/gi,
    message: 'Check: "acidity" ("acidity") vs "avidity" ("avidity"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'avidity';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_acidly_avidly_814',
    name: 'acidly vs avidly',
    description: 'Commonly confused words: acidly and avidly',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bacidly\b/gi,
    message: 'Check: "acidly" ("acidly") vs "avidly" ("avidly"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'avidly';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_acknowledged_acknowledges_815',
    name: 'acknowledged vs acknowledges',
    description: 'Commonly confused words: acknowledged and acknowledges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\backnowledged\b/gi,
    message: 'Check: "acknowledged" ("acknowledged") vs "acknowledges" ("acknowledges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'acknowledges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_acme_acne_816',
    name: 'acme vs acne',
    description: 'Commonly confused words: acme and acne',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bacme\b/gi,
    message: 'Check: "acme" ("acme") vs "acne" ("acne"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'acne';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_acorn_adorn_817',
    name: 'acorn vs adorn',
    description: 'Commonly confused words: acorn and adorn',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bacorn\b/gi,
    message: 'Check: "acorn" ("acorn") vs "adorn" ("adorn"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'adorn';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_acorns_adorns_818',
    name: 'acorns vs adorns',
    description: 'Commonly confused words: acorns and adorns',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bacorns\b/gi,
    message: 'Check: "acorns" ("acorns") vs "adorns" ("adorns"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'adorns';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_acorn_scorn_819',
    name: 'acorn vs scorn',
    description: 'Commonly confused words: acorn and scorn',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bacorn\b/gi,
    message: 'Check: "acorn" ("acorn") vs "scorn" ("scorn"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scorn';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_acorns_scorns_820',
    name: 'acorns vs scorns',
    description: 'Commonly confused words: acorns and scorns',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bacorns\b/gi,
    message: 'Check: "acorns" ("acorns") vs "scorns" ("scorns"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scorns';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_acquiesced_acquiesces_821',
    name: 'acquiesced vs acquiesces',
    description: 'Commonly confused words: acquiesced and acquiesces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bacquiesced\b/gi,
    message: 'Check: "acquiesced" ("acquiesced") vs "acquiesces" ("acquiesces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'acquiesces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_actualized_actualizes_822',
    name: 'actualized vs actualizes',
    description: 'Commonly confused words: actualized and actualizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bactualized\b/gi,
    message: 'Check: "actualized" ("actualized") vs "actualizes" ("actualizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'actualizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_actuated_actuates_823',
    name: 'actuated vs actuates',
    description: 'Commonly confused words: actuated and actuates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bactuated\b/gi,
    message: 'Check: "actuated" ("actuated") vs "actuates" ("actuates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'actuates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_addends_addenda_824',
    name: 'addends vs addenda',
    description: 'Commonly confused words: addends and addenda',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baddends\b/gi,
    message: 'Check: "addends" ("addends") vs "addenda" ("addenda"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'addenda';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_addressed_addressee_825',
    name: 'addressed vs addressee',
    description: 'Commonly confused words: addressed and addressee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baddressed\b/gi,
    message: 'Check: "addressed" ("addressed") vs "addressee" ("addressee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'addressee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_addressed_addresses_826',
    name: 'addressed vs addresses',
    description: 'Commonly confused words: addressed and addresses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baddressed\b/gi,
    message: 'Check: "addressed" ("addressed") vs "addresses" ("addresses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'addresses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_adduced_adduces_827',
    name: 'adduced vs adduces',
    description: 'Commonly confused words: adduced and adduces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\badduced\b/gi,
    message: 'Check: "adduced" ("adduced") vs "adduces" ("adduces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'adduces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_adhered_adheres_828',
    name: 'adhered vs adheres',
    description: 'Commonly confused words: adhered and adheres',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\badhered\b/gi,
    message: 'Check: "adhered" ("adhered") vs "adheres" ("adheres"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'adheres';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_adjudicated_adjudicates_829',
    name: 'adjudicated vs adjudicates',
    description: 'Commonly confused words: adjudicated and adjudicates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\badjudicated\b/gi,
    message: 'Check: "adjudicated" ("adjudicated") vs "adjudicates" ("adjudicates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'adjudicates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_administrated_administrates_830',
    name: 'administrated vs administrates',
    description: 'Commonly confused words: administrated and administrates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\badministrated\b/gi,
    message: 'Check: "administrated" ("administrated") vs "administrates" ("administrates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'administrates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_advantaged_advantages_831',
    name: 'advantaged vs advantages',
    description: 'Commonly confused words: advantaged and advantages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\badvantaged\b/gi,
    message: 'Check: "advantaged" ("advantaged") vs "advantages" ("advantages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'advantages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_adventured_adventures_832',
    name: 'adventured vs adventures',
    description: 'Commonly confused words: adventured and adventures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\badventured\b/gi,
    message: 'Check: "adventured" ("adventured") vs "adventures" ("adventures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'adventures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_advocated_advocates_833',
    name: 'advocated vs advocates',
    description: 'Commonly confused words: advocated and advocates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\badvocated\b/gi,
    message: 'Check: "advocated" ("advocated") vs "advocates" ("advocates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'advocates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_aerated_aerates_834',
    name: 'aerated vs aerates',
    description: 'Commonly confused words: aerated and aerates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baerated\b/gi,
    message: 'Check: "aerated" ("aerated") vs "aerates" ("aerates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'aerates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_aerially_serially_835',
    name: 'aerially vs serially',
    description: 'Commonly confused words: aerially and serially',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baerially\b/gi,
    message: 'Check: "aerially" ("aerially") vs "serially" ("serially"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'serially';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_aerial_serial_836',
    name: 'aerial vs serial',
    description: 'Commonly confused words: aerial and serial',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baerial\b/gi,
    message: 'Check: "aerial" ("aerial") vs "serial" ("serial"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'serial';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_afar_agar_837',
    name: 'afar vs agar',
    description: 'Commonly confused words: afar and agar',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bafar\b/gi,
    message: 'Check: "afar" ("afar") vs "agar" ("agar"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'agar';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_affiliated_affiliates_838',
    name: 'affiliated vs affiliates',
    description: 'Commonly confused words: affiliated and affiliates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baffiliated\b/gi,
    message: 'Check: "affiliated" ("affiliated") vs "affiliates" ("affiliates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'affiliates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_affixed_affixes_839',
    name: 'affixed vs affixes',
    description: 'Commonly confused words: affixed and affixes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baffixed\b/gi,
    message: 'Check: "affixed" ("affixed") vs "affixes" ("affixes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'affixes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_aged_ages_840',
    name: 'aged vs ages',
    description: 'Commonly confused words: aged and ages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baged\b/gi,
    message: 'Check: "aged" ("aged") vs "ages" ("ages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_agglomerated_agglomerates_841',
    name: 'agglomerated vs agglomerates',
    description: 'Commonly confused words: agglomerated and agglomerates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bagglomerated\b/gi,
    message: 'Check: "agglomerated" ("agglomerated") vs "agglomerates" ("agglomerates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'agglomerates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_agglutinated_agglutinates_842',
    name: 'agglutinated vs agglutinates',
    description: 'Commonly confused words: agglutinated and agglutinates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bagglutinated\b/gi,
    message: 'Check: "agglutinated" ("agglutinated") vs "agglutinates" ("agglutinates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'agglutinates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_aggravated_aggravates_843',
    name: 'aggravated vs aggravates',
    description: 'Commonly confused words: aggravated and aggravates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baggravated\b/gi,
    message: 'Check: "aggravated" ("aggravated") vs "aggravates" ("aggravates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'aggravates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_agitated_agitates_844',
    name: 'agitated vs agitates',
    description: 'Commonly confused words: agitated and agitates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bagitated\b/gi,
    message: 'Check: "agitated" ("agitated") vs "agitates" ("agitates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'agitates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_agonized_agonizes_845',
    name: 'agonized vs agonizes',
    description: 'Commonly confused words: agonized and agonizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bagonized\b/gi,
    message: 'Check: "agonized" ("agonized") vs "agonizes" ("agonizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'agonizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_airbrushed_airbrushes_846',
    name: 'airbrushed vs airbrushes',
    description: 'Commonly confused words: airbrushed and airbrushes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bairbrushed\b/gi,
    message: 'Check: "airbrushed" ("airbrushed") vs "airbrushes" ("airbrushes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'airbrushes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_akin_skin_847',
    name: 'akin vs skin',
    description: 'Commonly confused words: akin and skin',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bakin\b/gi,
    message: 'Check: "akin" ("akin") vs "skin" ("skin"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'skin';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_alack_slack_848',
    name: 'alack vs slack',
    description: 'Commonly confused words: alack and slack',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\balack\b/gi,
    message: 'Check: "alack" ("alack") vs "slack" ("slack"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slack';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_aliased_aliases_849',
    name: 'aliased vs aliases',
    description: 'Commonly confused words: aliased and aliases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baliased\b/gi,
    message: 'Check: "aliased" ("aliased") vs "aliases" ("aliases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'aliases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_alighted_slighted_850',
    name: 'alighted vs slighted',
    description: 'Commonly confused words: alighted and slighted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\balighted\b/gi,
    message: 'Check: "alighted" ("alighted") vs "slighted" ("slighted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slighted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_alighting_slighting_851',
    name: 'alighting vs slighting',
    description: 'Commonly confused words: alighting and slighting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\balighting\b/gi,
    message: 'Check: "alighting" ("alighting") vs "slighting" ("slighting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slighting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_alights_slights_852',
    name: 'alights vs slights',
    description: 'Commonly confused words: alights and slights',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\balights\b/gi,
    message: 'Check: "alights" ("alights") vs "slights" ("slights"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slights';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_alleviated_alleviates_853',
    name: 'alleviated vs alleviates',
    description: 'Commonly confused words: alleviated and alleviates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\balleviated\b/gi,
    message: 'Check: "alleviated" ("alleviated") vs "alleviates" ("alleviates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'alleviates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_allied_allies_854',
    name: 'allied vs allies',
    description: 'Commonly confused words: allied and allies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\ballied\b/gi,
    message: 'Check: "allied" ("allied") vs "allies" ("allies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'allies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_allot_alloy_855',
    name: 'allot vs alloy',
    description: 'Commonly confused words: allot and alloy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\ballot\b/gi,
    message: 'Check: "allot" ("allot") vs "alloy" ("alloy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'alloy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_allots_alloys_856',
    name: 'allots vs alloys',
    description: 'Commonly confused words: allots and alloys',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\ballots\b/gi,
    message: 'Check: "allots" ("allots") vs "alloys" ("alloys"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'alloys';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_alluded_alludes_857',
    name: 'alluded vs alludes',
    description: 'Commonly confused words: alluded and alludes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\balluded\b/gi,
    message: 'Check: "alluded" ("alluded") vs "alludes" ("alludes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'alludes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_allured_allures_858',
    name: 'allured vs allures',
    description: 'Commonly confused words: allured and allures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\ballured\b/gi,
    message: 'Check: "allured" ("allured") vs "allures" ("allures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'allures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_alphabetized_alphabetizes_859',
    name: 'alphabetized vs alphabetizes',
    description: 'Commonly confused words: alphabetized and alphabetizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\balphabetized\b/gi,
    message: 'Check: "alphabetized" ("alphabetized") vs "alphabetizes" ("alphabetizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'alphabetizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_alum_slum_860',
    name: 'alum vs slum',
    description: 'Commonly confused words: alum and slum',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\balum\b/gi,
    message: 'Check: "alum" ("alum") vs "slum" ("slum"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'slum';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_amazed_amazes_861',
    name: 'amazed vs amazes',
    description: 'Commonly confused words: amazed and amazes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bamazed\b/gi,
    message: 'Check: "amazed" ("amazed") vs "amazes" ("amazes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'amazes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ameliorated_ameliorates_862',
    name: 'ameliorated vs ameliorates',
    description: 'Commonly confused words: ameliorated and ameliorates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bameliorated\b/gi,
    message: 'Check: "ameliorated" ("ameliorated") vs "ameliorates" ("ameliorates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ameliorates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_amnestied_amnesties_863',
    name: 'amnestied vs amnesties',
    description: 'Commonly confused words: amnestied and amnesties',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bamnestied\b/gi,
    message: 'Check: "amnestied" ("amnestied") vs "amnesties" ("amnesties"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'amnesties';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_amplified_amplifies_864',
    name: 'amplified vs amplifies',
    description: 'Commonly confused words: amplified and amplifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bamplified\b/gi,
    message: 'Check: "amplified" ("amplified") vs "amplifies" ("amplifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'amplifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_amputated_amputates_865',
    name: 'amputated vs amputates',
    description: 'Commonly confused words: amputated and amputates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bamputated\b/gi,
    message: 'Check: "amputated" ("amputated") vs "amputates" ("amputates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'amputates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_amused_amuses_866',
    name: 'amused vs amuses',
    description: 'Commonly confused words: amused and amuses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bamused\b/gi,
    message: 'Check: "amused" ("amused") vs "amuses" ("amuses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'amuses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_amuses_anuses_867',
    name: 'amuses vs anuses',
    description: 'Commonly confused words: amuses and anuses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bamuses\b/gi,
    message: 'Check: "amuses" ("amuses") vs "anuses" ("anuses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'anuses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_analogized_analogizes_868',
    name: 'analogized vs analogizes',
    description: 'Commonly confused words: analogized and analogizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\banalogized\b/gi,
    message: 'Check: "analogized" ("analogized") vs "analogizes" ("analogizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'analogizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_angled_angles_869',
    name: 'angled vs angles',
    description: 'Commonly confused words: angled and angles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bangled\b/gi,
    message: 'Check: "angled" ("angled") vs "angles" ("angles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'angles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_anglers_antlers_870',
    name: 'anglers vs antlers',
    description: 'Commonly confused words: anglers and antlers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\banglers\b/gi,
    message: 'Check: "anglers" ("anglers") vs "antlers" ("antlers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'antlers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_anguished_anguishes_871',
    name: 'anguished vs anguishes',
    description: 'Commonly confused words: anguished and anguishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\banguished\b/gi,
    message: 'Check: "anguished" ("anguished") vs "anguishes" ("anguishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'anguishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_annihilated_annihilates_872',
    name: 'annihilated vs annihilates',
    description: 'Commonly confused words: annihilated and annihilates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bannihilated\b/gi,
    message: 'Check: "annihilated" ("annihilated") vs "annihilates" ("annihilates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'annihilates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_antedated_antedates_873',
    name: 'antedated vs antedates',
    description: 'Commonly confused words: antedated and antedates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bantedated\b/gi,
    message: 'Check: "antedated" ("antedated") vs "antedates" ("antedates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'antedates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_antipasti_antipasto_874',
    name: 'antipasti vs antipasto',
    description: 'Commonly confused words: antipasti and antipasto',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bantipasti\b/gi,
    message: 'Check: "antipasti" ("antipasti") vs "antipasto" ("antipasto"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'antipasto';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_antiqued_antiques_875',
    name: 'antiqued vs antiques',
    description: 'Commonly confused words: antiqued and antiques',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bantiqued\b/gi,
    message: 'Check: "antiqued" ("antiqued") vs "antiques" ("antiques"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'antiques';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_aorta_sorta_876',
    name: 'aorta vs sorta',
    description: 'Commonly confused words: aorta and sorta',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baorta\b/gi,
    message: 'Check: "aorta" ("aorta") vs "sorta" ("sorta"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sorta';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_aped_apes_877',
    name: 'aped vs apes',
    description: 'Commonly confused words: aped and apes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baped\b/gi,
    message: 'Check: "aped" ("aped") vs "apes" ("apes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'apes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_apes_apex_878',
    name: 'apes vs apex',
    description: 'Commonly confused words: apes and apex',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bapes\b/gi,
    message: 'Check: "apes" ("apes") vs "apex" ("apex"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'apex';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_appeased_appeases_879',
    name: 'appeased vs appeases',
    description: 'Commonly confused words: appeased and appeases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bappeased\b/gi,
    message: 'Check: "appeased" ("appeased") vs "appeases" ("appeases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'appeases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_applied_applies_880',
    name: 'applied vs applies',
    description: 'Commonly confused words: applied and applies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bapplied\b/gi,
    message: 'Check: "applied" ("applied") vs "applies" ("applies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'applies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_appliqued_appliques_881',
    name: 'appliqued vs appliques',
    description: 'Commonly confused words: appliqued and appliques',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bappliqued\b/gi,
    message: 'Check: "appliqued" ("appliqued") vs "appliques" ("appliques"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'appliques';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_appraised_appraises_882',
    name: 'appraised vs appraises',
    description: 'Commonly confused words: appraised and appraises',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bappraised\b/gi,
    message: 'Check: "appraised" ("appraised") vs "appraises" ("appraises"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'appraises';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_apprenticed_apprentices_883',
    name: 'apprenticed vs apprentices',
    description: 'Commonly confused words: apprenticed and apprentices',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bapprenticed\b/gi,
    message: 'Check: "apprenticed" ("apprenticed") vs "apprentices" ("apprentices"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'apprentices';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_approached_approaches_884',
    name: 'approached vs approaches',
    description: 'Commonly confused words: approached and approaches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bapproached\b/gi,
    message: 'Check: "approached" ("approached") vs "approaches" ("approaches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'approaches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_approximated_approximates_885',
    name: 'approximated vs approximates',
    description: 'Commonly confused words: approximated and approximates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bapproximated\b/gi,
    message: 'Check: "approximated" ("approximated") vs "approximates" ("approximates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'approximates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_arbitrage_arbitrate_886',
    name: 'arbitrage vs arbitrate',
    description: 'Commonly confused words: arbitrage and arbitrate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\barbitrage\b/gi,
    message: 'Check: "arbitrage" ("arbitrage") vs "arbitrate" ("arbitrate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'arbitrate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_arbitrages_arbitrates_887',
    name: 'arbitrages vs arbitrates',
    description: 'Commonly confused words: arbitrages and arbitrates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\barbitrages\b/gi,
    message: 'Check: "arbitrages" ("arbitrages") vs "arbitrates" ("arbitrates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'arbitrates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_arbitrated_arbitrates_888',
    name: 'arbitrated vs arbitrates',
    description: 'Commonly confused words: arbitrated and arbitrates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\barbitrated\b/gi,
    message: 'Check: "arbitrated" ("arbitrated") vs "arbitrates" ("arbitrates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'arbitrates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_arched_arches_889',
    name: 'arched vs arches',
    description: 'Commonly confused words: arched and arches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\barched\b/gi,
    message: 'Check: "arched" ("arched") vs "arches" ("arches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'arches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_areas_arras_890',
    name: 'areas vs arras',
    description: 'Commonly confused words: areas and arras',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bareas\b/gi,
    message: 'Check: "areas" ("areas") vs "arras" ("arras"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'arras';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_argued_argues_891',
    name: 'argued vs argues',
    description: 'Commonly confused words: argued and argues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bargued\b/gi,
    message: 'Check: "argued" ("argued") vs "argues" ("argues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'argues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_arise_arose_892',
    name: 'arise vs arose',
    description: 'Commonly confused words: arise and arose',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\barise\b/gi,
    message: 'Check: "arise" ("arise") vs "arose" ("arose"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'arose';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_articulated_articulates_893',
    name: 'articulated vs articulates',
    description: 'Commonly confused words: articulated and articulates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\barticulated\b/gi,
    message: 'Check: "articulated" ("articulated") vs "articulates" ("articulates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'articulates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ashed_ashes_894',
    name: 'ashed vs ashes',
    description: 'Commonly confused words: ashed and ashes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bashed\b/gi,
    message: 'Check: "ashed" ("ashed") vs "ashes" ("ashes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ashes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_asphyxiated_asphyxiates_895',
    name: 'asphyxiated vs asphyxiates',
    description: 'Commonly confused words: asphyxiated and asphyxiates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\basphyxiated\b/gi,
    message: 'Check: "asphyxiated" ("asphyxiated") vs "asphyxiates" ("asphyxiates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'asphyxiates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_aspirated_aspirates_896',
    name: 'aspirated vs aspirates',
    description: 'Commonly confused words: aspirated and aspirates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baspirated\b/gi,
    message: 'Check: "aspirated" ("aspirated") vs "aspirates" ("aspirates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'aspirates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_assigned_assignee_897',
    name: 'assigned vs assignee',
    description: 'Commonly confused words: assigned and assignee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bassigned\b/gi,
    message: 'Check: "assigned" ("assigned") vs "assignee" ("assignee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'assignee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_associated_associates_898',
    name: 'associated vs associates',
    description: 'Commonly confused words: associated and associates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bassociated\b/gi,
    message: 'Check: "associated" ("associated") vs "associates" ("associates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'associates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_assuaged_assuages_899',
    name: 'assuaged vs assuages',
    description: 'Commonly confused words: assuaged and assuages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bassuaged\b/gi,
    message: 'Check: "assuaged" ("assuaged") vs "assuages" ("assuages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'assuages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_astonished_astonishes_900',
    name: 'astonished vs astonishes',
    description: 'Commonly confused words: astonished and astonishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bastonished\b/gi,
    message: 'Check: "astonished" ("astonished") vs "astonishes" ("astonishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'astonishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_atoned_atones_901',
    name: 'atoned vs atones',
    description: 'Commonly confused words: atoned and atones',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\batoned\b/gi,
    message: 'Check: "atoned" ("atoned") vs "atones" ("atones"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'atones';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_atoned_stoned_902',
    name: 'atoned vs stoned',
    description: 'Commonly confused words: atoned and stoned',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\batoned\b/gi,
    message: 'Check: "atoned" ("atoned") vs "stoned" ("stoned"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stoned';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_atones_stones_903',
    name: 'atones vs stones',
    description: 'Commonly confused words: atones and stones',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\batones\b/gi,
    message: 'Check: "atones" ("atones") vs "stones" ("stones"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stones';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_atoning_stoning_904',
    name: 'atoning vs stoning',
    description: 'Commonly confused words: atoning and stoning',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\batoning\b/gi,
    message: 'Check: "atoning" ("atoning") vs "stoning" ("stoning"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stoning';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_atop_stop_905',
    name: 'atop vs stop',
    description: 'Commonly confused words: atop and stop',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\batop\b/gi,
    message: 'Check: "atop" ("atop") vs "stop" ("stop"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stop';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_atria_stria_906',
    name: 'atria vs stria',
    description: 'Commonly confused words: atria and stria',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\batria\b/gi,
    message: 'Check: "atria" ("atria") vs "stria" ("stria"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'stria';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_atrophied_atrophies_907',
    name: 'atrophied vs atrophies',
    description: 'Commonly confused words: atrophied and atrophies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\batrophied\b/gi,
    message: 'Check: "atrophied" ("atrophied") vs "atrophies" ("atrophies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'atrophies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_attenuated_attenuates_908',
    name: 'attenuated vs attenuates',
    description: 'Commonly confused words: attenuated and attenuates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\battenuated\b/gi,
    message: 'Check: "attenuated" ("attenuated") vs "attenuates" ("attenuates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'attenuates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_attributed_attributes_909',
    name: 'attributed vs attributes',
    description: 'Commonly confused words: attributed and attributes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\battributed\b/gi,
    message: 'Check: "attributed" ("attributed") vs "attributes" ("attributes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'attributes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_auctioned_suctioned_910',
    name: 'auctioned vs suctioned',
    description: 'Commonly confused words: auctioned and suctioned',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bauctioned\b/gi,
    message: 'Check: "auctioned" ("auctioned") vs "suctioned" ("suctioned"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suctioned';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_auction_suction_911',
    name: 'auction vs suction',
    description: 'Commonly confused words: auction and suction',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bauction\b/gi,
    message: 'Check: "auction" ("auction") vs "suction" ("suction"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suction';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_auspiciously_suspiciously_912',
    name: 'auspiciously vs suspiciously',
    description: 'Commonly confused words: auspiciously and suspiciously',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bauspiciously\b/gi,
    message: 'Check: "auspiciously" ("auspiciously") vs "suspiciously" ("suspiciously"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suspiciously';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_auspicious_suspicious_913',
    name: 'auspicious vs suspicious',
    description: 'Commonly confused words: auspicious and suspicious',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bauspicious\b/gi,
    message: 'Check: "auspicious" ("auspicious") vs "suspicious" ("suspicious"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suspicious';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_authenticated_authenticates_914',
    name: 'authenticated vs authenticates',
    description: 'Commonly confused words: authenticated and authenticates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bauthenticated\b/gi,
    message: 'Check: "authenticated" ("authenticated") vs "authenticates" ("authenticates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'authenticates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_autopsied_autopsies_915',
    name: 'autopsied vs autopsies',
    description: 'Commonly confused words: autopsied and autopsies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bautopsied\b/gi,
    message: 'Check: "autopsied" ("autopsied") vs "autopsies" ("autopsies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'autopsies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_averred_averted_916',
    name: 'averred vs averted',
    description: 'Commonly confused words: averred and averted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baverred\b/gi,
    message: 'Check: "averred" ("averred") vs "averted" ("averted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'averted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_averring_averting_917',
    name: 'averring vs averting',
    description: 'Commonly confused words: averring and averting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baverring\b/gi,
    message: 'Check: "averring" ("averring") vs "averting" ("averting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'averting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_award_aware_918',
    name: 'award vs aware',
    description: 'Commonly confused words: award and aware',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baward\b/gi,
    message: 'Check: "award" ("award") vs "aware" ("aware"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'aware';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_awards_swards_919',
    name: 'awards vs swards',
    description: 'Commonly confused words: awards and swards',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bawards\b/gi,
    message: 'Check: "awards" ("awards") vs "swards" ("swards"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'swards';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_awash_swash_920',
    name: 'awash vs swash',
    description: 'Commonly confused words: awash and swash',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bawash\b/gi,
    message: 'Check: "awash" ("awash") vs "swash" ("swash"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'swash';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_awed_awes_921',
    name: 'awed vs awes',
    description: 'Commonly confused words: awed and awes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bawed\b/gi,
    message: 'Check: "awed" ("awed") vs "awes" ("awes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'awes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_axed_aced_922',
    name: 'axed vs aced',
    description: 'Commonly confused words: axed and aced',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baxed\b/gi,
    message: 'Check: "axed" ("axed") vs "aced" ("aced"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'aced';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_aye_ate_923',
    name: 'aye vs ate',
    description: 'Commonly confused words: aye and ate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\baye\b/gi,
    message: 'Check: "aye" ("aye") vs "ate" ("ate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
