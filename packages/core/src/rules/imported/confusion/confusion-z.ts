/**
 * Auto-generated confusion rules (Z)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.528Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesZ: GrammarRule[] = [
  {
    id: 'confusion_zeroed_zeroes_4350',
    name: 'zeroed vs zeroes',
    description: 'Commonly confused words: zeroed and zeroes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bzeroed\b/gi,
    message: 'Check: "zeroed" ("zeroed") vs "zeroes" ("zeroes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'zeroes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_zoned_zones_4351',
    name: 'zoned vs zones',
    description: 'Commonly confused words: zoned and zones',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bzoned\b/gi,
    message: 'Check: "zoned" ("zoned") vs "zones" ("zones"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'zones';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
