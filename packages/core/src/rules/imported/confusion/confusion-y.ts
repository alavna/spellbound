/**
 * Auto-generated confusion rules (Y)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.526Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesY: GrammarRule[] = [
  {
    id: 'confusion_yoke_yolk_759',
    name: 'yoke vs yolk',
    description: 'Commonly confused words: yoke and yolk',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byoke\b/gi,
    message: 'Check: "yoke" ("yoke") vs "yolk" ("yolk"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'yolk';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_you_your_760',
    name: 'you vs your',
    description: 'Commonly confused words: you and your',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byou\b/gi,
    message: 'Did you mean "your"? "you" ("you") is often confused with "your" (belonging to you).',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'your';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_your_you_761',
    name: 'your vs you',
    description: 'Commonly confused words: your and you',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byour\b/gi,
    message: 'Did you mean "you"? "your" (belonging to you) is often confused with "you" ("you").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'you';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_your_sour_768',
    name: 'your vs sour',
    description: 'Commonly confused words: your and sour',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byour\b/gi,
    message: 'Did you mean "sour"? "your" (belonging to you) is often confused with "sour" ("sour").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sour';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yams_tams_4330',
    name: 'yams vs tams',
    description: 'Commonly confused words: yams and tams',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byams\b/gi,
    message: 'Check: "yams" ("yams") vs "tams" ("tams"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tams';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yapping_tapping_4331',
    name: 'yapping vs tapping',
    description: 'Commonly confused words: yapping and tapping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byapping\b/gi,
    message: 'Check: "yapping" ("yapping") vs "tapping" ("tapping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tapping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yard_hard_4332',
    name: 'yard vs hard',
    description: 'Commonly confused words: yard and hard',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byard\b/gi,
    message: 'Check: "yard" ("yard") vs "hard" ("hard"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hard';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yarn_tarn_4333',
    name: 'yarn vs tarn',
    description: 'Commonly confused words: yarn and tarn',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byarn\b/gi,
    message: 'Check: "yarn" ("yarn") vs "tarn" ("tarn"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tarn';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yawed_hawed_4334',
    name: 'yawed vs hawed',
    description: 'Commonly confused words: yawed and hawed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byawed\b/gi,
    message: 'Check: "yawed" ("yawed") vs "hawed" ("hawed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hawed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yawing_hawing_4335',
    name: 'yawing vs hawing',
    description: 'Commonly confused words: yawing and hawing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byawing\b/gi,
    message: 'Check: "yawing" ("yawing") vs "hawing" ("hawing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hawing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_year_hear_4336',
    name: 'year vs hear',
    description: 'Commonly confused words: year and hear',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byear\b/gi,
    message: 'Check: "year" ("year") vs "hear" (to perceive sound). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hear';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yearling_yearlong_4337',
    name: 'yearling vs yearlong',
    description: 'Commonly confused words: yearling and yearlong',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byearling\b/gi,
    message: 'Check: "yearling" ("yearling") vs "yearlong" ("yearlong"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'yearlong';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yeas_teas_4338',
    name: 'yeas vs teas',
    description: 'Commonly confused words: yeas and teas',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byeas\b/gi,
    message: 'Check: "yeas" ("yeas") vs "teas" ("teas"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'teas';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yens_hens_4339',
    name: 'yens vs hens',
    description: 'Commonly confused words: yens and hens',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byens\b/gi,
    message: 'Check: "yens" ("yens") vs "hens" ("hens"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hens';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yews_hews_4340',
    name: 'yews vs hews',
    description: 'Commonly confused words: yews and hews',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byews\b/gi,
    message: 'Check: "yews" ("yews") vs "hews" ("hews"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hews';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yid_hid_4341',
    name: 'yid vs hid',
    description: 'Commonly confused words: yid and hid',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byid\b/gi,
    message: 'Check: "yid" ("yid") vs "hid" ("hid"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hid';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yikes_hikes_4342',
    name: 'yikes vs hikes',
    description: 'Commonly confused words: yikes and hikes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byikes\b/gi,
    message: 'Check: "yikes" ("yikes") vs "hikes" ("hikes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hikes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yip_hip_4343',
    name: 'yip vs hip',
    description: 'Commonly confused words: yip and hip',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byip\b/gi,
    message: 'Check: "yip" ("yip") vs "hip" ("hip"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hip';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yoked_yokes_4344',
    name: 'yoked vs yokes',
    description: 'Commonly confused words: yoked and yokes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byoked\b/gi,
    message: 'Check: "yoked" ("yoked") vs "yokes" ("yokes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'yokes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yoke_hoke_4345',
    name: 'yoke vs hoke',
    description: 'Commonly confused words: yoke and hoke',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byoke\b/gi,
    message: 'Check: "yoke" ("yoke") vs "hoke" ("hoke"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hoke';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yoke_toke_4346',
    name: 'yoke vs toke',
    description: 'Commonly confused words: yoke and toke',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byoke\b/gi,
    message: 'Check: "yoke" ("yoke") vs "toke" ("toke"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toke';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_your_hour_4347',
    name: 'your vs hour',
    description: 'Commonly confused words: your and hour',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byour\b/gi,
    message: 'Check: "your" (belonging to you) vs "hour" (sixty minutes). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hour';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yours_hours_4348',
    name: 'yours vs hours',
    description: 'Commonly confused words: yours and hours',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byours\b/gi,
    message: 'Check: "yours" ("yours") vs "hours" ("hours"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hours';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_yours_tours_4349',
    name: 'yours vs tours',
    description: 'Commonly confused words: yours and tours',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\byours\b/gi,
    message: 'Check: "yours" ("yours") vs "tours" ("tours"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tours';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
