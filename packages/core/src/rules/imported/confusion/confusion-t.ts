/**
 * Auto-generated confusion rules (T)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.521Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesT: GrammarRule[] = [
  {
    id: 'confusion_tails_tales_663',
    name: 'tails vs tales',
    description: 'Commonly confused words: tails and tales',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btails\b/gi,
    message: 'Check: "tails" ("tails") vs "tales" ("tales"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tales';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_taking_talking_664',
    name: 'taking vs talking',
    description: 'Commonly confused words: taking and talking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btaking\b/gi,
    message: 'Check: "taking" ("taking") vs "talking" ("talking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'talking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_taper_tapir_665',
    name: 'taper vs tapir',
    description: 'Commonly confused words: taper and tapir',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btaper\b/gi,
    message: 'Check: "taper" ("taper") vs "tapir" ("tapir"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tapir';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tapers_tapirs_666',
    name: 'tapers vs tapirs',
    description: 'Commonly confused words: tapers and tapirs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btapers\b/gi,
    message: 'Check: "tapers" ("tapers") vs "tapirs" ("tapirs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tapirs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_taught_taut_667',
    name: 'taught vs taut',
    description: 'Commonly confused words: taught and taut',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btaught\b/gi,
    message: 'Did you mean "taut"? "taught" ("taught") is often confused with "taut" ("taut").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'taut';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_taunt_taut_668',
    name: 'taunt vs taut',
    description: 'Commonly confused words: taunt and taut',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btaunt\b/gi,
    message: 'Check: "taunt" ("taunt") vs "taut" ("taut"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'taut';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_teaming_teeming_669',
    name: 'teaming vs teeming',
    description: 'Commonly confused words: teaming and teeming',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bteaming\b/gi,
    message: 'Check: "teaming" ("teaming") vs "teeming" ("teeming"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'teeming';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_teared_tiered_670',
    name: 'teared vs tiered',
    description: 'Commonly confused words: teared and tiered',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bteared\b/gi,
    message: 'Check: "teared" ("teared") vs "tiered" ("tiered"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tiered';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tear_tier_671',
    name: 'tear vs tier',
    description: 'Commonly confused words: tear and tier',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btear\b/gi,
    message: 'Check: "tear" ("tear") vs "tier" ("tier"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tier';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tease_tees_672',
    name: 'tease vs tees',
    description: 'Commonly confused words: tease and tees',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btease\b/gi,
    message: 'Check: "tease" ("tease") vs "tees" ("tees"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tees';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_teas_tease_673',
    name: 'teas vs tease',
    description: 'Commonly confused words: teas and tease',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bteas\b/gi,
    message: 'Check: "teas" ("teas") vs "tease" ("tease"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tease';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_teeth_tooth_674',
    name: 'teeth vs tooth',
    description: 'Commonly confused words: teeth and tooth',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bteeth\b/gi,
    message: 'Check: "teeth" ("teeth") vs "tooth" ("tooth"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tooth';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tense_tents_675',
    name: 'tense vs tents',
    description: 'Commonly confused words: tense and tents',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btense\b/gi,
    message: 'Check: "tense" ("tense") vs "tents" ("tents"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tents';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_them_then_676',
    name: 'them vs then',
    description: 'Commonly confused words: them and then',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthem\b/gi,
    message: 'Check: "them" ("them") vs "then" (at that time or next). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'then';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_theses_thesis_677',
    name: 'theses vs thesis',
    description: 'Commonly confused words: theses and thesis',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btheses\b/gi,
    message: 'Check: "theses" ("theses") vs "thesis" ("thesis"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'thesis';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_the_they_678',
    name: 'the vs they',
    description: 'Commonly confused words: the and they',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthe\b/gi,
    message: 'Check: "the" ("the") vs "they" ("they"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'they';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_the_to_679',
    name: 'the vs to',
    description: 'Commonly confused words: the and to',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthe\b/gi,
    message: 'Did you mean "to"? "the" ("the") is often confused with "to" (preposition indicating direction).',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'to';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_to_the_680',
    name: 'to vs the',
    description: 'Commonly confused words: to and the',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bto\b/gi,
    message: 'Did you mean "the"? "to" (preposition indicating direction) is often confused with "the" ("the").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'the';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_think_thing_681',
    name: 'think vs thing',
    description: 'Commonly confused words: think and thing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthink\b/gi,
    message: 'Did you mean "thing"? "think" ("think") is often confused with "thing" ("thing").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'thing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_thing_think_682',
    name: 'thing vs think',
    description: 'Commonly confused words: thing and think',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthing\b/gi,
    message: 'Did you mean "think"? "thing" ("thing") is often confused with "think" ("think").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'think';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_things_thinks_683',
    name: 'things vs thinks',
    description: 'Commonly confused words: things and thinks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthings\b/gi,
    message: 'Check: "things" ("things") vs "thinks" ("thinks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'thinks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_thin_thing_684',
    name: 'thin vs thing',
    description: 'Commonly confused words: thin and thing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthin\b/gi,
    message: 'Check: "thin" ("thin") vs "thing" ("thing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'thing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_thin_think_685',
    name: 'thin vs think',
    description: 'Commonly confused words: thin and think',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthin\b/gi,
    message: 'Check: "thin" ("thin") vs "think" ("think"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'think';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_threw_through_686',
    name: 'threw vs through',
    description: 'Commonly confused words: threw and through',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthrew\b/gi,
    message: 'Did you mean "through"? "threw" ("threw") is often confused with "through" ("through").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'through';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_through_threw_687',
    name: 'through vs threw',
    description: 'Commonly confused words: through and threw',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthrough\b/gi,
    message: 'Did you mean "threw"? "through" ("through") is often confused with "threw" ("threw").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'threw';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_throe_throw_688',
    name: 'throe vs throw',
    description: 'Commonly confused words: throe and throw',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthroe\b/gi,
    message: 'Check: "throe" ("throe") vs "throw" ("throw"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'throw';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_throes_throws_689',
    name: 'throes vs throws',
    description: 'Commonly confused words: throes and throws',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthroes\b/gi,
    message: 'Check: "throes" ("throes") vs "throws" ("throws"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'throws';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_throne_thrown_690',
    name: 'throne vs thrown',
    description: 'Commonly confused words: throne and thrown',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthrone\b/gi,
    message: 'Check: "throne" ("throne") vs "thrown" ("thrown"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'thrown';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ticks_tics_691',
    name: 'ticks vs tics',
    description: 'Commonly confused words: ticks and tics',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bticks\b/gi,
    message: 'Check: "ticks" ("ticks") vs "tics" ("tics"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tics';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tide_tied_692',
    name: 'tide vs tied',
    description: 'Commonly confused words: tide and tied',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btide\b/gi,
    message: 'Check: "tide" ("tide") vs "tied" ("tied"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tied';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tightens_titans_693',
    name: 'tightens vs titans',
    description: 'Commonly confused words: tightens and titans',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btightens\b/gi,
    message: 'Check: "tightens" ("tightens") vs "titans" ("titans"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'titans';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_timber_timbre_694',
    name: 'timber vs timbre',
    description: 'Commonly confused words: timber and timbre',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btimber\b/gi,
    message: 'Check: "timber" ("timber") vs "timbre" ("timbre"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'timbre';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tired_tried_695',
    name: 'tired vs tried',
    description: 'Commonly confused words: tired and tried',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btired\b/gi,
    message: 'Check: "tired" ("tired") vs "tried" ("tried"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tried';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ti_to_696',
    name: 'ti vs to',
    description: 'Commonly confused words: ti and to',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bti\b/gi,
    message: 'Check: "ti" ("ti") vs "to" (preposition indicating direction). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'to';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toad_toed_697',
    name: 'toad vs toed',
    description: 'Commonly confused words: toad and toed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoad\b/gi,
    message: 'Check: "toad" ("toad") vs "toed" ("toed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tocsin_toxin_698',
    name: 'tocsin vs toxin',
    description: 'Commonly confused words: tocsin and toxin',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btocsin\b/gi,
    message: 'Check: "tocsin" ("tocsin") vs "toxin" ("toxin"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toxin';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tocsins_toxins_699',
    name: 'tocsins vs toxins',
    description: 'Commonly confused words: tocsins and toxins',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btocsins\b/gi,
    message: 'Check: "tocsins" ("tocsins") vs "toxins" ("toxins"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toxins';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toed_towed_700',
    name: 'toed vs towed',
    description: 'Commonly confused words: toed and towed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoed\b/gi,
    message: 'Check: "toed" ("toed") vs "towed" ("towed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'towed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toe_tow_701',
    name: 'toe vs tow',
    description: 'Commonly confused words: toe and tow',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoe\b/gi,
    message: 'Check: "toe" ("toe") vs "tow" ("tow"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tow';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toll_tool_702',
    name: 'toll vs tool',
    description: 'Commonly confused words: toll and tool',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoll\b/gi,
    message: 'Check: "toll" ("toll") vs "tool" ("tool"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tool';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tracked_tract_703',
    name: 'tracked vs tract',
    description: 'Commonly confused words: tracked and tract',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btracked\b/gi,
    message: 'Check: "tracked" ("tracked") vs "tract" ("tract"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tract';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tray_trey_704',
    name: 'tray vs trey',
    description: 'Commonly confused words: tray and trey',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btray\b/gi,
    message: 'Check: "tray" ("tray") vs "trey" ("trey"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trey';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trays_treys_705',
    name: 'trays vs treys',
    description: 'Commonly confused words: trays and treys',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrays\b/gi,
    message: 'Check: "trays" ("trays") vs "treys" ("treys"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'treys';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_thought_taught_776',
    name: 'thought vs taught',
    description: 'Commonly confused words: thought and taught',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthought\b/gi,
    message: 'Did you mean "taught"? "thought" ("thought") is often confused with "taught" ("taught").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'taught';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_taught_thought_777',
    name: 'taught vs thought',
    description: 'Commonly confused words: taught and thought',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btaught\b/gi,
    message: 'Did you mean "thought"? "taught" ("taught") is often confused with "thought" ("thought").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'thought';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_thins_this_781',
    name: 'thins vs this',
    description: 'Commonly confused words: thins and this',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthins\b/gi,
    message: 'Did you mean "this"? "thins" ("thins") is often confused with "this" ("this").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'this';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tabulated_tabulates_3944',
    name: 'tabulated vs tabulates',
    description: 'Commonly confused words: tabulated and tabulates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btabulated\b/gi,
    message: 'Check: "tabulated" ("tabulated") vs "tabulates" ("tabulates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tabulates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tacked_racked_3945',
    name: 'tacked vs racked',
    description: 'Commonly confused words: tacked and racked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btacked\b/gi,
    message: 'Check: "tacked" ("tacked") vs "racked" ("racked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'racked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tackled_tackles_3946',
    name: 'tackled vs tackles',
    description: 'Commonly confused words: tackled and tackles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btackled\b/gi,
    message: 'Check: "tackled" ("tackled") vs "tackles" ("tackles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tackles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tacks_racks_3947',
    name: 'tacks vs racks',
    description: 'Commonly confused words: tacks and racks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btacks\b/gi,
    message: 'Check: "tacks" ("tacks") vs "racks" ("racks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'racks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tagged_ragged_3948',
    name: 'tagged vs ragged',
    description: 'Commonly confused words: tagged and ragged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btagged\b/gi,
    message: 'Check: "tagged" ("tagged") vs "ragged" ("ragged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ragged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tailed_railed_3949',
    name: 'tailed vs railed',
    description: 'Commonly confused words: tailed and railed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btailed\b/gi,
    message: 'Check: "tailed" ("tailed") vs "railed" ("railed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'railed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tails_rails_3950',
    name: 'tails vs rails',
    description: 'Commonly confused words: tails and rails',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btails\b/gi,
    message: 'Check: "tails" ("tails") vs "rails" ("rails"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rails';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_taints_taunts_3951',
    name: 'taints vs taunts',
    description: 'Commonly confused words: taints and taunts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btaints\b/gi,
    message: 'Check: "taints" ("taints") vs "taunts" ("taunts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'taunts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_taint_taunt_3952',
    name: 'taint vs taunt',
    description: 'Commonly confused words: taint and taunt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btaint\b/gi,
    message: 'Check: "taint" ("taint") vs "taunt" ("taunt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'taunt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tale_gale_3953',
    name: 'tale vs gale',
    description: 'Commonly confused words: tale and gale',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btale\b/gi,
    message: 'Check: "tale" (story) vs "gale" ("gale"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gale';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_talkies_tallies_3954',
    name: 'talkies vs tallies',
    description: 'Commonly confused words: talkies and tallies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btalkies\b/gi,
    message: 'Check: "talkies" ("talkies") vs "tallies" ("tallies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tallies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_taller_talker_3955',
    name: 'taller vs talker',
    description: 'Commonly confused words: taller and talker',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btaller\b/gi,
    message: 'Check: "taller" ("taller") vs "talker" ("talker"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'talker';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tallied_tallies_3956',
    name: 'tallied vs tallies',
    description: 'Commonly confused words: tallied and tallies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btallied\b/gi,
    message: 'Check: "tallied" ("tallied") vs "tallies" ("tallies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tallies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tamed_tames_3957',
    name: 'tamed vs tames',
    description: 'Commonly confused words: tamed and tames',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btamed\b/gi,
    message: 'Check: "tamed" ("tamed") vs "tames" ("tames"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tames';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tames_games_3958',
    name: 'tames vs games',
    description: 'Commonly confused words: tames and games',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btames\b/gi,
    message: 'Check: "tames" ("tames") vs "games" ("games"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'games';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tamping_ramping_3959',
    name: 'tamping vs ramping',
    description: 'Commonly confused words: tamping and ramping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btamping\b/gi,
    message: 'Check: "tamping" ("tamping") vs "ramping" ("ramping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ramping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tamp_ramp_3960',
    name: 'tamp vs ramp',
    description: 'Commonly confused words: tamp and ramp',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btamp\b/gi,
    message: 'Check: "tamp" ("tamp") vs "ramp" ("ramp"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ramp';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tamps_ramps_3961',
    name: 'tamps vs ramps',
    description: 'Commonly confused words: tamps and ramps',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btamps\b/gi,
    message: 'Check: "tamps" ("tamps") vs "ramps" ("ramps"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ramps';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tangled_tangles_3962',
    name: 'tangled vs tangles',
    description: 'Commonly confused words: tangled and tangles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btangled\b/gi,
    message: 'Check: "tangled" ("tangled") vs "tangles" ("tangles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tangles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tanks_ranks_3963',
    name: 'tanks vs ranks',
    description: 'Commonly confused words: tanks and ranks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btanks\b/gi,
    message: 'Check: "tanks" ("tanks") vs "ranks" ("ranks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ranks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_taped_gaped_3964',
    name: 'taped vs gaped',
    description: 'Commonly confused words: taped and gaped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btaped\b/gi,
    message: 'Check: "taped" ("taped") vs "gaped" ("gaped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gaped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_taped_raped_3965',
    name: 'taped vs raped',
    description: 'Commonly confused words: taped and raped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btaped\b/gi,
    message: 'Check: "taped" ("taped") vs "raped" ("raped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'raped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_taped_tapes_3966',
    name: 'taped vs tapes',
    description: 'Commonly confused words: taped and tapes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btaped\b/gi,
    message: 'Check: "taped" ("taped") vs "tapes" ("tapes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tapes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_taping_raping_3967',
    name: 'taping vs raping',
    description: 'Commonly confused words: taping and raping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btaping\b/gi,
    message: 'Check: "taping" ("taping") vs "raping" ("raping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'raping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tapper_rapper_3968',
    name: 'tapper vs rapper',
    description: 'Commonly confused words: tapper and rapper',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btapper\b/gi,
    message: 'Check: "tapper" ("tapper") vs "rapper" ("rapper"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rapper';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tarnished_tarnishes_3969',
    name: 'tarnished vs tarnishes',
    description: 'Commonly confused words: tarnished and tarnishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btarnished\b/gi,
    message: 'Check: "tarnished" ("tarnished") vs "tarnishes" ("tarnishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tarnishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_taros_tarps_3970',
    name: 'taros vs tarps',
    description: 'Commonly confused words: taros and tarps',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btaros\b/gi,
    message: 'Check: "taros" ("taros") vs "tarps" ("tarps"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tarps';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tarred_tarted_3971',
    name: 'tarred vs tarted',
    description: 'Commonly confused words: tarred and tarted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btarred\b/gi,
    message: 'Check: "tarred" ("tarred") vs "tarted" ("tarted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tarted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tarring_tarting_3972',
    name: 'tarring vs tarting',
    description: 'Commonly confused words: tarring and tarting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btarring\b/gi,
    message: 'Check: "tarring" ("tarring") vs "tarting" ("tarting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tarting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tarter_garter_3973',
    name: 'tarter vs garter',
    description: 'Commonly confused words: tarter and garter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btarter\b/gi,
    message: 'Check: "tarter" ("tarter") vs "garter" ("garter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'garter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tarty_tarry_3974',
    name: 'tarty vs tarry',
    description: 'Commonly confused words: tarty and tarry',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btarty\b/gi,
    message: 'Check: "tarty" ("tarty") vs "tarry" ("tarry"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tarry';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tatting_tarting_3975',
    name: 'tatting vs tarting',
    description: 'Commonly confused words: tatting and tarting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btatting\b/gi,
    message: 'Check: "tatting" ("tatting") vs "tarting" ("tarting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tarting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tattle_rattle_3976',
    name: 'tattle vs rattle',
    description: 'Commonly confused words: tattle and rattle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btattle\b/gi,
    message: 'Check: "tattle" ("tattle") vs "rattle" ("rattle"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rattle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_taxed_taxes_3977',
    name: 'taxed vs taxes',
    description: 'Commonly confused words: taxed and taxes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btaxed\b/gi,
    message: 'Check: "taxed" ("taxed") vs "taxes" ("taxes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'taxes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_teaches_reaches_3978',
    name: 'teaches vs reaches',
    description: 'Commonly confused words: teaches and reaches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bteaches\b/gi,
    message: 'Check: "teaches" ("teaches") vs "reaches" ("reaches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reaches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_teak_teal_3979',
    name: 'teak vs teal',
    description: 'Commonly confused words: teak and teal',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bteak\b/gi,
    message: 'Check: "teak" ("teak") vs "teal" ("teal"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'teal';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_teals_reals_3980',
    name: 'teals vs reals',
    description: 'Commonly confused words: teals and reals',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bteals\b/gi,
    message: 'Check: "teals" ("teals") vs "reals" ("reals"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reals';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_teamed_reamed_3981',
    name: 'teamed vs reamed',
    description: 'Commonly confused words: teamed and reamed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bteamed\b/gi,
    message: 'Check: "teamed" ("teamed") vs "reamed" ("reamed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reamed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_teaming_reaming_3982',
    name: 'teaming vs reaming',
    description: 'Commonly confused words: teaming and reaming',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bteaming\b/gi,
    message: 'Check: "teaming" ("teaming") vs "reaming" ("reaming"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reaming';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tear_gear_3983',
    name: 'tear vs gear',
    description: 'Commonly confused words: tear and gear',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btear\b/gi,
    message: 'Check: "tear" ("tear") vs "gear" ("gear"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gear';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tearing_gearing_3984',
    name: 'tearing vs gearing',
    description: 'Commonly confused words: tearing and gearing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btearing\b/gi,
    message: 'Check: "tearing" ("tearing") vs "gearing" ("gearing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gearing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tearing_rearing_3985',
    name: 'tearing vs rearing',
    description: 'Commonly confused words: tearing and rearing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btearing\b/gi,
    message: 'Check: "tearing" ("tearing") vs "rearing" ("rearing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rearing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_teemed_termed_3986',
    name: 'teemed vs termed',
    description: 'Commonly confused words: teemed and termed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bteemed\b/gi,
    message: 'Check: "teemed" ("teemed") vs "termed" ("termed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'termed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_teeming_terming_3987',
    name: 'teeming vs terming',
    description: 'Commonly confused words: teeming and terming',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bteeming\b/gi,
    message: 'Check: "teeming" ("teeming") vs "terming" ("terming"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'terming';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_teens_terns_3988',
    name: 'teens vs terns',
    description: 'Commonly confused words: teens and terns',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bteens\b/gi,
    message: 'Check: "teens" ("teens") vs "terns" ("terns"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'terns';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_telephoned_telephones_3989',
    name: 'telephoned vs telephones',
    description: 'Commonly confused words: telephoned and telephones',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btelephoned\b/gi,
    message: 'Check: "telephoned" ("telephoned") vs "telephones" ("telephones"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'telephones';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_telescoped_telescopes_3990',
    name: 'telescoped vs telescopes',
    description: 'Commonly confused words: telescoped and telescopes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btelescoped\b/gi,
    message: 'Check: "telescoped" ("telescoped") vs "telescopes" ("telescopes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'telescopes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tempers_tempera_3991',
    name: 'tempers vs tempera',
    description: 'Commonly confused words: tempers and tempera',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btempers\b/gi,
    message: 'Check: "tempers" ("tempers") vs "tempera" ("tempera"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tempera';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tender_gender_3992',
    name: 'tender vs gender',
    description: 'Commonly confused words: tender and gender',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btender\b/gi,
    message: 'Check: "tender" ("tender") vs "gender" ("gender"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gender';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tenders_renders_3993',
    name: 'tenders vs renders',
    description: 'Commonly confused words: tenders and renders',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btenders\b/gi,
    message: 'Check: "tenders" ("tenders") vs "renders" ("renders"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'renders';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tending_rending_3994',
    name: 'tending vs rending',
    description: 'Commonly confused words: tending and rending',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btending\b/gi,
    message: 'Check: "tending" ("tending") vs "rending" ("rending"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rending';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tending_tensing_3995',
    name: 'tending vs tensing',
    description: 'Commonly confused words: tending and tensing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btending\b/gi,
    message: 'Check: "tending" ("tending") vs "tensing" ("tensing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tensing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tend_rend_3996',
    name: 'tend vs rend',
    description: 'Commonly confused words: tend and rend',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btend\b/gi,
    message: 'Check: "tend" ("tend") vs "rend" ("rend"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rend';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tends_rends_3997',
    name: 'tends vs rends',
    description: 'Commonly confused words: tends and rends',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btends\b/gi,
    message: 'Check: "tends" ("tends") vs "rends" ("rends"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rends';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tend_tens_3998',
    name: 'tend vs tens',
    description: 'Commonly confused words: tend and tens',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btend\b/gi,
    message: 'Check: "tend" ("tend") vs "tens" ("tens"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tens';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tensed_tenses_3999',
    name: 'tensed vs tenses',
    description: 'Commonly confused words: tensed and tenses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btensed\b/gi,
    message: 'Check: "tensed" ("tensed") vs "tenses" ("tenses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tenses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tens_gens_4000',
    name: 'tens vs gens',
    description: 'Commonly confused words: tens and gens',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btens\b/gi,
    message: 'Check: "tens" ("tens") vs "gens" ("gens"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gens';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tent_gent_4001',
    name: 'tent vs gent',
    description: 'Commonly confused words: tent and gent',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btent\b/gi,
    message: 'Check: "tent" ("tent") vs "gent" ("gent"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gent';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tents_rents_4002',
    name: 'tents vs rents',
    description: 'Commonly confused words: tents and rents',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btents\b/gi,
    message: 'Check: "tents" ("tents") vs "rents" ("rents"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rents';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tenured_tenures_4003',
    name: 'tenured vs tenures',
    description: 'Commonly confused words: tenured and tenures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btenured\b/gi,
    message: 'Check: "tenured" ("tenured") vs "tenures" ("tenures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tenures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_terminated_terminates_4004',
    name: 'terminated vs terminates',
    description: 'Commonly confused words: terminated and terminates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bterminated\b/gi,
    message: 'Check: "terminated" ("terminated") vs "terminates" ("terminates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'terminates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_terraced_terraces_4005',
    name: 'terraced vs terraces',
    description: 'Commonly confused words: terraced and terraces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bterraced\b/gi,
    message: 'Check: "terraced" ("terraced") vs "terraces" ("terraces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'terraces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_terrified_terrifies_4006',
    name: 'terrified vs terrifies',
    description: 'Commonly confused words: terrified and terrifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bterrified\b/gi,
    message: 'Check: "terrified" ("terrified") vs "terrifies" ("terrifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'terrifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_terrorized_terrorizes_4007',
    name: 'terrorized vs terrorizes',
    description: 'Commonly confused words: terrorized and terrorizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bterrorized\b/gi,
    message: 'Check: "terrorized" ("terrorized") vs "terrorizes" ("terrorizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'terrorizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tessellated_tessellates_4008',
    name: 'tessellated vs tessellates',
    description: 'Commonly confused words: tessellated and tessellates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btessellated\b/gi,
    message: 'Check: "tessellated" ("tessellated") vs "tessellates" ("tessellates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tessellates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_texts_tests_4009',
    name: 'texts vs tests',
    description: 'Commonly confused words: texts and tests',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btexts\b/gi,
    message: 'Check: "texts" ("texts") vs "tests" ("tests"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tests';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_text_test_4010',
    name: 'text vs test',
    description: 'Commonly confused words: text and test',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btext\b/gi,
    message: 'Check: "text" ("text") vs "test" ("test"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'test';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_textured_textures_4011',
    name: 'textured vs textures',
    description: 'Commonly confused words: textured and textures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btextured\b/gi,
    message: 'Check: "textured" ("textured") vs "textures" ("textures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'textures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_thatched_thatches_4012',
    name: 'thatched vs thatches',
    description: 'Commonly confused words: thatched and thatches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthatched\b/gi,
    message: 'Check: "thatched" ("thatched") vs "thatches" ("thatches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'thatches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_theorized_theorizes_4013',
    name: 'theorized vs theorizes',
    description: 'Commonly confused words: theorized and theorizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btheorized\b/gi,
    message: 'Check: "theorized" ("theorized") vs "theorizes" ("theorizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'theorizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_thicker_thicket_4014',
    name: 'thicker vs thicket',
    description: 'Commonly confused words: thicker and thicket',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthicker\b/gi,
    message: 'Check: "thicker" ("thicker") vs "thicket" ("thicket"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'thicket';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_thing_tying_4015',
    name: 'thing vs tying',
    description: 'Commonly confused words: thing and tying',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthing\b/gi,
    message: 'Check: "thing" ("thing") vs "tying" ("tying"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tying';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_three_threw_4016',
    name: 'three vs threw',
    description: 'Commonly confused words: three and threw',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthree\b/gi,
    message: 'Check: "three" ("three") vs "threw" ("threw"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'threw';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_thrice_thrive_4017',
    name: 'thrice vs thrive',
    description: 'Commonly confused words: thrice and thrive',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthrice\b/gi,
    message: 'Check: "thrice" ("thrice") vs "thrive" ("thrive"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'thrive';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_throttled_throttles_4018',
    name: 'throttled vs throttles',
    description: 'Commonly confused words: throttled and throttles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthrottled\b/gi,
    message: 'Check: "throttled" ("throttled") vs "throttles" ("throttles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'throttles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_throughout_throughput_4019',
    name: 'throughout vs throughput',
    description: 'Commonly confused words: throughout and throughput',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bthroughout\b/gi,
    message: 'Check: "throughout" ("throughout") vs "throughput" ("throughput"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'throughput';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tickled_tickles_4020',
    name: 'tickled vs tickles',
    description: 'Commonly confused words: tickled and tickles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btickled\b/gi,
    message: 'Check: "tickled" ("tickled") vs "tickles" ("tickles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tickles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tiddlers_toddlers_4021',
    name: 'tiddlers vs toddlers',
    description: 'Commonly confused words: tiddlers and toddlers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btiddlers\b/gi,
    message: 'Check: "tiddlers" ("tiddlers") vs "toddlers" ("toddlers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toddlers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tided_tides_4022',
    name: 'tided vs tides',
    description: 'Commonly confused words: tided and tides',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btided\b/gi,
    message: 'Check: "tided" ("tided") vs "tides" ("tides"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tides';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tides_rides_4023',
    name: 'tides vs rides',
    description: 'Commonly confused words: tides and rides',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btides\b/gi,
    message: 'Check: "tides" ("tides") vs "rides" ("rides"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rides';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tidied_tidies_4024',
    name: 'tidied vs tidies',
    description: 'Commonly confused words: tidied and tidies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btidied\b/gi,
    message: 'Check: "tidied" ("tidied") vs "tidies" ("tidies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tidies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tiled_riled_4025',
    name: 'tiled vs riled',
    description: 'Commonly confused words: tiled and riled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btiled\b/gi,
    message: 'Check: "tiled" ("tiled") vs "riled" ("riled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'riled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tile_rile_4026',
    name: 'tile vs rile',
    description: 'Commonly confused words: tile and rile',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btile\b/gi,
    message: 'Check: "tile" ("tile") vs "rile" ("rile"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rile';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tiling_riling_4027',
    name: 'tiling vs riling',
    description: 'Commonly confused words: tiling and riling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btiling\b/gi,
    message: 'Check: "tiling" ("tiling") vs "riling" ("riling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'riling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tilt_gilt_4028',
    name: 'tilt vs gilt',
    description: 'Commonly confused words: tilt and gilt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btilt\b/gi,
    message: 'Check: "tilt" ("tilt") vs "gilt" ("gilt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gilt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tilts_gilts_4029',
    name: 'tilts vs gilts',
    description: 'Commonly confused words: tilts and gilts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btilts\b/gi,
    message: 'Check: "tilts" ("tilts") vs "gilts" ("gilts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gilts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_timbrel_tumbrel_4030',
    name: 'timbrel vs tumbrel',
    description: 'Commonly confused words: timbrel and tumbrel',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btimbrel\b/gi,
    message: 'Check: "timbrel" ("timbrel") vs "tumbrel" ("tumbrel"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tumbrel';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_timed_rimed_4031',
    name: 'timed vs rimed',
    description: 'Commonly confused words: timed and rimed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btimed\b/gi,
    message: 'Check: "timed" ("timed") vs "rimed" ("rimed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rimed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_timid_tumid_4032',
    name: 'timid vs tumid',
    description: 'Commonly confused words: timid and tumid',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btimid\b/gi,
    message: 'Check: "timid" ("timid") vs "tumid" ("tumid"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tumid';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_timing_riming_4033',
    name: 'timing vs riming',
    description: 'Commonly confused words: timing and riming',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btiming\b/gi,
    message: 'Check: "timing" ("timing") vs "riming" ("riming"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'riming';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_timorous_tumorous_4034',
    name: 'timorous vs tumorous',
    description: 'Commonly confused words: timorous and tumorous',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btimorous\b/gi,
    message: 'Check: "timorous" ("timorous") vs "tumorous" ("tumorous"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tumorous';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tinctured_tinctures_4035',
    name: 'tinctured vs tinctures',
    description: 'Commonly confused words: tinctured and tinctures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btinctured\b/gi,
    message: 'Check: "tinctured" ("tinctured") vs "tinctures" ("tinctures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tinctures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tinged_ringed_4036',
    name: 'tinged vs ringed',
    description: 'Commonly confused words: tinged and ringed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btinged\b/gi,
    message: 'Check: "tinged" ("tinged") vs "ringed" ("ringed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ringed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tinged_tinges_4037',
    name: 'tinged vs tinges',
    description: 'Commonly confused words: tinged and tinges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btinged\b/gi,
    message: 'Check: "tinged" ("tinged") vs "tinges" ("tinges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tinges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tinier_tinker_4038',
    name: 'tinier vs tinker',
    description: 'Commonly confused words: tinier and tinker',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btinier\b/gi,
    message: 'Check: "tinier" ("tinier") vs "tinker" ("tinker"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tinker';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tipped_ripped_4039',
    name: 'tipped vs ripped',
    description: 'Commonly confused words: tipped and ripped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btipped\b/gi,
    message: 'Check: "tipped" ("tipped") vs "ripped" ("ripped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ripped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tipping_ripping_4040',
    name: 'tipping vs ripping',
    description: 'Commonly confused words: tipping and ripping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btipping\b/gi,
    message: 'Check: "tipping" ("tipping") vs "ripping" ("ripping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ripping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tipple_topple_4041',
    name: 'tipple vs topple',
    description: 'Commonly confused words: tipple and topple',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btipple\b/gi,
    message: 'Check: "tipple" ("tipple") vs "topple" ("topple"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'topple';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tippling_toppling_4042',
    name: 'tippling vs toppling',
    description: 'Commonly confused words: tippling and toppling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btippling\b/gi,
    message: 'Check: "tippling" ("tippling") vs "toppling" ("toppling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toppling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tiptoed_tiptoes_4043',
    name: 'tiptoed vs tiptoes',
    description: 'Commonly confused words: tiptoed and tiptoes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btiptoed\b/gi,
    message: 'Check: "tiptoed" ("tiptoed") vs "tiptoes" ("tiptoes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tiptoes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tithed_tithes_4044',
    name: 'tithed vs tithes',
    description: 'Commonly confused words: tithed and tithes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btithed\b/gi,
    message: 'Check: "tithed" ("tithed") vs "tithes" ("tithes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tithes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_titled_titles_4045',
    name: 'titled vs titles',
    description: 'Commonly confused words: titled and titles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btitled\b/gi,
    message: 'Check: "titled" ("titled") vs "titles" ("titles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'titles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toads_goads_4046',
    name: 'toads vs goads',
    description: 'Commonly confused words: toads and goads',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoads\b/gi,
    message: 'Check: "toads" ("toads") vs "goads" ("goads"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'goads';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toed_tied_4047',
    name: 'toed vs tied',
    description: 'Commonly confused words: toed and tied',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoed\b/gi,
    message: 'Check: "toed" ("toed") vs "tied" ("tied"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tied';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toed_toes_4048',
    name: 'toed vs toes',
    description: 'Commonly confused words: toed and toes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoed\b/gi,
    message: 'Check: "toed" ("toed") vs "toes" ("toes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toeing_towing_4049',
    name: 'toeing vs towing',
    description: 'Commonly confused words: toeing and towing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoeing\b/gi,
    message: 'Check: "toeing" ("toeing") vs "towing" ("towing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'towing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toes_goes_4050',
    name: 'toes vs goes',
    description: 'Commonly confused words: toes and goes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoes\b/gi,
    message: 'Check: "toes" ("toes") vs "goes" ("goes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'goes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toes_roes_4051',
    name: 'toes vs roes',
    description: 'Commonly confused words: toes and roes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoes\b/gi,
    message: 'Check: "toes" ("toes") vs "roes" ("roes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'roes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toggled_toggles_4052',
    name: 'toggled vs toggles',
    description: 'Commonly confused words: toggled and toggles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoggled\b/gi,
    message: 'Check: "toggled" ("toggled") vs "toggles" ("toggles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toggles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toiled_roiled_4053',
    name: 'toiled vs roiled',
    description: 'Commonly confused words: toiled and roiled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoiled\b/gi,
    message: 'Check: "toiled" ("toiled") vs "roiled" ("roiled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'roiled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toilers_toilets_4054',
    name: 'toilers vs toilets',
    description: 'Commonly confused words: toilers and toilets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoilers\b/gi,
    message: 'Check: "toilers" ("toilers") vs "toilets" ("toilets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toilets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toiler_toilet_4055',
    name: 'toiler vs toilet',
    description: 'Commonly confused words: toiler and toilet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoiler\b/gi,
    message: 'Check: "toiler" ("toiler") vs "toilet" ("toilet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toilet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toiling_roiling_4056',
    name: 'toiling vs roiling',
    description: 'Commonly confused words: toiling and roiling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoiling\b/gi,
    message: 'Check: "toiling" ("toiling") vs "roiling" ("roiling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'roiling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toiling_tooling_4057',
    name: 'toiling vs tooling',
    description: 'Commonly confused words: toiling and tooling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoiling\b/gi,
    message: 'Check: "toiling" ("toiling") vs "tooling" ("tooling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tooling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toil_roil_4058',
    name: 'toil vs roil',
    description: 'Commonly confused words: toil and roil',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoil\b/gi,
    message: 'Check: "toil" ("toil") vs "roil" ("roil"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'roil';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_told_gold_4059',
    name: 'told vs gold',
    description: 'Commonly confused words: told and gold',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btold\b/gi,
    message: 'Check: "told" ("told") vs "gold" ("gold"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gold';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tolerated_tolerates_4060',
    name: 'tolerated vs tolerates',
    description: 'Commonly confused words: tolerated and tolerates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btolerated\b/gi,
    message: 'Check: "tolerated" ("tolerated") vs "tolerates" ("tolerates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tolerates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tolling_tilling_4061',
    name: 'tolling vs tilling',
    description: 'Commonly confused words: tolling and tilling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btolling\b/gi,
    message: 'Check: "tolling" ("tolling") vs "tilling" ("tilling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tilling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tolls_tills_4062',
    name: 'tolls vs tills',
    description: 'Commonly confused words: tolls and tills',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btolls\b/gi,
    message: 'Check: "tolls" ("tolls") vs "tills" ("tills"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tills';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tone_gone_4063',
    name: 'tone vs gone',
    description: 'Commonly confused words: tone and gone',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btone\b/gi,
    message: 'Check: "tone" ("tone") vs "gone" ("gone"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gone';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tongued_tongues_4064',
    name: 'tongued vs tongues',
    description: 'Commonly confused words: tongued and tongues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btongued\b/gi,
    message: 'Check: "tongued" ("tongued") vs "tongues" ("tongues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tongues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_took_gook_4065',
    name: 'took vs gook',
    description: 'Commonly confused words: took and gook',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btook\b/gi,
    message: 'Check: "took" ("took") vs "gook" ("gook"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gook';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tooled_toiled_4066',
    name: 'tooled vs toiled',
    description: 'Commonly confused words: tooled and toiled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btooled\b/gi,
    message: 'Check: "tooled" ("tooled") vs "toiled" ("toiled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toiled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tool_toil_4067',
    name: 'tool vs toil',
    description: 'Commonly confused words: tool and toil',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btool\b/gi,
    message: 'Check: "tool" ("tool") vs "toil" ("toil"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toil';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tool_took_4068',
    name: 'tool vs took',
    description: 'Commonly confused words: tool and took',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btool\b/gi,
    message: 'Check: "tool" ("tool") vs "took" ("took"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'took';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tooting_rooting_4069',
    name: 'tooting vs rooting',
    description: 'Commonly confused words: tooting and rooting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btooting\b/gi,
    message: 'Check: "tooting" ("tooting") vs "rooting" ("rooting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rooting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toppers_tippers_4070',
    name: 'toppers vs tippers',
    description: 'Commonly confused words: toppers and tippers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoppers\b/gi,
    message: 'Check: "toppers" ("toppers") vs "tippers" ("tippers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tippers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_torpedoed_torpedoes_4071',
    name: 'torpedoed vs torpedoes',
    description: 'Commonly confused words: torpedoed and torpedoes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btorpedoed\b/gi,
    message: 'Check: "torpedoed" ("torpedoed") vs "torpedoes" ("torpedoes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'torpedoes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_torqued_torques_4072',
    name: 'torqued vs torques',
    description: 'Commonly confused words: torqued and torques',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btorqued\b/gi,
    message: 'Check: "torqued" ("torqued") vs "torques" ("torques"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'torques';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tor_toe_4073',
    name: 'tor vs toe',
    description: 'Commonly confused words: tor and toe',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btor\b/gi,
    message: 'Check: "tor" ("tor") vs "toe" ("toe"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toe';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tosh_gosh_4074',
    name: 'tosh vs gosh',
    description: 'Commonly confused words: tosh and gosh',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btosh\b/gi,
    message: 'Check: "tosh" ("tosh") vs "gosh" ("gosh"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gosh';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tote_rote_4075',
    name: 'tote vs rote',
    description: 'Commonly confused words: tote and rote',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btote\b/gi,
    message: 'Check: "tote" ("tote") vs "rote" ("rote"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rote';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_totters_titters_4076',
    name: 'totters vs titters',
    description: 'Commonly confused words: totters and titters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btotters\b/gi,
    message: 'Check: "totters" ("totters") vs "titters" ("titters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'titters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_totter_titter_4077',
    name: 'totter vs titter',
    description: 'Commonly confused words: totter and titter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btotter\b/gi,
    message: 'Check: "totter" ("totter") vs "titter" ("titter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'titter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_totting_rotting_4078',
    name: 'totting vs rotting',
    description: 'Commonly confused words: totting and rotting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btotting\b/gi,
    message: 'Check: "totting" ("totting") vs "rotting" ("rotting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rotting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toured_touted_4079',
    name: 'toured vs touted',
    description: 'Commonly confused words: toured and touted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoured\b/gi,
    message: 'Check: "toured" ("toured") vs "touted" ("touted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'touted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_touted_routed_4080',
    name: 'touted vs routed',
    description: 'Commonly confused words: touted and routed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btouted\b/gi,
    message: 'Check: "touted" ("touted") vs "routed" ("routed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'routed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tout_gout_4081',
    name: 'tout vs gout',
    description: 'Commonly confused words: tout and gout',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btout\b/gi,
    message: 'Check: "tout" ("tout") vs "gout" ("gout"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gout';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tout_rout_4082',
    name: 'tout vs rout',
    description: 'Commonly confused words: tout and rout',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btout\b/gi,
    message: 'Check: "tout" ("tout") vs "rout" ("rout"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rout';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_towed_rowed_4083',
    name: 'towed vs rowed',
    description: 'Commonly confused words: towed and rowed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btowed\b/gi,
    message: 'Check: "towed" ("towed") vs "rowed" ("rowed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rowed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toyed_toted_4084',
    name: 'toyed vs toted',
    description: 'Commonly confused words: toyed and toted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoyed\b/gi,
    message: 'Check: "toyed" ("toyed") vs "toted" ("toted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_toying_toting_4085',
    name: 'toying vs toting',
    description: 'Commonly confused words: toying and toting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btoying\b/gi,
    message: 'Check: "toying" ("toying") vs "toting" ("toting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_traced_traces_4086',
    name: 'traced vs traces',
    description: 'Commonly confused words: traced and traces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btraced\b/gi,
    message: 'Check: "traced" ("traced") vs "traces" ("traces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'traces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_traced_traded_4087',
    name: 'traced vs traded',
    description: 'Commonly confused words: traced and traded',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btraced\b/gi,
    message: 'Check: "traced" ("traced") vs "traded" ("traded"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'traded';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_traces_graces_4088',
    name: 'traces vs graces',
    description: 'Commonly confused words: traces and graces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btraces\b/gi,
    message: 'Check: "traces" ("traces") vs "graces" ("graces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'graces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trade_grade_4089',
    name: 'trade vs grade',
    description: 'Commonly confused words: trade and grade',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrade\b/gi,
    message: 'Check: "trade" ("trade") vs "grade" ("grade"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grade';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trades_grades_4090',
    name: 'trades vs grades',
    description: 'Commonly confused words: trades and grades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrades\b/gi,
    message: 'Check: "trades" ("trades") vs "grades" ("grades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trades_traces_4091',
    name: 'trades vs traces',
    description: 'Commonly confused words: trades and traces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrades\b/gi,
    message: 'Check: "trades" ("trades") vs "traces" ("traces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'traces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trampled_tramples_4092',
    name: 'trampled vs tramples',
    description: 'Commonly confused words: trampled and tramples',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrampled\b/gi,
    message: 'Check: "trampled" ("trampled") vs "tramples" ("tramples"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tramples';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_transfigured_transfigures_4093',
    name: 'transfigured vs transfigures',
    description: 'Commonly confused words: transfigured and transfigures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btransfigured\b/gi,
    message: 'Check: "transfigured" ("transfigured") vs "transfigures" ("transfigures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'transfigures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_transgressed_transgresses_4094',
    name: 'transgressed vs transgresses',
    description: 'Commonly confused words: transgressed and transgresses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btransgressed\b/gi,
    message: 'Check: "transgressed" ("transgressed") vs "transgresses" ("transgresses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'transgresses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_translated_translates_4095',
    name: 'translated vs translates',
    description: 'Commonly confused words: translated and translates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btranslated\b/gi,
    message: 'Check: "translated" ("translated") vs "translates" ("translates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'translates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_transmuted_transmutes_4096',
    name: 'transmuted vs transmutes',
    description: 'Commonly confused words: transmuted and transmutes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btransmuted\b/gi,
    message: 'Check: "transmuted" ("transmuted") vs "transmutes" ("transmutes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'transmutes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_transpired_transpires_4097',
    name: 'transpired vs transpires',
    description: 'Commonly confused words: transpired and transpires',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btranspired\b/gi,
    message: 'Check: "transpired" ("transpired") vs "transpires" ("transpires"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'transpires';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_traumatized_traumatizes_4098',
    name: 'traumatized vs traumatizes',
    description: 'Commonly confused words: traumatized and traumatizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btraumatized\b/gi,
    message: 'Check: "traumatized" ("traumatized") vs "traumatizes" ("traumatizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'traumatizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_traversed_traverses_4099',
    name: 'traversed vs traverses',
    description: 'Commonly confused words: traversed and traverses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btraversed\b/gi,
    message: 'Check: "traversed" ("traversed") vs "traverses" ("traverses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'traverses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_travestied_travesties_4100',
    name: 'travestied vs travesties',
    description: 'Commonly confused words: travestied and travesties',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btravestied\b/gi,
    message: 'Check: "travestied" ("travestied") vs "travesties" ("travesties"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'travesties';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_treacle_treadle_4101',
    name: 'treacle vs treadle',
    description: 'Commonly confused words: treacle and treadle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btreacle\b/gi,
    message: 'Check: "treacle" ("treacle") vs "treadle" ("treadle"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'treadle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_treasured_treasures_4102',
    name: 'treasured vs treasures',
    description: 'Commonly confused words: treasured and treasures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btreasured\b/gi,
    message: 'Check: "treasured" ("treasured") vs "treasures" ("treasures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'treasures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trebled_trebles_4103',
    name: 'trebled vs trebles',
    description: 'Commonly confused words: trebled and trebles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrebled\b/gi,
    message: 'Check: "trebled" ("trebled") vs "trebles" ("trebles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trebles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trellised_trellises_4104',
    name: 'trellised vs trellises',
    description: 'Commonly confused words: trellised and trellises',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrellised\b/gi,
    message: 'Check: "trellised" ("trellised") vs "trellises" ("trellises"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trellises';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trenched_trenches_4105',
    name: 'trenched vs trenches',
    description: 'Commonly confused words: trenched and trenches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrenched\b/gi,
    message: 'Check: "trenched" ("trenched") vs "trenches" ("trenches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trenches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trespassed_trespasses_4106',
    name: 'trespassed vs trespasses',
    description: 'Commonly confused words: trespassed and trespasses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrespassed\b/gi,
    message: 'Check: "trespassed" ("trespassed") vs "trespasses" ("trespasses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trespasses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_triangulated_triangulates_4107',
    name: 'triangulated vs triangulates',
    description: 'Commonly confused words: triangulated and triangulates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btriangulated\b/gi,
    message: 'Check: "triangulated" ("triangulated") vs "triangulates" ("triangulates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'triangulates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tricked_trucked_4108',
    name: 'tricked vs trucked',
    description: 'Commonly confused words: tricked and trucked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btricked\b/gi,
    message: 'Check: "tricked" ("tricked") vs "trucked" ("trucked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trucked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trickled_trickles_4109',
    name: 'trickled vs trickles',
    description: 'Commonly confused words: trickled and trickles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrickled\b/gi,
    message: 'Check: "trickled" ("trickled") vs "trickles" ("trickles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trickles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trickle_truckle_4110',
    name: 'trickle vs truckle',
    description: 'Commonly confused words: trickle and truckle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrickle\b/gi,
    message: 'Check: "trickle" ("trickle") vs "truckle" ("truckle"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'truckle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tricks_trucks_4111',
    name: 'tricks vs trucks',
    description: 'Commonly confused words: tricks and trucks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btricks\b/gi,
    message: 'Check: "tricks" ("tricks") vs "trucks" ("trucks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trucks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trifled_trifles_4112',
    name: 'trifled vs trifles',
    description: 'Commonly confused words: trifled and trifles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrifled\b/gi,
    message: 'Check: "trifled" ("trifled") vs "trifles" ("trifles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trifles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trig_trug_4113',
    name: 'trig vs trug',
    description: 'Commonly confused words: trig and trug',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrig\b/gi,
    message: 'Check: "trig" ("trig") vs "trug" ("trug"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trug';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trim_grim_4114',
    name: 'trim vs grim',
    description: 'Commonly confused words: trim and grim',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrim\b/gi,
    message: 'Check: "trim" ("trim") vs "grim" ("grim"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grim';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tripe_gripe_4115',
    name: 'tripe vs gripe',
    description: 'Commonly confused words: tripe and gripe',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btripe\b/gi,
    message: 'Check: "tripe" ("tripe") vs "gripe" ("gripe"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gripe';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tripled_triples_4116',
    name: 'tripled vs triples',
    description: 'Commonly confused words: tripled and triples',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btripled\b/gi,
    message: 'Check: "tripled" ("tripled") vs "triples" ("triples"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'triples';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tripod_tripos_4117',
    name: 'tripod vs tripos',
    description: 'Commonly confused words: tripod and tripos',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btripod\b/gi,
    message: 'Check: "tripod" ("tripod") vs "tripos" ("tripos"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tripos';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trope_tripe_4118',
    name: 'trope vs tripe',
    description: 'Commonly confused words: trope and tripe',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrope\b/gi,
    message: 'Check: "trope" ("trope") vs "tripe" ("tripe"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tripe';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trots_troys_4119',
    name: 'trots vs troys',
    description: 'Commonly confused words: trots and troys',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrots\b/gi,
    message: 'Check: "trots" ("trots") vs "troys" ("troys"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'troys';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_troubled_troubles_4120',
    name: 'troubled vs troubles',
    description: 'Commonly confused words: troubled and troubles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btroubled\b/gi,
    message: 'Check: "troubled" ("troubled") vs "troubles" ("troubles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'troubles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trounced_trounces_4121',
    name: 'trounced vs trounces',
    description: 'Commonly confused words: trounced and trounces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrounced\b/gi,
    message: 'Check: "trounced" ("trounced") vs "trounces" ("trounces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trounces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trows_grows_4122',
    name: 'trows vs grows',
    description: 'Commonly confused words: trows and grows',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrows\b/gi,
    message: 'Check: "trows" ("trows") vs "grows" ("grows"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grows';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_troy_trot_4123',
    name: 'troy vs trot',
    description: 'Commonly confused words: troy and trot',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btroy\b/gi,
    message: 'Check: "troy" ("troy") vs "trot" ("trot"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trot';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_truce_trice_4124',
    name: 'truce vs trice',
    description: 'Commonly confused words: truce and trice',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btruce\b/gi,
    message: 'Check: "truce" ("truce") vs "trice" ("trice"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trice';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_truer_trier_4125',
    name: 'truer vs trier',
    description: 'Commonly confused words: truer and trier',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btruer\b/gi,
    message: 'Check: "truer" ("truer") vs "trier" ("trier"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trier';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_truncated_truncates_4126',
    name: 'truncated vs truncates',
    description: 'Commonly confused words: truncated and truncates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btruncated\b/gi,
    message: 'Check: "truncated" ("truncated") vs "truncates" ("truncates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'truncates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trundled_trundles_4127',
    name: 'trundled vs trundles',
    description: 'Commonly confused words: trundled and trundles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrundled\b/gi,
    message: 'Check: "trundled" ("trundled") vs "trundles" ("trundles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trundles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trussed_trusses_4128',
    name: 'trussed vs trusses',
    description: 'Commonly confused words: trussed and trusses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrussed\b/gi,
    message: 'Check: "trussed" ("trussed") vs "trusses" ("trusses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trusses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trusted_trustee_4129',
    name: 'trusted vs trustee',
    description: 'Commonly confused words: trusted and trustee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrusted\b/gi,
    message: 'Check: "trusted" ("trusted") vs "trustee" ("trustee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trustee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_trusting_trysting_4130',
    name: 'trusting vs trysting',
    description: 'Commonly confused words: trusting and trysting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btrusting\b/gi,
    message: 'Check: "trusting" ("trusting") vs "trysting" ("trysting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'trysting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tsp_tap_4131',
    name: 'tsp vs tap',
    description: 'Commonly confused words: tsp and tap',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btsp\b/gi,
    message: 'Check: "tsp" ("tsp") vs "tap" ("tap"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tap';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tuba_tubs_4132',
    name: 'tuba vs tubs',
    description: 'Commonly confused words: tuba and tubs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btuba\b/gi,
    message: 'Check: "tuba" ("tuba") vs "tubs" ("tubs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tubs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tuba_tuna_4133',
    name: 'tuba vs tuna',
    description: 'Commonly confused words: tuba and tuna',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btuba\b/gi,
    message: 'Check: "tuba" ("tuba") vs "tuna" ("tuna"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tuna';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tubed_tuned_4134',
    name: 'tubed vs tuned',
    description: 'Commonly confused words: tubed and tuned',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btubed\b/gi,
    message: 'Check: "tubed" ("tubed") vs "tuned" ("tuned"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tuned';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tubing_tuning_4135',
    name: 'tubing vs tuning',
    description: 'Commonly confused words: tubing and tuning',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btubing\b/gi,
    message: 'Check: "tubing" ("tubing") vs "tuning" ("tuning"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tuning';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tub_rub_4136',
    name: 'tub vs rub',
    description: 'Commonly confused words: tub and rub',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btub\b/gi,
    message: 'Check: "tub" ("tub") vs "rub" ("rub"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rub';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tuckers_tickers_4137',
    name: 'tuckers vs tickers',
    description: 'Commonly confused words: tuckers and tickers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btuckers\b/gi,
    message: 'Check: "tuckers" ("tuckers") vs "tickers" ("tickers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tickers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tucking_rucking_4138',
    name: 'tucking vs rucking',
    description: 'Commonly confused words: tucking and rucking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btucking\b/gi,
    message: 'Check: "tucking" ("tucking") vs "rucking" ("rucking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rucking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tucking_ticking_4139',
    name: 'tucking vs ticking',
    description: 'Commonly confused words: tucking and ticking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btucking\b/gi,
    message: 'Check: "tucking" ("tucking") vs "ticking" ("ticking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ticking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tugged_rugged_4140',
    name: 'tugged vs rugged',
    description: 'Commonly confused words: tugged and rugged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btugged\b/gi,
    message: 'Check: "tugged" ("tugged") vs "rugged" ("rugged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rugged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tumbled_rumbled_4141',
    name: 'tumbled vs rumbled',
    description: 'Commonly confused words: tumbled and rumbled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btumbled\b/gi,
    message: 'Check: "tumbled" ("tumbled") vs "rumbled" ("rumbled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rumbled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tums_gums_4142',
    name: 'tums vs gums',
    description: 'Commonly confused words: tums and gums',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btums\b/gi,
    message: 'Check: "tums" ("tums") vs "gums" ("gums"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gums';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tuned_tunes_4143',
    name: 'tuned vs tunes',
    description: 'Commonly confused words: tuned and tunes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btuned\b/gi,
    message: 'Check: "tuned" ("tuned") vs "tunes" ("tunes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tunes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tuner_tuber_4144',
    name: 'tuner vs tuber',
    description: 'Commonly confused words: tuner and tuber',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btuner\b/gi,
    message: 'Check: "tuner" ("tuner") vs "tuber" ("tuber"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tuber';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tune_tube_4145',
    name: 'tune vs tube',
    description: 'Commonly confused words: tune and tube',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btune\b/gi,
    message: 'Check: "tune" ("tune") vs "tube" ("tube"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tube';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_turbidity_turgidity_4146',
    name: 'turbidity vs turgidity',
    description: 'Commonly confused words: turbidity and turgidity',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bturbidity\b/gi,
    message: 'Check: "turbidity" ("turbidity") vs "turgidity" ("turgidity"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'turgidity';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_turbid_turgid_4147',
    name: 'turbid vs turgid',
    description: 'Commonly confused words: turbid and turgid',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bturbid\b/gi,
    message: 'Check: "turbid" ("turbid") vs "turgid" ("turgid"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'turgid';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_tussled_tussles_4148',
    name: 'tussled vs tussles',
    description: 'Commonly confused words: tussled and tussles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btussled\b/gi,
    message: 'Check: "tussled" ("tussled") vs "tussles" ("tussles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tussles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_twas_teas_4149',
    name: 'twas vs teas',
    description: 'Commonly confused words: twas and teas',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btwas\b/gi,
    message: 'Check: "twas" ("twas") vs "teas" ("teas"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'teas';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_twined_twines_4150',
    name: 'twined vs twines',
    description: 'Commonly confused words: twined and twines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btwined\b/gi,
    message: 'Check: "twined" ("twined") vs "twines" ("twines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'twines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_twinkled_twinkles_4151',
    name: 'twinkled vs twinkles',
    description: 'Commonly confused words: twinkled and twinkles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btwinkled\b/gi,
    message: 'Check: "twinkled" ("twinkled") vs "twinkles" ("twinkles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'twinkles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_twp_tsp_4152',
    name: 'twp vs tsp',
    description: 'Commonly confused words: twp and tsp',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btwp\b/gi,
    message: 'Check: "twp" ("twp") vs "tsp" ("tsp"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tsp';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_typed_types_4153',
    name: 'typed vs types',
    description: 'Commonly confused words: typed and types',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btyped\b/gi,
    message: 'Check: "typed" ("typed") vs "types" ("types"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'types';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_typified_typifies_4154',
    name: 'typified vs typifies',
    description: 'Commonly confused words: typified and typifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\btypified\b/gi,
    message: 'Check: "typified" ("typified") vs "typifies" ("typifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'typifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
