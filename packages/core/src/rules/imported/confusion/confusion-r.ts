/**
 * Auto-generated confusion rules (R)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.494Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesR: GrammarRule[] = [
  {
    id: 'confusion_respects_aspects_46',
    name: 'respects vs aspects',
    description: 'Commonly confused words: respects and aspects',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brespects\b/gi,
    message: 'Did you mean "aspects"? "respects" ("respects") is often confused with "aspects" ("aspects").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'aspects';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_racking_wracking_552',
    name: 'racking vs wracking',
    description: 'Commonly confused words: racking and wracking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bracking\b/gi,
    message: 'Check: "racking" ("racking") vs "wracking" ("wracking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wracking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_raise_rays_553',
    name: 'raise vs rays',
    description: 'Commonly confused words: raise and rays',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\braise\b/gi,
    message: 'Check: "raise" ("raise") vs "rays" ("rays"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rays';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rampant_rampart_554',
    name: 'rampant vs rampart',
    description: 'Commonly confused words: rampant and rampart',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brampant\b/gi,
    message: 'Check: "rampant" ("rampant") vs "rampart" ("rampart"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rampart';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rap_wrap_555',
    name: 'rap vs wrap',
    description: 'Commonly confused words: rap and wrap',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brap\b/gi,
    message: 'Check: "rap" ("rap") vs "wrap" ("wrap"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wrap';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_raps_wraps_556',
    name: 'raps vs wraps',
    description: 'Commonly confused words: raps and wraps',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\braps\b/gi,
    message: 'Check: "raps" ("raps") vs "wraps" ("wraps"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wraps';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rapt_wrapped_557',
    name: 'rapt vs wrapped',
    description: 'Commonly confused words: rapt and wrapped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brapt\b/gi,
    message: 'Check: "rapt" ("rapt") vs "wrapped" ("wrapped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wrapped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ray_re_558',
    name: 'ray vs re',
    description: 'Commonly confused words: ray and re',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bray\b/gi,
    message: 'Check: "ray" ("ray") vs "re" ("re"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 're';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reach_rich_559',
    name: 'reach vs rich',
    description: 'Commonly confused words: reach and rich',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breach\b/gi,
    message: 'Check: "reach" ("reach") vs "rich" ("rich"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rich';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reaches_riches_560',
    name: 'reaches vs riches',
    description: 'Commonly confused words: reaches and riches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breaches\b/gi,
    message: 'Check: "reaches" ("reaches") vs "riches" ("riches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'riches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_read_red_561',
    name: 'read vs red',
    description: 'Commonly confused words: read and red',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bread\b/gi,
    message: 'Check: "read" ("read") vs "red" ("red"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'red';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reads_reeds_562',
    name: 'reads vs reeds',
    description: 'Commonly confused words: reads and reeds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breads\b/gi,
    message: 'Check: "reads" ("reads") vs "reeds" ("reeds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reeds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_real_reel_563',
    name: 'real vs reel',
    description: 'Commonly confused words: real and reel',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breal\b/gi,
    message: 'Check: "real" ("real") vs "reel" ("reel"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reel';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_recede_reseed_564',
    name: 'recede vs reseed',
    description: 'Commonly confused words: recede and reseed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brecede\b/gi,
    message: 'Check: "recede" ("recede") vs "reseed" ("reseed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reseed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reeked_wreaked_565',
    name: 'reeked vs wreaked',
    description: 'Commonly confused words: reeked and wreaked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breeked\b/gi,
    message: 'Check: "reeked" ("reeked") vs "wreaked" ("wreaked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wreaked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reek_wreak_566',
    name: 'reek vs wreak',
    description: 'Commonly confused words: reek and wreak',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breek\b/gi,
    message: 'Check: "reek" ("reek") vs "wreak" ("wreak"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wreak';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rested_wrested_567',
    name: 'rested vs wrested',
    description: 'Commonly confused words: rested and wrested',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brested\b/gi,
    message: 'Check: "rested" ("rested") vs "wrested" ("wrested"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wrested';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_review_revue_568',
    name: 'review vs revue',
    description: 'Commonly confused words: review and revue',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breview\b/gi,
    message: 'Check: "review" ("review") vs "revue" ("revue"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'revue';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rigger_rigor_569',
    name: 'rigger vs rigor',
    description: 'Commonly confused words: rigger and rigor',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brigger\b/gi,
    message: 'Check: "rigger" ("rigger") vs "rigor" ("rigor"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rigor';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_riggers_rigors_570',
    name: 'riggers vs rigors',
    description: 'Commonly confused words: riggers and rigors',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\briggers\b/gi,
    message: 'Check: "riggers" ("riggers") vs "rigors" ("rigors"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rigors';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_right_write_571',
    name: 'right vs write',
    description: 'Commonly confused words: right and write',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bright\b/gi,
    message: 'Check: "right" (correct or direction) vs "write" (to put words on paper). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'write';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_right_rite_572',
    name: 'right vs rite',
    description: 'Commonly confused words: right and rite',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bright\b/gi,
    message: 'Check: "right" (correct or direction) vs "rite" ("rite"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rite';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rights_writes_573',
    name: 'rights vs writes',
    description: 'Commonly confused words: rights and writes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brights\b/gi,
    message: 'Check: "rights" ("rights") vs "writes" ("writes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'writes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ringing_wringing_574',
    name: 'ringing vs wringing',
    description: 'Commonly confused words: ringing and wringing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bringing\b/gi,
    message: 'Check: "ringing" ("ringing") vs "wringing" ("wringing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wringing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rite_wright_575',
    name: 'rite vs wright',
    description: 'Commonly confused words: rite and wright',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brite\b/gi,
    message: 'Check: "rite" ("rite") vs "wright" ("wright"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wright';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rites_writes_576',
    name: 'rites vs writes',
    description: 'Commonly confused words: rites and writes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brites\b/gi,
    message: 'Check: "rites" ("rites") vs "writes" ("writes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'writes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_role_roll_577',
    name: 'role vs roll',
    description: 'Commonly confused words: role and roll',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brole\b/gi,
    message: 'Check: "role" (part played) vs "roll" (to turn over). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'roll';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_roles_rolls_578',
    name: 'roles vs rolls',
    description: 'Commonly confused words: roles and rolls',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broles\b/gi,
    message: 'Check: "roles" ("roles") vs "rolls" ("rolls"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rolls';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rooted_routed_579',
    name: 'rooted vs routed',
    description: 'Commonly confused words: rooted and routed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brooted\b/gi,
    message: 'Check: "rooted" ("rooted") vs "routed" ("routed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'routed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_root_route_580',
    name: 'root vs route',
    description: 'Commonly confused words: root and route',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broot\b/gi,
    message: 'Check: "root" ("root") vs "route" ("route"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'route';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rose_rows_581',
    name: 'rose vs rows',
    description: 'Commonly confused words: rose and rows',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brose\b/gi,
    message: 'Check: "rose" ("rose") vs "rows" ("rows"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rows';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rote_wrote_582',
    name: 'rote vs wrote',
    description: 'Commonly confused words: rote and wrote',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brote\b/gi,
    message: 'Check: "rote" ("rote") vs "wrote" ("wrote"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wrote';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rot_wrought_583',
    name: 'rot vs wrought',
    description: 'Commonly confused words: rot and wrought',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brot\b/gi,
    message: 'Check: "rot" ("rot") vs "wrought" ("wrought"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'wrought';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_roux_rue_584',
    name: 'roux vs rue',
    description: 'Commonly confused words: roux and rue',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broux\b/gi,
    message: 'Check: "roux" ("roux") vs "rue" ("rue"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rue';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rude_rued_585',
    name: 'rude vs rued',
    description: 'Commonly confused words: rude and rued',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brude\b/gi,
    message: 'Check: "rude" ("rude") vs "rued" ("rued"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rued';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_racially_radially_3270',
    name: 'racially vs radially',
    description: 'Commonly confused words: racially and radially',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bracially\b/gi,
    message: 'Check: "racially" ("racially") vs "radially" ("radially"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'radially';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_racial_radial_3271',
    name: 'racial vs radial',
    description: 'Commonly confused words: racial and radial',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bracial\b/gi,
    message: 'Check: "racial" ("racial") vs "radial" ("radial"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'radial';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_racing_facing_3272',
    name: 'racing vs facing',
    description: 'Commonly confused words: racing and facing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bracing\b/gi,
    message: 'Check: "racing" ("racing") vs "facing" ("facing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'facing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_radiated_radiates_3273',
    name: 'radiated vs radiates',
    description: 'Commonly confused words: radiated and radiates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bradiated\b/gi,
    message: 'Check: "radiated" ("radiated") vs "radiates" ("radiates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'radiates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rads_fads_3274',
    name: 'rads vs fads',
    description: 'Commonly confused words: rads and fads',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brads\b/gi,
    message: 'Check: "rads" ("rads") vs "fads" ("fads"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fads';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_raider_raiser_3275',
    name: 'raider vs raiser',
    description: 'Commonly confused words: raider and raiser',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\braider\b/gi,
    message: 'Check: "raider" ("raider") vs "raiser" ("raiser"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'raiser';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_raiders_raisers_3276',
    name: 'raiders vs raisers',
    description: 'Commonly confused words: raiders and raisers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\braiders\b/gi,
    message: 'Check: "raiders" ("raiders") vs "raisers" ("raisers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'raisers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rail_fail_3277',
    name: 'rail vs fail',
    description: 'Commonly confused words: rail and fail',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brail\b/gi,
    message: 'Check: "rail" ("rail") vs "fail" ("fail"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fail';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_railing_failing_3278',
    name: 'railing vs failing',
    description: 'Commonly confused words: railing and failing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brailing\b/gi,
    message: 'Check: "railing" ("railing") vs "failing" ("failing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'failing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_raked_faked_3279',
    name: 'raked vs faked',
    description: 'Commonly confused words: raked and faked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\braked\b/gi,
    message: 'Check: "raked" ("raked") vs "faked" ("faked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'faked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rake_fake_3280',
    name: 'rake vs fake',
    description: 'Commonly confused words: rake and fake',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brake\b/gi,
    message: 'Check: "rake" ("rake") vs "fake" ("fake"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fake';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_raking_faking_3281',
    name: 'raking vs faking',
    description: 'Commonly confused words: raking and faking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\braking\b/gi,
    message: 'Check: "raking" ("raking") vs "faking" ("faking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'faking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rallied_rallies_3282',
    name: 'rallied vs rallies',
    description: 'Commonly confused words: rallied and rallies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brallied\b/gi,
    message: 'Check: "rallied" ("rallied") vs "rallies" ("rallies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rallies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rambled_rambles_3283',
    name: 'rambled vs rambles',
    description: 'Commonly confused words: rambled and rambles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brambled\b/gi,
    message: 'Check: "rambled" ("rambled") vs "rambles" ("rambles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rambles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ramified_ramifies_3284',
    name: 'ramified vs ramifies',
    description: 'Commonly confused words: ramified and ramifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bramified\b/gi,
    message: 'Check: "ramified" ("ramified") vs "ramifies" ("ramifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ramifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ranched_ranches_3285',
    name: 'ranched vs ranches',
    description: 'Commonly confused words: ranched and ranches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\branched\b/gi,
    message: 'Check: "ranched" ("ranched") vs "ranches" ("ranches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ranches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_randomized_randomizes_3286',
    name: 'randomized vs randomizes',
    description: 'Commonly confused words: randomized and randomizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brandomized\b/gi,
    message: 'Check: "randomized" ("randomized") vs "randomizes" ("randomizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'randomizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rangy_tangy_3287',
    name: 'rangy vs tangy',
    description: 'Commonly confused words: rangy and tangy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brangy\b/gi,
    message: 'Check: "rangy" ("rangy") vs "tangy" ("tangy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tangy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rankled_rankles_3288',
    name: 'rankled vs rankles',
    description: 'Commonly confused words: rankled and rankles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brankled\b/gi,
    message: 'Check: "rankled" ("rankled") vs "rankles" ("rankles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rankles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ranted_ranged_3289',
    name: 'ranted vs ranged',
    description: 'Commonly confused words: ranted and ranged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\branted\b/gi,
    message: 'Check: "ranted" ("ranted") vs "ranged" ("ranged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ranged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ranting_ranging_3290',
    name: 'ranting vs ranging',
    description: 'Commonly confused words: ranting and ranging',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\branting\b/gi,
    message: 'Check: "ranting" ("ranting") vs "ranging" ("ranging"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ranging';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_raring_faring_3291',
    name: 'raring vs faring',
    description: 'Commonly confused words: raring and faring',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\braring\b/gi,
    message: 'Check: "raring" ("raring") vs "faring" ("faring"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'faring';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rated_fated_3292',
    name: 'rated vs fated',
    description: 'Commonly confused words: rated and fated',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brated\b/gi,
    message: 'Check: "rated" ("rated") vs "fated" ("fated"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fated';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rate_fate_3293',
    name: 'rate vs fate',
    description: 'Commonly confused words: rate and fate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brate\b/gi,
    message: 'Check: "rate" ("rate") vs "fate" ("fate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rater_eater_3294',
    name: 'rater vs eater',
    description: 'Commonly confused words: rater and eater',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brater\b/gi,
    message: 'Check: "rater" ("rater") vs "eater" ("eater"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eater';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_raters_eaters_3295',
    name: 'raters vs eaters',
    description: 'Commonly confused words: raters and eaters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\braters\b/gi,
    message: 'Check: "raters" ("raters") vs "eaters" ("eaters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eaters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rather_father_3296',
    name: 'rather vs father',
    description: 'Commonly confused words: rather and father',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brather\b/gi,
    message: 'Check: "rather" ("rather") vs "father" ("father"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'father';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rating_eating_3297',
    name: 'rating vs eating',
    description: 'Commonly confused words: rating and eating',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brating\b/gi,
    message: 'Check: "rating" ("rating") vs "eating" ("eating"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eating';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rationalized_rationalizes_3298',
    name: 'rationalized vs rationalizes',
    description: 'Commonly confused words: rationalized and rationalizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brationalized\b/gi,
    message: 'Check: "rationalized" ("rationalized") vs "rationalizes" ("rationalizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rationalizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rats_eats_3299',
    name: 'rats vs eats',
    description: 'Commonly confused words: rats and eats',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brats\b/gi,
    message: 'Check: "rats" ("rats") vs "eats" ("eats"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eats';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rats_fats_3300',
    name: 'rats vs fats',
    description: 'Commonly confused words: rats and fats',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brats\b/gi,
    message: 'Check: "rats" ("rats") vs "fats" ("fats"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fats';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ratter_fatter_3301',
    name: 'ratter vs fatter',
    description: 'Commonly confused words: ratter and fatter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bratter\b/gi,
    message: 'Check: "ratter" ("ratter") vs "fatter" ("fatter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fatter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rattles_tattles_3302',
    name: 'rattles vs tattles',
    description: 'Commonly confused words: rattles and tattles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brattles\b/gi,
    message: 'Check: "rattles" ("rattles") vs "tattles" ("tattles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tattles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rattling_tattling_3303',
    name: 'rattling vs tattling',
    description: 'Commonly confused words: rattling and tattling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brattling\b/gi,
    message: 'Check: "rattling" ("rattling") vs "tattling" ("tattling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tattling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ravaged_ravages_3304',
    name: 'ravaged vs ravages',
    description: 'Commonly confused words: ravaged and ravages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bravaged\b/gi,
    message: 'Check: "ravaged" ("ravaged") vs "ravages" ("ravages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ravages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_razed_fazed_3305',
    name: 'razed vs fazed',
    description: 'Commonly confused words: razed and fazed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brazed\b/gi,
    message: 'Check: "razed" ("razed") vs "fazed" ("fazed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fazed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reachable_teachable_3306',
    name: 'reachable vs teachable',
    description: 'Commonly confused words: reachable and teachable',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breachable\b/gi,
    message: 'Check: "reachable" ("reachable") vs "teachable" ("teachable"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'teachable';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reaching_teaching_3307',
    name: 'reaching vs teaching',
    description: 'Commonly confused words: reaching and teaching',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breaching\b/gi,
    message: 'Check: "reaching" ("reaching") vs "teaching" ("teaching"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'teaching';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reacquired_reacquires_3308',
    name: 'reacquired vs reacquires',
    description: 'Commonly confused words: reacquired and reacquires',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breacquired\b/gi,
    message: 'Check: "reacquired" ("reacquired") vs "reacquires" ("reacquires"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reacquires';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_readied_readies_3309',
    name: 'readied vs readies',
    description: 'Commonly confused words: readied and readies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breadied\b/gi,
    message: 'Check: "readied" ("readied") vs "readies" ("readies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'readies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reared_teared_3310',
    name: 'reared vs teared',
    description: 'Commonly confused words: reared and teared',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breared\b/gi,
    message: 'Check: "reared" ("reared") vs "teared" ("teared"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'teared';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rear_fear_3311',
    name: 'rear vs fear',
    description: 'Commonly confused words: rear and fear',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brear\b/gi,
    message: 'Check: "rear" ("rear") vs "fear" ("fear"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fear';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rearranged_rearranges_3312',
    name: 'rearranged vs rearranges',
    description: 'Commonly confused words: rearranged and rearranges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brearranged\b/gi,
    message: 'Check: "rearranged" ("rearranged") vs "rearranges" ("rearranges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rearranges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reassembled_reassembles_3313',
    name: 'reassembled vs reassembles',
    description: 'Commonly confused words: reassembled and reassembles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breassembled\b/gi,
    message: 'Check: "reassembled" ("reassembled") vs "reassembles" ("reassembles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reassembles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reassessed_reassesses_3314',
    name: 'reassessed vs reassesses',
    description: 'Commonly confused words: reassessed and reassesses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breassessed\b/gi,
    message: 'Check: "reassessed" ("reassessed") vs "reassesses" ("reassesses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reassesses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reauthorized_reauthorizes_3315',
    name: 'reauthorized vs reauthorizes',
    description: 'Commonly confused words: reauthorized and reauthorizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breauthorized\b/gi,
    message: 'Check: "reauthorized" ("reauthorized") vs "reauthorizes" ("reauthorizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reauthorizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rebated_rebates_3316',
    name: 'rebated vs rebates',
    description: 'Commonly confused words: rebated and rebates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brebated\b/gi,
    message: 'Check: "rebated" ("rebated") vs "rebates" ("rebates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rebates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rebuked_rebukes_3317',
    name: 'rebuked vs rebukes',
    description: 'Commonly confused words: rebuked and rebukes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brebuked\b/gi,
    message: 'Check: "rebuked" ("rebuked") vs "rebukes" ("rebukes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rebukes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_recapitulated_recapitulates_3318',
    name: 'recapitulated vs recapitulates',
    description: 'Commonly confused words: recapitulated and recapitulates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brecapitulated\b/gi,
    message: 'Check: "recapitulated" ("recapitulated") vs "recapitulates" ("recapitulates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'recapitulates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_receded_recedes_3319',
    name: 'receded vs recedes',
    description: 'Commonly confused words: receded and recedes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breceded\b/gi,
    message: 'Check: "receded" ("receded") vs "recedes" ("recedes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'recedes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_recessed_recesses_3320',
    name: 'recessed vs recesses',
    description: 'Commonly confused words: recessed and recesses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brecessed\b/gi,
    message: 'Check: "recessed" ("recessed") vs "recesses" ("recesses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'recesses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_recharged_recharges_3321',
    name: 'recharged vs recharges',
    description: 'Commonly confused words: recharged and recharges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brecharged\b/gi,
    message: 'Check: "recharged" ("recharged") vs "recharges" ("recharges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'recharges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reciprocated_reciprocates_3322',
    name: 'reciprocated vs reciprocates',
    description: 'Commonly confused words: reciprocated and reciprocates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breciprocated\b/gi,
    message: 'Check: "reciprocated" ("reciprocated") vs "reciprocates" ("reciprocates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reciprocates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_recirculated_recirculates_3323',
    name: 'recirculated vs recirculates',
    description: 'Commonly confused words: recirculated and recirculates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brecirculated\b/gi,
    message: 'Check: "recirculated" ("recirculated") vs "recirculates" ("recirculates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'recirculates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reclassified_reclassifies_3324',
    name: 'reclassified vs reclassifies',
    description: 'Commonly confused words: reclassified and reclassifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breclassified\b/gi,
    message: 'Check: "reclassified" ("reclassified") vs "reclassifies" ("reclassifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reclassifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reclining_redlining_3325',
    name: 'reclining vs redlining',
    description: 'Commonly confused words: reclining and redlining',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breclining\b/gi,
    message: 'Check: "reclining" ("reclining") vs "redlining" ("redlining"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'redlining';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_recombined_recombines_3326',
    name: 'recombined vs recombines',
    description: 'Commonly confused words: recombined and recombines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brecombined\b/gi,
    message: 'Check: "recombined" ("recombined") vs "recombines" ("recombines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'recombines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_recommencing_recommending_3327',
    name: 'recommencing vs recommending',
    description: 'Commonly confused words: recommencing and recommending',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brecommencing\b/gi,
    message: 'Check: "recommencing" ("recommencing") vs "recommending" ("recommending"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'recommending';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reconstituted_reconstitutes_3328',
    name: 'reconstituted vs reconstitutes',
    description: 'Commonly confused words: reconstituted and reconstitutes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breconstituted\b/gi,
    message: 'Check: "reconstituted" ("reconstituted") vs "reconstitutes" ("reconstitutes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reconstitutes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_recrossed_recrosses_3329',
    name: 'recrossed vs recrosses',
    description: 'Commonly confused words: recrossed and recrosses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brecrossed\b/gi,
    message: 'Check: "recrossed" ("recrossed") vs "recrosses" ("recrosses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'recrosses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rectified_rectifies_3330',
    name: 'rectified vs rectifies',
    description: 'Commonly confused words: rectified and rectifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brectified\b/gi,
    message: 'Check: "rectified" ("rectified") vs "rectifies" ("rectifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rectifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_redecorated_redecorates_3331',
    name: 'redecorated vs redecorates',
    description: 'Commonly confused words: redecorated and redecorates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bredecorated\b/gi,
    message: 'Check: "redecorated" ("redecorated") vs "redecorates" ("redecorates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'redecorates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rededicated_rededicates_3332',
    name: 'rededicated vs rededicates',
    description: 'Commonly confused words: rededicated and rededicates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brededicated\b/gi,
    message: 'Check: "rededicated" ("rededicated") vs "rededicates" ("rededicates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rededicates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_redefined_redefines_3333',
    name: 'redefined vs redefines',
    description: 'Commonly confused words: redefined and redefines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bredefined\b/gi,
    message: 'Check: "redefined" ("redefined") vs "redefines" ("redefines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'redefines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_redistributed_redistributes_3334',
    name: 'redistributed vs redistributes',
    description: 'Commonly confused words: redistributed and redistributes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bredistributed\b/gi,
    message: 'Check: "redistributed" ("redistributed") vs "redistributes" ("redistributes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'redistributes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_redoubled_redoubles_3335',
    name: 'redoubled vs redoubles',
    description: 'Commonly confused words: redoubled and redoubles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bredoubled\b/gi,
    message: 'Check: "redoubled" ("redoubled") vs "redoubles" ("redoubles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'redoubles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_redressed_redresses_3336',
    name: 'redressed vs redresses',
    description: 'Commonly confused words: redressed and redresses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bredressed\b/gi,
    message: 'Check: "redressed" ("redressed") vs "redresses" ("redresses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'redresses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reed_feed_3337',
    name: 'reed vs feed',
    description: 'Commonly confused words: reed and feed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breed\b/gi,
    message: 'Check: "reed" ("reed") vs "feed" ("feed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'feed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reeds_feeds_3338',
    name: 'reeds vs feeds',
    description: 'Commonly confused words: reeds and feeds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breeds\b/gi,
    message: 'Check: "reeds" ("reeds") vs "feeds" ("feeds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'feeds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reefs_reeds_3339',
    name: 'reefs vs reeds',
    description: 'Commonly confused words: reefs and reeds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breefs\b/gi,
    message: 'Check: "reefs" ("reefs") vs "reeds" ("reeds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reeds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reeked_reeled_3340',
    name: 'reeked vs reeled',
    description: 'Commonly confused words: reeked and reeled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breeked\b/gi,
    message: 'Check: "reeked" ("reeked") vs "reeled" ("reeled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reeled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reeking_reeling_3341',
    name: 'reeking vs reeling',
    description: 'Commonly confused words: reeking and reeling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breeking\b/gi,
    message: 'Check: "reeking" ("reeking") vs "reeling" ("reeling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reeling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reestablished_reestablishes_3342',
    name: 'reestablished vs reestablishes',
    description: 'Commonly confused words: reestablished and reestablishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breestablished\b/gi,
    message: 'Check: "reestablished" ("reestablished") vs "reestablishes" ("reestablishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reestablishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reevaluated_reevaluates_3343',
    name: 'reevaluated vs reevaluates',
    description: 'Commonly confused words: reevaluated and reevaluates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breevaluated\b/gi,
    message: 'Check: "reevaluated" ("reevaluated") vs "reevaluates" ("reevaluates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reevaluates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reexamined_reexamines_3344',
    name: 'reexamined vs reexamines',
    description: 'Commonly confused words: reexamined and reexamines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breexamined\b/gi,
    message: 'Check: "reexamined" ("reexamined") vs "reexamines" ("reexamines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reexamines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_refereed_referees_3345',
    name: 'refereed vs referees',
    description: 'Commonly confused words: refereed and referees',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brefereed\b/gi,
    message: 'Check: "refereed" ("refereed") vs "referees" ("referees"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'referees';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_referenced_reverenced_3346',
    name: 'referenced vs reverenced',
    description: 'Commonly confused words: referenced and reverenced',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breferenced\b/gi,
    message: 'Check: "referenced" ("referenced") vs "reverenced" ("reverenced"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reverenced';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_referential_reverential_3347',
    name: 'referential vs reverential',
    description: 'Commonly confused words: referential and reverential',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breferential\b/gi,
    message: 'Check: "referential" ("referential") vs "reverential" ("reverential"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reverential';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_referent_reverent_3348',
    name: 'referent vs reverent',
    description: 'Commonly confused words: referent and reverent',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breferent\b/gi,
    message: 'Check: "referent" ("referent") vs "reverent" ("reverent"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reverent';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_refers_revers_3349',
    name: 'refers vs revers',
    description: 'Commonly confused words: refers and revers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brefers\b/gi,
    message: 'Check: "refers" ("refers") vs "revers" ("revers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'revers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_refinanced_refinances_3350',
    name: 'refinanced vs refinances',
    description: 'Commonly confused words: refinanced and refinances',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brefinanced\b/gi,
    message: 'Check: "refinanced" ("refinanced") vs "refinances" ("refinances"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'refinances';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_refined_refines_3351',
    name: 'refined vs refines',
    description: 'Commonly confused words: refined and refines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brefined\b/gi,
    message: 'Check: "refined" ("refined") vs "refines" ("refines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'refines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_refocused_refocuses_3352',
    name: 'refocused vs refocuses',
    description: 'Commonly confused words: refocused and refocuses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brefocused\b/gi,
    message: 'Check: "refocused" ("refocused") vs "refocuses" ("refocuses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'refocuses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reformulated_reformulates_3353',
    name: 'reformulated vs reformulates',
    description: 'Commonly confused words: reformulated and reformulates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breformulated\b/gi,
    message: 'Check: "reformulated" ("reformulated") vs "reformulates" ("reformulates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reformulates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_refreshed_refreshes_3354',
    name: 'refreshed vs refreshes',
    description: 'Commonly confused words: refreshed and refreshes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brefreshed\b/gi,
    message: 'Check: "refreshed" ("refreshed") vs "refreshes" ("refreshes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'refreshes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_refuge_refute_3355',
    name: 'refuge vs refute',
    description: 'Commonly confused words: refuge and refute',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brefuge\b/gi,
    message: 'Check: "refuge" ("refuge") vs "refute" ("refute"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'refute';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_refuges_refutes_3356',
    name: 'refuges vs refutes',
    description: 'Commonly confused words: refuges and refutes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brefuges\b/gi,
    message: 'Check: "refuges" ("refuges") vs "refutes" ("refutes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'refutes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_refurbished_refurbishes_3357',
    name: 'refurbished vs refurbishes',
    description: 'Commonly confused words: refurbished and refurbishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brefurbished\b/gi,
    message: 'Check: "refurbished" ("refurbished") vs "refurbishes" ("refurbishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'refurbishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_refuted_refutes_3358',
    name: 'refuted vs refutes',
    description: 'Commonly confused words: refuted and refutes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brefuted\b/gi,
    message: 'Check: "refuted" ("refuted") vs "refutes" ("refutes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'refutes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_regaining_retaining_3359',
    name: 'regaining vs retaining',
    description: 'Commonly confused words: regaining and retaining',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bregaining\b/gi,
    message: 'Check: "regaining" ("regaining") vs "retaining" ("retaining"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'retaining';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_regales_regaled_3360',
    name: 'regales vs regaled',
    description: 'Commonly confused words: regales and regaled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bregales\b/gi,
    message: 'Check: "regales" ("regales") vs "regaled" ("regaled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'regaled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_regarded_retarded_3361',
    name: 'regarded vs retarded',
    description: 'Commonly confused words: regarded and retarded',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bregarded\b/gi,
    message: 'Check: "regarded" ("regarded") vs "retarded" ("retarded"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'retarded';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_regressed_regresses_3362',
    name: 'regressed vs regresses',
    description: 'Commonly confused words: regressed and regresses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bregressed\b/gi,
    message: 'Check: "regressed" ("regressed") vs "regresses" ("regresses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'regresses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_regulated_regulates_3363',
    name: 'regulated vs regulates',
    description: 'Commonly confused words: regulated and regulates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bregulated\b/gi,
    message: 'Check: "regulated" ("regulated") vs "regulates" ("regulates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'regulates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rehabilitated_rehabilitates_3364',
    name: 'rehabilitated vs rehabilitates',
    description: 'Commonly confused words: rehabilitated and rehabilitates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brehabilitated\b/gi,
    message: 'Check: "rehabilitated" ("rehabilitated") vs "rehabilitates" ("rehabilitates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rehabilitates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rehearing_reheating_3365',
    name: 'rehearing vs reheating',
    description: 'Commonly confused words: rehearing and reheating',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brehearing\b/gi,
    message: 'Check: "rehearing" ("rehearing") vs "reheating" ("reheating"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reheating';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rehear_reheat_3366',
    name: 'rehear vs reheat',
    description: 'Commonly confused words: rehear and reheat',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brehear\b/gi,
    message: 'Check: "rehear" ("rehear") vs "reheat" ("reheat"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reheat';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rehearses_rehearsed_3367',
    name: 'rehearses vs rehearsed',
    description: 'Commonly confused words: rehearses and rehearsed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brehearses\b/gi,
    message: 'Check: "rehearses" ("rehearses") vs "rehearsed" ("rehearsed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rehearsed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reimbursed_reimburses_3368',
    name: 'reimbursed vs reimburses',
    description: 'Commonly confused words: reimbursed and reimburses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breimbursed\b/gi,
    message: 'Check: "reimbursed" ("reimbursed") vs "reimburses" ("reimburses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reimburses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reincarnated_reincarnates_3369',
    name: 'reincarnated vs reincarnates',
    description: 'Commonly confused words: reincarnated and reincarnates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breincarnated\b/gi,
    message: 'Check: "reincarnated" ("reincarnated") vs "reincarnates" ("reincarnates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reincarnates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reinforced_reinforces_3370',
    name: 'reinforced vs reinforces',
    description: 'Commonly confused words: reinforced and reinforces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breinforced\b/gi,
    message: 'Check: "reinforced" ("reinforced") vs "reinforces" ("reinforces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reinforces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reintegrated_reintegrates_3371',
    name: 'reintegrated vs reintegrates',
    description: 'Commonly confused words: reintegrated and reintegrates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breintegrated\b/gi,
    message: 'Check: "reintegrated" ("reintegrated") vs "reintegrates" ("reintegrates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reintegrates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reintroduced_reintroduces_3372',
    name: 'reintroduced vs reintroduces',
    description: 'Commonly confused words: reintroduced and reintroduces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breintroduced\b/gi,
    message: 'Check: "reintroduced" ("reintroduced") vs "reintroduces" ("reintroduces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reintroduces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reinvigorated_reinvigorates_3373',
    name: 'reinvigorated vs reinvigorates',
    description: 'Commonly confused words: reinvigorated and reinvigorates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breinvigorated\b/gi,
    message: 'Check: "reinvigorated" ("reinvigorated") vs "reinvigorates" ("reinvigorates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reinvigorates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rejoiced_rejoices_3374',
    name: 'rejoiced vs rejoices',
    description: 'Commonly confused words: rejoiced and rejoices',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brejoiced\b/gi,
    message: 'Check: "rejoiced" ("rejoiced") vs "rejoices" ("rejoices"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rejoices';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rejuvenated_rejuvenates_3375',
    name: 'rejuvenated vs rejuvenates',
    description: 'Commonly confused words: rejuvenated and rejuvenates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brejuvenated\b/gi,
    message: 'Check: "rejuvenated" ("rejuvenated") vs "rejuvenates" ("rejuvenates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rejuvenates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rekindled_rekindles_3376',
    name: 'rekindled vs rekindles',
    description: 'Commonly confused words: rekindled and rekindles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brekindled\b/gi,
    message: 'Check: "rekindled" ("rekindled") vs "rekindles" ("rekindles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rekindles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_relapsed_relapses_3377',
    name: 'relapsed vs relapses',
    description: 'Commonly confused words: relapsed and relapses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brelapsed\b/gi,
    message: 'Check: "relapsed" ("relapsed") vs "relapses" ("relapses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'relapses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_related_relates_3378',
    name: 'related vs relates',
    description: 'Commonly confused words: related and relates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brelated\b/gi,
    message: 'Check: "related" ("related") vs "relates" ("relates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'relates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_released_releases_3379',
    name: 'released vs releases',
    description: 'Commonly confused words: released and releases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breleased\b/gi,
    message: 'Check: "released" ("released") vs "releases" ("releases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'releases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rel_eel_3380',
    name: 'rel vs eel',
    description: 'Commonly confused words: rel and eel',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brel\b/gi,
    message: 'Check: "rel" ("rel") vs "eel" ("eel"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eel';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_relied_relies_3381',
    name: 'relied vs relies',
    description: 'Commonly confused words: relied and relies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brelied\b/gi,
    message: 'Check: "relied" ("relied") vs "relies" ("relies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'relies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_relief_relied_3382',
    name: 'relief vs relied',
    description: 'Commonly confused words: relief and relied',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brelief\b/gi,
    message: 'Check: "relief" ("relief") vs "relied" ("relied"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'relied';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_remonstrated_remonstrates_3383',
    name: 'remonstrated vs remonstrates',
    description: 'Commonly confused words: remonstrated and remonstrates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bremonstrated\b/gi,
    message: 'Check: "remonstrated" ("remonstrated") vs "remonstrates" ("remonstrates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'remonstrates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_render_fender_3384',
    name: 'render vs fender',
    description: 'Commonly confused words: render and fender',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brender\b/gi,
    message: 'Check: "render" ("render") vs "fender" ("fender"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fender';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rend_fend_3385',
    name: 'rend vs fend',
    description: 'Commonly confused words: rend and fend',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brend\b/gi,
    message: 'Check: "rend" ("rend") vs "fend" ("fend"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fend';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rending_fending_3386',
    name: 'rending vs fending',
    description: 'Commonly confused words: rending and fending',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brending\b/gi,
    message: 'Check: "rending" ("rending") vs "fending" ("fending"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fending';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rends_fends_3387',
    name: 'rends vs fends',
    description: 'Commonly confused words: rends and fends',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brends\b/gi,
    message: 'Check: "rends" ("rends") vs "fends" ("fends"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fends';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_renominated_renominates_3388',
    name: 'renominated vs renominates',
    description: 'Commonly confused words: renominated and renominates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brenominated\b/gi,
    message: 'Check: "renominated" ("renominated") vs "renominates" ("renominates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'renominates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_renovated_renovates_3389',
    name: 'renovated vs renovates',
    description: 'Commonly confused words: renovated and renovates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brenovated\b/gi,
    message: 'Check: "renovated" ("renovated") vs "renovates" ("renovates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'renovates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_repackaged_repackages_3390',
    name: 'repackaged vs repackages',
    description: 'Commonly confused words: repackaged and repackages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brepackaged\b/gi,
    message: 'Check: "repackaged" ("repackaged") vs "repackages" ("repackages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'repackages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_repatriated_repatriates_3391',
    name: 'repatriated vs repatriates',
    description: 'Commonly confused words: repatriated and repatriates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brepatriated\b/gi,
    message: 'Check: "repatriated" ("repatriated") vs "repatriates" ("repatriates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'repatriates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rephrased_rephrases_3392',
    name: 'rephrased vs rephrases',
    description: 'Commonly confused words: rephrased and rephrases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brephrased\b/gi,
    message: 'Check: "rephrased" ("rephrased") vs "rephrases" ("rephrases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rephrases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_replenished_replenishes_3393',
    name: 'replenished vs replenishes',
    description: 'Commonly confused words: replenished and replenishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breplenished\b/gi,
    message: 'Check: "replenished" ("replenished") vs "replenishes" ("replenishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'replenishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_replicated_replicates_3394',
    name: 'replicated vs replicates',
    description: 'Commonly confused words: replicated and replicates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breplicated\b/gi,
    message: 'Check: "replicated" ("replicated") vs "replicates" ("replicates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'replicates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reposed_reposes_3395',
    name: 'reposed vs reposes',
    description: 'Commonly confused words: reposed and reposes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breposed\b/gi,
    message: 'Check: "reposed" ("reposed") vs "reposes" ("reposes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reposes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_repossessed_repossesses_3396',
    name: 'repossessed vs repossesses',
    description: 'Commonly confused words: repossessed and repossesses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brepossessed\b/gi,
    message: 'Check: "repossessed" ("repossessed") vs "repossesses" ("repossesses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'repossesses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reprieved_reprieves_3397',
    name: 'reprieved vs reprieves',
    description: 'Commonly confused words: reprieved and reprieves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breprieved\b/gi,
    message: 'Check: "reprieved" ("reprieved") vs "reprieves" ("reprieves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reprieves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reproached_reproaches_3398',
    name: 'reproached vs reproaches',
    description: 'Commonly confused words: reproached and reproaches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breproached\b/gi,
    message: 'Check: "reproached" ("reproached") vs "reproaches" ("reproaches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reproaches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reproved_reproves_3399',
    name: 'reproved vs reproves',
    description: 'Commonly confused words: reproved and reproves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breproved\b/gi,
    message: 'Check: "reproved" ("reproved") vs "reproves" ("reproves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reproves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_republished_republishes_3400',
    name: 'republished vs republishes',
    description: 'Commonly confused words: republished and republishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brepublished\b/gi,
    message: 'Check: "republished" ("republished") vs "republishes" ("republishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'republishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_repudiated_repudiates_3401',
    name: 'repudiated vs repudiates',
    description: 'Commonly confused words: repudiated and repudiates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brepudiated\b/gi,
    message: 'Check: "repudiated" ("repudiated") vs "repudiates" ("repudiates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'repudiates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_repulsed_repulses_3402',
    name: 'repulsed vs repulses',
    description: 'Commonly confused words: repulsed and repulses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brepulsed\b/gi,
    message: 'Check: "repulsed" ("repulsed") vs "repulses" ("repulses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'repulses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_repurchased_repurchases_3403',
    name: 'repurchased vs repurchases',
    description: 'Commonly confused words: repurchased and repurchases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brepurchased\b/gi,
    message: 'Check: "repurchased" ("repurchased") vs "repurchases" ("repurchases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'repurchases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_requires_required_3404',
    name: 'requires vs required',
    description: 'Commonly confused words: requires and required',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brequires\b/gi,
    message: 'Check: "requires" ("requires") vs "required" ("required"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'required';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rerouted_reroutes_3405',
    name: 'rerouted vs reroutes',
    description: 'Commonly confused words: rerouted and reroutes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brerouted\b/gi,
    message: 'Check: "rerouted" ("rerouted") vs "reroutes" ("reroutes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reroutes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rescheduled_reschedules_3406',
    name: 'rescheduled vs reschedules',
    description: 'Commonly confused words: rescheduled and reschedules',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brescheduled\b/gi,
    message: 'Check: "rescheduled" ("rescheduled") vs "reschedules" ("reschedules"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reschedules';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_researched_researches_3407',
    name: 'researched vs researches',
    description: 'Commonly confused words: researched and researches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bresearched\b/gi,
    message: 'Check: "researched" ("researched") vs "researches" ("researches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'researches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reserved_reserves_3408',
    name: 'reserved vs reserves',
    description: 'Commonly confused words: reserved and reserves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breserved\b/gi,
    message: 'Check: "reserved" ("reserved") vs "reserves" ("reserves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reserves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_resettled_resettles_3409',
    name: 'resettled vs resettles',
    description: 'Commonly confused words: resettled and resettles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bresettled\b/gi,
    message: 'Check: "resettled" ("resettled") vs "resettles" ("resettles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'resettles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reshaped_reshapes_3410',
    name: 'reshaped vs reshapes',
    description: 'Commonly confused words: reshaped and reshapes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breshaped\b/gi,
    message: 'Check: "reshaped" ("reshaped") vs "reshapes" ("reshapes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reshapes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reshuffled_reshuffles_3411',
    name: 'reshuffled vs reshuffles',
    description: 'Commonly confused words: reshuffled and reshuffles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breshuffled\b/gi,
    message: 'Check: "reshuffled" ("reshuffled") vs "reshuffles" ("reshuffles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reshuffles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_resided_resides_3412',
    name: 'resided vs resides',
    description: 'Commonly confused words: resided and resides',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bresided\b/gi,
    message: 'Check: "resided" ("resided") vs "resides" ("resides"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'resides';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_resized_resizes_3413',
    name: 'resized vs resizes',
    description: 'Commonly confused words: resized and resizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bresized\b/gi,
    message: 'Check: "resized" ("resized") vs "resizes" ("resizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'resizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_resolved_resolves_3414',
    name: 'resolved vs resolves',
    description: 'Commonly confused words: resolved and resolves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bresolved\b/gi,
    message: 'Check: "resolved" ("resolved") vs "resolves" ("resolves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'resolves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_respired_respires_3415',
    name: 'respired vs respires',
    description: 'Commonly confused words: respired and respires',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brespired\b/gi,
    message: 'Check: "respired" ("respired") vs "respires" ("respires"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'respires';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_respire_respite_3416',
    name: 'respire vs respite',
    description: 'Commonly confused words: respire and respite',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brespire\b/gi,
    message: 'Check: "respire" ("respire") vs "respite" ("respite"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'respite';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_respires_respites_3417',
    name: 'respires vs respites',
    description: 'Commonly confused words: respires and respites',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brespires\b/gi,
    message: 'Check: "respires" ("respires") vs "respites" ("respites"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'respites';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_resp_reap_3418',
    name: 'resp vs reap',
    description: 'Commonly confused words: resp and reap',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bresp\b/gi,
    message: 'Check: "resp" ("resp") vs "reap" ("reap"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reap';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_restated_restates_3419',
    name: 'restated vs restates',
    description: 'Commonly confused words: restated and restates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brestated\b/gi,
    message: 'Check: "restated" ("restated") vs "restates" ("restates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'restates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_restate_testate_3420',
    name: 'restate vs testate',
    description: 'Commonly confused words: restate and testate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brestate\b/gi,
    message: 'Check: "restate" ("restate") vs "testate" ("testate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'testate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_restructured_restructures_3421',
    name: 'restructured vs restructures',
    description: 'Commonly confused words: restructured and restructures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brestructured\b/gi,
    message: 'Check: "restructured" ("restructured") vs "restructures" ("restructures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'restructures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_resupplied_resupplies_3422',
    name: 'resupplied vs resupplies',
    description: 'Commonly confused words: resupplied and resupplies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bresupplied\b/gi,
    message: 'Check: "resupplied" ("resupplied") vs "resupplies" ("resupplies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'resupplies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_resuscitated_resuscitates_3423',
    name: 'resuscitated vs resuscitates',
    description: 'Commonly confused words: resuscitated and resuscitates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bresuscitated\b/gi,
    message: 'Check: "resuscitated" ("resuscitated") vs "resuscitates" ("resuscitates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'resuscitates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_retook_retool_3424',
    name: 'retook vs retool',
    description: 'Commonly confused words: retook and retool',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bretook\b/gi,
    message: 'Check: "retook" ("retook") vs "retool" ("retool"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'retool';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_retouched_retouches_3425',
    name: 'retouched vs retouches',
    description: 'Commonly confused words: retouched and retouches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bretouched\b/gi,
    message: 'Check: "retouched" ("retouched") vs "retouches" ("retouches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'retouches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_retried_retries_3426',
    name: 'retried vs retries',
    description: 'Commonly confused words: retried and retries',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bretried\b/gi,
    message: 'Check: "retried" ("retried") vs "retries" ("retries"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'retries';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_retrograded_retrogrades_3427',
    name: 'retrograded vs retrogrades',
    description: 'Commonly confused words: retrograded and retrogrades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bretrograded\b/gi,
    message: 'Check: "retrograded" ("retrograded") vs "retrogrades" ("retrogrades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'retrogrades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_returnee_returner_3428',
    name: 'returnee vs returner',
    description: 'Commonly confused words: returnee and returner',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breturnee\b/gi,
    message: 'Check: "returnee" ("returnee") vs "returner" ("returner"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'returner';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reused_reuses_3429',
    name: 'reused vs reuses',
    description: 'Commonly confused words: reused and reuses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breused\b/gi,
    message: 'Check: "reused" ("reused") vs "reuses" ("reuses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reuses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_revered_reveres_3430',
    name: 'revered vs reveres',
    description: 'Commonly confused words: revered and reveres',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brevered\b/gi,
    message: 'Check: "revered" ("revered") vs "reveres" ("reveres"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reveres';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reverenced_reverences_3431',
    name: 'reverenced vs reverences',
    description: 'Commonly confused words: reverenced and reverences',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breverenced\b/gi,
    message: 'Check: "reverenced" ("reverenced") vs "reverences" ("reverences"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reverences';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reversed_reverses_3432',
    name: 'reversed vs reverses',
    description: 'Commonly confused words: reversed and reverses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breversed\b/gi,
    message: 'Check: "reversed" ("reversed") vs "reverses" ("reverses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reverses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_reviled_reviles_3433',
    name: 'reviled vs reviles',
    description: 'Commonly confused words: reviled and reviles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\breviled\b/gi,
    message: 'Check: "reviled" ("reviled") vs "reviles" ("reviles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reviles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_revitalized_revitalizes_3434',
    name: 'revitalized vs revitalizes',
    description: 'Commonly confused words: revitalized and revitalizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brevitalized\b/gi,
    message: 'Check: "revitalized" ("revitalized") vs "revitalizes" ("revitalizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'revitalizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_revoked_revokes_3435',
    name: 'revoked vs revokes',
    description: 'Commonly confused words: revoked and revokes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brevoked\b/gi,
    message: 'Check: "revoked" ("revoked") vs "revokes" ("revokes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'revokes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rewired_rewires_3436',
    name: 'rewired vs rewires',
    description: 'Commonly confused words: rewired and rewires',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brewired\b/gi,
    message: 'Check: "rewired" ("rewired") vs "rewires" ("rewires"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rewires';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rho_tho_3437',
    name: 'rho vs tho',
    description: 'Commonly confused words: rho and tho',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brho\b/gi,
    message: 'Check: "rho" ("rho") vs "tho" ("tho"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tho';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rhymed_rhymes_3438',
    name: 'rhymed vs rhymes',
    description: 'Commonly confused words: rhymed and rhymes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brhymed\b/gi,
    message: 'Check: "rhymed" ("rhymed") vs "rhymes" ("rhymes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rhymes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ribbed_robbed_3439',
    name: 'ribbed vs robbed',
    description: 'Commonly confused words: ribbed and robbed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bribbed\b/gi,
    message: 'Check: "ribbed" ("ribbed") vs "robbed" ("robbed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'robbed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ribbed_rubbed_3440',
    name: 'ribbed vs rubbed',
    description: 'Commonly confused words: ribbed and rubbed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bribbed\b/gi,
    message: 'Check: "ribbed" ("ribbed") vs "rubbed" ("rubbed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rubbed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ribbing_robbing_3441',
    name: 'ribbing vs robbing',
    description: 'Commonly confused words: ribbing and robbing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bribbing\b/gi,
    message: 'Check: "ribbing" ("ribbing") vs "robbing" ("robbing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'robbing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ribbing_rubbing_3442',
    name: 'ribbing vs rubbing',
    description: 'Commonly confused words: ribbing and rubbing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bribbing\b/gi,
    message: 'Check: "ribbing" ("ribbing") vs "rubbing" ("rubbing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rubbing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rickets_tickets_3443',
    name: 'rickets vs tickets',
    description: 'Commonly confused words: rickets and tickets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brickets\b/gi,
    message: 'Check: "rickets" ("rickets") vs "tickets" ("tickets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tickets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_riddled_riddles_3444',
    name: 'riddled vs riddles',
    description: 'Commonly confused words: riddled and riddles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\briddled\b/gi,
    message: 'Check: "riddled" ("riddled") vs "riddles" ("riddles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'riddles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ridged_ridges_3445',
    name: 'ridged vs ridges',
    description: 'Commonly confused words: ridged and ridges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bridged\b/gi,
    message: 'Check: "ridged" ("ridged") vs "ridges" ("ridges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ridges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rife_fife_3446',
    name: 'rife vs fife',
    description: 'Commonly confused words: rife and fife',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brife\b/gi,
    message: 'Check: "rife" ("rife") vs "fife" ("fife"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fife';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rife_ride_3447',
    name: 'rife vs ride',
    description: 'Commonly confused words: rife and ride',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brife\b/gi,
    message: 'Check: "rife" ("rife") vs "ride" ("ride"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ride';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rife_rive_3448',
    name: 'rife vs rive',
    description: 'Commonly confused words: rife and rive',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brife\b/gi,
    message: 'Check: "rife" ("rife") vs "rive" ("rive"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rive';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_riffed_ruffed_3449',
    name: 'riffed vs ruffed',
    description: 'Commonly confused words: riffed and ruffed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\briffed\b/gi,
    message: 'Check: "riffed" ("riffed") vs "ruffed" ("ruffed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ruffed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_riffled_ruffled_3450',
    name: 'riffled vs ruffled',
    description: 'Commonly confused words: riffled and ruffled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\briffled\b/gi,
    message: 'Check: "riffled" ("riffled") vs "ruffled" ("ruffled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ruffled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_riffs_ruffs_3451',
    name: 'riffs vs ruffs',
    description: 'Commonly confused words: riffs and ruffs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\briffs\b/gi,
    message: 'Check: "riffs" ("riffs") vs "ruffs" ("ruffs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ruffs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_riffs_tiffs_3452',
    name: 'riffs vs tiffs',
    description: 'Commonly confused words: riffs and tiffs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\briffs\b/gi,
    message: 'Check: "riffs" ("riffs") vs "tiffs" ("tiffs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tiffs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rigged_rugged_3453',
    name: 'rigged vs rugged',
    description: 'Commonly confused words: rigged and rugged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brigged\b/gi,
    message: 'Check: "rigged" ("rigged") vs "rugged" ("rugged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rugged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_right_eight_3454',
    name: 'right vs eight',
    description: 'Commonly confused words: right and eight',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bright\b/gi,
    message: 'Check: "right" (correct or direction) vs "eight" ("eight"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eight';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_righter_tighter_3455',
    name: 'righter vs tighter',
    description: 'Commonly confused words: righter and tighter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brighter\b/gi,
    message: 'Check: "righter" ("righter") vs "tighter" ("tighter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tighter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rightly_tightly_3456',
    name: 'rightly vs tightly',
    description: 'Commonly confused words: rightly and tightly',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brightly\b/gi,
    message: 'Check: "rightly" ("rightly") vs "tightly" ("tightly"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tightly';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rightness_tightness_3457',
    name: 'rightness vs tightness',
    description: 'Commonly confused words: rightness and tightness',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brightness\b/gi,
    message: 'Check: "rightness" ("rightness") vs "tightness" ("tightness"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tightness';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rigs_figs_3458',
    name: 'rigs vs figs',
    description: 'Commonly confused words: rigs and figs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brigs\b/gi,
    message: 'Check: "rigs" ("rigs") vs "figs" ("figs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'figs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_riled_riles_3459',
    name: 'riled vs riles',
    description: 'Commonly confused words: riled and riles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\briled\b/gi,
    message: 'Check: "riled" ("riled") vs "riles" ("riles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'riles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rills_fills_3460',
    name: 'rills vs fills',
    description: 'Commonly confused words: rills and fills',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brills\b/gi,
    message: 'Check: "rills" ("rills") vs "fills" ("fills"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fills';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rimed_rimes_3461',
    name: 'rimed vs rimes',
    description: 'Commonly confused words: rimed and rimes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brimed\b/gi,
    message: 'Check: "rimed" ("rimed") vs "rimes" ("rimes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rimes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rinds_finds_3462',
    name: 'rinds vs finds',
    description: 'Commonly confused words: rinds and finds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brinds\b/gi,
    message: 'Check: "rinds" ("rinds") vs "finds" ("finds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'finds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rink_fink_3463',
    name: 'rink vs fink',
    description: 'Commonly confused words: rink and fink',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brink\b/gi,
    message: 'Check: "rink" ("rink") vs "fink" ("fink"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fink';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rinks_finks_3464',
    name: 'rinks vs finks',
    description: 'Commonly confused words: rinks and finks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brinks\b/gi,
    message: 'Check: "rinks" ("rinks") vs "finks" ("finks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'finks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rinsed_rinses_3465',
    name: 'rinsed vs rinses',
    description: 'Commonly confused words: rinsed and rinses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brinsed\b/gi,
    message: 'Check: "rinsed" ("rinsed") vs "rinses" ("rinses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rinses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rioted_rooted_3466',
    name: 'rioted vs rooted',
    description: 'Commonly confused words: rioted and rooted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brioted\b/gi,
    message: 'Check: "rioted" ("rioted") vs "rooted" ("rooted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rooted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rioting_rooting_3467',
    name: 'rioting vs rooting',
    description: 'Commonly confused words: rioting and rooting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brioting\b/gi,
    message: 'Check: "rioting" ("rioting") vs "rooting" ("rooting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rooting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_riot_root_3468',
    name: 'riot vs root',
    description: 'Commonly confused words: riot and root',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\briot\b/gi,
    message: 'Check: "riot" ("riot") vs "root" ("root"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'root';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rippled_ripples_3469',
    name: 'rippled vs ripples',
    description: 'Commonly confused words: rippled and ripples',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brippled\b/gi,
    message: 'Check: "rippled" ("rippled") vs "ripples" ("ripples"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ripples';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ripples_tipples_3470',
    name: 'ripples vs tipples',
    description: 'Commonly confused words: ripples and tipples',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bripples\b/gi,
    message: 'Check: "ripples" ("ripples") vs "tipples" ("tipples"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tipples';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rippling_tippling_3471',
    name: 'rippling vs tippling',
    description: 'Commonly confused words: rippling and tippling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brippling\b/gi,
    message: 'Check: "rippling" ("rippling") vs "tippling" ("tippling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tippling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rises_rides_3472',
    name: 'rises vs rides',
    description: 'Commonly confused words: rises and rides',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brises\b/gi,
    message: 'Check: "rises" ("rises") vs "rides" ("rides"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rides';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rives_fives_3473',
    name: 'rives vs fives',
    description: 'Commonly confused words: rives and fives',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brives\b/gi,
    message: 'Check: "rives" ("rives") vs "fives" ("fives"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fives';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_riyals_royals_3474',
    name: 'riyals vs royals',
    description: 'Commonly confused words: riyals and royals',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\briyals\b/gi,
    message: 'Check: "riyals" ("riyals") vs "royals" ("royals"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'royals';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_roached_roaches_3475',
    name: 'roached vs roaches',
    description: 'Commonly confused words: roached and roaches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broached\b/gi,
    message: 'Check: "roached" ("roached") vs "roaches" ("roaches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'roaches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_roam_roan_3476',
    name: 'roam vs roan',
    description: 'Commonly confused words: roam and roan',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broam\b/gi,
    message: 'Check: "roam" ("roam") vs "roan" ("roan"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'roan';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_roams_roans_3477',
    name: 'roams vs roans',
    description: 'Commonly confused words: roams and roans',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broams\b/gi,
    message: 'Check: "roams" ("roams") vs "roans" ("roans"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'roans';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_roast_toast_3478',
    name: 'roast vs toast',
    description: 'Commonly confused words: roast and toast',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broast\b/gi,
    message: 'Check: "roast" ("roast") vs "toast" ("toast"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toast';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_robed_robes_3479',
    name: 'robed vs robes',
    description: 'Commonly confused words: robed and robes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brobed\b/gi,
    message: 'Check: "robed" ("robed") vs "robes" ("robes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'robes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_robed_roved_3480',
    name: 'robed vs roved',
    description: 'Commonly confused words: robed and roved',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brobed\b/gi,
    message: 'Check: "robed" ("robed") vs "roved" ("roved"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'roved';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_robing_roving_3481',
    name: 'robing vs roving',
    description: 'Commonly confused words: robing and roving',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brobing\b/gi,
    message: 'Check: "robing" ("robing") vs "roving" ("roving"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'roving';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_robs_fobs_3482',
    name: 'robs vs fobs',
    description: 'Commonly confused words: robs and fobs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brobs\b/gi,
    message: 'Check: "robs" ("robs") vs "fobs" ("fobs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fobs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_roes_foes_3483',
    name: 'roes vs foes',
    description: 'Commonly confused words: roes and foes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broes\b/gi,
    message: 'Check: "roes" ("roes") vs "foes" ("foes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'foes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_roil_foil_3484',
    name: 'roil vs foil',
    description: 'Commonly confused words: roil and foil',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broil\b/gi,
    message: 'Check: "roil" ("roil") vs "foil" ("foil"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'foil';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_roiling_foiling_3485',
    name: 'roiling vs foiling',
    description: 'Commonly confused words: roiling and foiling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broiling\b/gi,
    message: 'Check: "roiling" ("roiling") vs "foiling" ("foiling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'foiling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_roister_rooster_3486',
    name: 'roister vs rooster',
    description: 'Commonly confused words: roister and rooster',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broister\b/gi,
    message: 'Check: "roister" ("roister") vs "rooster" ("rooster"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rooster';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_romanced_romances_3487',
    name: 'romanced vs romances',
    description: 'Commonly confused words: romanced and romances',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bromanced\b/gi,
    message: 'Check: "romanced" ("romanced") vs "romances" ("romances"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'romances';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_romanticized_romanticizes_3488',
    name: 'romanticized vs romanticizes',
    description: 'Commonly confused words: romanticized and romanticizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bromanticized\b/gi,
    message: 'Check: "romanticized" ("romanticized") vs "romanticizes" ("romanticizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'romanticizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_root_foot_3489',
    name: 'root vs foot',
    description: 'Commonly confused words: root and foot',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broot\b/gi,
    message: 'Check: "root" ("root") vs "foot" ("foot"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'foot';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rooting_footing_3490',
    name: 'rooting vs footing',
    description: 'Commonly confused words: rooting and footing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brooting\b/gi,
    message: 'Check: "rooting" ("rooting") vs "footing" ("footing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'footing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rotated_rotates_3491',
    name: 'rotated vs rotates',
    description: 'Commonly confused words: rotated and rotates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brotated\b/gi,
    message: 'Check: "rotated" ("rotated") vs "rotates" ("rotates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rotates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rouged_rouges_3492',
    name: 'rouged vs rouges',
    description: 'Commonly confused words: rouged and rouges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brouged\b/gi,
    message: 'Check: "rouged" ("rouged") vs "rouges" ("rouges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rouges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rouged_routed_3493',
    name: 'rouged vs routed',
    description: 'Commonly confused words: rouged and routed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brouged\b/gi,
    message: 'Check: "rouged" ("rouged") vs "routed" ("routed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'routed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rouge_route_3494',
    name: 'rouge vs route',
    description: 'Commonly confused words: rouge and route',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brouge\b/gi,
    message: 'Check: "rouge" ("rouge") vs "route" ("route"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'route';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_roughed_toughed_3495',
    name: 'roughed vs toughed',
    description: 'Commonly confused words: roughed and toughed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broughed\b/gi,
    message: 'Check: "roughed" ("roughed") vs "toughed" ("toughed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toughed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_roughen_toughen_3496',
    name: 'roughen vs toughen',
    description: 'Commonly confused words: roughen and toughen',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broughen\b/gi,
    message: 'Check: "roughen" ("roughen") vs "toughen" ("toughen"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toughen';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_roughing_toughing_3497',
    name: 'roughing vs toughing',
    description: 'Commonly confused words: roughing and toughing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broughing\b/gi,
    message: 'Check: "roughing" ("roughing") vs "toughing" ("toughing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toughing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_roughly_toughly_3498',
    name: 'roughly vs toughly',
    description: 'Commonly confused words: roughly and toughly',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broughly\b/gi,
    message: 'Check: "roughly" ("roughly") vs "toughly" ("toughly"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toughly';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_roughness_toughness_3499',
    name: 'roughness vs toughness',
    description: 'Commonly confused words: roughness and toughness',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broughness\b/gi,
    message: 'Check: "roughness" ("roughness") vs "toughness" ("toughness"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'toughness';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_routed_routes_3500',
    name: 'routed vs routes',
    description: 'Commonly confused words: routed and routes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brouted\b/gi,
    message: 'Check: "routed" ("routed") vs "routes" ("routes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'routes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_roving_riving_3501',
    name: 'roving vs riving',
    description: 'Commonly confused words: roving and riving',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\broving\b/gi,
    message: 'Check: "roving" ("roving") vs "riving" ("riving"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'riving';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rowels_towels_3502',
    name: 'rowels vs towels',
    description: 'Commonly confused words: rowels and towels',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\browels\b/gi,
    message: 'Check: "rowels" ("rowels") vs "towels" ("towels"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'towels';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rude_ride_3503',
    name: 'rude vs ride',
    description: 'Commonly confused words: rude and ride',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brude\b/gi,
    message: 'Check: "rude" ("rude") vs "ride" ("ride"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ride';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rued_rues_3504',
    name: 'rued vs rues',
    description: 'Commonly confused words: rued and rues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brued\b/gi,
    message: 'Check: "rued" ("rued") vs "rues" ("rues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ruffled_ruffles_3505',
    name: 'ruffled vs ruffles',
    description: 'Commonly confused words: ruffled and ruffles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bruffled\b/gi,
    message: 'Check: "ruffled" ("ruffled") vs "ruffles" ("ruffles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ruffles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ruled_rules_3506',
    name: 'ruled vs rules',
    description: 'Commonly confused words: ruled and rules',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bruled\b/gi,
    message: 'Check: "ruled" ("ruled") vs "rules" ("rules"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rules';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rummaged_rummages_3507',
    name: 'rummaged vs rummages',
    description: 'Commonly confused words: rummaged and rummages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brummaged\b/gi,
    message: 'Check: "rummaged" ("rummaged") vs "rummages" ("rummages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rummages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rumors_tumors_3508',
    name: 'rumors vs tumors',
    description: 'Commonly confused words: rumors and tumors',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brumors\b/gi,
    message: 'Check: "rumors" ("rumors") vs "tumors" ("tumors"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tumors';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_run_fun_3509',
    name: 'run vs fun',
    description: 'Commonly confused words: run and fun',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brun\b/gi,
    message: 'Check: "run" ("run") vs "fun" ("fun"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fun';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_runic_tunic_3510',
    name: 'runic vs tunic',
    description: 'Commonly confused words: runic and tunic',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brunic\b/gi,
    message: 'Check: "runic" ("runic") vs "tunic" ("tunic"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tunic';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_runny_tunny_3511',
    name: 'runny vs tunny',
    description: 'Commonly confused words: runny and tunny',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brunny\b/gi,
    message: 'Check: "runny" ("runny") vs "tunny" ("tunny"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tunny';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ruptured_ruptures_3512',
    name: 'ruptured vs ruptures',
    description: 'Commonly confused words: ruptured and ruptures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bruptured\b/gi,
    message: 'Check: "ruptured" ("ruptured") vs "ruptures" ("ruptures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ruptures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ruse_fuse_3513',
    name: 'ruse vs fuse',
    description: 'Commonly confused words: ruse and fuse',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bruse\b/gi,
    message: 'Check: "ruse" ("ruse") vs "fuse" ("fuse"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fuse';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ruses_rises_3514',
    name: 'ruses vs rises',
    description: 'Commonly confused words: ruses and rises',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bruses\b/gi,
    message: 'Check: "ruses" ("ruses") vs "rises" ("rises"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rises';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rusks_tusks_3515',
    name: 'rusks vs tusks',
    description: 'Commonly confused words: rusks and tusks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brusks\b/gi,
    message: 'Check: "rusks" ("rusks") vs "tusks" ("tusks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'tusks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rustled_rustles_3516',
    name: 'rustled vs rustles',
    description: 'Commonly confused words: rustled and rustles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brustled\b/gi,
    message: 'Check: "rustled" ("rustled") vs "rustles" ("rustles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rustles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_rut_fut_3517',
    name: 'rut vs fut',
    description: 'Commonly confused words: rut and fut',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\brut\b/gi,
    message: 'Check: "rut" ("rut") vs "fut" ("fut"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fut';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
