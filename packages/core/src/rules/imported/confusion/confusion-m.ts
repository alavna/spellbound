/**
 * Auto-generated confusion rules (M)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.514Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesM: GrammarRule[] = [
  {
    id: 'confusion_mach_mock_424',
    name: 'mach vs mock',
    description: 'Commonly confused words: mach and mock',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmach\b/gi,
    message: 'Check: "mach" ("mach") vs "mock" ("mock"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mock';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_made_maid_425',
    name: 'made vs maid',
    description: 'Commonly confused words: made and maid',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmade\b/gi,
    message: 'Check: "made" ("made") vs "maid" ("maid"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'maid';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mail_male_426',
    name: 'mail vs male',
    description: 'Commonly confused words: mail and male',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmail\b/gi,
    message: 'Check: "mail" (postal delivery) vs "male" (masculine). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'male';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_maize_maze_427',
    name: 'maize vs maze',
    description: 'Commonly confused words: maize and maze',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmaize\b/gi,
    message: 'Check: "maize" ("maize") vs "maze" ("maze"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'maze';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_manners_manors_428',
    name: 'manners vs manors',
    description: 'Commonly confused words: manners and manors',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmanners\b/gi,
    message: 'Check: "manners" ("manners") vs "manors" ("manors"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'manors';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_manner_manor_429',
    name: 'manner vs manor',
    description: 'Commonly confused words: manner and manor',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmanner\b/gi,
    message: 'Check: "manner" ("manner") vs "manor" ("manor"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'manor';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_man_men_430',
    name: 'man vs men',
    description: 'Commonly confused words: man and men',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bman\b/gi,
    message: 'Check: "man" ("man") vs "men" ("men"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'men';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_marital_martial_431',
    name: 'marital vs martial',
    description: 'Commonly confused words: marital and martial',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmarital\b/gi,
    message: 'Check: "marital" ("marital") vs "martial" ("martial"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'martial';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_marry_merry_432',
    name: 'marry vs merry',
    description: 'Commonly confused words: marry and merry',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmarry\b/gi,
    message: 'Check: "marry" ("marry") vs "merry" ("merry"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'merry';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_massed_mast_433',
    name: 'massed vs mast',
    description: 'Commonly confused words: massed and mast',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmassed\b/gi,
    message: 'Check: "massed" ("massed") vs "mast" ("mast"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mast';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_matrices_matrix_434',
    name: 'matrices vs matrix',
    description: 'Commonly confused words: matrices and matrix',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmatrices\b/gi,
    message: 'Check: "matrices" ("matrices") vs "matrix" ("matrix"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'matrix';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_median_medium_435',
    name: 'median vs medium',
    description: 'Commonly confused words: median and medium',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmedian\b/gi,
    message: 'Check: "median" ("median") vs "medium" ("medium"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'medium';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_memoranda_memorandum_436',
    name: 'memoranda vs memorandum',
    description: 'Commonly confused words: memoranda and memorandum',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmemoranda\b/gi,
    message: 'Check: "memoranda" ("memoranda") vs "memorandum" ("memorandum"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'memorandum';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mewl_mule_437',
    name: 'mewl vs mule',
    description: 'Commonly confused words: mewl and mule',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmewl\b/gi,
    message: 'Check: "mewl" ("mewl") vs "mule" ("mule"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mule';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mice_mouse_438',
    name: 'mice vs mouse',
    description: 'Commonly confused words: mice and mouse',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmice\b/gi,
    message: 'Check: "mice" ("mice") vs "mouse" ("mouse"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mouse';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mice_nice_439',
    name: 'mice vs nice',
    description: 'Commonly confused words: mice and nice',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmice\b/gi,
    message: 'Check: "mice" ("mice") vs "nice" ("nice"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nice';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_might_mite_440',
    name: 'might vs mite',
    description: 'Commonly confused words: might and mite',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmight\b/gi,
    message: 'Check: "might" ("might") vs "mite" ("mite"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mite';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_militate_mitigate_441',
    name: 'militate vs mitigate',
    description: 'Commonly confused words: militate and mitigate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmilitate\b/gi,
    message: 'Check: "militate" ("militate") vs "mitigate" ("mitigate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mitigate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_militates_mitigates_442',
    name: 'militates vs mitigates',
    description: 'Commonly confused words: militates and mitigates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmilitates\b/gi,
    message: 'Check: "militates" ("militates") vs "mitigates" ("mitigates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mitigates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mince_mints_443',
    name: 'mince vs mints',
    description: 'Commonly confused words: mince and mints',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmince\b/gi,
    message: 'Check: "mince" ("mince") vs "mints" ("mints"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mints';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_minces_mints_444',
    name: 'minces vs mints',
    description: 'Commonly confused words: minces and mints',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bminces\b/gi,
    message: 'Check: "minces" ("minces") vs "mints" ("mints"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mints';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mind_mined_445',
    name: 'mind vs mined',
    description: 'Commonly confused words: mind and mined',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmind\b/gi,
    message: 'Check: "mind" ("mind") vs "mined" ("mined"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mined';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_minds_mines_446',
    name: 'minds vs mines',
    description: 'Commonly confused words: minds and mines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bminds\b/gi,
    message: 'Check: "minds" ("minds") vs "mines" ("mines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_minding_mining_447',
    name: 'minding vs mining',
    description: 'Commonly confused words: minding and mining',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bminding\b/gi,
    message: 'Check: "minding" ("minding") vs "mining" ("mining"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mining';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_minuet_minute_448',
    name: 'minuet vs minute',
    description: 'Commonly confused words: minuet and minute',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bminuet\b/gi,
    message: 'Check: "minuet" ("minuet") vs "minute" ("minute"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'minute';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_minuets_minutes_449',
    name: 'minuets vs minutes',
    description: 'Commonly confused words: minuets and minutes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bminuets\b/gi,
    message: 'Check: "minuets" ("minuets") vs "minutes" ("minutes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'minutes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_missed_mist_450',
    name: 'missed vs mist',
    description: 'Commonly confused words: missed and mist',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmissed\b/gi,
    message: 'Check: "missed" ("missed") vs "mist" ("mist"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mist';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mist_must_451',
    name: 'mist vs must',
    description: 'Commonly confused words: mist and must',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmist\b/gi,
    message: 'Check: "mist" ("mist") vs "must" ("must"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'must';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mode_mowed_452',
    name: 'mode vs mowed',
    description: 'Commonly confused words: mode and mowed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmode\b/gi,
    message: 'Check: "mode" ("mode") vs "mowed" ("mowed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mowed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mood_mooed_453',
    name: 'mood vs mooed',
    description: 'Commonly confused words: mood and mooed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmood\b/gi,
    message: 'Check: "mood" ("mood") vs "mooed" ("mooed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mooed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_morale_morel_454',
    name: 'morale vs morel',
    description: 'Commonly confused words: morale and morel',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmorale\b/gi,
    message: 'Check: "morale" ("morale") vs "morel" ("morel"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'morel';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_moral_morale_455',
    name: 'moral vs morale',
    description: 'Commonly confused words: moral and morale',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmoral\b/gi,
    message: 'Check: "moral" ("moral") vs "morale" ("morale"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'morale';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_morales_morals_456',
    name: 'morales vs morals',
    description: 'Commonly confused words: morales and morals',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmorales\b/gi,
    message: 'Check: "morales" ("morales") vs "morals" ("morals"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'morals';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_morning_mourning_457',
    name: 'morning vs mourning',
    description: 'Commonly confused words: morning and mourning',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmorning\b/gi,
    message: 'Check: "morning" ("morning") vs "mourning" ("mourning"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mourning';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_morn_mourn_458',
    name: 'morn vs mourn',
    description: 'Commonly confused words: morn and mourn',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmorn\b/gi,
    message: 'Check: "morn" ("morn") vs "mourn" ("mourn"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mourn';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_most_mot_459',
    name: 'most vs mot',
    description: 'Commonly confused words: most and mot',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmost\b/gi,
    message: 'Check: "most" ("most") vs "mot" ("mot"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mot';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mow_now_460',
    name: 'mow vs now',
    description: 'Commonly confused words: mow and now',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmow\b/gi,
    message: 'Check: "mow" ("mow") vs "now" ("now"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'now';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mustard_mustered_461',
    name: 'mustard vs mustered',
    description: 'Commonly confused words: mustard and mustered',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmustard\b/gi,
    message: 'Check: "mustard" ("mustard") vs "mustered" ("mustered"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mustered';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_make_male_787',
    name: 'make vs male',
    description: 'Commonly confused words: make and male',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmake\b/gi,
    message: 'Check: "make" ("make") vs "male" (masculine). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'male';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_maced_maxed_2771',
    name: 'maced vs maxed',
    description: 'Commonly confused words: maced and maxed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmaced\b/gi,
    message: 'Check: "maced" ("maced") vs "maxed" ("maxed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'maxed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_magnetized_magnetizes_2772',
    name: 'magnetized vs magnetizes',
    description: 'Commonly confused words: magnetized and magnetizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmagnetized\b/gi,
    message: 'Check: "magnetized" ("magnetized") vs "magnetizes" ("magnetizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'magnetizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_magnified_magnifies_2773',
    name: 'magnified vs magnifies',
    description: 'Commonly confused words: magnified and magnifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmagnified\b/gi,
    message: 'Check: "magnified" ("magnified") vs "magnifies" ("magnifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'magnifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_maims_mains_2774',
    name: 'maims vs mains',
    description: 'Commonly confused words: maims and mains',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmaims\b/gi,
    message: 'Check: "maims" ("maims") vs "mains" ("mains"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mains';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mama_mams_2775',
    name: 'mama vs mams',
    description: 'Commonly confused words: mama and mams',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmama\b/gi,
    message: 'Check: "mama" ("mama") vs "mams" ("mams"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mams';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mandated_mandates_2776',
    name: 'mandated vs mandates',
    description: 'Commonly confused words: mandated and mandates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmandated\b/gi,
    message: 'Check: "mandated" ("mandated") vs "mandates" ("mandates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mandates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_maned_manes_2777',
    name: 'maned vs manes',
    description: 'Commonly confused words: maned and manes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmaned\b/gi,
    message: 'Check: "maned" ("maned") vs "manes" ("manes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'manes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mangled_mangles_2778',
    name: 'mangled vs mangles',
    description: 'Commonly confused words: mangled and mangles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmangled\b/gi,
    message: 'Check: "mangled" ("mangled") vs "mangles" ("mangles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mangles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mangled_mantled_2779',
    name: 'mangled vs mantled',
    description: 'Commonly confused words: mangled and mantled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmangled\b/gi,
    message: 'Check: "mangled" ("mangled") vs "mantled" ("mantled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mantled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mangles_mangoes_2780',
    name: 'mangles vs mangoes',
    description: 'Commonly confused words: mangles and mangoes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmangles\b/gi,
    message: 'Check: "mangles" ("mangles") vs "mangoes" ("mangoes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mangoes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mangles_mantles_2781',
    name: 'mangles vs mantles',
    description: 'Commonly confused words: mangles and mantles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmangles\b/gi,
    message: 'Check: "mangles" ("mangles") vs "mantles" ("mantles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mantles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mangling_mantling_2782',
    name: 'mangling vs mantling',
    description: 'Commonly confused words: mangling and mantling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmangling\b/gi,
    message: 'Check: "mangling" ("mangling") vs "mantling" ("mantling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mantling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_manhandled_manhandles_2783',
    name: 'manhandled vs manhandles',
    description: 'Commonly confused words: manhandled and manhandles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmanhandled\b/gi,
    message: 'Check: "manhandled" ("manhandled") vs "manhandles" ("manhandles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'manhandles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_manicured_manicures_2784',
    name: 'manicured vs manicures',
    description: 'Commonly confused words: manicured and manicures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmanicured\b/gi,
    message: 'Check: "manicured" ("manicured") vs "manicures" ("manicures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'manicures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mantled_mantles_2785',
    name: 'mantled vs mantles',
    description: 'Commonly confused words: mantled and mantles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmantled\b/gi,
    message: 'Check: "mantled" ("mantled") vs "mantles" ("mantles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mantles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_manured_manures_2786',
    name: 'manured vs manures',
    description: 'Commonly confused words: manured and manures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmanured\b/gi,
    message: 'Check: "manured" ("manured") vs "manures" ("manures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'manures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mapped_napped_2787',
    name: 'mapped vs napped',
    description: 'Commonly confused words: mapped and napped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmapped\b/gi,
    message: 'Check: "mapped" ("mapped") vs "napped" ("napped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'napped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_marbled_marbles_2788',
    name: 'marbled vs marbles',
    description: 'Commonly confused words: marbled and marbles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmarbled\b/gi,
    message: 'Check: "marbled" ("marbled") vs "marbles" ("marbles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'marbles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_marched_marches_2789',
    name: 'marched vs marches',
    description: 'Commonly confused words: marched and marches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmarched\b/gi,
    message: 'Check: "marched" ("marched") vs "marches" ("marches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'marches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_marched_matched_2790',
    name: 'marched vs matched',
    description: 'Commonly confused words: marched and matched',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmarched\b/gi,
    message: 'Check: "marched" ("marched") vs "matched" ("matched"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'matched';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_marching_matching_2791',
    name: 'marching vs matching',
    description: 'Commonly confused words: marching and matching',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmarching\b/gi,
    message: 'Check: "marching" ("marching") vs "matching" ("matching"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'matching';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_marginalized_marginalizes_2792',
    name: 'marginalized vs marginalizes',
    description: 'Commonly confused words: marginalized and marginalizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmarginalized\b/gi,
    message: 'Check: "marginalized" ("marginalized") vs "marginalizes" ("marginalizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'marginalizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_margins_martins_2793',
    name: 'margins vs martins',
    description: 'Commonly confused words: margins and martins',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmargins\b/gi,
    message: 'Check: "margins" ("margins") vs "martins" ("martins"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'martins';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_marrows_narrows_2794',
    name: 'marrows vs narrows',
    description: 'Commonly confused words: marrows and narrows',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmarrows\b/gi,
    message: 'Check: "marrows" ("marrows") vs "narrows" ("narrows"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'narrows';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mashed_mashes_2795',
    name: 'mashed vs mashes',
    description: 'Commonly confused words: mashed and mashes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmashed\b/gi,
    message: 'Check: "mashed" ("mashed") vs "mashes" ("mashes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mashes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_masqueraded_masquerades_2796',
    name: 'masqueraded vs masquerades',
    description: 'Commonly confused words: masqueraded and masquerades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmasqueraded\b/gi,
    message: 'Check: "masqueraded" ("masqueraded") vs "masquerades" ("masquerades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'masquerades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_massacred_massacres_2797',
    name: 'massacred vs massacres',
    description: 'Commonly confused words: massacred and massacres',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmassacred\b/gi,
    message: 'Check: "massacred" ("massacred") vs "massacres" ("massacres"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'massacres';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_massed_masses_2798',
    name: 'massed vs masses',
    description: 'Commonly confused words: massed and masses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmassed\b/gi,
    message: 'Check: "massed" ("massed") vs "masses" ("masses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'masses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_matches_marches_2799',
    name: 'matches vs marches',
    description: 'Commonly confused words: matches and marches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmatches\b/gi,
    message: 'Check: "matches" ("matches") vs "marches" ("marches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'marches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_matted_mattes_2800',
    name: 'matted vs mattes',
    description: 'Commonly confused words: matted and mattes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmatted\b/gi,
    message: 'Check: "matted" ("matted") vs "mattes" ("mattes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mattes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mattering_nattering_2801',
    name: 'mattering vs nattering',
    description: 'Commonly confused words: mattering and nattering',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmattering\b/gi,
    message: 'Check: "mattering" ("mattering") vs "nattering" ("nattering"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nattering';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_matured_matures_2802',
    name: 'matured vs matures',
    description: 'Commonly confused words: matured and matures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmatured\b/gi,
    message: 'Check: "matured" ("matured") vs "matures" ("matures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'matures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_matures_natures_2803',
    name: 'matures vs natures',
    description: 'Commonly confused words: matures and natures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmatures\b/gi,
    message: 'Check: "matures" ("matures") vs "natures" ("natures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'natures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mauled_mailed_2804',
    name: 'mauled vs mailed',
    description: 'Commonly confused words: mauled and mailed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmauled\b/gi,
    message: 'Check: "mauled" ("mauled") vs "mailed" ("mailed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mailed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mauls_mails_2805',
    name: 'mauls vs mails',
    description: 'Commonly confused words: mauls and mails',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmauls\b/gi,
    message: 'Check: "mauls" ("mauls") vs "mails" ("mails"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mails';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_maximized_maximizes_2806',
    name: 'maximized vs maximizes',
    description: 'Commonly confused words: maximized and maximizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmaximized\b/gi,
    message: 'Check: "maximized" ("maximized") vs "maximizes" ("maximizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'maximizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_meant_meany_2807',
    name: 'meant vs meany',
    description: 'Commonly confused words: meant and meany',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmeant\b/gi,
    message: 'Check: "meant" ("meant") vs "meany" ("meany"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'meany';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_measured_measures_2808',
    name: 'measured vs measures',
    description: 'Commonly confused words: measured and measures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmeasured\b/gi,
    message: 'Check: "measured" ("measured") vs "measures" ("measures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'measures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_meditated_meditates_2809',
    name: 'meditated vs meditates',
    description: 'Commonly confused words: meditated and meditates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmeditated\b/gi,
    message: 'Check: "meditated" ("meditated") vs "meditates" ("meditates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'meditates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_memorialized_memorializes_2810',
    name: 'memorialized vs memorializes',
    description: 'Commonly confused words: memorialized and memorializes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmemorialized\b/gi,
    message: 'Check: "memorialized" ("memorialized") vs "memorializes" ("memorializes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'memorializes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_memorized_memorizes_2811',
    name: 'memorized vs memorizes',
    description: 'Commonly confused words: memorized and memorizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmemorized\b/gi,
    message: 'Check: "memorized" ("memorized") vs "memorizes" ("memorizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'memorizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_menaced_menaces_2812',
    name: 'menaced vs menaces',
    description: 'Commonly confused words: menaced and menaces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmenaced\b/gi,
    message: 'Check: "menaced" ("menaced") vs "menaces" ("menaces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'menaces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_merchandised_merchandises_2813',
    name: 'merchandised vs merchandises',
    description: 'Commonly confused words: merchandised and merchandises',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmerchandised\b/gi,
    message: 'Check: "merchandised" ("merchandised") vs "merchandises" ("merchandises"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'merchandises';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_meres_metes_2814',
    name: 'meres vs metes',
    description: 'Commonly confused words: meres and metes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmeres\b/gi,
    message: 'Check: "meres" ("meres") vs "metes" ("metes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'metes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mesa_mess_2815',
    name: 'mesa vs mess',
    description: 'Commonly confused words: mesa and mess',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmesa\b/gi,
    message: 'Check: "mesa" ("mesa") vs "mess" ("mess"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mess';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mesmerized_mesmerizes_2816',
    name: 'mesmerized vs mesmerizes',
    description: 'Commonly confused words: mesmerized and mesmerizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmesmerized\b/gi,
    message: 'Check: "mesmerized" ("mesmerized") vs "mesmerizes" ("mesmerizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mesmerizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_metabolized_metabolizes_2817',
    name: 'metabolized vs metabolizes',
    description: 'Commonly confused words: metabolized and metabolizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmetabolized\b/gi,
    message: 'Check: "metabolized" ("metabolized") vs "metabolizes" ("metabolizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'metabolizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_metamorphosed_metamorphoses_2818',
    name: 'metamorphosed vs metamorphoses',
    description: 'Commonly confused words: metamorphosed and metamorphoses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmetamorphosed\b/gi,
    message: 'Check: "metamorphosed" ("metamorphosed") vs "metamorphoses" ("metamorphoses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'metamorphoses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_metastasized_metastasizes_2819',
    name: 'metastasized vs metastasizes',
    description: 'Commonly confused words: metastasized and metastasizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmetastasized\b/gi,
    message: 'Check: "metastasized" ("metastasized") vs "metastasizes" ("metastasizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'metastasizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_meted_metes_2820',
    name: 'meted vs metes',
    description: 'Commonly confused words: meted and metes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmeted\b/gi,
    message: 'Check: "meted" ("meted") vs "metes" ("metes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'metes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mettle_nettle_2821',
    name: 'mettle vs nettle',
    description: 'Commonly confused words: mettle and nettle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmettle\b/gi,
    message: 'Check: "mettle" ("mettle") vs "nettle" ("nettle"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nettle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_microwaved_microwaves_2822',
    name: 'microwaved vs microwaves',
    description: 'Commonly confused words: microwaved and microwaves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmicrowaved\b/gi,
    message: 'Check: "microwaved" ("microwaved") vs "microwaves" ("microwaves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'microwaves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_middies_muddies_2823',
    name: 'middies vs muddies',
    description: 'Commonly confused words: middies and muddies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmiddies\b/gi,
    message: 'Check: "middies" ("middies") vs "muddies" ("muddies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'muddies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_middling_muddling_2824',
    name: 'middling vs muddling',
    description: 'Commonly confused words: middling and muddling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmiddling\b/gi,
    message: 'Check: "middling" ("middling") vs "muddling" ("muddling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'muddling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_might_night_2825',
    name: 'might vs night',
    description: 'Commonly confused words: might and night',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmight\b/gi,
    message: 'Check: "might" ("might") vs "night" ("night"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'night';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_miked_mikes_2826',
    name: 'miked vs mikes',
    description: 'Commonly confused words: miked and mikes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmiked\b/gi,
    message: 'Check: "miked" ("miked") vs "mikes" ("mikes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mikes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_milch_mulch_2827',
    name: 'milch vs mulch',
    description: 'Commonly confused words: milch and mulch',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmilch\b/gi,
    message: 'Check: "milch" ("milch") vs "mulch" ("mulch"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mulch';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mild_mile_2828',
    name: 'mild vs mile',
    description: 'Commonly confused words: mild and mile',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmild\b/gi,
    message: 'Check: "mild" ("mild") vs "mile" ("mile"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mile';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_militated_militates_2829',
    name: 'militated vs militates',
    description: 'Commonly confused words: militated and militates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmilitated\b/gi,
    message: 'Check: "militated" ("militated") vs "militates" ("militates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'militates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_milked_milled_2830',
    name: 'milked vs milled',
    description: 'Commonly confused words: milked and milled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmilked\b/gi,
    message: 'Check: "milked" ("milked") vs "milled" ("milled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'milled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_milkers_millers_2831',
    name: 'milkers vs millers',
    description: 'Commonly confused words: milkers and millers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmilkers\b/gi,
    message: 'Check: "milkers" ("milkers") vs "millers" ("millers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'millers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_milking_milling_2832',
    name: 'milking vs milling',
    description: 'Commonly confused words: milking and milling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmilking\b/gi,
    message: 'Check: "milking" ("milking") vs "milling" ("milling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'milling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_minced_minces_2833',
    name: 'minced vs minces',
    description: 'Commonly confused words: minced and minces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bminced\b/gi,
    message: 'Check: "minced" ("minced") vs "minces" ("minces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'minces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_minced_minded_2834',
    name: 'minced vs minded',
    description: 'Commonly confused words: minced and minded',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bminced\b/gi,
    message: 'Check: "minced" ("minced") vs "minded" ("minded"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'minded';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mincer_minder_2835',
    name: 'mincer vs minder',
    description: 'Commonly confused words: mincer and minder',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmincer\b/gi,
    message: 'Check: "mincer" ("mincer") vs "minder" ("minder"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'minder';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mincing_minding_2836',
    name: 'mincing vs minding',
    description: 'Commonly confused words: mincing and minding',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmincing\b/gi,
    message: 'Check: "mincing" ("mincing") vs "minding" ("minding"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'minding';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_minimized_minimizes_2837',
    name: 'minimized vs minimizes',
    description: 'Commonly confused words: minimized and minimizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bminimized\b/gi,
    message: 'Check: "minimized" ("minimized") vs "minimizes" ("minimizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'minimizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mired_mires_2838',
    name: 'mired vs mires',
    description: 'Commonly confused words: mired and mires',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmired\b/gi,
    message: 'Check: "mired" ("mired") vs "mires" ("mires"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mires';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_miscalculated_miscalculates_2839',
    name: 'miscalculated vs miscalculates',
    description: 'Commonly confused words: miscalculated and miscalculates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmiscalculated\b/gi,
    message: 'Check: "miscalculated" ("miscalculated") vs "miscalculates" ("miscalculates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'miscalculates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_misconstrued_misconstrues_2840',
    name: 'misconstrued vs misconstrues',
    description: 'Commonly confused words: misconstrued and misconstrues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmisconstrued\b/gi,
    message: 'Check: "misconstrued" ("misconstrued") vs "misconstrues" ("misconstrues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'misconstrues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_misdiagnosed_misdiagnoses_2841',
    name: 'misdiagnosed vs misdiagnoses',
    description: 'Commonly confused words: misdiagnosed and misdiagnoses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmisdiagnosed\b/gi,
    message: 'Check: "misdiagnosed" ("misdiagnosed") vs "misdiagnoses" ("misdiagnoses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'misdiagnoses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_misers_mixers_2842',
    name: 'misers vs mixers',
    description: 'Commonly confused words: misers and mixers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmisers\b/gi,
    message: 'Check: "misers" ("misers") vs "mixers" ("mixers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mixers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_misguided_misguides_2843',
    name: 'misguided vs misguides',
    description: 'Commonly confused words: misguided and misguides',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmisguided\b/gi,
    message: 'Check: "misguided" ("misguided") vs "misguides" ("misguides"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'misguides';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_misidentified_misidentifies_2844',
    name: 'misidentified vs misidentifies',
    description: 'Commonly confused words: misidentified and misidentifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmisidentified\b/gi,
    message: 'Check: "misidentified" ("misidentified") vs "misidentifies" ("misidentifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'misidentifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_misjudged_misjudges_2845',
    name: 'misjudged vs misjudges',
    description: 'Commonly confused words: misjudged and misjudges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmisjudged\b/gi,
    message: 'Check: "misjudged" ("misjudged") vs "misjudges" ("misjudges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'misjudges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mismatched_mismatches_2846',
    name: 'mismatched vs mismatches',
    description: 'Commonly confused words: mismatched and mismatches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmismatched\b/gi,
    message: 'Check: "mismatched" ("mismatched") vs "mismatches" ("mismatches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mismatches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_misplaced_misplaces_2847',
    name: 'misplaced vs misplaces',
    description: 'Commonly confused words: misplaced and misplaces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmisplaced\b/gi,
    message: 'Check: "misplaced" ("misplaced") vs "misplaces" ("misplaces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'misplaces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_misquoted_misquotes_2848',
    name: 'misquoted vs misquotes',
    description: 'Commonly confused words: misquoted and misquotes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmisquoted\b/gi,
    message: 'Check: "misquoted" ("misquoted") vs "misquotes" ("misquotes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'misquotes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_missing_mussing_2849',
    name: 'missing vs mussing',
    description: 'Commonly confused words: missing and mussing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmissing\b/gi,
    message: 'Check: "missing" ("missing") vs "mussing" ("mussing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mussing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_misstated_misstates_2850',
    name: 'misstated vs misstates',
    description: 'Commonly confused words: misstated and misstates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmisstated\b/gi,
    message: 'Check: "misstated" ("misstated") vs "misstates" ("misstates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'misstates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_misters_musters_2851',
    name: 'misters vs musters',
    description: 'Commonly confused words: misters and musters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmisters\b/gi,
    message: 'Check: "misters" ("misters") vs "musters" ("musters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'musters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_misused_misuses_2852',
    name: 'misused vs misuses',
    description: 'Commonly confused words: misused and misuses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmisused\b/gi,
    message: 'Check: "misused" ("misused") vs "misuses" ("misuses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'misuses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mitigated_mitigates_2853',
    name: 'mitigated vs mitigates',
    description: 'Commonly confused words: mitigated and mitigates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmitigated\b/gi,
    message: 'Check: "mitigated" ("mitigated") vs "mitigates" ("mitigates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mitigates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mobility_nobility_2854',
    name: 'mobility vs nobility',
    description: 'Commonly confused words: mobility and nobility',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmobility\b/gi,
    message: 'Check: "mobility" ("mobility") vs "nobility" ("nobility"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nobility';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mocks_micks_2855',
    name: 'mocks vs micks',
    description: 'Commonly confused words: mocks and micks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmocks\b/gi,
    message: 'Check: "mocks" ("mocks") vs "micks" ("micks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'micks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_modal_nodal_2856',
    name: 'modal vs nodal',
    description: 'Commonly confused words: modal and nodal',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmodal\b/gi,
    message: 'Check: "modal" ("modal") vs "nodal" ("nodal"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nodal';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_moderated_moderates_2857',
    name: 'moderated vs moderates',
    description: 'Commonly confused words: moderated and moderates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmoderated\b/gi,
    message: 'Check: "moderated" ("moderated") vs "moderates" ("moderates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'moderates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_modernized_modernizes_2858',
    name: 'modernized vs modernizes',
    description: 'Commonly confused words: modernized and modernizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmodernized\b/gi,
    message: 'Check: "modernized" ("modernized") vs "modernizes" ("modernizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'modernizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_modular_nodular_2859',
    name: 'modular vs nodular',
    description: 'Commonly confused words: modular and nodular',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmodular\b/gi,
    message: 'Check: "modular" ("modular") vs "nodular" ("nodular"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nodular';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_module_nodule_2860',
    name: 'module vs nodule',
    description: 'Commonly confused words: module and nodule',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmodule\b/gi,
    message: 'Check: "module" ("module") vs "nodule" ("nodule"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nodule';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_modules_nodules_2861',
    name: 'modules vs nodules',
    description: 'Commonly confused words: modules and nodules',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmodules\b/gi,
    message: 'Check: "modules" ("modules") vs "nodules" ("nodules"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nodules';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mold_mild_2862',
    name: 'mold vs mild',
    description: 'Commonly confused words: mold and mild',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmold\b/gi,
    message: 'Check: "mold" ("mold") vs "mild" ("mild"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mild';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_molds_moles_2863',
    name: 'molds vs moles',
    description: 'Commonly confused words: molds and moles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmolds\b/gi,
    message: 'Check: "molds" ("molds") vs "moles" ("moles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'moles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_moored_mooted_2864',
    name: 'moored vs mooted',
    description: 'Commonly confused words: moored and mooted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmoored\b/gi,
    message: 'Check: "moored" ("moored") vs "mooted" ("mooted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mooted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mooted_molted_2865',
    name: 'mooted vs molted',
    description: 'Commonly confused words: mooted and molted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmooted\b/gi,
    message: 'Check: "mooted" ("mooted") vs "molted" ("molted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'molted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_moots_molts_2866',
    name: 'moots vs molts',
    description: 'Commonly confused words: moots and molts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmoots\b/gi,
    message: 'Check: "moots" ("moots") vs "molts" ("molts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'molts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_morels_motels_2867',
    name: 'morels vs motels',
    description: 'Commonly confused words: morels and motels',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmorels\b/gi,
    message: 'Check: "morels" ("morels") vs "motels" ("motels"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'motels';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mortgaged_mortgagee_2868',
    name: 'mortgaged vs mortgagee',
    description: 'Commonly confused words: mortgaged and mortgagee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmortgaged\b/gi,
    message: 'Check: "mortgaged" ("mortgaged") vs "mortgagee" ("mortgagee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mortgagee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mortgaged_mortgages_2869',
    name: 'mortgaged vs mortgages',
    description: 'Commonly confused words: mortgaged and mortgages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmortgaged\b/gi,
    message: 'Check: "mortgaged" ("mortgaged") vs "mortgages" ("mortgages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mortgages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mortised_mortises_2870',
    name: 'mortised vs mortises',
    description: 'Commonly confused words: mortised and mortises',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmortised\b/gi,
    message: 'Check: "mortised" ("mortised") vs "mortises" ("mortises"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mortises';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mosses_misses_2871',
    name: 'mosses vs misses',
    description: 'Commonly confused words: mosses and misses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmosses\b/gi,
    message: 'Check: "mosses" ("mosses") vs "misses" ("misses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'misses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_motes_mites_2872',
    name: 'motes vs mites',
    description: 'Commonly confused words: motes and mites',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmotes\b/gi,
    message: 'Check: "motes" ("motes") vs "mites" ("mites"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mites';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_motion_notion_2873',
    name: 'motion vs notion',
    description: 'Commonly confused words: motion and notion',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmotion\b/gi,
    message: 'Check: "motion" ("motion") vs "notion" ("notion"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'notion';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_motions_notions_2874',
    name: 'motions vs notions',
    description: 'Commonly confused words: motions and notions',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmotions\b/gi,
    message: 'Check: "motions" ("motions") vs "notions" ("notions"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'notions';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_motivated_motivates_2875',
    name: 'motivated vs motivates',
    description: 'Commonly confused words: motivated and motivates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmotivated\b/gi,
    message: 'Check: "motivated" ("motivated") vs "motivates" ("motivates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'motivates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mottled_mottles_2876',
    name: 'mottled vs mottles',
    description: 'Commonly confused words: mottled and mottles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmottled\b/gi,
    message: 'Check: "mottled" ("mottled") vs "mottles" ("mottles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mottles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_moused_mouses_2877',
    name: 'moused vs mouses',
    description: 'Commonly confused words: moused and mouses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmoused\b/gi,
    message: 'Check: "moused" ("moused") vs "mouses" ("mouses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mouses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_muddied_muddies_2878',
    name: 'muddied vs muddies',
    description: 'Commonly confused words: muddied and muddies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmuddied\b/gi,
    message: 'Check: "muddied" ("muddied") vs "muddies" ("muddies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'muddies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_muddled_muddles_2879',
    name: 'muddled vs muddles',
    description: 'Commonly confused words: muddled and muddles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmuddled\b/gi,
    message: 'Check: "muddled" ("muddled") vs "muddles" ("muddles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'muddles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_muffled_muffles_2880',
    name: 'muffled vs muffles',
    description: 'Commonly confused words: muffled and muffles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmuffled\b/gi,
    message: 'Check: "muffled" ("muffled") vs "muffles" ("muffles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'muffles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mug_jug_2881',
    name: 'mug vs jug',
    description: 'Commonly confused words: mug and jug',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmug\b/gi,
    message: 'Check: "mug" ("mug") vs "jug" ("jug"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jug';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mulched_mulches_2882',
    name: 'mulched vs mulches',
    description: 'Commonly confused words: mulched and mulches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmulched\b/gi,
    message: 'Check: "mulched" ("mulched") vs "mulches" ("mulches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mulches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mule_mile_2883',
    name: 'mule vs mile',
    description: 'Commonly confused words: mule and mile',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmule\b/gi,
    message: 'Check: "mule" ("mule") vs "mile" ("mile"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mile';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_multiples_multiplex_2884',
    name: 'multiples vs multiplex',
    description: 'Commonly confused words: multiples and multiplex',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmultiples\b/gi,
    message: 'Check: "multiples" ("multiples") vs "multiplex" ("multiplex"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'multiplex';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_muscled_muscles_2885',
    name: 'muscled vs muscles',
    description: 'Commonly confused words: muscled and muscles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmuscled\b/gi,
    message: 'Check: "muscled" ("muscled") vs "muscles" ("muscles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'muscles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mushed_mushes_2886',
    name: 'mushed vs mushes',
    description: 'Commonly confused words: mushed and mushes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmushed\b/gi,
    message: 'Check: "mushed" ("mushed") vs "mushes" ("mushes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mushes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_muskeg_musket_2887',
    name: 'muskeg vs musket',
    description: 'Commonly confused words: muskeg and musket',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmuskeg\b/gi,
    message: 'Check: "muskeg" ("muskeg") vs "musket" ("musket"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'musket';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_musts_mists_2888',
    name: 'musts vs mists',
    description: 'Commonly confused words: musts and mists',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmusts\b/gi,
    message: 'Check: "musts" ("musts") vs "mists" ("mists"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mists';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_muted_mutes_2889',
    name: 'muted vs mutes',
    description: 'Commonly confused words: muted and mutes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmuted\b/gi,
    message: 'Check: "muted" ("muted") vs "mutes" ("mutes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mutes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mute_mite_2890',
    name: 'mute vs mite',
    description: 'Commonly confused words: mute and mite',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmute\b/gi,
    message: 'Check: "mute" ("mute") vs "mite" ("mite"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mite';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_muter_miter_2891',
    name: 'muter vs miter',
    description: 'Commonly confused words: muter and miter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmuter\b/gi,
    message: 'Check: "muter" ("muter") vs "miter" ("miter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'miter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mutilated_mutilates_2892',
    name: 'mutilated vs mutilates',
    description: 'Commonly confused words: mutilated and mutilates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmutilated\b/gi,
    message: 'Check: "mutilated" ("mutilated") vs "mutilates" ("mutilates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mutilates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_mutinied_mutinies_2893',
    name: 'mutinied vs mutinies',
    description: 'Commonly confused words: mutinied and mutinies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmutinied\b/gi,
    message: 'Check: "mutinied" ("mutinied") vs "mutinies" ("mutinies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'mutinies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_muzzled_muzzles_2894',
    name: 'muzzled vs muzzles',
    description: 'Commonly confused words: muzzled and muzzles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmuzzled\b/gi,
    message: 'Check: "muzzled" ("muzzled") vs "muzzles" ("muzzles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'muzzles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_muzzle_nuzzle_2895',
    name: 'muzzle vs nuzzle',
    description: 'Commonly confused words: muzzle and nuzzle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmuzzle\b/gi,
    message: 'Check: "muzzle" ("muzzle") vs "nuzzle" ("nuzzle"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nuzzle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_muzzling_nuzzling_2896',
    name: 'muzzling vs nuzzling',
    description: 'Commonly confused words: muzzling and nuzzling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bmuzzling\b/gi,
    message: 'Check: "muzzling" ("muzzling") vs "nuzzling" ("nuzzling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nuzzling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
