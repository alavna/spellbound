/**
 * Auto-generated confusion rules (V)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.525Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesV: GrammarRule[] = [
  {
    id: 'confusion_vague_vogue_706',
    name: 'vague vs vogue',
    description: 'Commonly confused words: vague and vogue',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvague\b/gi,
    message: 'Check: "vague" ("vague") vs "vogue" ("vogue"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vogue';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vain_vane_707',
    name: 'vain vs vane',
    description: 'Commonly confused words: vain and vane',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvain\b/gi,
    message: 'Check: "vain" ("vain") vs "vane" ("vane"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vane';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vale_veil_708',
    name: 'vale vs veil',
    description: 'Commonly confused words: vale and veil',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvale\b/gi,
    message: 'Check: "vale" ("vale") vs "veil" ("veil"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'veil';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vane_vein_709',
    name: 'vane vs vein',
    description: 'Commonly confused words: vane and vein',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvane\b/gi,
    message: 'Check: "vane" ("vane") vs "vein" ("vein"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vein';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vantage_vintage_710',
    name: 'vantage vs vintage',
    description: 'Commonly confused words: vantage and vintage',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvantage\b/gi,
    message: 'Check: "vantage" ("vantage") vs "vintage" ("vintage"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vintage';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vary_very_711',
    name: 'vary vs very',
    description: 'Commonly confused words: vary and very',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvary\b/gi,
    message: 'Check: "vary" ("vary") vs "very" ("very"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'very';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_verses_versus_712',
    name: 'verses vs versus',
    description: 'Commonly confused words: verses and versus',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bverses\b/gi,
    message: 'Check: "verses" ("verses") vs "versus" ("versus"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'versus';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vial_vile_713',
    name: 'vial vs vile',
    description: 'Commonly confused words: vial and vile',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvial\b/gi,
    message: 'Check: "vial" ("vial") vs "vile" ("vile"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vile';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vicious_viscous_714',
    name: 'vicious vs viscous',
    description: 'Commonly confused words: vicious and viscous',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvicious\b/gi,
    message: 'Check: "vicious" ("vicious") vs "viscous" ("viscous"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'viscous';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vile_viol_715',
    name: 'vile vs viol',
    description: 'Commonly confused words: vile and viol',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvile\b/gi,
    message: 'Check: "vile" ("vile") vs "viol" ("viol"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'viol';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vacillated_vacillates_4174',
    name: 'vacillated vs vacillates',
    description: 'Commonly confused words: vacillated and vacillates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvacillated\b/gi,
    message: 'Check: "vacillated" ("vacillated") vs "vacillates" ("vacillates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vacillates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vain_fain_4175',
    name: 'vain vs fain',
    description: 'Commonly confused words: vain and fain',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvain\b/gi,
    message: 'Check: "vain" ("vain") vs "fain" ("fain"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fain';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_validated_validates_4176',
    name: 'validated vs validates',
    description: 'Commonly confused words: validated and validates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvalidated\b/gi,
    message: 'Check: "validated" ("validated") vs "validates" ("validates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'validates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_valued_values_4177',
    name: 'valued vs values',
    description: 'Commonly confused words: valued and values',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvalued\b/gi,
    message: 'Check: "valued" ("valued") vs "values" ("values"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'values';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vamped_camped_4178',
    name: 'vamped vs camped',
    description: 'Commonly confused words: vamped and camped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvamped\b/gi,
    message: 'Check: "vamped" ("vamped") vs "camped" ("camped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'camped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vane_cane_4179',
    name: 'vane vs cane',
    description: 'Commonly confused words: vane and cane',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvane\b/gi,
    message: 'Check: "vane" ("vane") vs "cane" ("cane"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cane';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vanished_vanishes_4180',
    name: 'vanished vs vanishes',
    description: 'Commonly confused words: vanished and vanishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvanished\b/gi,
    message: 'Check: "vanished" ("vanished") vs "vanishes" ("vanishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vanishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vanning_banning_4181',
    name: 'vanning vs banning',
    description: 'Commonly confused words: vanning and banning',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvanning\b/gi,
    message: 'Check: "vanning" ("vanning") vs "banning" ("banning"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'banning';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vanquished_vanquishes_4182',
    name: 'vanquished vs vanquishes',
    description: 'Commonly confused words: vanquished and vanquishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvanquished\b/gi,
    message: 'Check: "vanquished" ("vanquished") vs "vanquishes" ("vanquishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vanquishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vaporized_vaporizes_4183',
    name: 'vaporized vs vaporizes',
    description: 'Commonly confused words: vaporized and vaporizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvaporized\b/gi,
    message: 'Check: "vaporized" ("vaporized") vs "vaporizes" ("vaporizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vaporizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_var_bar_4184',
    name: 'var vs bar',
    description: 'Commonly confused words: var and bar',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvar\b/gi,
    message: 'Check: "var" ("var") vs "bar" ("bar"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bar';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_varied_varies_4185',
    name: 'varied vs varies',
    description: 'Commonly confused words: varied and varies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvaried\b/gi,
    message: 'Check: "varied" ("varied") vs "varies" ("varies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'varies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_varnished_varnishes_4186',
    name: 'varnished vs varnishes',
    description: 'Commonly confused words: varnished and varnishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvarnished\b/gi,
    message: 'Check: "varnished" ("varnished") vs "varnishes" ("varnishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'varnishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vases_bases_4187',
    name: 'vases vs bases',
    description: 'Commonly confused words: vases and bases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvases\b/gi,
    message: 'Check: "vases" ("vases") vs "bases" ("bases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vast_bast_4188',
    name: 'vast vs bast',
    description: 'Commonly confused words: vast and bast',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvast\b/gi,
    message: 'Check: "vast" ("vast") vs "bast" ("bast"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bast';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vast_cast_4189',
    name: 'vast vs cast',
    description: 'Commonly confused words: vast and cast',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvast\b/gi,
    message: 'Check: "vast" ("vast") vs "cast" ("cast"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cast';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vaster_baster_4190',
    name: 'vaster vs baster',
    description: 'Commonly confused words: vaster and baster',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvaster\b/gi,
    message: 'Check: "vaster" ("vaster") vs "baster" ("baster"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'baster';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vaster_caster_4191',
    name: 'vaster vs caster',
    description: 'Commonly confused words: vaster and caster',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvaster\b/gi,
    message: 'Check: "vaster" ("vaster") vs "caster" ("caster"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'caster';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vast_fast_4192',
    name: 'vast vs fast',
    description: 'Commonly confused words: vast and fast',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvast\b/gi,
    message: 'Check: "vast" ("vast") vs "fast" ("fast"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fast';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vatted_batted_4193',
    name: 'vatted vs batted',
    description: 'Commonly confused words: vatted and batted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvatted\b/gi,
    message: 'Check: "vatted" ("vatted") vs "batted" ("batted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'batted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_veers_beers_4194',
    name: 'veers vs beers',
    description: 'Commonly confused words: veers and beers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bveers\b/gi,
    message: 'Check: "veers" ("veers") vs "beers" ("beers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'beers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vended_fended_4195',
    name: 'vended vs fended',
    description: 'Commonly confused words: vended and fended',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvended\b/gi,
    message: 'Check: "vended" ("vended") vs "fended" ("fended"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fended';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vend_fend_4196',
    name: 'vend vs fend',
    description: 'Commonly confused words: vend and fend',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvend\b/gi,
    message: 'Check: "vend" ("vend") vs "fend" ("fend"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fend';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vending_bending_4197',
    name: 'vending vs bending',
    description: 'Commonly confused words: vending and bending',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvending\b/gi,
    message: 'Check: "vending" ("vending") vs "bending" ("bending"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bending';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vending_fending_4198',
    name: 'vending vs fending',
    description: 'Commonly confused words: vending and fending',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvending\b/gi,
    message: 'Check: "vending" ("vending") vs "fending" ("fending"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fending';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vends_bends_4199',
    name: 'vends vs bends',
    description: 'Commonly confused words: vends and bends',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvends\b/gi,
    message: 'Check: "vends" ("vends") vs "bends" ("bends"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bends';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vends_fends_4200',
    name: 'vends vs fends',
    description: 'Commonly confused words: vends and fends',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvends\b/gi,
    message: 'Check: "vends" ("vends") vs "fends" ("fends"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fends';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_venerated_venerates_4201',
    name: 'venerated vs venerates',
    description: 'Commonly confused words: venerated and venerates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvenerated\b/gi,
    message: 'Check: "venerated" ("venerated") vs "venerates" ("venerates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'venerates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vents_bents_4202',
    name: 'vents vs bents',
    description: 'Commonly confused words: vents and bents',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvents\b/gi,
    message: 'Check: "vents" ("vents") vs "bents" ("bents"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bents';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vents_cents_4203',
    name: 'vents vs cents',
    description: 'Commonly confused words: vents and cents',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvents\b/gi,
    message: 'Check: "vents" ("vents") vs "cents" ("cents"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cents';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_verbalized_verbalizes_4204',
    name: 'verbalized vs verbalizes',
    description: 'Commonly confused words: verbalized and verbalizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bverbalized\b/gi,
    message: 'Check: "verbalized" ("verbalized") vs "verbalizes" ("verbalizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'verbalizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_verbal_vernal_4205',
    name: 'verbal vs vernal',
    description: 'Commonly confused words: verbal and vernal',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bverbal\b/gi,
    message: 'Check: "verbal" ("verbal") vs "vernal" ("vernal"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vernal';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_verged_verges_4206',
    name: 'verged vs verges',
    description: 'Commonly confused words: verged and verges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bverged\b/gi,
    message: 'Check: "verged" ("verged") vs "verges" ("verges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'verges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_versed_verses_4207',
    name: 'versed vs verses',
    description: 'Commonly confused words: versed and verses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bversed\b/gi,
    message: 'Check: "versed" ("versed") vs "verses" ("verses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'verses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vests_bests_4208',
    name: 'vests vs bests',
    description: 'Commonly confused words: vests and bests',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvests\b/gi,
    message: 'Check: "vests" ("vests") vs "bests" ("bests"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bests';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vexed_vexes_4209',
    name: 'vexed vs vexes',
    description: 'Commonly confused words: vexed and vexes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvexed\b/gi,
    message: 'Check: "vexed" ("vexed") vs "vexes" ("vexes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vexes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_victimized_victimizes_4210',
    name: 'victimized vs victimizes',
    description: 'Commonly confused words: victimized and victimizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvictimized\b/gi,
    message: 'Check: "victimized" ("victimized") vs "victimizes" ("victimizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'victimizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_videotaped_videotapes_4211',
    name: 'videotaped vs videotapes',
    description: 'Commonly confused words: videotaped and videotapes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvideotaped\b/gi,
    message: 'Check: "videotaped" ("videotaped") vs "videotapes" ("videotapes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'videotapes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vied_vies_4212',
    name: 'vied vs vies',
    description: 'Commonly confused words: vied and vies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvied\b/gi,
    message: 'Check: "vied" ("vied") vs "vies" ("vies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vie_fie_4213',
    name: 'vie vs fie',
    description: 'Commonly confused words: vie and fie',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvie\b/gi,
    message: 'Check: "vie" ("vie") vs "fie" ("fie"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fie';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vignetted_vignettes_4214',
    name: 'vignetted vs vignettes',
    description: 'Commonly confused words: vignetted and vignettes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvignetted\b/gi,
    message: 'Check: "vignetted" ("vignetted") vs "vignettes" ("vignettes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vignettes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vile_bile_4215',
    name: 'vile vs bile',
    description: 'Commonly confused words: vile and bile',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvile\b/gi,
    message: 'Check: "vile" ("vile") vs "bile" ("bile"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bile';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vilified_vilifies_4216',
    name: 'vilified vs vilifies',
    description: 'Commonly confused words: vilified and vilifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvilified\b/gi,
    message: 'Check: "vilified" ("vilified") vs "vilifies" ("vilifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vilifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_villa_viola_4217',
    name: 'villa vs viola',
    description: 'Commonly confused words: villa and viola',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvilla\b/gi,
    message: 'Check: "villa" ("villa") vs "viola" ("viola"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'viola';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vindicated_vindicates_4218',
    name: 'vindicated vs vindicates',
    description: 'Commonly confused words: vindicated and vindicates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvindicated\b/gi,
    message: 'Check: "vindicated" ("vindicated") vs "vindicates" ("vindicates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vindicates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vine_fine_4219',
    name: 'vine vs fine',
    description: 'Commonly confused words: vine and fine',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvine\b/gi,
    message: 'Check: "vine" ("vine") vs "fine" ("fine"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fine';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vines_fines_4220',
    name: 'vines vs fines',
    description: 'Commonly confused words: vines and fines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvines\b/gi,
    message: 'Check: "vines" ("vines") vs "fines" ("fines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_visualized_visualizes_4221',
    name: 'visualized vs visualizes',
    description: 'Commonly confused words: visualized and visualizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvisualized\b/gi,
    message: 'Check: "visualized" ("visualized") vs "visualizes" ("visualizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'visualizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vitiated_vitiates_4222',
    name: 'vitiated vs vitiates',
    description: 'Commonly confused words: vitiated and vitiates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvitiated\b/gi,
    message: 'Check: "vitiated" ("vitiated") vs "vitiates" ("vitiates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vitiates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_viz_biz_4223',
    name: 'viz vs biz',
    description: 'Commonly confused words: viz and biz',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bviz\b/gi,
    message: 'Check: "viz" ("viz") vs "biz" ("biz"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'biz';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_voiced_voices_4224',
    name: 'voiced vs voices',
    description: 'Commonly confused words: voiced and voices',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvoiced\b/gi,
    message: 'Check: "voiced" ("voiced") vs "voices" ("voices"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'voices';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vole_bole_4225',
    name: 'vole vs bole',
    description: 'Commonly confused words: vole and bole',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvole\b/gi,
    message: 'Check: "vole" ("vole") vs "bole" ("bole"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bole';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_voles_boles_4226',
    name: 'voles vs boles',
    description: 'Commonly confused words: voles and boles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvoles\b/gi,
    message: 'Check: "voles" ("voles") vs "boles" ("boles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'boles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vols_cols_4227',
    name: 'vols vs cols',
    description: 'Commonly confused words: vols and cols',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvols\b/gi,
    message: 'Check: "vols" ("vols") vs "cols" ("cols"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cols';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_volt_bolt_4228',
    name: 'volt vs bolt',
    description: 'Commonly confused words: volt and bolt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvolt\b/gi,
    message: 'Check: "volt" ("volt") vs "bolt" ("bolt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bolt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_volts_bolts_4229',
    name: 'volts vs bolts',
    description: 'Commonly confused words: volts and bolts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvolts\b/gi,
    message: 'Check: "volts" ("volts") vs "bolts" ("bolts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bolts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_volts_colts_4230',
    name: 'volts vs colts',
    description: 'Commonly confused words: volts and colts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvolts\b/gi,
    message: 'Check: "volts" ("volts") vs "colts" ("colts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'colts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_votes_cotes_4231',
    name: 'votes vs cotes',
    description: 'Commonly confused words: votes and cotes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvotes\b/gi,
    message: 'Check: "votes" ("votes") vs "cotes" ("cotes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cotes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vouched_vouches_4232',
    name: 'vouched vs vouches',
    description: 'Commonly confused words: vouched and vouches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvouched\b/gi,
    message: 'Check: "vouched" ("vouched") vs "vouches" ("vouches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vouches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vow_cow_4233',
    name: 'vow vs cow',
    description: 'Commonly confused words: vow and cow',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvow\b/gi,
    message: 'Check: "vow" ("vow") vs "cow" ("cow"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cow';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vowed_bowed_4234',
    name: 'vowed vs bowed',
    description: 'Commonly confused words: vowed and bowed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvowed\b/gi,
    message: 'Check: "vowed" ("vowed") vs "bowed" ("bowed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bowed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vowed_cowed_4235',
    name: 'vowed vs cowed',
    description: 'Commonly confused words: vowed and cowed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvowed\b/gi,
    message: 'Check: "vowed" ("vowed") vs "cowed" ("cowed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cowed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vowel_bowel_4236',
    name: 'vowel vs bowel',
    description: 'Commonly confused words: vowel and bowel',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvowel\b/gi,
    message: 'Check: "vowel" ("vowel") vs "bowel" ("bowel"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bowel';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vowing_bowing_4237',
    name: 'vowing vs bowing',
    description: 'Commonly confused words: vowing and bowing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvowing\b/gi,
    message: 'Check: "vowing" ("vowing") vs "bowing" ("bowing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bowing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vowing_cowing_4238',
    name: 'vowing vs cowing',
    description: 'Commonly confused words: vowing and cowing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvowing\b/gi,
    message: 'Check: "vowing" ("vowing") vs "cowing" ("cowing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cowing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vows_bows_4239',
    name: 'vows vs bows',
    description: 'Commonly confused words: vows and bows',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvows\b/gi,
    message: 'Check: "vows" ("vows") vs "bows" ("bows"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'bows';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_vows_cows_4240',
    name: 'vows vs cows',
    description: 'Commonly confused words: vows and cows',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvows\b/gi,
    message: 'Check: "vows" ("vows") vs "cows" ("cows"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cows';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_voyaged_voyages_4241',
    name: 'voyaged vs voyages',
    description: 'Commonly confused words: voyaged and voyages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bvoyaged\b/gi,
    message: 'Check: "voyaged" ("voyaged") vs "voyages" ("voyages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'voyages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
