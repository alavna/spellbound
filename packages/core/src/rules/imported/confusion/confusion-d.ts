/**
 * Auto-generated confusion rules (D)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.504Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesD: GrammarRule[] = [
  {
    id: 'confusion_dairy_diary_223',
    name: 'dairy vs diary',
    description: 'Commonly confused words: dairy and diary',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdairy\b/gi,
    message: 'Check: "dairy" ("dairy") vs "diary" ("diary"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'diary';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dam_damn_224',
    name: 'dam vs damn',
    description: 'Commonly confused words: dam and damn',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdam\b/gi,
    message: 'Check: "dam" ("dam") vs "damn" ("damn"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'damn';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dear_deer_225',
    name: 'dear vs deer',
    description: 'Commonly confused words: dear and deer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdear\b/gi,
    message: 'Check: "dear" ("dear") vs "deer" ("deer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deceased_diseased_226',
    name: 'deceased vs diseased',
    description: 'Commonly confused words: deceased and diseased',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeceased\b/gi,
    message: 'Check: "deceased" ("deceased") vs "diseased" ("diseased"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'diseased';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_decent_descent_227',
    name: 'decent vs descent',
    description: 'Commonly confused words: decent and descent',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdecent\b/gi,
    message: 'Check: "decent" ("decent") vs "descent" ("descent"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'descent';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deducible_deductible_228',
    name: 'deducible vs deductible',
    description: 'Commonly confused words: deducible and deductible',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeducible\b/gi,
    message: 'Check: "deducible" ("deducible") vs "deductible" ("deductible"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deductible';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_defamation_deformation_229',
    name: 'defamation vs deformation',
    description: 'Commonly confused words: defamation and deformation',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdefamation\b/gi,
    message: 'Check: "defamation" ("defamation") vs "deformation" ("deformation"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deformation';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_defamations_deformations_230',
    name: 'defamations vs deformations',
    description: 'Commonly confused words: defamations and deformations',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdefamations\b/gi,
    message: 'Check: "defamations" ("defamations") vs "deformations" ("deformations"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deformations';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_defiantly_definitely_231',
    name: 'defiantly vs definitely',
    description: 'Commonly confused words: defiantly and definitely',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdefiantly\b/gi,
    message: 'Check: "defiantly" ("defiantly") vs "definitely" ("definitely"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'definitely';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_defuse_diffuse_232',
    name: 'defuse vs diffuse',
    description: 'Commonly confused words: defuse and diffuse',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdefuse\b/gi,
    message: 'Check: "defuse" ("defuse") vs "diffuse" ("diffuse"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'diffuse';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_defuses_diffuses_233',
    name: 'defuses vs diffuses',
    description: 'Commonly confused words: defuses and diffuses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdefuses\b/gi,
    message: 'Check: "defuses" ("defuses") vs "diffuses" ("diffuses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'diffuses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_demur_demure_234',
    name: 'demur vs demure',
    description: 'Commonly confused words: demur and demure',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdemur\b/gi,
    message: 'Check: "demur" ("demur") vs "demure" ("demure"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'demure';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_descent_dissent_235',
    name: 'descent vs dissent',
    description: 'Commonly confused words: descent and dissent',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdescent\b/gi,
    message: 'Check: "descent" ("descent") vs "dissent" ("dissent"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dissent';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_desert_dessert_236',
    name: 'desert vs dessert',
    description: 'Commonly confused words: desert and dessert',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdesert\b/gi,
    message: 'Check: "desert" (dry land or to abandon) vs "dessert" (sweet course after a meal). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dessert';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deviant_deviate_237',
    name: 'deviant vs deviate',
    description: 'Commonly confused words: deviant and deviate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeviant\b/gi,
    message: 'Check: "deviant" ("deviant") vs "deviate" ("deviate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deviate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deviants_deviates_238',
    name: 'deviants vs deviates',
    description: 'Commonly confused words: deviants and deviates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeviants\b/gi,
    message: 'Check: "deviants" ("deviants") vs "deviates" ("deviates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deviates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_device_devise_239',
    name: 'device vs devise',
    description: 'Commonly confused words: device and devise',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdevice\b/gi,
    message: 'Check: "device" ("device") vs "devise" ("devise"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'devise';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_diagnoses_diagnosis_240',
    name: 'diagnoses vs diagnosis',
    description: 'Commonly confused words: diagnoses and diagnosis',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiagnoses\b/gi,
    message: 'Check: "diagnoses" ("diagnoses") vs "diagnosis" ("diagnosis"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'diagnosis';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dialogue_discuss_241',
    name: 'dialogue vs discuss',
    description: 'Commonly confused words: dialogue and discuss',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdialogue\b/gi,
    message: 'Check: "dialogue" ("dialogue") vs "discuss" ("discuss"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'discuss';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_died_dyed_242',
    name: 'died vs dyed',
    description: 'Commonly confused words: died and dyed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdied\b/gi,
    message: 'Check: "died" ("died") vs "dyed" ("dyed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dyed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_die_dye_243',
    name: 'die vs dye',
    description: 'Commonly confused words: die and dye',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdie\b/gi,
    message: 'Check: "die" (to cease living) vs "dye" (to color). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dye';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dies_dyes_244',
    name: 'dies vs dyes',
    description: 'Commonly confused words: dies and dyes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdies\b/gi,
    message: 'Check: "dies" ("dies") vs "dyes" ("dyes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dyes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dire_dyer_245',
    name: 'dire vs dyer',
    description: 'Commonly confused words: dire and dyer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdire\b/gi,
    message: 'Check: "dire" ("dire") vs "dyer" ("dyer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dyer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_discreet_discrete_246',
    name: 'discreet vs discrete',
    description: 'Commonly confused words: discreet and discrete',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiscreet\b/gi,
    message: 'Check: "discreet" (careful or prudent) vs "discrete" (separate or distinct). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'discrete';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_discussed_disgust_247',
    name: 'discussed vs disgust',
    description: 'Commonly confused words: discussed and disgust',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiscussed\b/gi,
    message: 'Check: "discussed" ("discussed") vs "disgust" ("disgust"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disgust';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disinterested_uninterested_248',
    name: 'disinterested vs uninterested',
    description: 'Commonly confused words: disinterested and uninterested',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisinterested\b/gi,
    message: 'Check: "disinterested" ("disinterested") vs "uninterested" ("uninterested"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'uninterested';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_does_dose_249',
    name: 'does vs dose',
    description: 'Commonly confused words: does and dose',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdoes\b/gi,
    message: 'Check: "does" ("does") vs "dose" ("dose"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dose';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dos_dues_250',
    name: 'dos vs dues',
    description: 'Commonly confused words: dos and dues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdos\b/gi,
    message: 'Check: "dos" ("dos") vs "dues" ("dues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dosed_dozed_251',
    name: 'dosed vs dozed',
    description: 'Commonly confused words: dosed and dozed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdosed\b/gi,
    message: 'Check: "dosed" ("dosed") vs "dozed" ("dozed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dozed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_do_to_252',
    name: 'do vs to',
    description: 'Commonly confused words: do and to',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdo\b/gi,
    message: 'Check: "do" ("do") vs "to" (preposition indicating direction). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'to';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dragged_drug_253',
    name: 'dragged vs drug',
    description: 'Commonly confused words: dragged and drug',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdragged\b/gi,
    message: 'Check: "dragged" ("dragged") vs "drug" ("drug"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drug';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dual_duel_254',
    name: 'dual vs duel',
    description: 'Commonly confused words: dual and duel',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdual\b/gi,
    message: 'Check: "dual" ("dual") vs "duel" ("duel"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'duel';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dhows_shows_764',
    name: 'dhows vs shows',
    description: 'Commonly confused words: dhows and shows',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdhows\b/gi,
    message: 'Check: "dhows" ("dhows") vs "shows" ("shows"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'shows';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_delegate_delicate_784',
    name: 'delegate vs delicate',
    description: 'Commonly confused words: delegate and delicate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdelegate\b/gi,
    message: 'Check: "delegate" ("delegate") vs "delicate" ("delicate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'delicate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dabbled_dabbles_1482',
    name: 'dabbled vs dabbles',
    description: 'Commonly confused words: dabbled and dabbles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdabbled\b/gi,
    message: 'Check: "dabbled" ("dabbled") vs "dabbles" ("dabbles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dabbles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_daddy_faddy_1483',
    name: 'daddy vs faddy',
    description: 'Commonly confused words: daddy and faddy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdaddy\b/gi,
    message: 'Check: "daddy" ("daddy") vs "faddy" ("faddy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'faddy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dafter_darter_1484',
    name: 'dafter vs darter',
    description: 'Commonly confused words: dafter and darter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdafter\b/gi,
    message: 'Check: "dafter" ("dafter") vs "darter" ("darter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'darter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dairy_fairy_1485',
    name: 'dairy vs fairy',
    description: 'Commonly confused words: dairy and fairy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdairy\b/gi,
    message: 'Check: "dairy" ("dairy") vs "fairy" ("fairy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fairy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dallied_sallied_1486',
    name: 'dallied vs sallied',
    description: 'Commonly confused words: dallied and sallied',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdallied\b/gi,
    message: 'Check: "dallied" ("dallied") vs "sallied" ("sallied"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sallied';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dallies_sallies_1487',
    name: 'dallies vs sallies',
    description: 'Commonly confused words: dallies and sallies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdallies\b/gi,
    message: 'Check: "dallies" ("dallies") vs "sallies" ("sallies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sallies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_damaged_damages_1488',
    name: 'damaged vs damages',
    description: 'Commonly confused words: damaged and damages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdamaged\b/gi,
    message: 'Check: "damaged" ("damaged") vs "damages" ("damages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'damages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dame_came_1489',
    name: 'dame vs came',
    description: 'Commonly confused words: dame and came',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdame\b/gi,
    message: 'Check: "dame" ("dame") vs "came" ("came"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'came';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_danced_dances_1490',
    name: 'danced vs dances',
    description: 'Commonly confused words: danced and dances',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdanced\b/gi,
    message: 'Check: "danced" ("danced") vs "dances" ("dances"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dances';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_danged_fanged_1491',
    name: 'danged vs fanged',
    description: 'Commonly confused words: danged and fanged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdanged\b/gi,
    message: 'Check: "danged" ("danged") vs "fanged" ("fanged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fanged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dangs_fangs_1492',
    name: 'dangs vs fangs',
    description: 'Commonly confused words: dangs and fangs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdangs\b/gi,
    message: 'Check: "dangs" ("dangs") vs "fangs" ("fangs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fangs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dappled_dapples_1493',
    name: 'dappled vs dapples',
    description: 'Commonly confused words: dappled and dapples',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdappled\b/gi,
    message: 'Check: "dappled" ("dappled") vs "dapples" ("dapples"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dapples';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dared_cared_1494',
    name: 'dared vs cared',
    description: 'Commonly confused words: dared and cared',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdared\b/gi,
    message: 'Check: "dared" ("dared") vs "cared" ("cared"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cared';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_darned_earned_1495',
    name: 'darned vs earned',
    description: 'Commonly confused words: darned and earned',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdarned\b/gi,
    message: 'Check: "darned" ("darned") vs "earned" ("earned"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'earned';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_darning_earning_1496',
    name: 'darning vs earning',
    description: 'Commonly confused words: darning and earning',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdarning\b/gi,
    message: 'Check: "darning" ("darning") vs "earning" ("earning"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'earning';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_darted_carted_1497',
    name: 'darted vs carted',
    description: 'Commonly confused words: darted and carted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdarted\b/gi,
    message: 'Check: "darted" ("darted") vs "carted" ("carted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'carted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_darted_farted_1498',
    name: 'darted vs farted',
    description: 'Commonly confused words: darted and farted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdarted\b/gi,
    message: 'Check: "darted" ("darted") vs "farted" ("farted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'farted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_darts_carts_1499',
    name: 'darts vs carts',
    description: 'Commonly confused words: darts and carts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdarts\b/gi,
    message: 'Check: "darts" ("darts") vs "carts" ("carts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'carts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dashed_dashes_1500',
    name: 'dashed vs dashes',
    description: 'Commonly confused words: dashed and dashes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdashed\b/gi,
    message: 'Check: "dashed" ("dashed") vs "dashes" ("dashes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dashes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dated_dared_1501',
    name: 'dated vs dared',
    description: 'Commonly confused words: dated and dared',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdated\b/gi,
    message: 'Check: "dated" ("dated") vs "dared" ("dared"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dared';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dated_dates_1502',
    name: 'dated vs dates',
    description: 'Commonly confused words: dated and dates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdated\b/gi,
    message: 'Check: "dated" ("dated") vs "dates" ("dates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_datelined_datelines_1503',
    name: 'datelined vs datelines',
    description: 'Commonly confused words: datelined and datelines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdatelined\b/gi,
    message: 'Check: "datelined" ("datelined") vs "datelines" ("datelines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'datelines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_daters_caters_1504',
    name: 'daters vs caters',
    description: 'Commonly confused words: daters and caters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdaters\b/gi,
    message: 'Check: "daters" ("daters") vs "caters" ("caters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'caters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_daters_eaters_1505',
    name: 'daters vs eaters',
    description: 'Commonly confused words: daters and eaters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdaters\b/gi,
    message: 'Check: "daters" ("daters") vs "eaters" ("eaters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eaters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_daubed_daybed_1506',
    name: 'daubed vs daybed',
    description: 'Commonly confused words: daubed and daybed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdaubed\b/gi,
    message: 'Check: "daubed" ("daubed") vs "daybed" ("daybed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'daybed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dawned_fawned_1507',
    name: 'dawned vs fawned',
    description: 'Commonly confused words: dawned and fawned',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdawned\b/gi,
    message: 'Check: "dawned" ("dawned") vs "fawned" ("fawned"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fawned';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dawns_fawns_1508',
    name: 'dawns vs fawns',
    description: 'Commonly confused words: dawns and fawns',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdawns\b/gi,
    message: 'Check: "dawns" ("dawns") vs "fawns" ("fawns"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fawns';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dazzled_dazzles_1509',
    name: 'dazzled vs dazzles',
    description: 'Commonly confused words: dazzled and dazzles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdazzled\b/gi,
    message: 'Check: "dazzled" ("dazzled") vs "dazzles" ("dazzles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dazzles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deactivated_deactivates_1510',
    name: 'deactivated vs deactivates',
    description: 'Commonly confused words: deactivated and deactivates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeactivated\b/gi,
    message: 'Check: "deactivated" ("deactivated") vs "deactivates" ("deactivates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deactivates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deaden_deafen_1511',
    name: 'deaden vs deafen',
    description: 'Commonly confused words: deaden and deafen',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeaden\b/gi,
    message: 'Check: "deaden" ("deaden") vs "deafen" ("deafen"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deafen';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deadened_deafened_1512',
    name: 'deadened vs deafened',
    description: 'Commonly confused words: deadened and deafened',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeadened\b/gi,
    message: 'Check: "deadened" ("deadened") vs "deafened" ("deafened"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deafened';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deadening_deafening_1513',
    name: 'deadening vs deafening',
    description: 'Commonly confused words: deadening and deafening',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeadening\b/gi,
    message: 'Check: "deadening" ("deadening") vs "deafening" ("deafening"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deafening';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deafness_dearness_1514',
    name: 'deafness vs dearness',
    description: 'Commonly confused words: deafness and dearness',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeafness\b/gi,
    message: 'Check: "deafness" ("deafness") vs "dearness" ("dearness"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dearness';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dealing_sealing_1515',
    name: 'dealing vs sealing',
    description: 'Commonly confused words: dealing and sealing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdealing\b/gi,
    message: 'Check: "dealing" ("dealing") vs "sealing" ("sealing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sealing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_debased_debases_1516',
    name: 'debased vs debases',
    description: 'Commonly confused words: debased and debases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdebased\b/gi,
    message: 'Check: "debased" ("debased") vs "debases" ("debases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'debases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_debated_debates_1517',
    name: 'debated vs debates',
    description: 'Commonly confused words: debated and debates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdebated\b/gi,
    message: 'Check: "debated" ("debated") vs "debates" ("debates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'debates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_debauched_debauchee_1518',
    name: 'debauched vs debauchee',
    description: 'Commonly confused words: debauched and debauchee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdebauched\b/gi,
    message: 'Check: "debauched" ("debauched") vs "debauchee" ("debauchee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'debauchee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_debit_debut_1519',
    name: 'debit vs debut',
    description: 'Commonly confused words: debit and debut',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdebit\b/gi,
    message: 'Check: "debit" ("debit") vs "debut" ("debut"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'debut';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_debt_dent_1520',
    name: 'debt vs dent',
    description: 'Commonly confused words: debt and dent',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdebt\b/gi,
    message: 'Check: "debt" ("debt") vs "dent" ("dent"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dent';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_debug_debut_1521',
    name: 'debug vs debut',
    description: 'Commonly confused words: debug and debut',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdebug\b/gi,
    message: 'Check: "debug" ("debug") vs "debut" ("debut"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'debut';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_decal_fecal_1522',
    name: 'decal vs fecal',
    description: 'Commonly confused words: decal and fecal',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdecal\b/gi,
    message: 'Check: "decal" ("decal") vs "fecal" ("fecal"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fecal';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_decapitated_decapitates_1523',
    name: 'decapitated vs decapitates',
    description: 'Commonly confused words: decapitated and decapitates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdecapitated\b/gi,
    message: 'Check: "decapitated" ("decapitated") vs "decapitates" ("decapitates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'decapitates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deceased_deceases_1524',
    name: 'deceased vs deceases',
    description: 'Commonly confused words: deceased and deceases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeceased\b/gi,
    message: 'Check: "deceased" ("deceased") vs "deceases" ("deceases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deceases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deceived_deceives_1525',
    name: 'deceived vs deceives',
    description: 'Commonly confused words: deceived and deceives',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeceived\b/gi,
    message: 'Check: "deceived" ("deceived") vs "deceives" ("deceives"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deceives';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_decimated_decimates_1526',
    name: 'decimated vs decimates',
    description: 'Commonly confused words: decimated and decimates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdecimated\b/gi,
    message: 'Check: "decimated" ("decimated") vs "decimates" ("decimates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'decimates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_decoded_decodes_1527',
    name: 'decoded vs decodes',
    description: 'Commonly confused words: decoded and decodes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdecoded\b/gi,
    message: 'Check: "decoded" ("decoded") vs "decodes" ("decodes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'decodes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_decoding_deciding_1528',
    name: 'decoding vs deciding',
    description: 'Commonly confused words: decoding and deciding',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdecoding\b/gi,
    message: 'Check: "decoding" ("decoding") vs "deciding" ("deciding"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deciding';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_decomposed_decomposes_1529',
    name: 'decomposed vs decomposes',
    description: 'Commonly confused words: decomposed and decomposes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdecomposed\b/gi,
    message: 'Check: "decomposed" ("decomposed") vs "decomposes" ("decomposes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'decomposes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_decompressed_decompresses_1530',
    name: 'decompressed vs decompresses',
    description: 'Commonly confused words: decompressed and decompresses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdecompressed\b/gi,
    message: 'Check: "decompressed" ("decompressed") vs "decompresses" ("decompresses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'decompresses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_decorated_decorates_1531',
    name: 'decorated vs decorates',
    description: 'Commonly confused words: decorated and decorates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdecorated\b/gi,
    message: 'Check: "decorated" ("decorated") vs "decorates" ("decorates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'decorates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_decoupled_decouples_1532',
    name: 'decoupled vs decouples',
    description: 'Commonly confused words: decoupled and decouples',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdecoupled\b/gi,
    message: 'Check: "decoupled" ("decoupled") vs "decouples" ("decouples"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'decouples';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_decreed_decrees_1533',
    name: 'decreed vs decrees',
    description: 'Commonly confused words: decreed and decrees',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdecreed\b/gi,
    message: 'Check: "decreed" ("decreed") vs "decrees" ("decrees"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'decrees';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_decried_decries_1534',
    name: 'decried vs decries',
    description: 'Commonly confused words: decried and decries',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdecried\b/gi,
    message: 'Check: "decried" ("decried") vs "decries" ("decries"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'decries';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deduced_deduces_1535',
    name: 'deduced vs deduces',
    description: 'Commonly confused words: deduced and deduces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeduced\b/gi,
    message: 'Check: "deduced" ("deduced") vs "deduces" ("deduces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deduces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deduced_seduced_1536',
    name: 'deduced vs seduced',
    description: 'Commonly confused words: deduced and seduced',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeduced\b/gi,
    message: 'Check: "deduced" ("deduced") vs "seduced" ("seduced"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seduced';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deduce_seduce_1537',
    name: 'deduce vs seduce',
    description: 'Commonly confused words: deduce and seduce',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeduce\b/gi,
    message: 'Check: "deduce" ("deduce") vs "seduce" ("seduce"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seduce';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deduces_seduces_1538',
    name: 'deduces vs seduces',
    description: 'Commonly confused words: deduces and seduces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeduces\b/gi,
    message: 'Check: "deduces" ("deduces") vs "seduces" ("seduces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seduces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deducing_seducing_1539',
    name: 'deducing vs seducing',
    description: 'Commonly confused words: deducing and seducing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeducing\b/gi,
    message: 'Check: "deducing" ("deducing") vs "seducing" ("seducing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seducing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deductions_seductions_1540',
    name: 'deductions vs seductions',
    description: 'Commonly confused words: deductions and seductions',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeductions\b/gi,
    message: 'Check: "deductions" ("deductions") vs "seductions" ("seductions"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seductions';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deductively_seductively_1541',
    name: 'deductively vs seductively',
    description: 'Commonly confused words: deductively and seductively',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeductively\b/gi,
    message: 'Check: "deductively" ("deductively") vs "seductively" ("seductively"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seductively';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deductive_seductive_1542',
    name: 'deductive vs seductive',
    description: 'Commonly confused words: deductive and seductive',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeductive\b/gi,
    message: 'Check: "deductive" ("deductive") vs "seductive" ("seductive"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seductive';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deeding_seeding_1543',
    name: 'deeding vs seeding',
    description: 'Commonly confused words: deeding and seeding',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeeding\b/gi,
    message: 'Check: "deeding" ("deeding") vs "seeding" ("seeding"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seeding';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deeds_feeds_1544',
    name: 'deeds vs feeds',
    description: 'Commonly confused words: deeds and feeds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeeds\b/gi,
    message: 'Check: "deeds" ("deeds") vs "feeds" ("feeds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'feeds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deemed_seemed_1545',
    name: 'deemed vs seemed',
    description: 'Commonly confused words: deemed and seemed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeemed\b/gi,
    message: 'Check: "deemed" ("deemed") vs "seemed" ("seemed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seemed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deeming_seeming_1546',
    name: 'deeming vs seeming',
    description: 'Commonly confused words: deeming and seeming',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeeming\b/gi,
    message: 'Check: "deeming" ("deeming") vs "seeming" ("seeming"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seeming';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_defaced_defaces_1547',
    name: 'defaced vs defaces',
    description: 'Commonly confused words: defaced and defaces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdefaced\b/gi,
    message: 'Check: "defaced" ("defaced") vs "defaces" ("defaces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'defaces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_defamed_defames_1548',
    name: 'defamed vs defames',
    description: 'Commonly confused words: defamed and defames',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdefamed\b/gi,
    message: 'Check: "defamed" ("defamed") vs "defames" ("defames"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'defames';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_defecated_defecates_1549',
    name: 'defecated vs defecates',
    description: 'Commonly confused words: defecated and defecates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdefecated\b/gi,
    message: 'Check: "defecated" ("defecated") vs "defecates" ("defecates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'defecates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_defiled_defiles_1550',
    name: 'defiled vs defiles',
    description: 'Commonly confused words: defiled and defiles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdefiled\b/gi,
    message: 'Check: "defiled" ("defiled") vs "defiles" ("defiles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'defiles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_defined_defines_1551',
    name: 'defined vs defines',
    description: 'Commonly confused words: defined and defines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdefined\b/gi,
    message: 'Check: "defined" ("defined") vs "defines" ("defines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'defines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deflated_deflates_1552',
    name: 'deflated vs deflates',
    description: 'Commonly confused words: deflated and deflates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeflated\b/gi,
    message: 'Check: "deflated" ("deflated") vs "deflates" ("deflates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deflates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deft_defy_1553',
    name: 'deft vs defy',
    description: 'Commonly confused words: deft and defy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeft\b/gi,
    message: 'Check: "deft" ("deft") vs "defy" ("defy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'defy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_defused_defuses_1554',
    name: 'defused vs defuses',
    description: 'Commonly confused words: defused and defuses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdefused\b/gi,
    message: 'Check: "defused" ("defused") vs "defuses" ("defuses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'defuses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_degenerated_degenerates_1555',
    name: 'degenerated vs degenerates',
    description: 'Commonly confused words: degenerated and degenerates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdegenerated\b/gi,
    message: 'Check: "degenerated" ("degenerated") vs "degenerates" ("degenerates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'degenerates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dehumanized_dehumanizes_1556',
    name: 'dehumanized vs dehumanizes',
    description: 'Commonly confused words: dehumanized and dehumanizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdehumanized\b/gi,
    message: 'Check: "dehumanized" ("dehumanized") vs "dehumanizes" ("dehumanizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dehumanizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dehydrated_dehydrates_1557',
    name: 'dehydrated vs dehydrates',
    description: 'Commonly confused words: dehydrated and dehydrates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdehydrated\b/gi,
    message: 'Check: "dehydrated" ("dehydrated") vs "dehydrates" ("dehydrates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dehydrates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deigned_feigned_1558',
    name: 'deigned vs feigned',
    description: 'Commonly confused words: deigned and feigned',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeigned\b/gi,
    message: 'Check: "deigned" ("deigned") vs "feigned" ("feigned"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'feigned';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deign_feign_1559',
    name: 'deign vs feign',
    description: 'Commonly confused words: deign and feign',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeign\b/gi,
    message: 'Check: "deign" ("deign") vs "feign" ("feign"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'feign';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deigning_feigning_1560',
    name: 'deigning vs feigning',
    description: 'Commonly confused words: deigning and feigning',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeigning\b/gi,
    message: 'Check: "deigning" ("deigning") vs "feigning" ("feigning"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'feigning';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deigns_feigns_1561',
    name: 'deigns vs feigns',
    description: 'Commonly confused words: deigns and feigns',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeigns\b/gi,
    message: 'Check: "deigns" ("deigns") vs "feigns" ("feigns"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'feigns';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_delegated_delegates_1562',
    name: 'delegated vs delegates',
    description: 'Commonly confused words: delegated and delegates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdelegated\b/gi,
    message: 'Check: "delegated" ("delegated") vs "delegates" ("delegates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'delegates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_delineated_delineates_1563',
    name: 'delineated vs delineates',
    description: 'Commonly confused words: delineated and delineates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdelineated\b/gi,
    message: 'Check: "delineated" ("delineated") vs "delineates" ("delineates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'delineates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dell_cell_1564',
    name: 'dell vs cell',
    description: 'Commonly confused words: dell and cell',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdell\b/gi,
    message: 'Check: "dell" ("dell") vs "cell" ("cell"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cell';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deluded_deludes_1565',
    name: 'deluded vs deludes',
    description: 'Commonly confused words: deluded and deludes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeluded\b/gi,
    message: 'Check: "deluded" ("deluded") vs "deludes" ("deludes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deludes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deluged_deluges_1566',
    name: 'deluged vs deluges',
    description: 'Commonly confused words: deluged and deluges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeluged\b/gi,
    message: 'Check: "deluged" ("deluged") vs "deluges" ("deluges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deluges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_delves_selves_1567',
    name: 'delves vs selves',
    description: 'Commonly confused words: delves and selves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdelves\b/gi,
    message: 'Check: "delves" ("delves") vs "selves" ("selves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'selves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_demarcated_demarcates_1568',
    name: 'demarcated vs demarcates',
    description: 'Commonly confused words: demarcated and demarcates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdemarcated\b/gi,
    message: 'Check: "demarcated" ("demarcated") vs "demarcates" ("demarcates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'demarcates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_demised_demises_1569',
    name: 'demised vs demises',
    description: 'Commonly confused words: demised and demises',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdemised\b/gi,
    message: 'Check: "demised" ("demised") vs "demises" ("demises"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'demises';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_demolished_demolishes_1570',
    name: 'demolished vs demolishes',
    description: 'Commonly confused words: demolished and demolishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdemolished\b/gi,
    message: 'Check: "demolished" ("demolished") vs "demolishes" ("demolishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'demolishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_demoralized_demoralizes_1571',
    name: 'demoralized vs demoralizes',
    description: 'Commonly confused words: demoralized and demoralizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdemoralized\b/gi,
    message: 'Check: "demoralized" ("demoralized") vs "demoralizes" ("demoralizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'demoralizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_demoted_denoted_1572',
    name: 'demoted vs denoted',
    description: 'Commonly confused words: demoted and denoted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdemoted\b/gi,
    message: 'Check: "demoted" ("demoted") vs "denoted" ("denoted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'denoted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_demoting_denoting_1573',
    name: 'demoting vs denoting',
    description: 'Commonly confused words: demoting and denoting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdemoting\b/gi,
    message: 'Check: "demoting" ("demoting") vs "denoting" ("denoting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'denoting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_demur_femur_1574',
    name: 'demur vs femur',
    description: 'Commonly confused words: demur and femur',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdemur\b/gi,
    message: 'Check: "demur" ("demur") vs "femur" ("femur"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'femur';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_demystified_demystifies_1575',
    name: 'demystified vs demystifies',
    description: 'Commonly confused words: demystified and demystifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdemystified\b/gi,
    message: 'Check: "demystified" ("demystified") vs "demystifies" ("demystifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'demystifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_denatured_denatures_1576',
    name: 'denatured vs denatures',
    description: 'Commonly confused words: denatured and denatures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdenatured\b/gi,
    message: 'Check: "denatured" ("denatured") vs "denatures" ("denatures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'denatures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_denigrated_denigrates_1577',
    name: 'denigrated vs denigrates',
    description: 'Commonly confused words: denigrated and denigrates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdenigrated\b/gi,
    message: 'Check: "denigrated" ("denigrated") vs "denigrates" ("denigrates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'denigrates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_denominated_denominates_1578',
    name: 'denominated vs denominates',
    description: 'Commonly confused words: denominated and denominates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdenominated\b/gi,
    message: 'Check: "denominated" ("denominated") vs "denominates" ("denominates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'denominates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_denoted_denotes_1579',
    name: 'denoted vs denotes',
    description: 'Commonly confused words: denoted and denotes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdenoted\b/gi,
    message: 'Check: "denoted" ("denoted") vs "denotes" ("denotes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'denotes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dense_sense_1580',
    name: 'dense vs sense',
    description: 'Commonly confused words: dense and sense',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdense\b/gi,
    message: 'Check: "dense" ("dense") vs "sense" ("sense"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sense';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dent_cent_1581',
    name: 'dent vs cent',
    description: 'Commonly confused words: dent and cent',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdent\b/gi,
    message: 'Check: "dent" ("dent") vs "cent" ("cent"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cent';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deny_dent_1582',
    name: 'deny vs dent',
    description: 'Commonly confused words: deny and dent',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeny\b/gi,
    message: 'Check: "deny" ("deny") vs "dent" ("dent"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dent';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deplored_deplores_1583',
    name: 'deplored vs deplores',
    description: 'Commonly confused words: deplored and deplores',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeplored\b/gi,
    message: 'Check: "deplored" ("deplored") vs "deplores" ("deplores"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deplores';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deportee_deported_1584',
    name: 'deportee vs deported',
    description: 'Commonly confused words: deportee and deported',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeportee\b/gi,
    message: 'Check: "deportee" ("deportee") vs "deported" ("deported"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deported';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_depreciated_depreciates_1585',
    name: 'depreciated vs depreciates',
    description: 'Commonly confused words: depreciated and depreciates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdepreciated\b/gi,
    message: 'Check: "depreciated" ("depreciated") vs "depreciates" ("depreciates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'depreciates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_depressed_depresses_1586',
    name: 'depressed vs depresses',
    description: 'Commonly confused words: depressed and depresses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdepressed\b/gi,
    message: 'Check: "depressed" ("depressed") vs "depresses" ("depresses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'depresses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deprived_deprives_1587',
    name: 'deprived vs deprives',
    description: 'Commonly confused words: deprived and deprives',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeprived\b/gi,
    message: 'Check: "deprived" ("deprived") vs "deprives" ("deprives"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deprives';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deputed_deputes_1588',
    name: 'deputed vs deputes',
    description: 'Commonly confused words: deputed and deputes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeputed\b/gi,
    message: 'Check: "deputed" ("deputed") vs "deputes" ("deputes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deputes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_derided_derides_1589',
    name: 'derided vs derides',
    description: 'Commonly confused words: derided and derides',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bderided\b/gi,
    message: 'Check: "derided" ("derided") vs "derides" ("derides"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'derides';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_described_describes_1590',
    name: 'described vs describes',
    description: 'Commonly confused words: described and describes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdescribed\b/gi,
    message: 'Check: "described" ("described") vs "describes" ("describes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'describes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_desecrated_desecrates_1591',
    name: 'desecrated vs desecrates',
    description: 'Commonly confused words: desecrated and desecrates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdesecrated\b/gi,
    message: 'Check: "desecrated" ("desecrated") vs "desecrates" ("desecrates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'desecrates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_desensitized_desensitizes_1592',
    name: 'desensitized vs desensitizes',
    description: 'Commonly confused words: desensitized and desensitizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdesensitized\b/gi,
    message: 'Check: "desensitized" ("desensitized") vs "desensitizes" ("desensitizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'desensitizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_desired_desires_1593',
    name: 'desired vs desires',
    description: 'Commonly confused words: desired and desires',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdesired\b/gi,
    message: 'Check: "desired" ("desired") vs "desires" ("desires"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'desires';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_destabilized_destabilizes_1594',
    name: 'destabilized vs destabilizes',
    description: 'Commonly confused words: destabilized and destabilizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdestabilized\b/gi,
    message: 'Check: "destabilized" ("destabilized") vs "destabilizes" ("destabilizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'destabilizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_destined_destines_1595',
    name: 'destined vs destines',
    description: 'Commonly confused words: destined and destines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdestined\b/gi,
    message: 'Check: "destined" ("destined") vs "destines" ("destines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'destines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_detained_detainee_1596',
    name: 'detained vs detainee',
    description: 'Commonly confused words: detained and detainee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdetained\b/gi,
    message: 'Check: "detained" ("detained") vs "detainee" ("detainee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'detainee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_determined_determines_1597',
    name: 'determined vs determines',
    description: 'Commonly confused words: determined and determines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdetermined\b/gi,
    message: 'Check: "determined" ("determined") vs "determines" ("determines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'determines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dethroned_dethrones_1598',
    name: 'dethroned vs dethrones',
    description: 'Commonly confused words: dethroned and dethrones',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdethroned\b/gi,
    message: 'Check: "dethroned" ("dethroned") vs "dethrones" ("dethrones"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dethrones';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_devalued_devalues_1599',
    name: 'devalued vs devalues',
    description: 'Commonly confused words: devalued and devalues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdevalued\b/gi,
    message: 'Check: "devalued" ("devalued") vs "devalues" ("devalues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'devalues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deviance_defiance_1600',
    name: 'deviance vs defiance',
    description: 'Commonly confused words: deviance and defiance',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeviance\b/gi,
    message: 'Check: "deviance" ("deviance") vs "defiance" ("defiance"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'defiance';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deviated_deviates_1601',
    name: 'deviated vs deviates',
    description: 'Commonly confused words: deviated and deviates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeviated\b/gi,
    message: 'Check: "deviated" ("deviated") vs "deviates" ("deviates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deviates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_deviled_defiled_1602',
    name: 'deviled vs defiled',
    description: 'Commonly confused words: deviled and defiled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdeviled\b/gi,
    message: 'Check: "deviled" ("deviled") vs "defiled" ("defiled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'defiled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_devolved_devolves_1603',
    name: 'devolved vs devolves',
    description: 'Commonly confused words: devolved and devolves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdevolved\b/gi,
    message: 'Check: "devolved" ("devolved") vs "devolves" ("devolves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'devolves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_devoted_devotee_1604',
    name: 'devoted vs devotee',
    description: 'Commonly confused words: devoted and devotee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdevoted\b/gi,
    message: 'Check: "devoted" ("devoted") vs "devotee" ("devotee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'devotee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_devour_devout_1605',
    name: 'devour vs devout',
    description: 'Commonly confused words: devour and devout',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdevour\b/gi,
    message: 'Check: "devour" ("devour") vs "devout" ("devout"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'devout';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_diagnosed_diagnoses_1606',
    name: 'diagnosed vs diagnoses',
    description: 'Commonly confused words: diagnosed and diagnoses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiagnosed\b/gi,
    message: 'Check: "diagnosed" ("diagnosed") vs "diagnoses" ("diagnoses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'diagnoses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_diatomic_diatonic_1607',
    name: 'diatomic vs diatonic',
    description: 'Commonly confused words: diatomic and diatonic',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiatomic\b/gi,
    message: 'Check: "diatomic" ("diatomic") vs "diatonic" ("diatonic"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'diatonic';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_diced_dices_1608',
    name: 'diced vs dices',
    description: 'Commonly confused words: diced and dices',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiced\b/gi,
    message: 'Check: "diced" ("diced") vs "dices" ("dices"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dices';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dickens_sickens_1609',
    name: 'dickens vs sickens',
    description: 'Commonly confused words: dickens and sickens',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdickens\b/gi,
    message: 'Check: "dickens" ("dickens") vs "sickens" ("sickens"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sickens';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dicks_docks_1610',
    name: 'dicks vs docks',
    description: 'Commonly confused words: dicks and docks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdicks\b/gi,
    message: 'Check: "dicks" ("dicks") vs "docks" ("docks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'docks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dictated_dictates_1611',
    name: 'dictated vs dictates',
    description: 'Commonly confused words: dictated and dictates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdictated\b/gi,
    message: 'Check: "dictated" ("dictated") vs "dictates" ("dictates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dictates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_diddling_fiddling_1612',
    name: 'diddling vs fiddling',
    description: 'Commonly confused words: diddling and fiddling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiddling\b/gi,
    message: 'Check: "diddling" ("diddling") vs "fiddling" ("fiddling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fiddling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_diddly_fiddly_1613',
    name: 'diddly vs fiddly',
    description: 'Commonly confused words: diddly and fiddly',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiddly\b/gi,
    message: 'Check: "diddly" ("diddly") vs "fiddly" ("fiddly"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fiddly';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_differ_duffer_1614',
    name: 'differ vs duffer',
    description: 'Commonly confused words: differ and duffer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiffer\b/gi,
    message: 'Check: "differ" ("differ") vs "duffer" ("duffer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'duffer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_differentiated_differentiates_1615',
    name: 'differentiated vs differentiates',
    description: 'Commonly confused words: differentiated and differentiates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdifferentiated\b/gi,
    message: 'Check: "differentiated" ("differentiated") vs "differentiates" ("differentiates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'differentiates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_differs_duffers_1616',
    name: 'differs vs duffers',
    description: 'Commonly confused words: differs and duffers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiffers\b/gi,
    message: 'Check: "differs" ("differs") vs "duffers" ("duffers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'duffers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_diffs_doffs_1617',
    name: 'diffs vs doffs',
    description: 'Commonly confused words: diffs and doffs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiffs\b/gi,
    message: 'Check: "diffs" ("diffs") vs "doffs" ("doffs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'doffs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_diffused_diffuses_1618',
    name: 'diffused vs diffuses',
    description: 'Commonly confused words: diffused and diffuses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiffused\b/gi,
    message: 'Check: "diffused" ("diffused") vs "diffuses" ("diffuses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'diffuses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_digitized_digitizes_1619',
    name: 'digitized vs digitizes',
    description: 'Commonly confused words: digitized and digitizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdigitized\b/gi,
    message: 'Check: "digitized" ("digitized") vs "digitizes" ("digitizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'digitizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dignified_dignifies_1620',
    name: 'dignified vs dignifies',
    description: 'Commonly confused words: dignified and dignifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdignified\b/gi,
    message: 'Check: "dignified" ("dignified") vs "dignifies" ("dignifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dignifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dignified_signified_1621',
    name: 'dignified vs signified',
    description: 'Commonly confused words: dignified and signified',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdignified\b/gi,
    message: 'Check: "dignified" ("dignified") vs "signified" ("signified"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'signified';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dignifies_signifies_1622',
    name: 'dignifies vs signifies',
    description: 'Commonly confused words: dignifies and signifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdignifies\b/gi,
    message: 'Check: "dignifies" ("dignifies") vs "signifies" ("signifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'signifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dignifying_signifying_1623',
    name: 'dignifying vs signifying',
    description: 'Commonly confused words: dignifying and signifying',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdignifying\b/gi,
    message: 'Check: "dignifying" ("dignifying") vs "signifying" ("signifying"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'signifying';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_digressed_digresses_1624',
    name: 'digressed vs digresses',
    description: 'Commonly confused words: digressed and digresses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdigressed\b/gi,
    message: 'Check: "digressed" ("digressed") vs "digresses" ("digresses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'digresses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_diked_dikes_1625',
    name: 'diked vs dikes',
    description: 'Commonly confused words: diked and dikes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiked\b/gi,
    message: 'Check: "diked" ("diked") vs "dikes" ("dikes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dikes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dikes_dukes_1626',
    name: 'dikes vs dukes',
    description: 'Commonly confused words: dikes and dukes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdikes\b/gi,
    message: 'Check: "dikes" ("dikes") vs "dukes" ("dukes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dukes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dilated_dilates_1627',
    name: 'dilated vs dilates',
    description: 'Commonly confused words: dilated and dilates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdilated\b/gi,
    message: 'Check: "dilated" ("dilated") vs "dilates" ("dilates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dilates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dills_dolls_1628',
    name: 'dills vs dolls',
    description: 'Commonly confused words: dills and dolls',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdills\b/gi,
    message: 'Check: "dills" ("dills") vs "dolls" ("dolls"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dolls';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dime_dine_1629',
    name: 'dime vs dine',
    description: 'Commonly confused words: dime and dine',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdime\b/gi,
    message: 'Check: "dime" ("dime") vs "dine" ("dine"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dine';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dimer_diner_1630',
    name: 'dimer vs diner',
    description: 'Commonly confused words: dimer and diner',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdimer\b/gi,
    message: 'Check: "dimer" ("dimer") vs "diner" ("diner"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'diner';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dimes_dines_1631',
    name: 'dimes vs dines',
    description: 'Commonly confused words: dimes and dines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdimes\b/gi,
    message: 'Check: "dimes" ("dimes") vs "dines" ("dines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dimes_domes_1632',
    name: 'dimes vs domes',
    description: 'Commonly confused words: dimes and domes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdimes\b/gi,
    message: 'Check: "dimes" ("dimes") vs "domes" ("domes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'domes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dimmer_simmer_1633',
    name: 'dimmer vs simmer',
    description: 'Commonly confused words: dimmer and simmer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdimmer\b/gi,
    message: 'Check: "dimmer" ("dimmer") vs "simmer" ("simmer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'simmer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dimmers_simmers_1634',
    name: 'dimmers vs simmers',
    description: 'Commonly confused words: dimmers and simmers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdimmers\b/gi,
    message: 'Check: "dimmers" ("dimmers") vs "simmers" ("simmers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'simmers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dined_fined_1635',
    name: 'dined vs fined',
    description: 'Commonly confused words: dined and fined',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdined\b/gi,
    message: 'Check: "dined" ("dined") vs "fined" ("fined"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fined';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dingles_dingoes_1636',
    name: 'dingles vs dingoes',
    description: 'Commonly confused words: dingles and dingoes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdingles\b/gi,
    message: 'Check: "dingles" ("dingles") vs "dingoes" ("dingoes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dingoes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dingles_singles_1637',
    name: 'dingles vs singles',
    description: 'Commonly confused words: dingles and singles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdingles\b/gi,
    message: 'Check: "dingles" ("dingles") vs "singles" ("singles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'singles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dining_fining_1638',
    name: 'dining vs fining',
    description: 'Commonly confused words: dining and fining',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdining\b/gi,
    message: 'Check: "dining" ("dining") vs "fining" ("fining"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fining';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dinners_sinners_1639',
    name: 'dinners vs sinners',
    description: 'Commonly confused words: dinners and sinners',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdinners\b/gi,
    message: 'Check: "dinners" ("dinners") vs "sinners" ("sinners"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sinners';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dinning_donning_1640',
    name: 'dinning vs donning',
    description: 'Commonly confused words: dinning and donning',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdinning\b/gi,
    message: 'Check: "dinning" ("dinning") vs "donning" ("donning"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'donning';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_diplomats_diplomata_1641',
    name: 'diplomats vs diplomata',
    description: 'Commonly confused words: diplomats and diplomata',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiplomats\b/gi,
    message: 'Check: "diplomats" ("diplomats") vs "diplomata" ("diplomata"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'diplomata';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dipping_sipping_1642',
    name: 'dipping vs sipping',
    description: 'Commonly confused words: dipping and sipping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdipping\b/gi,
    message: 'Check: "dipping" ("dipping") vs "sipping" ("sipping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sipping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dirks_dorks_1643',
    name: 'dirks vs dorks',
    description: 'Commonly confused words: dirks and dorks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdirks\b/gi,
    message: 'Check: "dirks" ("dirks") vs "dorks" ("dorks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dorks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dirtied_dirties_1644',
    name: 'dirtied vs dirties',
    description: 'Commonly confused words: dirtied and dirties',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdirtied\b/gi,
    message: 'Check: "dirtied" ("dirtied") vs "dirties" ("dirties"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dirties';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disadvantaged_disadvantages_1645',
    name: 'disadvantaged vs disadvantages',
    description: 'Commonly confused words: disadvantaged and disadvantages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisadvantaged\b/gi,
    message: 'Check: "disadvantaged" ("disadvantaged") vs "disadvantages" ("disadvantages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disadvantages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disagreed_disagrees_1646',
    name: 'disagreed vs disagrees',
    description: 'Commonly confused words: disagreed and disagrees',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisagreed\b/gi,
    message: 'Check: "disagreed" ("disagreed") vs "disagrees" ("disagrees"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disagrees';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disassembled_disassembles_1647',
    name: 'disassembled vs disassembles',
    description: 'Commonly confused words: disassembled and disassembles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisassembled\b/gi,
    message: 'Check: "disassembled" ("disassembled") vs "disassembles" ("disassembles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disassembles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disassociated_disassociates_1648',
    name: 'disassociated vs disassociates',
    description: 'Commonly confused words: disassociated and disassociates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisassociated\b/gi,
    message: 'Check: "disassociated" ("disassociated") vs "disassociates" ("disassociates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disassociates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disbursed_disburses_1649',
    name: 'disbursed vs disburses',
    description: 'Commonly confused words: disbursed and disburses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisbursed\b/gi,
    message: 'Check: "disbursed" ("disbursed") vs "disburses" ("disburses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disburses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_discharged_discharges_1650',
    name: 'discharged vs discharges',
    description: 'Commonly confused words: discharged and discharges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdischarged\b/gi,
    message: 'Check: "discharged" ("discharged") vs "discharges" ("discharges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'discharges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disciplined_disciplines_1651',
    name: 'disciplined vs disciplines',
    description: 'Commonly confused words: disciplined and disciplines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisciplined\b/gi,
    message: 'Check: "disciplined" ("disciplined") vs "disciplines" ("disciplines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disciplines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disclosed_discloses_1652',
    name: 'disclosed vs discloses',
    description: 'Commonly confused words: disclosed and discloses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisclosed\b/gi,
    message: 'Check: "disclosed" ("disclosed") vs "discloses" ("discloses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'discloses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_discoursed_discourses_1653',
    name: 'discoursed vs discourses',
    description: 'Commonly confused words: discoursed and discourses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiscoursed\b/gi,
    message: 'Check: "discoursed" ("discoursed") vs "discourses" ("discourses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'discourses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_discriminated_discriminates_1654',
    name: 'discriminated vs discriminates',
    description: 'Commonly confused words: discriminated and discriminates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiscriminated\b/gi,
    message: 'Check: "discriminated" ("discriminated") vs "discriminates" ("discriminates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'discriminates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disengaged_disengages_1655',
    name: 'disengaged vs disengages',
    description: 'Commonly confused words: disengaged and disengages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisengaged\b/gi,
    message: 'Check: "disengaged" ("disengaged") vs "disengages" ("disengages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disengages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disfigured_disfigures_1656',
    name: 'disfigured vs disfigures',
    description: 'Commonly confused words: disfigured and disfigures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisfigured\b/gi,
    message: 'Check: "disfigured" ("disfigured") vs "disfigures" ("disfigures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disfigures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disgraced_disgraces_1657',
    name: 'disgraced vs disgraces',
    description: 'Commonly confused words: disgraced and disgraces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisgraced\b/gi,
    message: 'Check: "disgraced" ("disgraced") vs "disgraces" ("disgraces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disgraces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dished_fished_1658',
    name: 'dished vs fished',
    description: 'Commonly confused words: dished and fished',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdished\b/gi,
    message: 'Check: "dished" ("dished") vs "fished" ("fished"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fished';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dishes_fishes_1659',
    name: 'dishes vs fishes',
    description: 'Commonly confused words: dishes and fishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdishes\b/gi,
    message: 'Check: "dishes" ("dishes") vs "fishes" ("fishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dishy_fishy_1660',
    name: 'dishy vs fishy',
    description: 'Commonly confused words: dishy and fishy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdishy\b/gi,
    message: 'Check: "dishy" ("dishy") vs "fishy" ("fishy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fishy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dislocated_dislocates_1661',
    name: 'dislocated vs dislocates',
    description: 'Commonly confused words: dislocated and dislocates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdislocated\b/gi,
    message: 'Check: "dislocated" ("dislocated") vs "dislocates" ("dislocates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dislocates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dislodged_dislodges_1662',
    name: 'dislodged vs dislodges',
    description: 'Commonly confused words: dislodged and dislodges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdislodged\b/gi,
    message: 'Check: "dislodged" ("dislodged") vs "dislodges" ("dislodges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dislodges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dismantled_dismantles_1663',
    name: 'dismantled vs dismantles',
    description: 'Commonly confused words: dismantled and dismantles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdismantled\b/gi,
    message: 'Check: "dismantled" ("dismantled") vs "dismantles" ("dismantles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dismantles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disparaged_disparages_1664',
    name: 'disparaged vs disparages',
    description: 'Commonly confused words: disparaged and disparages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisparaged\b/gi,
    message: 'Check: "disparaged" ("disparaged") vs "disparages" ("disparages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disparages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disparage_disparate_1665',
    name: 'disparage vs disparate',
    description: 'Commonly confused words: disparage and disparate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisparage\b/gi,
    message: 'Check: "disparage" ("disparage") vs "disparate" ("disparate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disparate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dispatched_dispatches_1666',
    name: 'dispatched vs dispatches',
    description: 'Commonly confused words: dispatched and dispatches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdispatched\b/gi,
    message: 'Check: "dispatched" ("dispatched") vs "dispatches" ("dispatches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dispatches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_displeased_displeases_1667',
    name: 'displeased vs displeases',
    description: 'Commonly confused words: displeased and displeases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdispleased\b/gi,
    message: 'Check: "displeased" ("displeased") vs "displeases" ("displeases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'displeases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disposed_disposes_1668',
    name: 'disposed vs disposes',
    description: 'Commonly confused words: disposed and disposes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisposed\b/gi,
    message: 'Check: "disposed" ("disposed") vs "disposes" ("disposes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disposes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disproved_disproves_1669',
    name: 'disproved vs disproves',
    description: 'Commonly confused words: disproved and disproves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisproved\b/gi,
    message: 'Check: "disproved" ("disproved") vs "disproves" ("disproves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disproves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disqualified_disqualifies_1670',
    name: 'disqualified vs disqualifies',
    description: 'Commonly confused words: disqualified and disqualifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisqualified\b/gi,
    message: 'Check: "disqualified" ("disqualified") vs "disqualifies" ("disqualifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disqualifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disrobed_disrobes_1671',
    name: 'disrobed vs disrobes',
    description: 'Commonly confused words: disrobed and disrobes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisrobed\b/gi,
    message: 'Check: "disrobed" ("disrobed") vs "disrobes" ("disrobes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disrobes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_disseminated_disseminates_1672',
    name: 'disseminated vs disseminates',
    description: 'Commonly confused words: disseminated and disseminates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdisseminated\b/gi,
    message: 'Check: "disseminated" ("disseminated") vs "disseminates" ("disseminates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disseminates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dissociated_dissociates_1673',
    name: 'dissociated vs dissociates',
    description: 'Commonly confused words: dissociated and dissociates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdissociated\b/gi,
    message: 'Check: "dissociated" ("dissociated") vs "dissociates" ("dissociates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dissociates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dissuaded_dissuades_1674',
    name: 'dissuaded vs dissuades',
    description: 'Commonly confused words: dissuaded and dissuades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdissuaded\b/gi,
    message: 'Check: "dissuaded" ("dissuaded") vs "dissuades" ("dissuades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dissuades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_distanced_distances_1675',
    name: 'distanced vs distances',
    description: 'Commonly confused words: distanced and distances',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdistanced\b/gi,
    message: 'Check: "distanced" ("distanced") vs "distances" ("distances"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'distances';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_distinguished_distinguishes_1676',
    name: 'distinguished vs distinguishes',
    description: 'Commonly confused words: distinguished and distinguishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdistinguished\b/gi,
    message: 'Check: "distinguished" ("distinguished") vs "distinguishes" ("distinguishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'distinguishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_distressed_distresses_1677',
    name: 'distressed vs distresses',
    description: 'Commonly confused words: distressed and distresses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdistressed\b/gi,
    message: 'Check: "distressed" ("distressed") vs "distresses" ("distresses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'distresses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dive_dice_1678',
    name: 'dive vs dice',
    description: 'Commonly confused words: dive and dice',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdive\b/gi,
    message: 'Check: "dive" ("dive") vs "dice" ("dice"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dice';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_diverged_diverges_1679',
    name: 'diverged vs diverges',
    description: 'Commonly confused words: diverged and diverges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiverged\b/gi,
    message: 'Check: "diverged" ("diverged") vs "diverges" ("diverges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'diverges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_diverging_diverting_1680',
    name: 'diverging vs diverting',
    description: 'Commonly confused words: diverging and diverting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiverging\b/gi,
    message: 'Check: "diverging" ("diverging") vs "diverting" ("diverting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'diverting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_divers_fivers_1681',
    name: 'divers vs fivers',
    description: 'Commonly confused words: divers and fivers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdivers\b/gi,
    message: 'Check: "divers" ("divers") vs "fivers" ("fivers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fivers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_diversified_diversifies_1682',
    name: 'diversified vs diversifies',
    description: 'Commonly confused words: diversified and diversifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdiversified\b/gi,
    message: 'Check: "diversified" ("diversified") vs "diversifies" ("diversifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'diversifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_divined_divines_1683',
    name: 'divined vs divines',
    description: 'Commonly confused words: divined and divines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdivined\b/gi,
    message: 'Check: "divined" ("divined") vs "divines" ("divines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'divines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dockers_dockets_1684',
    name: 'dockers vs dockets',
    description: 'Commonly confused words: dockers and dockets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdockers\b/gi,
    message: 'Check: "dockers" ("dockers") vs "dockets" ("dockets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dockets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_docket_socket_1685',
    name: 'docket vs socket',
    description: 'Commonly confused words: docket and socket',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdocket\b/gi,
    message: 'Check: "docket" ("docket") vs "socket" ("socket"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'socket';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dockets_sockets_1686',
    name: 'dockets vs sockets',
    description: 'Commonly confused words: dockets and sockets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdockets\b/gi,
    message: 'Check: "dockets" ("dockets") vs "sockets" ("sockets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sockets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dodder_fodder_1687',
    name: 'dodder vs fodder',
    description: 'Commonly confused words: dodder and fodder',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdodder\b/gi,
    message: 'Check: "dodder" ("dodder") vs "fodder" ("fodder"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fodder';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_doges_dotes_1688',
    name: 'doges vs dotes',
    description: 'Commonly confused words: doges and dotes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdoges\b/gi,
    message: 'Check: "doges" ("doges") vs "dotes" ("dotes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dotes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dogged_fogged_1689',
    name: 'dogged vs fogged',
    description: 'Commonly confused words: dogged and fogged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdogged\b/gi,
    message: 'Check: "dogged" ("dogged") vs "fogged" ("fogged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fogged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_doggy_soggy_1690',
    name: 'doggy vs soggy',
    description: 'Commonly confused words: doggy and soggy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdoggy\b/gi,
    message: 'Check: "doggy" ("doggy") vs "soggy" ("soggy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'soggy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dogies_fogies_1691',
    name: 'dogies vs fogies',
    description: 'Commonly confused words: dogies and fogies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdogies\b/gi,
    message: 'Check: "dogies" ("dogies") vs "fogies" ("fogies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fogies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_doled_doles_1692',
    name: 'doled vs doles',
    description: 'Commonly confused words: doled and doles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdoled\b/gi,
    message: 'Check: "doled" ("doled") vs "doles" ("doles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'doles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dome_come_1693',
    name: 'dome vs come',
    description: 'Commonly confused words: dome and come',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdome\b/gi,
    message: 'Check: "dome" ("dome") vs "come" ("come"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'come';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_domed_domes_1694',
    name: 'domed vs domes',
    description: 'Commonly confused words: domed and domes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdomed\b/gi,
    message: 'Check: "domed" ("domed") vs "domes" ("domes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'domes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dome_dime_1695',
    name: 'dome vs dime',
    description: 'Commonly confused words: dome and dime',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdome\b/gi,
    message: 'Check: "dome" ("dome") vs "dime" ("dime"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dime';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_domesticated_domesticates_1696',
    name: 'domesticated vs domesticates',
    description: 'Commonly confused words: domesticated and domesticates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdomesticated\b/gi,
    message: 'Check: "domesticated" ("domesticated") vs "domesticates" ("domesticates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'domesticates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_domiciled_domiciles_1697',
    name: 'domiciled vs domiciles',
    description: 'Commonly confused words: domiciled and domiciles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdomiciled\b/gi,
    message: 'Check: "domiciled" ("domiciled") vs "domiciles" ("domiciles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'domiciles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dominated_dominates_1698',
    name: 'dominated vs dominates',
    description: 'Commonly confused words: dominated and dominates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdominated\b/gi,
    message: 'Check: "dominated" ("dominated") vs "dominates" ("dominates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dominates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_done_cone_1699',
    name: 'done vs cone',
    description: 'Commonly confused words: done and cone',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdone\b/gi,
    message: 'Check: "done" ("done") vs "cone" ("cone"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cone';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dons_cons_1700',
    name: 'dons vs cons',
    description: 'Commonly confused words: dons and cons',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdons\b/gi,
    message: 'Check: "dons" ("dons") vs "cons" ("cons"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cons';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_doodled_doodles_1701',
    name: 'doodled vs doodles',
    description: 'Commonly confused words: doodled and doodles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdoodled\b/gi,
    message: 'Check: "doodled" ("doodled") vs "doodles" ("doodles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'doodles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_doped_dopes_1702',
    name: 'doped vs dopes',
    description: 'Commonly confused words: doped and dopes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdoped\b/gi,
    message: 'Check: "doped" ("doped") vs "dopes" ("dopes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dopes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_doping_coping_1703',
    name: 'doping vs coping',
    description: 'Commonly confused words: doping and coping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdoping\b/gi,
    message: 'Check: "doping" ("doping") vs "coping" ("coping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dosed_doses_1704',
    name: 'dosed vs doses',
    description: 'Commonly confused words: dosed and doses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdosed\b/gi,
    message: 'Check: "dosed" ("dosed") vs "doses" ("doses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'doses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_doted_dotes_1705',
    name: 'doted vs dotes',
    description: 'Commonly confused words: doted and dotes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdoted\b/gi,
    message: 'Check: "doted" ("doted") vs "dotes" ("dotes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dotes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dote_doge_1706',
    name: 'dote vs doge',
    description: 'Commonly confused words: dote and doge',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdote\b/gi,
    message: 'Check: "dote" ("dote") vs "doge" ("doge"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'doge';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dotes_cotes_1707',
    name: 'dotes vs cotes',
    description: 'Commonly confused words: dotes and cotes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdotes\b/gi,
    message: 'Check: "dotes" ("dotes") vs "cotes" ("cotes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cotes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dots_cots_1708',
    name: 'dots vs cots',
    description: 'Commonly confused words: dots and cots',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdots\b/gi,
    message: 'Check: "dots" ("dots") vs "cots" ("cots"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cots';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_doubled_doubles_1709',
    name: 'doubled vs doubles',
    description: 'Commonly confused words: doubled and doubles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdoubled\b/gi,
    message: 'Check: "doubled" ("doubled") vs "doubles" ("doubles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'doubles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_doused_douses_1710',
    name: 'doused vs douses',
    description: 'Commonly confused words: doused and douses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdoused\b/gi,
    message: 'Check: "doused" ("doused") vs "douses" ("douses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'douses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_doused_soused_1711',
    name: 'doused vs soused',
    description: 'Commonly confused words: doused and soused',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdoused\b/gi,
    message: 'Check: "doused" ("doused") vs "soused" ("soused"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'soused';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dove_cove_1712',
    name: 'dove vs cove',
    description: 'Commonly confused words: dove and cove',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdove\b/gi,
    message: 'Check: "dove" ("dove") vs "cove" ("cove"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cove';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dove_dive_1713',
    name: 'dove vs dive',
    description: 'Commonly confused words: dove and dive',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdove\b/gi,
    message: 'Check: "dove" ("dove") vs "dive" ("dive"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dive';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_doves_dives_1714',
    name: 'doves vs dives',
    description: 'Commonly confused words: doves and dives',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdoves\b/gi,
    message: 'Check: "doves" ("doves") vs "dives" ("dives"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dives';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_downstage_downstate_1715',
    name: 'downstage vs downstate',
    description: 'Commonly confused words: downstage and downstate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdownstage\b/gi,
    message: 'Check: "downstage" ("downstage") vs "downstate" ("downstate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'downstate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_drab_crab_1716',
    name: 'drab vs crab',
    description: 'Commonly confused words: drab and crab',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdrab\b/gi,
    message: 'Check: "drab" ("drab") vs "crab" ("crab"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crab';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_drabs_crabs_1717',
    name: 'drabs vs crabs',
    description: 'Commonly confused words: drabs and crabs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdrabs\b/gi,
    message: 'Check: "drabs" ("drabs") vs "crabs" ("crabs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crabs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_drabs_drags_1718',
    name: 'drabs vs drags',
    description: 'Commonly confused words: drabs and drags',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdrabs\b/gi,
    message: 'Check: "drabs" ("drabs") vs "drags" ("drags"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drags';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_draftees_drafters_1719',
    name: 'draftees vs drafters',
    description: 'Commonly confused words: draftees and drafters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdraftees\b/gi,
    message: 'Check: "draftees" ("draftees") vs "drafters" ("drafters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drafters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_drags_crags_1720',
    name: 'drags vs crags',
    description: 'Commonly confused words: drags and crags',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdrags\b/gi,
    message: 'Check: "drags" ("drags") vs "crags" ("crags"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crags';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dramatized_dramatizes_1721',
    name: 'dramatized vs dramatizes',
    description: 'Commonly confused words: dramatized and dramatizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdramatized\b/gi,
    message: 'Check: "dramatized" ("dramatized") vs "dramatizes" ("dramatizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dramatizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_drape_crape_1722',
    name: 'drape vs crape',
    description: 'Commonly confused words: drape and crape',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdrape\b/gi,
    message: 'Check: "drape" ("drape") vs "crape" ("crape"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crape';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_draped_drapes_1723',
    name: 'draped vs drapes',
    description: 'Commonly confused words: draped and drapes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdraped\b/gi,
    message: 'Check: "draped" ("draped") vs "drapes" ("drapes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drapes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_drat_frat_1724',
    name: 'drat vs frat',
    description: 'Commonly confused words: drat and frat',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdrat\b/gi,
    message: 'Check: "drat" ("drat") vs "frat" ("frat"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'frat';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_draws_craws_1725',
    name: 'draws vs craws',
    description: 'Commonly confused words: draws and craws',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdraws\b/gi,
    message: 'Check: "draws" ("draws") vs "craws" ("craws"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'craws';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_drays_frays_1726',
    name: 'drays vs frays',
    description: 'Commonly confused words: drays and frays',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdrays\b/gi,
    message: 'Check: "drays" ("drays") vs "frays" ("frays"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'frays';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dredged_dredges_1727',
    name: 'dredged vs dredges',
    description: 'Commonly confused words: dredged and dredges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdredged\b/gi,
    message: 'Check: "dredged" ("dredged") vs "dredges" ("dredges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dredges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_drenched_drenches_1728',
    name: 'drenched vs drenches',
    description: 'Commonly confused words: drenched and drenches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdrenched\b/gi,
    message: 'Check: "drenched" ("drenched") vs "drenches" ("drenches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drenches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_drew_crew_1729',
    name: 'drew vs crew',
    description: 'Commonly confused words: drew and crew',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdrew\b/gi,
    message: 'Check: "drew" ("drew") vs "crew" ("crew"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crew';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dries_cries_1730',
    name: 'dries vs cries',
    description: 'Commonly confused words: dries and cries',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdries\b/gi,
    message: 'Check: "dries" ("dries") vs "cries" ("cries"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cries';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_drippings_droppings_1731',
    name: 'drippings vs droppings',
    description: 'Commonly confused words: drippings and droppings',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdrippings\b/gi,
    message: 'Check: "drippings" ("drippings") vs "droppings" ("droppings"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'droppings';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_drizzled_drizzles_1732',
    name: 'drizzled vs drizzles',
    description: 'Commonly confused words: drizzled and drizzles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdrizzled\b/gi,
    message: 'Check: "drizzled" ("drizzled") vs "drizzles" ("drizzles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drizzles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_drizzled_frizzled_1733',
    name: 'drizzled vs frizzled',
    description: 'Commonly confused words: drizzled and frizzled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdrizzled\b/gi,
    message: 'Check: "drizzled" ("drizzled") vs "frizzled" ("frizzled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'frizzled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_drops_crops_1734',
    name: 'drops vs crops',
    description: 'Commonly confused words: drops and crops',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdrops\b/gi,
    message: 'Check: "drops" ("drops") vs "crops" ("crops"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crops';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_drowned_frowned_1735',
    name: 'drowned vs frowned',
    description: 'Commonly confused words: drowned and frowned',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdrowned\b/gi,
    message: 'Check: "drowned" ("drowned") vs "frowned" ("frowned"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'frowned';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_drown_frown_1736',
    name: 'drown vs frown',
    description: 'Commonly confused words: drown and frown',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdrown\b/gi,
    message: 'Check: "drown" ("drown") vs "frown" ("frown"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'frown';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_drowns_frowns_1737',
    name: 'drowns vs frowns',
    description: 'Commonly confused words: drowns and frowns',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdrowns\b/gi,
    message: 'Check: "drowns" ("drowns") vs "frowns" ("frowns"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'frowns';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dry_cry_1738',
    name: 'dry vs cry',
    description: 'Commonly confused words: dry and cry',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdry\b/gi,
    message: 'Check: "dry" ("dry") vs "cry" ("cry"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cry';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ducked_fucked_1739',
    name: 'ducked vs fucked',
    description: 'Commonly confused words: ducked and fucked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bducked\b/gi,
    message: 'Check: "ducked" ("ducked") vs "fucked" ("fucked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fucked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ducked_sucked_1740',
    name: 'ducked vs sucked',
    description: 'Commonly confused words: ducked and sucked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bducked\b/gi,
    message: 'Check: "ducked" ("ducked") vs "sucked" ("sucked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sucked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ducking_fucking_1741',
    name: 'ducking vs fucking',
    description: 'Commonly confused words: ducking and fucking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bducking\b/gi,
    message: 'Check: "ducking" ("ducking") vs "fucking" ("fucking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fucking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_duckling_suckling_1742',
    name: 'duckling vs suckling',
    description: 'Commonly confused words: duckling and suckling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bduckling\b/gi,
    message: 'Check: "duckling" ("duckling") vs "suckling" ("suckling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suckling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dueling_fueling_1743',
    name: 'dueling vs fueling',
    description: 'Commonly confused words: dueling and fueling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdueling\b/gi,
    message: 'Check: "dueling" ("dueling") vs "fueling" ("fueling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fueling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dues_cues_1744',
    name: 'dues vs cues',
    description: 'Commonly confused words: dues and cues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdues\b/gi,
    message: 'Check: "dues" ("dues") vs "cues" ("cues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dues_dies_1745',
    name: 'dues vs dies',
    description: 'Commonly confused words: dues and dies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdues\b/gi,
    message: 'Check: "dues" ("dues") vs "dies" ("dies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_duet_suet_1746',
    name: 'duet vs suet',
    description: 'Commonly confused words: duet and suet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bduet\b/gi,
    message: 'Check: "duet" ("duet") vs "suet" ("suet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_duffers_suffers_1747',
    name: 'duffers vs suffers',
    description: 'Commonly confused words: duffers and suffers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bduffers\b/gi,
    message: 'Check: "duffers" ("duffers") vs "suffers" ("suffers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'suffers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dull_dill_1748',
    name: 'dull vs dill',
    description: 'Commonly confused words: dull and dill',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdull\b/gi,
    message: 'Check: "dull" ("dull") vs "dill" ("dill"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dill';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dulled_fulled_1749',
    name: 'dulled vs fulled',
    description: 'Commonly confused words: dulled and fulled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdulled\b/gi,
    message: 'Check: "dulled" ("dulled") vs "fulled" ("fulled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fulled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dullest_fullest_1750',
    name: 'dullest vs fullest',
    description: 'Commonly confused words: dullest and fullest',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdullest\b/gi,
    message: 'Check: "dullest" ("dullest") vs "fullest" ("fullest"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fullest';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dulling_fulling_1751',
    name: 'dulling vs fulling',
    description: 'Commonly confused words: dulling and fulling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdulling\b/gi,
    message: 'Check: "dulling" ("dulling") vs "fulling" ("fulling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fulling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dullness_fullness_1752',
    name: 'dullness vs fullness',
    description: 'Commonly confused words: dullness and fullness',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdullness\b/gi,
    message: 'Check: "dullness" ("dullness") vs "fullness" ("fullness"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fullness';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dulls_dills_1753',
    name: 'dulls vs dills',
    description: 'Commonly confused words: dulls and dills',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdulls\b/gi,
    message: 'Check: "dulls" ("dulls") vs "dills" ("dills"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dills';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dune_dine_1754',
    name: 'dune vs dine',
    description: 'Commonly confused words: dune and dine',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdune\b/gi,
    message: 'Check: "dune" ("dune") vs "dine" ("dine"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dine';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dunes_dines_1755',
    name: 'dunes vs dines',
    description: 'Commonly confused words: dunes and dines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdunes\b/gi,
    message: 'Check: "dunes" ("dunes") vs "dines" ("dines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dung_ding_1756',
    name: 'dung vs ding',
    description: 'Commonly confused words: dung and ding',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdung\b/gi,
    message: 'Check: "dung" ("dung") vs "ding" ("ding"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ding';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dunked_funked_1757',
    name: 'dunked vs funked',
    description: 'Commonly confused words: dunked and funked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdunked\b/gi,
    message: 'Check: "dunked" ("dunked") vs "funked" ("funked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'funked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dunking_funking_1758',
    name: 'dunking vs funking',
    description: 'Commonly confused words: dunking and funking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdunking\b/gi,
    message: 'Check: "dunking" ("dunking") vs "funking" ("funking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'funking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dusk_disk_1759',
    name: 'dusk vs disk',
    description: 'Commonly confused words: dusk and disk',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdusk\b/gi,
    message: 'Check: "dusk" ("dusk") vs "disk" ("disk"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'disk';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dust_dist_1760',
    name: 'dust vs dist',
    description: 'Commonly confused words: dust and dist',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdust\b/gi,
    message: 'Check: "dust" ("dust") vs "dist" ("dist"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dist';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dwellings_swellings_1761',
    name: 'dwellings vs swellings',
    description: 'Commonly confused words: dwellings and swellings',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdwellings\b/gi,
    message: 'Check: "dwellings" ("dwellings") vs "swellings" ("swellings"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'swellings';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dwelling_swelling_1762',
    name: 'dwelling vs swelling',
    description: 'Commonly confused words: dwelling and swelling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdwelling\b/gi,
    message: 'Check: "dwelling" ("dwelling") vs "swelling" ("swelling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'swelling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dwells_swells_1763',
    name: 'dwells vs swells',
    description: 'Commonly confused words: dwells and swells',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdwells\b/gi,
    message: 'Check: "dwells" ("dwells") vs "swells" ("swells"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'swells';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dwell_swell_1764',
    name: 'dwell vs swell',
    description: 'Commonly confused words: dwell and swell',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdwell\b/gi,
    message: 'Check: "dwell" ("dwell") vs "swell" ("swell"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'swell';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dwindled_dwindles_1765',
    name: 'dwindled vs dwindles',
    description: 'Commonly confused words: dwindled and dwindles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdwindled\b/gi,
    message: 'Check: "dwindled" ("dwindled") vs "dwindles" ("dwindles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dwindles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dwindled_swindled_1766',
    name: 'dwindled vs swindled',
    description: 'Commonly confused words: dwindled and swindled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdwindled\b/gi,
    message: 'Check: "dwindled" ("dwindled") vs "swindled" ("swindled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'swindled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dwindle_swindle_1767',
    name: 'dwindle vs swindle',
    description: 'Commonly confused words: dwindle and swindle',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdwindle\b/gi,
    message: 'Check: "dwindle" ("dwindle") vs "swindle" ("swindle"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'swindle';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dwindling_swindling_1768',
    name: 'dwindling vs swindling',
    description: 'Commonly confused words: dwindling and swindling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdwindling\b/gi,
    message: 'Check: "dwindling" ("dwindling") vs "swindling" ("swindling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'swindling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dyed_dyes_1769',
    name: 'dyed vs dyes',
    description: 'Commonly confused words: dyed and dyes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdyed\b/gi,
    message: 'Check: "dyed" ("dyed") vs "dyes" ("dyes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dyes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dye_due_1770',
    name: 'dye vs due',
    description: 'Commonly confused words: dye and due',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdye\b/gi,
    message: 'Check: "dye" (to color) vs "due" ("due"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'due';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dyeing_eyeing_1771',
    name: 'dyeing vs eyeing',
    description: 'Commonly confused words: dyeing and eyeing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdyeing\b/gi,
    message: 'Check: "dyeing" ("dyeing") vs "eyeing" ("eyeing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'eyeing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dyes_dues_1772',
    name: 'dyes vs dues',
    description: 'Commonly confused words: dyes and dues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdyes\b/gi,
    message: 'Check: "dyes" ("dyes") vs "dues" ("dues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_dynamited_dynamites_1773',
    name: 'dynamited vs dynamites',
    description: 'Commonly confused words: dynamited and dynamites',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bdynamited\b/gi,
    message: 'Check: "dynamited" ("dynamited") vs "dynamites" ("dynamites"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dynamites';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
