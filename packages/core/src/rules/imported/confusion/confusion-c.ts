/**
 * Auto-generated confusion rules (C)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.498Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesC: GrammarRule[] = [
  {
    id: 'confusion_cached_cashed_132',
    name: 'cached vs cashed',
    description: 'Commonly confused words: cached and cashed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcached\b/gi,
    message: 'Check: "cached" ("cached") vs "cashed" ("cashed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cashed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_caller_collar_133',
    name: 'caller vs collar',
    description: 'Commonly confused words: caller and collar',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcaller\b/gi,
    message: 'Check: "caller" ("caller") vs "collar" ("collar"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'collar';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_callers_collars_134',
    name: 'callers vs collars',
    description: 'Commonly confused words: callers and collars',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcallers\b/gi,
    message: 'Check: "callers" ("callers") vs "collars" ("collars"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'collars';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_capital_capitol_135',
    name: 'capital vs capitol',
    description: 'Commonly confused words: capital and capitol',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcapital\b/gi,
    message: 'Check: "capital" (city or uppercase letter) vs "capitol" (government building). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'capitol';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_carat_caret_136',
    name: 'carat vs caret',
    description: 'Commonly confused words: carat and caret',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcarat\b/gi,
    message: 'Check: "carat" ("carat") vs "caret" ("caret"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'caret';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_carpal_carpel_137',
    name: 'carpal vs carpel',
    description: 'Commonly confused words: carpal and carpel',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcarpal\b/gi,
    message: 'Check: "carpal" ("carpal") vs "carpel" ("carpel"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'carpel';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_carpals_carpels_138',
    name: 'carpals vs carpels',
    description: 'Commonly confused words: carpals and carpels',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcarpals\b/gi,
    message: 'Check: "carpals" ("carpals") vs "carpels" ("carpels"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'carpels';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_case_cause_139',
    name: 'case vs cause',
    description: 'Commonly confused words: case and cause',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcase\b/gi,
    message: 'Check: "case" ("case") vs "cause" ("cause"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cause';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cases_causes_140',
    name: 'cases vs causes',
    description: 'Commonly confused words: cases and causes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcases\b/gi,
    message: 'Check: "cases" ("cases") vs "causes" ("causes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'causes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cast_caste_141',
    name: 'cast vs caste',
    description: 'Commonly confused words: cast and caste',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcast\b/gi,
    message: 'Check: "cast" ("cast") vs "caste" ("caste"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'caste';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_caught_cot_142',
    name: 'caught vs cot',
    description: 'Commonly confused words: caught and cot',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcaught\b/gi,
    message: 'Check: "caught" ("caught") vs "cot" ("cot"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cot';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_caught_court_143',
    name: 'caught vs court',
    description: 'Commonly confused words: caught and court',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcaught\b/gi,
    message: 'Did you mean "court"? "caught" ("caught") is often confused with "court" ("court").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'court';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_court_caught_144',
    name: 'court vs caught',
    description: 'Commonly confused words: court and caught',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcourt\b/gi,
    message: 'Did you mean "caught"? "court" ("court") is often confused with "caught" ("caught").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'caught';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cawed_chord_145',
    name: 'cawed vs chord',
    description: 'Commonly confused words: cawed and chord',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcawed\b/gi,
    message: 'Check: "cawed" ("cawed") vs "chord" ("chord"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chord';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cease_seize_146',
    name: 'cease vs seize',
    description: 'Commonly confused words: cease and seize',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcease\b/gi,
    message: 'Check: "cease" ("cease") vs "seize" ("seize"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seize';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ceases_seizes_147',
    name: 'ceases vs seizes',
    description: 'Commonly confused words: ceases and seizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bceases\b/gi,
    message: 'Check: "ceases" ("ceases") vs "seizes" ("seizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cede_seed_148',
    name: 'cede vs seed',
    description: 'Commonly confused words: cede and seed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcede\b/gi,
    message: 'Check: "cede" ("cede") vs "seed" ("seed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cedes_seeds_149',
    name: 'cedes vs seeds',
    description: 'Commonly confused words: cedes and seeds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcedes\b/gi,
    message: 'Check: "cedes" ("cedes") vs "seeds" ("seeds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seeds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_celebrate_celibate_150',
    name: 'celebrate vs celibate',
    description: 'Commonly confused words: celebrate and celibate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcelebrate\b/gi,
    message: 'Check: "celebrate" ("celebrate") vs "celibate" ("celibate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'celibate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_celebrates_celibates_151',
    name: 'celebrates vs celibates',
    description: 'Commonly confused words: celebrates and celibates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcelebrates\b/gi,
    message: 'Check: "celebrates" ("celebrates") vs "celibates" ("celibates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'celibates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_celery_salary_152',
    name: 'celery vs salary',
    description: 'Commonly confused words: celery and salary',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcelery\b/gi,
    message: 'Check: "celery" ("celery") vs "salary" ("salary"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'salary';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cellar_seller_153',
    name: 'cellar vs seller',
    description: 'Commonly confused words: cellar and seller',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcellar\b/gi,
    message: 'Check: "cellar" ("cellar") vs "seller" ("seller"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'seller';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cells_sells_154',
    name: 'cells vs sells',
    description: 'Commonly confused words: cells and sells',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcells\b/gi,
    message: 'Check: "cells" ("cells") vs "sells" ("sells"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sells';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cell_sell_155',
    name: 'cell vs sell',
    description: 'Commonly confused words: cell and sell',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcell\b/gi,
    message: 'Did you mean "sell"? "cell" ("cell") is often confused with "sell" ("sell").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sell';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_censer_censor_157',
    name: 'censer vs censor',
    description: 'Commonly confused words: censer and censor',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcenser\b/gi,
    message: 'Check: "censer" ("censer") vs "censor" ("censor"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'censor';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_censor_censure_158',
    name: 'censor vs censure',
    description: 'Commonly confused words: censor and censure',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcensor\b/gi,
    message: 'Check: "censor" ("censor") vs "censure" ("censure"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'censure';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_censor_sensor_159',
    name: 'censor vs sensor',
    description: 'Commonly confused words: censor and sensor',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcensor\b/gi,
    message: 'Check: "censor" ("censor") vs "sensor" ("sensor"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sensor';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_censors_sensors_160',
    name: 'censors vs sensors',
    description: 'Commonly confused words: censors and sensors',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcensors\b/gi,
    message: 'Check: "censors" ("censors") vs "sensors" ("sensors"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sensors';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_census_senses_161',
    name: 'census vs senses',
    description: 'Commonly confused words: census and senses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcensus\b/gi,
    message: 'Check: "census" ("census") vs "senses" ("senses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'senses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cent_scent_162',
    name: 'cent vs scent',
    description: 'Commonly confused words: cent and scent',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcent\b/gi,
    message: 'Check: "cent" ("cent") vs "scent" ("scent"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'scent';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cereal_serial_163',
    name: 'cereal vs serial',
    description: 'Commonly confused words: cereal and serial',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcereal\b/gi,
    message: 'Check: "cereal" ("cereal") vs "serial" ("serial"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'serial';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cereals_serials_164',
    name: 'cereals vs serials',
    description: 'Commonly confused words: cereals and serials',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcereals\b/gi,
    message: 'Check: "cereals" ("cereals") vs "serials" ("serials"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'serials';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chard_charred_165',
    name: 'chard vs charred',
    description: 'Commonly confused words: chard and charred',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchard\b/gi,
    message: 'Check: "chard" ("chard") vs "charred" ("charred"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'charred';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chased_chaste_166',
    name: 'chased vs chaste',
    description: 'Commonly confused words: chased and chaste',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchased\b/gi,
    message: 'Check: "chased" ("chased") vs "chaste" ("chaste"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chaste';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_Chicago_Chicano_167',
    name: 'Chicago vs Chicano',
    description: 'Commonly confused words: Chicago and Chicano',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bChicago\b/gi,
    message: 'Check: "Chicago" ("Chicago") vs "Chicano" ("Chicano"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'Chicano';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_choral_coral_168',
    name: 'choral vs coral',
    description: 'Commonly confused words: choral and coral',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchoral\b/gi,
    message: 'Check: "choral" ("choral") vs "coral" ("coral"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coral';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chord_cord_169',
    name: 'chord vs cord',
    description: 'Commonly confused words: chord and cord',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchord\b/gi,
    message: 'Check: "chord" ("chord") vs "cord" ("cord"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cord';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chords_cords_170',
    name: 'chords vs cords',
    description: 'Commonly confused words: chords and cords',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchords\b/gi,
    message: 'Check: "chords" ("chords") vs "cords" ("cords"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cords';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_citing_sighting_171',
    name: 'citing vs sighting',
    description: 'Commonly confused words: citing and sighting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bciting\b/gi,
    message: 'Check: "citing" ("citing") vs "sighting" ("sighting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'sighting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_clack_claque_172',
    name: 'clack vs claque',
    description: 'Commonly confused words: clack and claque',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclack\b/gi,
    message: 'Check: "clack" ("clack") vs "claque" ("claque"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'claque';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_clamber_clamor_173',
    name: 'clamber vs clamor',
    description: 'Commonly confused words: clamber and clamor',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclamber\b/gi,
    message: 'Check: "clamber" ("clamber") vs "clamor" ("clamor"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clamor';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_clambers_clamors_174',
    name: 'clambers vs clamors',
    description: 'Commonly confused words: clambers and clamors',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclambers\b/gi,
    message: 'Check: "clambers" ("clambers") vs "clamors" ("clamors"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clamors';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_click_clique_175',
    name: 'click vs clique',
    description: 'Commonly confused words: click and clique',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclick\b/gi,
    message: 'Check: "click" ("click") vs "clique" ("clique"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clique';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_climactic_climatic_176',
    name: 'climactic vs climatic',
    description: 'Commonly confused words: climactic and climatic',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclimactic\b/gi,
    message: 'Check: "climactic" ("climactic") vs "climatic" ("climatic"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'climatic';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_climb_clime_177',
    name: 'climb vs clime',
    description: 'Commonly confused words: climb and clime',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclimb\b/gi,
    message: 'Check: "climb" ("climb") vs "clime" ("clime"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clime';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_climbs_climes_178',
    name: 'climbs vs climes',
    description: 'Commonly confused words: climbs and climes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclimbs\b/gi,
    message: 'Check: "climbs" ("climbs") vs "climes" ("climes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'climes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_close_clothes_179',
    name: 'close vs clothes',
    description: 'Commonly confused words: close and clothes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclose\b/gi,
    message: 'Check: "close" ("close") vs "clothes" ("clothes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clothes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coal_cole_180',
    name: 'coal vs cole',
    description: 'Commonly confused words: coal and cole',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoal\b/gi,
    message: 'Check: "coal" ("coal") vs "cole" ("cole"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cole';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coarse_course_181',
    name: 'coarse vs course',
    description: 'Commonly confused words: coarse and course',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoarse\b/gi,
    message: 'Check: "coarse" (rough texture) vs "course" (path or class). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'course';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coat_cote_182',
    name: 'coat vs cote',
    description: 'Commonly confused words: coat and cote',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoat\b/gi,
    message: 'Check: "coat" ("coat") vs "cote" ("cote"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cote';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coats_cotes_183',
    name: 'coats vs cotes',
    description: 'Commonly confused words: coats and cotes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoats\b/gi,
    message: 'Check: "coats" ("coats") vs "cotes" ("cotes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cotes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coax_cokes_184',
    name: 'coax vs cokes',
    description: 'Commonly confused words: coax and cokes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoax\b/gi,
    message: 'Check: "coax" ("coax") vs "cokes" ("cokes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cokes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coco_cocoa_185',
    name: 'coco vs cocoa',
    description: 'Commonly confused words: coco and cocoa',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoco\b/gi,
    message: 'Check: "coco" ("coco") vs "cocoa" ("cocoa"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cocoa';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coffer_cougher_186',
    name: 'coffer vs cougher',
    description: 'Commonly confused words: coffer and cougher',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoffer\b/gi,
    message: 'Check: "coffer" ("coffer") vs "cougher" ("cougher"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cougher';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coif_quaff_187',
    name: 'coif vs quaff',
    description: 'Commonly confused words: coif and quaff',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoif\b/gi,
    message: 'Check: "coif" ("coif") vs "quaff" ("quaff"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'quaff';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coir_coyer_188',
    name: 'coir vs coyer',
    description: 'Commonly confused words: coir and coyer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoir\b/gi,
    message: 'Check: "coir" ("coir") vs "coyer" ("coyer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coyer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_collaborate_corroborate_189',
    name: 'collaborate vs corroborate',
    description: 'Commonly confused words: collaborate and corroborate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcollaborate\b/gi,
    message: 'Check: "collaborate" ("collaborate") vs "corroborate" ("corroborate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'corroborate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_collaborates_corroborates_190',
    name: 'collaborates vs corroborates',
    description: 'Commonly confused words: collaborates and corroborates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcollaborates\b/gi,
    message: 'Check: "collaborates" ("collaborates") vs "corroborates" ("corroborates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'corroborates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coma_comber_191',
    name: 'coma vs comber',
    description: 'Commonly confused words: coma and comber',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoma\b/gi,
    message: 'Check: "coma" ("coma") vs "comber" ("comber"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'comber';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coma_comma_192',
    name: 'coma vs comma',
    description: 'Commonly confused words: coma and comma',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoma\b/gi,
    message: 'Check: "coma" ("coma") vs "comma" ("comma"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'comma';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_compete_complete_193',
    name: 'compete vs complete',
    description: 'Commonly confused words: compete and complete',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcompete\b/gi,
    message: 'Check: "compete" ("compete") vs "complete" ("complete"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'complete';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_competes_completes_194',
    name: 'competes vs completes',
    description: 'Commonly confused words: competes and completes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcompetes\b/gi,
    message: 'Check: "competes" ("competes") vs "completes" ("completes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'completes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_complacently_complaisantly_195',
    name: 'complacently vs complaisantly',
    description: 'Commonly confused words: complacently and complaisantly',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcomplacently\b/gi,
    message: 'Check: "complacently" ("complacently") vs "complaisantly" ("complaisantly"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'complaisantly';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_complacent_complaisant_196',
    name: 'complacent vs complaisant',
    description: 'Commonly confused words: complacent and complaisant',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcomplacent\b/gi,
    message: 'Check: "complacent" ("complacent") vs "complaisant" ("complaisant"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'complaisant';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_complains_complaints_197',
    name: 'complains vs complaints',
    description: 'Commonly confused words: complains and complaints',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcomplains\b/gi,
    message: 'Check: "complains" ("complains") vs "complaints" ("complaints"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'complaints';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_complain_complaint_198',
    name: 'complain vs complaint',
    description: 'Commonly confused words: complain and complaint',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcomplain\b/gi,
    message: 'Check: "complain" ("complain") vs "complaint" ("complaint"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'complaint';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_complement_compliment_199',
    name: 'complement vs compliment',
    description: 'Commonly confused words: complement and compliment',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcomplement\b/gi,
    message: 'Check: "complement" (to complete or enhance) vs "compliment" (praise or flattery). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'compliment';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_conch_conk_200',
    name: 'conch vs conk',
    description: 'Commonly confused words: conch and conk',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconch\b/gi,
    message: 'Check: "conch" ("conch") vs "conk" ("conk"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conk';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_conjecture_conjuncture_201',
    name: 'conjecture vs conjuncture',
    description: 'Commonly confused words: conjecture and conjuncture',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconjecture\b/gi,
    message: 'Check: "conjecture" ("conjecture") vs "conjuncture" ("conjuncture"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conjuncture';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_con_khan_202',
    name: 'con vs khan',
    description: 'Commonly confused words: con and khan',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcon\b/gi,
    message: 'Check: "con" ("con") vs "khan" ("khan"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'khan';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_conscience_conscious_203',
    name: 'conscience vs conscious',
    description: 'Commonly confused words: conscience and conscious',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconscience\b/gi,
    message: 'Check: "conscience" ("conscience") vs "conscious" ("conscious"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conscious';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coo_coup_204',
    name: 'coo vs coup',
    description: 'Commonly confused words: coo and coup',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoo\b/gi,
    message: 'Check: "coo" ("coo") vs "coup" ("coup"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coup';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coos_coups_205',
    name: 'coos vs coups',
    description: 'Commonly confused words: coos and coups',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoos\b/gi,
    message: 'Check: "coos" ("coos") vs "coups" ("coups"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coups';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_copes_copse_206',
    name: 'copes vs copse',
    description: 'Commonly confused words: copes and copse',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcopes\b/gi,
    message: 'Check: "copes" ("copes") vs "copse" ("copse"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'copse';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cor_core_207',
    name: 'cor vs core',
    description: 'Commonly confused words: cor and core',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcor\b/gi,
    message: 'Check: "cor" ("cor") vs "core" ("core"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'core';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cord_cored_208',
    name: 'cord vs cored',
    description: 'Commonly confused words: cord and cored',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcord\b/gi,
    message: 'Check: "cord" ("cord") vs "cored" ("cored"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cored';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cosign_cosine_209',
    name: 'cosign vs cosine',
    description: 'Commonly confused words: cosign and cosine',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcosign\b/gi,
    message: 'Check: "cosign" ("cosign") vs "cosine" ("cosine"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cosine';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cosigns_cosines_210',
    name: 'cosigns vs cosines',
    description: 'Commonly confused words: cosigns and cosines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcosigns\b/gi,
    message: 'Check: "cosigns" ("cosigns") vs "cosines" ("cosines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cosines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cousin_cozen_211',
    name: 'cousin vs cozen',
    description: 'Commonly confused words: cousin and cozen',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcousin\b/gi,
    message: 'Check: "cousin" ("cousin") vs "cozen" ("cozen"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cozen';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cousins_cozens_212',
    name: 'cousins vs cozens',
    description: 'Commonly confused words: cousins and cozens',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcousins\b/gi,
    message: 'Check: "cousins" ("cousins") vs "cozens" ("cozens"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cozens';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coward_cowered_213',
    name: 'coward vs cowered',
    description: 'Commonly confused words: coward and cowered',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoward\b/gi,
    message: 'Check: "coward" ("coward") vs "cowered" ("cowered"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cowered';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_craft_kraft_214',
    name: 'craft vs kraft',
    description: 'Commonly confused words: craft and kraft',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcraft\b/gi,
    message: 'Check: "craft" ("craft") vs "kraft" ("kraft"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'kraft';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crate_create_215',
    name: 'crate vs create',
    description: 'Commonly confused words: crate and create',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrate\b/gi,
    message: 'Check: "crate" ("crate") vs "create" ("create"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'create';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crates_creates_216',
    name: 'crates vs creates',
    description: 'Commonly confused words: crates and creates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrates\b/gi,
    message: 'Check: "crates" ("crates") vs "creates" ("creates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'creates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crewel_cruel_217',
    name: 'crewel vs cruel',
    description: 'Commonly confused words: crewel and cruel',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrewel\b/gi,
    message: 'Check: "crewel" ("crewel") vs "cruel" ("cruel"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cruel';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crews_cruise_218',
    name: 'crews vs cruise',
    description: 'Commonly confused words: crews and cruise',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrews\b/gi,
    message: 'Check: "crews" ("crews") vs "cruise" ("cruise"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cruise';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crises_crisis_219',
    name: 'crises vs crisis',
    description: 'Commonly confused words: crises and crisis',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrises\b/gi,
    message: 'Check: "crises" ("crises") vs "crisis" ("crisis"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crisis';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crochet_crotchet_220',
    name: 'crochet vs crotchet',
    description: 'Commonly confused words: crochet and crotchet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrochet\b/gi,
    message: 'Check: "crochet" ("crochet") vs "crotchet" ("crotchet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crotchet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cue_queue_221',
    name: 'cue vs queue',
    description: 'Commonly confused words: cue and queue',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcue\b/gi,
    message: 'Check: "cue" ("cue") vs "queue" ("queue"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'queue';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cued_queued_222',
    name: 'cued vs queued',
    description: 'Commonly confused words: cued and queued',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcued\b/gi,
    message: 'Check: "cued" ("cued") vs "queued" ("queued"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'queued';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_case_vase_774',
    name: 'case vs vase',
    description: 'Commonly confused words: case and vase',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcase\b/gi,
    message: 'Did you mean "vase"? "case" ("case") is often confused with "vase" ("vase").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vase';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_confirm_conform_783',
    name: 'confirm vs conform',
    description: 'Commonly confused words: confirm and conform',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconfirm\b/gi,
    message: 'Check: "confirm" ("confirm") vs "conform" ("conform"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conform';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cached_caches_1187',
    name: 'cached vs caches',
    description: 'Commonly confused words: cached and caches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcached\b/gi,
    message: 'Check: "cached" ("cached") vs "caches" ("caches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'caches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_caddied_caddies_1188',
    name: 'caddied vs caddies',
    description: 'Commonly confused words: caddied and caddies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcaddied\b/gi,
    message: 'Check: "caddied" ("caddied") vs "caddies" ("caddies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'caddies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cadenced_cadences_1189',
    name: 'cadenced vs cadences',
    description: 'Commonly confused words: cadenced and cadences',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcadenced\b/gi,
    message: 'Check: "cadenced" ("cadenced") vs "cadences" ("cadences"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cadences';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cafes_cares_1190',
    name: 'cafes vs cares',
    description: 'Commonly confused words: cafes and cares',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcafes\b/gi,
    message: 'Check: "cafes" ("cafes") vs "cares" ("cares"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cares';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_caged_cages_1191',
    name: 'caged vs cages',
    description: 'Commonly confused words: caged and cages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcaged\b/gi,
    message: 'Check: "caged" ("caged") vs "cages" ("cages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cajoled_cajoles_1192',
    name: 'cajoled vs cajoles',
    description: 'Commonly confused words: cajoled and cajoles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcajoled\b/gi,
    message: 'Check: "cajoled" ("cajoled") vs "cajoles" ("cajoles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cajoles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_caked_cakes_1193',
    name: 'caked vs cakes',
    description: 'Commonly confused words: caked and cakes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcaked\b/gi,
    message: 'Check: "caked" ("caked") vs "cakes" ("cakes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cakes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_calibrated_calibrates_1194',
    name: 'calibrated vs calibrates',
    description: 'Commonly confused words: calibrated and calibrates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcalibrated\b/gi,
    message: 'Check: "calibrated" ("calibrated") vs "calibrates" ("calibrates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'calibrates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_calk_call_1195',
    name: 'calk vs call',
    description: 'Commonly confused words: calk and call',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcalk\b/gi,
    message: 'Check: "calk" ("calk") vs "call" ("call"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'call';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_calloused_callouses_1196',
    name: 'calloused vs callouses',
    description: 'Commonly confused words: calloused and callouses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcalloused\b/gi,
    message: 'Check: "calloused" ("calloused") vs "callouses" ("callouses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'callouses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_callused_calluses_1197',
    name: 'callused vs calluses',
    description: 'Commonly confused words: callused and calluses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcallused\b/gi,
    message: 'Check: "callused" ("callused") vs "calluses" ("calluses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'calluses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_calved_calves_1198',
    name: 'calved vs calves',
    description: 'Commonly confused words: calved and calves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcalved\b/gi,
    message: 'Check: "calved" ("calved") vs "calves" ("calves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'calves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_calved_valved_1199',
    name: 'calved vs valved',
    description: 'Commonly confused words: calved and valved',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcalved\b/gi,
    message: 'Check: "calved" ("calved") vs "valved" ("valved"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'valved';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_calves_valves_1200',
    name: 'calves vs valves',
    description: 'Commonly confused words: calves and valves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcalves\b/gi,
    message: 'Check: "calves" ("calves") vs "valves" ("valves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'valves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_calve_valve_1201',
    name: 'calve vs valve',
    description: 'Commonly confused words: calve and valve',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcalve\b/gi,
    message: 'Check: "calve" ("calve") vs "valve" ("valve"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'valve';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_calving_valving_1202',
    name: 'calving vs valving',
    description: 'Commonly confused words: calving and valving',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcalving\b/gi,
    message: 'Check: "calving" ("calving") vs "valving" ("valving"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'valving';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_camouflaged_camouflages_1203',
    name: 'camouflaged vs camouflages',
    description: 'Commonly confused words: camouflaged and camouflages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcamouflaged\b/gi,
    message: 'Check: "camouflaged" ("camouflaged") vs "camouflages" ("camouflages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'camouflages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_camped_damped_1204',
    name: 'camped vs damped',
    description: 'Commonly confused words: camped and damped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcamped\b/gi,
    message: 'Check: "camped" ("camped") vs "damped" ("damped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'damped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_camper_damper_1205',
    name: 'camper vs damper',
    description: 'Commonly confused words: camper and damper',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcamper\b/gi,
    message: 'Check: "camper" ("camper") vs "damper" ("damper"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'damper';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_campers_dampers_1206',
    name: 'campers vs dampers',
    description: 'Commonly confused words: campers and dampers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcampers\b/gi,
    message: 'Check: "campers" ("campers") vs "dampers" ("dampers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dampers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_camping_damping_1207',
    name: 'camping vs damping',
    description: 'Commonly confused words: camping and damping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcamping\b/gi,
    message: 'Check: "camping" ("camping") vs "damping" ("damping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'damping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cancers_dancers_1208',
    name: 'cancers vs dancers',
    description: 'Commonly confused words: cancers and dancers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcancers\b/gi,
    message: 'Check: "cancers" ("cancers") vs "dancers" ("dancers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dancers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_candied_candies_1209',
    name: 'candied vs candies',
    description: 'Commonly confused words: candied and candies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcandied\b/gi,
    message: 'Check: "candied" ("candied") vs "candies" ("candies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'candies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_candies_dandies_1210',
    name: 'candies vs dandies',
    description: 'Commonly confused words: candies and dandies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcandies\b/gi,
    message: 'Check: "candies" ("candies") vs "dandies" ("dandies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dandies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_caned_canes_1211',
    name: 'caned vs canes',
    description: 'Commonly confused words: caned and canes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcaned\b/gi,
    message: 'Check: "caned" ("caned") vs "canes" ("canes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'canes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_canker_danker_1212',
    name: 'canker vs danker',
    description: 'Commonly confused words: canker and danker',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcanker\b/gi,
    message: 'Check: "canker" ("canker") vs "danker" ("danker"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'danker';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cannibalized_cannibalizes_1213',
    name: 'cannibalized vs cannibalizes',
    description: 'Commonly confused words: cannibalized and cannibalizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcannibalized\b/gi,
    message: 'Check: "cannibalized" ("cannibalized") vs "cannibalizes" ("cannibalizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cannibalizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_canoed_canoes_1214',
    name: 'canoed vs canoes',
    description: 'Commonly confused words: canoed and canoes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcanoed\b/gi,
    message: 'Check: "canoed" ("canoed") vs "canoes" ("canoes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'canoes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_canonized_canonizes_1215',
    name: 'canonized vs canonizes',
    description: 'Commonly confused words: canonized and canonizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcanonized\b/gi,
    message: 'Check: "canonized" ("canonized") vs "canonizes" ("canonizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'canonizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_canopied_canopies_1216',
    name: 'canopied vs canopies',
    description: 'Commonly confused words: canopied and canopies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcanopied\b/gi,
    message: 'Check: "canopied" ("canopied") vs "canopies" ("canopies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'canopies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cantons_canyons_1217',
    name: 'cantons vs canyons',
    description: 'Commonly confused words: cantons and canyons',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcantons\b/gi,
    message: 'Check: "cantons" ("cantons") vs "canyons" ("canyons"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'canyons';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_canvased_canvases_1218',
    name: 'canvased vs canvases',
    description: 'Commonly confused words: canvased and canvases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcanvased\b/gi,
    message: 'Check: "canvased" ("canvased") vs "canvases" ("canvases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'canvases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_caped_capes_1219',
    name: 'caped vs capes',
    description: 'Commonly confused words: caped and capes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcaped\b/gi,
    message: 'Check: "caped" ("caped") vs "capes" ("capes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'capes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_capitalized_capitalizes_1220',
    name: 'capitalized vs capitalizes',
    description: 'Commonly confused words: capitalized and capitalizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcapitalized\b/gi,
    message: 'Check: "capitalized" ("capitalized") vs "capitalizes" ("capitalizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'capitalizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_capitulated_capitulates_1221',
    name: 'capitulated vs capitulates',
    description: 'Commonly confused words: capitulated and capitulates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcapitulated\b/gi,
    message: 'Check: "capitulated" ("capitulated") vs "capitulates" ("capitulates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'capitulates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_captivated_captivates_1222',
    name: 'captivated vs captivates',
    description: 'Commonly confused words: captivated and captivates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcaptivated\b/gi,
    message: 'Check: "captivated" ("captivated") vs "captivates" ("captivates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'captivates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_carbonated_carbonates_1223',
    name: 'carbonated vs carbonates',
    description: 'Commonly confused words: carbonated and carbonates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcarbonated\b/gi,
    message: 'Check: "carbonated" ("carbonated") vs "carbonates" ("carbonates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'carbonates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_card_cars_1224',
    name: 'card vs cars',
    description: 'Commonly confused words: card and cars',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcard\b/gi,
    message: 'Check: "card" ("card") vs "cars" ("cars"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cars';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cards_cares_1225',
    name: 'cards vs cares',
    description: 'Commonly confused words: cards and cares',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcards\b/gi,
    message: 'Check: "cards" ("cards") vs "cares" ("cares"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cares';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_caricatured_caricatures_1226',
    name: 'caricatured vs caricatures',
    description: 'Commonly confused words: caricatured and caricatures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcaricatured\b/gi,
    message: 'Check: "caricatured" ("caricatured") vs "caricatures" ("caricatures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'caricatures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_caries_varies_1227',
    name: 'caries vs varies',
    description: 'Commonly confused words: caries and varies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcaries\b/gi,
    message: 'Check: "caries" ("caries") vs "varies" ("varies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'varies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_carrel_cartel_1228',
    name: 'carrel vs cartel',
    description: 'Commonly confused words: carrel and cartel',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcarrel\b/gi,
    message: 'Check: "carrel" ("carrel") vs "cartel" ("cartel"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cartel';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_carrels_cartels_1229',
    name: 'carrels vs cartels',
    description: 'Commonly confused words: carrels and cartels',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcarrels\b/gi,
    message: 'Check: "carrels" ("carrels") vs "cartels" ("cartels"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cartels';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_carried_carries_1230',
    name: 'carried vs carries',
    description: 'Commonly confused words: carried and carries',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcarried\b/gi,
    message: 'Check: "carried" ("carried") vs "carries" ("carries"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'carries';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_carted_catted_1231',
    name: 'carted vs catted',
    description: 'Commonly confused words: carted and catted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcarted\b/gi,
    message: 'Check: "carted" ("carted") vs "catted" ("catted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'catted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cashes_cashew_1232',
    name: 'cashes vs cashew',
    description: 'Commonly confused words: cashes and cashew',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcashes\b/gi,
    message: 'Check: "cashes" ("cashes") vs "cashew" ("cashew"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cashew';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cashing_dashing_1233',
    name: 'cashing vs dashing',
    description: 'Commonly confused words: cashing and dashing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcashing\b/gi,
    message: 'Check: "cashing" ("cashing") vs "dashing" ("dashing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dashing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_castled_castles_1234',
    name: 'castled vs castles',
    description: 'Commonly confused words: castled and castles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcastled\b/gi,
    message: 'Check: "castled" ("castled") vs "castles" ("castles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'castles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_castrated_castrates_1235',
    name: 'castrated vs castrates',
    description: 'Commonly confused words: castrated and castrates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcastrated\b/gi,
    message: 'Check: "castrated" ("castrated") vs "castrates" ("castrates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'castrates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_catalyzed_catalyzes_1236',
    name: 'catalyzed vs catalyzes',
    description: 'Commonly confused words: catalyzed and catalyzes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcatalyzed\b/gi,
    message: 'Check: "catalyzed" ("catalyzed") vs "catalyzes" ("catalyzes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'catalyzes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_caters_carers_1237',
    name: 'caters vs carers',
    description: 'Commonly confused words: caters and carers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcaters\b/gi,
    message: 'Check: "caters" ("caters") vs "carers" ("carers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'carers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_caucused_caucuses_1238',
    name: 'caucused vs caucuses',
    description: 'Commonly confused words: caucused and caucuses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcaucused\b/gi,
    message: 'Check: "caucused" ("caucused") vs "caucuses" ("caucuses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'caucuses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_caudal_causal_1239',
    name: 'caudal vs causal',
    description: 'Commonly confused words: caudal and causal',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcaudal\b/gi,
    message: 'Check: "caudal" ("caudal") vs "causal" ("causal"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'causal';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_caudally_causally_1240',
    name: 'caudally vs causally',
    description: 'Commonly confused words: caudally and causally',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcaudally\b/gi,
    message: 'Check: "caudally" ("caudally") vs "causally" ("causally"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'causally';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_caused_causes_1241',
    name: 'caused vs causes',
    description: 'Commonly confused words: caused and causes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcaused\b/gi,
    message: 'Check: "caused" ("caused") vs "causes" ("causes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'causes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cawing_casing_1242',
    name: 'cawing vs casing',
    description: 'Commonly confused words: cawing and casing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcawing\b/gi,
    message: 'Check: "cawing" ("cawing") vs "casing" ("casing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'casing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cecal_decal_1243',
    name: 'cecal vs decal',
    description: 'Commonly confused words: cecal and decal',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcecal\b/gi,
    message: 'Check: "cecal" ("cecal") vs "decal" ("decal"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'decal';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_censer_denser_1244',
    name: 'censer vs denser',
    description: 'Commonly confused words: censer and denser',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcenser\b/gi,
    message: 'Check: "censer" ("censer") vs "denser" ("denser"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'denser';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_censured_censures_1245',
    name: 'censured vs censures',
    description: 'Commonly confused words: censured and censures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcensured\b/gi,
    message: 'Check: "censured" ("censured") vs "censures" ("censures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'censures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_centrifuged_centrifuges_1246',
    name: 'centrifuged vs centrifuges',
    description: 'Commonly confused words: centrifuged and centrifuges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcentrifuged\b/gi,
    message: 'Check: "centrifuged" ("centrifuged") vs "centrifuges" ("centrifuges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'centrifuges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chafed_chafes_1247',
    name: 'chafed vs chafes',
    description: 'Commonly confused words: chafed and chafes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchafed\b/gi,
    message: 'Check: "chafed" ("chafed") vs "chafes" ("chafes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chafes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_challenged_challenges_1248',
    name: 'challenged vs challenges',
    description: 'Commonly confused words: challenged and challenges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchallenged\b/gi,
    message: 'Check: "challenged" ("challenged") vs "challenges" ("challenges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'challenges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chanced_chances_1249',
    name: 'chanced vs chances',
    description: 'Commonly confused words: chanced and chances',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchanced\b/gi,
    message: 'Check: "chanced" ("chanced") vs "chances" ("chances"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chances';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_changed_changes_1250',
    name: 'changed vs changes',
    description: 'Commonly confused words: changed and changes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchanged\b/gi,
    message: 'Check: "changed" ("changed") vs "changes" ("changes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'changes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_characterized_characterizes_1251',
    name: 'characterized vs characterizes',
    description: 'Commonly confused words: characterized and characterizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcharacterized\b/gi,
    message: 'Check: "characterized" ("characterized") vs "characterizes" ("characterizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'characterizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_charged_charges_1252',
    name: 'charged vs charges',
    description: 'Commonly confused words: charged and charges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcharged\b/gi,
    message: 'Check: "charged" ("charged") vs "charges" ("charges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'charges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_charted_chatted_1253',
    name: 'charted vs chatted',
    description: 'Commonly confused words: charted and chatted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcharted\b/gi,
    message: 'Check: "charted" ("charted") vs "chatted" ("chatted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chatted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chartered_chattered_1254',
    name: 'chartered vs chattered',
    description: 'Commonly confused words: chartered and chattered',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchartered\b/gi,
    message: 'Check: "chartered" ("chartered") vs "chattered" ("chattered"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chattered';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cheated_chested_1255',
    name: 'cheated vs chested',
    description: 'Commonly confused words: cheated and chested',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcheated\b/gi,
    message: 'Check: "cheated" ("cheated") vs "chested" ("chested"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chested';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cheats_chests_1256',
    name: 'cheats vs chests',
    description: 'Commonly confused words: cheats and chests',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcheats\b/gi,
    message: 'Check: "cheats" ("cheats") vs "chests" ("chests"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chests';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cheesed_cheeses_1257',
    name: 'cheesed vs cheeses',
    description: 'Commonly confused words: cheesed and cheeses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcheesed\b/gi,
    message: 'Check: "cheesed" ("cheesed") vs "cheeses" ("cheeses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cheeses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cherished_cherishes_1258',
    name: 'cherished vs cherishes',
    description: 'Commonly confused words: cherished and cherishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcherished\b/gi,
    message: 'Check: "cherished" ("cherished") vs "cherishes" ("cherishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cherishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chimed_chimes_1259',
    name: 'chimed vs chimes',
    description: 'Commonly confused words: chimed and chimes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchimed\b/gi,
    message: 'Check: "chimed" ("chimed") vs "chimes" ("chimes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chimes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chine_chime_1260',
    name: 'chine vs chime',
    description: 'Commonly confused words: chine and chime',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchine\b/gi,
    message: 'Check: "chine" ("chine") vs "chime" ("chime"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chime';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chink_chunk_1261',
    name: 'chink vs chunk',
    description: 'Commonly confused words: chink and chunk',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchink\b/gi,
    message: 'Check: "chink" ("chink") vs "chunk" ("chunk"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chunk';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chinked_chunked_1262',
    name: 'chinked vs chunked',
    description: 'Commonly confused words: chinked and chunked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchinked\b/gi,
    message: 'Check: "chinked" ("chinked") vs "chunked" ("chunked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chunked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chinks_chunks_1263',
    name: 'chinks vs chunks',
    description: 'Commonly confused words: chinks and chunks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchinks\b/gi,
    message: 'Check: "chinks" ("chinks") vs "chunks" ("chunks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chunks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chipped_chopped_1264',
    name: 'chipped vs chopped',
    description: 'Commonly confused words: chipped and chopped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchipped\b/gi,
    message: 'Check: "chipped" ("chipped") vs "chopped" ("chopped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chopped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chipping_chopping_1265',
    name: 'chipping vs chopping',
    description: 'Commonly confused words: chipping and chopping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchipping\b/gi,
    message: 'Check: "chipping" ("chipping") vs "chopping" ("chopping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chopping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chippy_choppy_1266',
    name: 'chippy vs choppy',
    description: 'Commonly confused words: chippy and choppy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchippy\b/gi,
    message: 'Check: "chippy" ("chippy") vs "choppy" ("choppy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'choppy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chords_chores_1267',
    name: 'chords vs chores',
    description: 'Commonly confused words: chords and chores',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchords\b/gi,
    message: 'Check: "chords" ("chords") vs "chores" ("chores"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chores';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chores_chorea_1268',
    name: 'chores vs chorea',
    description: 'Commonly confused words: chores and chorea',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchores\b/gi,
    message: 'Check: "chores" ("chores") vs "chorea" ("chorea"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chorea';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chorused_choruses_1269',
    name: 'chorused vs choruses',
    description: 'Commonly confused words: chorused and choruses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchorused\b/gi,
    message: 'Check: "chorused" ("chorused") vs "choruses" ("choruses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'choruses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chromed_chromes_1270',
    name: 'chromed vs chromes',
    description: 'Commonly confused words: chromed and chromes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchromed\b/gi,
    message: 'Check: "chromed" ("chromed") vs "chromes" ("chromes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chromes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chronicled_chronicles_1271',
    name: 'chronicled vs chronicles',
    description: 'Commonly confused words: chronicled and chronicles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchronicled\b/gi,
    message: 'Check: "chronicled" ("chronicled") vs "chronicles" ("chronicles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chronicles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chub_chug_1272',
    name: 'chub vs chug',
    description: 'Commonly confused words: chub and chug',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchub\b/gi,
    message: 'Check: "chub" ("chub") vs "chug" ("chug"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chug';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_chubs_chugs_1273',
    name: 'chubs vs chugs',
    description: 'Commonly confused words: chubs and chugs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bchubs\b/gi,
    message: 'Check: "chubs" ("chubs") vs "chugs" ("chugs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'chugs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_circumnavigated_circumnavigates_1274',
    name: 'circumnavigated vs circumnavigates',
    description: 'Commonly confused words: circumnavigated and circumnavigates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcircumnavigated\b/gi,
    message: 'Check: "circumnavigated" ("circumnavigated") vs "circumnavigates" ("circumnavigates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'circumnavigates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_circumscribed_circumscribes_1275',
    name: 'circumscribed vs circumscribes',
    description: 'Commonly confused words: circumscribed and circumscribes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcircumscribed\b/gi,
    message: 'Check: "circumscribed" ("circumscribed") vs "circumscribes" ("circumscribes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'circumscribes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cite_cote_1276',
    name: 'cite vs cote',
    description: 'Commonly confused words: cite and cote',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcite\b/gi,
    message: 'Check: "cite" (to quote or reference) vs "cote" ("cote"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cote';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cite_cute_1277',
    name: 'cite vs cute',
    description: 'Commonly confused words: cite and cute',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcite\b/gi,
    message: 'Check: "cite" (to quote or reference) vs "cute" ("cute"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cute';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cites_cotes_1278',
    name: 'cites vs cotes',
    description: 'Commonly confused words: cites and cotes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcites\b/gi,
    message: 'Check: "cites" ("cites") vs "cotes" ("cotes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cotes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_civet_covet_1279',
    name: 'civet vs covet',
    description: 'Commonly confused words: civet and covet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcivet\b/gi,
    message: 'Check: "civet" ("civet") vs "covet" ("covet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'covet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_clarified_clarifies_1280',
    name: 'clarified vs clarifies',
    description: 'Commonly confused words: clarified and clarifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclarified\b/gi,
    message: 'Check: "clarified" ("clarified") vs "clarifies" ("clarifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clarifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_clashed_clashes_1281',
    name: 'clashed vs clashes',
    description: 'Commonly confused words: clashed and clashes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclashed\b/gi,
    message: 'Check: "clashed" ("clashed") vs "clashes" ("clashes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clashes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_classed_classes_1282',
    name: 'classed vs classes',
    description: 'Commonly confused words: classed and classes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclassed\b/gi,
    message: 'Check: "classed" ("classed") vs "classes" ("classes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'classes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cleansed_cleanses_1283',
    name: 'cleansed vs cleanses',
    description: 'Commonly confused words: cleansed and cleanses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcleansed\b/gi,
    message: 'Check: "cleansed" ("cleansed") vs "cleanses" ("cleanses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cleanses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_clears_cleats_1284',
    name: 'clears vs cleats',
    description: 'Commonly confused words: clears and cleats',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclears\b/gi,
    message: 'Check: "clears" ("clears") vs "cleats" ("cleats"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cleats';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_clenched_clenches_1285',
    name: 'clenched vs clenches',
    description: 'Commonly confused words: clenched and clenches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclenched\b/gi,
    message: 'Check: "clenched" ("clenched") vs "clenches" ("clenches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clenches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cliched_cliches_1286',
    name: 'cliched vs cliches',
    description: 'Commonly confused words: cliched and cliches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcliched\b/gi,
    message: 'Check: "cliched" ("cliched") vs "cliches" ("cliches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cliches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_clicking_clucking_1287',
    name: 'clicking vs clucking',
    description: 'Commonly confused words: clicking and clucking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclicking\b/gi,
    message: 'Check: "clicking" ("clicking") vs "clucking" ("clucking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clucking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_climaxed_climaxes_1288',
    name: 'climaxed vs climaxes',
    description: 'Commonly confused words: climaxed and climaxes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclimaxed\b/gi,
    message: 'Check: "climaxed" ("climaxed") vs "climaxes" ("climaxes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'climaxes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cling_clung_1289',
    name: 'cling vs clung',
    description: 'Commonly confused words: cling and clung',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcling\b/gi,
    message: 'Check: "cling" ("cling") vs "clung" ("clung"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clung';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_clinkers_clunkers_1290',
    name: 'clinkers vs clunkers',
    description: 'Commonly confused words: clinkers and clunkers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclinkers\b/gi,
    message: 'Check: "clinkers" ("clinkers") vs "clunkers" ("clunkers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clunkers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_clinking_clunking_1291',
    name: 'clinking vs clunking',
    description: 'Commonly confused words: clinking and clunking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclinking\b/gi,
    message: 'Check: "clinking" ("clinking") vs "clunking" ("clunking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clunking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_clits_clots_1292',
    name: 'clits vs clots',
    description: 'Commonly confused words: clits and clots',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclits\b/gi,
    message: 'Check: "clits" ("clits") vs "clots" ("clots"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clots';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_clog_clot_1293',
    name: 'clog vs clot',
    description: 'Commonly confused words: clog and clot',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclog\b/gi,
    message: 'Check: "clog" ("clog") vs "clot" ("clot"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clot';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_clogs_clots_1294',
    name: 'clogs vs clots',
    description: 'Commonly confused words: clogs and clots',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclogs\b/gi,
    message: 'Check: "clogs" ("clogs") vs "clots" ("clots"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clots';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cloned_clones_1295',
    name: 'cloned vs clones',
    description: 'Commonly confused words: cloned and clones',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcloned\b/gi,
    message: 'Check: "cloned" ("cloned") vs "clones" ("clones"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clones';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_closer_closet_1296',
    name: 'closer vs closet',
    description: 'Commonly confused words: closer and closet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcloser\b/gi,
    message: 'Check: "closer" ("closer") vs "closet" ("closet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'closet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cloy_clot_1297',
    name: 'cloy vs clot',
    description: 'Commonly confused words: cloy and clot',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcloy\b/gi,
    message: 'Check: "cloy" ("cloy") vs "clot" ("clot"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clot';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_clunker_clinker_1298',
    name: 'clunker vs clinker',
    description: 'Commonly confused words: clunker and clinker',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bclunker\b/gi,
    message: 'Check: "clunker" ("clunker") vs "clinker" ("clinker"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clinker';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coached_coaches_1299',
    name: 'coached vs coaches',
    description: 'Commonly confused words: coached and coaches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoached\b/gi,
    message: 'Check: "coached" ("coached") vs "coaches" ("coaches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coaches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coagulated_coagulates_1300',
    name: 'coagulated vs coagulates',
    description: 'Commonly confused words: coagulated and coagulates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoagulated\b/gi,
    message: 'Check: "coagulated" ("coagulated") vs "coagulates" ("coagulates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coagulates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coalesced_coalesces_1301',
    name: 'coalesced vs coalesces',
    description: 'Commonly confused words: coalesced and coalesces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoalesced\b/gi,
    message: 'Check: "coalesced" ("coalesced") vs "coalesces" ("coalesces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coalesces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coating_costing_1302',
    name: 'coating vs costing',
    description: 'Commonly confused words: coating and costing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoating\b/gi,
    message: 'Check: "coating" ("coating") vs "costing" ("costing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'costing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cobbled_cobbles_1303',
    name: 'cobbled vs cobbles',
    description: 'Commonly confused words: cobbled and cobbles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcobbled\b/gi,
    message: 'Check: "cobbled" ("cobbled") vs "cobbles" ("cobbles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cobbles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cochleae_cochlear_1304',
    name: 'cochleae vs cochlear',
    description: 'Commonly confused words: cochleae and cochlear',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcochleae\b/gi,
    message: 'Check: "cochleae" ("cochleae") vs "cochlear" ("cochlear"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cochlear';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cocked_docked_1305',
    name: 'cocked vs docked',
    description: 'Commonly confused words: cocked and docked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcocked\b/gi,
    message: 'Check: "cocked" ("cocked") vs "docked" ("docked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'docked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cocos_cocoa_1306',
    name: 'cocos vs cocoa',
    description: 'Commonly confused words: cocos and cocoa',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcocos\b/gi,
    message: 'Check: "cocos" ("cocos") vs "cocoa" ("cocoa"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cocoa';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coded_codes_1307',
    name: 'coded vs codes',
    description: 'Commonly confused words: coded and codes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoded\b/gi,
    message: 'Check: "coded" ("coded") vs "codes" ("codes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'codes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_codified_codifies_1308',
    name: 'codified vs codifies',
    description: 'Commonly confused words: codified and codifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcodified\b/gi,
    message: 'Check: "codified" ("codified") vs "codifies" ("codifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'codifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coeds_cords_1309',
    name: 'coeds vs cords',
    description: 'Commonly confused words: coeds and cords',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoeds\b/gi,
    message: 'Check: "coeds" ("coeds") vs "cords" ("cords"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cords';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coerced_coerces_1310',
    name: 'coerced vs coerces',
    description: 'Commonly confused words: coerced and coerces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoerced\b/gi,
    message: 'Check: "coerced" ("coerced") vs "coerces" ("coerces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coerces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coffees_coffers_1311',
    name: 'coffees vs coffers',
    description: 'Commonly confused words: coffees and coffers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoffees\b/gi,
    message: 'Check: "coffees" ("coffees") vs "coffers" ("coffers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coffers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cohered_coheres_1312',
    name: 'cohered vs coheres',
    description: 'Commonly confused words: cohered and coheres',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcohered\b/gi,
    message: 'Check: "cohered" ("cohered") vs "coheres" ("coheres"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coheres';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coif_coir_1313',
    name: 'coif vs coir',
    description: 'Commonly confused words: coif and coir',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoif\b/gi,
    message: 'Check: "coif" ("coif") vs "coir" ("coir"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coir';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coiffured_coiffures_1314',
    name: 'coiffured vs coiffures',
    description: 'Commonly confused words: coiffured and coiffures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoiffured\b/gi,
    message: 'Check: "coiffured" ("coiffured") vs "coiffures" ("coiffures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coiffures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coiled_cooled_1315',
    name: 'coiled vs cooled',
    description: 'Commonly confused words: coiled and cooled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoiled\b/gi,
    message: 'Check: "coiled" ("coiled") vs "cooled" ("cooled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cooled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coils_cools_1316',
    name: 'coils vs cools',
    description: 'Commonly confused words: coils and cools',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoils\b/gi,
    message: 'Check: "coils" ("coils") vs "cools" ("cools"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cools';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coked_cokes_1317',
    name: 'coked vs cokes',
    description: 'Commonly confused words: coked and cokes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoked\b/gi,
    message: 'Check: "coked" ("coked") vs "cokes" ("cokes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cokes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_collage_collate_1318',
    name: 'collage vs collate',
    description: 'Commonly confused words: collage and collate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcollage\b/gi,
    message: 'Check: "collage" ("collage") vs "collate" ("collate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'collate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_collages_collates_1319',
    name: 'collages vs collates',
    description: 'Commonly confused words: collages and collates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcollages\b/gi,
    message: 'Check: "collages" ("collages") vs "collates" ("collates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'collates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_collar_dollar_1320',
    name: 'collar vs dollar',
    description: 'Commonly confused words: collar and dollar',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcollar\b/gi,
    message: 'Check: "collar" ("collar") vs "dollar" ("dollar"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dollar';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_collared_collated_1321',
    name: 'collared vs collated',
    description: 'Commonly confused words: collared and collated',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcollared\b/gi,
    message: 'Check: "collared" ("collared") vs "collated" ("collated"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'collated';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_collaring_collating_1322',
    name: 'collaring vs collating',
    description: 'Commonly confused words: collaring and collating',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcollaring\b/gi,
    message: 'Check: "collaring" ("collaring") vs "collating" ("collating"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'collating';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_collide_collude_1323',
    name: 'collide vs collude',
    description: 'Commonly confused words: collide and collude',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcollide\b/gi,
    message: 'Check: "collide" ("collide") vs "collude" ("collude"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'collude';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_collided_colluded_1324',
    name: 'collided vs colluded',
    description: 'Commonly confused words: collided and colluded',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcollided\b/gi,
    message: 'Check: "collided" ("collided") vs "colluded" ("colluded"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'colluded';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_collides_colludes_1325',
    name: 'collides vs colludes',
    description: 'Commonly confused words: collides and colludes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcollides\b/gi,
    message: 'Check: "collides" ("collides") vs "colludes" ("colludes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'colludes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_colliding_colluding_1326',
    name: 'colliding vs colluding',
    description: 'Commonly confused words: colliding and colluding',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcolliding\b/gi,
    message: 'Check: "colliding" ("colliding") vs "colluding" ("colluding"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'colluding';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_collocated_collocates_1327',
    name: 'collocated vs collocates',
    description: 'Commonly confused words: collocated and collocates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcollocated\b/gi,
    message: 'Check: "collocated" ("collocated") vs "collocates" ("collocates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'collocates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_colluded_colludes_1328',
    name: 'colluded vs colludes',
    description: 'Commonly confused words: colluded and colludes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcolluded\b/gi,
    message: 'Check: "colluded" ("colluded") vs "colludes" ("colludes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'colludes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_colonized_colonizes_1329',
    name: 'colonized vs colonizes',
    description: 'Commonly confused words: colonized and colonizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcolonized\b/gi,
    message: 'Check: "colonized" ("colonized") vs "colonizes" ("colonizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'colonizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_colonnaded_colonnades_1330',
    name: 'colonnaded vs colonnades',
    description: 'Commonly confused words: colonnaded and colonnades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcolonnaded\b/gi,
    message: 'Check: "colonnaded" ("colonnaded") vs "colonnades" ("colonnades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'colonnades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_comers_comets_1331',
    name: 'comers vs comets',
    description: 'Commonly confused words: comers and comets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcomers\b/gi,
    message: 'Check: "comers" ("comers") vs "comets" ("comets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'comets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_comical_conical_1332',
    name: 'comical vs conical',
    description: 'Commonly confused words: comical and conical',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcomical\b/gi,
    message: 'Check: "comical" ("comical") vs "conical" ("conical"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conical';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_comically_conically_1333',
    name: 'comically vs conically',
    description: 'Commonly confused words: comically and conically',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcomically\b/gi,
    message: 'Check: "comically" ("comically") vs "conically" ("conically"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conically';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_comic_conic_1334',
    name: 'comic vs conic',
    description: 'Commonly confused words: comic and conic',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcomic\b/gi,
    message: 'Check: "comic" ("comic") vs "conic" ("conic"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conic';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_commemorated_commemorates_1335',
    name: 'commemorated vs commemorates',
    description: 'Commonly confused words: commemorated and commemorates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcommemorated\b/gi,
    message: 'Check: "commemorated" ("commemorated") vs "commemorates" ("commemorates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'commemorates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_commenced_commended_1336',
    name: 'commenced vs commended',
    description: 'Commonly confused words: commenced and commended',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcommenced\b/gi,
    message: 'Check: "commenced" ("commenced") vs "commended" ("commended"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'commended';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_commencing_commending_1337',
    name: 'commencing vs commending',
    description: 'Commonly confused words: commencing and commending',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcommencing\b/gi,
    message: 'Check: "commencing" ("commencing") vs "commending" ("commending"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'commending';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_commercialized_commercializes_1338',
    name: 'commercialized vs commercializes',
    description: 'Commonly confused words: commercialized and commercializes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcommercialized\b/gi,
    message: 'Check: "commercialized" ("commercialized") vs "commercializes" ("commercializes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'commercializes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_communed_communes_1339',
    name: 'communed vs communes',
    description: 'Commonly confused words: communed and communes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcommuned\b/gi,
    message: 'Check: "communed" ("communed") vs "communes" ("communes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'communes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_communicated_communicates_1340',
    name: 'communicated vs communicates',
    description: 'Commonly confused words: communicated and communicates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcommunicated\b/gi,
    message: 'Check: "communicated" ("communicated") vs "communicates" ("communicates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'communicates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_commuted_commutes_1341',
    name: 'commuted vs commutes',
    description: 'Commonly confused words: commuted and commutes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcommuted\b/gi,
    message: 'Check: "commuted" ("commuted") vs "commutes" ("commutes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'commutes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_compassed_compasses_1342',
    name: 'compassed vs compasses',
    description: 'Commonly confused words: compassed and compasses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcompassed\b/gi,
    message: 'Check: "compassed" ("compassed") vs "compasses" ("compasses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'compasses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_compensated_compensates_1343',
    name: 'compensated vs compensates',
    description: 'Commonly confused words: compensated and compensates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcompensated\b/gi,
    message: 'Check: "compensated" ("compensated") vs "compensates" ("compensates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'compensates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_compered_comperes_1344',
    name: 'compered vs comperes',
    description: 'Commonly confused words: compered and comperes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcompered\b/gi,
    message: 'Check: "compered" ("compered") vs "comperes" ("comperes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'comperes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_comperes_competes_1345',
    name: 'comperes vs competes',
    description: 'Commonly confused words: comperes and competes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcomperes\b/gi,
    message: 'Check: "comperes" ("comperes") vs "competes" ("competes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'competes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_composited_composites_1346',
    name: 'composited vs composites',
    description: 'Commonly confused words: composited and composites',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcomposited\b/gi,
    message: 'Check: "composited" ("composited") vs "composites" ("composites"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'composites';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_computed_computes_1347',
    name: 'computed vs computes',
    description: 'Commonly confused words: computed and computes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcomputed\b/gi,
    message: 'Check: "computed" ("computed") vs "computes" ("computes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'computes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_concatenated_concatenates_1348',
    name: 'concatenated vs concatenates',
    description: 'Commonly confused words: concatenated and concatenates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconcatenated\b/gi,
    message: 'Check: "concatenated" ("concatenated") vs "concatenates" ("concatenates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'concatenates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_conceptualized_conceptualizes_1349',
    name: 'conceptualized vs conceptualizes',
    description: 'Commonly confused words: conceptualized and conceptualizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconceptualized\b/gi,
    message: 'Check: "conceptualized" ("conceptualized") vs "conceptualizes" ("conceptualizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conceptualizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_concert_convert_1350',
    name: 'concert vs convert',
    description: 'Commonly confused words: concert and convert',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconcert\b/gi,
    message: 'Check: "concert" ("concert") vs "convert" ("convert"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'convert';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_concerts_converts_1351',
    name: 'concerts vs converts',
    description: 'Commonly confused words: concerts and converts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconcerts\b/gi,
    message: 'Check: "concerts" ("concerts") vs "converts" ("converts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'converts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_conch_cinch_1352',
    name: 'conch vs cinch',
    description: 'Commonly confused words: conch and cinch',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconch\b/gi,
    message: 'Check: "conch" ("conch") vs "cinch" ("cinch"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cinch';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_concreted_concretes_1353',
    name: 'concreted vs concretes',
    description: 'Commonly confused words: concreted and concretes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconcreted\b/gi,
    message: 'Check: "concreted" ("concreted") vs "concretes" ("concretes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'concretes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_condensed_condenses_1354',
    name: 'condensed vs condenses',
    description: 'Commonly confused words: condensed and condenses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcondensed\b/gi,
    message: 'Check: "condensed" ("condensed") vs "condenses" ("condenses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'condenses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_condign_consign_1355',
    name: 'condign vs consign',
    description: 'Commonly confused words: condign and consign',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcondign\b/gi,
    message: 'Check: "condign" ("condign") vs "consign" ("consign"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'consign';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_condole_console_1356',
    name: 'condole vs console',
    description: 'Commonly confused words: condole and console',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcondole\b/gi,
    message: 'Check: "condole" ("condole") vs "console" ("console"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'console';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_condoned_condones_1357',
    name: 'condoned vs condones',
    description: 'Commonly confused words: condoned and condones',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcondoned\b/gi,
    message: 'Check: "condoned" ("condoned") vs "condones" ("condones"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'condones';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cones_comes_1358',
    name: 'cones vs comes',
    description: 'Commonly confused words: cones and comes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcones\b/gi,
    message: 'Check: "cones" ("cones") vs "comes" ("comes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'comes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_confederated_confederates_1359',
    name: 'confederated vs confederates',
    description: 'Commonly confused words: confederated and confederates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconfederated\b/gi,
    message: 'Check: "confederated" ("confederated") vs "confederates" ("confederates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'confederates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_confer_conger_1360',
    name: 'confer vs conger',
    description: 'Commonly confused words: confer and conger',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconfer\b/gi,
    message: 'Check: "confer" ("confer") vs "conger" ("conger"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conger';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_confers_congers_1361',
    name: 'confers vs congers',
    description: 'Commonly confused words: confers and congers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconfers\b/gi,
    message: 'Check: "confers" ("confers") vs "congers" ("congers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'congers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_configured_configures_1362',
    name: 'configured vs configures',
    description: 'Commonly confused words: configured and configures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconfigured\b/gi,
    message: 'Check: "configured" ("configured") vs "configures" ("configures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'configures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_confined_confines_1363',
    name: 'confined vs confines',
    description: 'Commonly confused words: confined and confines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconfined\b/gi,
    message: 'Check: "confined" ("confined") vs "confines" ("confines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'confines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_confirmations_conformations_1364',
    name: 'confirmations vs conformations',
    description: 'Commonly confused words: confirmations and conformations',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconfirmations\b/gi,
    message: 'Check: "confirmations" ("confirmations") vs "conformations" ("conformations"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conformations';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_confirming_conforming_1365',
    name: 'confirming vs conforming',
    description: 'Commonly confused words: confirming and conforming',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconfirming\b/gi,
    message: 'Check: "confirming" ("confirming") vs "conforming" ("conforming"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conforming';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_confirms_conforms_1366',
    name: 'confirms vs conforms',
    description: 'Commonly confused words: confirms and conforms',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconfirms\b/gi,
    message: 'Check: "confirms" ("confirms") vs "conforms" ("conforms"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conforms';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_conflated_conflates_1367',
    name: 'conflated vs conflates',
    description: 'Commonly confused words: conflated and conflates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconflated\b/gi,
    message: 'Check: "conflated" ("conflated") vs "conflates" ("conflates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conflates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_congesting_contesting_1368',
    name: 'congesting vs contesting',
    description: 'Commonly confused words: congesting and contesting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcongesting\b/gi,
    message: 'Check: "congesting" ("congesting") vs "contesting" ("contesting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'contesting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_conglomerated_conglomerates_1369',
    name: 'conglomerated vs conglomerates',
    description: 'Commonly confused words: conglomerated and conglomerates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconglomerated\b/gi,
    message: 'Check: "conglomerated" ("conglomerated") vs "conglomerates" ("conglomerates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conglomerates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_conjectured_conjectures_1370',
    name: 'conjectured vs conjectures',
    description: 'Commonly confused words: conjectured and conjectures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconjectured\b/gi,
    message: 'Check: "conjectured" ("conjectured") vs "conjectures" ("conjectures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conjectures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_conjugated_conjugates_1371',
    name: 'conjugated vs conjugates',
    description: 'Commonly confused words: conjugated and conjugates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconjugated\b/gi,
    message: 'Check: "conjugated" ("conjugated") vs "conjugates" ("conjugates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conjugates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_conned_donned_1372',
    name: 'conned vs donned',
    description: 'Commonly confused words: conned and donned',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconned\b/gi,
    message: 'Check: "conned" ("conned") vs "donned" ("donned"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'donned';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_conning_donning_1373',
    name: 'conning vs donning',
    description: 'Commonly confused words: conning and donning',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconning\b/gi,
    message: 'Check: "conning" ("conning") vs "donning" ("donning"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'donning';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_connived_connives_1374',
    name: 'connived vs connives',
    description: 'Commonly confused words: connived and connives',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconnived\b/gi,
    message: 'Check: "connived" ("connived") vs "connives" ("connives"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'connives';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_consecrated_consecrates_1375',
    name: 'consecrated vs consecrates',
    description: 'Commonly confused words: consecrated and consecrates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconsecrated\b/gi,
    message: 'Check: "consecrated" ("consecrated") vs "consecrates" ("consecrates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'consecrates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_conserved_conserves_1376',
    name: 'conserved vs conserves',
    description: 'Commonly confused words: conserved and conserves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconserved\b/gi,
    message: 'Check: "conserved" ("conserved") vs "conserves" ("conserves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'conserves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_consigned_consignee_1377',
    name: 'consigned vs consignee',
    description: 'Commonly confused words: consigned and consignee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconsigned\b/gi,
    message: 'Check: "consigned" ("consigned") vs "consignee" ("consignee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'consignee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_consoled_consoles_1378',
    name: 'consoled vs consoles',
    description: 'Commonly confused words: consoled and consoles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconsoled\b/gi,
    message: 'Check: "consoled" ("consoled") vs "consoles" ("consoles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'consoles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_constituted_constitutes_1379',
    name: 'constituted vs constitutes',
    description: 'Commonly confused words: constituted and constitutes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconstituted\b/gi,
    message: 'Check: "constituted" ("constituted") vs "constitutes" ("constitutes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'constitutes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_constrictor_constructor_1380',
    name: 'constrictor vs constructor',
    description: 'Commonly confused words: constrictor and constructor',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconstrictor\b/gi,
    message: 'Check: "constrictor" ("constrictor") vs "constructor" (function Object() { [native code] }). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'constructor';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_construed_construes_1381',
    name: 'construed vs construes',
    description: 'Commonly confused words: construed and construes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconstrued\b/gi,
    message: 'Check: "construed" ("construed") vs "construes" ("construes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'construes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_consumed_consumes_1382',
    name: 'consumed vs consumes',
    description: 'Commonly confused words: consumed and consumes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconsumed\b/gi,
    message: 'Check: "consumed" ("consumed") vs "consumes" ("consumes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'consumes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_consummated_consummates_1383',
    name: 'consummated vs consummates',
    description: 'Commonly confused words: consummated and consummates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconsummated\b/gi,
    message: 'Check: "consummated" ("consummated") vs "consummates" ("consummates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'consummates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_contests_contexts_1384',
    name: 'contests vs contexts',
    description: 'Commonly confused words: contests and contexts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcontests\b/gi,
    message: 'Check: "contests" ("contests") vs "contexts" ("contexts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'contexts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_contextualized_contextualizes_1385',
    name: 'contextualized vs contextualizes',
    description: 'Commonly confused words: contextualized and contextualizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcontextualized\b/gi,
    message: 'Check: "contextualized" ("contextualized") vs "contextualizes" ("contextualizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'contextualizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_contravened_contravenes_1386',
    name: 'contravened vs contravenes',
    description: 'Commonly confused words: contravened and contravenes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcontravened\b/gi,
    message: 'Check: "contravened" ("contravened") vs "contravenes" ("contravenes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'contravenes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_contrived_contrives_1387',
    name: 'contrived vs contrives',
    description: 'Commonly confused words: contrived and contrives',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcontrived\b/gi,
    message: 'Check: "contrived" ("contrived") vs "contrives" ("contrives"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'contrives';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_converged_converges_1388',
    name: 'converged vs converges',
    description: 'Commonly confused words: converged and converges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconverged\b/gi,
    message: 'Check: "converged" ("converged") vs "converges" ("converges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'converges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_conversed_converses_1389',
    name: 'conversed vs converses',
    description: 'Commonly confused words: conversed and converses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconversed\b/gi,
    message: 'Check: "conversed" ("conversed") vs "converses" ("converses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'converses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_converting_converging_1390',
    name: 'converting vs converging',
    description: 'Commonly confused words: converting and converging',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconverting\b/gi,
    message: 'Check: "converting" ("converting") vs "converging" ("converging"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'converging';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_convulsed_convulses_1391',
    name: 'convulsed vs convulses',
    description: 'Commonly confused words: convulsed and convulses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bconvulsed\b/gi,
    message: 'Check: "convulsed" ("convulsed") vs "convulses" ("convulses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'convulses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cooked_cooled_1392',
    name: 'cooked vs cooled',
    description: 'Commonly confused words: cooked and cooled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcooked\b/gi,
    message: 'Check: "cooked" ("cooked") vs "cooled" ("cooled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cooled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cookers_coolers_1393',
    name: 'cookers vs coolers',
    description: 'Commonly confused words: cookers and coolers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcookers\b/gi,
    message: 'Check: "cookers" ("cookers") vs "coolers" ("coolers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coolers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cookie_coolie_1394',
    name: 'cookie vs coolie',
    description: 'Commonly confused words: cookie and coolie',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcookie\b/gi,
    message: 'Check: "cookie" ("cookie") vs "coolie" ("coolie"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coolie';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cookies_coolies_1395',
    name: 'cookies vs coolies',
    description: 'Commonly confused words: cookies and coolies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcookies\b/gi,
    message: 'Check: "cookies" ("cookies") vs "coolies" ("coolies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coolies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cool_coll_1396',
    name: 'cool vs coll',
    description: 'Commonly confused words: cool and coll',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcool\b/gi,
    message: 'Check: "cool" ("cool") vs "coll" ("coll"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coll';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cooler_cooker_1397',
    name: 'cooler vs cooker',
    description: 'Commonly confused words: cooler and cooker',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcooler\b/gi,
    message: 'Check: "cooler" ("cooler") vs "cooker" ("cooker"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cooker';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coon_coin_1398',
    name: 'coon vs coin',
    description: 'Commonly confused words: coon and coin',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoon\b/gi,
    message: 'Check: "coon" ("coon") vs "coin" ("coin"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coin';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cooperage_cooperate_1399',
    name: 'cooperage vs cooperate',
    description: 'Commonly confused words: cooperage and cooperate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcooperage\b/gi,
    message: 'Check: "cooperage" ("cooperage") vs "cooperate" ("cooperate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cooperate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coordinated_coordinates_1400',
    name: 'coordinated vs coordinates',
    description: 'Commonly confused words: coordinated and coordinates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoordinated\b/gi,
    message: 'Check: "coordinated" ("coordinated") vs "coordinates" ("coordinates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coordinates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coot_clot_1401',
    name: 'coot vs clot',
    description: 'Commonly confused words: coot and clot',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoot\b/gi,
    message: 'Check: "coot" ("coot") vs "clot" ("clot"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clot';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coots_clots_1402',
    name: 'coots vs clots',
    description: 'Commonly confused words: coots and clots',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoots\b/gi,
    message: 'Check: "coots" ("coots") vs "clots" ("clots"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'clots';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_copied_copies_1403',
    name: 'copied vs copies',
    description: 'Commonly confused words: copied and copies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcopied\b/gi,
    message: 'Check: "copied" ("copied") vs "copies" ("copies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'copies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coping_cooing_1404',
    name: 'coping vs cooing',
    description: 'Commonly confused words: coping and cooing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoping\b/gi,
    message: 'Check: "coping" ("coping") vs "cooing" ("cooing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cooing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_copper_cooper_1405',
    name: 'copper vs cooper',
    description: 'Commonly confused words: copper and cooper',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcopper\b/gi,
    message: 'Check: "copper" ("copper") vs "cooper" ("cooper"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cooper';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_copulated_copulates_1406',
    name: 'copulated vs copulates',
    description: 'Commonly confused words: copulated and copulates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcopulated\b/gi,
    message: 'Check: "copulated" ("copulated") vs "copulates" ("copulates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'copulates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cords_cores_1407',
    name: 'cords vs cores',
    description: 'Commonly confused words: cords and cores',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcords\b/gi,
    message: 'Check: "cords" ("cords") vs "cores" ("cores"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cores';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_corks_dorks_1408',
    name: 'corks vs dorks',
    description: 'Commonly confused words: corks and dorks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcorks\b/gi,
    message: 'Check: "corks" ("corks") vs "dorks" ("dorks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dorks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coroner_coronet_1409',
    name: 'coroner vs coronet',
    description: 'Commonly confused words: coroner and coronet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoroner\b/gi,
    message: 'Check: "coroner" ("coroner") vs "coronet" ("coronet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'coronet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_correlated_correlates_1410',
    name: 'correlated vs correlates',
    description: 'Commonly confused words: correlated and correlates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcorrelated\b/gi,
    message: 'Check: "correlated" ("correlated") vs "correlates" ("correlates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'correlates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_corroborated_corroborates_1411',
    name: 'corroborated vs corroborates',
    description: 'Commonly confused words: corroborated and corroborates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcorroborated\b/gi,
    message: 'Check: "corroborated" ("corroborated") vs "corroborates" ("corroborates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'corroborates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_corroded_corrodes_1412',
    name: 'corroded vs corrodes',
    description: 'Commonly confused words: corroded and corrodes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcorroded\b/gi,
    message: 'Check: "corroded" ("corroded") vs "corrodes" ("corrodes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'corrodes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_couched_couches_1413',
    name: 'couched vs couches',
    description: 'Commonly confused words: couched and couches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcouched\b/gi,
    message: 'Check: "couched" ("couched") vs "couches" ("couches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'couches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_couched_vouched_1414',
    name: 'couched vs vouched',
    description: 'Commonly confused words: couched and vouched',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcouched\b/gi,
    message: 'Check: "couched" ("couched") vs "vouched" ("vouched"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vouched';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_couches_vouches_1415',
    name: 'couches vs vouches',
    description: 'Commonly confused words: couches and vouches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcouches\b/gi,
    message: 'Check: "couches" ("couches") vs "vouches" ("vouches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vouches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_couching_vouching_1416',
    name: 'couching vs vouching',
    description: 'Commonly confused words: couching and vouching',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcouching\b/gi,
    message: 'Check: "couching" ("couching") vs "vouching" ("vouching"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vouching';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_couch_vouch_1417',
    name: 'couch vs vouch',
    description: 'Commonly confused words: couch and vouch',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcouch\b/gi,
    message: 'Check: "couch" ("couch") vs "vouch" ("vouch"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vouch';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cough_dough_1418',
    name: 'cough vs dough',
    description: 'Commonly confused words: cough and dough',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcough\b/gi,
    message: 'Check: "cough" ("cough") vs "dough" ("dough"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dough';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_countenanced_countenances_1419',
    name: 'countenanced vs countenances',
    description: 'Commonly confused words: countenanced and countenances',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcountenanced\b/gi,
    message: 'Check: "countenanced" ("countenanced") vs "countenances" ("countenances"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'countenances';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_counterbalanced_counterbalances_1420',
    name: 'counterbalanced vs counterbalances',
    description: 'Commonly confused words: counterbalanced and counterbalances',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcounterbalanced\b/gi,
    message: 'Check: "counterbalanced" ("counterbalanced") vs "counterbalances" ("counterbalances"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'counterbalances';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_countersink_countersunk_1421',
    name: 'countersink vs countersunk',
    description: 'Commonly confused words: countersink and countersunk',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcountersink\b/gi,
    message: 'Check: "countersink" ("countersink") vs "countersunk" ("countersunk"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'countersunk';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_coupled_couples_1422',
    name: 'coupled vs couples',
    description: 'Commonly confused words: coupled and couples',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcoupled\b/gi,
    message: 'Check: "coupled" ("coupled") vs "couples" ("couples"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'couples';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cowered_dowered_1423',
    name: 'cowered vs dowered',
    description: 'Commonly confused words: cowered and dowered',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcowered\b/gi,
    message: 'Check: "cowered" ("cowered") vs "dowered" ("dowered"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dowered';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cozen_dozen_1424',
    name: 'cozen vs dozen',
    description: 'Commonly confused words: cozen and dozen',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcozen\b/gi,
    message: 'Check: "cozen" ("cozen") vs "dozen" ("dozen"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dozen';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cozens_dozens_1425',
    name: 'cozens vs dozens',
    description: 'Commonly confused words: cozens and dozens',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcozens\b/gi,
    message: 'Check: "cozens" ("cozens") vs "dozens" ("dozens"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dozens';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crab_crag_1426',
    name: 'crab vs crag',
    description: 'Commonly confused words: crab and crag',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrab\b/gi,
    message: 'Check: "crab" ("crab") vs "crag" ("crag"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crag';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crafts_drafts_1427',
    name: 'crafts vs drafts',
    description: 'Commonly confused words: crafts and drafts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrafts\b/gi,
    message: 'Check: "crafts" ("crafts") vs "drafts" ("drafts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drafts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crafty_drafty_1428',
    name: 'crafty vs drafty',
    description: 'Commonly confused words: crafty and drafty',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrafty\b/gi,
    message: 'Check: "crafty" ("crafty") vs "drafty" ("drafty"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drafty';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_craggy_draggy_1429',
    name: 'craggy vs draggy',
    description: 'Commonly confused words: craggy and draggy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcraggy\b/gi,
    message: 'Check: "craggy" ("craggy") vs "draggy" ("draggy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'draggy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_craned_cranes_1430',
    name: 'craned vs cranes',
    description: 'Commonly confused words: craned and cranes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcraned\b/gi,
    message: 'Check: "craned" ("craned") vs "cranes" ("cranes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cranes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crank_drank_1431',
    name: 'crank vs drank',
    description: 'Commonly confused words: crank and drank',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrank\b/gi,
    message: 'Check: "crank" ("crank") vs "drank" ("drank"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drank';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crawl_drawl_1432',
    name: 'crawl vs drawl',
    description: 'Commonly confused words: crawl and drawl',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrawl\b/gi,
    message: 'Check: "crawl" ("crawl") vs "drawl" ("drawl"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drawl';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crawled_drawled_1433',
    name: 'crawled vs drawled',
    description: 'Commonly confused words: crawled and drawled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrawled\b/gi,
    message: 'Check: "crawled" ("crawled") vs "drawled" ("drawled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drawled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crawling_drawling_1434',
    name: 'crawling vs drawling',
    description: 'Commonly confused words: crawling and drawling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrawling\b/gi,
    message: 'Check: "crawling" ("crawling") vs "drawling" ("drawling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drawling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crawls_drawls_1435',
    name: 'crawls vs drawls',
    description: 'Commonly confused words: crawls and drawls',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrawls\b/gi,
    message: 'Check: "crawls" ("crawls") vs "drawls" ("drawls"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drawls';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_craws_crass_1436',
    name: 'craws vs crass',
    description: 'Commonly confused words: craws and crass',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcraws\b/gi,
    message: 'Check: "craws" ("craws") vs "crass" ("crass"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crass';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crazed_crazes_1437',
    name: 'crazed vs crazes',
    description: 'Commonly confused words: crazed and crazes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrazed\b/gi,
    message: 'Check: "crazed" ("crazed") vs "crazes" ("crazes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crazes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_creamed_dreamed_1438',
    name: 'creamed vs dreamed',
    description: 'Commonly confused words: creamed and dreamed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcreamed\b/gi,
    message: 'Check: "creamed" ("creamed") vs "dreamed" ("dreamed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dreamed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_creamier_dreamier_1439',
    name: 'creamier vs dreamier',
    description: 'Commonly confused words: creamier and dreamier',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcreamier\b/gi,
    message: 'Check: "creamier" ("creamier") vs "dreamier" ("dreamier"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dreamier';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_creased_creases_1440',
    name: 'creased vs creases',
    description: 'Commonly confused words: creased and creases',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcreased\b/gi,
    message: 'Check: "creased" ("creased") vs "creases" ("creases"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'creases';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_created_crested_1441',
    name: 'created vs crested',
    description: 'Commonly confused words: created and crested',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcreated\b/gi,
    message: 'Check: "created" ("created") vs "crested" ("crested"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crested';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_creeks_creels_1442',
    name: 'creeks vs creels',
    description: 'Commonly confused words: creeks and creels',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcreeks\b/gi,
    message: 'Check: "creeks" ("creeks") vs "creels" ("creels"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'creels';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cremated_cremates_1443',
    name: 'cremated vs cremates',
    description: 'Commonly confused words: cremated and cremates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcremated\b/gi,
    message: 'Check: "cremated" ("cremated") vs "cremates" ("cremates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cremates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cried_dried_1444',
    name: 'cried vs dried',
    description: 'Commonly confused words: cried and dried',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcried\b/gi,
    message: 'Check: "cried" ("cried") vs "dried" ("dried"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dried';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_criers_driers_1445',
    name: 'criers vs driers',
    description: 'Commonly confused words: criers and driers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcriers\b/gi,
    message: 'Check: "criers" ("criers") vs "driers" ("driers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'driers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_criminalized_criminalizes_1446',
    name: 'criminalized vs criminalizes',
    description: 'Commonly confused words: criminalized and criminalizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcriminalized\b/gi,
    message: 'Check: "criminalized" ("criminalized") vs "criminalizes" ("criminalizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'criminalizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cringed_cringes_1447',
    name: 'cringed vs cringes',
    description: 'Commonly confused words: cringed and cringes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcringed\b/gi,
    message: 'Check: "cringed" ("cringed") vs "cringes" ("cringes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cringes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crisscrossed_crisscrosses_1448',
    name: 'crisscrossed vs crisscrosses',
    description: 'Commonly confused words: crisscrossed and crisscrosses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrisscrossed\b/gi,
    message: 'Check: "crisscrossed" ("crisscrossed") vs "crisscrosses" ("crisscrosses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crisscrosses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_critiqued_critiques_1449',
    name: 'critiqued vs critiques',
    description: 'Commonly confused words: critiqued and critiques',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcritiqued\b/gi,
    message: 'Check: "critiqued" ("critiqued") vs "critiques" ("critiques"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'critiques';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crossed_crosses_1450',
    name: 'crossed vs crosses',
    description: 'Commonly confused words: crossed and crosses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrossed\b/gi,
    message: 'Check: "crossed" ("crossed") vs "crosses" ("crosses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crosses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crouched_crouches_1451',
    name: 'crouched vs crouches',
    description: 'Commonly confused words: crouched and crouches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrouched\b/gi,
    message: 'Check: "crouched" ("crouched") vs "crouches" ("crouches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crouches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crowd_crows_1452',
    name: 'crowd vs crows',
    description: 'Commonly confused words: crowd and crows',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrowd\b/gi,
    message: 'Check: "crowd" ("crowd") vs "crows" ("crows"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crows';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crowned_drowned_1453',
    name: 'crowned vs drowned',
    description: 'Commonly confused words: crowned and drowned',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrowned\b/gi,
    message: 'Check: "crowned" ("crowned") vs "drowned" ("drowned"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drowned';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crowning_drowning_1454',
    name: 'crowning vs drowning',
    description: 'Commonly confused words: crowning and drowning',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrowning\b/gi,
    message: 'Check: "crowning" ("crowning") vs "drowning" ("drowning"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drowning';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crucified_crucifies_1455',
    name: 'crucified vs crucifies',
    description: 'Commonly confused words: crucified and crucifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrucified\b/gi,
    message: 'Check: "crucified" ("crucified") vs "crucifies" ("crucifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crucifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crude_cruse_1456',
    name: 'crude vs cruse',
    description: 'Commonly confused words: crude and cruse',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrude\b/gi,
    message: 'Check: "crude" ("crude") vs "cruse" ("cruse"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cruse';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crusaded_crusades_1457',
    name: 'crusaded vs crusades',
    description: 'Commonly confused words: crusaded and crusades',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrusaded\b/gi,
    message: 'Check: "crusaded" ("crusaded") vs "crusades" ("crusades"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crusades';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crying_drying_1458',
    name: 'crying vs drying',
    description: 'Commonly confused words: crying and drying',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrying\b/gi,
    message: 'Check: "crying" ("crying") vs "drying" ("drying"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drying';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_crystallized_crystallizes_1459',
    name: 'crystallized vs crystallizes',
    description: 'Commonly confused words: crystallized and crystallizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcrystallized\b/gi,
    message: 'Check: "crystallized" ("crystallized") vs "crystallizes" ("crystallizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'crystallizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cued_cues_1460',
    name: 'cued vs cues',
    description: 'Commonly confused words: cued and cues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcued\b/gi,
    message: 'Check: "cued" ("cued") vs "cues" ("cues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cued_curd_1461',
    name: 'cued vs curd',
    description: 'Commonly confused words: cued and curd',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcued\b/gi,
    message: 'Check: "cued" ("cued") vs "curd" ("curd"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'curd';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cuffing_duffing_1462',
    name: 'cuffing vs duffing',
    description: 'Commonly confused words: cuffing and duffing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcuffing\b/gi,
    message: 'Check: "cuffing" ("cuffing") vs "duffing" ("duffing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'duffing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cuffs_duffs_1463',
    name: 'cuffs vs duffs',
    description: 'Commonly confused words: cuffs and duffs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcuffs\b/gi,
    message: 'Check: "cuffs" ("cuffs") vs "duffs" ("duffs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'duffs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_culled_dulled_1464',
    name: 'culled vs dulled',
    description: 'Commonly confused words: culled and dulled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bculled\b/gi,
    message: 'Check: "culled" ("culled") vs "dulled" ("dulled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dulled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cultured_cultures_1465',
    name: 'cultured vs cultures',
    description: 'Commonly confused words: cultured and cultures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcultured\b/gi,
    message: 'Check: "cultured" ("cultured") vs "cultures" ("cultures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cultures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cumber_dumber_1466',
    name: 'cumber vs dumber',
    description: 'Commonly confused words: cumber and dumber',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcumber\b/gi,
    message: 'Check: "cumber" ("cumber") vs "dumber" ("dumber"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dumber';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cunning_dunning_1467',
    name: 'cunning vs dunning',
    description: 'Commonly confused words: cunning and dunning',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcunning\b/gi,
    message: 'Check: "cunning" ("cunning") vs "dunning" ("dunning"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dunning';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_curability_durability_1468',
    name: 'curability vs durability',
    description: 'Commonly confused words: curability and durability',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcurability\b/gi,
    message: 'Check: "curability" ("curability") vs "durability" ("durability"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'durability';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_curable_durable_1469',
    name: 'curable vs durable',
    description: 'Commonly confused words: curable and durable',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcurable\b/gi,
    message: 'Check: "curable" ("curable") vs "durable" ("durable"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'durable';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_curare_curate_1470',
    name: 'curare vs curate',
    description: 'Commonly confused words: curare and curate',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcurare\b/gi,
    message: 'Check: "curare" ("curare") vs "curate" ("curate"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'curate';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_curbing_curving_1471',
    name: 'curbing vs curving',
    description: 'Commonly confused words: curbing and curving',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcurbing\b/gi,
    message: 'Check: "curbing" ("curbing") vs "curving" ("curving"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'curving';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cur_cir_1472',
    name: 'cur vs cir',
    description: 'Commonly confused words: cur and cir',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcur\b/gi,
    message: 'Check: "cur" ("cur") vs "cir" ("cir"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cir';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_curd_curs_1473',
    name: 'curd vs curs',
    description: 'Commonly confused words: curd and curs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcurd\b/gi,
    message: 'Check: "curd" ("curd") vs "curs" ("curs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'curs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_curdled_curdles_1474',
    name: 'curdled vs curdles',
    description: 'Commonly confused words: curdled and curdles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcurdled\b/gi,
    message: 'Check: "curdled" ("curdled") vs "curdles" ("curdles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'curdles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_curds_cures_1475',
    name: 'curds vs cures',
    description: 'Commonly confused words: curds and cures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcurds\b/gi,
    message: 'Check: "curds" ("curds") vs "cures" ("cures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cured_cures_1476',
    name: 'cured vs cures',
    description: 'Commonly confused words: cured and cures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcured\b/gi,
    message: 'Check: "cured" ("cured") vs "cures" ("cures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_curried_curries_1477',
    name: 'curried vs curries',
    description: 'Commonly confused words: curried and curries',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcurried\b/gi,
    message: 'Check: "curried" ("curried") vs "curries" ("curries"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'curries';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_curs_cues_1478',
    name: 'curs vs cues',
    description: 'Commonly confused words: curs and cues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcurs\b/gi,
    message: 'Check: "curs" ("curs") vs "cues" ("cues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_curved_curves_1479',
    name: 'curved vs curves',
    description: 'Commonly confused words: curved and curves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcurved\b/gi,
    message: 'Check: "curved" ("curved") vs "curves" ("curves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'curves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_customized_customizes_1480',
    name: 'customized vs customizes',
    description: 'Commonly confused words: customized and customizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcustomized\b/gi,
    message: 'Check: "customized" ("customized") vs "customizes" ("customizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'customizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_cute_cure_1481',
    name: 'cute vs cure',
    description: 'Commonly confused words: cute and cure',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bcute\b/gi,
    message: 'Check: "cute" ("cute") vs "cure" ("cure"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'cure';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
