/**
 * Auto-generated confusion rules (F)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.508Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesF: GrammarRule[] = [
  {
    id: 'confusion_fain_feign_284',
    name: 'fain vs feign',
    description: 'Commonly confused words: fain and feign',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfain\b/gi,
    message: 'Check: "fain" ("fain") vs "feign" ("feign"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'feign';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fair_fare_285',
    name: 'fair vs fare',
    description: 'Commonly confused words: fair and fare',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfair\b/gi,
    message: 'Did you mean "fare"? "fair" (just or carnival) is often confused with "fare" (price of travel).',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fare';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fare_fair_286',
    name: 'fare vs fair',
    description: 'Commonly confused words: fare and fair',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfare\b/gi,
    message: 'Did you mean "fair"? "fare" (price of travel) is often confused with "fair" (just or carnival).',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fair';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fairs_fares_287',
    name: 'fairs vs fares',
    description: 'Commonly confused words: fairs and fares',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfairs\b/gi,
    message: 'Check: "fairs" ("fairs") vs "fares" ("fares"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fares';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fairy_ferry_288',
    name: 'fairy vs ferry',
    description: 'Commonly confused words: fairy and ferry',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfairy\b/gi,
    message: 'Check: "fairy" ("fairy") vs "ferry" ("ferry"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ferry';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_farther_father_289',
    name: 'farther vs father',
    description: 'Commonly confused words: farther and father',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfarther\b/gi,
    message: 'Check: "farther" (physical distance) vs "father" ("father"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'father';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fast_fats_290',
    name: 'fast vs fats',
    description: 'Commonly confused words: fast and fats',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfast\b/gi,
    message: 'Check: "fast" ("fast") vs "fats" ("fats"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fats';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fazed_phased_291',
    name: 'fazed vs phased',
    description: 'Commonly confused words: fazed and phased',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfazed\b/gi,
    message: 'Check: "fazed" ("fazed") vs "phased" ("phased"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'phased';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_feat_feet_292',
    name: 'feat vs feet',
    description: 'Commonly confused words: feat and feet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfeat\b/gi,
    message: 'Check: "feat" ("feat") vs "feet" ("feet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'feet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fee_feel_293',
    name: 'fee vs feel',
    description: 'Commonly confused words: fee and feel',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfee\b/gi,
    message: 'Check: "fee" ("fee") vs "feel" ("feel"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'feel';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_feels_fees_294',
    name: 'feels vs fees',
    description: 'Commonly confused words: feels and fees',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfeels\b/gi,
    message: 'Check: "feels" ("feels") vs "fees" ("fees"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fees';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fee_free_295',
    name: 'fee vs free',
    description: 'Commonly confused words: fee and free',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfee\b/gi,
    message: 'Check: "fee" ("fee") vs "free" ("free"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'free';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_feet_foot_296',
    name: 'feet vs foot',
    description: 'Commonly confused words: feet and foot',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfeet\b/gi,
    message: 'Check: "feet" ("feet") vs "foot" ("foot"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'foot';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_feted_fetid_297',
    name: 'feted vs fetid',
    description: 'Commonly confused words: feted and fetid',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfeted\b/gi,
    message: 'Check: "feted" ("feted") vs "fetid" ("fetid"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fetid';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_field_filled_298',
    name: 'field vs filled',
    description: 'Commonly confused words: field and filled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfield\b/gi,
    message: 'Check: "field" ("field") vs "filled" ("filled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'filled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fill_full_299',
    name: 'fill vs full',
    description: 'Commonly confused words: fill and full',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfill\b/gi,
    message: 'Check: "fill" ("fill") vs "full" ("full"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'full';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_find_fine_300',
    name: 'find vs fine',
    description: 'Commonly confused words: find and fine',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfind\b/gi,
    message: 'Check: "find" ("find") vs "fine" ("fine"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fine';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_finds_fines_301',
    name: 'finds vs fines',
    description: 'Commonly confused words: finds and fines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfinds\b/gi,
    message: 'Check: "finds" ("finds") vs "fines" ("fines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fir_for_302',
    name: 'fir vs for',
    description: 'Commonly confused words: fir and for',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfir\b/gi,
    message: 'Check: "fir" ("fir") vs "for" ("for"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'for';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fir_fur_303',
    name: 'fir vs fur',
    description: 'Commonly confused words: fir and fur',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfir\b/gi,
    message: 'Check: "fir" ("fir") vs "fur" ("fur"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fur';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_firs_furs_304',
    name: 'firs vs furs',
    description: 'Commonly confused words: firs and furs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfirs\b/gi,
    message: 'Check: "firs" ("firs") vs "furs" ("furs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'furs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_first_fist_305',
    name: 'first vs fist',
    description: 'Commonly confused words: first and fist',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfirst\b/gi,
    message: 'Check: "first" ("first") vs "fist" ("fist"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fist';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fiscal_physical_306',
    name: 'fiscal vs physical',
    description: 'Commonly confused words: fiscal and physical',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfiscal\b/gi,
    message: 'Check: "fiscal" ("fiscal") vs "physical" ("physical"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'physical';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_five_give_308',
    name: 'five vs give',
    description: 'Commonly confused words: five and give',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfive\b/gi,
    message: 'Did you mean "give"? "five" ("five") is often confused with "give" ("give").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'give';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fives_gives_309',
    name: 'fives vs gives',
    description: 'Commonly confused words: fives and gives',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfives\b/gi,
    message: 'Check: "fives" ("fives") vs "gives" ("gives"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gives';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flea_flee_310',
    name: 'flea vs flee',
    description: 'Commonly confused words: flea and flee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflea\b/gi,
    message: 'Check: "flea" ("flea") vs "flee" ("flee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fleas_flees_311',
    name: 'fleas vs flees',
    description: 'Commonly confused words: fleas and flees',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfleas\b/gi,
    message: 'Check: "fleas" ("fleas") vs "flees" ("flees"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flees';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flecks_flex_312',
    name: 'flecks vs flex',
    description: 'Commonly confused words: flecks and flex',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflecks\b/gi,
    message: 'Check: "flecks" ("flecks") vs "flex" ("flex"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flex';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flew_flu_313',
    name: 'flew vs flu',
    description: 'Commonly confused words: flew and flu',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflew\b/gi,
    message: 'Check: "flew" ("flew") vs "flu" ("flu"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flu';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flocks_phlox_314',
    name: 'flocks vs phlox',
    description: 'Commonly confused words: flocks and phlox',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflocks\b/gi,
    message: 'Check: "flocks" ("flocks") vs "phlox" ("phlox"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'phlox';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flour_flower_315',
    name: 'flour vs flower',
    description: 'Commonly confused words: flour and flower',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflour\b/gi,
    message: 'Check: "flour" (ground wheat) vs "flower" (plant bloom). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flower';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_foe_for_316',
    name: 'foe vs for',
    description: 'Commonly confused words: foe and for',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfoe\b/gi,
    message: 'Check: "foe" ("foe") vs "for" ("for"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'for';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_foreword_forward_317',
    name: 'foreword vs forward',
    description: 'Commonly confused words: foreword and forward',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bforeword\b/gi,
    message: 'Check: "foreword" ("foreword") vs "forward" ("forward"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'forward';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_form_from_318',
    name: 'form vs from',
    description: 'Commonly confused words: form and from',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bform\b/gi,
    message: 'Did you mean "from"? "form" ("form") is often confused with "from" ("from").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'from';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_from_form_319',
    name: 'from vs form',
    description: 'Commonly confused words: from and form',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfrom\b/gi,
    message: 'Did you mean "form"? "from" ("from") is often confused with "form" ("form").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'form';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_for_four_320',
    name: 'for vs four',
    description: 'Commonly confused words: for and four',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfor\b/gi,
    message: 'Check: "for" ("for") vs "four" ("four"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'four';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_forth_fourth_321',
    name: 'forth vs fourth',
    description: 'Commonly confused words: forth and fourth',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bforth\b/gi,
    message: 'Check: "forth" ("forth") vs "fourth" ("fourth"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fourth';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_foul_fowl_322',
    name: 'foul vs fowl',
    description: 'Commonly confused words: foul and fowl',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfoul\b/gi,
    message: 'Check: "foul" ("foul") vs "fowl" ("fowl"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fowl';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fraud_fraught_323',
    name: 'fraud vs fraught',
    description: 'Commonly confused words: fraud and fraught',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfraud\b/gi,
    message: 'Did you mean "fraught"? "fraud" ("fraud") is often confused with "fraught" ("fraught").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fraught';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fraught_fraud_324',
    name: 'fraught vs fraud',
    description: 'Commonly confused words: fraught and fraud',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfraught\b/gi,
    message: 'Did you mean "fraud"? "fraught" ("fraught") is often confused with "fraud" ("fraud").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fraud';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_frays_phrase_325',
    name: 'frays vs phrase',
    description: 'Commonly confused words: frays and phrase',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfrays\b/gi,
    message: 'Check: "frays" ("frays") vs "phrase" ("phrase"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'phrase';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_freeze_frieze_326',
    name: 'freeze vs frieze',
    description: 'Commonly confused words: freeze and frieze',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfreeze\b/gi,
    message: 'Check: "freeze" ("freeze") vs "frieze" ("frieze"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'frieze';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fermenting_fomenting_763',
    name: 'fermenting vs fomenting',
    description: 'Commonly confused words: fermenting and fomenting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfermenting\b/gi,
    message: 'Check: "fermenting" ("fermenting") vs "fomenting" ("fomenting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fomenting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fix_fox_785',
    name: 'fix vs fox',
    description: 'Commonly confused words: fix and fox',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfix\b/gi,
    message: 'Check: "fix" ("fix") vs "fox" ("fox"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fox';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fabled_gabled_1898',
    name: 'fabled vs gabled',
    description: 'Commonly confused words: fabled and gabled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfabled\b/gi,
    message: 'Check: "fabled" ("fabled") vs "gabled" ("gabled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gabled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fable_gable_1899',
    name: 'fable vs gable',
    description: 'Commonly confused words: fable and gable',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfable\b/gi,
    message: 'Check: "fable" ("fable") vs "gable" ("gable"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gable';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_faced_faded_1900',
    name: 'faced vs faded',
    description: 'Commonly confused words: faced and faded',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfaced\b/gi,
    message: 'Check: "faced" ("faced") vs "faded" ("faded"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'faded';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_facially_racially_1901',
    name: 'facially vs racially',
    description: 'Commonly confused words: facially and racially',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfacially\b/gi,
    message: 'Check: "facially" ("facially") vs "racially" ("racially"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'racially';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_facial_racial_1902',
    name: 'facial vs racial',
    description: 'Commonly confused words: facial and racial',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfacial\b/gi,
    message: 'Check: "facial" ("facial") vs "racial" ("racial"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'racial';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_facilitated_facilitates_1903',
    name: 'facilitated vs facilitates',
    description: 'Commonly confused words: facilitated and facilitates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfacilitated\b/gi,
    message: 'Check: "facilitated" ("facilitated") vs "facilitates" ("facilitates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'facilitates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_factorized_factorizes_1904',
    name: 'factorized vs factorizes',
    description: 'Commonly confused words: factorized and factorizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfactorized\b/gi,
    message: 'Check: "factorized" ("factorized") vs "factorizes" ("factorizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'factorizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fagging_gagging_1905',
    name: 'fagging vs gagging',
    description: 'Commonly confused words: fagging and gagging',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfagging\b/gi,
    message: 'Check: "fagging" ("fagging") vs "gagging" ("gagging"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gagging';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_failings_railings_1906',
    name: 'failings vs railings',
    description: 'Commonly confused words: failings and railings',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfailings\b/gi,
    message: 'Check: "failings" ("failings") vs "railings" ("railings"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'railings';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_falsified_falsifies_1907',
    name: 'falsified vs falsifies',
    description: 'Commonly confused words: falsified and falsifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfalsified\b/gi,
    message: 'Check: "falsified" ("falsified") vs "falsifies" ("falsifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'falsifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fame_dame_1908',
    name: 'fame vs dame',
    description: 'Commonly confused words: fame and dame',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfame\b/gi,
    message: 'Check: "fame" ("fame") vs "dame" ("dame"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dame';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_familiarized_familiarizes_1909',
    name: 'familiarized vs familiarizes',
    description: 'Commonly confused words: familiarized and familiarizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfamiliarized\b/gi,
    message: 'Check: "familiarized" ("familiarized") vs "familiarizes" ("familiarizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'familiarizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fanged_ganged_1910',
    name: 'fanged vs ganged',
    description: 'Commonly confused words: fanged and ganged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfanged\b/gi,
    message: 'Check: "fanged" ("fanged") vs "ganged" ("ganged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ganged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fanged_ranged_1911',
    name: 'fanged vs ranged',
    description: 'Commonly confused words: fanged and ranged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfanged\b/gi,
    message: 'Check: "fanged" ("fanged") vs "ranged" ("ranged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ranged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fare_dare_1912',
    name: 'fare vs dare',
    description: 'Commonly confused words: fare and dare',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfare\b/gi,
    message: 'Check: "fare" (price of travel) vs "dare" ("dare"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dare';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fared_dared_1913',
    name: 'fared vs dared',
    description: 'Commonly confused words: fared and dared',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfared\b/gi,
    message: 'Check: "fared" ("fared") vs "dared" ("dared"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dared';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fares_dares_1914',
    name: 'fares vs dares',
    description: 'Commonly confused words: fares and dares',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfares\b/gi,
    message: 'Check: "fares" ("fares") vs "dares" ("dares"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dares';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fares_fared_1915',
    name: 'fares vs fared',
    description: 'Commonly confused words: fares and fared',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfares\b/gi,
    message: 'Check: "fares" ("fares") vs "fared" ("fared"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fared';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_faring_daring_1916',
    name: 'faring vs daring',
    description: 'Commonly confused words: faring and daring',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfaring\b/gi,
    message: 'Check: "faring" ("faring") vs "daring" ("daring"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'daring';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fascinated_fascinates_1917',
    name: 'fascinated vs fascinates',
    description: 'Commonly confused words: fascinated and fascinates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfascinated\b/gi,
    message: 'Check: "fascinated" ("fascinated") vs "fascinates" ("fascinates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fascinates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fastness_vastness_1918',
    name: 'fastness vs vastness',
    description: 'Commonly confused words: fastness and vastness',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfastness\b/gi,
    message: 'Check: "fastness" ("fastness") vs "vastness" ("vastness"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vastness';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fated_fared_1919',
    name: 'fated vs fared',
    description: 'Commonly confused words: fated and fared',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfated\b/gi,
    message: 'Check: "fated" ("fated") vs "fared" ("fared"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fared';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fatigued_fatigues_1920',
    name: 'fatigued vs fatigues',
    description: 'Commonly confused words: fatigued and fatigues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfatigued\b/gi,
    message: 'Check: "fatigued" ("fatigued") vs "fatigues" ("fatigues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fatigues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fatty_ratty_1921',
    name: 'fatty vs ratty',
    description: 'Commonly confused words: fatty and ratty',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfatty\b/gi,
    message: 'Check: "fatty" ("fatty") vs "ratty" ("ratty"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ratty';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_faulted_vaulted_1922',
    name: 'faulted vs vaulted',
    description: 'Commonly confused words: faulted and vaulted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfaulted\b/gi,
    message: 'Check: "faulted" ("faulted") vs "vaulted" ("vaulted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vaulted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_faulting_vaulting_1923',
    name: 'faulting vs vaulting',
    description: 'Commonly confused words: faulting and vaulting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfaulting\b/gi,
    message: 'Check: "faulting" ("faulting") vs "vaulting" ("vaulting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vaulting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_faults_vaults_1924',
    name: 'faults vs vaults',
    description: 'Commonly confused words: faults and vaults',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfaults\b/gi,
    message: 'Check: "faults" ("faults") vs "vaults" ("vaults"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vaults';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fault_vault_1925',
    name: 'fault vs vault',
    description: 'Commonly confused words: fault and vault',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfault\b/gi,
    message: 'Check: "fault" ("fault") vs "vault" ("vault"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vault';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_faxed_faxes_1926',
    name: 'faxed vs faxes',
    description: 'Commonly confused words: faxed and faxes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfaxed\b/gi,
    message: 'Check: "faxed" ("faxed") vs "faxes" ("faxes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'faxes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_faxed_fazed_1927',
    name: 'faxed vs fazed',
    description: 'Commonly confused words: faxed and fazed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfaxed\b/gi,
    message: 'Check: "faxed" ("faxed") vs "fazed" ("fazed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fazed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fealty_realty_1928',
    name: 'fealty vs realty',
    description: 'Commonly confused words: fealty and realty',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfealty\b/gi,
    message: 'Check: "fealty" ("fealty") vs "realty" ("realty"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'realty';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fear_dear_1929',
    name: 'fear vs dear',
    description: 'Commonly confused words: fear and dear',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfear\b/gi,
    message: 'Check: "fear" ("fear") vs "dear" ("dear"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dear';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_feared_geared_1930',
    name: 'feared vs geared',
    description: 'Commonly confused words: feared and geared',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfeared\b/gi,
    message: 'Check: "feared" ("feared") vs "geared" ("geared"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'geared';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_feared_reared_1931',
    name: 'feared vs reared',
    description: 'Commonly confused words: feared and reared',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfeared\b/gi,
    message: 'Check: "feared" ("feared") vs "reared" ("reared"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reared';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fear_feat_1932',
    name: 'fear vs feat',
    description: 'Commonly confused words: fear and feat',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfear\b/gi,
    message: 'Check: "fear" ("fear") vs "feat" ("feat"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'feat';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fearing_gearing_1933',
    name: 'fearing vs gearing',
    description: 'Commonly confused words: fearing and gearing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfearing\b/gi,
    message: 'Check: "fearing" ("fearing") vs "gearing" ("gearing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gearing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fearing_rearing_1934',
    name: 'fearing vs rearing',
    description: 'Commonly confused words: fearing and rearing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfearing\b/gi,
    message: 'Check: "fearing" ("fearing") vs "rearing" ("rearing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rearing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fecklessness_recklessness_1935',
    name: 'fecklessness vs recklessness',
    description: 'Commonly confused words: fecklessness and recklessness',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfecklessness\b/gi,
    message: 'Check: "fecklessness" ("fecklessness") vs "recklessness" ("recklessness"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'recklessness';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_federated_federates_1936',
    name: 'federated vs federates',
    description: 'Commonly confused words: federated and federates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfederated\b/gi,
    message: 'Check: "federated" ("federated") vs "federates" ("federates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'federates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_feed_deed_1937',
    name: 'feed vs deed',
    description: 'Commonly confused words: feed and deed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfeed\b/gi,
    message: 'Check: "feed" ("feed") vs "deed" ("deed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'deed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fee_fed_1938',
    name: 'fee vs fed',
    description: 'Commonly confused words: fee and fed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfee\b/gi,
    message: 'Check: "fee" ("fee") vs "fed" ("fed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fee_fer_1939',
    name: 'fee vs fer',
    description: 'Commonly confused words: fee and fer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfee\b/gi,
    message: 'Check: "fee" ("fee") vs "fer" ("fer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_feels_reels_1940',
    name: 'feels vs reels',
    description: 'Commonly confused words: feels and reels',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfeels\b/gi,
    message: 'Check: "feels" ("feels") vs "reels" ("reels"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reels';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fees_feed_1941',
    name: 'fees vs feed',
    description: 'Commonly confused words: fees and feed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfees\b/gi,
    message: 'Check: "fees" ("fees") vs "feed" ("feed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'feed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_feigned_reigned_1942',
    name: 'feigned vs reigned',
    description: 'Commonly confused words: feigned and reigned',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfeigned\b/gi,
    message: 'Check: "feigned" ("feigned") vs "reigned" ("reigned"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reigned';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_feigning_reigning_1943',
    name: 'feigning vs reigning',
    description: 'Commonly confused words: feigning and reigning',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfeigning\b/gi,
    message: 'Check: "feigning" ("feigning") vs "reigning" ("reigning"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reigning';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_feign_reign_1944',
    name: 'feign vs reign',
    description: 'Commonly confused words: feign and reign',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfeign\b/gi,
    message: 'Check: "feign" ("feign") vs "reign" (to rule). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reign';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_feigns_reigns_1945',
    name: 'feigns vs reigns',
    description: 'Commonly confused words: feigns and reigns',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfeigns\b/gi,
    message: 'Check: "feigns" ("feigns") vs "reigns" ("reigns"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reigns';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_feline_reline_1946',
    name: 'feline vs reline',
    description: 'Commonly confused words: feline and reline',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfeline\b/gi,
    message: 'Check: "feline" ("feline") vs "reline" ("reline"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'reline';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fell_dell_1947',
    name: 'fell vs dell',
    description: 'Commonly confused words: fell and dell',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfell\b/gi,
    message: 'Check: "fell" ("fell") vs "dell" ("dell"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dell';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fells_dells_1948',
    name: 'fells vs dells',
    description: 'Commonly confused words: fells and dells',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfells\b/gi,
    message: 'Check: "fells" ("fells") vs "dells" ("dells"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dells';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fells_fella_1949',
    name: 'fells vs fella',
    description: 'Commonly confused words: fells and fella',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfells\b/gi,
    message: 'Check: "fells" ("fells") vs "fella" ("fella"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fella';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fenced_fences_1950',
    name: 'fenced vs fences',
    description: 'Commonly confused words: fenced and fences',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfenced\b/gi,
    message: 'Check: "fenced" ("fenced") vs "fences" ("fences"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fences';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fenced_fended_1951',
    name: 'fenced vs fended',
    description: 'Commonly confused words: fenced and fended',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfenced\b/gi,
    message: 'Check: "fenced" ("fenced") vs "fended" ("fended"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fended';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fencing_fending_1952',
    name: 'fencing vs fending',
    description: 'Commonly confused words: fencing and fending',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfencing\b/gi,
    message: 'Check: "fencing" ("fencing") vs "fending" ("fending"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fending';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fenders_genders_1953',
    name: 'fenders vs genders',
    description: 'Commonly confused words: fenders and genders',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfenders\b/gi,
    message: 'Check: "fenders" ("fenders") vs "genders" ("genders"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'genders';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fenders_renders_1954',
    name: 'fenders vs renders',
    description: 'Commonly confused words: fenders and renders',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfenders\b/gi,
    message: 'Check: "fenders" ("fenders") vs "renders" ("renders"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'renders';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fend_fens_1955',
    name: 'fend vs fens',
    description: 'Commonly confused words: fend and fens',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfend\b/gi,
    message: 'Check: "fend" ("fend") vs "fens" ("fens"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fens';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fens_dens_1956',
    name: 'fens vs dens',
    description: 'Commonly confused words: fens and dens',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfens\b/gi,
    message: 'Check: "fens" ("fens") vs "dens" ("dens"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dens';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_ferried_ferries_1957',
    name: 'ferried vs ferries',
    description: 'Commonly confused words: ferried and ferries',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bferried\b/gi,
    message: 'Check: "ferried" ("ferried") vs "ferries" ("ferries"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ferries';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fertilized_fertilizes_1958',
    name: 'fertilized vs fertilizes',
    description: 'Commonly confused words: fertilized and fertilizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfertilized\b/gi,
    message: 'Check: "fertilized" ("fertilized") vs "fertilizes" ("fertilizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fertilizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_festive_restive_1959',
    name: 'festive vs restive',
    description: 'Commonly confused words: festive and restive',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfestive\b/gi,
    message: 'Check: "festive" ("festive") vs "restive" ("restive"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'restive';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fetches_vetches_1960',
    name: 'fetches vs vetches',
    description: 'Commonly confused words: fetches and vetches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfetches\b/gi,
    message: 'Check: "fetches" ("fetches") vs "vetches" ("vetches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vetches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fetching_retching_1961',
    name: 'fetching vs retching',
    description: 'Commonly confused words: fetching and retching',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfetching\b/gi,
    message: 'Check: "fetching" ("fetching") vs "retching" ("retching"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'retching';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fetch_retch_1962',
    name: 'fetch vs retch',
    description: 'Commonly confused words: fetch and retch',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfetch\b/gi,
    message: 'Check: "fetch" ("fetch") vs "retch" ("retch"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'retch';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fetch_vetch_1963',
    name: 'fetch vs vetch',
    description: 'Commonly confused words: fetch and vetch',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfetch\b/gi,
    message: 'Check: "fetch" ("fetch") vs "vetch" ("vetch"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vetch';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_feted_fetes_1964',
    name: 'feted vs fetes',
    description: 'Commonly confused words: feted and fetes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfeted\b/gi,
    message: 'Check: "feted" ("feted") vs "fetes" ("fetes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fetes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fevered_revered_1965',
    name: 'fevered vs revered',
    description: 'Commonly confused words: fevered and revered',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfevered\b/gi,
    message: 'Check: "fevered" ("fevered") vs "revered" ("revered"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'revered';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fevers_revers_1966',
    name: 'fevers vs revers',
    description: 'Commonly confused words: fevers and revers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfevers\b/gi,
    message: 'Check: "fevers" ("fevers") vs "revers" ("revers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'revers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_few_dew_1967',
    name: 'few vs dew',
    description: 'Commonly confused words: few and dew',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfew\b/gi,
    message: 'Check: "few" ("few") vs "dew" ("dew"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dew';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fiats_fists_1968',
    name: 'fiats vs fists',
    description: 'Commonly confused words: fiats and fists',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfiats\b/gi,
    message: 'Check: "fiats" ("fiats") vs "fists" ("fists"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fists';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fibbing_ribbing_1969',
    name: 'fibbing vs ribbing',
    description: 'Commonly confused words: fibbing and ribbing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfibbing\b/gi,
    message: 'Check: "fibbing" ("fibbing") vs "ribbing" ("ribbing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ribbing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fiber_finer_1970',
    name: 'fiber vs finer',
    description: 'Commonly confused words: fiber and finer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfiber\b/gi,
    message: 'Check: "fiber" ("fiber") vs "finer" ("finer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'finer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fibers_fivers_1971',
    name: 'fibers vs fivers',
    description: 'Commonly confused words: fibers and fivers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfibers\b/gi,
    message: 'Check: "fibers" ("fibers") vs "fivers" ("fivers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fivers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fibs_fins_1972',
    name: 'fibs vs fins',
    description: 'Commonly confused words: fibs and fins',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfibs\b/gi,
    message: 'Check: "fibs" ("fibs") vs "fins" ("fins"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fins';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fibulae_fibular_1973',
    name: 'fibulae vs fibular',
    description: 'Commonly confused words: fibulae and fibular',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfibulae\b/gi,
    message: 'Check: "fibulae" ("fibulae") vs "fibular" ("fibular"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fibular';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fictionalized_fictionalizes_1974',
    name: 'fictionalized vs fictionalizes',
    description: 'Commonly confused words: fictionalized and fictionalizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfictionalized\b/gi,
    message: 'Check: "fictionalized" ("fictionalized") vs "fictionalizes" ("fictionalizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fictionalizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fiddled_fiddles_1975',
    name: 'fiddled vs fiddles',
    description: 'Commonly confused words: fiddled and fiddles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfiddled\b/gi,
    message: 'Check: "fiddled" ("fiddled") vs "fiddles" ("fiddles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fiddles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fiddled_riddled_1976',
    name: 'fiddled vs riddled',
    description: 'Commonly confused words: fiddled and riddled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfiddled\b/gi,
    message: 'Check: "fiddled" ("fiddled") vs "riddled" ("riddled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'riddled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fiddling_riddling_1977',
    name: 'fiddling vs riddling',
    description: 'Commonly confused words: fiddling and riddling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfiddling\b/gi,
    message: 'Check: "fiddling" ("fiddling") vs "riddling" ("riddling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'riddling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fifers_firers_1978',
    name: 'fifers vs firers',
    description: 'Commonly confused words: fifers and firers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfifers\b/gi,
    message: 'Check: "fifers" ("fifers") vs "firers" ("firers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'firers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fifth_fifty_1979',
    name: 'fifth vs fifty',
    description: 'Commonly confused words: fifth and fifty',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfifth\b/gi,
    message: 'Check: "fifth" ("fifth") vs "fifty" ("fifty"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fifty';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fifths_firths_1980',
    name: 'fifths vs firths',
    description: 'Commonly confused words: fifths and firths',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfifths\b/gi,
    message: 'Check: "fifths" ("fifths") vs "firths" ("firths"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'firths';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_figment_fitment_1981',
    name: 'figment vs fitment',
    description: 'Commonly confused words: figment and fitment',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfigment\b/gi,
    message: 'Check: "figment" ("figment") vs "fitment" ("fitment"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fitment';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_figments_fitments_1982',
    name: 'figments vs fitments',
    description: 'Commonly confused words: figments and fitments',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfigments\b/gi,
    message: 'Check: "figments" ("figments") vs "fitments" ("fitments"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fitments';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_figs_fits_1983',
    name: 'figs vs fits',
    description: 'Commonly confused words: figs and fits',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfigs\b/gi,
    message: 'Check: "figs" ("figs") vs "fits" ("fits"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fits';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_figs_fogs_1984',
    name: 'figs vs fogs',
    description: 'Commonly confused words: figs and fogs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfigs\b/gi,
    message: 'Check: "figs" ("figs") vs "fogs" ("fogs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fogs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_figured_figures_1985',
    name: 'figured vs figures',
    description: 'Commonly confused words: figured and figures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfigured\b/gi,
    message: 'Check: "figured" ("figured") vs "figures" ("figures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'figures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_filed_files_1986',
    name: 'filed vs files',
    description: 'Commonly confused words: filed and files',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfiled\b/gi,
    message: 'Check: "filed" ("filed") vs "files" ("files"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'files';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fill_dill_1987',
    name: 'fill vs dill',
    description: 'Commonly confused words: fill and dill',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfill\b/gi,
    message: 'Check: "fill" ("fill") vs "dill" ("dill"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dill';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fills_dills_1988',
    name: 'fills vs dills',
    description: 'Commonly confused words: fills and dills',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfills\b/gi,
    message: 'Check: "fills" ("fills") vs "dills" ("dills"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dills';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_finalized_finalizes_1989',
    name: 'finalized vs finalizes',
    description: 'Commonly confused words: finalized and finalizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfinalized\b/gi,
    message: 'Check: "finalized" ("finalized") vs "finalizes" ("finalizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'finalizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_financed_finances_1990',
    name: 'financed vs finances',
    description: 'Commonly confused words: financed and finances',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfinanced\b/gi,
    message: 'Check: "financed" ("financed") vs "finances" ("finances"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'finances';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_find_fins_1991',
    name: 'find vs fins',
    description: 'Commonly confused words: find and fins',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfind\b/gi,
    message: 'Check: "find" ("find") vs "fins" ("fins"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fins';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fin_din_1992',
    name: 'fin vs din',
    description: 'Commonly confused words: fin and din',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfin\b/gi,
    message: 'Check: "fin" ("fin") vs "din" ("din"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'din';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_finding_funding_1993',
    name: 'finding vs funding',
    description: 'Commonly confused words: finding and funding',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfinding\b/gi,
    message: 'Check: "finding" ("finding") vs "funding" ("funding"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'funding';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fined_fines_1994',
    name: 'fined vs fines',
    description: 'Commonly confused words: fined and fines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfined\b/gi,
    message: 'Check: "fined" ("fined") vs "fines" ("fines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_finer_diner_1995',
    name: 'finer vs diner',
    description: 'Commonly confused words: finer and diner',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfiner\b/gi,
    message: 'Check: "finer" ("finer") vs "diner" ("diner"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'diner';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fines_dines_1996',
    name: 'fines vs dines',
    description: 'Commonly confused words: fines and dines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfines\b/gi,
    message: 'Check: "fines" ("fines") vs "dines" ("dines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_finessed_finesses_1997',
    name: 'finessed vs finesses',
    description: 'Commonly confused words: finessed and finesses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfinessed\b/gi,
    message: 'Check: "finessed" ("finessed") vs "finesses" ("finesses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'finesses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_finger_ginger_1998',
    name: 'finger vs ginger',
    description: 'Commonly confused words: finger and ginger',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfinger\b/gi,
    message: 'Check: "finger" ("finger") vs "ginger" ("ginger"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ginger';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_finned_ginned_1999',
    name: 'finned vs ginned',
    description: 'Commonly confused words: finned and ginned',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfinned\b/gi,
    message: 'Check: "finned" ("finned") vs "ginned" ("ginned"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ginned';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_finny_funny_2000',
    name: 'finny vs funny',
    description: 'Commonly confused words: finny and funny',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfinny\b/gi,
    message: 'Check: "finny" ("finny") vs "funny" ("funny"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'funny';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fins_dins_2001',
    name: 'fins vs dins',
    description: 'Commonly confused words: fins and dins',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfins\b/gi,
    message: 'Check: "fins" ("fins") vs "dins" ("dins"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dins';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_firm_form_2002',
    name: 'firm vs form',
    description: 'Commonly confused words: firm and form',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfirm\b/gi,
    message: 'Check: "firm" ("firm") vs "form" ("form"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'form';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_firms_forms_2003',
    name: 'firms vs forms',
    description: 'Commonly confused words: firms and forms',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfirms\b/gi,
    message: 'Check: "firms" ("firms") vs "forms" ("forms"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'forms';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_firth_forth_2004',
    name: 'firth vs forth',
    description: 'Commonly confused words: firth and forth',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfirth\b/gi,
    message: 'Check: "firth" ("firth") vs "forth" ("forth"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'forth';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fished_fishes_2005',
    name: 'fished vs fishes',
    description: 'Commonly confused words: fished and fishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfished\b/gi,
    message: 'Check: "fished" ("fished") vs "fishes" ("fishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fits_firs_2006',
    name: 'fits vs firs',
    description: 'Commonly confused words: fits and firs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfits\b/gi,
    message: 'Check: "fits" ("fits") vs "firs" ("firs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'firs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fixated_fixates_2007',
    name: 'fixated vs fixates',
    description: 'Commonly confused words: fixated and fixates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfixated\b/gi,
    message: 'Check: "fixated" ("fixated") vs "fixates" ("fixates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fixates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flag_flat_2008',
    name: 'flag vs flat',
    description: 'Commonly confused words: flag and flat',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflag\b/gi,
    message: 'Check: "flag" ("flag") vs "flat" ("flat"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flat';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flags_flats_2009',
    name: 'flags vs flats',
    description: 'Commonly confused words: flags and flats',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflags\b/gi,
    message: 'Check: "flags" ("flags") vs "flats" ("flats"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flats';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flaked_flakes_2010',
    name: 'flaked vs flakes',
    description: 'Commonly confused words: flaked and flakes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflaked\b/gi,
    message: 'Check: "flaked" ("flaked") vs "flakes" ("flakes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flakes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flaming_foaming_2011',
    name: 'flaming vs foaming',
    description: 'Commonly confused words: flaming and foaming',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflaming\b/gi,
    message: 'Check: "flaming" ("flaming") vs "foaming" ("foaming"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'foaming';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flared_glared_2012',
    name: 'flared vs glared',
    description: 'Commonly confused words: flared and glared',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflared\b/gi,
    message: 'Check: "flared" ("flared") vs "glared" ("glared"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glared';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flares_glares_2013',
    name: 'flares vs glares',
    description: 'Commonly confused words: flares and glares',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflares\b/gi,
    message: 'Check: "flares" ("flares") vs "glares" ("glares"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glares';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flaring_glaring_2014',
    name: 'flaring vs glaring',
    description: 'Commonly confused words: flaring and glaring',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflaring\b/gi,
    message: 'Check: "flaring" ("flaring") vs "glaring" ("glaring"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glaring';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flashed_flashes_2015',
    name: 'flashed vs flashes',
    description: 'Commonly confused words: flashed and flashes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflashed\b/gi,
    message: 'Check: "flashed" ("flashed") vs "flashes" ("flashes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flashes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flays_flats_2016',
    name: 'flays vs flats',
    description: 'Commonly confused words: flays and flats',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflays\b/gi,
    message: 'Check: "flays" ("flays") vs "flats" ("flats"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flats';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fleeced_fleeces_2017',
    name: 'fleeced vs fleeces',
    description: 'Commonly confused words: fleeced and fleeces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfleeced\b/gi,
    message: 'Check: "fleeced" ("fleeced") vs "fleeces" ("fleeces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fleeces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flee_flew_2018',
    name: 'flee vs flew',
    description: 'Commonly confused words: flee and flew',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflee\b/gi,
    message: 'Check: "flee" ("flee") vs "flew" ("flew"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flew';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fleshed_fleshes_2019',
    name: 'fleshed vs fleshes',
    description: 'Commonly confused words: fleshed and fleshes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfleshed\b/gi,
    message: 'Check: "fleshed" ("fleshed") vs "fleshes" ("fleshes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fleshes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flexes_flexed_2020',
    name: 'flexes vs flexed',
    description: 'Commonly confused words: flexes and flexed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflexes\b/gi,
    message: 'Check: "flexes" ("flexes") vs "flexed" ("flexed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flexed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flicked_flocked_2021',
    name: 'flicked vs flocked',
    description: 'Commonly confused words: flicked and flocked',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflicked\b/gi,
    message: 'Check: "flicked" ("flicked") vs "flocked" ("flocked"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flocked';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flicking_flocking_2022',
    name: 'flicking vs flocking',
    description: 'Commonly confused words: flicking and flocking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflicking\b/gi,
    message: 'Check: "flicking" ("flicking") vs "flocking" ("flocking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flocking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flies_floes_2023',
    name: 'flies vs floes',
    description: 'Commonly confused words: flies and floes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflies\b/gi,
    message: 'Check: "flies" ("flies") vs "floes" ("floes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'floes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flies_flues_2024',
    name: 'flies vs flues',
    description: 'Commonly confused words: flies and flues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflies\b/gi,
    message: 'Check: "flies" ("flies") vs "flues" ("flues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fling_flung_2025',
    name: 'fling vs flung',
    description: 'Commonly confused words: fling and flung',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfling\b/gi,
    message: 'Check: "fling" ("fling") vs "flung" ("flung"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flung';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flings_flints_2026',
    name: 'flings vs flints',
    description: 'Commonly confused words: flings and flints',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflings\b/gi,
    message: 'Check: "flings" ("flings") vs "flints" ("flints"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flints';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flip_flop_2027',
    name: 'flip vs flop',
    description: 'Commonly confused words: flip and flop',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflip\b/gi,
    message: 'Check: "flip" ("flip") vs "flop" ("flop"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flop';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flipping_flopping_2028',
    name: 'flipping vs flopping',
    description: 'Commonly confused words: flipping and flopping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflipping\b/gi,
    message: 'Check: "flipping" ("flipping") vs "flopping" ("flopping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flopping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flips_flops_2029',
    name: 'flips vs flops',
    description: 'Commonly confused words: flips and flops',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflips\b/gi,
    message: 'Check: "flips" ("flips") vs "flops" ("flops"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flops';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flirted_flitted_2030',
    name: 'flirted vs flitted',
    description: 'Commonly confused words: flirted and flitted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflirted\b/gi,
    message: 'Check: "flirted" ("flirted") vs "flitted" ("flitted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flitted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flirting_flitting_2031',
    name: 'flirting vs flitting',
    description: 'Commonly confused words: flirting and flitting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflirting\b/gi,
    message: 'Check: "flirting" ("flirting") vs "flitting" ("flitting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flitting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_floated_gloated_2032',
    name: 'floated vs gloated',
    description: 'Commonly confused words: floated and gloated',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfloated\b/gi,
    message: 'Check: "floated" ("floated") vs "gloated" ("gloated"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gloated';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_floats_gloats_2033',
    name: 'floats vs gloats',
    description: 'Commonly confused words: floats and gloats',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfloats\b/gi,
    message: 'Check: "floats" ("floats") vs "gloats" ("gloats"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gloats';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_floss_flows_2034',
    name: 'floss vs flows',
    description: 'Commonly confused words: floss and flows',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfloss\b/gi,
    message: 'Check: "floss" ("floss") vs "flows" ("flows"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flows';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_floss_gloss_2035',
    name: 'floss vs gloss',
    description: 'Commonly confused words: floss and gloss',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfloss\b/gi,
    message: 'Check: "floss" ("floss") vs "gloss" ("gloss"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gloss';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flossing_glossing_2036',
    name: 'flossing vs glossing',
    description: 'Commonly confused words: flossing and glossing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflossing\b/gi,
    message: 'Check: "flossing" ("flossing") vs "glossing" ("glossing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glossing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flossy_glossy_2037',
    name: 'flossy vs glossy',
    description: 'Commonly confused words: flossy and glossy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflossy\b/gi,
    message: 'Check: "flossy" ("flossy") vs "glossy" ("glossy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glossy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_floured_flouted_2038',
    name: 'floured vs flouted',
    description: 'Commonly confused words: floured and flouted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfloured\b/gi,
    message: 'Check: "floured" ("floured") vs "flouted" ("flouted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flouted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flour_flout_2039',
    name: 'flour vs flout',
    description: 'Commonly confused words: flour and flout',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflour\b/gi,
    message: 'Check: "flour" (ground wheat) vs "flout" ("flout"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flout';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flouring_flouting_2040',
    name: 'flouring vs flouting',
    description: 'Commonly confused words: flouring and flouting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflouring\b/gi,
    message: 'Check: "flouring" ("flouring") vs "flouting" ("flouting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flouting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flours_flouts_2041',
    name: 'flours vs flouts',
    description: 'Commonly confused words: flours and flouts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflours\b/gi,
    message: 'Check: "flours" ("flours") vs "flouts" ("flouts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flouts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flowed_glowed_2042',
    name: 'flowed vs glowed',
    description: 'Commonly confused words: flowed and glowed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflowed\b/gi,
    message: 'Check: "flowed" ("flowed") vs "glowed" ("glowed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glowed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flowering_glowering_2043',
    name: 'flowering vs glowering',
    description: 'Commonly confused words: flowering and glowering',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflowering\b/gi,
    message: 'Check: "flowering" ("flowering") vs "glowering" ("glowering"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glowering';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flowing_glowing_2044',
    name: 'flowing vs glowing',
    description: 'Commonly confused words: flowing and glowing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflowing\b/gi,
    message: 'Check: "flowing" ("flowing") vs "glowing" ("glowing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'glowing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fluctuated_fluctuates_2045',
    name: 'fluctuated vs fluctuates',
    description: 'Commonly confused words: fluctuated and fluctuates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfluctuated\b/gi,
    message: 'Check: "fluctuated" ("fluctuated") vs "fluctuates" ("fluctuates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fluctuates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_flushed_flushes_2046',
    name: 'flushed vs flushes',
    description: 'Commonly confused words: flushed and flushes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bflushed\b/gi,
    message: 'Check: "flushed" ("flushed") vs "flushes" ("flushes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flushes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fluted_flutes_2047',
    name: 'fluted vs flutes',
    description: 'Commonly confused words: fluted and flutes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfluted\b/gi,
    message: 'Check: "fluted" ("fluted") vs "flutes" ("flutes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'flutes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fluxes_fluxed_2048',
    name: 'fluxes vs fluxed',
    description: 'Commonly confused words: fluxes and fluxed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfluxes\b/gi,
    message: 'Check: "fluxes" ("fluxes") vs "fluxed" ("fluxed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fluxed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_foamed_roamed_2049',
    name: 'foamed vs roamed',
    description: 'Commonly confused words: foamed and roamed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfoamed\b/gi,
    message: 'Check: "foamed" ("foamed") vs "roamed" ("roamed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'roamed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_foaming_roaming_2050',
    name: 'foaming vs roaming',
    description: 'Commonly confused words: foaming and roaming',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfoaming\b/gi,
    message: 'Check: "foaming" ("foaming") vs "roaming" ("roaming"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'roaming';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_foam_roam_2051',
    name: 'foam vs roam',
    description: 'Commonly confused words: foam and roam',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfoam\b/gi,
    message: 'Check: "foam" ("foam") vs "roam" ("roam"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'roam';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_foams_roams_2052',
    name: 'foams vs roams',
    description: 'Commonly confused words: foams and roams',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfoams\b/gi,
    message: 'Check: "foams" ("foams") vs "roams" ("roams"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'roams';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fobbed_robbed_2053',
    name: 'fobbed vs robbed',
    description: 'Commonly confused words: fobbed and robbed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfobbed\b/gi,
    message: 'Check: "fobbed" ("fobbed") vs "robbed" ("robbed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'robbed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fobbing_robbing_2054',
    name: 'fobbing vs robbing',
    description: 'Commonly confused words: fobbing and robbing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfobbing\b/gi,
    message: 'Check: "fobbing" ("fobbing") vs "robbing" ("robbing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'robbing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_focally_vocally_2055',
    name: 'focally vs vocally',
    description: 'Commonly confused words: focally and vocally',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfocally\b/gi,
    message: 'Check: "focally" ("focally") vs "vocally" ("vocally"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vocally';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_focal_vocal_2056',
    name: 'focal vs vocal',
    description: 'Commonly confused words: focal and vocal',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfocal\b/gi,
    message: 'Check: "focal" ("focal") vs "vocal" ("vocal"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'vocal';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_focused_focuses_2057',
    name: 'focused vs focuses',
    description: 'Commonly confused words: focused and focuses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfocused\b/gi,
    message: 'Check: "focused" ("focused") vs "focuses" ("focuses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'focuses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_foiled_fouled_2058',
    name: 'foiled vs fouled',
    description: 'Commonly confused words: foiled and fouled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfoiled\b/gi,
    message: 'Check: "foiled" ("foiled") vs "fouled" ("fouled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fouled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_foiled_roiled_2059',
    name: 'foiled vs roiled',
    description: 'Commonly confused words: foiled and roiled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfoiled\b/gi,
    message: 'Check: "foiled" ("foiled") vs "roiled" ("roiled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'roiled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_foil_fool_2060',
    name: 'foil vs fool',
    description: 'Commonly confused words: foil and fool',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfoil\b/gi,
    message: 'Check: "foil" ("foil") vs "fool" ("fool"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fool';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_foil_foul_2061',
    name: 'foil vs foul',
    description: 'Commonly confused words: foil and foul',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfoil\b/gi,
    message: 'Check: "foil" ("foil") vs "foul" ("foul"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'foul';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_foiling_fooling_2062',
    name: 'foiling vs fooling',
    description: 'Commonly confused words: foiling and fooling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfoiling\b/gi,
    message: 'Check: "foiling" ("foiling") vs "fooling" ("fooling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fooling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_foiling_fouling_2063',
    name: 'foiling vs fouling',
    description: 'Commonly confused words: foiling and fouling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfoiling\b/gi,
    message: 'Check: "foiling" ("foiling") vs "fouling" ("fouling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fouling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_folly_dolly_2064',
    name: 'folly vs dolly',
    description: 'Commonly confused words: folly and dolly',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfolly\b/gi,
    message: 'Check: "folly" ("folly") vs "dolly" ("dolly"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dolly';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fond_find_2065',
    name: 'fond vs find',
    description: 'Commonly confused words: fond and find',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfond\b/gi,
    message: 'Check: "fond" ("fond") vs "find" ("find"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'find';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_foods_goods_2066',
    name: 'foods vs goods',
    description: 'Commonly confused words: foods and goods',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfoods\b/gi,
    message: 'Check: "foods" ("foods") vs "goods" ("goods"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'goods';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_foo_fol_2067',
    name: 'foo vs fol',
    description: 'Commonly confused words: foo and fol',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfoo\b/gi,
    message: 'Check: "foo" ("foo") vs "fol" ("fol"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fol';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_footed_rooted_2068',
    name: 'footed vs rooted',
    description: 'Commonly confused words: footed and rooted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfooted\b/gi,
    message: 'Check: "footed" ("footed") vs "rooted" ("rooted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rooted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_footers_rooters_2069',
    name: 'footers vs rooters',
    description: 'Commonly confused words: footers and rooters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfooters\b/gi,
    message: 'Check: "footers" ("footers") vs "rooters" ("rooters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rooters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_footless_rootless_2070',
    name: 'footless vs rootless',
    description: 'Commonly confused words: footless and rootless',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfootless\b/gi,
    message: 'Check: "footless" ("footless") vs "rootless" ("rootless"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rootless';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_forced_forces_2071',
    name: 'forced vs forces',
    description: 'Commonly confused words: forced and forces',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bforced\b/gi,
    message: 'Check: "forced" ("forced") vs "forces" ("forces"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'forces';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fords_fores_2072',
    name: 'fords vs fores',
    description: 'Commonly confused words: fords and fores',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfords\b/gi,
    message: 'Check: "fords" ("fords") vs "fores" ("fores"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fores';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fores_fires_2073',
    name: 'fores vs fires',
    description: 'Commonly confused words: fores and fires',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfores\b/gi,
    message: 'Check: "fores" ("fores") vs "fires" ("fires"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fires';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_forgers_forgets_2074',
    name: 'forgers vs forgets',
    description: 'Commonly confused words: forgers and forgets',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bforgers\b/gi,
    message: 'Check: "forgers" ("forgers") vs "forgets" ("forgets"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'forgets';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_formalized_formalizes_2075',
    name: 'formalized vs formalizes',
    description: 'Commonly confused words: formalized and formalizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bformalized\b/gi,
    message: 'Check: "formalized" ("formalized") vs "formalizes" ("formalizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'formalizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fortes_forges_2076',
    name: 'fortes vs forges',
    description: 'Commonly confused words: fortes and forges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfortes\b/gi,
    message: 'Check: "fortes" ("fortes") vs "forges" ("forges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'forges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_forth_forty_2077',
    name: 'forth vs forty',
    description: 'Commonly confused words: forth and forty',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bforth\b/gi,
    message: 'Check: "forth" ("forth") vs "forty" ("forty"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'forty';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fortified_fortifies_2078',
    name: 'fortified vs fortifies',
    description: 'Commonly confused words: fortified and fortifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfortified\b/gi,
    message: 'Check: "fortified" ("fortified") vs "fortifies" ("fortifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fortifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_founded_rounded_2079',
    name: 'founded vs rounded',
    description: 'Commonly confused words: founded and rounded',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfounded\b/gi,
    message: 'Check: "founded" ("founded") vs "rounded" ("rounded"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rounded';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_founder_rounder_2080',
    name: 'founder vs rounder',
    description: 'Commonly confused words: founder and rounder',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfounder\b/gi,
    message: 'Check: "founder" ("founder") vs "rounder" ("rounder"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rounder';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_found_round_2081',
    name: 'found vs round',
    description: 'Commonly confused words: found and round',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfound\b/gi,
    message: 'Check: "found" ("found") vs "round" ("round"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'round';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_founds_rounds_2082',
    name: 'founds vs rounds',
    description: 'Commonly confused words: founds and rounds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfounds\b/gi,
    message: 'Check: "founds" ("founds") vs "rounds" ("rounds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rounds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_foxed_foxes_2083',
    name: 'foxed vs foxes',
    description: 'Commonly confused words: foxed and foxes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfoxed\b/gi,
    message: 'Check: "foxed" ("foxed") vs "foxes" ("foxes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'foxes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fractured_fractures_2084',
    name: 'fractured vs fractures',
    description: 'Commonly confused words: fractured and fractures',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfractured\b/gi,
    message: 'Check: "fractured" ("fractured") vs "fractures" ("fractures"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fractures';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_frag_frat_2085',
    name: 'frag vs frat',
    description: 'Commonly confused words: frag and frat',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfrag\b/gi,
    message: 'Check: "frag" ("frag") vs "frat" ("frat"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'frat';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_frags_drags_2086',
    name: 'frags vs drags',
    description: 'Commonly confused words: frags and drags',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfrags\b/gi,
    message: 'Check: "frags" ("frags") vs "drags" ("drags"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'drags';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_framed_frames_2087',
    name: 'framed vs frames',
    description: 'Commonly confused words: framed and frames',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bframed\b/gi,
    message: 'Check: "framed" ("framed") vs "frames" ("frames"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'frames';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_frats_frays_2088',
    name: 'frats vs frays',
    description: 'Commonly confused words: frats and frays',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfrats\b/gi,
    message: 'Check: "frats" ("frats") vs "frays" ("frays"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'frays';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_frayed_grayed_2089',
    name: 'frayed vs grayed',
    description: 'Commonly confused words: frayed and grayed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfrayed\b/gi,
    message: 'Check: "frayed" ("frayed") vs "grayed" ("grayed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grayed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fraying_graying_2090',
    name: 'fraying vs graying',
    description: 'Commonly confused words: fraying and graying',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfraying\b/gi,
    message: 'Check: "fraying" ("fraying") vs "graying" ("graying"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'graying';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_freckled_freckles_2091',
    name: 'freckled vs freckles',
    description: 'Commonly confused words: freckled and freckles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfreckled\b/gi,
    message: 'Check: "freckled" ("freckled") vs "freckles" ("freckles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'freckles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_freed_greed_2092',
    name: 'freed vs greed',
    description: 'Commonly confused words: freed and greed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfreed\b/gi,
    message: 'Check: "freed" ("freed") vs "greed" ("greed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'greed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_frenzied_frenzies_2093',
    name: 'frenzied vs frenzies',
    description: 'Commonly confused words: frenzied and frenzies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfrenzied\b/gi,
    message: 'Check: "frenzied" ("frenzied") vs "frenzies" ("frenzies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'frenzies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fresher_freshet_2094',
    name: 'fresher vs freshet',
    description: 'Commonly confused words: fresher and freshet',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfresher\b/gi,
    message: 'Check: "fresher" ("fresher") vs "freshet" ("freshet"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'freshet';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fries_dries_2095',
    name: 'fries vs dries',
    description: 'Commonly confused words: fries and dries',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfries\b/gi,
    message: 'Check: "fries" ("fries") vs "dries" ("dries"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'dries';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_frilled_grilled_2096',
    name: 'frilled vs grilled',
    description: 'Commonly confused words: frilled and grilled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfrilled\b/gi,
    message: 'Check: "frilled" ("frilled") vs "grilled" ("grilled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grilled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fringed_fringes_2097',
    name: 'fringed vs fringes',
    description: 'Commonly confused words: fringed and fringes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfringed\b/gi,
    message: 'Check: "fringed" ("fringed") vs "fringes" ("fringes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fringes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fritters_gritters_2098',
    name: 'fritters vs gritters',
    description: 'Commonly confused words: fritters and gritters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfritters\b/gi,
    message: 'Check: "fritters" ("fritters") vs "gritters" ("gritters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gritters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_frown_grown_2099',
    name: 'frown vs grown',
    description: 'Commonly confused words: frown and grown',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfrown\b/gi,
    message: 'Check: "frown" ("frown") vs "grown" ("grown"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'grown';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_frustrated_frustrates_2100',
    name: 'frustrated vs frustrates',
    description: 'Commonly confused words: frustrated and frustrates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfrustrated\b/gi,
    message: 'Check: "frustrated" ("frustrated") vs "frustrates" ("frustrates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'frustrates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fucking_rucking_2101',
    name: 'fucking vs rucking',
    description: 'Commonly confused words: fucking and rucking',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfucking\b/gi,
    message: 'Check: "fucking" ("fucking") vs "rucking" ("rucking"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rucking';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fucks_rucks_2102',
    name: 'fucks vs rucks',
    description: 'Commonly confused words: fucks and rucks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfucks\b/gi,
    message: 'Check: "fucks" ("fucks") vs "rucks" ("rucks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rucks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fueled_furled_2103',
    name: 'fueled vs furled',
    description: 'Commonly confused words: fueled and furled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfueled\b/gi,
    message: 'Check: "fueled" ("fueled") vs "furled" ("furled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'furled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fuel_furl_2104',
    name: 'fuel vs furl',
    description: 'Commonly confused words: fuel and furl',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfuel\b/gi,
    message: 'Check: "fuel" ("fuel") vs "furl" ("furl"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'furl';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fulls_fills_2105',
    name: 'fulls vs fills',
    description: 'Commonly confused words: fulls and fills',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfulls\b/gi,
    message: 'Check: "fulls" ("fulls") vs "fills" ("fills"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fills';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fully_gully_2106',
    name: 'fully vs gully',
    description: 'Commonly confused words: fully and gully',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfully\b/gi,
    message: 'Check: "fully" ("fully") vs "gully" ("gully"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gully';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fulminated_fulminates_2107',
    name: 'fulminated vs fulminates',
    description: 'Commonly confused words: fulminated and fulminates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfulminated\b/gi,
    message: 'Check: "fulminated" ("fulminated") vs "fulminates" ("fulminates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fulminates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fumbled_fumbles_2108',
    name: 'fumbled vs fumbles',
    description: 'Commonly confused words: fumbled and fumbles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfumbled\b/gi,
    message: 'Check: "fumbled" ("fumbled") vs "fumbles" ("fumbles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fumbles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fumbled_rumbled_2109',
    name: 'fumbled vs rumbled',
    description: 'Commonly confused words: fumbled and rumbled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfumbled\b/gi,
    message: 'Check: "fumbled" ("fumbled") vs "rumbled" ("rumbled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rumbled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fumbling_rumbling_2110',
    name: 'fumbling vs rumbling',
    description: 'Commonly confused words: fumbling and rumbling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfumbling\b/gi,
    message: 'Check: "fumbling" ("fumbling") vs "rumbling" ("rumbling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'rumbling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fumed_fumes_2111',
    name: 'fumed vs fumes',
    description: 'Commonly confused words: fumed and fumes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfumed\b/gi,
    message: 'Check: "fumed" ("fumed") vs "fumes" ("fumes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fumes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fund_find_2112',
    name: 'fund vs find',
    description: 'Commonly confused words: fund and find',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfund\b/gi,
    message: 'Check: "fund" ("fund") vs "find" ("find"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'find';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_funds_finds_2113',
    name: 'funds vs finds',
    description: 'Commonly confused words: funds and finds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfunds\b/gi,
    message: 'Check: "funds" ("funds") vs "finds" ("finds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'finds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_funnels_gunnels_2114',
    name: 'funnels vs gunnels',
    description: 'Commonly confused words: funnels and gunnels',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfunnels\b/gi,
    message: 'Check: "funnels" ("funnels") vs "gunnels" ("gunnels"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gunnels';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_funnels_runnels_2115',
    name: 'funnels vs runnels',
    description: 'Commonly confused words: funnels and runnels',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfunnels\b/gi,
    message: 'Check: "funnels" ("funnels") vs "runnels" ("runnels"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'runnels';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_funner_gunner_2116',
    name: 'funner vs gunner',
    description: 'Commonly confused words: funner and gunner',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfunner\b/gi,
    message: 'Check: "funner" ("funner") vs "gunner" ("gunner"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gunner';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_funner_runner_2117',
    name: 'funner vs runner',
    description: 'Commonly confused words: funner and runner',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfunner\b/gi,
    message: 'Check: "funner" ("funner") vs "runner" ("runner"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'runner';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_funny_gunny_2118',
    name: 'funny vs gunny',
    description: 'Commonly confused words: funny and gunny',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfunny\b/gi,
    message: 'Check: "funny" ("funny") vs "gunny" ("gunny"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gunny';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_furbish_furnish_2119',
    name: 'furbish vs furnish',
    description: 'Commonly confused words: furbish and furnish',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfurbish\b/gi,
    message: 'Check: "furbish" ("furbish") vs "furnish" ("furnish"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'furnish';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_furnished_furnishes_2120',
    name: 'furnished vs furnishes',
    description: 'Commonly confused words: furnished and furnishes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfurnished\b/gi,
    message: 'Check: "furnished" ("furnished") vs "furnishes" ("furnishes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'furnishes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fused_fusee_2121',
    name: 'fused vs fusee',
    description: 'Commonly confused words: fused and fusee',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfused\b/gi,
    message: 'Check: "fused" ("fused") vs "fusee" ("fusee"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fusee';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fused_fuses_2122',
    name: 'fused vs fuses',
    description: 'Commonly confused words: fused and fuses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfused\b/gi,
    message: 'Check: "fused" ("fused") vs "fuses" ("fuses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fuses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fuses_ruses_2123',
    name: 'fuses vs ruses',
    description: 'Commonly confused words: fuses and ruses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfuses\b/gi,
    message: 'Check: "fuses" ("fuses") vs "ruses" ("ruses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ruses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fussed_fusses_2124',
    name: 'fussed vs fusses',
    description: 'Commonly confused words: fussed and fusses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfussed\b/gi,
    message: 'Check: "fussed" ("fussed") vs "fusses" ("fusses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fusses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fussy_gussy_2125',
    name: 'fussy vs gussy',
    description: 'Commonly confused words: fussy and gussy',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfussy\b/gi,
    message: 'Check: "fussy" ("fussy") vs "gussy" ("gussy"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gussy';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fusty_gusty_2126',
    name: 'fusty vs gusty',
    description: 'Commonly confused words: fusty and gusty',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfusty\b/gi,
    message: 'Check: "fusty" ("fusty") vs "gusty" ("gusty"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gusty';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_fwy_fey_2127',
    name: 'fwy vs fey',
    description: 'Commonly confused words: fwy and fey',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bfwy\b/gi,
    message: 'Check: "fwy" ("fwy") vs "fey" ("fey"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'fey';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
