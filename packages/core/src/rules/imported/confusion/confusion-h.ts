/**
 * Auto-generated confusion rules (H)
 * Source: LanguageTool confusion_sets.txt
 * Generated: 2026-01-28T19:51:36.510Z
 * 
 * These rules detect commonly confused word pairs like:
 * - affect/effect
 * - their/there/they're
 * - accept/except
 * etc.
 * 
 * DO NOT EDIT - Generated by convert-confusion-sets.cjs
 */

import type { GrammarRule } from '../../../types';

export const confusionRulesH: GrammarRule[] = [
  {
    id: 'confusion_had_hat_340',
    name: 'had vs hat',
    description: 'Commonly confused words: had and hat',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhad\b/gi,
    message: 'Check: "had" ("had") vs "hat" ("hat"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hat';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hail_hale_341',
    name: 'hail vs hale',
    description: 'Commonly confused words: hail and hale',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhail\b/gi,
    message: 'Check: "hail" ("hail") vs "hale" ("hale"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hale';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hair_hare_342',
    name: 'hair vs hare',
    description: 'Commonly confused words: hair and hare',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhair\b/gi,
    message: 'Check: "hair" ("hair") vs "hare" ("hare"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hare';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hairs_hares_343',
    name: 'hairs vs hares',
    description: 'Commonly confused words: hairs and hares',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhairs\b/gi,
    message: 'Check: "hairs" ("hairs") vs "hares" ("hares"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hares';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_half_have_344',
    name: 'half vs have',
    description: 'Commonly confused words: half and have',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhalf\b/gi,
    message: 'Check: "half" ("half") vs "have" ("have"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'have';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_half_halve_345',
    name: 'half vs halve',
    description: 'Commonly confused words: half and halve',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhalf\b/gi,
    message: 'Check: "half" ("half") vs "halve" ("halve"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'halve';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_halves_haves_346',
    name: 'halves vs haves',
    description: 'Commonly confused words: halves and haves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhalves\b/gi,
    message: 'Check: "halves" ("halves") vs "haves" ("haves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'haves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_heal_heel_347',
    name: 'heal vs heel',
    description: 'Commonly confused words: heal and heel',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bheal\b/gi,
    message: 'Check: "heal" ("heal") vs "heel" ("heel"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'heel';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_heard_herd_348',
    name: 'heard vs herd',
    description: 'Commonly confused words: heard and herd',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bheard\b/gi,
    message: 'Check: "heard" ("heard") vs "herd" ("herd"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'herd';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hear_heart_349',
    name: 'hear vs heart',
    description: 'Commonly confused words: hear and heart',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhear\b/gi,
    message: 'Check: "hear" (to perceive sound) vs "heart" ("heart"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'heart';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hears_hearts_350',
    name: 'hears vs hearts',
    description: 'Commonly confused words: hears and hearts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhears\b/gi,
    message: 'Did you mean "hearts"? "hears" ("hears") is often confused with "hearts" ("hearts").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hearts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hearts_hears_351',
    name: 'hearts vs hears',
    description: 'Commonly confused words: hearts and hears',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhearts\b/gi,
    message: 'Did you mean "hears"? "hearts" ("hearts") is often confused with "hears" ("hears").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hears';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hear_here_352',
    name: 'hear vs here',
    description: 'Commonly confused words: hear and here',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhear\b/gi,
    message: 'Check: "hear" (to perceive sound) vs "here" (in this place). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'here';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_heroin_heroine_353',
    name: 'heroin vs heroine',
    description: 'Commonly confused words: heroin and heroine',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bheroin\b/gi,
    message: 'Check: "heroin" ("heroin") vs "heroine" ("heroine"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'heroine';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_he_the_354',
    name: 'he vs the',
    description: 'Commonly confused words: he and the',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhe\b/gi,
    message: 'Did you mean "the"? "he" ("he") is often confused with "the" ("the").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'the';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hew_hue_355',
    name: 'hew vs hue',
    description: 'Commonly confused words: hew and hue',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhew\b/gi,
    message: 'Check: "hew" ("hew") vs "hue" ("hue"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hue';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hews_hues_356',
    name: 'hews vs hues',
    description: 'Commonly confused words: hews and hues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhews\b/gi,
    message: 'Check: "hews" ("hews") vs "hues" ("hues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hide_hied_357',
    name: 'hide vs hied',
    description: 'Commonly confused words: hide and hied',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhide\b/gi,
    message: 'Check: "hide" ("hide") vs "hied" ("hied"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hied';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_higher_hire_358',
    name: 'higher vs hire',
    description: 'Commonly confused words: higher and hire',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhigher\b/gi,
    message: 'Did you mean "hire"? "higher" ("higher") is often confused with "hire" ("hire").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hire';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hire_higher_359',
    name: 'hire vs higher',
    description: 'Commonly confused words: hire and higher',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhire\b/gi,
    message: 'Did you mean "higher"? "hire" ("hire") is often confused with "higher" ("higher").',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'higher';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_him_hymn_360',
    name: 'him vs hymn',
    description: 'Commonly confused words: him and hymn',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhim\b/gi,
    message: 'Check: "him" ("him") vs "hymn" ("hymn"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hymn';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hoard_horde_361',
    name: 'hoard vs horde',
    description: 'Commonly confused words: hoard and horde',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhoard\b/gi,
    message: 'Check: "hoard" ("hoard") vs "horde" ("horde"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'horde';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hoping_hopping_362',
    name: 'hoping vs hopping',
    description: 'Commonly confused words: hoping and hopping',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhoping\b/gi,
    message: 'Check: "hoping" ("hoping") vs "hopping" ("hopping"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hopping';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hostel_hostile_363',
    name: 'hostel vs hostile',
    description: 'Commonly confused words: hostel and hostile',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhostel\b/gi,
    message: 'Check: "hostel" ("hostel") vs "hostile" ("hostile"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hostile';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hostels_hostiles_364',
    name: 'hostels vs hostiles',
    description: 'Commonly confused words: hostels and hostiles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhostels\b/gi,
    message: 'Check: "hostels" ("hostels") vs "hostiles" ("hostiles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hostiles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hours_ours_365',
    name: 'hours vs ours',
    description: 'Commonly confused words: hours and ours',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhours\b/gi,
    message: 'Check: "hours" ("hours") vs "ours" ("ours"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'ours';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hag_gag_2341',
    name: 'hag vs gag',
    description: 'Commonly confused words: hag and gag',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhag\b/gi,
    message: 'Check: "hag" ("hag") vs "gag" ("gag"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gag';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_haggled_haggles_2342',
    name: 'haggled vs haggles',
    description: 'Commonly confused words: haggled and haggles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhaggled\b/gi,
    message: 'Check: "haggled" ("haggled") vs "haggles" ("haggles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'haggles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hailed_hauled_2343',
    name: 'hailed vs hauled',
    description: 'Commonly confused words: hailed and hauled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhailed\b/gi,
    message: 'Check: "hailed" ("hailed") vs "hauled" ("hauled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hauled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hailed_jailed_2344',
    name: 'hailed vs jailed',
    description: 'Commonly confused words: hailed and jailed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhailed\b/gi,
    message: 'Check: "hailed" ("hailed") vs "jailed" ("jailed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jailed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hailed_nailed_2345',
    name: 'hailed vs nailed',
    description: 'Commonly confused words: hailed and nailed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhailed\b/gi,
    message: 'Check: "hailed" ("hailed") vs "nailed" ("nailed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nailed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hail_haul_2346',
    name: 'hail vs haul',
    description: 'Commonly confused words: hail and haul',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhail\b/gi,
    message: 'Check: "hail" ("hail") vs "haul" ("haul"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'haul';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hailing_jailing_2347',
    name: 'hailing vs jailing',
    description: 'Commonly confused words: hailing and jailing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhailing\b/gi,
    message: 'Check: "hailing" ("hailing") vs "jailing" ("jailing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jailing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hail_jail_2348',
    name: 'hail vs jail',
    description: 'Commonly confused words: hail and jail',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhail\b/gi,
    message: 'Check: "hail" ("hail") vs "jail" ("jail"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jail';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hails_hauls_2349',
    name: 'hails vs hauls',
    description: 'Commonly confused words: hails and hauls',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhails\b/gi,
    message: 'Check: "hails" ("hails") vs "hauls" ("hauls"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hauls';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hails_jails_2350',
    name: 'hails vs jails',
    description: 'Commonly confused words: hails and jails',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhails\b/gi,
    message: 'Check: "hails" ("hails") vs "jails" ("jails"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jails';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_halved_halves_2351',
    name: 'halved vs halves',
    description: 'Commonly confused words: halved and halves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhalved\b/gi,
    message: 'Check: "halved" ("halved") vs "halves" ("halves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'halves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hammed_jammed_2352',
    name: 'hammed vs jammed',
    description: 'Commonly confused words: hammed and jammed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhammed\b/gi,
    message: 'Check: "hammed" ("hammed") vs "jammed" ("jammed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jammed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hammering_yammering_2353',
    name: 'hammering vs yammering',
    description: 'Commonly confused words: hammering and yammering',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhammering\b/gi,
    message: 'Check: "hammering" ("hammering") vs "yammering" ("yammering"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'yammering';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hamstring_hamstrung_2354',
    name: 'hamstring vs hamstrung',
    description: 'Commonly confused words: hamstring and hamstrung',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhamstring\b/gi,
    message: 'Check: "hamstring" ("hamstring") vs "hamstrung" ("hamstrung"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hamstrung';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_happier_nappier_2355',
    name: 'happier vs nappier',
    description: 'Commonly confused words: happier and nappier',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhappier\b/gi,
    message: 'Check: "happier" ("happier") vs "nappier" ("nappier"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nappier';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hard_hare_2356',
    name: 'hard vs hare',
    description: 'Commonly confused words: hard and hare',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhard\b/gi,
    message: 'Check: "hard" ("hard") vs "hare" ("hare"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hare';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hared_hares_2357',
    name: 'hared vs hares',
    description: 'Commonly confused words: hared and hares',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhared\b/gi,
    message: 'Check: "hared" ("hared") vs "hares" ("hares"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hares';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_harmonics_harmonica_2358',
    name: 'harmonics vs harmonica',
    description: 'Commonly confused words: harmonics and harmonica',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bharmonics\b/gi,
    message: 'Check: "harmonics" ("harmonics") vs "harmonica" ("harmonica"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'harmonica';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_harmonized_harmonizes_2359',
    name: 'harmonized vs harmonizes',
    description: 'Commonly confused words: harmonized and harmonizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bharmonized\b/gi,
    message: 'Check: "harmonized" ("harmonized") vs "harmonizes" ("harmonizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'harmonizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_harnessed_harnesses_2360',
    name: 'harnessed vs harnesses',
    description: 'Commonly confused words: harnessed and harnesses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bharnessed\b/gi,
    message: 'Check: "harnessed" ("harnessed") vs "harnesses" ("harnesses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'harnesses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_harrowing_narrowing_2361',
    name: 'harrowing vs narrowing',
    description: 'Commonly confused words: harrowing and narrowing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bharrowing\b/gi,
    message: 'Check: "harrowing" ("harrowing") vs "narrowing" ("narrowing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'narrowing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_harrow_narrow_2362',
    name: 'harrow vs narrow',
    description: 'Commonly confused words: harrow and narrow',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bharrow\b/gi,
    message: 'Check: "harrow" ("harrow") vs "narrow" ("narrow"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'narrow';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hashed_hashes_2363',
    name: 'hashed vs hashes',
    description: 'Commonly confused words: hashed and hashes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhashed\b/gi,
    message: 'Check: "hashed" ("hashed") vs "hashes" ("hashes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hashes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hash_gash_2364',
    name: 'hash vs gash',
    description: 'Commonly confused words: hash and gash',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhash\b/gi,
    message: 'Check: "hash" ("hash") vs "gash" ("gash"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gash';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hashing_gashing_2365',
    name: 'hashing vs gashing',
    description: 'Commonly confused words: hashing and gashing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhashing\b/gi,
    message: 'Check: "hashing" ("hashing") vs "gashing" ("gashing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gashing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hastily_nastily_2366',
    name: 'hastily vs nastily',
    description: 'Commonly confused words: hastily and nastily',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhastily\b/gi,
    message: 'Check: "hastily" ("hastily") vs "nastily" ("nastily"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nastily';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hatched_hatches_2367',
    name: 'hatched vs hatches',
    description: 'Commonly confused words: hatched and hatches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhatched\b/gi,
    message: 'Check: "hatched" ("hatched") vs "hatches" ("hatches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hatches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hated_gated_2368',
    name: 'hated vs gated',
    description: 'Commonly confused words: hated and gated',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhated\b/gi,
    message: 'Check: "hated" ("hated") vs "gated" ("gated"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gated';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hated_hared_2369',
    name: 'hated vs hared',
    description: 'Commonly confused words: hated and hared',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhated\b/gi,
    message: 'Check: "hated" ("hated") vs "hared" ("hared"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hared';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hating_gating_2370',
    name: 'hating vs gating',
    description: 'Commonly confused words: hating and gating',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhating\b/gi,
    message: 'Check: "hating" ("hating") vs "gating" ("gating"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gating';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hatters_natters_2371',
    name: 'hatters vs natters',
    description: 'Commonly confused words: hatters and natters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhatters\b/gi,
    message: 'Check: "hatters" ("hatters") vs "natters" ("natters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'natters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_haunt_jaunt_2372',
    name: 'haunt vs jaunt',
    description: 'Commonly confused words: haunt and jaunt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhaunt\b/gi,
    message: 'Check: "haunt" ("haunt") vs "jaunt" ("jaunt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jaunt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_haunts_jaunts_2373',
    name: 'haunts vs jaunts',
    description: 'Commonly confused words: haunts and jaunts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhaunts\b/gi,
    message: 'Check: "haunts" ("haunts") vs "jaunts" ("jaunts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jaunts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hawed_jawed_2374',
    name: 'hawed vs jawed',
    description: 'Commonly confused words: hawed and jawed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhawed\b/gi,
    message: 'Check: "hawed" ("hawed") vs "jawed" ("jawed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jawed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_haying_hating_2375',
    name: 'haying vs hating',
    description: 'Commonly confused words: haying and hating',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhaying\b/gi,
    message: 'Check: "haying" ("haying") vs "hating" ("hating"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hating';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hazed_gazed_2376',
    name: 'hazed vs gazed',
    description: 'Commonly confused words: hazed and gazed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhazed\b/gi,
    message: 'Check: "hazed" ("hazed") vs "gazed" ("gazed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gazed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_headlined_headlines_2377',
    name: 'headlined vs headlines',
    description: 'Commonly confused words: headlined and headlines',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bheadlined\b/gi,
    message: 'Check: "headlined" ("headlined") vs "headlines" ("headlines"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'headlines';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hearer_heater_2378',
    name: 'hearer vs heater',
    description: 'Commonly confused words: hearer and heater',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhearer\b/gi,
    message: 'Check: "hearer" ("hearer") vs "heater" ("heater"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'heater';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hearers_heaters_2379',
    name: 'hearers vs heaters',
    description: 'Commonly confused words: hearers and heaters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhearers\b/gi,
    message: 'Check: "hearers" ("hearers") vs "heaters" ("heaters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'heaters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hearth_hearty_2380',
    name: 'hearth vs hearty',
    description: 'Commonly confused words: hearth and hearty',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhearth\b/gi,
    message: 'Check: "hearth" ("hearth") vs "hearty" ("hearty"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hearty';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_heat_hear_2381',
    name: 'heat vs hear',
    description: 'Commonly confused words: heat and hear',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bheat\b/gi,
    message: 'Check: "heat" ("heat") vs "hear" (to perceive sound). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hear';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_heating_hearing_2382',
    name: 'heating vs hearing',
    description: 'Commonly confused words: heating and hearing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bheating\b/gi,
    message: 'Check: "heating" ("heating") vs "hearing" ("hearing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hearing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_heaved_heaves_2383',
    name: 'heaved vs heaves',
    description: 'Commonly confused words: heaved and heaves',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bheaved\b/gi,
    message: 'Check: "heaved" ("heaved") vs "heaves" ("heaves"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'heaves';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_heeded_herded_2384',
    name: 'heeded vs herded',
    description: 'Commonly confused words: heeded and herded',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bheeded\b/gi,
    message: 'Check: "heeded" ("heeded") vs "herded" ("herded"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'herded';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_heeding_herding_2385',
    name: 'heeding vs herding',
    description: 'Commonly confused words: heeding and herding',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bheeding\b/gi,
    message: 'Check: "heeding" ("heeding") vs "herding" ("herding"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'herding';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_heeding_needing_2386',
    name: 'heeding vs needing',
    description: 'Commonly confused words: heeding and needing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bheeding\b/gi,
    message: 'Check: "heeding" ("heeding") vs "needing" ("needing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'needing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_heedlessly_needlessly_2387',
    name: 'heedlessly vs needlessly',
    description: 'Commonly confused words: heedlessly and needlessly',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bheedlessly\b/gi,
    message: 'Check: "heedlessly" ("heedlessly") vs "needlessly" ("needlessly"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'needlessly';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_heedless_needless_2388',
    name: 'heedless vs needless',
    description: 'Commonly confused words: heedless and needless',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bheedless\b/gi,
    message: 'Check: "heedless" ("heedless") vs "needless" ("needless"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'needless';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_heeds_herds_2389',
    name: 'heeds vs herds',
    description: 'Commonly confused words: heeds and herds',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bheeds\b/gi,
    message: 'Check: "heeds" ("heeds") vs "herds" ("herds"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'herds';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hemorrhaged_hemorrhages_2390',
    name: 'hemorrhaged vs hemorrhages',
    description: 'Commonly confused words: hemorrhaged and hemorrhages',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhemorrhaged\b/gi,
    message: 'Check: "hemorrhaged" ("hemorrhaged") vs "hemorrhages" ("hemorrhages"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hemorrhages';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hems_hens_2391',
    name: 'hems vs hens',
    description: 'Commonly confused words: hems and hens',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhems\b/gi,
    message: 'Check: "hems" ("hems") vs "hens" ("hens"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hens';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hens_gens_2392',
    name: 'hens vs gens',
    description: 'Commonly confused words: hens and gens',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhens\b/gi,
    message: 'Check: "hens" ("hens") vs "gens" ("gens"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gens';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_herd_hers_2393',
    name: 'herd vs hers',
    description: 'Commonly confused words: herd and hers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bherd\b/gi,
    message: 'Check: "herd" ("herd") vs "hers" ("hers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_heroes_herpes_2394',
    name: 'heroes vs herpes',
    description: 'Commonly confused words: heroes and herpes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bheroes\b/gi,
    message: 'Check: "heroes" ("heroes") vs "herpes" ("herpes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'herpes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hewer_newer_2395',
    name: 'hewer vs newer',
    description: 'Commonly confused words: hewer and newer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhewer\b/gi,
    message: 'Check: "hewer" ("hewer") vs "newer" ("newer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'newer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hibernated_hibernates_2396',
    name: 'hibernated vs hibernates',
    description: 'Commonly confused words: hibernated and hibernates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhibernated\b/gi,
    message: 'Check: "hibernated" ("hibernated") vs "hibernates" ("hibernates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hibernates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hickey_hockey_2397',
    name: 'hickey vs hockey',
    description: 'Commonly confused words: hickey and hockey',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhickey\b/gi,
    message: 'Check: "hickey" ("hickey") vs "hockey" ("hockey"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hockey';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hided_hides_2398',
    name: 'hided vs hides',
    description: 'Commonly confused words: hided and hides',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhided\b/gi,
    message: 'Check: "hided" ("hided") vs "hides" ("hides"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hides';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hie_hid_2399',
    name: 'hie vs hid',
    description: 'Commonly confused words: hie and hid',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhie\b/gi,
    message: 'Check: "hie" ("hie") vs "hid" ("hid"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hid';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hiked_hikes_2400',
    name: 'hiked vs hikes',
    description: 'Commonly confused words: hiked and hikes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhiked\b/gi,
    message: 'Check: "hiked" ("hiked") vs "hikes" ("hikes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hikes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hilt_gilt_2401',
    name: 'hilt vs gilt',
    description: 'Commonly confused words: hilt and gilt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhilt\b/gi,
    message: 'Check: "hilt" ("hilt") vs "gilt" ("gilt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gilt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hilt_jilt_2402',
    name: 'hilt vs jilt',
    description: 'Commonly confused words: hilt and jilt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhilt\b/gi,
    message: 'Check: "hilt" ("hilt") vs "jilt" ("jilt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jilt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hilts_jilts_2403',
    name: 'hilts vs jilts',
    description: 'Commonly confused words: hilts and jilts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhilts\b/gi,
    message: 'Check: "hilts" ("hilts") vs "jilts" ("jilts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jilts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hinged_hinges_2404',
    name: 'hinged vs hinges',
    description: 'Commonly confused words: hinged and hinges',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhinged\b/gi,
    message: 'Check: "hinged" ("hinged") vs "hinges" ("hinges"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hinges';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hinged_hinted_2405',
    name: 'hinged vs hinted',
    description: 'Commonly confused words: hinged and hinted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhinged\b/gi,
    message: 'Check: "hinged" ("hinged") vs "hinted" ("hinted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hinted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hinging_hinting_2406',
    name: 'hinging vs hinting',
    description: 'Commonly confused words: hinging and hinting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhinging\b/gi,
    message: 'Check: "hinging" ("hinging") vs "hinting" ("hinting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hinting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hinted_hunted_2407',
    name: 'hinted vs hunted',
    description: 'Commonly confused words: hinted and hunted',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhinted\b/gi,
    message: 'Check: "hinted" ("hinted") vs "hunted" ("hunted"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hunted';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hint_hing_2408',
    name: 'hint vs hing',
    description: 'Commonly confused words: hint and hing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhint\b/gi,
    message: 'Check: "hint" ("hint") vs "hing" ("hing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hint_hunt_2409',
    name: 'hint vs hunt',
    description: 'Commonly confused words: hint and hunt',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhint\b/gi,
    message: 'Check: "hint" ("hint") vs "hunt" ("hunt"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hunt';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hipped_hopped_2410',
    name: 'hipped vs hopped',
    description: 'Commonly confused words: hipped and hopped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhipped\b/gi,
    message: 'Check: "hipped" ("hipped") vs "hopped" ("hopped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hopped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hipped_nipped_2411',
    name: 'hipped vs nipped',
    description: 'Commonly confused words: hipped and nipped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhipped\b/gi,
    message: 'Check: "hipped" ("hipped") vs "nipped" ("nipped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nipped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hipper_hopper_2412',
    name: 'hipper vs hopper',
    description: 'Commonly confused words: hipper and hopper',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhipper\b/gi,
    message: 'Check: "hipper" ("hipper") vs "hopper" ("hopper"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hopper';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hist_gist_2413',
    name: 'hist vs gist',
    description: 'Commonly confused words: hist and gist',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhist\b/gi,
    message: 'Check: "hist" ("hist") vs "gist" ("gist"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gist';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hitches_hutches_2414',
    name: 'hitches vs hutches',
    description: 'Commonly confused words: hitches and hutches',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhitches\b/gi,
    message: 'Check: "hitches" ("hitches") vs "hutches" ("hutches"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hutches';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hitch_hutch_2415',
    name: 'hitch vs hutch',
    description: 'Commonly confused words: hitch and hutch',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhitch\b/gi,
    message: 'Check: "hitch" ("hitch") vs "hutch" ("hutch"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hutch';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hitter_hotter_2416',
    name: 'hitter vs hotter',
    description: 'Commonly confused words: hitter and hotter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhitter\b/gi,
    message: 'Check: "hitter" ("hitter") vs "hotter" ("hotter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hotter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hitters_jitters_2417',
    name: 'hitters vs jitters',
    description: 'Commonly confused words: hitters and jitters',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhitters\b/gi,
    message: 'Check: "hitters" ("hitters") vs "jitters" ("jitters"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jitters';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hitting_hotting_2418',
    name: 'hitting vs hotting',
    description: 'Commonly confused words: hitting and hotting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhitting\b/gi,
    message: 'Check: "hitting" ("hitting") vs "hotting" ("hotting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hotting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hived_hives_2419',
    name: 'hived vs hives',
    description: 'Commonly confused words: hived and hives',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhived\b/gi,
    message: 'Check: "hived" ("hived") vs "hives" ("hives"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hives';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hives_gives_2420',
    name: 'hives vs gives',
    description: 'Commonly confused words: hives and gives',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhives\b/gi,
    message: 'Check: "hives" ("hives") vs "gives" ("gives"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gives';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hoaxed_hoaxes_2421',
    name: 'hoaxed vs hoaxes',
    description: 'Commonly confused words: hoaxed and hoaxes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhoaxed\b/gi,
    message: 'Check: "hoaxed" ("hoaxed") vs "hoaxes" ("hoaxes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hoaxes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hobbled_hobbles_2422',
    name: 'hobbled vs hobbles',
    description: 'Commonly confused words: hobbled and hobbles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhobbled\b/gi,
    message: 'Check: "hobbled" ("hobbled") vs "hobbles" ("hobbles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hobbles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hobble_nobble_2423',
    name: 'hobble vs nobble',
    description: 'Commonly confused words: hobble and nobble',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhobble\b/gi,
    message: 'Check: "hobble" ("hobble") vs "nobble" ("nobble"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'nobble';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hods_gods_2424',
    name: 'hods vs gods',
    description: 'Commonly confused words: hods and gods',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhods\b/gi,
    message: 'Check: "hods" ("hods") vs "gods" ("gods"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gods';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hoer_goer_2425',
    name: 'hoer vs goer',
    description: 'Commonly confused words: hoer and goer',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhoer\b/gi,
    message: 'Check: "hoer" ("hoer") vs "goer" ("goer"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'goer';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hoes_hies_2426',
    name: 'hoes vs hies',
    description: 'Commonly confused words: hoes and hies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhoes\b/gi,
    message: 'Check: "hoes" ("hoes") vs "hies" ("hies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hogged_jogged_2427',
    name: 'hogged vs jogged',
    description: 'Commonly confused words: hogged and jogged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhogged\b/gi,
    message: 'Check: "hogged" ("hogged") vs "jogged" ("jogged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jogged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hogging_jogging_2428',
    name: 'hogging vs jogging',
    description: 'Commonly confused words: hogging and jogging',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhogging\b/gi,
    message: 'Check: "hogging" ("hogging") vs "jogging" ("jogging"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jogging';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hoist_joist_2429',
    name: 'hoist vs joist',
    description: 'Commonly confused words: hoist and joist',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhoist\b/gi,
    message: 'Check: "hoist" ("hoist") vs "joist" ("joist"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'joist';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hoists_joists_2430',
    name: 'hoists vs joists',
    description: 'Commonly confused words: hoists and joists',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhoists\b/gi,
    message: 'Check: "hoists" ("hoists") vs "joists" ("joists"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'joists';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hoke_hike_2431',
    name: 'hoke vs hike',
    description: 'Commonly confused words: hoke and hike',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhoke\b/gi,
    message: 'Check: "hoke" ("hoke") vs "hike" ("hike"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hike';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hokes_hikes_2432',
    name: 'hokes vs hikes',
    description: 'Commonly confused words: hokes and hikes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhokes\b/gi,
    message: 'Check: "hokes" ("hokes") vs "hikes" ("hikes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hikes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hokes_holes_2433',
    name: 'hokes vs holes',
    description: 'Commonly confused words: hokes and holes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhokes\b/gi,
    message: 'Check: "hokes" ("hokes") vs "holes" ("holes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'holes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hold_gold_2434',
    name: 'hold vs gold',
    description: 'Commonly confused words: hold and gold',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhold\b/gi,
    message: 'Check: "hold" ("hold") vs "gold" ("gold"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gold';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_holds_holes_2435',
    name: 'holds vs holes',
    description: 'Commonly confused words: holds and holes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bholds\b/gi,
    message: 'Check: "holds" ("holds") vs "holes" ("holes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'holes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_holds_hoods_2436',
    name: 'holds vs hoods',
    description: 'Commonly confused words: holds and hoods',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bholds\b/gi,
    message: 'Check: "holds" ("holds") vs "hoods" ("hoods"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hoods';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_homed_honed_2437',
    name: 'homed vs honed',
    description: 'Commonly confused words: homed and honed',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhomed\b/gi,
    message: 'Check: "homed" ("homed") vs "honed" ("honed"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'honed';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_homing_honing_2438',
    name: 'homing vs honing',
    description: 'Commonly confused words: homing and honing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhoming\b/gi,
    message: 'Check: "homing" ("homing") vs "honing" ("honing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'honing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_honed_hones_2439',
    name: 'honed vs hones',
    description: 'Commonly confused words: honed and hones',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhoned\b/gi,
    message: 'Check: "honed" ("honed") vs "hones" ("hones"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hones';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hood_hold_2440',
    name: 'hood vs hold',
    description: 'Commonly confused words: hood and hold',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhood\b/gi,
    message: 'Check: "hood" ("hood") vs "hold" ("hold"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hold';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hooding_hoofing_2441',
    name: 'hooding vs hoofing',
    description: 'Commonly confused words: hooding and hoofing',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhooding\b/gi,
    message: 'Check: "hooding" ("hooding") vs "hoofing" ("hoofing"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hoofing';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hooey_holey_2442',
    name: 'hooey vs holey',
    description: 'Commonly confused words: hooey and holey',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhooey\b/gi,
    message: 'Check: "hooey" ("hooey") vs "holey" ("holey"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'holey';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hooks_gooks_2443',
    name: 'hooks vs gooks',
    description: 'Commonly confused words: hooks and gooks',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhooks\b/gi,
    message: 'Check: "hooks" ("hooks") vs "gooks" ("gooks"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gooks';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hooped_hopped_2444',
    name: 'hooped vs hopped',
    description: 'Commonly confused words: hooped and hopped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhooped\b/gi,
    message: 'Check: "hooped" ("hooped") vs "hopped" ("hopped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hopped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hoped_hopes_2445',
    name: 'hoped vs hopes',
    description: 'Commonly confused words: hoped and hopes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhoped\b/gi,
    message: 'Check: "hoped" ("hoped") vs "hopes" ("hopes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hopes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hops_hips_2446',
    name: 'hops vs hips',
    description: 'Commonly confused words: hops and hips',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhops\b/gi,
    message: 'Check: "hops" ("hops") vs "hips" ("hips"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hips';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_horrified_horrifies_2447',
    name: 'horrified vs horrifies',
    description: 'Commonly confused words: horrified and horrifies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhorrified\b/gi,
    message: 'Check: "horrified" ("horrified") vs "horrifies" ("horrifies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'horrifies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hosed_hoses_2448',
    name: 'hosed vs hoses',
    description: 'Commonly confused words: hosed and hoses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhosed\b/gi,
    message: 'Check: "hosed" ("hosed") vs "hoses" ("hoses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hoses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hotter_jotter_2449',
    name: 'hotter vs jotter',
    description: 'Commonly confused words: hotter and jotter',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhotter\b/gi,
    message: 'Check: "hotter" ("hotter") vs "jotter" ("jotter"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jotter';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hotting_jotting_2450',
    name: 'hotting vs jotting',
    description: 'Commonly confused words: hotting and jotting',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhotting\b/gi,
    message: 'Check: "hotting" ("hotting") vs "jotting" ("jotting"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jotting';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_housed_houses_2451',
    name: 'housed vs houses',
    description: 'Commonly confused words: housed and houses',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhoused\b/gi,
    message: 'Check: "housed" ("housed") vs "houses" ("houses"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'houses';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hove_hive_2452',
    name: 'hove vs hive',
    description: 'Commonly confused words: hove and hive',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhove\b/gi,
    message: 'Check: "hove" ("hove") vs "hive" ("hive"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hive';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hovels_novels_2453',
    name: 'hovels vs novels',
    description: 'Commonly confused words: hovels and novels',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhovels\b/gi,
    message: 'Check: "hovels" ("hovels") vs "novels" ("novels"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'novels';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_howls_jowls_2454',
    name: 'howls vs jowls',
    description: 'Commonly confused words: howls and jowls',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhowls\b/gi,
    message: 'Check: "howls" ("howls") vs "jowls" ("jowls"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jowls';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hubs_hugs_2455',
    name: 'hubs vs hugs',
    description: 'Commonly confused words: hubs and hugs',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhubs\b/gi,
    message: 'Check: "hubs" ("hubs") vs "hugs" ("hugs"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hugs';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hued_hues_2456',
    name: 'hued vs hues',
    description: 'Commonly confused words: hued and hues',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhued\b/gi,
    message: 'Check: "hued" ("hued") vs "hues" ("hues"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hues';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hues_hies_2457',
    name: 'hues vs hies',
    description: 'Commonly confused words: hues and hies',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhues\b/gi,
    message: 'Check: "hues" ("hues") vs "hies" ("hies"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hies';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hugged_jugged_2458',
    name: 'hugged vs jugged',
    description: 'Commonly confused words: hugged and jugged',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhugged\b/gi,
    message: 'Check: "hugged" ("hugged") vs "jugged" ("jugged"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jugged';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hulking_hulling_2459',
    name: 'hulking vs hulling',
    description: 'Commonly confused words: hulking and hulling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhulking\b/gi,
    message: 'Check: "hulking" ("hulking") vs "hulling" ("hulling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hulling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hull_gull_2460',
    name: 'hull vs gull',
    description: 'Commonly confused words: hull and gull',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhull\b/gi,
    message: 'Check: "hull" ("hull") vs "gull" ("gull"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gull';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hulls_gulls_2461',
    name: 'hulls vs gulls',
    description: 'Commonly confused words: hulls and gulls',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhulls\b/gi,
    message: 'Check: "hulls" ("hulls") vs "gulls" ("gulls"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'gulls';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_humanized_humanizes_2462',
    name: 'humanized vs humanizes',
    description: 'Commonly confused words: humanized and humanizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhumanized\b/gi,
    message: 'Check: "humanized" ("humanized") vs "humanizes" ("humanizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'humanizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_humbled_humbles_2463',
    name: 'humbled vs humbles',
    description: 'Commonly confused words: humbled and humbles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhumbled\b/gi,
    message: 'Check: "humbled" ("humbled") vs "humbles" ("humbles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'humbles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_humbled_jumbled_2464',
    name: 'humbled vs jumbled',
    description: 'Commonly confused words: humbled and jumbled',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhumbled\b/gi,
    message: 'Check: "humbled" ("humbled") vs "jumbled" ("jumbled"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jumbled';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_humbling_jumbling_2465',
    name: 'humbling vs jumbling',
    description: 'Commonly confused words: humbling and jumbling',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhumbling\b/gi,
    message: 'Check: "humbling" ("humbling") vs "jumbling" ("jumbling"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jumbling';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_humeral_numeral_2466',
    name: 'humeral vs numeral',
    description: 'Commonly confused words: humeral and numeral',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhumeral\b/gi,
    message: 'Check: "humeral" ("humeral") vs "numeral" ("numeral"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'numeral';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_humiliated_humiliates_2467',
    name: 'humiliated vs humiliates',
    description: 'Commonly confused words: humiliated and humiliates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhumiliated\b/gi,
    message: 'Check: "humiliated" ("humiliated") vs "humiliates" ("humiliates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'humiliates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_humped_jumped_2468',
    name: 'humped vs jumped',
    description: 'Commonly confused words: humped and jumped',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhumped\b/gi,
    message: 'Check: "humped" ("humped") vs "jumped" ("jumped"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'jumped';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hunkers_junkers_2469',
    name: 'hunkers vs junkers',
    description: 'Commonly confused words: hunkers and junkers',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhunkers\b/gi,
    message: 'Check: "hunkers" ("hunkers") vs "junkers" ("junkers"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'junkers';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hurdled_hurdles_2470',
    name: 'hurdled vs hurdles',
    description: 'Commonly confused words: hurdled and hurdles',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhurdled\b/gi,
    message: 'Check: "hurdled" ("hurdled") vs "hurdles" ("hurdles"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hurdles';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hurts_yurts_2471',
    name: 'hurts vs yurts',
    description: 'Commonly confused words: hurts and yurts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhurts\b/gi,
    message: 'Check: "hurts" ("hurts") vs "yurts" ("yurts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'yurts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hushed_hushes_2472',
    name: 'hushed vs hushes',
    description: 'Commonly confused words: hushed and hushes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhushed\b/gi,
    message: 'Check: "hushed" ("hushed") vs "hushes" ("hushes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hushes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_huts_guts_2473',
    name: 'huts vs guts',
    description: 'Commonly confused words: huts and guts',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhuts\b/gi,
    message: 'Check: "huts" ("huts") vs "guts" ("guts"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'guts';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_huts_hits_2474',
    name: 'huts vs hits',
    description: 'Commonly confused words: huts and hits',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhuts\b/gi,
    message: 'Check: "huts" ("huts") vs "hits" ("hits"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hits';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hwy_hey_2475',
    name: 'hwy vs hey',
    description: 'Commonly confused words: hwy and hey',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhwy\b/gi,
    message: 'Check: "hwy" ("hwy") vs "hey" ("hey"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hey';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hybridized_hybridizes_2476',
    name: 'hybridized vs hybridizes',
    description: 'Commonly confused words: hybridized and hybridizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhybridized\b/gi,
    message: 'Check: "hybridized" ("hybridized") vs "hybridizes" ("hybridizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hybridizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hydrated_hydrates_2477',
    name: 'hydrated vs hydrates',
    description: 'Commonly confused words: hydrated and hydrates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhydrated\b/gi,
    message: 'Check: "hydrated" ("hydrated") vs "hydrates" ("hydrates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hydrates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hydrolyzed_hydrolyzes_2478',
    name: 'hydrolyzed vs hydrolyzes',
    description: 'Commonly confused words: hydrolyzed and hydrolyzes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhydrolyzed\b/gi,
    message: 'Check: "hydrolyzed" ("hydrolyzed") vs "hydrolyzes" ("hydrolyzes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hydrolyzes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hydroplaned_hydroplanes_2479',
    name: 'hydroplaned vs hydroplanes',
    description: 'Commonly confused words: hydroplaned and hydroplanes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhydroplaned\b/gi,
    message: 'Check: "hydroplaned" ("hydroplaned") vs "hydroplanes" ("hydroplanes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hydroplanes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hyped_hypes_2480',
    name: 'hyped vs hypes',
    description: 'Commonly confused words: hyped and hypes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhyped\b/gi,
    message: 'Check: "hyped" ("hyped") vs "hypes" ("hypes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hypes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hyphenated_hyphenates_2481',
    name: 'hyphenated vs hyphenates',
    description: 'Commonly confused words: hyphenated and hyphenates',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhyphenated\b/gi,
    message: 'Check: "hyphenated" ("hyphenated") vs "hyphenates" ("hyphenates"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hyphenates';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  },
  {
    id: 'confusion_hypothesized_hypothesizes_2482',
    name: 'hypothesized vs hypothesizes',
    description: 'Commonly confused words: hypothesized and hypothesizes',
    category: 'confusion',
    severity: 'warning' as const,
    pattern: /\bhypothesized\b/gi,
    message: 'Check: "hypothesized" ("hypothesized") vs "hypothesizes" ("hypothesizes"). These words are often confused.',
    suggestions: (match: RegExpMatchArray) => {
      // Preserve case
      const orig = match[0];
      let replacement = 'hypothesizes';
      if (orig[0] === orig[0].toUpperCase()) {
        replacement = replacement[0].toUpperCase() + replacement.slice(1);
      }
      if (orig === orig.toUpperCase()) {
        replacement = replacement.toUpperCase();
      }
      return [replacement];
    },
  }
];
